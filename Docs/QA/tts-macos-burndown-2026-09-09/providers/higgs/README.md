# Higgs Audio V2 / CPU float32

Pinned source: boson-ai/higgs-audio@05a145bb490501b534563bf51bf2f7aa2326b271. Pinned generation model 10840182ca4ad5d9d9113b60b9bb3c1ef1ba3f84, tokenizer 9d4988fbd4ad07b4cac3a5fa462741a41810dbec, and Hubert d7a7039bd0d93005f6532853db1934722d9a5e02 are in the [asset manifest](provenance/asset-manifest.json). No reference audio is used; the built-in professional_female preset is a text description. Runtime settings enter through the existing HiggsSettings section, with CPU, float32, cloning disabled, and 512 max_new_tokens. [Initial dependencies](provenance/installed-requirements-with-app.txt) and [retry dependencies](provenance/installed-requirements-with-app-056.txt) are explicit.

[Run1](runs/live-wav-01/evidence.json) loaded in 18.4 s and made 126/132 real model forwards for Lab/Console; [run2](runs/live-wav-02/evidence.json) loaded in 11.8 s and made 109/112 forwards. All 4 saved complete generated WAVs are 24000 Hz mono and [transcribe exactly](provenance/partial-generation-content.json). That content receipt includes the complete Console output while retaining its failed runtime status and original phase outcome.

Lab completes real file playback. Console callbacks fill the complete PCM frame count with the exact PCM hash and lifecycle reaches playing→stopped, but only SinkStarted arrives. SinkDrained misses the 10 s deadline, and the interpreter remains blocked after tracked application owners reach zero. No Python callback exception/SinkFailed event is recorded. [First native sample](runs/live-wav-01/worker-sample.txt) and [second native sample](runs/live-wav-02/worker-sample.txt) show PortAudio Pa_Terminate/FinishStoppingStream/AudioOutputUnitStop and CoreAudio startStopCallback/AudioUnitGetProperty blocked. Cancellation/successor were never reached.

One bounded retry upgraded isolated sounddevice 0.5.5 to 0.5.6; the failure persisted. The [metadata-only comparison](provenance/portaudio-comparison.json) proves root and dedicated environments load identical CPython 3.12.11, sounddevice 0.5.6, and PortAudio V19.7.0-devel binary SHA256 f48bedb7f4f79c0ca050776afe943458555db7341e0dae00511d109ccde907c0. It does not prove the package alignment is a fix.

Both owned parents performed bounded process-group cleanup, worker exits -15; both final receipts remain failed_cleanup and cleanup_joined=false. Original pre-interruption receipts and exact manual actions are retained beside each sample. The source-level zero-owner claim is preserved separately, not accepted as native teardown completion. [All 16 recorded provider processes were absent after ASR](provenance/provider-process-release.json). Root is investigating an upstream native PortAudio correction separately. These original attempts remain failures regardless of any later control.
