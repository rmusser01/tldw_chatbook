# Higgs V2 with the isolated PortAudio candidate

The new [runtime receipt](runs/live-wav-portaudio-02/evidence.json) passes real Lab playback, Console warmup, active-generation Stop and successor playback. Both Console requests receive the real SinkDrained event. [Whisper-small](runs/live-wav-portaudio-02/content.json) transcribes all 3 complete successful clips exactly with all three anchors. The cancelled request remains excluded from success audio because it produced no playback. Real forward counts are 118 / 101 / 512 / 132 for Lab, warmup, cancel, and successor.

Stop begins during a real model generate call and returns 96.400 seconds later, after its 512 forwards finish. Native ownership and the model lease remain retained until that call exits; no cancelled audio escapes, and the successor enters after settlement. This is correct joined cancellation with a long CPU delay. The read-only [stopping-criterion contract](STOPPING-CRITERIA-CONTRACT.md) identifies an underlying model API that the selected serving engine does not expose; no new cancellation implementation is claimed.

The actual Pa_GetVersion function image resolves through dladdr to the isolated libportaudio.19.8.dylib with SHA256 `48d9fcaff582ff65c8a51d67570bf69ce900d51278841d563e19b7ef73746586`. The gate runs before a model or stream opens. See the separate [upstream candidate/build and sink controls](../../portaudio/README.md). The bundled runtime library remains untouched. This is an isolated, unmerged upstream candidate result; it does not establish that the default installed runtime is fixed. The [original two failed-cleanup runs](README.md) remain failures.

The first candidate [attempt](runs/live-wav-portaudio-01/evidence.json) exited 1 before model loading: default-sandbox CoreAudio enumeration exposed no output device. The [default-sandbox](provenance/device-enumeration-default-sandbox.json) and [escalated](provenance/device-enumeration-escalated.json) import-only probes distinguish that execution-context limitation. The escalated rerun used a fresh directory and received MacBook Pro Speakers.

[Ownership release](runs/live-wav-portaudio-02/ownership-release.json) records worker 31849 and player 32519 absent, actual supervisor/worker exit 0, no forced exit, joined cleanup, and unchanged package source. The final [10-PID check](provenance/candidate-provider-process-release.json) also includes AllTalk and all separately run ASR children. No audio, private profile, model, or binary is copied into this QA package.
