# AllTalk V2 / VITS p225

Pinned server: erew123/alltalk_tts@f16117e95b540e9bbbd8247b49ca6c6b1350b172. Engine coqui-tts 0.24.3, Torch/torchaudio 2.2.1, native ARM CPU float32. The public VCTK VITS model contains 109 speakers; the selected upstream OpenAI aliases map to p225. Exact [asset manifest](provenance/asset-manifest.json), [installed versions](provenance/installed-requirements.txt), and independent [OpenAI voice schema probe](provenance/voice-contract-probe.json) are retained.

The final [WAV run](runs/live-wav-02/evidence.json) and [MP3 run](runs/live-mp3-01/evidence.json) pass the runtime checks. WAV is 22050 Hz mono PCM16; MP3 is actual 44100 Hz stereo MP3 as verified with ffprobe. All successful clips complete the real file player or sink. WAV exercises actual native generation overlap with Stop, successor, owned-server stop and real connection failure, then restart and successful retry.

Client Stop closes HTTP/application ownership promptly; external VITS generation already in progress continues until its actual recorded exit. The successor waits for that observed exit. This is not client hard preemption of native model execution. The original [bind-probe failure](runs/live-wav-01/evidence.json) occurred after successful generation/playback and expected server failure: the task-only restart probe omitted SO_REUSEADDR and hit TCP TIME_WAIT. The probe was corrected and the full sequence repeated. The failed receipt remains a failure.

Content is still review-required: [Whisper-small WAV](runs/live-wav-02/content.json) is 2/4 exact and [base.en WAV](runs/live-wav-02/content-base-en.json) 3/4; [small MP3](runs/live-mp3-01/content.json) is 0/3 and [base.en MP3](runs/live-mp3-01/content-base-en.json) 1/3. Both recognizers call the WAV successor ending “amber semis” instead of “amber sunrise”; MP3 Lab/warmup retain material end-phrase differences in both passes. Full lexical differences and every complete clip remain in the denominator; no retakes or transcript substitutions promote p225 to a content pass. No human/acoustic-loopback assertion is made.
