# tldw_chatbook Data Compatibility Map

## Purpose

Track how local `tldw_chatbook` entities relate to `tldw_server` models and formats so future interoperability and sync work can build on stable assumptions.

## Entity Map

| Entity | Local Source | Server Source | ID Strategy | Timestamp Strategy | Metadata / Versioning | Delete Semantics | Import/Export Format | Sync-Safe Later? | Notes |
|---|---|---|---|---|---|---|---|---|---|
| Conversations | `DB/ChaChaNotes_DB.py`, `Chat/chat_conversation_service.py`, `Chat/chat_persistence_service.py`, `UI/Screens/chat_screen.py`, `Widgets/Chat_Widgets/chat_tab_container.py`, `UI/Chat_Window_Enhanced.py` | `chat.py`, `messages.py`, `chat_loop.py`, `chat_workflows.py` | `conversation_id` is now a string-based contract end-to-end locally and in the client/service seam; local storage persists it as a `TEXT PRIMARY KEY` instead of remapping it to a separate UI-only identifier | Local records and server payloads are normalized into the same session/state seam for created/updated metadata | Local-first conversation state now preserves assistant, character, scope, persona-memory, and title metadata without lossy flattening across DB, service, and UI layers | Local-first persistence remains authoritative for standalone use; remote delete/sync policy is still deferred to the later sync vertical | Local DB rows and server REST payloads now share one adapter/service seam, while chatbook export remains a separate packaging path | Yes for future sync work once dual-write/sync policy is defined | Implemented in the chat/conversations vertical: local metadata, local search/tree helpers, state alignment, server conversation client contracts, and tab/session restoration now share one normalized seam without changing the existing chat UI. |
| Messages | `DB/ChaChaNotes_DB.py`, `Chat/chat_persistence_service.py`, `Widgets/Chat_Widgets/chat_message_enhanced.py` | `messages.py`, `character_messages.py`, `chat.py` | Stable message IDs and parent/child relationships are preserved through the local persistence seam when present | Message created/updated metadata is preserved through local save/load flows where available | Local persistence now retains role/content metadata, attachments, variants, and assistant-context fields instead of flattening them away | Local cleanup removes stale linear messages during resave while preserving surviving variants; broader delete/sync semantics remain future work | Local DB rows and chatbook-packaged message payloads can now be reconstructed without the earlier fidelity loss | Partially; core message fidelity is aligned locally, but tool-call/tool-result and remote sync semantics still need explicit parity work | Message fidelity work in this vertical closed the biggest local parity gap: resaves preserve variants and metadata, and malformed image payloads now fail soft instead of aborting the save path. |
| Notes | `Notes/Notes_Library.py`, `Notes/sync_engine.py`, `DB/ChaChaNotes_DB.py` | `notes.py`, `workspaces.py`, `notes_graph.py` | TBD | TBD | TBD | TBD | Local note files and DB-backed notes | TBD | Workspace membership and graph edges are likely compatibility pressure points. |
| Characters / Persona Profiles / CCP Runtime | `Character_Chat/Character_Chat_Lib.py`, `Character_Chat/ccv3_parser.py`, `Character_Chat/server_character_persona_service.py`, `Character_Chat/character_persona_scope_service.py`, `UI/CCP_Modules/ccp_character_handler.py`, `UI/CCP_Modules/ccp_persona_handler.py`, `UI/CCP_Modules/ccp_conversation_handler.py`, `UI/Screens/ccp_screen.py`, `DB/ChaChaNotes_DB.py` | `characters_endpoint.py`, `character_chat_sessions.py`, `character_messages.py`, `persona.py` | Server-facing character, persona, conversation, greeting, and preset seams are now string-first. Legacy local numeric character IDs remain an adapter detail and are normalized before launch/history flows. | Runtime ownership now rides on the same conversation contract used by chat/session restore, so local rows and server-shaped payloads preserve the same `runtime_backend` and discovery metadata even when CCP launches back into main chat. | CCP/main-chat state now preserves canonical `assistant_id`, `assistant_kind`, persona/character discovery ownership, and server persona profile fields like `mode`, `system_prompt`, and `version`. Greeting and preset compatibility is intentionally modeled as chat-scoped execution metadata rather than character-catalog metadata. | Local character deletes remain standalone-first. Server persona/profile lifecycle and any later sync or dual-write reconciliation are still deferred. | Local character cards still import/export through card files and local DB flows. Server interoperability now comes through the `tldw_api` client plus mode-aware adapter services; greeting/preset support is exposed as chat execution helpers, not card export content. | Partially; core runtime/discovery parity is now in place, but local persona-profile persistence, local greeting/preset parity, and sync policy remain follow-on work. | Implemented in the character/persona runtime vertical. CCP now has first-class persona browsing/editing, persona-scoped history isolation, and backend-aware launch into main chat. Server-mode greeting selection and preset CRUD are available through the service/handler seam, while fuller chat UI controls remain intentionally deferred. |
| Prompts | `Prompt_Management/Prompts_Interop.py`, `Prompt_Management/server_prompt_adapter.py`, `DB/Prompts_DB.py` | `prompts.py`, `writing.py`, `writing_manuscripts.py` | UUID-backed on both sides; preserve UUIDs and treat numeric DB IDs as implementation details | `last_modified` exists locally and server responses can be normalized into the same modified-time seam in the adapter layer | Local DB now stores `prompt_format`, `prompt_schema_version`, and `prompt_definition` alongside legacy prompt fields; server version/restore semantics are exposed through `tldw_api` client methods and the adapter layer preserves legacy compatibility | Soft delete on both sides | Local DB interop plus CSV/Markdown export remain available; REST parity now exists for prompt preview/create/version listing/restore and structured prompt payload round-trips | Yes for future sync work on the prompt entity itself | Implemented in the first vertical: structured prompt metadata survives local storage, legacy prompts still work, and server-shaped prompt payloads can round-trip without lossy flattening. |
| Chatbooks | `Chatbooks/chatbook_models.py`, `Chatbooks/chatbook_importer.py`, `Chatbooks/chatbook_creator.py`, `Chatbooks/server_chatbook_service.py`, `UI/Wizards/ChatbookCreationWizard.py`, `UI/Wizards/ChatbookImportWizard.py` | `chatbooks.py` | Manifest item IDs plus packaged record IDs; preserve source identifiers and add provenance instead of reminting where possible | Local manifest uses ISO timestamps for `created_at` and `updated_at`; server adds async job timestamps and export/import lifecycle metadata around the package | Local manifest stays versioned (`1.0` today); local UI now tracks server job metadata separately for visibility in the management window | Packaged snapshot semantics, not live delete semantics; conflict resolution remains explicit on import | ZIP + manifest on both sides, wrapped by job-backed REST flows exposed via `tldw_api` and the `ServerChatbookService` helpers | Partially; packaging and job semantics now align, but live sync is still deferred | Implemented in the first vertical: `ChatbooksWindowImproved` is the primary surface, server-backed export/import flows are wired into the wizards, and recent server jobs are visible locally. Current server-backed import support is limited to conversations, notes, and characters. Packages containing prompts, media, or embeddings must currently use the local import path. |
| Media | `DB/Client_Media_DB_v2.py`, `Local_Ingestion/`, `Media/`, `UI/MediaWindow_v2.py`, `UI/MediaIngestWindowRebuilt.py`, `Widgets/Media/media_ingestion_source_panel.py` | `files.py`, `ingestion_sources.py`, `reading.py`, `media/reading_progress.py` | Canonical IDs now follow `<backend>:<entity_kind>:<source_id>`. UI state stores normalized record IDs while backend-specific `source_id` and `backing_media_id` remain seam details. | Local and server payloads are normalized into shared `created_at` / `updated_at` fields in the media seam. | Local reading progress is stored separately from sync/version history; reading-progress operations use raw `backing_media_id`; document-version actions flow through the scope seam and server-mode unsupported actions fail explicitly. | Local media delete/undelete remains available through the seam; server reading-item delete is supported; server undelete and server document-version lifecycle remain explicitly unavailable for now. | Local media DB, local ingestion flows, and server ingestion-source management now share one boundary; server-mode ingest uses ingestion-source CRUD/sync/archive flows instead of the legacy direct remote-processing tab. | Partially; browse/detail/progress/source-management parity is in place, but sync/dual-write policy and broader derived-artifact parity remain follow-on work. | Implemented in the media/files/ingestion vertical. Canonical IDs do not leak into widget IDs, reading progress depends on `backing_media_id`, and server-mode ingest now manages sources/items instead of constructing a direct API-processing client in the UI. |
| Embeddings / Retrieval Artifacts | `Embeddings/`, `Chunking/`, `RAG_Admin/`, `RAG_Search/`, `RAG_Search/simplified/`, `Widgets/chunking_templates_widget.py`, `Widgets/chunking_template_editor.py`, `UI/Embeddings_Management_Window.py` | `rag_unified.py`, `chunking.py`, `chunking_templates.py`, `chat_documents.py`, `embeddings_v5_production_enhanced.py` | Admin surfaces now normalize chunking templates and embedding collections onto canonical record IDs of the form `<backend>:chunking_template:<template_name>` and `<backend>:embedding_collection:<collection_name>`, while backend-native template/collection names remain the durable backing IDs. | Chunking templates preserve `created_at` / `updated_at` when the backend provides them. Collection-admin parity currently treats counts/dimensions as the stable detail surface and does not invent timestamps where local/server payloads do not agree. | Template admin now preserves normalized `template`, `template_json`, `is_builtin`, `tags`, `version`, and backend metadata through one seam. Collection admin now normalizes `count`, `embedding_dimension`, `metadata`, `provider`, and `status`. Local chunking templates still do not persist server-style tags, and broader retrieval/search metadata remains follow-on work. | Template delete and collection delete now route through the active local/server backend. Built-in template edit/delete actions are disabled in the UI. Collection export, cross-backend moves, and sync/dual-write reconciliation remain deferred. | Local template import/export remains available only in local mode. Server-mode template import/export and collection export remain intentionally unsupported in the current admin surfaces. | Partially; chunking-template and embedding-collection admin parity is in place, but broader RAG search/chat-documents parity, embeddings creation/export parity, and sync policy remain follow-on work. | Implemented in the retrieval-admin vertical. `tldw_api` now exposes chunking-template and embedding-collection admin contracts, `RAG_Admin` provides a mode-aware local/server seam, the chunking templates widget/editor are backend-aware, and the embeddings management pane now performs real collection list/detail/delete operations through that seam without changing the broader search UI. |
| Evaluations / Study Artifacts | `Evals/`, `Evaluations_Interop/`, `Study_Interop/`, `UI/Evals/evals_window_v3.py`, `UI/Evals/screens/evaluation_browser.py`, `UI/Study_Window.py`, `DB/ChaChaNotes_DB.py` | `evaluations/evaluations_unified.py`, `evaluations/evaluations_crud.py`, `evaluations/evaluations_datasets.py`, `flashcards.py`, `quizzes.py` | Evaluation definitions, datasets, runs, and local saved targets now normalize onto canonical IDs of the form `<backend>:evaluation:<id>`, `<backend>:evaluation_dataset:<id>`, `<backend>:evaluation_run:<id>`, and `<backend>:evaluation_target:<id>`. Study flashcard decks and cards now normalize onto `<backend>:study_deck:<id>` and `<backend>:study_flashcard:<id>`, while quiz entities normalize onto `<backend>:quiz:<id>`, `<backend>:quiz_question:<id>`, and `<backend>:quiz_attempt:<id>`. Local UUIDs and server IDs remain the durable backing IDs. Review sessions remain ephemeral server-side IDs rather than durable local records. | Local eval rows keep ISO-like strings while server evaluation payloads may expose integer timestamps; the seam normalizes both into shared `created_at` / `updated_at` string fields without inventing fields where the backend does not provide them. Study decks/cards/quizzes/questions now follow the same normalized string timestamp seam where available. Quiz attempt timestamps now round-trip through explicit attempt-history/detail records; broader review-session persistence beyond those explicit study objects remains deferred. | Local eval tasks now preserve server-style metadata in a reserved config payload shim while still exposing legacy `config_format` as normalized metadata. Local run records now preserve normalized `target_model` alongside metrics/progress/config, while server runs continue to expose only the summary-level result surface available from the unified API. Local flashcards now preserve `notes` / `extra` inside metadata JSON, normalized blank-search behavior is explicit instead of inheriting backend quirks, server review responses are merged onto the current candidate because the server review payload is partial, flashcard lifecycle operations now thread normalized `version` values through delete/move/deck-delete calls, and local quiz parity now preserves quiz/question/attempt payloads with local-first UUID storage while keeping workspace scope server-backed only for now. | Evaluation definition delete now routes through the active local/server backend, run cancel normalizes across both sides, and run creation now routes through one seam: local mode resolves saved targets to local model IDs, while server mode forwards explicit `target_model` strings. Study parity now covers flashcard deck/card create-list/review flows plus lifecycle delete/move semantics, local deck delete, server card delete/move routing, server deck delete as an explicit unsupported state in the Study UI, quiz create/list/question authoring/attempt flows, quiz/question delete semantics, quiz attempt-history/detail browsing in the Study UI, and the workspace-scoped Study shell with scope-aware empty/unavailable states plus active review/attempt reset on scope/backend changes; study import/export and remediation conversion remain deferred. | Local eval DB rows and server evaluation REST payloads now share one mode-aware seam, and the eval navigation exposes paired `Tasks`/`Results` browser modes on top of it. Local mode can browse saved targets plus sample-level run detail; server mode keeps target entry string-first and results summary-only. Study flashcards and quizzes now share one local/server seam through `Study_Interop` and the study pane in `UI/Study_Window.py`, including explicit global/workspace scope routing for server-backed workspace study; study/flashcard/quiz import-export mapping is still TBD. | Partially; evaluation browsing/admin parity now covers definitions, datasets, runs, backend-aware run creation, and a safe results browser. Study parity now covers flashcard decks/cards/review plus flashcard lifecycle management, quiz CRUD/question authoring/attempt/delete/history flow, and server-backed workspace Study routing across local/server backends, but server target-catalog discovery, server sample-level evaluation detail parity, remediation/import-export mapping, cross-scope move/sync policy, and later dual-write behavior remain follow-on work. | Implemented across the evaluations foundation/execution verticals plus the first five study verticals. `tldw_api` now exposes evaluation dataset/evaluation/run contracts plus flashcard and quiz study contracts, `Evaluations_Interop` provides local/server seams for definitions, targets, run creation, and run detail, and `Study_Interop` plus screen-local controllers now drive backend-aware flashcard and quiz flows with explicit empty states, lifecycle controls, review invalidation on destructive actions, quiz lifecycle/history controls, workspace-scoped Study routing from Notes, fail-closed local workspace behavior, and no hidden default selection fallback. Quick Test remains intentionally local-first. |
