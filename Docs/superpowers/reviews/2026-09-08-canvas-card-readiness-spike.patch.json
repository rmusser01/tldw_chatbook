{
  "baseline": "8b54cbf884",
  "format": "unified-diff",
  "patch": "diff --git a/Tests/Canvas/browser/canvas_live_chatbook_child.py b/Tests/Canvas/browser/canvas_live_chatbook_child.py\nindex 8269fb6337..2bf9890bef 100644\n--- a/Tests/Canvas/browser/canvas_live_chatbook_child.py\n+++ b/Tests/Canvas/browser/canvas_live_chatbook_child.py\n@@ -6,10 +6,27 @@ import hashlib\n import json\n import os\n import sqlite3\n+import time\n from collections.abc import Mapping\n from pathlib import Path\n from types import SimpleNamespace\n \n+_spike_rows: list[dict] = []\n+\n+\n+def _spike_stage(stage: str, **facts: bool | int) -> None:\n+    \"\"\"Throwaway approved probe: bounded metadata only, no product mutation.\"\"\"\n+    directory = os.environ.get(\"TLDW_CANVAS_TEST_STAGE_DIAGNOSTICS\")\n+    if not directory or len(_spike_rows) >= 64:\n+        return\n+    assert all(type(value) in (bool, int) for value in facts.values())\n+    _spike_rows.append({\"stage\": stage, \"time\": time.monotonic(), **facts})\n+    path = Path(directory) / f\"stage-{os.getpid()}.json\"\n+    path.write_text(json.dumps(_spike_rows), encoding=\"ascii\")\n+\n+\n+_spike_stage(\"imports_started\")\n+\n from textual.widgets import Button\n \n from Tests.UI.app_factory import (\n@@ -26,6 +43,8 @@ from tldw_chatbook.Chat.console_library_destination import (\n from tldw_chatbook.DB.ChaChaNotes_DB import CharactersRAGDB\n from tldw_chatbook.Widgets.Console.console_canvas_card import ConsoleCanvasCard\n \n+_spike_stage(\"imports_complete\")\n+\n \n def _document(version: str) -> str:\n     return (\n@@ -305,6 +324,7 @@ class _ScriptedCanvasGateway:\n \n \n def main() -> None:\n+    _spike_stage(\"main_enter\")\n     diagnostic_root = os.environ.get(\"TLDW_CANVAS_TEST_DB_DIAGNOSTICS\")\n     if diagnostic_root:\n         import faulthandler\n@@ -391,12 +411,15 @@ def main() -> None:\n     app = None\n     database = None\n     try:\n+        _spike_stage(\"app_factory_start\")\n         app = _build_test_app(configured_default=\"chat\")\n+        _spike_stage(\"app_factory_complete\")\n         data_root = Path(os.environ[\"XDG_DATA_HOME\"])\n         data_root.mkdir(parents=True, exist_ok=True)\n         database = CharactersRAGDB(\n             data_root / \"canvas-live-chatbook.sqlite\", \"canvas-live-chatbook\"\n         )\n+        _spike_stage(\"database_complete\")\n         app.chachanotes_db = database\n         # The app factory initially wires services with no DB. Bind the normal\n         # saved-conversation reader to this same owned durable database as well.\n@@ -419,8 +442,28 @@ def main() -> None:\n         app.console_provider_gateway_factory = lambda: gateway\n         recovered_root_revision = None\n \n+        def record_card_state(stage):\n+            screen = app.screen\n+            target = recovered_root_revision or gateway._revision_id\n+            cards = list(screen.query(ConsoleCanvasCard))\n+            matches = [card for card in cards if card.presentation.revision_id == target]\n+            store = getattr(screen, \"_console_chat_store\", None)\n+            active = getattr(store, \"active_session_id\", None)\n+            _spike_stage(\n+                stage,\n+                recovered=recovered_root_revision is not None,\n+                target_set=target is not None,\n+                sync_in_progress=bool(getattr(screen, \"_console_sync_in_progress\", False)),\n+                sync_requested=bool(getattr(screen, \"_console_sync_requested\", False)),\n+                cards=min(len(cards), 32),\n+                matches=min(len(matches), 32),\n+                mounted_matches=min(sum(card.is_mounted for card in matches), 32),\n+                active_matches=min(sum(card.session_id == active for card in matches), 32),\n+            )\n+\n         async def load_saved_conversation():\n             nonlocal recovered_root_revision\n+            _spike_stage(\"f10_enter\")\n             path = data_root / \"canvas-live-chatbook.sqlite\"\n             with sqlite3.connect(f\"file:{path}?mode=ro\", uri=True) as saved:\n                 roots = saved.execute(\n@@ -434,6 +477,7 @@ def main() -> None:\n             loaded = await app.screen._workspace.open_console_workspace_conversation(\n                 conversation_id\n             )\n+            record_card_state(\"f10_load_complete\")\n             (data_root / \"canvas-live-saved-loaded\").write_text(\n                 \"loaded-without-provider\"\n                 if loaded is True and gateway.calls == 0\n@@ -451,6 +495,7 @@ def main() -> None:\n         def reopen_exact_created_card():\n             # Test keyboard adapter presses the real transcript-card button;\n             # routing, selected revision and authority remain production-owned.\n+            record_card_state(\"f12_enter\")\n             target_revision = recovered_root_revision or gateway._revision_id\n             card = next(\n                 card\n@@ -467,6 +512,7 @@ def main() -> None:\n                 pinned = (\n                     exact and not handler._authority.describe_selection(scope).following\n                 )\n+                _spike_stage(\"selection_ack\", pinned=bool(pinned))\n                 if recovered_root_revision is not None:\n                     (data_root / \"canvas-live-restored-provider-calls\").write_text(\n                         str(gateway.calls), encoding=\"ascii\"\n@@ -477,15 +523,19 @@ def main() -> None:\n                 )\n \n             async def observe_open_completion(**kwargs):\n+                _spike_stage(\"production_open_enter\")\n                 screen._open_console_canvas_selection = original_open\n                 result = await original_open(**kwargs)\n+                _spike_stage(\"production_open_return\")\n                 # The real card handler has no further await after this call;\n                 # acknowledge on the next refresh, after its dispatch returns.\n                 app.call_after_refresh(acknowledge_applied_selection)\n                 return result\n \n             screen._open_console_canvas_selection = observe_open_completion\n+            _spike_stage(\"card_button_press\")\n             card.query_one(\"Button\", Button).press()\n+            _spike_stage(\"card_button_return\")\n \n         app.action_canvas_fixture_reopen = reopen_exact_created_card\n         app._bindings.bind(\"f12\", \"canvas_fixture_reopen\", priority=True)\n@@ -520,7 +570,9 @@ def main() -> None:\n \n         app.action_canvas_fixture_focus_ack = acknowledge_composer_focus\n         app._bindings.bind(\"f11\", \"canvas_fixture_focus_ack\", priority=True)\n+        _spike_stage(\"run_enter\")\n         app.run()\n+        _spike_stage(\"run_return\")\n     finally:\n         if database is not None:\n             database.close()\ndiff --git a/Tests/Canvas/browser/test_canvas_served_flow.py b/Tests/Canvas/browser/test_canvas_served_flow.py\nindex 21125a46e2..feed8bf264 100644\n--- a/Tests/Canvas/browser/test_canvas_served_flow.py\n+++ b/Tests/Canvas/browser/test_canvas_served_flow.py\n@@ -23,7 +23,10 @@ from aiohttp.test_utils import TestClient, TestServer\n from playwright.async_api import TimeoutError as PlaywrightTimeoutError\n from playwright.async_api import async_playwright, expect\n \n-from Tests.Canvas.browser.canvas_live_chatbook_child import _ScriptedCanvasGateway\n+from Tests.Canvas.browser.canvas_live_chatbook_child import (\n+    _ScriptedCanvasGateway,\n+    _spike_stage,\n+)\n from Tests.Canvas.browser.canvas_live_harness import (\n     LiveServedStack,\n     ProductRouteRecorder,\n@@ -689,6 +692,7 @@ async def test_actual_chatbook_console_finalizes_canvas_create_and_update(\n ) -> None:\n     \"\"\"Run deterministic provider tool cycles through an actual TldwCli child.\"\"\"\n \n+    _spike_stage(\"parent_test_enter\")\n     access_token = secrets.token_urlsafe(24)\n     if diagrams:\n         monkeypatch.setenv(\"TLDW_CANVAS_TEST_CANDIDATE\", \"1\")\n@@ -701,7 +705,7 @@ async def test_actual_chatbook_console_finalizes_canvas_create_and_update(\n     )\n \n     broker_events = []\n-    diagnostic_name = f\"actual-{interleaving}-{diagrams}\"\n+    diagnostic_name = f\"spike-{interleaving}-{diagrams}\"\n     record_lifecycle = _release_lifecycle_diagnostics(stack, monkeypatch, diagnostic_name)\n     original_request = stack.server._canvas_control_broker.request\n \n@@ -834,12 +838,16 @@ async def test_actual_chatbook_console_finalizes_canvas_create_and_update(\n                 }\n               };\n             })();\"\"\")\n+            _spike_stage(\"parent_login_start\")\n             await _login_live_page(page, origin=stack.origin, access_token=access_token)\n+            _spike_stage(\"parent_login_complete\")\n             await expect(page.locator(\"body\")).to_have_class(re.compile(\"first-byte\"))\n+            _spike_stage(\"parent_first_byte_seen\")\n             await expect(page.locator(\"#terminal\")).to_contain_text(\n                 \"Composer\", timeout=45_000\n             )\n             await expect(page.locator(\"#terminal\")).not_to_contain_text(\"Check online\")\n+            _spike_stage(\"parent_composer_seen\")\n             child_id = await _live_child_for_page(stack.server, page, port=stack.port)\n             await stack.server._canvas_control_broker.wait_connected(\n                 child_id, timeout=10.0\n@@ -991,6 +999,7 @@ async def test_actual_chatbook_console_finalizes_canvas_create_and_update(\n                     break\n                 await asyncio.sleep(0.05)\n             assert browser_id not in stack.server._served_browser_children\n+            _spike_stage(\"parent_reconnect_start\")\n             await page.goto(stack.origin)\n             await expect(page.locator(\"#terminal.-connected\")).to_be_visible(\n                 timeout=15_000\n@@ -1020,6 +1029,7 @@ async def test_actual_chatbook_console_finalizes_canvas_create_and_update(\n                 \"normal saved-conversation load was not acknowledged\"\n             )\n             assert loaded_ack.read_text(encoding=\"ascii\") == \"loaded-without-provider\"\n+            _spike_stage(\"parent_saved_loaded_seen\")\n             (tmp_path / \"test_data\" / \"canvas-live-card-pressed\").unlink()\n             await page.locator(\"#terminal .xterm-helper-textarea\").press(\"F12\")\n             for _ in range(300):\n@@ -1056,7 +1066,20 @@ async def test_actual_chatbook_console_finalizes_canvas_create_and_update(\n             await browser.close()\n     finally:\n         release_read.set()\n-        await stack.aclose()\n+        try:\n+            _spike_stage(\n+                \"parent_cleanup_start\",\n+                services=len(stack.services),\n+                live_children=sum(\n+                    service._process is not None\n+                    and service._process.returncode is None\n+                    for service in stack.services\n+                ),\n+            )\n+            record_lifecycle(\"spike-before-cleanup\")\n+        finally:\n+            await stack.aclose()\n+        _spike_stage(\"parent_cleanup_complete\")\n     assert all(not path.exists() for path in stack.owned_paths)\n     assert not stack.runner.sites\n     assert all(\n"
}
