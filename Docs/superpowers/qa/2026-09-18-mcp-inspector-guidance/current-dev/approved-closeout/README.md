# PR2722 approved closeout qualification

The owner approved the current inspector gallery and continuation. The branch was rebased without conflicts onto dev `5cdc9ddda00a4112ec06cf8b81fd23bd31d9edb7`. Inspector product source remains byte-for-byte identical to approved head `b42368fd9a8bf7803353018b0414d0fb210c9d7f`; this follow-up changes test synchronization and evidence only. Existing ADR-150/161 apply; no new ADR. Current-head CI, accumulated Qodo review and final dev review still gate merge.

## CI diagnosis and repair

[CI run35685471584](https://github.com/rmusser01/tldw_chatbook/actions/runs/35685471584) failed five Workbench Test Tool cases (1,147 main passes; 123 admission passes and one expected failure). The [failed log](tests/2722-failed-ci.log) is retained. All five passed locally without instrumentation. A [diagnostic plugin](tests/2722_click_probe.py) inserted 35ms at Pilot pauses and recorded mouse events/geometry: Run moved from row14 to10 during the mouse sequence, producing `hit:false` and no prepared execution. The built-in case moved from row8 to5. The [delayed run](tests/2722-ci-probe.txt) reproduced one failure; a second redaction case wrongly accepted an empty result because its assertions only checked absence of sensitive strings.

Nineteen existing Run clicks now use a shared helper that waits for scheduled animations, retains the real Pilot mouse dispatch and asserts the hit. No retry, handler bypass or execution expectations were weakened. The redaction case also requires a `Failed` outcome. The [fixed delayed run](tests/2722-ci-settled-clicks.txt) passes all five; the [strengthened redaction cases](tests/2722-redaction-outcome.txt) pass both parameters. The event JSONL contains consecutive diagnostic runs, including an unmodified rerun before the final fixed probe; it is not represented as one run.

[Workbench and guidance](tests/2722-workbench-green.txt): **359 passed**. This run includes the helper; the stronger redaction assertion was separately rerun afterward. [Incoming startup/private-path and runner admission](tests/2722-incoming-tests.txt): **231 passed**. These are 590 distinct targeted cases, not a full suite. Unrelated old pytest temporary-directory cleanup warnings remain in the raw log. All [nine artifact guards](tests/2722-final-preflight.txt) pass. [Static analysis](tests/2722-static-analysis.json) introduces no diagnostics (five existing test-file findings); changed ranges format cleanly. F811 reference line numbers are normalized solely for the baseline comparison.

## Native requalification

All four dark/light, 80×24/170×48 cells and 16 Servers → Audit → Tools → Servers captures pass. [Result](native/result.json) and [lifecycle](lifecycle/lifecycle.json) record normal App.run return, exit0, process absent, lock reacquired, ten healthy private databases, zero conversations/messages, unchanged defaults/sentinels, no network attempts/errors/faulthandler output and matching fourteen source hashes plus runner. The runner uses the real built-in catalog and one synthetic private audit metadata record; no tools execute or servers connect. The disposable launch/profile harness was reconstructed after earlier temporary scripts expired; committed shared preparation and runner code were used.

[Comparison](lifecycle/2722-capture-comparison.json) preserves geometry and styles, normalizing only generated Rich IDs/styles, insignificant XML whitespace and numeric timestamps. All16 inspector regions exactly match the approved captures. Fourteen whole frames exactly match; two dark compact Audit/Tool frames differ only in fractional scrollbar thumb glyphs in the central pane. Those two frames plus light compact restored and dark wide Tool were rendered and manually inspected again; all16 were machine-compared. The approved gallery remains valid and unchanged.

Publication manifests in tests/native/lifecycle retain raw and normalized hashes. Raw private-profile databases are not published. The prior current-dev README and captures remain the approval evidence; this supplement records the later integration and CI repair.
