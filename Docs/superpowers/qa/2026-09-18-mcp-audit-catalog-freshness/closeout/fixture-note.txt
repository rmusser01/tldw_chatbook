Initial modal-before-001 fixture selected modal.children[0], which was Textual internal chrome with zero geometry rather than the content body. No product change followed. The fixture now selects each modal direct Vertical by its class type. The failed fixture was cancelled and quit through real keys; App.run returned0 and the QA wrapper returned1 as expected. Before-002 passed all12 cells and lifecycle before product edits. Failure teardown now pops an open modal before sending the global quit key.
