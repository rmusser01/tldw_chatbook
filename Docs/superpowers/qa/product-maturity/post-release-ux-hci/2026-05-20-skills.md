# Post-Release UX/HCI Walkthrough Evidence: Skills

## Metadata

- Task: `TASK-60.2`
- Screen or workflow: Skills
- Date: 2026-05-20
- Branch: `codex/post-release-screen-functionality-audit`
- App command: textual-web on `127.0.0.1:8871` with isolated HOME/XDG profile
- Evidence method: textual-web/CDP with Playwright browser automation
- Actual screenshot path: `Docs/superpowers/qa/product-maturity/post-release-ux-hci/actual-screenshots/2026-05-20-screen-skills.png`
- Screenshot approval: pending
- Reviewer: pending user approval

## User Goal

Inspect local Agent Skills availability and understand install, validate, and Console attachment paths.

## Steps Attempted

1. Opened Skills in the isolated profile.
2. Reviewed empty local skills directory and inspector actions.

## What Worked

- Screen shows the local skills directory path.
- Columns separate skill library, skill detail, and skill inspector.
- Import and attach buttons are disabled when no local skills are installed.
- Inspector honestly states skill import is not wired in this shell yet.

## What Broke Or Slowed The Workflow

- The primary import path is not wired, so a first-time user cannot add a skill from this screen.
- Attach-to-Console cannot be validated without an installed skill.

## Nielsen Norman Heuristic Findings

- Visibility of system status: empty local skills state is visible.
- Match between system and real world: installed/discover/validate/scripts/references/assets map to the skill spec.
- User control and freedom: disabled actions prevent invalid attach.
- Consistency and standards: follows the destination column shell.
- Error prevention: no dead attach attempt is exposed.
- Recognition rather than recall: local directory path is visible.
- Flexibility and efficiency of use: blocked until import/install path exists.
- Aesthetic and minimalist design: readable.
- Error recognition, diagnosis, and recovery: import-not-wired state is honest, but not actionable.
- Help and documentation: no direct instructions for placing a skill in the directory.

## Keyboard And Focus Findings

- Not deeply validated in this pass.

## Empty Error Setup State Findings

- Empty local skills state is visible, but setup guidance is thin.

## Cross-Screen Handoff Findings

- Skill-to-Console attachment remains blocked until an installed skill exists and belongs to `TASK-60.3`.

## Power-User Repetition Findings

- Shortcuts: global palette remains visible.
- Batch actions: not available in empty state.
- State persistence: not validated.
- Recovery paths: manual local directory path is visible, import is not wired.
- Repeated-use friction: high for first skill import.

## Severity Decisions

| Finding | Severity | Follow-Up Task | Decision |
| --- | --- | --- | --- |
| Skill import is not wired in the shell | P1 | `TASK-60.3` | Must be validated or split into implementation follow-up during workflow pass. |

## Acceptance Decision

- Accepted: no
- Reason: screenshot approval is pending and import/attach workflow is unvalidated.
- Required follow-up before acceptance: user approval plus `TASK-60.3` skill import/attach evidence.
