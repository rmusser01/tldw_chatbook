.......................................................FFFFFFFFFFFFFFFF. [ 70%]
...................FF.......FF                                           [100%]
=================================== FAILURES ===================================
_ test_option_actions_preserve_other_editors_and_visible_focus[checkbox-textual-dark-size0] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x114ed7500>
size = (170, 48), theme = 'textual-dark', action = 'checkbox'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[checkbox-textual-dark-size1] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o1')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x114ed6db0>
size = (80, 24), theme = 'textual-dark', action = 'checkbox'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[checkbox-textual-light-size0] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o2')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x114ed5d30>
size = (170, 48), theme = 'textual-light', action = 'checkbox'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[checkbox-textual-light-size1] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o3')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x114ed6d50>
size = (80, 24), theme = 'textual-light', action = 'checkbox'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[select-textual-dark-size0] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o4')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x114ed6540>
size = (170, 48), theme = 'textual-dark', action = 'select'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[select-textual-dark-size1] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o5')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1094cc8f0>
size = (80, 24), theme = 'textual-dark', action = 'select'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[select-textual-light-size0] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o6')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1153f9670>
size = (170, 48), theme = 'textual-light', action = 'select'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[select-textual-light-size1] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o7')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1153f91f0>
size = (80, 24), theme = 'textual-light', action = 'select'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[reset-textual-dark-size0] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o8')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1153fa510>
size = (170, 48), theme = 'textual-dark', action = 'reset'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[reset-textual-dark-size1] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o9')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1153fb590>
size = (80, 24), theme = 'textual-dark', action = 'reset'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[reset-textual-light-size0] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o10')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1153f8b30>
size = (170, 48), theme = 'textual-light', action = 'reset'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[reset-textual-light-size1] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o11')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1153f91f0>
size = (80, 24), theme = 'textual-light', action = 'reset'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[text-textual-dark-size0] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o12')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x115c6be60>
size = (170, 48), theme = 'textual-dark', action = 'text'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[text-textual-dark-size1] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o13')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x115c6b1d0>
size = (80, 24), theme = 'textual-dark', action = 'text'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[text-textual-light-size0] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o14')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x11574ec90>
size = (170, 48), theme = 'textual-light', action = 'text'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_ test_option_actions_preserve_other_editors_and_visible_focus[text-textual-light-size1] _

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_option_actions_preserve_o15')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x115c68980>
size = (80, 24), theme = 'textual-light', action = 'text'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("size", [(170, 48), (80, 24)])
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    @pytest.mark.parametrize("action", ["checkbox", "select", "reset", "text"])
    async def test_option_actions_preserve_other_editors_and_visible_focus(
        tmp_path, monkeypatch, size, theme, action
    ):
        monkeypatch.delenv("NO_COLOR", raising=False)
        # Availability is UI-only; these journeys never extract or install anything.
        monkeypatch.setattr(canvas_module, "_is_installed", lambda _feature: True)
        sources = tmp_path / "sources"
        sources.mkdir()
        (sources / "notes.txt").write_text("Local preflight fixture")
        (sources / "document.pdf").write_bytes(b"%PDF-1.4\nPreflight only\n")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:37:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
______ test_compact_library_shell_keeps_full_install_reason[textual-dark] ______

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_compact_library_shell_kee0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x114b05730>
theme = 'textual-dark'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    async def test_compact_library_shell_keeps_full_install_reason(
        tmp_path, monkeypatch, theme
    ):
        """The compact shell's toolbar height must not clip this option explanation."""
        monkeypatch.delenv("NO_COLOR", raising=False)
        source = tmp_path / "audio.wav"
        source.write_bytes(b"Audio preflight only")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:395:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_____ test_compact_library_shell_keeps_full_install_reason[textual-light] ______

tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-1316/test_compact_library_shell_kee1')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1153fac00>
theme = 'textual-light'

    @pytest.mark.asyncio
    @pytest.mark.parametrize("theme", ["textual-dark", "textual-light"])
    async def test_compact_library_shell_keeps_full_install_reason(
        tmp_path, monkeypatch, theme
    ):
        """The compact shell's toolbar height must not clip this option explanation."""
        monkeypatch.delenv("NO_COLOR", raising=False)
        source = tmp_path / "audio.wav"
        source.write_bytes(b"Audio preflight only")
>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_journeys.py:395:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
____________ test_forwarded_option_edit_cannot_overtake_later_reset ____________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x116622ea0>

    @pytest.mark.asyncio
    async def test_forwarded_option_edit_cannot_overtake_later_reset(monkeypatch):
        """Screen delivery must reject a real forwarded edit superseded by Reset."""
        from Tests.UI.app_factory import _build_test_app
        from Tests.UI.test_library_shell import (
            LibraryProductionCSSHarness,
            _seed_conversations,
            _wait_for_library_shell,
            _wait_for_selector,
        )

>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_event_order.py:196:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
_______ test_option_dependency_refresh_keeps_later_pending_sibling_edit ________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1171d3e00>

    @pytest.mark.asyncio
    async def test_option_dependency_refresh_keeps_later_pending_sibling_edit(monkeypatch):
        """Delivering two real edits in order must retain the newer live editor."""
        from Tests.UI.app_factory import _build_test_app
        from Tests.UI.test_library_shell import (
            LibraryProductionCSSHarness,
            _seed_conversations,
            _wait_for_library_shell,
            _wait_for_selector,
        )

>       app = _build_test_app()
              ^^^^^^^^^^^^^^^^^

Tests/UI/test_library_ingest_option_event_order.py:271:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
Tests/UI/app_factory.py:321: in _build_test_app
    fake_app_config = build_test_app_config(
Tests/UI/app_factory.py:112: in build_test_app_config
    config = load_settings()
             ^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:419: in wrapped
    with operation(source, target=target):
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/config_participants.py:352: in operation
    with raw._scope(source, route, writing=True, selected_read=target) as active:
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:611: in _scope
    participant = _raw_participant(source)
                  ^^^^^^^^^^^^^^^^^^^^^^^^
tldw_chatbook/Backup_Recovery/raw_participants.py:269: in _raw_participant
    state = _participant_state(participant)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

participant = <tldw_chatbook.Backup_Recovery.raw_participants._RawParticipant object at 0x10b53e0c0>

    def _participant_state(participant):
        if type(participant) is not _RawParticipant or participant not in _participants:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        state = _participants[participant]
        source = state.source()
        if source is None or type(source) is not state.source_type:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if _source_participants.get(source) is not participant:
            raise bootstrap.RecoveryRequired("raw_participant_not_installed")
        if state.owner.startswith("mcp."):
            bound = mcp_sources.binding(source)
            if bound is None or not bound[2] or bound[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "chat.dictionaries":
            binding = dictionary_files.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner in {"personas", "chat.dictionary_history", "chat.rag_context"}:
            binding = chat_sources.binding(source)
            if binding is None or binding[1] != state.selected:
                raise bootstrap.RecoveryRequired("raw_source_selection_changed")
        if state.owner == "config":
            binding = config_files.binding(source)
            if binding is None or binding[1] != state.selected:
>               raise bootstrap.RecoveryRequired("raw_source_selection_changed")
E               tldw_chatbook.Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed

tldw_chatbook/Backup_Recovery/raw_participants.py:127: RecoveryRequired
============================= slowest 25 durations =============================
6.34s call     Tests/UI/test_library_ingest_structural.py::test_the_open_fold_survives_a_registry_tick_in_the_shipped_screen
6.18s call     Tests/UI/test_library_ingest_structural.py::test_the_fold_pays_for_itself_in_the_shipped_screen
5.79s call     Tests/UI/test_library_ingest_structural.py::test_start_and_forecast_are_visible_without_scrolling_at_52_rows
5.19s call     Tests/UI/test_library_ingest_structural.py::test_submit_brings_the_queue_heading_into_view
3.07s call     Tests/UI/test_library_ingest_option_journeys.py::test_option_labels_and_disabled_reasons_fit_compact_panel[textual-light-unavailable]
2.80s call     Tests/UI/test_library_ingest_structural.py::test_invalid_saved_stt_provider_is_visible_and_can_be_reset_to_auto
2.77s call     Tests/UI/test_library_ingest_option_journeys.py::test_option_labels_and_disabled_reasons_fit_compact_panel[textual-light-available]
2.74s call     Tests/UI/test_library_ingest_option_journeys.py::test_option_labels_and_disabled_reasons_fit_compact_panel[textual-dark-available]
2.71s call     Tests/UI/test_library_ingest_option_journeys.py::test_option_labels_and_disabled_reasons_fit_compact_panel[textual-dark-unavailable]
2.34s call     Tests/UI/test_library_ingest_structural.py::test_every_canvas_focusable_changes_at_the_glyph_level_on_focus
2.01s call     Tests/Utils/test_install_clipboard_native.py::test_timed_out_clipboard_descendant_cannot_overwrite_a_later_copy
1.50s call     Tests/Local_Ingestion/test_youtube_stt_selection.py::test_youtube_job_reaches_selected_transcription_runner[default-faster-whisper]
1.48s call     Tests/Local_Ingestion/test_youtube_stt_selection.py::test_youtube_job_reaches_selected_transcription_runner[faster-whisper-faster-whisper]
1.47s call     Tests/Local_Ingestion/test_youtube_stt_selection.py::test_youtube_job_reaches_selected_transcription_runner[parakeet-onnx-parakeet-onnx]
1.46s call     Tests/Local_Ingestion/test_youtube_stt_selection.py::test_youtube_job_reaches_selected_transcription_runner[transcribe-cpp-transcribe-cpp]

(10 durations < 1s hidden.)
=========================== short test summary info ============================
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[checkbox-textual-dark-size0]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[checkbox-textual-dark-size1]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[checkbox-textual-light-size0]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[checkbox-textual-light-size1]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[select-textual-dark-size0]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[select-textual-dark-size1]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[select-textual-light-size0]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[select-textual-light-size1]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[reset-textual-dark-size0]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[reset-textual-dark-size1]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[reset-textual-light-size0]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[reset-textual-light-size1]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[text-textual-dark-size0]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[text-textual-dark-size1]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[text-textual-light-size0]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_option_actions_preserve_other_editors_and_visible_focus[text-textual-light-size1]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_compact_library_shell_keeps_full_install_reason[textual-dark]
FAILED Tests/UI/test_library_ingest_option_journeys.py::test_compact_library_shell_keeps_full_install_reason[textual-light]
FAILED Tests/UI/test_library_ingest_option_event_order.py::test_forwarded_option_edit_cannot_overtake_later_reset
FAILED Tests/UI/test_library_ingest_option_event_order.py::test_option_dependency_refresh_keeps_later_pending_sibling_edit
20 failed, 82 passed in 70.56s (0:01:10)
