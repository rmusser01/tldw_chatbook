tldw_chatbook/Widgets/base_components.py (file removed by the component-pattern migration)
  logger.bind(module="base_components")
  logger.warning(f"Error updating collapsed state: {e}")
  logger.warning(f"Could not find widget for field {field.name}: {e}")
tldw_chatbook/Widgets/detailed_progress.py (file removed by the component-pattern migration)
  logger.bind(module="detailed_progress")
  logger.error(f"Error updating stage display: {e}")
  logger.error(f"Error updating current item: {e}")
  logger.error(f"Error updating progress bars: {e}")
  logger.error(f"Error updating metrics: {e}")
tldw_chatbook/Widgets/enhanced_sidebar.py (file removed by the component-pattern migration)
  logger.error(f"Error loading section content: {e}")
tldw_chatbook/Widgets/loading_states.py (file removed by the component-pattern migration)
  logger.debug(f"Loading completed in {elapsed:.2f}s")
  logger.error(f"Loading failed: {e}")
tldw_chatbook/Widgets/toast_notification.py (file removed by the component-pattern migration)
  logger.bind(module="toast_notification")
  logger.info(f"Showing toast: {message} ({severity})")
