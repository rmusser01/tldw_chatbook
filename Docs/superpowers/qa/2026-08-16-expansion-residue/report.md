# TASK-16688 AC#5 -- continuation walk over a long document

Generated by `continuation_walk.py` (this directory). No TUI, no LLM, no
network, no embeddings: one seeded note, the production
`ExpandDocumentTool`, and `next_offset` followed to exhaustion.

**Verdict: PASS** (4 calls, 100.0% coverage).

## Why this run exists

TASK-16174's Phase E oracle run expanded 8 documents of 289-4,924 chars
against a 8000-char budget, so all 8 recorded
`expand_truncated: false` and the continuation half of the contract was
never exercised outside unit tests (finding 16). This walk is that
exercise: a 25,228-char note, expanded at the DEFAULT
budget and then followed.

## Corpus and profile

| item | value |
|---|---|
| seam | `note` (`add_note` -> `get_note_by_id`, the production writer/reader pair) |
| note id | `6322165f-f8cf-4500-9356-457f3be836c9` |
| document chars | 25,228 (> 20,000 by design) |
| budget | 8000 (`DEFAULT_MAX_CHARS`, unchanged -- no `max_chars` passed) |
| scratch data_dir | `/private/var/folders/p_/x47tgtn57cv43r7yxxn40tyh0000gn/T/task16688-continuation-walk-r3u7kecb/profile/data/walk16688` |
| real config sha256 before / after | `42e2f42de95915f5` / `42e2f42de95915f5` |

The document carries two unique markers -- `HEADWINDOW-...` in the first
budget, `TAILWINDOW-...` in the last -- built from a vocabulary the filler
never uses, so "which window contains which marker" is a real reading.

## The walk

| call | window start | window end | chars | truncated | next_offset | marker |
|---|---|---|---|---|---|---|
| 1 | 0 | 8000 | 8000 | true | 8000 | HEAD |
| 2 | 8000 | 16000 | 8000 | true | 16000 |  |
| 3 | 16000 | 24000 | 8000 | true | 24000 |  |
| 4 | 24000 | 25228 | 1228 | true | None | TAIL |

Coverage: **25,228 of 25,228 chars
(100.0%)** across **4 calls** -- the
concatenation of the windows, compared against the stored note body with
`==`, not a length check.

`truncated` reads `true` on the LAST call too, and that is the tool's
contract rather than an unfinished walk: it means "this window is not the
whole document" (`start > 0 or end < total`), so a payload can never be
mistaken for a complete read. The signal that the walk is DONE is
`next_offset: None`.

## Checks

| check | reading | verdict |
|---|---|---|
| document exceeds the default budget | 25228 chars vs budget 8000 | **PASS** |
| first call reports truncated + a next_offset | truncated=True, next_offset=8000 | **PASS** |
| walk terminates (final next_offset is None) | last next_offset=None | **PASS** |
| windows are contiguous (no gap, no re-served overlap) | contiguous=True, starts_at_zero=True, ends_at_total=True | **PASS** |
| concatenated windows reassemble the document byte-for-byte | 25228 / 25228 chars (100.0%) | **PASS** |
| total_size describes the whole document | reported 25228 | **PASS** |
| head marker appears in exactly one window (the first) | windows [1] | **PASS** |
| tail marker appears in exactly one window (the last) | windows [4] | **PASS** |

## What this does and does not show

It shows the walk terminates, covers the document exactly once, and reports
its own incompleteness at every step but the last. It does NOT show an
agent choosing to continue -- that is a model behaviour, and the Phase E
oracle run is the instrument for it. The `chunk_start`-anchored window (the
other way into a long document) is TASK-16588's probe, which planted a
marker past this same budget and got 22/22 anchored windows containing it
against 0/22 head windows.

## Reproduce

```
.venv/bin/python Docs/superpowers/qa/2026-08-16-expansion-residue/continuation_walk.py
```

Raw walk data:

```json
{
  "budget": 8000,
  "document_chars": 25228,
  "total_size_reported": 25228,
  "calls": 4,
  "windows": [
    {
      "call": 1,
      "start": 0,
      "end": 8000,
      "chars": 8000,
      "truncated": true,
      "next_offset": 8000,
      "head_marker": true,
      "tail_marker": false
    },
    {
      "call": 2,
      "start": 8000,
      "end": 16000,
      "chars": 8000,
      "truncated": true,
      "next_offset": 16000,
      "head_marker": false,
      "tail_marker": false
    },
    {
      "call": 3,
      "start": 16000,
      "end": 24000,
      "chars": 8000,
      "truncated": true,
      "next_offset": 24000,
      "head_marker": false,
      "tail_marker": false
    },
    {
      "call": 4,
      "start": 24000,
      "end": 25228,
      "chars": 1228,
      "truncated": true,
      "next_offset": null,
      "head_marker": false,
      "tail_marker": true
    }
  ],
  "first_call_truncated": true,
  "first_next_offset": 8000,
  "last_next_offset": null,
  "starts_at_zero": true,
  "ends_at_total": true,
  "contiguous": true,
  "reassembles": true,
  "coverage_chars": 25228,
  "coverage_pct": 100.0,
  "head_marker_windows": [
    1
  ],
  "tail_marker_windows": [
    4
  ]
}
```
