FFFFFFFF..FFFFFF..FFFFFF                                                 [100%]
=================================== FAILURES ===================================
__ test_obsolete_permission_controls_cannot_target_a_replacement[same-remove] __

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[same-remove]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'remove', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10c438200>, 'request': <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[same-remove]>>, 'transition': 'same'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[same-remove]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr0/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr0/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr0/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr0/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr0/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           __ test_obsolete_permission_controls_cannot_target_a_replacement[same-remove] __
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[same-remove]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10e15baa0>
E           action = 'remove', transition = 'same'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [RemoveArgRuleRequested()] == []
E           E
E           E             Left contains one more item: RemoveArgRuleRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:50:56.081 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:50:56.125 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:50:56.133 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:50:56.134 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:50:56.333 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:50:56.717 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr0/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[same-remove]
E           1 failed in 2.22s

Tests/private_profile.py:84: Failed
---------------------------- Captured stderr setup -----------------------------
2026-09-19 08:50:54.496 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
2026-09-19 08:50:54.571 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
2026-09-19 08:50:54.587 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
2026-09-19 08:50:54.594 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
_ test_obsolete_permission_controls_cannot_target_a_replacement[same-reallow] __

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[same-reallow]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'reallow', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10c4383b0>, 'request': <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[same-reallow]>>, 'transition': 'same'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[same-reallow]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr1/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr1/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr1/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr1/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr1/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_obsolete_permission_controls_cannot_target_a_replacement[same-reallow] __
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[same-reallow]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10e6fc440>
E           action = 'reallow', transition = 'same'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [ReallowRequested()] == []
E           E
E           E             Left contains one more item: ReallowRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:50:58.529 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:50:58.573 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:50:58.580 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:50:58.582 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:50:58.705 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:50:58.948 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr1/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[same-reallow]
E           1 failed in 1.40s

Tests/private_profile.py:84: Failed
_ test_obsolete_permission_controls_cannot_target_a_replacement[reorder-remove] _

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[reorder-remove]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'remove', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10c438200>, 'request': <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[reorder-remove]>>, 'transition': 'reorder'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[reorder-remove]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr2/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr2/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr2/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr2/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr2/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_obsolete_permission_controls_cannot_target_a_replacement[reorder-remove] _
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[reorder-remove]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10a5b0770>
E           action = 'remove', transition = 'reorder'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [RemoveArgRuleRequested()] == []
E           E
E           E             Left contains one more item: RemoveArgRuleRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:00.624 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:00.663 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:00.670 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:00.671 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:00.790 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:01.025 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr2/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[reorder-remove]
E           1 failed in 1.36s

Tests/private_profile.py:84: Failed
_ test_obsolete_permission_controls_cannot_target_a_replacement[reorder-reallow] _

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[reorder-reallow]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'reallow', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10c43a930>, 'request': <FixtureReque...r <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[reorder-reallow]>>, 'transition': 'reorder'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[reorder-reallow]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr3/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr3/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr3/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr3/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr3/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_obsolete_permission_controls_cannot_target_a_replacement[reorder-reallow] _
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[reorder-reallow]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10c95c560>
E           action = 'reallow', transition = 'reorder'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [ReallowRequested()] == []
E           E
E           E             Left contains one more item: ReallowRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:02.728 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:02.773 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:02.781 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:02.782 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:02.912 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:03.157 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr3/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[reorder-reallow]
E           1 failed in 1.39s

Tests/private_profile.py:84: Failed
__ test_obsolete_permission_controls_cannot_target_a_replacement[tool-remove] __

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[tool-remove]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'remove', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10c4388c0>, 'request': <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[tool-remove]>>, 'transition': 'tool'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[tool-remove]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr4/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr4/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr4/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr4/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr4/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           __ test_obsolete_permission_controls_cannot_target_a_replacement[tool-remove] __
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[tool-remove]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10e9bc680>
E           action = 'remove', transition = 'tool'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [RemoveArgRuleRequested()] == []
E           E
E           E             Left contains one more item: RemoveArgRuleRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:04.773 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:04.811 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:04.817 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:04.818 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:04.936 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:05.179 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr4/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[tool-remove]
E           1 failed in 1.39s

Tests/private_profile.py:84: Failed
_ test_obsolete_permission_controls_cannot_target_a_replacement[tool-reallow] __

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[tool-reallow]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'reallow', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x109992690>, 'request': <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[tool-reallow]>>, 'transition': 'tool'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[tool-reallow]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr5/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr5/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr5/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr5/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr5/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_obsolete_permission_controls_cannot_target_a_replacement[tool-reallow] __
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[tool-reallow]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10cb80a10>
E           action = 'reallow', transition = 'tool'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [ReallowRequested()] == []
E           E
E           E             Left contains one more item: ReallowRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:06.860 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:06.899 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:06.908 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:06.909 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:07.034 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:07.284 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr5/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[tool-reallow]
E           1 failed in 1.47s

Tests/private_profile.py:84: Failed
_ test_obsolete_permission_controls_cannot_target_a_replacement[profile-remove] _

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[profile-remove]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'remove', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10da27950>, 'request': <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[profile-remove]>>, 'transition': 'profile'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[profile-remove]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr6/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr6/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr6/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr6/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr6/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_obsolete_permission_controls_cannot_target_a_replacement[profile-remove] _
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[profile-remove]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10aa75250>
E           action = 'remove', transition = 'profile'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [RemoveArgRuleRequested()] == []
E           E
E           E             Left contains one more item: RemoveArgRuleRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:09.169 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:09.223 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:09.231 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:09.232 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:09.370 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:09.613 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr6/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[profile-remove]
E           1 failed in 1.56s

Tests/private_profile.py:84: Failed
_ test_obsolete_permission_controls_cannot_target_a_replacement[profile-reallow] _

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[profile-reallow]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'reallow', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10da26000>, 'request': <FixtureReque...r <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[profile-reallow]>>, 'transition': 'profile'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[profile-reallow]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr7/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr7/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr7/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr7/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr7/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_obsolete_permission_controls_cannot_target_a_replacement[profile-reallow] _
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[profile-reallow]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10e46c500>
E           action = 'reallow', transition = 'profile'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [ReallowRequested()] == []
E           E
E           E             Left contains one more item: ReallowRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:11.137 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:11.177 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:11.184 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:11.185 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:11.312 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:11.543 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr7/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[profile-reallow]
E           1 failed in 1.31s

Tests/private_profile.py:84: Failed
_ test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-remove] _

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-remove]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'remove', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10da25ee0>, 'request': <FixtureReques...Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-remove]>>, 'transition': 'roundtrip'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-remove]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr10/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr10/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr10/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr10/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr10/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-remove] _
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-remove]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x109f28740>
E           action = 'remove', transition = 'roundtrip'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [RemoveArgRuleRequested()] == []
E           E
E           E             Left contains one more item: RemoveArgRuleRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:16.954 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:16.999 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:17.009 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:17.010 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:17.142 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:17.398 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr10/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-remove]
E           1 failed in 1.58s

Tests/private_profile.py:84: Failed
_ test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-reallow] _

self = <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-reallow]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'reallow', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10da26090>, 'request': <FixtureReque...oroutine test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-reallow]>>, 'transition': 'roundtrip'}
request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-reallow]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr11/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr11/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr11/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr11/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr11/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-reallow] _
E
E           request = <FixtureRequest for <Coroutine test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-reallow]>>
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10cbc0740>
E           action = 'reallow', transition = 'roundtrip'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["same", "reorder", "tool", "profile", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_obsolete_permission_controls_cannot_target_a_replacement(request, monkeypatch, action, transition):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       event = held_press(app.query_one(SELECTORS[action], Button), monkeypatch)
E                       if transition == "clear":
E                           await inspector.show_tool(None)
E                       elif transition == "reorder":
E                           await show(inspector, rules=list(reversed(RULES)))
E                       elif transition == "tool":
E                           await show(inspector, tool=_tool(name="fetch"))
E                       elif transition == "profile":
E                           await show(inspector, context=PermissionProfileContext("other", 8, "c" * 64, 4))
E                       elif transition == "roundtrip":
E                           await show(inspector, tool=_tool(name="fetch"))
E                           await show(inspector)
E                       else:
E                           await show(inspector)
E                       inspector.post_message(event)
E                       await pilot.pause()
E           >           assert actions(app) == []
E           E           assert [ReallowRequested()] == []
E           E
E           E             Left contains one more item: ReallowRequested()
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:69: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:19.173 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:19.214 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:19.223 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:19.224 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:19.346 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:19.592 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_obsolete_permission_contr11/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-reallow]
E           1 failed in 1.47s

Tests/private_profile.py:84: Failed
_ test_live_permission_controls_are_consumed_once_with_the_displayed_target[remove] _

self = <Coroutine test_live_permission_controls_are_consumed_once_with_the_displayed_target[remove]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'remove', 'request': <FixtureRequest for <Coroutine test_live_permission_controls_are_consumed_once_with_the_displayed_target[remove]>>}
request = <FixtureRequest for <Coroutine test_live_permission_controls_are_consumed_once_with_the_displayed_target[remove]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_0/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_0/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_0/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_0/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_0/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_live_permission_controls_are_consumed_once_with_the_displayed_target[remove] _
E
E           request = <FixtureRequest for <Coroutine test_live_permission_controls_are_consumed_once_with_the_displayed_target[remove]>>
E           action = 'remove'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @private_profile_test
E               async def test_live_permission_controls_are_consumed_once_with_the_displayed_target(request, action):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       button = app.query_one(SELECTORS[action], Button)
E                       button.press()
E                       button.press()
E                       await pilot.pause()
E           >           assert len(actions(app)) == 1
E           E           AssertionError: assert 2 == 1
E           E            +  where 2 = len([RemoveArgRuleRequested(), RemoveArgRuleRequested()])
E           E            +    where [RemoveArgRuleRequested(), RemoveArgRuleRequested()] = actions(InspectorApp(title='InspectorApp', classes={'-dark-mode'}, pseudo_classes={'nocolor', 'focus', 'dark'}))
E
E           Tests/UI/test_mcp_permission_action_ownership.py:85: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:21.596 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:21.643 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:21.652 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:21.653 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:21.780 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:22.029 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_0/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_live_permission_controls_are_consumed_once_with_the_displayed_target[remove]
E           1 failed in 1.73s

Tests/private_profile.py:84: Failed
_ test_live_permission_controls_are_consumed_once_with_the_displayed_target[reallow] _

self = <Coroutine test_live_permission_controls_are_consumed_once_with_the_displayed_target[reallow]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'reallow', 'request': <FixtureRequest for <Coroutine test_live_permission_controls_are_consumed_once_with_the_displayed_target[reallow]>>}
request = <FixtureRequest for <Coroutine test_live_permission_controls_are_consumed_once_with_the_displayed_target[reallow]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_1/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_1/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_1/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_1/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_1/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _ test_live_permission_controls_are_consumed_once_with_the_displayed_target[reallow] _
E
E           request = <FixtureRequest for <Coroutine test_live_permission_controls_are_consumed_once_with_the_displayed_target[reallow]>>
E           action = 'reallow'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @private_profile_test
E               async def test_live_permission_controls_are_consumed_once_with_the_displayed_target(request, action):
E                   app = InspectorApp()
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       inspector = app.query_one(MCPInspector)
E                       await show(inspector)
E                       await pilot.pause()
E                       button = app.query_one(SELECTORS[action], Button)
E                       button.press()
E                       button.press()
E                       await pilot.pause()
E           >           assert len(actions(app)) == 1
E           E           AssertionError: assert 2 == 1
E           E            +  where 2 = len([ReallowRequested(), ReallowRequested()])
E           E            +    where [ReallowRequested(), ReallowRequested()] = actions(InspectorApp(title='InspectorApp', classes={'-dark-mode'}, pseudo_classes={'nocolor', 'dark', 'focus'}))
E
E           Tests/UI/test_mcp_permission_action_ownership.py:85: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:23.650 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:23.689 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:23.696 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:23.697 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:23.813 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:24.045 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_live_permission_controls_1/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E
E           (3 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_live_permission_controls_are_consumed_once_with_the_displayed_target[reallow]
E           1 failed in 1.34s

Tests/private_profile.py:84: Failed
____ test_reallow_refuses_a_definition_changed_since_rendering[description] ____

self = <Coroutine test_reallow_refuses_a_definition_changed_since_rendering[description]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'drift': 'description', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10da27b90>, 'request': <FixtureRe...ivate/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit0')}
request = <FixtureRequest for <Coroutine test_reallow_refuses_a_definition_changed_since_rendering[description]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit0/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit0/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit0/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit0/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit0/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           ____ test_reallow_refuses_a_definition_changed_since_rendering[description] ____
E
E           request = <FixtureRequest for <Coroutine test_reallow_refuses_a_definition_changed_since_rendering[description]>>
E           tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit0/private-profile-process/pytest/test_reallow_refuses_a_definit0')
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10a1209e0>
E           drift = 'description'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("drift", ["description", "schema"])
E               @private_profile_test
E               async def test_reallow_refuses_a_definition_changed_since_rendering(request, tmp_path, monkeypatch, drift):
E                   app = PermissionsApp(tmp_path / "permissions.json")
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       workbench, inspector, tool = await open_permissions(app, pilot)
E                       event = held_press(app.query_one(SELECTORS["reallow"], Button), monkeypatch)
E                       if drift == "description":
E                           changed = replace(tool, description="A newly changed definition")
E                           workbench._last_hub_tools = [changed if t.tool_id == tool.tool_id else t for t in workbench._last_hub_tools]
E                       else:
E                           tool.input_schema["description"] = "A newly changed schema"
E           >           before = app.unified_mcp_service.permission_store.snapshot()
E                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           E           AttributeError: 'MCPPermissionStore' object has no attribute 'snapshot'
E
E           Tests/UI/test_mcp_permission_action_ownership.py:130: AttributeError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:25.573 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:25.614 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:25.628 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:25.629 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:25.751 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:25.994 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           ----------------------------- Captured stderr call -----------------------------
E           2026-09-19 08:51:26.506 | WARNING  | tldw_chatbook.UI.MCP_Modules.mcp_workbench:_sync_audit_log_entries:1617 - MCP execution log access failed: 'PermissionsHubService' object has no attribute 'execution_log'
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit0/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E           1.00s call     Tests/UI/test_mcp_permission_action_ownership.py::test_reallow_refuses_a_definition_changed_since_rendering[description]
E
E           (2 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_reallow_refuses_a_definition_changed_since_rendering[description]
E           1 failed in 2.20s

Tests/private_profile.py:84: Failed
______ test_reallow_refuses_a_definition_changed_since_rendering[schema] _______

self = <Coroutine test_reallow_refuses_a_definition_changed_since_rendering[schema]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'drift': 'schema', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10da25fd0>, 'request': <FixtureRequest...ivate/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit1')}
request = <FixtureRequest for <Coroutine test_reallow_refuses_a_definition_changed_since_rendering[schema]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit1/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit1/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit1/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit1/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit1/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           ______ test_reallow_refuses_a_definition_changed_since_rendering[schema] _______
E
E           request = <FixtureRequest for <Coroutine test_reallow_refuses_a_definition_changed_since_rendering[schema]>>
E           tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit1/private-profile-process/pytest/test_reallow_refuses_a_definit0')
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10ddb0290>
E           drift = 'schema'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("drift", ["description", "schema"])
E               @private_profile_test
E               async def test_reallow_refuses_a_definition_changed_since_rendering(request, tmp_path, monkeypatch, drift):
E                   app = PermissionsApp(tmp_path / "permissions.json")
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       workbench, inspector, tool = await open_permissions(app, pilot)
E                       event = held_press(app.query_one(SELECTORS["reallow"], Button), monkeypatch)
E                       if drift == "description":
E                           changed = replace(tool, description="A newly changed definition")
E                           workbench._last_hub_tools = [changed if t.tool_id == tool.tool_id else t for t in workbench._last_hub_tools]
E                       else:
E           >               tool.input_schema["description"] = "A newly changed schema"
E                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           E               TypeError: 'NoneType' object does not support item assignment
E
E           Tests/UI/test_mcp_permission_action_ownership.py:129: TypeError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:28.591 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:28.632 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:28.639 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:28.640 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:28.759 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:29.004 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           ----------------------------- Captured stderr call -----------------------------
E           2026-09-19 08:51:29.531 | WARNING  | tldw_chatbook.UI.MCP_Modules.mcp_workbench:_sync_audit_log_entries:1617 - MCP execution log access failed: 'PermissionsHubService' object has no attribute 'execution_log'
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_reallow_refuses_a_definit1/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E           1.08s call     Tests/UI/test_mcp_permission_action_ownership.py::test_reallow_refuses_a_definition_changed_since_rendering[schema]
E
E           (2 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_reallow_refuses_a_definition_changed_since_rendering[schema]
E           1 failed in 2.36s

Tests/private_profile.py:84: Failed
_____ test_permission_completion_preserves_a_newer_selection[tool-remove] ______

self = <Coroutine test_permission_completion_preserves_a_newer_selection[tool-remove]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'remove', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10daff560>, 'request': <FixtureReques.../var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre0'), ...}
request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[tool-remove]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre0/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre0/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre0/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre0/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre0/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _____ test_permission_completion_preserves_a_newer_selection[tool-remove] ______
E
E           request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[tool-remove]>>
E           tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre0/private-profile-process/pytest/test_permission_completion_pre0')
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10ebc0b90>
E           action = 'remove', transition = 'tool'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["tool", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_permission_completion_preserves_a_newer_selection(request, tmp_path, monkeypatch, action, transition):
E                   app = PermissionsApp(tmp_path / "permissions.json")
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       workbench, inspector, tool = await open_permissions(app, pilot)
E                       entered, release = asyncio.Event(), asyncio.Event()
E                       original = workbench._sync_permissions_mode
E
E                       async def hold_sync(*args, **kwargs):
E                           entered.set()
E                           await release.wait()
E                           await original(*args, **kwargs)
E
E                       monkeypatch.setattr(workbench, "_sync_permissions_mode", hold_sync)
E                       app.query_one(SELECTORS[action], Button).press()
E                       try:
E                           await asyncio.wait_for(entered.wait(), 3)
E                           if transition == "clear":
E                               await inspector.show_tool(None)
E                           else:
E                               await show(inspector, tool=_tool(name="fetch"), context=workbench._tool_policy_profile_context)
E                               if transition == "roundtrip":
E                                   await show(inspector, tool=tool, rules=list(reversed(RULES)), context=workbench._tool_policy_profile_context)
E                           expected_tool = inspector._current_permission_tool
E                           expected_rules = inspector._current_permission_arg_rules.copy()
E                       finally:
E                           release.set()
E                       await pilot.pause()
E           >           assert inspector._current_permission_tool == expected_tool
E           E           AssertionError: assert HubTool(serve...ecutable=True) == HubTool(serve...ecutable=True)
E           E
E           E             Omitting 6 identical items, use -vv to show
E           E             Differing attributes:
E           E             ['name', 'description', 'input_schema']
E           E
E           E             Drill down into differing attribute name:
E           E               name: 'search' != 'fetch'...
E           E
E           E             ...Full output truncated (11 lines hidden), use '-vv' to show
E
E           Tests/UI/test_mcp_permission_action_ownership.py:201: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:37.575 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:37.612 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:37.619 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:37.620 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:37.733 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:37.955 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           ----------------------------- Captured stderr call -----------------------------
E           2026-09-19 08:51:38.443 | WARNING  | tldw_chatbook.UI.MCP_Modules.mcp_workbench:_sync_audit_log_entries:1617 - MCP execution log access failed: 'PermissionsHubService' object has no attribute 'execution_log'
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre0/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E           1.25s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[tool-remove]
E
E           (2 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[tool-remove]
E           1 failed in 2.36s

Tests/private_profile.py:84: Failed
_____ test_permission_completion_preserves_a_newer_selection[tool-reallow] _____

self = <Coroutine test_permission_completion_preserves_a_newer_selection[tool-reallow]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'reallow', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10dafff80>, 'request': <FixtureReque.../var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre1'), ...}
request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[tool-reallow]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre1/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre1/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre1/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre1/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre1/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _____ test_permission_completion_preserves_a_newer_selection[tool-reallow] _____
E
E           request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[tool-reallow]>>
E           tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre1/private-profile-process/pytest/test_permission_completion_pre0')
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10a1d4380>
E           action = 'reallow', transition = 'tool'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["tool", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_permission_completion_preserves_a_newer_selection(request, tmp_path, monkeypatch, action, transition):
E                   app = PermissionsApp(tmp_path / "permissions.json")
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       workbench, inspector, tool = await open_permissions(app, pilot)
E                       entered, release = asyncio.Event(), asyncio.Event()
E                       original = workbench._sync_permissions_mode
E
E                       async def hold_sync(*args, **kwargs):
E                           entered.set()
E                           await release.wait()
E                           await original(*args, **kwargs)
E
E                       monkeypatch.setattr(workbench, "_sync_permissions_mode", hold_sync)
E                       app.query_one(SELECTORS[action], Button).press()
E                       try:
E                           await asyncio.wait_for(entered.wait(), 3)
E                           if transition == "clear":
E                               await inspector.show_tool(None)
E                           else:
E                               await show(inspector, tool=_tool(name="fetch"), context=workbench._tool_policy_profile_context)
E                               if transition == "roundtrip":
E                                   await show(inspector, tool=tool, rules=list(reversed(RULES)), context=workbench._tool_policy_profile_context)
E                           expected_tool = inspector._current_permission_tool
E                           expected_rules = inspector._current_permission_arg_rules.copy()
E                       finally:
E                           release.set()
E                       await pilot.pause()
E           >           assert inspector._current_permission_tool == expected_tool
E           E           AssertionError: assert HubTool(serve...ecutable=True) == HubTool(serve...ecutable=True)
E           E
E           E             Omitting 6 identical items, use -vv to show
E           E             Differing attributes:
E           E             ['name', 'description', 'input_schema']
E           E
E           E             Drill down into differing attribute name:
E           E               name: 'search' != 'fetch'...
E           E
E           E             ...Full output truncated (11 lines hidden), use '-vv' to show
E
E           Tests/UI/test_mcp_permission_action_ownership.py:201: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:40.535 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:40.572 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:40.578 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:40.579 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:40.691 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:40.917 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           ----------------------------- Captured stderr call -----------------------------
E           2026-09-19 08:51:41.411 | WARNING  | tldw_chatbook.UI.MCP_Modules.mcp_workbench:_sync_audit_log_entries:1617 - MCP execution log access failed: 'PermissionsHubService' object has no attribute 'execution_log'
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre1/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E           1.29s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[tool-reallow]
E
E           (2 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[tool-reallow]
E           1 failed in 2.41s

Tests/private_profile.py:84: Failed
_____ test_permission_completion_preserves_a_newer_selection[clear-remove] _____

self = <Coroutine test_permission_completion_preserves_a_newer_selection[clear-remove]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'remove', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10daff3b0>, 'request': <FixtureReques.../var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre2'), ...}
request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[clear-remove]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre2/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre2/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre2/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre2/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre2/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           _____ test_permission_completion_preserves_a_newer_selection[clear-remove] _____
E
E           request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[clear-remove]>>
E           tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre2/private-profile-process/pytest/test_permission_completion_pre0')
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10a91c3e0>
E           action = 'remove', transition = 'clear'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["tool", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_permission_completion_preserves_a_newer_selection(request, tmp_path, monkeypatch, action, transition):
E                   app = PermissionsApp(tmp_path / "permissions.json")
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       workbench, inspector, tool = await open_permissions(app, pilot)
E                       entered, release = asyncio.Event(), asyncio.Event()
E                       original = workbench._sync_permissions_mode
E
E                       async def hold_sync(*args, **kwargs):
E                           entered.set()
E                           await release.wait()
E                           await original(*args, **kwargs)
E
E                       monkeypatch.setattr(workbench, "_sync_permissions_mode", hold_sync)
E                       app.query_one(SELECTORS[action], Button).press()
E                       try:
E                           await asyncio.wait_for(entered.wait(), 3)
E                           if transition == "clear":
E                               await inspector.show_tool(None)
E                           else:
E                               await show(inspector, tool=_tool(name="fetch"), context=workbench._tool_policy_profile_context)
E                               if transition == "roundtrip":
E                                   await show(inspector, tool=tool, rules=list(reversed(RULES)), context=workbench._tool_policy_profile_context)
E                           expected_tool = inspector._current_permission_tool
E                           expected_rules = inspector._current_permission_arg_rules.copy()
E                       finally:
E                           release.set()
E                       await pilot.pause()
E           >           assert inspector._current_permission_tool == expected_tool
E           E           AssertionError: assert HubTool(server_key='local:docs', server_label='docs', source='local', name='search', description='Search docs.', input_schema=None, tags=(), stale=False, executable=True) == None
E           E            +  where HubTool(server_key='local:docs', server_label='docs', source='local', name='search', description='Search docs.', input_schema=None, tags=(), stale=False, executable=True) = MCPInspector(id='mcp-hub-inspector', classes='ds-inspector destination-workbench-pane')._current_permission_tool
E
E           Tests/UI/test_mcp_permission_action_ownership.py:201: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:43.579 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:43.616 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:43.623 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:43.624 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:43.735 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:43.954 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           ----------------------------- Captured stderr call -----------------------------
E           2026-09-19 08:51:44.440 | WARNING  | tldw_chatbook.UI.MCP_Modules.mcp_workbench:_sync_audit_log_entries:1617 - MCP execution log access failed: 'PermissionsHubService' object has no attribute 'execution_log'
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre2/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E           1.26s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[clear-remove]
E
E           (2 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[clear-remove]
E           1 failed in 2.38s

Tests/private_profile.py:84: Failed
____ test_permission_completion_preserves_a_newer_selection[clear-reallow] _____

self = <Coroutine test_permission_completion_preserves_a_newer_selection[clear-reallow]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'reallow', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10dafdb50>, 'request': <FixtureReque.../var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre3'), ...}
request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[clear-reallow]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre3/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre3/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre3/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre3/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre3/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           ____ test_permission_completion_preserves_a_newer_selection[clear-reallow] _____
E
E           request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[clear-reallow]>>
E           tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre3/private-profile-process/pytest/test_permission_completion_pre0')
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10a0b5100>
E           action = 'reallow', transition = 'clear'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["tool", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_permission_completion_preserves_a_newer_selection(request, tmp_path, monkeypatch, action, transition):
E                   app = PermissionsApp(tmp_path / "permissions.json")
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       workbench, inspector, tool = await open_permissions(app, pilot)
E                       entered, release = asyncio.Event(), asyncio.Event()
E                       original = workbench._sync_permissions_mode
E
E                       async def hold_sync(*args, **kwargs):
E                           entered.set()
E                           await release.wait()
E                           await original(*args, **kwargs)
E
E                       monkeypatch.setattr(workbench, "_sync_permissions_mode", hold_sync)
E                       app.query_one(SELECTORS[action], Button).press()
E                       try:
E                           await asyncio.wait_for(entered.wait(), 3)
E                           if transition == "clear":
E                               await inspector.show_tool(None)
E                           else:
E                               await show(inspector, tool=_tool(name="fetch"), context=workbench._tool_policy_profile_context)
E                               if transition == "roundtrip":
E                                   await show(inspector, tool=tool, rules=list(reversed(RULES)), context=workbench._tool_policy_profile_context)
E                           expected_tool = inspector._current_permission_tool
E                           expected_rules = inspector._current_permission_arg_rules.copy()
E                       finally:
E                           release.set()
E                       await pilot.pause()
E           >           assert inspector._current_permission_tool == expected_tool
E           E           AssertionError: assert HubTool(server_key='local:docs', server_label='docs', source='local', name='search', description='Search docs.', input_schema=None, tags=(), stale=False, executable=True) == None
E           E            +  where HubTool(server_key='local:docs', server_label='docs', source='local', name='search', description='Search docs.', input_schema=None, tags=(), stale=False, executable=True) = MCPInspector(id='mcp-hub-inspector', classes='destination-workbench-pane ds-inspector')._current_permission_tool
E
E           Tests/UI/test_mcp_permission_action_ownership.py:201: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:46.657 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:46.695 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:46.706 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:46.707 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:46.826 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:47.047 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           ----------------------------- Captured stderr call -----------------------------
E           2026-09-19 08:51:47.523 | WARNING  | tldw_chatbook.UI.MCP_Modules.mcp_workbench:_sync_audit_log_entries:1617 - MCP execution log access failed: 'PermissionsHubService' object has no attribute 'execution_log'
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre3/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E           1.23s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[clear-reallow]
E
E           (2 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[clear-reallow]
E           1 failed in 2.37s

Tests/private_profile.py:84: Failed
___ test_permission_completion_preserves_a_newer_selection[roundtrip-remove] ___

self = <Coroutine test_permission_completion_preserves_a_newer_selection[roundtrip-remove]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'remove', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10daff320>, 'request': <FixtureReques.../var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre4'), ...}
request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[roundtrip-remove]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre4/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre4/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre4/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre4/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre4/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           ___ test_permission_completion_preserves_a_newer_selection[roundtrip-remove] ___
E
E           request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[roundtrip-remove]>>
E           tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre4/private-profile-process/pytest/test_permission_completion_pre0')
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10a895070>
E           action = 'remove', transition = 'roundtrip'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["tool", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_permission_completion_preserves_a_newer_selection(request, tmp_path, monkeypatch, action, transition):
E                   app = PermissionsApp(tmp_path / "permissions.json")
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       workbench, inspector, tool = await open_permissions(app, pilot)
E                       entered, release = asyncio.Event(), asyncio.Event()
E                       original = workbench._sync_permissions_mode
E
E                       async def hold_sync(*args, **kwargs):
E                           entered.set()
E                           await release.wait()
E                           await original(*args, **kwargs)
E
E                       monkeypatch.setattr(workbench, "_sync_permissions_mode", hold_sync)
E                       app.query_one(SELECTORS[action], Button).press()
E                       try:
E                           await asyncio.wait_for(entered.wait(), 3)
E                           if transition == "clear":
E                               await inspector.show_tool(None)
E                           else:
E                               await show(inspector, tool=_tool(name="fetch"), context=workbench._tool_policy_profile_context)
E                               if transition == "roundtrip":
E                                   await show(inspector, tool=tool, rules=list(reversed(RULES)), context=workbench._tool_policy_profile_context)
E                           expected_tool = inspector._current_permission_tool
E                           expected_rules = inspector._current_permission_arg_rules.copy()
E                       finally:
E                           release.set()
E                       await pilot.pause()
E                       assert inspector._current_permission_tool == expected_tool
E           >           assert inspector._current_permission_arg_rules == expected_rules
E           E           assert [] == [{'args_json'...ery":"one"}'}]
E           E
E           E             Right contains 2 more items, first extra item: {'args_json': '{"query":"two"}', 'profile_id': 'research', 'rule_id': '{"query":"two"}'}
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:202: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:49.571 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:49.608 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:49.615 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:49.616 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:49.726 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:49.941 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           ----------------------------- Captured stderr call -----------------------------
E           2026-09-19 08:51:50.425 | WARNING  | tldw_chatbook.UI.MCP_Modules.mcp_workbench:_sync_audit_log_entries:1617 - MCP execution log access failed: 'PermissionsHubService' object has no attribute 'execution_log'
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre4/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E           1.24s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[roundtrip-remove]
E
E           (2 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[roundtrip-remove]
E           1 failed in 2.34s

Tests/private_profile.py:84: Failed
__ test_permission_completion_preserves_a_newer_selection[roundtrip-reallow] ___

self = <Coroutine test_permission_completion_preserves_a_newer_selection[roundtrip-reallow]>

    def runtest(self) -> None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        runner = self._request.getfixturevalue(runner_fixture_id)
        context = contextvars.copy_context()
        synchronized_obj = _synchronize_coroutine(
            getattr(*self._synchronization_target_attr), runner, context
        )
        with MonkeyPatch.context() as c:
            c.setattr(*self._synchronization_target_attr, synchronized_obj)
>           super().runtest()

.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:474:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
.venv/lib/python3.12/site-packages/pytest_asyncio/plugin.py:721: in inner
    runner.run(coro, context=context)
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/runners.py:118: in run
    return self._loop.run_until_complete(task)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../../../.local/share/uv/python/cpython-3.12.11-macos-aarch64-none/lib/python3.12/asyncio/base_events.py:691: in run_until_complete
    return future.result()
           ^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

args = ()
kwargs = {'action': 'reallow', 'monkeypatch': <_pytest.monkeypatch.MonkeyPatch object at 0x10daffa40>, 'request': <FixtureReque.../var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre5'), ...}
request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[roundtrip-reallow]>>
work = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre5/private-profile-process')
profile = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre5/private-profile-process/profile')
environment = {'BUN_INSTALL': '/Users/macbook-dev/.bun', 'CODEX_APP_TOOLS_PIPE_PATH': '/tmp/codex-browser-use/b507bb60-436f-4f1a-a669-62329d01e853.sock', 'CODEX_CI': '1', 'CODEX_INTERNAL_ORIGINATOR_OVERRIDE': 'Codex Desktop', ...}
name = 'PYTEST_ADDOPTS', marker = None, timeout = 120.0
report = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre5/private-profile-process/pytest.xml')
log = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre5/private-profile-process/pytest.log')

    @wraps(function)
    async def wrapped(*args, **kwargs):
        request = kwargs["request"]
        if is_private_profile_child(request):
            result = function(*args, **kwargs)
            return await result if inspect.isawaitable(result) else result
        if _CHILD_NODE in os.environ:
            pytest.fail("private profile child node changed")
        work = request.getfixturevalue("tmp_path") / "private-profile-process"
        work.mkdir(mode=0o700)
        profile = work / "profile"
        profile.mkdir(mode=0o700)
        environment = dict(os.environ)
        environment.update(
            TLDW_TEST_PRIVATE_PROFILE_NODE=request.node.nodeid,
            TLDW_TEST_CONFIG_ROOT=str(profile),
            HOME=str(profile / "home"),
            USERPROFILE=str(profile / "home"),
            XDG_CONFIG_HOME=str(profile / "config"),
            XDG_DATA_HOME=str(profile / "data"),
            TLDW_CONFIG_PATH=str(profile / "config" / "config.toml"),
            PYTEST_DISABLE_PLUGIN_AUTOLOAD="1",
        )
        for name in (
            "TLDW_TEST_CONFIG_ROOT_OWNER", "PYTEST_XDIST_WORKER",
            "PYTEST_XDIST_WORKER_COUNT", "PYTEST_ADDOPTS",
        ):
            environment.pop(name, None)
        marker = request.node.get_closest_marker("timeout")
        timeout = marker.args[0] if marker and marker.args else None
        if timeout is None and marker:
            timeout = marker.kwargs.get("timeout")
        if timeout is None:
            timeout = get_env_settings(request.config).timeout
        timeout = float(timeout or 0)
        report = work / "pytest.xml"
        log = work / "pytest.log"
        with log.open("w") as output:
            process = await asyncio.create_subprocess_exec(
                sys.executable, "-m", "pytest", request.node.nodeid,
                "-p", "pytest_asyncio.plugin", "-p", "pytest_timeout", "-q",
                f"--timeout={timeout}", f"--basetemp={work / 'pytest'}",
                f"--junitxml={report}",
                cwd=Path(__file__).resolve().parents[1],
                env=environment,
                stdout=output,
                stderr=asyncio.subprocess.STDOUT,
            )
            try:
                await asyncio.wait_for(process.wait(), timeout if timeout > 0 else None)
            finally:
                if process.returncode is None:
                    process.kill()
                    await process.wait()
        if process.returncode:
>           pytest.fail(f"{log}\n{log.read_text()[-16000:]}")
E           Failed: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre5/private-profile-process/pytest.log
E           F                                                                        [100%]
E           =================================== FAILURES ===================================
E           __ test_permission_completion_preserves_a_newer_selection[roundtrip-reallow] ___
E
E           request = <FixtureRequest for <Coroutine test_permission_completion_preserves_a_newer_selection[roundtrip-reallow]>>
E           tmp_path = PosixPath('/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre5/private-profile-process/pytest/test_permission_completion_pre0')
E           monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10ddec380>
E           action = 'reallow', transition = 'roundtrip'
E
E               @pytest.mark.asyncio
E               @pytest.mark.parametrize("action", SELECTORS)
E               @pytest.mark.parametrize("transition", ["tool", "clear", "roundtrip"])
E               @private_profile_test
E               async def test_permission_completion_preserves_a_newer_selection(request, tmp_path, monkeypatch, action, transition):
E                   app = PermissionsApp(tmp_path / "permissions.json")
E                   async with app.run_test(size=(100, 60)) as pilot:
E                       workbench, inspector, tool = await open_permissions(app, pilot)
E                       entered, release = asyncio.Event(), asyncio.Event()
E                       original = workbench._sync_permissions_mode
E
E                       async def hold_sync(*args, **kwargs):
E                           entered.set()
E                           await release.wait()
E                           await original(*args, **kwargs)
E
E                       monkeypatch.setattr(workbench, "_sync_permissions_mode", hold_sync)
E                       app.query_one(SELECTORS[action], Button).press()
E                       try:
E                           await asyncio.wait_for(entered.wait(), 3)
E                           if transition == "clear":
E                               await inspector.show_tool(None)
E                           else:
E                               await show(inspector, tool=_tool(name="fetch"), context=workbench._tool_policy_profile_context)
E                               if transition == "roundtrip":
E                                   await show(inspector, tool=tool, rules=list(reversed(RULES)), context=workbench._tool_policy_profile_context)
E                           expected_tool = inspector._current_permission_tool
E                           expected_rules = inspector._current_permission_arg_rules.copy()
E                       finally:
E                           release.set()
E                       await pilot.pause()
E                       assert inspector._current_permission_tool == expected_tool
E           >           assert inspector._current_permission_arg_rules == expected_rules
E           E           assert [] == [{'args_json'...ery":"one"}'}]
E           E
E           E             Right contains 2 more items, first extra item: {'args_json': '{"query":"two"}', 'profile_id': 'research', 'rule_id': '{"query":"two"}'}
E           E             Use -v to get more diff
E
E           Tests/UI/test_mcp_permission_action_ownership.py:202: AssertionError
E           ---------------------------- Captured stderr setup -----------------------------
E           2026-09-19 08:51:52.459 | DEBUG    | tldw_chatbook.Utils.optional_deps:check_dependency:632 - ✅ huggingface_hub dependency found. Feature 'huggingface_hub' is enabled.
E           2026-09-19 08:51:52.496 | WARNING  | tldw_chatbook.Audio.recording_service:<module>:52 - PyAudio not available. Install with: pip install pyaudio
E           2026-09-19 08:51:52.503 | INFO     | tldw_chatbook.Audio.recording_service:<module>:58 - Sounddevice backend available
E           2026-09-19 08:51:52.504 | INFO     | tldw_chatbook.Audio.recording_service:<module>:73 - WebRTC VAD available for voice activity detection
E           2026-09-19 08:51:52.623 | WARNING  | tldw_chatbook.Prompt_Management.Prompts_Interop:<module>:53 - python-frontmatter not installed. Markdown import will not be available.
E           2026-09-19 08:51:52.845 | INFO     | tldw_chatbook.Evals.task_loader:<module>:36 - HuggingFace evaluation datasets are unavailable. Install with: pip install datasets
E           ----------------------------- Captured stderr call -----------------------------
E           2026-09-19 08:51:53.327 | WARNING  | tldw_chatbook.UI.MCP_Modules.mcp_workbench:_sync_audit_log_entries:1617 - MCP execution log access failed: 'PermissionsHubService' object has no attribute 'execution_log'
E           - generated xml file: /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/pytest-4/test_permission_completion_pre5/private-profile-process/pytest.xml -
E           ============================= slowest 25 durations =============================
E           1.28s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[roundtrip-reallow]
E
E           (2 durations < 1s hidden.)
E           =========================== short test summary info ============================
E           FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[roundtrip-reallow]
E           1 failed in 2.38s

Tests/private_profile.py:84: Failed
------- generated xml file: /private/tmp/tldw-32866-red-001/results.xml --------
============================= slowest 25 durations =============================
3.09s call     Tests/UI/test_mcp_permission_action_ownership.py::test_failed_permission_action_is_retryable[remove]
3.03s call     Tests/UI/test_mcp_permission_action_ownership.py::test_reallow_refuses_a_definition_changed_since_rendering[schema]
3.01s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[clear-remove]
3.00s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[tool-reallow]
2.99s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[same-remove]
2.96s call     Tests/UI/test_mcp_permission_action_ownership.py::test_failed_permission_action_is_retryable[reallow]
2.92s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[roundtrip-reallow]
2.92s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[clear-reallow]
2.91s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[tool-remove]
2.86s call     Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[roundtrip-remove]
2.81s call     Tests/UI/test_mcp_permission_action_ownership.py::test_reallow_refuses_a_definition_changed_since_rendering[description]
2.38s call     Tests/UI/test_mcp_permission_action_ownership.py::test_live_permission_controls_are_consumed_once_with_the_displayed_target[remove]
2.24s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-remove]
2.22s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[profile-remove]
2.09s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[tool-reallow]
2.09s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-reallow]
2.06s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[same-reallow]
2.04s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[reorder-reallow]
2.02s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[reorder-remove]
1.96s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[tool-remove]
1.92s call     Tests/UI/test_mcp_permission_action_ownership.py::test_live_permission_controls_are_consumed_once_with_the_displayed_target[reallow]
1.87s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[clear-reallow]
1.86s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[profile-reallow]
1.78s call     Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[clear-remove]

(1 durations < 1s hidden.)
=========================== short test summary info ============================
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[same-remove]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[same-reallow]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[reorder-remove]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[reorder-reallow]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[tool-remove]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[tool-reallow]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[profile-remove]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[profile-reallow]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-remove]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_obsolete_permission_controls_cannot_target_a_replacement[roundtrip-reallow]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_live_permission_controls_are_consumed_once_with_the_displayed_target[remove]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_live_permission_controls_are_consumed_once_with_the_displayed_target[reallow]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_reallow_refuses_a_definition_changed_since_rendering[description]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_reallow_refuses_a_definition_changed_since_rendering[schema]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[tool-remove]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[tool-reallow]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[clear-remove]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[clear-reallow]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[roundtrip-remove]
FAILED Tests/UI/test_mcp_permission_action_ownership.py::test_permission_completion_preserves_a_newer_selection[roundtrip-reallow]
20 failed, 4 passed in 61.01s (0:01:01)
/Users/macbook-dev/Documents/GitHub/tldw_chatbook/.worktrees/approval-batch-action-review/.venv/lib/python3.12/site-packages/_pytest/pathlib.py:96: PytestWarning: (rm_rf) error removing /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/garbage-26540f80-d602-4d10-b080-9eeb3ff563f5/test_fs_read_of_file_in_unread0
<class 'OSError'>: [Errno 66] Directory not empty: '/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/garbage-26540f80-d602-4d10-b080-9eeb3ff563f5/test_fs_read_of_file_in_unread0'
  warnings.warn(
/Users/macbook-dev/Documents/GitHub/tldw_chatbook/.worktrees/approval-batch-action-review/.venv/lib/python3.12/site-packages/_pytest/pathlib.py:96: PytestWarning: (rm_rf) error removing /private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/garbage-26540f80-d602-4d10-b080-9eeb3ff563f5
<class 'OSError'>: [Errno 66] Directory not empty: '/private/var/folders/sn/m80n2j152t9gw3w8qwk2nykh0000gn/T/pytest-of-macbook-dev/garbage-26540f80-d602-4d10-b080-9eeb3ff563f5'
  warnings.warn(
