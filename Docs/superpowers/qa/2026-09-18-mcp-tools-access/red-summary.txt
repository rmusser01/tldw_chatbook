Production-styled baseline showed the switch wider than its clipped pane and only one of nine table rows visible at 80x24. All four initial dark/light tests failed on actual geometry (4 failed in 29.73s).
First repair passed both filter/row cases but a max-width-only toggle constraint clipped its arrow on a state/resize transition. Using full pane width with auto height passed both label cases.
Independent review seeded 60 tools, selected the last row and resized without extra navigation; the table was visible but its selected row remained below the viewport. Two new regressions reproduced this (2 failed, 4 deselected in 16.44s). The final reveal also scrolls the focused table cursor into view. All six final cases pass; no post-resize navigation conceals the cursor regression.
