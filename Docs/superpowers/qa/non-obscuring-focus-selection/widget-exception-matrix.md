# Non-Obscuring Focus Widget Exception Matrix

Date: 2026-05-28

| Widget/control | Supported cues | PR 1 behavior | Exception/fallback | Test |
| --- | --- | --- | --- | --- |
| `Button` | background, foreground, text-style, outline | underline plus subtle background, no heavy outline | none | `Tests/UI/test_non_obscuring_focus_contract.py` |
| `Input` | border, background, foreground, outline | thin border plus subtle bottom emphasis | no full-field heavy frame | `Tests/UI/test_focus_accessibility.py` and `Tests/UI/test_non_obscuring_focus_contract.py` |
| `TextArea` | border, background, foreground, outline | thin border plus subtle bottom emphasis | no full-field heavy frame | `Tests/UI/test_focus_accessibility.py` and `Tests/UI/test_non_obscuring_focus_contract.py` |
| `Select` | border/background varies by Textual widget | thin border plus subtle bottom emphasis where supported | if native styling resists bottom emphasis, document verified fallback here and in visual QA | `Tests/UI/test_focus_accessibility.py` and visual/mounted exception note |
| `Checkbox` | native check mark, foreground, outline | shared toggle focus keeps labels readable with subtle background and underline | native check mark remains semantic; label focus must not use block cursor fills | `Tests/UI/test_non_obscuring_focus_contract.py` |
| `RadioButton` | native check mark, foreground, outline | shared toggle focus keeps labels readable with subtle background and underline | native selected mark remains semantic; label focus must not use block cursor fills | `Tests/UI/test_non_obscuring_focus_contract.py` |
| `DataTable` | cursor row and selected row styling | shared neutral row background, readable text, bold underline for cursor/selected rows | feature styles must not reintroduce primary/accent row fills | `Tests/UI/test_non_obscuring_focus_contract.py` |
| `Tree` | cursor/selected row styling | shared neutral cursor and hover backgrounds, readable text, bold underline for cursor rows | feature styles may scope local tree rows but must keep the shared row-state contract | `Tests/UI/test_non_obscuring_focus_contract.py` |
| `ListView` | item hover and selected/highlight classes | shared neutral hover background, readable text, bold underline for highlighted rows | feature styles may scope local rows but must keep the shared row-state contract | `Tests/UI/test_non_obscuring_focus_contract.py` |
| `SelectionList` | selected/highlight option styling | shared neutral selected and highlighted button backgrounds, readable text, bold underline | feature styles may scope local choices but must keep the shared row-state contract | `Tests/UI/test_non_obscuring_focus_contract.py` |
| `Tabs` | tab active/focus classes | shared native active/focused-active tabs use subtle background, readable text, and underline | feature styles may scope native tab active states but must not reintroduce block-cursor fills | `Tests/UI/test_non_obscuring_focus_contract.py` |
| Custom button/list rows | background, marker text, classes, text-style | named Console/Library rows, Console session tabs, legacy chat tabs, Settings category selections, Search/RAG nav selections, Embeddings row selections, and ACP selected session rows use readable focus/selected cues | each custom row needs explicit selector audit | source contract or mounted test |
| `Collapsible` active headers | background, foreground, text-style, border-bottom | conversation active/expanded headers use readable selected cues with a stable bottom border | feature overrides may scope collapsible headers but active headers must not use dominant primary/accent fills | `Tests/UI/test_non_obscuring_focus_contract.py` |
| Sidebar focus-within sections | border, background | sidebar collapsible focus-within containers use shared focus tokens without raw accent or primary fills | focus-within may highlight a region, but must keep border geometry stable and avoid text-obscuring fills | `Tests/UI/test_non_obscuring_focus_contract.py` |
| Ingest selected-files list | border, background | selected-files containers use a stable focus-accent border without raw primary/accent fills | duplicate source/bundle rules must all keep the same non-obscuring container contract | `Tests/UI/test_non_obscuring_focus_contract.py` |
| Shared SectionContainer collapse button hover | background, foreground | widget `DEFAULT_CSS` collapse buttons use a subtle surface hover with readable foreground | shared collapsible affordances must not use raw primary fills that obscure compact glyph labels | `Tests/UI/test_non_obscuring_focus_contract.py` |
| Shared NavigationButton states | background, foreground, text-style | widget `DEFAULT_CSS` hover uses a subtle surface lift, while active selection uses shared selected tokens with underline | shared navigation hover and active states must not obscure labels across downstream screens | `Tests/UI/test_non_obscuring_focus_contract.py` |
| LLM Management inline nav buttons | background, foreground, text-style | widget `DEFAULT_CSS` hover and active states use shared readable tokens with underline for active selection | inline widget CSS must not reintroduce raw primary/accent fills outside the bundled stylesheet | `Tests/UI/test_non_obscuring_focus_contract.py` |
| Code repo inline tree rows | background, foreground, text-style | selected repository tree rows use the same readable selected-row contract as bundled code-repo CSS | widget `DEFAULT_CSS` must stay aligned with source CSS so inline tree selection cannot drift back to accent fills | `Tests/UI/test_non_obscuring_focus_contract.py` |
| Media inline hover rows | background, foreground | media type buttons and media list rows use subtle surface hover states with readable foreground | widget `DEFAULT_CSS` hover rules must not use raw accent fills that can obscure labels or compete with selection | `Tests/UI/test_non_obscuring_focus_contract.py` |
| Customize inline nav buttons | background, foreground, text-style | active Customize nav buttons use shared readable selected tokens with underline instead of raw primary fills | inline widget CSS must keep selected theme/splash nav labels readable after click and keyboard activation | `Tests/UI/test_non_obscuring_focus_contract.py` |
