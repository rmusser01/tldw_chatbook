Mounted production baseline at 80/100/120/170: 80-column Source Select has only four label cells, wrapping Local; purpose is fixed to one row; content-auto Tool width pushes State offscreen (including at 120 columns). Baseline captures and geometry retained.
Corrected baseline purpose cases fail in both themes; corrected table cases fail in both themes because a 46-cell Tool region has only 39 visible cells (2 failed, 4 deselected in 15.85s). Initial test attempts used two incorrect private Textual helper signatures and one whitespace-sensitive wrapped-copy comparison; those fixture errors are not product evidence.
The Unicode fixture exposed a real first-implementation gap: the scrollbar width property returns zero until the bar appears. Reserving its style-defined width before wrapping prevents State clipping. Subsequent full text, State, resize and filter checks pass. An overbroad no-event assertion was narrowed to reject mutation events while permitting ordinary RowSelected observations. Final readability run separately qualifies tags through horizontal keyboard scroll and confirms resize causes no service reload.
