# Release 0.2.1 implementation plan

Goal: Publish the latest committed dev changes to main and PyPI, preserving the
owner's dirty working checkout. Track execution in TASK-32506.

Architecture: Follow the existing protected dev/TestPyPI and main/PyPI workflow.
Use an isolated release worktree synchronized to dev392ce191fd and reconcile main
d0aa66ea97 before verification. The approved app-only exception and the
installed-app shutdown repair below are the bounded behavior changes needed
for this release.

ADR required: yes (amend existing ADR-098 for the approved app-only exception)
ADR path: backlog/decisions/098-low-latency-speculative-duplex-voice-pipeline.md
Packaging contract: backlog/decisions/032-immutable-installed-distribution-assets.md
Reason: Existing installed-distribution and trusted-publishing contracts apply.

1. Cancel the superseded pending 0.2.0 production upload.
2. Update pyproject.toml, tldw_chatbook/__init__.py, CHANGELOG.md, and the README
   current-version statement to 0.2.1.
   TestPyPI already has immutable 0.2.0 files from an older source commit.
3. Correct the manifest gate for the fixed runtime Canvas guide resources under
   ADR-032 and ADR-149, covering missing guides and unrelated Markdown rejection.
   Run Packaging/build_dist.sh, Tests/Packaging/test_release_metadata.py, and
   Tests/Packaging/test_installed_distribution.py with the integration marker.
   Check the diff and validate installation and both console entry points in a
   disposable environment. Use targeted verification only.
4. Integrate the release preparation to dev and dispatch publish-pypi.yml there.
   Verify TestPyPI publication and install the exact new version.
5. Merge the verified source to main, wait for fresh distribution gates, and
   approve the requested production publication through its environment gate.
6. Verify PyPI hashes against the production artifacts and installed CLI help;
   create the source tag and GitHub release at the main release commit.
7. Record test counts, source commits, run URLs, and publication evidence in
   TASK-32506; mark Done only after all acceptance criteria are met.

Verification follow-up: five Library assertions failed on both the candidate
and pristine pre-release dev. Before integration, update their test oracles to
exclude the focus bar from label underline checks, distinguish the 36-cell
Items pane from its 32-cell canvas, and account for the documented danger
action margin when comparing grid origins while pinning the expected margins.
Retain painted label checks and
rerun affected tests and shared helper callers. No application change is needed.

Voice companion follow-up: synchronize its version and exact optional pin, run the
existing version-lock tests, and resolve application release scope before publishing.
Existing ADR-098 requires companion qualification and publication first; current
checked-in rollout authority is unqualified and physical reports are absent.

Approved app-only implementation (2026-09-13):
1. Remove the native dependency, retaining ordinary recording dependencies and
   synchronized source versions. Preserve runtime qualification and acoustic flags.
2. Add an explicit app-only version-check mode that rejects native dependencies
   and any qualified platform; retain strict default companion-release mode.
3. Use that mode in app publishing and structural native builds; document the
   exception. Add source, metadata, and installed-runtime regressions.
4. Run targeted verification, independent review, required PR checks, TestPyPI,
   and main/PyPI publication. No native companion publication or hardware work.

Installed Linux shutdown follow-up: reproduce queued Buddy rebuild/change events
against the real empty Textual stack after shutdown; ignore these stale events,
retain active-screen reconciliation, then rerun Buddy lifecycle and installed-app
checks. ADR required: no new ADR; existing ADR-074 owns this lifecycle boundary.

Completion evidence:

- Release source: `4631b60f8dd9623fc55bf16f4a37e29fcb1240c7`, shared by main and
  annotated tag `v0.2.1`.
- [TestPyPI workflow](https://github.com/rmusser01/tldw_chatbook/actions/runs/34794286775)
  and [production workflow](https://github.com/rmusser01/tldw_chatbook/actions/runs/34795954783)
  each passed 180 installed-distribution tests and 24 metadata tests.
- Each registry's wheel and source archive match its own workflow artifact hashes.
  Fresh Python 3.12 installations pass dependency checks, runtime version,
  both CLI entry points, and the disabled experimental voice qualification gate.
- [PyPI 0.2.1](https://pypi.org/project/tldw-chatbook/0.2.1/) and
  [GitHub release](https://github.com/rmusser01/tldw_chatbook/releases/tag/v0.2.1)
  are published. No native companion was published.
- Broader core CI shards 1–4 aborted collection because timestamped gzip fixtures
  generated different parallel-worker test IDs. The affected network file passed
  all 41 tests directly; independent review identified a test-harness issue.
  This is a coverage limitation, not a passing full-suite result. Remaining
  automatic matrix jobs were still pending at release verification.
- TASK-32506 records the final result. Original user working changes were preserved.
