# task-577 PR1 — Window-Family Retirement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 1 of task-577 — retire the never-instantiated enhanced-window family: CWE, both dead sidebars, `Chat_Modules/`, the tabs subsystem, three orphan packages, the `use_enhanced_window`/`enable_tabs`/`max_tabs` flags, chat_screen's dead seams, and ~17 test files — behind per-unit grep-gates.

**Architecture:** Deletion campaign, 562-method. Ordering is load-bearing: chat_screen's MODULE-LEVEL imports of `ChatWindowEnhanced`/`ChatTabContainer` strip FIRST (Task 1), whole-file deletions follow (Task 2), flags (Task 3), residuals+CSS+guards (Task 4). Gate per symbol/id: composed/imported nowhere live + zero direct Python callers; every gate output pasted into the report; reviewers re-run gates; gate failure ⇒ DEFER, never force.

**Spec:** `Docs/superpowers/specs/2026-07-25-task-577-enhanced-window-retirement-design.md` (f448281ef) — its Scout Verdicts, Keep-Set, Ambiguous Gates, and the "Accepted inter-PR dangling" declaration are binding.

## Global Constraints

- Worktree `/Users/macbook-dev/Documents/GitHub/tldw_chatbook/.claude/worktrees/personas-redesign`, branch `claude/task-577-enhanced-window-retirement`. Subagent shells start in the MAIN checkout; a hook strips a LEADING `cd` — prepend `true; cd /Users/macbook-dev/Documents/GitHub/tldw_chatbook/.claude/worktrees/personas-redesign; ` to EVERY Bash command.
- Test prefix: `HOME=/private/tmp/tldw-chatbook-test-home XDG_DATA_HOME=/private/tmp/tldw-chatbook-test-home/.local/share /Users/macbook-dev/Documents/GitHub/tldw_chatbook/.venv/bin/python -m pytest <paths> -q -p no:cacheprovider -o addopts="" --timeout=300 --timeout-method=thread`. Tests/UI never mixed with other dirs in one invocation. Foreground only. Stage explicit paths; never `.superpowers/`.
- After every commit: venv `python -m pyflakes` on touched production files + `python -c "import tldw_chatbook.app"` + the task's suites.
- KEEP-SET (never touch): `chat_events_console_dictionaries.py`, `chat_rag_events.py`, `chat_token_events.py`, `Chat/chat_models.py` (`ChatSessionData`/`ConsoleChatSession`), `chat_message_enhanced.py`/`chat_message.py`, `Chat_Functions.py`, all `Widgets/Console/*`, `conv_char_events.py`, `chat_events.py` (Phase 2's file — except NOTHING in Phase 1 edits it; its deferred CWE import is the declared inter-PR dangling).
- Line numbers from dev `af726503c`; locate by symbol.

---

### Task 1: chat_screen seams + compact_model_bar (unblock the deletions)

**Files:** Modify `tldw_chatbook/UI/Screens/chat_screen.py`, `tldw_chatbook/Widgets/compact_model_bar.py`; update `Tests/` files that pin the edited chat_screen/compact-bar behavior (survey by grep).

**Interfaces:** Produces a chat_screen.py with ZERO references to `ChatWindowEnhanced`/`ChatTabContainer`/`self.chat_window` — the precondition for Task 2's `git rm`.

- [ ] **Step 1 — inventory + gates:** `grep -n "chat_window\|ChatWindowEnhanced\|ChatTabContainer\|_get_tab_container\|_get_active_chat_session\|console-chat-tabs" tldw_chatbook/UI/Screens/chat_screen.py` — enumerate every site (expect ~70). For each guarded block, classify: DELETE (dead branch only) vs PRESERVE-FALLTHROUGH (live else/fallthrough behavior kept byte-exact). Known classifications from the scout (verify each): `_toggle_console_chat_sidebar` keeps its "Chat settings are still loading." notify; `_chat_query_scope()` collapses to returning `self`; `_find_chat_log_container` keeps its Console-side fallback behavior; `_get_shell_bar` keeps the live `#console-*` paths if any. The provider/model/temperature methods (`_rebuild_chat_model_options`, `handle_provider_change`, `handle_model_catalog_refreshed`, `on_chat_api_model_changed`, `on_chat_temperature_changed`) are deleted whole IF their decorated ids (`#chat-api-provider`/`-api-model`/`#chat-temperature`) have zero live compose sites (gate — the scout says composed only in the two dead sidebar modules) AND no other caller invokes the methods directly (grep the method names).
- [ ] **Step 2 — edit:** remove the `:366` CWE import, `:275` ChatTabContainer import, `:1932` field, `_ensure_chat_window`, `_get_tab_container`/`_get_active_chat_session`/`on_chat_tab_container_active_session_changed`, `_save_tab_sessions`/`_restore_tab_sessions` tab arms inside `_perform_state_restoration` (verify what else that method does — preserve non-tab restoration), every `if self.chat_window:` dead branch per Step 1's classification, the diagnostics gate at `:9573` and `_run_diagnostic`'s chat_window usage (if the whole method becomes empty, delete it and its trigger — gate the trigger id).
- [ ] **Step 3 — compact_model_bar:** delete the CWE-ancestor walk (`:182-194` deferred import + `isinstance` branch), preserving the live sync behavior for Console hosts. Read the post-edit method end-to-end.
- [ ] **Step 4 — verify:** pyflakes both files; `grep -c "chat_window" chat_screen.py` → 0 (excluding comments you deliberately keep/reword); import smoke; run `Tests/UI/test_console_internals_decomposition.py` + `Tests/UI/test_chat_screen_state.py` (its non-tab tests must still pass — the TabContext sections are Task 2 casualties, so if they fail ONLY on Chat/tabs imports still present, note it and leave for Task 2) + `Tests/Widgets/` compact-bar tests (locate by grep) + `Tests/UI/test_console_native_chat_flow.py`.
- [ ] **Step 5 — commit:** `refactor(chat): task-577 T1 — strip chat_screen window/tabs seams + compact_model_bar dead branch`

### Task 2: the whole-file deletions (U1+U2+U3) + test casualties

**Files:** `git rm`: `UI/Chat_Window_Enhanced.py`, `Widgets/enhanced_settings_sidebar.py`, `Widgets/minimal_settings_sidebar.py`, `UI/Chat_Modules/` (whole package), `Widgets/Chat_Widgets/chat_tab_container.py`, `chat_session.py`, `chat_tab_bar.py`, `Chat/tabs/` (whole package), `Event_Handlers/Chat_Events/chat_events_tabs.py`, `Event_Handlers/tab_initializers/` (whole package), `Event_Handlers/Chat_Events/chat_events_sidebar.py`, `Event_Handlers/sidebar_events.py`. Modify: `tldw_chatbook/app.py` (only the import/fold lines these deletions orphan: the `chat_events_sidebar` fold at ~:5252, any `sidebar_events` import), `Utils/chat_diagnostics.py` (name-string refs). Tests: delete + update per Step 2.

- [ ] **Step 1 — gates per file:** for each module, `grep -rn "<module_basename>\|<primary class>" tldw_chatbook/ Tests/` — production hits allowed ONLY in the deleted set, Task-1-cleaned files (should be zero now), and app.py lines this task removes. The `chat_events.py` deferred-CWE-import hit is the DECLARED inter-PR dangling — record it, do not edit chat_events.py. Paste all outputs.
- [ ] **Step 2 — test casualties (grep-enumerated, not list-trusted):** grep Tests/ for every deleted module/class. Known: DELETE `Tests/UI/test_chat_window_enhanced.py`, `test_chat_window_enhanced_integration.py`, `test_chat_window_enhanced_modules.py`, `test_chat_first_run_orientation.py`, `test_chat_approvals_and_resume.py`, `test_chat_image_attachment.py`, `test_legacy_attach_picker.py`, `Tests/unit/test_chat_image_unit.py`, `Tests/integration/test_chat_tabs_integration.py`, `Tests/Widgets/test_chat_tab_container.py`, `Tests/UI/test_chat_tab_container.py`, `Tests/Widgets/test_chat_session.py`, `Tests/Widgets/test_chat_tab_bar.py`, `Tests/Event_Handlers/Chat_Events/test_chat_events_tabs.py`, `Tests/Event_Handlers/Chat_Events/test_chat_streaming_textual.py` (imports CWE at module level — Phase 1 casualty by import even though it pins Phase-2 behavior). UPDATE surgically (do NOT delete): `Tests/UI/test_chat_first_handoffs.py` (remove its ChatSession import at :23 and the ChatSession-dependent test(s) ~:1110 ONLY — the task-442 twins and all Console tests stay), `Tests/UI/test_chat_screen_state.py` (remove the TabContext/TabStateManager import + sections :316-364), `Tests/UI/test_ux_audit_smoke.py` (ChatTabContainer refs :29/:78/:81), `Tests/test_smoke.py` (delete `test_chat_window_creation` :173-186 only), the two monkeypatch-string sites (`Tests/UI/test_product_maturity_phase1_core_loop.py:130`, `test_product_maturity_phase1_empty_setup_states.py:165` — repoint or remove the patch of the deleted module path; understand what each test actually exercises first).
- [ ] **Step 3 — delete + strip:** `git rm` the files/packages; clean the app.py orphaned import/fold lines; clean `Utils/chat_diagnostics.py` string refs (or defer the module wholesale to Task 4's gate).
- [ ] **Step 4 — verify:** pyflakes app.py; import smoke; run (separate invocations): `Tests/Event_Handlers/Chat_Events/`, `Tests/UI/test_chat_first_handoffs.py Tests/UI/test_chat_screen_state.py Tests/UI/test_console_native_chat_flow.py Tests/UI/test_ux_audit_smoke.py`, `Tests/test_smoke.py`, `Tests/Widgets/` (whatever remains), `Tests/unit/ Tests/integration/` (remains). All green.
- [ ] **Step 5 — commit:** `refactor(chat): task-577 T2 — retire ChatWindowEnhanced family, tabs subsystem, orphan packages (never instantiated since 8ea71071f)`

### Task 3: flags + Settings UI (U5)

**Files:** `tldw_chatbook/config.py`, `tldw_chatbook/UI/Tools_Settings_Window.py`, `tldw_chatbook/app.py` (`:3531` handoff gate), `Tests/UI/test_chat_first_handoffs.py` (:72-73 config assertions), any Tools_Settings tests touching the removed rows (grep).

- [ ] **Step 1 — gates:** grep remaining readers of `use_enhanced_window` / `enable_tabs` / `max_tabs` in tldw_chatbook/ — after Task 2, expected survivors: `config.py` defaults, Tools_Settings sites, `app.py:3531`, and PHASE-2 files (`chat_events.py:785...`, `worker_events.py:231`, `chat_worker_handler.py:60`) — the Phase-2 reads are LEFT (they fall to call-site defaults once the keys are gone; they die in PR2). Paste output.
- [ ] **Step 2 — edit:** remove the three keys from `config.py` `[chat_defaults]` (reword the `[chat.images]` comment — it stays, its non-CWE consumers are live); remove both `use_enhanced_window` checkboxes + the tabs checkbox/input + their save/reset/restart-notice sites in Tools_Settings_Window.py (read each edited method end-to-end — other settings rows stay byte-identical); remove the `enable_tabs` refusal check in `app.py` `open_chat_with_handoff` (~:3531) so the handoff proceeds unconditionally (the ONE deliberate behavior change — cite the spec in the commit body).
- [ ] **Step 3 — tests:** update `test_chat_first_handoffs.py:72-73` (drop the enable_tabs assertions); grep Tools_Settings tests for the removed ids (`#general-enhanced-chat-window`, `#config-chat-enhanced-window`, `#general-enable-chat-tabs`, `#general-max-chat-tabs`) and update; add one pin: handoff proceeds without any tabs-flag check (locate the existing open_chat_with_handoff test file by grep and extend it — if none exists, add the pin in the handoffs suite).
- [ ] **Step 4 — verify:** pyflakes; import smoke; `Tests/UI/test_chat_first_handoffs.py` + the Tools_Settings suite (locate by grep) as separate invocations. Green.
- [ ] **Step 5 — commit:** `refactor(chat): task-577 T3 — remove use_enhanced_window + chat-tabs flags and Settings rows; handoff no longer gates on retired tabs`

### Task 4: residuals + CSS + retirement guards (U6+U7)

**Files:** `tldw_chatbook/app.py`, `tldw_chatbook/Constants.py`, `css/layout/_sidebars.tcss` + `css/features/_chat.tcss` + `css/components/_buttons.tcss` + any other SOURCE tcss the grep finds, regenerate `css/tldw_cli_modular.tcss` via `./build_css.sh`, `Utils/chat_diagnostics.py` (gated), `Tests/UI/test_legacy_entrypoints_retired.py`, `backlog/tasks/task-577 - ....md` (progress notes only — task stays In Progress until PR2).

- [ ] **Step 1 — app.py residuals (each gated):** `"chat-window"` in `ALL_MAIN_WINDOW_IDS` (~:2774); the `:6490-6491` `screen.chat_window` poke; the dead `toggle-chat-right-sidebar` entries in `chat_handlers_map` (~:5256) and `chat_events.py`'s CHAT_BUTTON_HANDLERS — NO: chat_events.py is Phase 2's file, leave its map entry, remove only the app.py side; `chat_right_sidebar_width` reactive (readers died in 562's U5 file removal — gate then delete); `chat_right_sidebar_collapsed` — VERIFY the live chat_screen reads (:10083/:10200 region) per Ambiguous Gate 3 and KEEP if live (report the evidence).
- [ ] **Step 2 — chat_diagnostics gate:** if its only consumers were Task-1-deleted blocks → `git rm` + strip imports; else keep and report.
- [ ] **Step 3 — CSS sweep:** grep SOURCE tcss + Constants.py for the retired family ids (`chat-window`, `chat-log`, `chat-input`, `chat-input-area`, `chat-enhanced-sidebar`, `chat-left-sidebar`, `toggle-chat-left-sidebar`, tab-family classes/ids, `.save-chat-button`, `.sidebar-resize-button`, `mic-button`, `send-stop-chat`, `image-attachment-indicator`, `chat-empty-state`, `chat-shell-bar` — CAREFUL: verify EACH id/class is composed nowhere live before removing its rules; `chat-shell-bar`/`chat-task-surface` families have Console twins — any selector also serving a live Console-composed id STAYS). Remove dead rules from sources; `./build_css.sh`; commit the regenerated bundle; if the bundle diff contains anything beyond your selector removals + timestamp, STOP and report.
- [ ] **Step 4 — retirement guards:** extend `test_legacy_entrypoints_retired.py`: add all Task-2 retired module paths to `RETIRED_MODULES`/`RETIRED_FILES`; add a `test_task_577_pr1_window_family_retired` pin asserting the module paths are unimportable and `chat_screen` no longer defines `_ensure_chat_window`/`chat_window` attribute default — match the verified reality including any DEFERRED units.
- [ ] **Step 5 — verify (the PR gate):** separate invocations — `Tests/UI/test_legacy_entrypoints_retired.py` alone; `Tests/Event_Handlers/Chat_Events/`; `Tests/test_smoke.py`; `Tests/UI/test_css_bundle_sync_guard.py Tests/UI/test_css_build_integrity.py`; `Tests/UI/test_console_native_chat_flow.py Tests/UI/test_chat_first_handoffs.py`; import smoke; pyflakes on all touched files.
- [ ] **Step 6 — task-577 file:** append a `## PR1 progress` note (units delivered/deferred + commit SHAs); status stays In Progress.
- [ ] **Step 7 — commit:** `refactor(chat): task-577 T4 — residual sweeps, window-family CSS retirement, guard pins`

---

### Controller-level (not subagent tasks)

- Final whole-branch review (sonnet) with cumulative gates + full suite sweep (add `Tests/UI/` console suites + `Tests/Chat/` to the verification list) + a "did we break any conftest/fixture import" check (`pytest --collect-only -q` on Tests/ roots).
- PR1 to `dev` (declare the inter-PR dangling in the body), Qodo adjudication, STOP for user merge-go. PR2 planning starts only after PR1 merges.
