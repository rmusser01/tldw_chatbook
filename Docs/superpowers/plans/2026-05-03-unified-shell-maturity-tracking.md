# Unified Shell Maturity Tracking Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the Phase 0 tracking foundation for the Unified Shell work so remaining product, QA, and PR-sized implementation slices are visible and verifiable.

**Architecture:** The design spec remains the requirements source, `Docs/superpowers/trackers/unified-shell-maturity-roadmap.md` becomes the executive tracker, Backlog.md stores PR-sized execution tasks, `backlog/docs/` points Backlog users to the canonical roadmap, and `Docs/superpowers/qa/unified-shell/` stores durable QA summaries. This plan deliberately stops at Phase 0; Home, Console, Skills, MCP, ACP, Library, and workflow service wiring must be implemented through later Backlog child tasks.

**Tech Stack:** Backlog.md CLI, Markdown docs, git, shell validation commands, existing `Docs/superpowers` conventions.

---

## Scope

Implement Phase 0: Canonical Tracking from `Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md`.

This plan creates the tracking system only. It does not change Textual UI code, shell wrappers, service adapters, or tests for product behavior. Later phases must still use TDD and running-app QA before claiming usability.

## Preconditions

- Work from a clean worktree based on current `origin/dev`.
- Do not initialize Backlog.md in the stale root checkout.
- Use Backlog CLI reads with `--plain`.
- Preserve the existing Backlog.md guidance in `AGENTS.md`; do not duplicate that section if `backlog init` tries to add it again.
- Create Phase 0 Backlog tasks and add their implementation plans immediately after `backlog init`, before doing roadmap or QA document work.
- Treat docs-only verification as Phase 0 tracking QA. Do not mark any product workflow as verified because this plan does not exercise the app UI.

## File Structure

Create:

- `backlog/` - generated by `backlog init`, then populated with phase parent tasks, initial child tasks, and a roadmap pointer document.
- `backlog/docs/unified-shell-maturity-roadmap.md` - Backlog-facing pointer to the canonical roadmap and QA evidence directory.
- `Docs/superpowers/trackers/unified-shell-maturity-roadmap.md` - canonical phase tracker, Backlog task index, known-gap baseline, QA evidence index, and update rules.
- `Docs/superpowers/qa/unified-shell/README.md` - durable QA evidence protocol and phase index.
- `Docs/superpowers/qa/unified-shell/phase-0/README.md` - Phase 0 tracking setup QA summary.
- `Docs/superpowers/qa/unified-shell/phase-1/README.md` - placeholder for shell contract QA summaries.
- `Docs/superpowers/qa/unified-shell/phase-2/README.md` - placeholder for Home control QA summaries.
- `Docs/superpowers/qa/unified-shell/phase-3/README.md` - placeholder for Console live-work QA summaries.
- `Docs/superpowers/qa/unified-shell/phase-4/README.md` - placeholder for destination service adoption QA summaries.
- `Docs/superpowers/qa/unified-shell/phase-5/README.md` - placeholder for capability/recovery QA summaries.
- `Docs/superpowers/qa/unified-shell/phase-6/README.md` - placeholder for final audit replay summaries.

Modify, only if generated by Backlog.md:

- `AGENTS.md` - inspect after `backlog init`; keep at most one Backlog.md instruction block.

Do not modify:

- `app.py`
- `tldw_chatbook/UI/*`
- `Tests/*`
- `Docs/Design/master-shell-route-inventory.md`
- `Docs/Design/master-shell-design-system-contract.md`

## Task 1: Verify Baseline And CLI Compatibility

**Files:**
- Read: `Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md`
- Read: `Docs/superpowers/specs/2026-05-02-agentic-terminal-design-system-design.md`
- Read: `Docs/Design/master-shell-route-inventory.md`
- Read: `Docs/Design/master-shell-design-system-contract.md`

- [ ] **Step 1: Confirm worktree branch and cleanliness**

Run:

```bash
git status --short --branch
```

Expected:

```text
## codex/unified-shell-maturity-tracking...origin/dev [ahead N]
```

No modified or untracked files except intentional plan/spec files if execution starts before this plan commit lands.

- [ ] **Step 2: Confirm Backlog.md CLI is available**

Run:

```bash
command -v backlog
backlog init --help
backlog task create --help
backlog task edit --help
backlog task view --help
backlog overview --help
```

Expected:

- `command -v backlog` prints an executable path.
- `backlog init --help` includes `--agent-instructions`, `--integration-mode`, and `--backlog-dir`.
- `backlog task create --help` includes `--description`, `--labels`, `--priority`, `--plain`, `--ac`, `--parent`, `--dep`, `--ref`, and `--doc`.
- `backlog task edit --help` includes `--plain`, `--plan`, `--notes`, `--check-ac`, `--dep`, and status editing.
- `backlog task view --help` and `backlog overview --help` run without error.

- [ ] **Step 3: Confirm Backlog is not already initialized**

Run:

```bash
find backlog -maxdepth 2 -type f -print
```

Expected before Phase 0 execution:

```text
find: backlog: No such file or directory
```

If `backlog/` already exists, stop and inspect it before initializing. Do not overwrite existing Backlog files.

- [ ] **Step 4: Re-read current source specs**

Run:

```bash
sed -n '1,320p' Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md
sed -n '1,220p' Docs/superpowers/specs/2026-05-02-agentic-terminal-design-system-design.md
sed -n '1,220p' Docs/Design/master-shell-route-inventory.md
sed -n '1,220p' Docs/Design/master-shell-design-system-contract.md
```

Expected:

- Unified Shell maturity tracking spec exists and names the canonical roadmap path.
- Design system spec exists and describes the agentic terminal visual/interaction grammar.
- Route inventory and design-system contract exist on current `dev`.

- [ ] **Step 5: Commit nothing in this task**

This task gathers evidence only. Continue to Task 2 without committing.

## Task 2: Initialize Backlog And Seed Tracking Tasks First

**Files:**
- Create: `backlog/*`
- Possible modify: `AGENTS.md`

- [ ] **Step 1: Initialize Backlog from the clean worktree**

Run:

```bash
backlog init --defaults --agent-instructions agents --integration-mode cli --backlog-dir backlog
```

Expected:

- `backlog/` is created.
- Command completes without interactive prompts.
- Any `AGENTS.md` change is visible in git diff.

- [ ] **Step 2: Validate generated Backlog structure**

Run:

```bash
backlog task list --plain
backlog overview
find backlog -maxdepth 2 -type f | sort
```

Expected:

- `backlog task list --plain` runs without prompting.
- `backlog overview` runs without prompting.
- `find` lists Backlog config and directory files generated by the installed Backlog.md version.

- [ ] **Step 3: Inspect AGENTS.md for duplicate Backlog blocks**

Run:

```bash
grep -n "BACKLOG.MD GUIDELINES" AGENTS.md
git diff -- AGENTS.md
```

Expected:

- `grep` reports one start marker and one end marker.
- If `backlog init` added a duplicate block, edit `AGENTS.md` with `apply_patch` so only one Backlog.md instructions block remains.
- Do not remove non-Backlog project instructions.

- [ ] **Step 4: Create seven phase parent tasks**

Run these commands and record the generated IDs as `<phase-0-id>` through `<phase-6-id>`:

```bash
backlog task create "Phase 0: Canonical Tracking" \
  --description "Make remaining Unified Shell work trackable through a canonical roadmap, Backlog task hierarchy, and durable QA evidence structure." \
  --labels "unified-shell,phase-0,tracking" \
  --priority high \
  --ac "Backlog is initialized and post-init smoke checks pass." \
  --ac "Canonical roadmap exists and links generated Backlog phase/task IDs." \
  --ac "QA evidence structure exists under Docs/superpowers/qa/unified-shell/." \
  --ac "Phase 0 QA summary records command evidence and product-QA boundary." \
  --doc "Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md" \
  --plain

backlog task create "Phase 1: Shell Contract Complete" \
  --description "Remove false shell affordances and prove the shell is navigable, understandable, and usable before deeper service wiring continues." \
  --labels "unified-shell,phase-1,shell-contract,qa" \
  --priority high \
  --ac "Every top-level destination has honest status and action ownership." \
  --ac "Manual shell walkthrough verifies navigation, layout, focus, labels, and primary actions." \
  --ac "Focused automated checks cover the changed shell contract seams." \
  --ac "Durable QA summary exists under Docs/superpowers/qa/unified-shell/phase-1/." \
  --doc "Docs/Design/master-shell-route-inventory.md" \
  --doc "Docs/Design/master-shell-design-system-contract.md" \
  --plain

backlog task create "Phase 2: Home Operational Control" \
  --description "Make Home a real dashboard and control surface for status, notifications, schedules, and active agent work." \
  --labels "unified-shell,phase-2,home,control-surface" \
  --priority high \
  --ac "Home controls route to real services or explicit adapters." \
  --ac "Approve, reject, pause, resume, retry, and open-detail workflows are verified in the running app." \
  --ac "Unavailable states remain honest and recoverable." \
  --ac "Durable QA summary exists under Docs/superpowers/qa/unified-shell/phase-2/." \
  --doc "Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md" \
  --plain

backlog task create "Phase 3: Console Live Work Hub" \
  --description "Make Console the primary live-agent control surface for workflows, schedules, ACP, MCP, RAG, artifacts, and active work." \
  --labels "unified-shell,phase-3,console,agentic-control" \
  --priority high \
  --ac "Console receives live work from the relevant product sources." \
  --ac "Launch, follow, status, and recovery flows are verified in the running app." \
  --ac "Console remains fast for repeated power-user use." \
  --ac "Durable QA summary exists under Docs/superpowers/qa/unified-shell/phase-3/." \
  --doc "Docs/superpowers/specs/2026-05-02-agentic-terminal-design-system-design.md" \
  --plain

backlog task create "Phase 4: Destination Service Adoption" \
  --description "Turn top-level wrappers into useful product surfaces by adopting real services and meaningful list, detail, and action flows where services exist." \
  --labels "unified-shell,phase-4,destinations,service-adoption" \
  --priority high \
  --ac "Each destination supports at least one meaningful end-to-end workflow or an honest recovery state." \
  --ac "Skills, MCP, ACP, Library, Workflows, Schedules, Personas, Artifacts, W+C, and Settings ownership is explicit." \
  --ac "Running-app QA verifies destination workflows rather than render-only behavior." \
  --ac "Durable QA summaries exist under Docs/superpowers/qa/unified-shell/phase-4/." \
  --doc "Docs/Design/master-shell-route-inventory.md" \
  --plain

backlog task create "Phase 5: Capability And Recovery System" \
  --description "Systematize missing dependency, auth, server, runtime, and policy states so blocked workflows are understandable and recoverable." \
  --labels "unified-shell,phase-5,capability-state,recovery" \
  --priority medium \
  --ac "Shared capability-state taxonomy exists and is applied to remaining blocker states." \
  --ac "Blocked states tell users what is unavailable, why, and what to do next." \
  --ac "Running-app QA verifies understandable recovery paths." \
  --ac "Durable QA summaries exist under Docs/superpowers/qa/unified-shell/phase-5/." \
  --doc "Docs/Design/master-shell-design-system-contract.md" \
  --plain

backlog task create "Phase 6: Audit Replay And Closeout" \
  --description "Replay first-time user, power-user, and Nielsen heuristic walkthroughs to prove the Unified Shell is usable end-to-end." \
  --labels "unified-shell,phase-6,audit-replay,nielsen" \
  --priority medium \
  --ac "First-time user walkthrough is replayed against the running app." \
  --ac "Power-user workflows are replayed against the running app." \
  --ac "Nielsen heuristic closeout documents remaining defects and residual risks." \
  --ac "Durable QA summaries exist under Docs/superpowers/qa/unified-shell/phase-6/." \
  --doc "Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md" \
  --plain
```

Expected:

- Seven parent tasks are created.
- IDs are recorded before continuing.

- [ ] **Step 5: Create initial child tasks**

Run with actual generated parent IDs. Use `--dep`, not `--depends-on`, to match the compatibility check.

```bash
backlog task create "Phase 0.1: Initialize Backlog.md and roadmap docs" \
  --parent "<phase-0-id>" \
  --description "Initialize Backlog.md and create the canonical roadmap plus durable QA evidence structure for Unified Shell maturity tracking." \
  --labels "unified-shell,phase-0,tracking,docs" \
  --priority high \
  --ac "Backlog initialization smoke commands pass." \
  --ac "Roadmap file exists and includes phase status, Backlog task links, QA evidence links, known gaps, and update rules." \
  --ac "QA evidence README files exist for phases 0 through 6." \
  --ac "Phase 0 QA summary records validation commands and outputs." \
  --doc "Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md" \
  --plain

backlog task create "Phase 0.2: Reconcile current dev state and merged evidence" \
  --parent "<phase-0-id>" \
  --description "Review current dev, merged shell docs, and prior UX remediation evidence so the roadmap reflects present state instead of stale plans." \
  --labels "unified-shell,phase-0,rebaseline,evidence" \
  --priority high \
  --ac "Roadmap baseline lists current merged shell docs and known product gaps." \
  --ac "Stale local-only plans are not treated as source of truth." \
  --ac "Residual risks and unverified product workflows are explicit." \
  --ac "Phase 0 QA summary records the reconciliation evidence." \
  --doc "Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md" \
  --plain

backlog task create "Phase 1.1: Create shell QA walkthrough harness and evidence template" \
  --parent "<phase-1-id>" \
  --dep "<phase-0-1-id>" \
  --description "Create the repeatable running-app walkthrough protocol and evidence template needed before shell destinations can be marked usable." \
  --labels "unified-shell,phase-1,qa,harness" \
  --priority high \
  --ac "Walkthrough template captures visual, keyboard, click, functional, severity, evidence, and residual-risk fields." \
  --ac "Harness or manual protocol can be run against the actual Textual app." \
  --ac "At least one smoke walkthrough summary is stored under Docs/superpowers/qa/unified-shell/phase-1/." \
  --ac "The roadmap links the harness/template evidence." \
  --doc "Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md" \
  --plain

backlog task create "Phase 1.2: Audit destination action functionality beyond render/click tests" \
  --parent "<phase-1-id>" \
  --dep "<phase-1-1-id>" \
  --description "Audit every top-level Unified Shell destination for real action ownership, honest disabled states, focus behavior, and workflow-level usability." \
  --labels "unified-shell,phase-1,audit,destinations" \
  --priority high \
  --ac "Each top-level destination has an action ownership and usability status recorded." \
  --ac "Findings distinguish working workflows, honest blocked states, and false affordances." \
  --ac "Running-app QA evidence exists under Docs/superpowers/qa/unified-shell/phase-1/." \
  --ac "Follow-on Backlog child tasks are created only for PR-sized fixes." \
  --doc "Docs/Design/master-shell-route-inventory.md" \
  --plain
```

Expected:

- Four child tasks are created.
- Phase 1 child dependencies are accepted by the installed CLI.

- [ ] **Step 6: Put Phase 0 tasks in progress and add implementation plans before docs work**

Run with actual Phase 0 IDs:

```bash
backlog task edit "<phase-0-id>" \
  -s "In Progress" \
  --plan $'1. Initialize Backlog.md safely.\n2. Seed phase parent tasks and initial child tasks.\n3. Create roadmap, Backlog docs pointer, and QA evidence skeleton.\n4. Reconcile task IDs and known product gaps.\n5. Verify commands and close Phase 0.' \
  --plain

backlog task edit "<phase-0-1-id>" \
  -s "In Progress" \
  --plan $'1. Validate Backlog CLI and current worktree.\n2. Initialize Backlog.md.\n3. Seed tracking tasks.\n4. Create roadmap, Backlog pointer, and QA evidence directories.\n5. Run Backlog and git validation commands.\n6. Document verification in Phase 0 QA summary.' \
  --plain

backlog task edit "<phase-0-2-id>" \
  -s "In Progress" \
  --plan $'1. Review current dev shell docs and tracking spec.\n2. Record current merged baseline, tracking gaps, and product gaps in the roadmap.\n3. Link generated Backlog IDs.\n4. Document residual product risks and unverified workflows.\n5. Validate no stale local-only plan is treated as source of truth.' \
  --plain
```

Expected:

- Phase 0 parent and Phase 0 child tasks are `In Progress`.
- Each has an `## Implementation Plan` section before roadmap or QA document work begins.

- [ ] **Step 7: Verify seeded Backlog state**

Run:

```bash
backlog task list --plain
backlog task view "<phase-0-id>" --plain
backlog task view "<phase-0-1-id>" --plain
backlog task view "<phase-0-2-id>" --plain
```

Expected:

- Seven phase parent tasks exist.
- Four initial child tasks exist.
- Child tasks show the correct parent relationship.
- Phase 1 child tasks show dependencies.
- Phase 0 parent and child tasks show `In Progress` and include implementation plans.

- [ ] **Step 8: Run whitespace validation**

Run:

```bash
git diff --check
```

Expected:

```text
```

No output and exit code `0`.

- [ ] **Step 9: Commit Backlog initialization and seeded tasks**

Run:

```bash
git add AGENTS.md backlog
git commit -m "docs: initialize unified shell backlog tracking"
```

Expected:

- Commit succeeds.
- The commit contains Backlog initialization files, task files, and any necessary idempotent `AGENTS.md` cleanup.

## Task 3: Create Roadmap, Backlog Pointer, And QA Evidence Skeleton

**Files:**
- Create: `backlog/docs/unified-shell-maturity-roadmap.md`
- Create: `Docs/superpowers/trackers/unified-shell-maturity-roadmap.md`
- Create: `Docs/superpowers/qa/unified-shell/README.md`
- Create: `Docs/superpowers/qa/unified-shell/phase-0/README.md`
- Create: `Docs/superpowers/qa/unified-shell/phase-1/README.md`
- Create: `Docs/superpowers/qa/unified-shell/phase-2/README.md`
- Create: `Docs/superpowers/qa/unified-shell/phase-3/README.md`
- Create: `Docs/superpowers/qa/unified-shell/phase-4/README.md`
- Create: `Docs/superpowers/qa/unified-shell/phase-5/README.md`
- Create: `Docs/superpowers/qa/unified-shell/phase-6/README.md`

- [ ] **Step 1: Create Backlog docs pointer**

Create `backlog/docs/unified-shell-maturity-roadmap.md` with `apply_patch` after Phase 0 tasks are already `In Progress`:

```markdown
# Unified Shell Maturity Roadmap

The canonical Unified Shell maturity tracker lives at:

`Docs/superpowers/trackers/unified-shell-maturity-roadmap.md`

Durable QA evidence lives at:

`Docs/superpowers/qa/unified-shell/`

Backlog tasks in `backlog/tasks/` are PR-sized execution units. Parent tasks represent maturity phases; child tasks represent implementation or QA slices.

Do not mark a task or phase complete because UI renders or a button is clickable. Completion requires automated evidence, running-app QA evidence where product behavior is in scope, and a repo-tracked QA summary.
```

- [ ] **Step 2: Create the canonical roadmap with actual task IDs and known gaps**

Create `Docs/superpowers/trackers/unified-shell-maturity-roadmap.md` with `apply_patch`. Use the real task IDs recorded in Task 2; do not commit placeholder IDs.

```markdown
# Unified Shell Maturity Roadmap

Date: 2026-05-03
Status: Phase 0 in progress
Source Branch: `origin/dev` at `534cf226` plus `codex/unified-shell-maturity-tracking`

## Purpose

Track remaining Unified Shell work in one place so rendered screens, clickable buttons, and real user workflow completion are not confused with each other.

## Current Verified Baseline

- PR #204 added the master shell UX foundation.
- PR #205 hardened shell action affordances and changed the visible Watchlists/Collections shell label to `W+C`.
- `Docs/Design/master-shell-route-inventory.md` maps top-level shell destinations and deferred surfaces.
- `Docs/Design/master-shell-design-system-contract.md` defines shared shell classes, state labels, and testing hooks.
- `Docs/superpowers/specs/2026-05-02-agentic-terminal-design-system-design.md` defines the agentic terminal visual and interaction grammar.
- `Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md` defines this maturity-gate tracking model.

## Known Tracking Gaps

- The detailed master-shell implementation plan exists locally in the stale root checkout but is not present on `origin/dev`.
- The UX remediation plan is useful history but is no longer the canonical tracker for Unified Shell completion.
- Backlog.md was not initialized before Phase 0.
- Current shell wrappers expose some honest unavailable states rather than complete product workflows.
- Phase-completion evidence was not centralized around manual QA walkthroughs.

## Known Product Gaps

- Home active-work controls still use placeholder notification hooks for approve, reject, pause, resume, and retry.
- Workflows has no wired workflow service in the shell wrapper.
- ACP launch is disabled until an ACP-compatible runtime is configured.
- MCP management is not embedded in the top-level MCP wrapper.
- Skills local/server services exist, but the top-level Skills shell still leaves import disabled and lacks list/detail/import UX adoption.
- Library still links into legacy Notes, Media, Ingest, Search/RAG, and conversation screens rather than fully Library-native views.
- Product workflows remain unverified until running-app QA evidence is added in later phases.

## Definition Of Done

A task or phase is not complete just because UI renders or a control is clickable.

Every completed implementation task must include:

- Automated regression evidence for the changed seam.
- Manual QA walkthrough evidence from the running app.
- Repo-tracked QA summary under `Docs/superpowers/qa/unified-shell/`.
- Visual usability notes for layout, labels, focus, and reachable controls.
- Functional workflow evidence proving the user can complete the relevant task or recover from the blocked state.
- Residual-risk notes for live server/API paths, optional dependencies, or environment limits not exercised.

## Backlog Task Hierarchy

Parent phase tasks:

- Phase 0: Canonical Tracking - `<phase-0-id>`
- Phase 1: Shell Contract Complete - `<phase-1-id>`
- Phase 2: Home Operational Control - `<phase-2-id>`
- Phase 3: Console Live Work Hub - `<phase-3-id>`
- Phase 4: Destination Service Adoption - `<phase-4-id>`
- Phase 5: Capability And Recovery System - `<phase-5-id>`
- Phase 6: Audit Replay And Closeout - `<phase-6-id>`

Initial child tasks:

- Phase 0.1: Initialize Backlog.md and roadmap docs - `<phase-0-1-id>`
- Phase 0.2: Reconcile current dev state and merged evidence - `<phase-0-2-id>`
- Phase 1.1: Create shell QA walkthrough harness and evidence template - `<phase-1-1-id>`
- Phase 1.2: Audit destination action functionality beyond render/click tests - `<phase-1-2-id>`

## QA Evidence Index

| Phase | Evidence Path | Status |
| --- | --- | --- |
| Phase 0 | `Docs/superpowers/qa/unified-shell/phase-0/` | in-progress |
| Phase 1 | `Docs/superpowers/qa/unified-shell/phase-1/` | not-started |
| Phase 2 | `Docs/superpowers/qa/unified-shell/phase-2/` | not-started |
| Phase 3 | `Docs/superpowers/qa/unified-shell/phase-3/` | not-started |
| Phase 4 | `Docs/superpowers/qa/unified-shell/phase-4/` | not-started |
| Phase 5 | `Docs/superpowers/qa/unified-shell/phase-5/` | not-started |
| Phase 6 | `Docs/superpowers/qa/unified-shell/phase-6/` | not-started |

## Phase Overview

| Phase | Goal | Status | Backlog Tasks | QA Evidence | Residual Risk |
| --- | --- | --- | --- | --- | --- |
| Phase 0: Canonical Tracking | Make remaining work trackable. | in-progress | `<phase-0-id>` | `phase-0/` | Product UI workflows are out of scope for Phase 0. |
| Phase 1: Shell Contract Complete | Remove false shell affordances and prove shell usability. | not-started | `<phase-1-id>` | `phase-1/` | App walkthrough harness not yet created. |
| Phase 2: Home Operational Control | Make Home a real dashboard/control surface. | not-started | `<phase-2-id>` | `phase-2/` | Home action adapters still need design and implementation. |
| Phase 3: Console Live Work Hub | Make Console the live-agent control surface. | not-started | `<phase-3-id>` | `phase-3/` | Live work event sources need explicit contracts. |
| Phase 4: Destination Service Adoption | Turn wrappers into useful product surfaces. | not-started | `<phase-4-id>` | `phase-4/` | Service coverage varies by destination. |
| Phase 5: Capability And Recovery System | Systematize unavailable and blocked states. | not-started | `<phase-5-id>` | `phase-5/` | Shared taxonomy not yet extracted. |
| Phase 6: Audit Replay And Closeout | Prove shell works for first-time and power users. | not-started | `<phase-6-id>` | `phase-6/` | Depends on prior phases and running-app QA. |

## QA Walkthrough Protocol

Each QA walkthrough must record:

- Environment: branch, commit, Python version, runtime source, config/home directory.
- Entry path: clean Home launch, direct route, command palette, or destination link.
- Visual check: layout, clipping, focus indication, labels, disabled states, and information hierarchy.
- Keyboard path: whether the workflow can be completed without mouse-only assumptions.
- Mouse/click path: whether clickable elements behave as promised.
- Functional result: completed workflow, blocked workflow with recovery, or defect.
- Defect severity: `blocker`, `workflow-degradation`, `recoverability`, or `polish`.
- Evidence: screenshots, logs, probe JSON, or concise notes linked from the roadmap.
- Residual risk: untested live server/API/dependency paths.

## Update Rules

- Roadmap changes must happen through PRs.
- Backlog task IDs must be linked from this roadmap.
- Backlog tasks must not be marked `Done` until acceptance criteria, automated verification, implementation notes, and QA walkthrough evidence are complete.
- Phase status must be one of: `not-started`, `in-progress`, `implemented`, `qa-needed`, `qa-failed`, `verified`, or `blocked`.
- `implemented` means code or docs landed but app-level QA has not passed.
- `qa-needed` means automated checks passed and manual walkthrough is pending.
- `qa-failed` means the running app walkthrough found a blocker or workflow-degradation issue.
- `verified` means both automated checks and manual QA evidence are complete.
- Render-only, mount-only, and click-event-only tests do not count as workflow completion.
- Preserve historical evidence without pretending stale plans are current.
```

- [ ] **Step 3: Verify no placeholder IDs remain in the roadmap**

Run:

```bash
if grep -R "<phase-[0-9]" Docs/superpowers/trackers/unified-shell-maturity-roadmap.md; then
  echo "Unexpected unresolved phase ID placeholders"
  exit 1
fi
```

Expected:

- No placeholder IDs remain.

- [ ] **Step 4: Add the QA evidence root README**

Create `Docs/superpowers/qa/unified-shell/README.md`:

```markdown
# Unified Shell QA Evidence

This directory stores durable QA summaries for Unified Shell maturity work.

Large screenshots, videos, temporary probe files, and logs may live outside git, but each task or phase must include a concise markdown summary here that records what was verified, what failed, and what remains uncertain.

## Phase Index

- `phase-0/` - Canonical tracking setup evidence.
- `phase-1/` - Shell contract and destination affordance walkthroughs.
- `phase-2/` - Home dashboard and active-work control walkthroughs.
- `phase-3/` - Console live-work hub walkthroughs.
- `phase-4/` - Destination service adoption walkthroughs.
- `phase-5/` - Capability-state and recovery walkthroughs.
- `phase-6/` - First-time user, power-user, and Nielsen heuristic closeout.

## Required Summary Fields

- Date
- Branch and commit
- Task or phase
- Environment
- Entry path
- Steps attempted
- Visual usability notes
- Keyboard path result
- Mouse/click path result
- Functional result
- Defect severity
- Evidence links or artifact paths
- Residual risk
```

- [ ] **Step 5: Add Phase 0 QA README**

Create `Docs/superpowers/qa/unified-shell/phase-0/README.md`:

```markdown
# Phase 0 QA Evidence

Phase 0 validates the tracking system, not product UI behavior.

## Current Summary

- Date: 2026-05-03
- Branch: `codex/unified-shell-maturity-tracking`
- Status: in-progress
- Scope: Backlog initialization, task seeding, roadmap creation, Backlog docs pointer, QA evidence structure, and current-state reconciliation.

## Verification Log

Add command output summaries here during execution:

- `backlog task list --plain`
- `backlog overview`
- `find backlog -maxdepth 2 -type f | sort`
- `find Docs/superpowers/qa/unified-shell -maxdepth 2 -type f | sort`
- `git diff --check`

## Reconciliation Summary

Record the merged shell foundation, known tracking gaps, known product gaps, and any residual risk from optional dependencies or untested live services.

## Product QA Boundary

No Textual UI code changes are made in Phase 0. Product workflows remain unverified until Phase 1 and later running-app walkthroughs.
```

- [ ] **Step 6: Add phase placeholder README files**

Create the six remaining phase README files. Use this template, replacing the phase number and title:

```markdown
# Phase N QA Evidence

Status: not-started

This directory will contain durable QA summaries for this phase. Do not mark the phase verified until running-app walkthrough evidence exists here.
```

- [ ] **Step 7: Validate docs were created**

Run:

```bash
test -f backlog/docs/unified-shell-maturity-roadmap.md
test -f Docs/superpowers/trackers/unified-shell-maturity-roadmap.md
find Docs/superpowers/qa/unified-shell -maxdepth 2 -type f | sort
sed -n '1,320p' Docs/superpowers/trackers/unified-shell-maturity-roadmap.md
```

Expected:

- The Backlog docs pointer exists.
- The QA root README and seven phase README files exist.
- The roadmap contains all required sections from the spec, including known tracking gaps and known product gaps.

- [ ] **Step 8: Update Backlog task docs references to include the canonical roadmap**

Run with actual Phase 0 IDs:

```bash
backlog task edit "<phase-0-id>" \
  --doc "Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md" \
  --doc "Docs/superpowers/trackers/unified-shell-maturity-roadmap.md" \
  --doc "backlog/docs/unified-shell-maturity-roadmap.md" \
  --plain

backlog task edit "<phase-0-1-id>" \
  --doc "Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md" \
  --doc "Docs/superpowers/trackers/unified-shell-maturity-roadmap.md" \
  --doc "backlog/docs/unified-shell-maturity-roadmap.md" \
  --plain

backlog task edit "<phase-0-2-id>" \
  --doc "Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md" \
  --doc "Docs/superpowers/trackers/unified-shell-maturity-roadmap.md" \
  --doc "backlog/docs/unified-shell-maturity-roadmap.md" \
  --plain
```

Expected:

- Phase 0 tasks now link both the design spec and the canonical roadmap/pointer docs.

- [ ] **Step 9: Run whitespace validation**

Run:

```bash
git diff --check
```

Expected:

```text
```

No output and exit code `0`.

- [ ] **Step 10: Commit roadmap, pointer, and QA skeleton**

Run:

```bash
git add backlog/tasks backlog/docs/unified-shell-maturity-roadmap.md Docs/superpowers/trackers/unified-shell-maturity-roadmap.md Docs/superpowers/qa/unified-shell
git commit -m "docs: add unified shell maturity roadmap"
```

Expected:

- Commit succeeds.
- The commit contains the Backlog docs pointer, roadmap, QA evidence skeleton, and updated Phase 0 task references.

## Task 4: Verify Phase 0 And Close Completed Backlog Tasks

**Files:**
- Modify: `backlog/tasks/*`
- Modify: `Docs/superpowers/trackers/unified-shell-maturity-roadmap.md`
- Modify: `Docs/superpowers/qa/unified-shell/phase-0/README.md`

- [ ] **Step 1: Run Phase 0 verification commands**

Run:

```bash
backlog task list --plain
backlog overview
find backlog -maxdepth 2 -type f | sort
find Docs/superpowers/qa/unified-shell -maxdepth 2 -type f | sort
if grep -R "<phase-[0-9]" Docs/superpowers/trackers/unified-shell-maturity-roadmap.md; then
  echo "Unexpected unresolved phase ID placeholders"
  exit 1
fi
git diff --check
```

Expected:

- Backlog list shows the seeded hierarchy.
- Backlog overview runs without prompts.
- Backlog docs pointer exists under `backlog/docs/`.
- QA files are listed.
- No placeholder IDs remain in the roadmap.
- `git diff --check` exits `0`.

- [ ] **Step 2: Update Phase 0 QA summary with evidence**

Edit `Docs/superpowers/qa/unified-shell/phase-0/README.md` and add:

- Backlog init command result.
- `backlog task list --plain` summary.
- `backlog overview` summary.
- `find backlog -maxdepth 2 -type f | sort` summary.
- Roadmap task-ID reconciliation result.
- Known tracking gaps and known product gaps captured.
- Product QA boundary statement.
- Residual risks.

- [ ] **Step 3: Update roadmap Phase 0 status**

Edit `Docs/superpowers/trackers/unified-shell-maturity-roadmap.md`:

- Set document status to `Phase 0 verified`.
- Set Phase 0 evidence status to `verified`.
- Set Phase 0 row status to `verified`.
- Keep Phase 1 through Phase 6 as `not-started`.
- Preserve the product QA boundary: Phase 0 does not verify product UI workflows.

- [ ] **Step 4: Close completed Phase 0 Backlog tasks**

Run with actual Phase 0 IDs:

```bash
backlog task edit "<phase-0-1-id>" \
  --check-ac 1 \
  --check-ac 2 \
  --check-ac 3 \
  --check-ac 4 \
  --notes $'Initialized Backlog.md, created the canonical roadmap and Backlog docs pointer, created the QA evidence structure, validated Backlog smoke commands, and recorded Phase 0 tracking evidence. Product UI workflows were not verified in this docs-only slice.' \
  -s Done \
  --plain

backlog task edit "<phase-0-2-id>" \
  --check-ac 1 \
  --check-ac 2 \
  --check-ac 3 \
  --check-ac 4 \
  --notes $'Reconciled the roadmap against current dev shell docs, the Unified Shell maturity tracking spec, known tracking gaps, and known product gaps. The roadmap explicitly separates merged foundation work from unverified product workflows.' \
  -s Done \
  --plain

backlog task edit "<phase-0-id>" \
  --check-ac 1 \
  --check-ac 2 \
  --check-ac 3 \
  --check-ac 4 \
  --notes $'Phase 0 tracking foundation is complete: Backlog.md is initialized, phase parent tasks and initial child tasks exist, the roadmap links generated task IDs, the Backlog docs pointer exists, and durable QA evidence directories are present. Product shell workflows remain unverified until later phases.' \
  -s Done \
  --plain
```

Expected:

- Phase 0 parent, Phase 0.1, and Phase 0.2 are marked `Done`.
- All acceptance criteria on those tasks are checked.
- Each completed task has implementation notes.
- Phase 1 parent and children remain open for later implementation.

- [ ] **Step 5: Re-run Backlog closeout verification**

Run:

```bash
backlog task list --plain
backlog task view "<phase-0-id>" --plain
backlog task view "<phase-0-1-id>" --plain
backlog task view "<phase-0-2-id>" --plain
git diff --check
```

Expected:

- Phase 0 parent and Phase 0 child tasks are `Done`.
- Phase 1 and later phase tasks are not incorrectly marked complete.
- Completed task files include checked ACs, implementation plans, and implementation notes.
- `git diff --check` exits `0`.

- [ ] **Step 6: Commit Phase 0 closeout**

Run:

```bash
git add backlog/tasks Docs/superpowers/trackers/unified-shell-maturity-roadmap.md Docs/superpowers/qa/unified-shell/phase-0/README.md
git commit -m "docs: close unified shell tracking phase zero"
```

Expected:

- Commit succeeds.
- Commit contains Phase 0 task closeout plus roadmap/QA status updates.

## Task 5: Final Verification And PR-Ready Closeout

**Files:**
- Read: `backlog/tasks/*`
- Read: `backlog/docs/unified-shell-maturity-roadmap.md`
- Read: `Docs/superpowers/trackers/unified-shell-maturity-roadmap.md`
- Read: `Docs/superpowers/qa/unified-shell/*/README.md`

- [ ] **Step 1: Verify Backlog CLI reads**

Run:

```bash
backlog task list --plain
backlog overview
```

Expected:

- Both commands complete without interactive prompts.
- Overview reflects initialized project state.

- [ ] **Step 2: Verify all required docs exist**

Run:

```bash
test -f backlog/docs/unified-shell-maturity-roadmap.md
test -f Docs/superpowers/trackers/unified-shell-maturity-roadmap.md
test -f Docs/superpowers/qa/unified-shell/README.md
test -f Docs/superpowers/qa/unified-shell/phase-0/README.md
test -f Docs/superpowers/qa/unified-shell/phase-1/README.md
test -f Docs/superpowers/qa/unified-shell/phase-2/README.md
test -f Docs/superpowers/qa/unified-shell/phase-3/README.md
test -f Docs/superpowers/qa/unified-shell/phase-4/README.md
test -f Docs/superpowers/qa/unified-shell/phase-5/README.md
test -f Docs/superpowers/qa/unified-shell/phase-6/README.md
```

Expected:

- All commands exit `0`.

- [ ] **Step 3: Verify no unresolved placeholders remain**

Run:

```bash
if grep -R "<phase-[0-9]\\|TASK-ID-TBD\\|TODO-REPLACE" Docs/superpowers/trackers/unified-shell-maturity-roadmap.md Docs/superpowers/qa/unified-shell backlog/tasks backlog/docs; then
  echo "Unexpected unresolved placeholders found"
  exit 1
fi
```

Expected:

- No unresolved task ID placeholders remain.

- [ ] **Step 4: Verify no product files changed**

Run:

```bash
git diff --name-only origin/dev...HEAD
```

Expected:

- Changed files are limited to `Docs/superpowers/specs/`, `Docs/superpowers/plans/`, `Docs/superpowers/trackers/`, `Docs/superpowers/qa/`, `backlog/`, and possibly `AGENTS.md`.
- No `tldw_chatbook/`, `UI/`, `Widgets/`, or `Tests/` product/test code appears in this Phase 0 PR.

- [ ] **Step 5: Run final whitespace validation**

Run:

```bash
git diff --check origin/dev...HEAD
```

Expected:

```text
```

No output and exit code `0`.

- [ ] **Step 6: Confirm git status is clean**

Run:

```bash
git status --short --branch
```

Expected:

- Branch is ahead of `origin/dev`.
- No unstaged or untracked files remain.

- [ ] **Step 7: Prepare PR summary**

Use this summary:

```markdown
## Summary

- Adds the Unified Shell maturity tracking design and Phase 0 implementation plan.
- Initializes Backlog.md tracking and seeds phase parent tasks plus initial Phase 0/1 child tasks.
- Adds the canonical roadmap, Backlog docs pointer, and durable QA evidence structure so later phases require running-app workflow evidence before being marked complete.

## Verification

- `backlog task list --plain`
- `backlog overview`
- `find backlog -maxdepth 2 -type f | sort`
- `find Docs/superpowers/qa/unified-shell -maxdepth 2 -type f | sort`
- `git diff --check origin/dev...HEAD`

## Product QA Boundary

Phase 0 changes tracking/docs only. It does not verify Textual product workflows. Later phases must include running-app QA evidence before any shell workflow is marked usable.
```

## Risks And Guardrails

- Backlog.md generated files may vary by installed version; inspect generated structure instead of hardcoding exact file names beyond `backlog/`.
- `backlog init --agent-instructions agents` may touch `AGENTS.md`; keep only one Backlog.md instructions block.
- Do not create roadmap or QA evidence docs until Phase 0 Backlog tasks are created, moved `In Progress`, and given implementation plans.
- Do not mark Phase 0 child tasks `Done` until their AC checkboxes, implementation notes, command evidence, and QA summary are complete.
- Do not create detailed Phase 2-6 child tasks yet. Later child tasks should be created only when each slice is small enough for one PR.
- Do not use this Phase 0 PR to claim that Home, Console, Skills, MCP, ACP, Library, Workflows, Schedules, or destination wrappers are usable.

## Review Check

Before execution, review this plan against:

- `Docs/superpowers/specs/2026-05-03-unified-shell-maturity-tracking-design.md`
- `AGENTS.md`
- Backlog.md CLI help output for the installed version

If the user explicitly asks for a plan-review subagent, run that review at the requested reasoning level before executing. Otherwise, perform an inline self-review and keep the plan PR-sized.
