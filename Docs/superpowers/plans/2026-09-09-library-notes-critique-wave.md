# Library ▸ Notes critique fix wave Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the 22 actionable findings of the Library ▸ Notes critique (tasks 32121–32142; the four idea tasks 32143–32146 wait for a design agreement) in eight branches that each ship as their own PR against `dev`.

**Architecture:** Each task below is one branch/worktree/PR, grouped by file overlap so the seven code groups can run in parallel; the docs group runs last. Work stays inside the Notes surface: `tldw_chatbook/Widgets/Library/library_notes_canvas.py`, `library_notes_add_from_files_canvas.py`, `library_file_notes_workspace.py`, `enhanced_file_picker.py`, `Third_Party/textual_fspicker/file_open.py`, `UI/Library_Modules/library_notes_controller.py`, `library_note_import_controller.py`, `Library/library_notes_state.py`, `library_note_import_state.py`, `Notes/note_import_*.py`, `UI/Screens/library_screen.py`, the split Library TCSS, and `Docs/User_Guide/library/{notes,file-notes}.md`. Behaviour changes are test-first against the existing Library UI harnesses; every group ends with a live tmux check on its own isolated scratch profile.

**Tech Stack:** Python 3.12, Textual 8.x, pytest, Backlog.md CLI, tmux.

**Spec:** `.impeccable/critique/2026-09-09T04-24-50Z__w-chatbook-widgets-library-library-notes-canvas-py.md` (the critique snapshot; its Priority Issues and docs-vs-live table are the binding requirements) and the task files `backlog/tasks/task-32121 … task-32142` (acceptance criteria). Two user decisions from 2026-09-09 bind this plan: Folder files is a **mode of Notes** (rail and source strip survive the switch), and Import once gets an **Obsidian mode** (adopt, not just copy).

## Global Constraints

- `SCRATCH` = `/private/tmp/claude-501/-Users-macbook-dev-Documents-GitHub-tldw-chatbook/ebe96959-a4f7-4839-8522-01876feccafb/scratchpad`. Work only inside your assigned worktree `SCRATCH/wave/<group>` (branch `fix/library-notes-<group>`, stacked on `docs/library-notes-critique-2026-09-08`, so the task files exist there). Every shell command starts with `cd SCRATCH/wave/<group> &&` because the shell cwd resets between calls. Never touch the main checkout `/Users/macbook-dev/Documents/GitHub/tldw_chatbook` or another group's worktree.
- Python: `/Users/macbook-dev/Documents/GitHub/tldw_chatbook/.venv/bin/python` (3.12). Run pytest FROM the worktree cwd: `cd <worktree> && …/.venv/bin/python -m pytest <file or node id> -q -p no:cacheprovider`. Never run the whole suite (over an hour) and never use `-k` as verification; run whole files or explicit node ids. `Tests/UI/test_library_shell.py` has hundreds of pre-existing failures on dev: if you must run it, compare failing NAME sets before and after your change (run it once against `git stash`-free HEAD by checking out `origin/dev` in a throwaway `git worktree add --detach`), and report only new names.
- TDD: write the failing test, run it and show it failing, implement, run it passing. New tests go in the most specific existing `Tests/UI/test_library_notes_*.py` / `Tests/Widgets/test_enhanced_*.py` / `Tests/Notes/test_note_import_*.py` file for the area, or a new `Tests/UI/test_library_notes_wave_<group>.py`; never into the 19k-line `test_library_shell.py`.
- Smallest diff that satisfies the acceptance criteria. No new abstractions, no config knobs for values that never change, no speculative extras. If the fix belongs in a shared function, fix it once there (grep every caller first) rather than in the one path the task names.
- CSS: edit the component source under `tldw_chatbook/css/components/` (Library rules live in `_agentic_terminal.tcss`), then run `cd <worktree> && …/.venv/bin/python -m tldw_chatbook.css.build_css` and commit the regenerated bundle files alongside. Widget `DEFAULT_CSS`/`BUNDLED_CSS` must parse standalone and never use ancestor-scoped bare-type subject rules (`Foo > Vertical`). A changed `BUNDLED_CSS` also needs the rebuilt `widget_defaults_{self,scoped}.tcss` committed.
- Git: stage explicit paths only (never `git add -A`); one commit per task id after its green step; do NOT push and do NOT open or merge PRs (the controller does that after review). NEVER run a bare `git stash` (the stash stack is shared across worktrees and a previous wave lost another group's edits that way); if you need to park work, commit it. Commit trailer: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Backlog hygiene from the worktree root: `backlog task edit <id> -s "In Progress" --plan "<steps>"` before the first code change; at the end tick every AC (`- [x]`) you satisfied by editing the task file, then `backlog task edit <id> -s Done --notes "<Implementation Notes>"` (the `--notes` flag REPLACES the notes section; write it once, at the end). Leave an AC unticked with a note if you could not satisfy it.
- Docs: every user-visible change updates `Docs/User_Guide/library/notes.md` or `file-notes.md` and appends a `*Verified against fix/library-notes-<group> — 2026-09-09 (task-NNNNN: …)*` stamp in the page's existing stamp style. Only the docs group (Task 8) does the whole-page sweep.
- Live verification (required before reporting): `SCRATCH/notes-crit/launch_wave.sh <group> <fresh|power> [cols rows]` launches the app from YOUR worktree on YOUR profile in tmux socket `nw-<group>`; `sleep 15`; drive with `tmux -L nw-<group> send-keys`; observe with `capture-pane -p` (`-e` for colour). Profiles: `SCRATCH/notes-crit/wave/<group>/power` (seeded: 10 notes incl. a 35 KB one and two titled "Reading list", 11 media, 6 conversations; `vault/` is a 179-file Obsidian-style vault with a git baseline; `file_notes/` holds 3 files) and `…/fresh` (empty; first launch shows the setup wizard: `Escape`, `Tab`, `Enter` skips it; a model-catalog modal may follow: `Escape`). Reach Library with `C-p`, type `Switch to Library`, `Down`, `Enter` (Ctrl+digit cannot be sent through tmux; `C-n` can). Click a rail row by anchoring on `^│ │    Notes (` in a capture (a bare `grep "Notes ("` matches the landing canvas first). Mouse clicks: `send-keys -l $'\x1b[<0;COL;ROWM'` then `…ROWm`; clicks reach rail rows, Inputs, Trees and list rows but often not Buttons in the middle pane (focus a nearby Input, then Tab/Enter). In EVERY folder picker, type the path, press **Enter**, THEN Select; Select alone commits the directory being browsed (that is task 32122's bug). Quit with `C-q` then `tmux -L nw-<group> kill-server`. One instance per profile at a time. Save every capture you cite to `SCRATCH/notes-crit/wave/<group>/caps/NN-name.txt`.
- Copy rules: blocked or disabled states carry a text reason and a next step on the same line; never colour-only meaning; no raw errno, UUID or ISO timestamp reaches the user.
- Scope: implement the acceptance criteria of your tasks and nothing else. If an AC needs a product decision you cannot make, implement the rest, leave that AC unticked, and say so in the report.

---

### Task 1: Folder files scan poisoning and Folder files as a mode (group `file-notes`, tasks 32121, 32136)

**Files:**
- Modify: `tldw_chatbook/Widgets/Library/library_file_notes_workspace.py` (`ROOT_CHANGE_TIMEOUT_SECONDS` / `ROOT_CHANGE_TIMEOUT_COPY` ~258–262; `set_root` ~5221: `asyncio.to_thread(self._canonical_root, path)`, `FileNotesService(canonical, self._replica, operation_lock=self._service_lock)`, `await asyncio.to_thread(service.scan)`; `_change_root_with_deadline` ~5919; `_abandon_root_change_task` ~5997; `_abandon_root_change`; `_end_structural_wait` ~5985 (calls `_update_root_surface()` + `_update_controls()`); `_set_action_status` ~4631 writes `#file-notes-action-status`; `_choose_root` opens `SelectDirectory(Path.home())`; the busy row that paints `#library-structural-wait-cancel` beside a second `Cancel`)
- Modify: `tldw_chatbook/Notes/file_notes_service.py` (`scan` — needs a cooperative cancel check per directory)
- Modify: `tldw_chatbook/UI/Screens/library_screen.py` (`_return_to_library_database_notes` ~19931; the Folder files canvas swap around the `Opening File Notes…` sites ~13528/13946; the `‹ Library / Notes` cue)
- Modify: `tldw_chatbook/Widgets/Library/library_notes_canvas.py` (source strip `Library notes | Folder files` and the mode blurb ~708–715)
- Test: `Tests/UI/test_library_crit8_waits.py` (extend), `Tests/UI/test_library_file_notes_workspace.py` (extend), `Tests/Library/test_library_structural_wait.py`, new `Tests/UI/test_library_notes_wave_file_notes.py`
- Docs: `Docs/User_Guide/library/file-notes.md`, `notes.md` (layout tour)

**Interfaces:**
- Consumes: `StructuralWait` (`tldw_chatbook/Library/library_structural_wait.py`, task-32055) with `status_line(now, patience=3.0)`, `request_cancel()`; `FileNotesService.scan()` runs on a thread under `operation_lock`.
- Produces: `FileNotesService.scan(*, should_cancel: Callable[[], bool] | None = None)` that raises `ScanCancelled` (new, in `file_notes_service.py`) when the callback returns True between directories; `LibraryFileNotesWorkspace._root_scan_cancel_event: threading.Event` set by `_abandon_root_change_task`; the timeout copy visible in `#file-notes-action-status` until the next root action.

- [ ] **32121 step 1: failing test — an abandoned scan must not block the next change.** In `Tests/UI/test_library_crit8_waits.py` (copy its existing monkeypatched-timeout fixture), give the workspace a fake `FileNotesService` whose `scan` blocks on a `threading.Event` for root A and returns immediately for root B; call `_root_selected(A)`, let the 0.4 s test deadline expire, then `_root_selected(B)`; assert within 2 s that `self._root == B` and the status reads `Linked · Local folder: B`. Today this fails because B's `set_root` waits on `_service_lock` behind A's thread (PROVEN live ×3 on dev c4a7b1911f).
- [ ] **32121 step 2: implement.** Add `should_cancel` to `FileNotesService.scan` (check between directory entries; raise `ScanCancelled`), create a `threading.Event` per root change in `_change_root_with_deadline`, pass `lambda: event.is_set()` into `scan`, and set the event in `_abandon_root_change_task` (timeout and Cancel both route there). Because the lock is still held by the abandoned thread until it observes the cancel, also make the next `set_root` acquire the service lock with a bounded wait (`operation_lock.acquire(timeout=…)` inside the thread) so it can never hang past the deadline; when it cannot acquire, report the timeout copy rather than waiting silently.
- [ ] **32121 step 3: failing test — the timeout copy paints and stays.** Assert after the deadline that `#file-notes-action-status` renders `Folder change timed out · previous folder kept. Try again or choose a different folder.` and that it is still there after `_update_root_surface()` runs once more. Today `_abandon_root_change` calls `_end_structural_wait` (which repaints the surface) and then `_set_action_status`, and the outer `finally` repaints again; live sampling at 0.5 s never showed the copy. Fix the ordering so the reason survives the final repaint (paint the reason from `_action_detail` inside `_update_root_surface`, or set it after the last `_end_structural_wait`).
- [ ] **32121 step 4: progress and the choice after 3 s.** Extend the wait's status line while a scan runs past `patience`: `Changing folder… · 1,240 entries so far · Keep waiting · Choose another` where the count comes from a `scan` progress callback (`on_progress(count)` every 200 entries); `Keep waiting` resets the deadline once; `Choose another` abandons and opens the picker. Never render two controls labelled `Cancel` in the busy row (the second one is the existing folder-row Cancel; hide it while a structural wait owns the row). Test the copy through the existing waits test file.
- [ ] **32136 step 1: failing compose test — Folder files keeps the rail and the strip.** New `Tests/UI/test_library_notes_wave_file_notes.py`: mount the Library screen at (235, 52) with the seeded profile pattern used in `Tests/UI/test_library_notes_files_sync_journey.py`, switch the source strip to Folder files, and assert the rail (`#library-rail`, or whatever id `library_rail.py` mounts) and the strip (`Library notes | Folder files`) are still visible and the cue is not the only way back.
- [ ] **32136 step 2: implement** by keeping the Notes canvas shell (strip + rail) mounted and swapping only the work area to the File Notes workspace at wide sizes; the compact single-stage behaviour (below 120 columns) is unchanged. Empty state: replace `Choose a notes folder.` with one line `Folder files edits Markdown files in a folder on disk, in place. Nothing is copied into the Library.` plus, when `[notes] sync_directory` is set in config, a second button `Use <folder name>` that links it.
- [ ] **Live-verify** on the power profile (socket `nw-file-notes`): (a) Folder files → Choose folder → type `SCRATCH/notes-crit/wave/file-notes/power/vault`, Enter, Select → `Linked · Local folder: vault` within 5 s; (b) Change… → `/Users/macbook-dev` (type, Enter, Select) → after 3 s the entry count and the two choices appear; after 30 s the timeout copy is painted and stays; (c) Change… → the vault again → links within 5 s (this is the poisoning check; capture every step); (d) the rail and strip are visible inside Folder files at 235x52. Captures to `SCRATCH/notes-crit/wave/file-notes/caps/`; docs stamps; backlog; one commit per task id.

### Task 2: Folder pickers commit the typed path (group `pickers`, task 32122)

**Files:**
- Modify: `tldw_chatbook/Widgets/enhanced_file_picker.py` (`EnhancedSelectDirectory` ~2476–2640: `_input_bar` mounts `#dir-path-input` with placeholder `Type path or select below`; `_on_dir_path_submit` navigates on Enter; `_select_viewed_directory` dismisses with `self._dir_nav().location`; `_shortcut_hint_text`)
- Modify: `tldw_chatbook/Third_Party/textual_fspicker/file_open.py` (`offer_select_folder` ~35–67; the `Select folder` button `#select-current-folder` ~327; the two dismiss sites ~431 and ~444 that return `DirectoryNavigation.location`; the `File name` input)
- Modify (call sites, copy only): `tldw_chatbook/UI/Screens/library_screen.py` `_push_library_note_import_picker` ~25340 (`FileOpen(title="Import once (files or one folder)", offer_select_folder=True)`), `tldw_chatbook/UI/Library_Modules/library_notes_controller.py:4433` (`Choose a folder to keep synced`), `tldw_chatbook/UI/Library_Modules/library_ingest_controller.py:1932`; the confirmation copy `1 folder selected: <basename>` in `tldw_chatbook/Widgets/Library/library_notes_add_from_files_canvas.py` (grep `folder selected`)
- Test: `Tests/Widgets/test_enhanced_select_directory.py` (extend), `Tests/Widgets/test_file_picker_start_dir.py` (extend), new `Tests/Widgets/test_file_open_select_folder.py`
- Docs: `notes.md` (Import once), `file-notes.md` (Folder link)

**Interfaces:**
- Produces: in both dialogs, the select action resolves the path field first: `_resolve_typed_directory(value) -> Path | str` returning a directory or an error message (`Path not found: <value>` / `Not a directory: <name>`); on error the dialog stays open, shows the message in its existing error slot, and refocuses the field. The dismiss value is always an absolute resolved `Path`.

- [ ] **Step 1: failing tests (both dialogs).** `EnhancedSelectDirectory`: type `<tmp>/vault` into `#dir-path-input` (no Enter), press `#select`; assert the dialog dismissed with `<tmp>/vault`, not the browsed directory. Same test with a nonexistent path: assert not dismissed and the error text `Path not found: …` is rendered. `FileOpen(offer_select_folder=True)`: type a directory path into the file-name input, press `#select-current-folder`; assert dismissed with that directory.
- [ ] **Step 2: implement** the shared resolution (one helper each dialog can call; do not duplicate the validation block — `_on_dir_path_submit` already has it, extract it), label the field `Folder path` in folder mode (`FileOpen` when `offer_select_folder` and the typed value is a directory; `EnhancedSelectDirectory` always), keep `Enter` navigating.
- [ ] **Step 3: confirmation and hint.** The Import once and Keep-synced canvases show the absolute path (`1 folder selected: /…/vault`, middle-elided to the pane width with the basename intact) — grep `folder selected` in the add-from-files canvas and the ingest canvas. The picker's hint line (`Enter Open · Select use this folder`) must be visible at 235x52 and 100x30: find why it was absent in every capture (likely hidden by `?` toggle default or clipped) and make it render by default.
- [ ] **Step 4: start location.** Both pickers open at the last-used directory for their context, else `Path.home()`; the Import once picker currently opens at the process cwd. Use the existing per-context remembered start directory (`context=` in `EnhancedFileDialog`).
- [ ] **Live-verify** on the power profile (socket `nw-pickers`): Notes → Add from files… → Import once → type the vault path → Select folder (NO Enter) → `1 folder selected: /…/vault`; a bad path → inline error, dialog stays. Folder files → Choose folder → type the vault path → Select (no Enter) → linked. Captures; docs stamps; backlog; commit.

### Task 3: Notes list geometry, receipt, Undo, Sort, ages (group `list`, tasks 32123, 32124, 32127, 32128, 32137)

**Files:**
- Modify: `tldw_chatbook/Widgets/Library/library_notes_canvas.py` (delete receipt: `ellipsize_note_title_cells(receipt.title or "Untitled", 18 if self.compact else 42)` inside `Horizontal(id="library-notes-delete-receipt")` with `#library-notes-delete-undo` / `#library-notes-delete-receipt-dismiss`; `_compose_tree_rows` ~945+ (rows have no age); `_compose_list` ~919–927 (the two-line title+age path, dead once a tree projection exists); the Sort strip (grep `Sort:` / `Newest`); the toolbar rows)
- Modify: `tldw_chatbook/UI/Library_Modules/library_notes_controller.py` (`_undo_library_note_delete` ~5019: appends the restored record via `_append_library_note_source_record` then `_sync_library_canvas(self, "notes", then=finish)`; `_build_library_notes_tree_projection`; `sort_mode` ~3069 and the `{"newest","oldest","title"}` guard ~4252)
- Modify: `tldw_chatbook/Library/library_notes_tree_state.py` (tree row model: add `age_label`), `tldw_chatbook/Library/library_notes_state.py:494` (`age_label=format_console_relative_age(raw, now=now)` already exists for flat rows)
- Modify: `tldw_chatbook/css/components/_agentic_terminal.tcss` (the Notes list pane width inside the three-pane shell; grep `library-notes` and the adaptive reader shell ids in `tldw_chatbook/Widgets/Library/library_adaptive_reader_shell.py`)
- Test: `Tests/UI/test_library_notes_folder_navigator.py` (extend), `Tests/UI/test_library_notes_rename_propagation_t31796.py:254` (`test_placement_title_sort_key_matches_repository_tiebreakers` — reconcile, do not delete blindly), `Tests/UI/test_library_crit8_polish_shell.py` (extend), new `Tests/UI/test_library_notes_wave_list.py`
- Docs: `notes.md` (Notes list, Layout tour)

**Interfaces:**
- Consumes: `LibraryNoteDeleteReceipt` (title, note_id, expected_version); `NotesScopeService.restore_note`.
- Produces: tree rows carry `age_label: str`; the receipt row composes its buttons on a second line when `pane_width < 60`; `sort_mode` either reorders placements within each folder (newest/oldest by `last_modified`, title) or the control is gone from the tree view (record which in the task notes; the spec allows either, the user's critique prefers a working control).

- [ ] **32123: failing geometry test** — mount the Notes canvas at a 38-column list pane with a 40-character receipt title; assert `#library-notes-delete-undo` and `#library-notes-delete-receipt-dismiss` both have a non-zero region inside the pane. Implement: budget = pane width − 12 (`✓ deleted · `) − widths of the two compact buttons; below 60 columns render the buttons on their own row (`Vertical` of two `Horizontal`s).
- [ ] **32124: failing test through the tree projection** — delete a note in a folder, call the undo handler, assert the row for `note_id` is back in the projection and focused. Implement by invalidating/refreshing the affected folder branch (and `Unfiled`) after `restore_note` succeeds, the way a create refreshes it (find that seam; do not add a second refresh mechanism).
- [ ] **32127: failing geometry pins** — at (235, 52) with no note open, the list pane is at least 100 columns wide and a 60-character title is not clipped; with a note open the list keeps ≥ 40 % of the canvas or 60 columns, whichever is smaller; the toolbar occupies ≤ 2 rows and `Move` / `Remove` / `Last import` fit. Implement in the TCSS (fr units, not fixed cells) and the adaptive shell's slot rule; rebuild the bundle.
- [ ] **32128: decide and pin.** Read the pinned test; if title order inside a folder is a repository contract, remove the Sort control from the tree view and record the decision in the task notes; otherwise make `sort_mode` order placements within each folder and keep `Title`. Either way: every remaining option renders at the narrowest pane, and a test pins the outcome.
- [ ] **32137: failing tests** — tree rows render an age label (`3m`, `1d`) next to the title; two visible rows with the same title in the same parent render `Reading list · Unfiled · 2h` and `Reading list · Unfiled · 5d` (folder then modified age suffix). Remove or use the dead flat-list age branch (`_compose_list` ~919–927) so there is one row renderer.
- [ ] **Live-verify** on the power profile (socket `nw-list`): list at 235x52 (widths, ages, both "Reading list" rows distinguishable, Sort behaviour), delete `Groceries (not work)` → receipt shows Undo and Dismiss → Undo → the row is back and `Notes (10)`. Captures; docs; backlog; one commit per task id.

### Task 4: Add from files chooser, import copy, source change, review layout (group `import-ux`, tasks 32125, 32130, 32134, 32135)

**Files:**
- Modify: `tldw_chatbook/Widgets/Library/library_notes_add_from_files_canvas.py` (`_compose_phase` choose phase ~284–320 yields only `#notes-add-keep-synced`; `_compose_pinned_actions` ~798–806 yields `Import once` + `Back to Notes`; the header `Lasting sync · Choose how files should relate…`; the review rows (grep `Ready to import as a new note`), the receipt (grep `imported ·`), the configure phase after a selection (`Check selection` / `Back to Notes` only))
- Modify: `tldw_chatbook/Library/library_note_import_state.py` (`can_revisit_receipt` ~155: requires `phase is RECEIPT` or `SELECT and not selected_paths`; `NoteImportPhase`), `tldw_chatbook/UI/Library_Modules/library_note_import_controller.py` (`accept_selected_path` ~150; the Back-to-Notes transition)
- Modify: `tldw_chatbook/Notes/note_import_parsers.py` (`_MESSAGES` ~58–75; `_parse_text` ~398 raises `invalid_content` for empty text; `_csv_payloads`), `tldw_chatbook/Notes/note_import_planner.py` (the per-item plan sentence `Content: create 1 new note`), `tldw_chatbook/Notes/note_import_receipts.py`
- Test: `Tests/Notes/test_note_import_planner.py`, `test_note_import_receipts.py` (extend), `Tests/UI/test_library_notes_lasting_sync_flow.py` (extend), new `Tests/UI/test_library_notes_wave_import_ux.py`
- Docs: `notes.md` (Add from files, Import once)

**Interfaces:**
- Produces: a new classification `ImportClassification.EMPTY` with message `Empty file — nothing to import.` (`_MESSAGES["empty_source"]`) used for zero-byte and whitespace-only text sources; `ParsedImportSource.note_count` so the planner can say `Content: create 2 new notes`; receipt `skipped_items: tuple[(display_path, reason_code), …]` rendered under a `Skipped (N)` disclosure; state transitions `SELECT → SELECT` via `clear_selection()` and `RECEIPT → (list view)` that keeps `latest_receipt` so `can_revisit_receipt` is true from the Notes list.

- [ ] **32125: failing compose test** — in phase `choose`, both `#notes-add-import-once` and `#notes-add-keep-synced` are children of the content body, in that order, each directly after its description; the pinned bar holds only `#notes-sync-back`; the header does not contain `Lasting sync` before a choice. Implement by moving the button out of `_compose_pinned_actions`.
- [ ] **32130: failing parser/planner tests** — an empty `.md` and a whitespace-only `.md` classify as EMPTY with the new message (not FAILED); `.obsidian/app.json` (a mapping without `content`) classifies as SKIPPED with `Not a note file (app configuration).` rather than `could not be imported safely` (keep `invalid_content` for genuinely malformed data); a 2-row CSV plans `create 2 new notes`; the receipt carries the skipped list and the canvas renders `Skipped (11)` with path + reason rows; the completion line reads `Import finished · 61 notes created · 11 files skipped` instead of `All planned items settled.`
- [ ] **32134: failing state tests** — after a folder selection the configure phase offers `Change selection` (reopens the picker, replaces the selection) and `Clear` (back to an empty SELECT phase); `Add another file` renders for file selections; after `Back to Notes` from a receipt, `can_revisit_receipt` is true and the Notes list shows `Last import`. Implement the state transitions in `library_note_import_state.py` and wire the buttons.
- [ ] **32135: failing compose test** — a review of 71 items renders one line per item (`path · action · destination`) with its Skip/Create controls adjacent to the path, grouped under headers per action class (`New (60)`, `Unchanged repeat (…)`, `Unsupported (4)`, `Failed (…)`) each with a per-group `Skip all` / `Create all`; at (235, 52) at least 15 rows are visible. Keep the existing per-row choices and the collision block; only the layout changes.
- [ ] **Live-verify** on the fresh profile (socket `nw-import-ux`): chooser (both buttons together), Import once on `…/fresh/vault` (type, Enter, then Select folder), review (groups, ≥15 rows/screen), Change selection, import, receipt with `Skipped (N)`, Back to Notes → `Last import` present. Captures; docs; backlog; one commit per task id. If Task 5's Obsidian toggle needs the review header, leave a clearly named slot (`#notes-import-review-options`) above the groups.

### Task 5: Obsidian mode for Import once (group `obsidian`, task 32129)

**Files:**
- Modify: `tldw_chatbook/Notes/note_import_discovery.py` (`_scan_entry` ~758, `_scan_child_directory` ~825: add the skip rule for `.obsidian/`, `.trash/`, `Templates/` when the root is a vault, recorded as skipped entries with a reason, not failures)
- Modify: `tldw_chatbook/Notes/note_import_parsers.py` (`_parse_text` ~398: parse a leading `---\n…\n---\n` YAML block with the existing `_UniqueKeySafeLoader`; `title` → title, `tags` (list or string) → keywords via `_keywords`, `aliases` kept on the payload; strip the block from `content`; ignore a first `# ` heading only when frontmatter supplied a title; never accept a `{{…}}` placeholder as a title — fall back to the stem), `note_import_plan_models.py` (payload gains `aliases: tuple[str, ...]`, `wikilinks: tuple[str, ...]`), `note_import_planner.py` (vault detection flag on the batch; the review shows the per-item effect: `title from frontmatter`, `2 tags`, `3 links`), `note_import_executor.py` (second pass after creation: rewrite `[[Target]]` / `[[Target|alias]]` / `[[folder/Target]]` to the created note's link form for targets created in the same batch; unresolved links stay literal)
- Modify: `tldw_chatbook/Widgets/Library/library_notes_add_from_files_canvas.py` (review phase: a default-on `Obsidian vault` toggle with the one-line explanation, mounted in `#notes-import-review-options`), `tldw_chatbook/Library/library_note_import_state.py` (`obsidian_mode: bool`), `tldw_chatbook/UI/Library_Modules/library_note_import_controller.py`
- Test: `Tests/Notes/test_note_import_planner.py`, `test_note_import_executor.py` (extend), new `Tests/Notes/test_note_import_obsidian.py` with a fixture vault built by `SCRATCH/notes-crit/mkvault.py` (copy the builder into the test as a fixture; do not import from the scratchpad)
- Docs: `notes.md` (Import once → new "Obsidian vaults" subsection)

**Interfaces:**
- Consumes: `ParsedNotePayload(title, content, keywords, template_name)`; the note link form used elsewhere in the app for `[[…]]` targets — grep `note:` link handling in `tldw_chatbook/Notes/` and the Console (`library_search_notes` returns `note:` ids; the guide mentions "related public `note:` IDs"). If no in-app note-link syntax exists, rewrite to a Markdown link `[Target](note:<id>)` and record that decision in the task notes.
- Produces: `discover_import_sources(..., obsidian_mode: bool)`; `parse_import_sources(..., obsidian_mode: bool)`; `ImportClassification.SKIPPED` with reason `Obsidian configuration` / `Obsidian trash` / `Obsidian template`; executor `rewrite_wikilinks(batch_result) -> int` returning the number of links rewritten (surfaced in the receipt as `14 links resolved`).

- [ ] **Step 1: failing discovery test** — a root containing `.obsidian/app.json` is detected as a vault; with `obsidian_mode=True`, `.obsidian/`, `.trash/` and `Templates/` entries come back as SKIPPED with their reasons; with it False the old behaviour holds.
- [ ] **Step 2: failing parser tests** — frontmatter `title:` wins; `tags: [project, ux]` and `tags:\n  - daily` both become keywords; `aliases` land on the payload; the body no longer starts with `---`; a body whose first heading is `# {{date:YYYY-MM-DD}}` gets the stem title; a note with no frontmatter and a `# Heading` keeps today's rule.
- [ ] **Step 3: failing executor test** — import `A.md` (`See [[B]] and [[Reading/C|see C]] and [[Missing]]`) with `B.md` and `Reading/C.md` in the batch; after execution A's body links B and C by note id and `[[Missing]]` is unchanged; the receipt says `2 links resolved`.
- [ ] **Step 4: review toggle** — default on when a vault is detected, off otherwise; toggling re-runs the check (it is read-only) and the rows show `title from frontmatter · 2 tags · 3 links` effects; the vault on disk stays byte-identical (assert with a hash in the UI test).
- [ ] **Live-verify** on the fresh profile (socket `nw-obsidian`): Import once on `…/fresh/vault` (type, Enter, Select folder), toggle visible and on, review shows skips for `.obsidian`/`.trash`/`Templates`, import, open `Projects/Library review` → title `Library ▸ Notes review`, keywords `project, ux`, body starts at `# Library ▸ Notes review`, Preview shows the links as links; `shasum` of the vault before and after identical. Captures; docs; backlog; commit.

### Task 6: Editor keys and copy (group `editor-keys`, tasks 32131, 32132, 32133, 32138, 32139, 32142)

**Files:**
- Modify: `tldw_chatbook/UI/Screens/library_screen.py` (the `/` binding for the notes filter — grep `find note` / `action_library_notes_filter`; the `ctrl+n` binding and `check_action` gates; footer contexts `_apply_library_notes_footer_context`; the Escape guard through `_flush_library_note_save` / `NoteFlushOutcomeKind` — a non-PERMITTED outcome must notify)
- Modify: `tldw_chatbook/UI/Library_Modules/library_notes_controller.py` (the delete handler that switches mode to Edit before composing the confirmation — grep `Delete this note?`; the `Saved` status for a fresh blank note — `NotesStatusChannels` / grep `"Saved"`)
- Modify: `tldw_chatbook/Widgets/Library/library_notes_canvas.py` (delete confirmation composition; back cues `‹ Notes` / `‹ Note` / `‹ Back to list` — grep each; Preview mode header; Info `Saved` duplicate; dates in Info → add an absolute timestamp beside the relative age using the existing `format_console_relative_age` source value)
- Test: `Tests/UI/test_library_crit8_keyboard.py` (extend), `Tests/UI/test_library_notes_reader.py`, `Tests/UI/test_library_notes_work_session.py` (extend), `Tests/UI/test_library_shell.py:6344` (`test_library_notes_bindings_are_inactive_outside_notes_workflow` — reconcile for 32138), new `Tests/UI/test_library_notes_wave_editor_keys.py`
- Docs: `notes.md` (Keyboard & commands, Edit/Preview/Info)

**Interfaces:**
- Produces: `/` focuses the filter and consumes the key (`event.prevent_default()` / `stop`) — a test types `/` then `Reading` and asserts the Input value is `Reading`; the delete confirmation is composed inside the Info Danger section without a mode change, with Tab trapped between `#library-note-delete-cancel` and `#library-note-delete-confirm` (use the ids the canvas already mounts) and the footer reading `enter cancel · tab, enter delete` while Cancel is focused; a refused Escape calls `self.app.notify("Can't leave yet — fix the title or press Discard new note.", severity="warning")` (exact copy from the task); a fresh blank note's status channel reads `Draft — not saved yet` until the first save lands; one back cue `‹ Notes` in Edit, Preview and Info at wide sizes and `‹ Back to list` on compact (matches the guide); `ctrl+n` bound on the Library landing as well (and the landing footer says `ctrl+n new note`) — or, if the pinned test's rationale holds after reading it, both keys work in both places; record the choice.

- [ ] **32131:** failing test then the one-line fix in the binding handler.
- [ ] **32132:** failing tests for in-place composition (mode stays `info`), footer copy per focused button, and the Tab trap; implement.
- [ ] **32133:** failing tests for the refused-Escape notification and the draft status; implement (the flush outcome kinds already exist — surface them).
- [ ] **32138:** read the pinned test's docstring; reconcile; failing test for the chosen behaviour; implement; update the pinned test with a one-line reason.
- [ ] **32139:** failing test for the back cue per mode/size; implement.
- [ ] **32142:** Preview header shows the title; Info shows `Saved` once; Info Properties show `Created 2026-09-08 21:14 · 3m ago`; receipts are dismissed when the user leaves the Notes list for Add from files or Folder files (the receipt survived a whole import in the critique).
- [ ] **Live-verify** on the power profile (socket `nw-editor-keys`): `/` then `Reading`; open a note → Info → Delete → prompt in place, footer, Tab cycles, Escape cancels; blank note draft status; Escape with a whitespace-padded title → toast; `ctrl+n` on the landing. Captures; docs; backlog; one commit per task id.

### Task 7: Empty state and first-run hand-off (group `onboarding`, tasks 32126, 32140)

**Files:**
- Modify: `tldw_chatbook/Widgets/Library/library_notes_canvas.py` (~903–905: `if self.tree_projection is not None: yield from self._compose_tree_rows(list_state); return` runs before `if not list_state.rows: yield Static(list_state.empty_copy, id="library-notes-empty")`), `tldw_chatbook/Library/library_notes_state.py:19` (`_EMPTY_NOTES_COPY = "No notes yet. Create your first note."`), `tldw_chatbook/Notes/agent_lessons.py` (the seeded folder; add the one-line gloss text next to it or the hide-until-content rule in the tree projection)
- Modify: `tldw_chatbook/UI/Wizards/FirstRunSetupWizard.py:7149` (`Button("Add your first document", id="setup-exit-library")` — add `Write your first note` beside it, routing to Library ▸ Notes ▸ New note), `tldw_chatbook/Chat/console_onboarding_state.py` (the Get started card `1. Connect a provider / 2. Pick a model / 3. Send your first message`: add the notes path for a profile with no provider)
- Test: `Tests/UI/test_library_shell.py:18982` asserts the code copy (`No notes yet. Create your first note.`) — keep that copy, fix the guide; `Tests/UI/test_first_run_*` (grep for the wizard summary tests), new `Tests/UI/test_library_notes_wave_onboarding.py`
- Docs: `notes.md` (empty state copy), `First_Run_Setup.md`

- [ ] **32126: failing test** — a projection with zero notes and the seeded `Agent_Lessons` folder renders `#library-notes-empty` with `No notes yet. Create your first note.` above the tree, and the folder row carries the gloss `Agent_Lessons — where Console agents file reusable lessons (empty)`; a projection with one note renders no empty line. Implement; align the guide to the code copy.
- [ ] **32140: failing wizard test** — the Summary step offers `Write your first note` next to `Add your first document`, and pressing it lands on Library ▸ Notes with the New note view open; the Console Get started card lists `Write a note in Library` as a step that needs no provider.
- [ ] **Live-verify** on the fresh profile (socket `nw-onboarding`): wizard Summary → `Write your first note` → New note view; Notes (0) shows the empty line + gloss. Captures; docs; backlog; one commit per task id.

### Task 8: Notes guide sweep (group `docs`, task 32141) — runs after Tasks 1–7 are reviewed

**Files:**
- Modify: `Docs/User_Guide/library/notes.md`, `Docs/User_Guide/library/file-notes.md`
- Worktree: `SCRATCH/wave/docs` on `fix/library-notes-docs`, created by the controller from `dev` after the seven code PRs are open, with each of their branches merged in (`git merge --no-ff fix/library-notes-<group>`) so the sweep sees the shipped behaviour.

- [ ] For each of the 13 contradicted claims in the snapshot's docs-vs-live table, either verify it live on the merged worktree (socket `nw-docs`, power profile `SCRATCH/notes-crit/wave/onboarding/power` reused) and leave it, or correct the prose. Rewrite the 2026-09-06 rename-propagation stamp to describe the deferred-refresh behaviour shipped in #2531 (the row updates when focus leaves the field). Every `Verified against` stamp on both pages names a commit.
- [ ] Commit; report the list of claims verified vs corrected.
