# Release 0.2.2 plan

Goal: Publish the owner-confirmed version-only successor to 0.2.1 from an
isolated checkout. Track execution in TASK-32567. Preserve existing user work.

ADR required: no new ADR
ADR path: backlog/decisions/098-low-latency-speculative-duplex-voice-pipeline.md
Packaging contract: backlog/decisions/032-immutable-installed-distribution-assets.md
Reason: Continue the approved app-only distribution and immutable installed-asset
contracts. No dependency, runtime boundary, or application behavior changes.

1. Start from `4631b60f8dd9623fc55bf16f4a37e29fcb1240c7`, shared by main, dev,
   and v0.2.1. Confirm 0.2.2 is absent on TestPyPI and PyPI.
2. Update the root and companion source versions, runtime version and tuple,
   README, changelog, and current app-only release documentation. If strict-pin
   tests assume 0.2.1, derive their positive fixture from project metadata.
3. Run focused metadata and voice release tests. Build fresh wheel/sdist,
   validate metadata and contents, and run the installed-distribution gate.
   Review the bounded diff independently before merging.
4. Merge release preparation to dev after required PR checks. Dispatch trusted
   TestPyPI publishing; verify the published files against that run's artifact
   hashes and install its wheel in a fresh Python 3.12 environment.
5. Fast-forward main to that exact verified commit. Wait for production package
   gates, approve the existing PyPI environment, and verify production hashes,
   dependency consistency, runtime version, and both CLI entry points.
6. Create annotated v0.2.2 and its GitHub release at the published source.
   Record evidence and mark TASK-32567 Done after publication succeeds.

Experimental duplex voice remains unqualified and disabled; no native companion
will be published. The known broader CI gzip-fixture collection issue is inherited
unchanged. Report that limitation accurately; do not claim a passing full suite.
