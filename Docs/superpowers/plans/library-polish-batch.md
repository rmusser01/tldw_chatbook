# Plan: Library polish batch (backlog tasks 2860, 3022, 2859, 3020, 3200)

Branch `fix/library-polish-batch` (worktree `.worktrees/library-polish`), cut from dev `023a04a48` (includes the P1 arc PR #1410 and the P2 batch PR #1420).
Five independent backlog tasks, one plan task each. Each plan task OWNS its backlog task file end-to-end: flip In Progress + add Implementation Plan before coding; check ACs + append Implementation Notes + flip Done after. The findings originate from the 2026-08-06/07 Library UAT arcs — **re-verify each item at HEAD before implementing** (two arcs have landed since some were filed; a finding may be partially fixed).

## Global Constraints

- **Python/tests**: `/Users/macbook-dev/Documents/GitHub/tldw_chatbook/.venv/bin/python` for everything; pytest is the ONLY entry point that may import app modules. Targeted tests only + `--collect-only -q` over `Tests/Library` as collection sanity. "no tests ran" = failed gate; read the passed count. Foreground Bash only — NEVER end your turn waiting on background work.
- **TDD**: failing test first with RED evidence, then green. Rendered-geometry assertions (region.x/width) for anything layout-visible — two arcs proved headless DOM queries miss off-screen/oversized widgets.
- **CSS**: never edit `tldw_chatbook/css/tldw_cli_modular.tcss` (generated). Edit sources under `tldw_chatbook/css/`, regenerate via `.venv/bin/python tldw_chatbook/css/build_css.py` (verify with `check_bundle_sync.py`); source + bundle in the same commit. Widget `DEFAULT_CSS` needs no bundle step. `outline: heavy` on 1-row compact widgets EATS the label.
- **Recompose discipline**: in-place updaters own every conditional compose() owns; display-managed widgets over recompose on hot paths.
- **Bindings/footer honesty**: multiple same-key bindings resolve via `check_action` in declaration order; footer/F1 advertise only keys that work on the current surface. LibraryScreen's F1 and footer now share `_library_footer_shortcuts_for_current_state()` + check_action-filtered BINDINGS extras — extend that seam, never fork it.
- **Live verification (required per task unless the task is test-only)**: from the worktree root, unique socket `p3T<N>lib$RANDOM`, scratch profile:
  `mkdir -p /tmp/p3T<N> && printf '[general]\nusers_name = "sdd_p3t<N>"\n[first_run]\nsetup_started = true\nsetup_completed = true\n' > /tmp/p3T<N>/config.toml`
  `tmux -L <socket> new-session -d -x 170 -y 50 'TLDW_CONFIG_PATH=/tmp/p3T<N>/config.toml /Users/macbook-dev/Documents/GitHub/tldw_chatbook/.venv/bin/python -m tldw_chatbook.app'`
  Cold start ~12s (don't navigate during it). Palette nav via `send-keys C-p` + exact entry text. Ctrl+digit CANNOT be sent via tmux. Clicks: SGR press+release, COL by CHARACTER index (python str.find), never byte offsets. Verify pane identity (`ps eww <pid>`) before trusting captures. Prove focus/selection with `capture-pane -p -e`. Cleanup: C-q, kill-server, `rm -rf ~/.local/share/tldw_cli/sdd_p3t<N>`.
- **Git**: commit per task, message ends `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; `git add` SPECIFIC paths only; NEVER stash; NEVER push (controller pushes).
- **Docs**: UI-visible changes update the matching `Docs/User_Guide/` page or its "Verified against" stamp (convention: `Verified against dev @ 023a04a48`).
- **Known ambient test debt**: task-3022 (being FIXED in this plan's Task 2) lists the current ambient failures — before Task 2 lands, A/B any surprise failure against clean HEAD; after Task 2 lands, the suites should be honest.
- **Key code map**: `tldw_chatbook/UI/Screens/library_screen.py`, `tldw_chatbook/Widgets/Library/*.py`, `tldw_chatbook/Library/*.py`, `tldw_chatbook/Widgets/AppFooterStatus.py`, `tldw_chatbook/UI/Navigation/main_navigation.py`. Tests: `Tests/Library/`, `Tests/UI/`, `Tests/Widgets/Library/`.

## Task 1: Footer F6 hint filter (backlog task-2860)

Read `backlog/tasks/task-2860 - *.md` (glob it — filed by a P1-arc implementer; its ACs bind). Summary: `AppFooterStatus.set_shortcut_context`'s `_RESERVED_GLOBAL_KEYS` filter silently DROPS a screen-supplied F6 hint from the context half, so screens that deliberately advertise F6 ("next pane") lose it. Coordinate with ADR-031 (read it if it exists — grep `Docs/` for ADR-031/reserved-global) — the filter's purpose is avoiding duplicate global hints, not censoring screen-supplied ones. Likely fix shape: dedupe against the ACTUALLY-RENDERED globals half instead of a hardcoded key list, or allow a screen-supplied hint to override/replace the global rendering. Mind the P2 arc's narrow-width compaction (`GLOBAL_HINTS_COMPACT`, `_apply_responsive_footer`) — the fix must hold at 100/80 cols too. The ambient `test_landing_footer_advertises_the_landing_keyboard_story` failure may be caused by this — check whether your fix turns it green; if yes, record that for Task 2.

## Task 2: Ambient Library test debt (backlog task-3022)

Read `backlog/tasks/task-3022 - Ambient-Library-test-debt-fixture-bypass-and-two-standing-failures.md` — its ACs bind. Test-only task (no live TUI needed): (1) fix the fixture-bypass cluster at the FIXTURE level (~7 tests constructing LibraryScreen without `__init__`, missing `_library_ingest_preflight_generation` — construct properly or set the attribute; do NOT touch production code); (2) the two standing failures (`test_landing_footer_advertises_the_landing_keyboard_story` — re-check after Task 1; `test_shared_form_and_native_inputs_use_thin_non_semantic_focus`) either pass or get traced to their owning open tasks with a note; (3) the two order-dependent notes-tail failures added during the P2 arc (`test_library_shell_note_save_result_after_switch_is_discarded`, `test_library_shell_note_conflict_reload_discards_local_edits`) — fix the ordering dependency at the fixture/isolation level if tractable, else document the trigger precisely in the task. Exit bar: the targeted Library suites run green on dev with NO known-ambient exclusion list.

## Task 3: Library UAT P3 polish batch (backlog task-2859)

Read `backlog/tasks/task-2859 - Library-UAT-P3-polish-batch.md` — 11 numbered items, ACs bind ("each item fixed or declined with a one-line reason in the notes"). Re-verify each at HEAD first — several may be stale: item 4 ("3 done — in queue" contradiction) may have changed with task-2857's renames; item 5's Details-label click + wrap; item 10's snippet padding sits near PR #1385's work. Highlights needing care: item 1 (Conversations canvas title header — match sibling "Name (n)" pattern; move the filter above the empty-state text); item 5 (Details disclosure: make the label clickable alongside the ▸ chip, fix mid-unit wrapping, give "Status" a value or drop the heading, decide whether -wal sizes fold in — the DB-sizes line understates by 1000× with an uncheckpointed WAL; smallest honest fix: include -wal/-shm in the reported size); item 6 (picker polish: human sizes, minute-precision timestamps); item 7 (title grammar: pick "Name (n)" everywhere, drop the "Library X" prefixes; rail and canvas agree on "Search / RAG" spacing); item 11 (toast overlap with the queue area — the Toast margin-bottom lever from the ingest arc is the known working knob). Declines are legitimate for items where the current behavior is defensible — one line each.

## Task 4: Media select-mode polish (backlog task-3020)

Read `backlog/tasks/task-3020 - Media-select-mode-polish-batch-from-the-P1-arc-final-review.md` — 6 ACs bind. All in the P1 arc's select-mode neighborhood: (1) exclusive/guarded bulk-delete worker (double-press can't double-decrement); (2) Escape cancels an armed bulk-delete confirm (parity with the single-item confirm — extend the existing Escape gate matrix, broadest-last ordering); (3) partial-failure focus lands on a still-checked row; (4) skill-editor footer advertises its working esc/ctrl+s (extend `_library_footer_shortcuts_for_current_state` with a skill-editor branch); (5) single-item viewer delete decrements the rail count in place (mirror the bulk path); (6) "item(s)" pluralization + the media-and-conversations.md stamp. The Escape work touches the 6-gate matrix — verify no two gates can both fire (the P1 final review's walk is the reference).

## Task 5: Nav-bar mid-word tab cut at 80 columns (backlog task-3200)

Read `backlog/tasks/task-3200 - *.md` (glob it — filed by the P2 arc with verified file:line citations into `main_navigation.py`; ACs bind). Shared chrome — the change affects EVERY screen's nav bar, so keep it minimal and test it against multiple screens' tab sets. The cited seams: the `.main-nav` overflow CSS block and the `More ›` overflow-hint construction. Direction: never render a partially-clipped tab label — either fit whole tabs + `More ›` or drop to the next whole tab (measure, don't guess: rendered-geometry tests at 80/100 cols). If the genuinely-correct fix requires nav-system surgery beyond a bounded change, STOP and report BLOCKED with the options — this is the one task in the plan allowed to conclude "needs its own arc" (record it in the task file and leave the task open with findings).
