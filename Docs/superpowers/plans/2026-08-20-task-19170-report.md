# TASK-19170 — Kimi response-side and UI surfaces still pin the literal kimi-k3 / glm-5.2 ids

**Branch:** `fix/task-19170-ui-model-pins` (from `origin/dev` `f79aa9082`, which contains the full predicate arc incl. TASK-18803)
**Status:** complete — both ACs met (with the GLM-replay unprobeable nuance recorded under AC #1); live-verified both ways.

---

## 1. Step 1 — probe verdicts (recorded before fixes)

All wire probes are standalone `curl` against `https://api.moonshot.ai/v1/chat/completions`
with the repo-root `moonshot-api-key.txt` — no `tldw_chatbook` import anywhere in the
probe path. Raw response bodies retained in the session scratchpad
(`probe_out/*.json`); the summaries below are `jq` projections of those bodies.
There is still no Z.ai key in the repo root (`ls *api-key*`), so every GLM wire
question stays **unprobeable** — recorded, not fabricated.

### Q1 — does `kimi-k2.6` return `reasoning_content` when `reasoning_effort` is set?

**YES — and not only k2.6, and not only when effort is set.** Single-turn legs
(`temperature: 1`, `max_tokens: 1024`):

```
== A_kimi-k2.6_effort_high HTTP 200
{"id":"chatcmpl-6a8768a3b5c429b466fbc42d","model":"kimi-k2.6","finish":"stop","content":"255","has_reasoning_content":true,"reasoning_content_chars":380,...}
== A_kimi-latest_effort_high HTTP 200
{"id":"chatcmpl-6a8768a616ceb0c0ae780f2c","model":"kimi-latest-8k","finish":"stop","content":"255","has_reasoning_content":false,"reasoning_content_chars":0,...}
== A_kimi-k3_effort_high HTTP 200
{"id":"chatcmpl-6a8768a7659da119063ca38f","model":"kimi-k3","finish":"stop","content":"255","has_reasoning_content":true,"reasoning_content_chars":12,...}
== B_kimi-k2.6_no_effort HTTP 200
{"id":"chatcmpl-6a8768a9b5c429b466fbc42f","model":"kimi-k2.6","finish":"stop","content":"255","has_reasoning_content":true,"reasoning_content_chars":187,...}
== B_kimi-k3_no_effort HTTP 200
{"id":"chatcmpl-6a8768ab93ddf5fc674f5f7d","model":"kimi-k3","finish":"stop","content":"255","has_reasoning_content":true,"reasoning_content_chars":62,...}
== E_kimi-k2.5_effort_high HTTP 200
{"id":"chatcmpl-6a8768d3666d8454604d8b5f","model":"kimi-k2.5","finish":"stop","content":"255","has_reasoning_content":true,"reasoning_content_chars":368,...}
== E_kimi-k2.7-code_effort_high HTTP 200
{"id":"chatcmpl-6a8768d705f910ba798aeca0","model":"kimi-k2.7-code","finish":"stop","content":"255","has_reasoning_content":true,"reasoning_content_chars":94,...}
```

Family map: **every versioned kimi id probed (k2.5, k2.6, k2.7-code, k3) returns
`reasoning_content` on every turn, with and without `reasoning_effort`.
`kimi-latest` (served as `kimi-latest-8k`) returns none.** This is exactly the
extension of TASK-18803's versioned-kimi regex (`kimi-k<major>`), and exactly
NOT the whole-series `moonshot_model_supports_reasoning_effort` extension
(which includes `kimi-latest`).

### Q2 — does a multi-turn continuation REQUIRE replaying `reasoning_content`?

**NO — the API accepts both and rejects neither.** Follow-up turn built from leg
A's actual response (`assistant` message with vs without the prior
`reasoning_content`), question "multiply that result by 2":

```
== C1_kimi-k2.6_with_replay HTTP 200
{"id":"chatcmpl-6a8768cbb5c429b466fbc43a","model":"kimi-k2.6","finish":"stop","content":"510",...}
== C2_kimi-k2.6_without_replay HTTP 200
{"id":"chatcmpl-6a8768ccb5c429b466fbc43c","model":"kimi-k2.6","finish":"stop","content":"510",...}
== C1_kimi-k3_with_replay HTTP 200
{"id":"chatcmpl-6a8768ce65640bfb1b0e4930","model":"kimi-k3","finish":"stop","content":"510",...}
== C2_kimi-k3_without_replay HTTP 200
{"id":"chatcmpl-6a8768d1a190ada43ce58fb8","model":"kimi-k3","finish":"stop","content":"510",...}
```

Correct answers both ways on both models; no protocol-level degradation
observed. Token-accounting nuance from `usage`: on the plain-turn replay,
`kimi-k3` **counts** the replayed reasoning (`prompt_tokens` 158 with vs 151
without, delta ≈ the 12-char replay) while `kimi-k2.6` **ignores** it
(`prompt_tokens` 48 both ways despite a 380-char replay) — so k2.6 plain-turn
replay is currently discarded server-side rather than consumed. In the
tool-call loop (below) k2.6 **does** consume it (156 vs 106).

### Q2b — tool-call loop (the shape checkpoints actually replay mid-run)

`kimi-k2.6` + one trivial function tool; follow-up carries the tool result with
the assistant `tool_calls` message with vs without its `reasoning_content`:

```
== D0_kimi-k2.6_tool_turn HTTP 200
{"id":"chatcmpl-6a8769144a7650349d8717a2","model":"kimi-k2.6","finish":"tool_calls","content":"","tool_calls":[{"name":"get_current_time"}],"has_reasoning_content":true,"reasoning_content_chars":227,...}
== D1_kimi-k2.6_result_with_rc HTTP 200
{"id":"chatcmpl-6a8769160d0672cb3fe3f24e",...,"content":"The current time in UTC is **2026-08-20T21:05:00Z** (August 20, 2026 at 21:05 UTC).","has_reasoning_content":true,"reasoning_content_chars":150,...}
== D2_kimi-k2.6_result_without_rc HTTP 200
{"id":"chatcmpl-6a876918e003e260ebb0721a",...,"content":"The current time in UTC is **2026-08-20 21:05:00** (9:05 PM UTC).","has_reasoning_content":true,"reasoning_content_chars":144,...}
```

Mid-loop replay is likewise accepted-not-required (200 both ways; with-replay
consumed at 156 prompt tokens vs 106 without). The loop-COMPLETING turn itself
returns `reasoning_content` (150/144 chars) — relevant because the completing
branch of `_moonshot_continuation_candidate` appends a final reasoning round
whose parse validation requires non-blank reasoning.

### GLM

* The Settings-list question (which effort options to SHOW for GLM family
  members) is decided **client-side**: TASK-18803's
  `zai_model_supports_reasoning_effort` (GLM ≥ 5.2 floor) is already the
  builder's acceptance gate, so the UI list follows the same predicate. No wire
  needed.
* The GLM replay question (does a GLM model return `reasoning_content` that a
  follow-up should replay) remains **unprobeable — no Z.ai key**. Recorded;
  no zai response-side behavior is changed on fabricated evidence. (zai.py's
  continuation candidate has no glm-5.2 branch to convert: its only literal is
  the `_DEFAULT_MODEL` fallback, and the parse layer only permits final
  reasoning rounds for `provider == "moonshot"`.)

### What the verdicts decide (fix directions, per site)

1. `provider_continuation.py` `_KIMI_K3_MODEL` no-calls-round gate: **real gap
   for the versioned kimi family** — k2.6/k2.5/k2.7-code turns DO return
   reasoning_content, so the k3-only preserved-thinking checkpoint shape must
   widen to the versioned family (new response-side predicate; `kimi-latest`
   excluded per probe A).
2. `_moonshot_continuation_candidate` / `_apply_continuations` k3 pins: widen
   per the same predicate. Replay is wire-safe (accepted everywhere, required
   nowhere), so extending k3-style preserved-thinking replay to the family
   cannot 400; not extending it silently drops recorded reasoning.
3. `console_chat_controller.py` `keep_all`: same predicate — resume history
   must retain the family's complete checkpoints, or the newly preserved
   reasoning is discarded on resume.
4. `settings_screen.py` `_model_profile_reasoning_effort_options`: derive from
   the 18803 REQUEST predicates (`moonshot_model_supports_reasoning_effort`,
   `zai_model_supports_reasoning_effort`) — the curated lists are the values
   the builders accept, so the UI list must follow the same family gates. The
   whole-series moonshot predicate (incl. `kimi-latest`) is correct HERE
   because the builder accepts `reasoning_effort` for the whole series
   (18803 wire probes: 200 on kimi-latest), even though `kimi-latest` returns
   no reasoning_content.

One durable-data constraint found during code-read (recorded before fixing):
pre-19170 versioned-kimi (non-k3) tool-loop checkpoints were persisted
**complete with all-calls rounds** (no final reasoning round). The parse rule
"a complete checkpoint must END with a no-calls reasoning round" therefore
cannot be widened from k3 to the family without invalidating stored data —
the fix keeps that must-end rule k3-pinned (its pipeline always appended),
accepts BOTH complete shapes for the rest of the family, and branches replay
on checkpoint SHAPE (final round has no calls) rather than on model id.

---

## 2. The fix

### New predicate (`tldw_chatbook/model_capabilities.py`)

`moonshot_model_returns_reasoning_content(model)` — the RESPONSE-side family
question, deliberately narrower than the request-side
`moonshot_model_supports_reasoning_effort` (which includes `kimi-latest`).
Extension: the versioned kimi reasoning family (`kimi-k<major>`, any
suffix), reusing `_MOONSHOT_KIMI_VERSIONED_RE`. Same
immutable-outside-config-tables design as the 18803 predicates; probe
chatcmpl ids in the docstring. Pinned as allowed to DISAGREE with the
request-side predicate on `kimi-latest`.

### Per-site decisions (AC #1)

| Site | Decision | Evidence |
|---|---|---|
| `provider_continuation.py` no-calls-round parse gate (`model != _KIMI_K3_MODEL`) | **CONVERTED** to `not moonshot_model_returns_reasoning_content(model)` | Probe A/B/E: the whole versioned family returns reasoning_content, so its preserved rounds must be storable; `kimi-latest` stays rejected (probe A). |
| `provider_continuation.py` must-end-with-reasoning-round invariant (`model == _KIMI_K3_MODEL`) | **DOCUMENTED-CORRECT, kept** | Durable-data constraint: pre-19170 non-k3 family checkpoints were persisted complete WITHOUT the final round; widening would invalidate stored private data. Replay branches on shape instead. Pinned by `test_schema_family_complete_tool_only_checkpoint_stays_valid_off_k3`. |
| `moonshot.py` `_moonshot_continuation_candidate` (2× `resolution.model == _DEFAULT_MODEL`) | **CONVERTED** via `_preserved_reasoning(turn, model)` (predicate + non-blank guard) | Probes B/D1/D2: family plain and loop-completing turns carry reasoning_content. The non-blank guard means a family turn with no usable reasoning keeps the pre-19170 complete shape instead of minting an unparseable candidate. |
| `moonshot.py` `_apply_continuations` complete-expansion gate (`resolution.model == _DEFAULT_MODEL`) | **CONVERTED** to shape (`not checkpoint.rounds[-1].calls`) | Probes C1/D1: replay accepted (never required) family-wide, so expansion is wire-safe; old-style complete checkpoints keep the annotate path. Model literal eliminated entirely. |
| `console_chat_controller.py` `keep_all` | **CONVERTED** to the response-side predicate | Family checkpoints now exist and must survive a resume; `kimi-latest` stays outside the retention policy (probe A). |
| `settings_screen.py` `_model_profile_reasoning_effort_options` | **CONVERTED** to the 18803 REQUEST-side predicates (`moonshot_model_supports_reasoning_effort` / `zai_model_supports_reasoning_effort`) | The curated lists are exactly the value sets the builders accept; 18803 wire probes cover the moonshot family incl. `kimi-latest` (chatcmpl-6a872ac016ceb0c0ae780b0c). GLM decided client-side (options shown must match the builder's client-side gate); wire remains unprobeable — no key. |

Same-rule companions converted in lockstep (the four named sites are
incoherent without them; each got its own red-first pin + mutation):
`Agents/agent_runtime.py` `_valid_final_update` + the first-create
FinalContinuation gate; `DB/ChaChaNotes_DB.py`
`_validate_continuation_owner_content`; `Character_Chat_Lib.py` exported
chat-history import owner rule (+ a no-calls shape guard so old-style
family checkpoints are not wrongly discarded); `Chatbooks/
chatbook_importer.py` `_imported_continuation_json` (same shape guard).
`settings_screen.py` `_hosted_provider_guidance` made family-aware
(preserved-thinking copy for the response-side family, effort-supported
copy for `kimi-latest`, historical copy unchanged), constants renamed
`KIMI_REASONING_EFFORT_SELECT_OPTIONS` / `GLM_REASONING_EFFORT_SELECT_
OPTIONS`; `Docs/User_Guide/settings.md` rows + stamp updated.

Deliberate edge change (documented, not pinned-away): a kimi-k3 plain turn
whose reasoning_content is whitespace-only previously minted an unparseable
candidate (hard error); it now gracefully mints no checkpoint. Never
observed on the wire (every probe leg carried non-blank reasoning).

## 3. Evidence

### Red → green (READ counts)

Selection: provenance + `test_provider_continuation.py` +
`test_moonshot.py` + `test_console_provider_continuation.py` +
`test_provider_continuation_runtime.py` + ChaChaNotes/UI-settings/
predicates/privacy/roundtrip files, `-p no:randomly`.

| Stage | Result |
|---|---|
| Baseline (branch = dev + probe docs) | **1 failed, 355 passed** — the fail is `test_settings_kimi_zai` kimi-k3 expecting the pre-18803 `{low,high,max}`: a **pre-existing dev red** (18803 added wire-verified `medium` to the constant without updating this pin); fixed here since it is this task's file surface |
| Predicate landed (20 new rows) | **71 passed** (predicate file + provenance) |
| All pins written, production sites untouched | **30 failed, 418 passed** — verbatim red reasons: `assert None is not None` (no k2.6 checkpoint minted), `AssertionError: assert 'error' == 'done'` (runtime rejects k2.6 first-create), `ContinuationValidationError` (parse rejects family reasoning-only round) |
| After the conversions | **448 passed** |
| Cluster gate (`Tests/LLM_Calls/` + continuation/kimi-zai chat tests + `Tests/Agents/…runtime` + `Tests/ChaChaNotesDB/` + `Tests/Chatbooks/` + settings + `Tests/test_model_capabilities.py` + provenance) | **1884 passed, 2 failed, 1 skipped** — both fails are the `test_summarization_diagnostic_privacy` manifest reds; reproduced on a detached clean-base worktree @ `f79aa9082` with the **byte-identical digest pair** (`67ec456b…` vs `32c915a3…`), i.e. my edits did not shift the diagnostic inventory at all (pre-existing, TASK-18801) |

### Mutations (all Edit-restored; `git diff tldw_chatbook/` empty, 0 markers; re-green 448)

| Mutation | Kill |
|---|---|
| M1 predicate → whole-series regex (kimi-latest admitted) | **9 failed** (predicate rows + parse/candidate/console kimi-latest exclusions) |
| M2 parse gate → literal restored | **22 failed** |
| M3 `_preserved_reasoning` predicate consultation dropped | **1 failed** (kimi-latest no-checkpoint pin) |
| M4 `_apply_continuations` → model literal restored | **1 failed** (multi-round expansion pin; the single-round case is annotate-equivalent by design) |
| M5 `keep_all` → literal restored | **2 failed** |
| M6a `_valid_final_update` → literal | **1 failed** · M6b first-create gate → literal | **1 failed** |
| M7 Settings options → exact-id checks | **7 failed** |
| M8 ChaChaNotes owner rule → literal | **1 failed** |
| M9a importer → literal | **1 failed** · M9b importer shape guard dropped | **1 failed** |
| M10a chat-history import → literal | **1 failed** · M10b its shape guard dropped | **1 failed** (control added mid-round after M10b initially had no killer) |

### Live verification (production seam `chat_api_call`, real key, uncommitted opt-in harness, provenance printed each run)

| Leg | Checkout | Result |
|---|---|---|
| kimi-k2.6 two-turn | fix | **PASS** — turn1 `'255'` with a complete checkpoint preserving **385 chars** of reasoning (the fix); turn2 `'510'` with the production wire payload replaying those exact 385 chars on the assistant owner |
| kimi-k3 two-turn (unchanged control, live) | fix | **PASS** — identical flow, 63 chars preserved + replayed |
| kimi-k2.6 two-turn | clean `f79aa9082` (detached control worktree) | **PASS (control)** — turn1 checkpoint `None` (the dev gap, live), turn2 wire payload carried NO reasoning_content |

## 4. Acceptance criteria

| AC | State | Evidence |
|---|---|---|
| #1 each literal site converted or documented with probe evidence | **met** | §2 table: five conversions + one documented-correct retention (the k3 must-end invariant — durable-data evidence rather than wire, stated explicitly). GLM nuance: the `glm-5.2` Settings scoping was decidable client-side and converted on the 18803 predicate; the GLM **replay** question (does GLM return reasoning worth replaying) remains **unprobeable — no Z.ai key** — recorded, and zai.py's response side is untouched (it has no glm-5.2 response literal; the parse layer only admits reasoning-only rounds for moonshot). |
| #2 new family members get a sensible effort list without a code edit | **met** | Options derive from the family predicates; pinned at the widget level (mounted Settings showing the curated set for `kimi-k2.6` and `glm-5.3`) and at the unit level for `kimi-k3-turbo`, `kimi-latest`, `glm-5.2-air`, with `moonshot-v1-8k`/`glm-4.6`/`gpt-5` generic-list controls. |

## 5. Commits

`2234cf50e` Step-1 probe verdicts (before any fix) · `95015e938` response-side
predicate + 20 pins · `24f0be5c8` the conversions + pins (red 30→green 448)
· `1bb5242c7` tool-only-shape import control · `645e850a0` report + task
hygiene.

## 6. Not changed, recorded

* zai.py response side — no GLM key; no glm-5.2 response literal exists to
  convert (only `_DEFAULT_MODEL`, a resolution default like moonshot's).
* `config.py` model catalog defaults (`kimi-k3` / `glm-5.2`) — defaults,
  not gates; also off-limits (concurrent agent owns config.py).
* The two `test_summarization_diagnostic_privacy` dev reds — TASK-18801,
  digest-identical on the clean base.
