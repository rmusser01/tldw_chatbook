# ADR Workflow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a canonical, lightweight ADR workflow for `tldw_chatbook` using `backlog/decisions/` as the source of truth.

**Architecture:** This is a documentation and workflow rollout. Add canonical ADR artifacts under `backlog/decisions/`, update `AGENTS.md` so agents perform an ADR check during Backlog/Superpowers planning, and leave global Superpowers skill changes and automation for a later proven need.

**Tech Stack:** Markdown, Backlog.md CLI, existing `AGENTS.md`, existing `Docs/superpowers/` planning flow, `rg`, `git diff --check`.

---

## Source Documents

- Spec: `Docs/superpowers/specs/2026-06-02-adr-workflow-design.md`
- Existing agent instructions: `AGENTS.md`
- Existing Backlog project config: `backlog/config.yml`
- Existing historical ADR-like docs:
  - `Docs/Development/TTS/Higgs-ADR-001-Backend-Architecture.md`
  - `Docs/Development/TTS/Higgs-ADR-002-Backend-Registration.md`
  - `Docs/Development/TTS/Higgs-ADR-003-Configuration-Design.md`
  - `Docs/Development/TTS/Higgs-ADR-004-Voice-Profile-Management.md`
  - `Docs/Development/TTS/Higgs-ADR-005-Implementation-Summary.md`
  - `Docs/Development/RAG/RAG-DESIGN.md`
  - `Docs/Development/RAG/RAG-TESTING-1.md`
  - `Docs/Development/Subscriptions/Subscriptions-Implementation-1.md`
  - `Docs/Development/Chatbook/Chatbook-UX-1.md`
  - `Docs/Development/Chat/Chat-redux.md`
  - `Docs/Development/World-Lorebooks/Worldbooks.md`
  - `Docs/Development/Diarization/Diarization-Improve-1.md`
  - `Docs/Development/TTS/TTS-Improve-1.md`
  - `Docs/Code-Gen-1.md`

## ADR Gate

ADR required: yes
ADR path: `backlog/decisions/001-adopt-backlog-decisions-as-canonical-adrs.md`
Reason: This rollout establishes the canonical project rule for where ADRs live and when they are required.

## Preflight

- Work in the existing repository root: `/Users/macbook-dev/Documents/GitHub/tldw_chatbook`.
- Do not modify global Superpowers files under `~/.codex/superpowers/skills/`.
- Preserve tracked `Docs/` casing in all links and new docs.
- The workspace may contain unrelated untracked files. Do not stage or modify them.
- This is a documentation-only change. Use focused text verification instead of Python unit tests.
- Record the starting commit before implementation begins so final review can compare against the correct base.

## File Structure

- Create: `backlog/decisions/000-template.md`
  - Canonical ADR template for future decisions.
- Create: `backlog/decisions/001-adopt-backlog-decisions-as-canonical-adrs.md`
  - First canonical ADR, recording this workflow decision.
- Create: `backlog/decisions/README.md`
  - ADR index and workflow guide.
- Create: `backlog/decisions/historical-index.md`
  - Historical index of pre-canonical ADR-like docs.
- Modify: `AGENTS.md`
  - Add ADR workflow rules and update Backlog.md DoD/checklist language.
- Modify: one Backlog task under `backlog/tasks/`
  - The exact path is generated by Backlog.md after task creation.

## Implementation Tasks

### Task 1: Create Backlog Task And Start ADR-Gated Work

**Files:**
- Create or modify: `backlog/tasks/task-<id> - Adopt-canonical-ADR-workflow.md`

- [ ] **Step 1: Record base commit and list current tasks**

Run:

```bash
git rev-parse --short HEAD
```

Expected: Output is the base commit for this rollout. Record it as `<BASE_COMMIT>` for final review.

Then run:

```bash
backlog task list --plain
```

Expected: Output lists current tasks. If a task for adopting the ADR workflow already exists, use that task instead of creating a duplicate.

- [ ] **Step 2: Create the Backlog task if none exists**

Run:

```bash
backlog task create "Adopt canonical ADR workflow" -d "Establish backlog/decisions as the canonical ADR source of truth and update agent workflow guidance so significant architectural decisions are written, linked, and tracked consistently." --ac "Canonical ADR template exists,Canonical ADR index explains workflow and trigger rules,First canonical ADR records this workflow decision,Historical ADR-like docs are indexed without migration,AGENTS.md requires ADR checks during planning and closeout,Documentation verification passes"
```

Expected: Backlog creates a task file under `backlog/tasks/`. Record the returned task id and file path. Use this concrete task id in later ADR and task-closeout steps.

- [ ] **Step 3: Move the task to In Progress**

Run, replacing `<TASK_ID>` with the actual id:

```bash
backlog task edit <TASK_ID> -s "In Progress"
```

Expected: The task status is `In Progress`.

- [ ] **Step 4: Add implementation plan reference and ADR gate to the task**

Run, replacing `<TASK_ID>` with the actual id:

```bash
backlog task edit <TASK_ID> --plan "1. Follow Docs/superpowers/plans/2026-06-02-adr-workflow-implementation.md task-by-task.\n2. ADR required: yes.\n3. ADR path: backlog/decisions/001-adopt-backlog-decisions-as-canonical-adrs.md.\n4. Implement canonical ADR template, first ADR, index, historical index, and AGENTS.md workflow updates.\n5. Run focused documentation verification and update task closeout notes."
```

Expected: The task has an Implementation Plan section that links this plan and includes the ADR gate.

- [ ] **Step 5: Commit Backlog task setup**

Run, replacing the task path with the actual generated path:

```bash
git add "backlog/tasks/task-<id> - Adopt-canonical-ADR-workflow.md"
git commit -m "Track ADR workflow rollout task"
```

Expected: Commit succeeds with only the Backlog task file staged.

### Task 2: Add Canonical ADR Template

**Files:**
- Create: `backlog/decisions/000-template.md`

- [ ] **Step 1: Create the template file**

Use `apply_patch` to add `backlog/decisions/000-template.md`:

```markdown
# ADR-NNN: Short title

Status: Proposed | Accepted | Superseded by ADR-NNN
Date: YYYY-MM-DD
Related Task: backlog/tasks/task-N - Title.md
Supersedes: ADR-NNN or N/A

## Decision

One sentence stating what was decided.

## Context

Why this decision needed to be made, what problem it solves, and what constraints matter.

## Alternatives Considered

| Option | Why rejected |
| --- | --- |
| Alternative A | Reason |

## Consequences

What this decision means going forward, including accepted tradeoffs.

## Links

- Related task/spec/plan links.
```

- [ ] **Step 2: Verify template has required fields**

Run:

```bash
rg -n "Status:|Date:|Related Task:|Supersedes:|## Decision|## Context|## Alternatives Considered|## Consequences|## Links" backlog/decisions/000-template.md
```

Expected: Each required field or heading is found.

- [ ] **Step 3: Commit the template**

Run:

```bash
git add backlog/decisions/000-template.md
git commit -m "Add canonical ADR template"
```

Expected: Commit succeeds with only the template staged.

### Task 3: Add First Canonical ADR

**Files:**
- Create: `backlog/decisions/001-adopt-backlog-decisions-as-canonical-adrs.md`

- [ ] **Step 1: Create the first canonical ADR**

Use `apply_patch` to add `backlog/decisions/001-adopt-backlog-decisions-as-canonical-adrs.md`. Replace `<TASK_PATH>` with the actual Backlog task path created or identified in Task 1:

```markdown
# ADR-001: Adopt Backlog Decisions As Canonical ADRs

Status: Accepted
Date: 2026-06-02
Related Task: <TASK_PATH>
Supersedes: N/A

## Decision

Canonical Architecture Decision Records for `tldw_chatbook` live in `backlog/decisions/`, and significant architectural choices must be checked against this ADR workflow during planning and closeout.

## Context

The project has historical ADR-like material spread across development docs and feature-specific files. `AGENTS.md` already names `backlog/decisions/` as the project decision location, but that directory did not have a canonical ADR template, index, trigger rule, or workflow.

The project also uses Backlog.md tasks and Superpowers specs/plans to manage work. The ADR workflow needs to fit those existing systems rather than add a disconnected documentation lane.

## Alternatives Considered

| Option | Why rejected |
| --- | --- |
| Store canonical ADRs in `Docs/adr/` | This is a common convention, but it conflicts with the existing Backlog.md source-of-truth guidance that project decisions live in `backlog/decisions/`. |
| Require ADRs for every implementation task | This would create too much ceremony and make ADRs less useful. |
| Rely only on reviewer requests for ADRs | This would not reliably revive ADR usage or help agents decide when a decision needs durable context. |
| Add automated ADR validation immediately | The rule is new and should prove useful through checklist enforcement before adding brittle automation. |

## Consequences

New significant architectural decisions have a single canonical home. Backlog tasks and Superpowers plans must include an ADR check, but routine bug fixes and small implementation changes do not need ADRs.

Historical ADR-like docs remain useful context, but they are not canonical under the new immutability rules unless a future ADR explicitly imports or supersedes them.

Global Superpowers skills are not changed by this first rollout. The project can generalize this workflow later after using it on real tasks.

## Links

- Spec: `Docs/superpowers/specs/2026-06-02-adr-workflow-design.md`
- Plan: `Docs/superpowers/plans/2026-06-02-adr-workflow-implementation.md`
- ADR index: `backlog/decisions/README.md`
- Historical index: `backlog/decisions/historical-index.md`
```

- [ ] **Step 2: Verify the ADR does not contain placeholders**

Run:

```bash
rg -n "<TASK_PATH>|TODO|TBD|PLACEHOLDER|FIXME|\\[ \\]" backlog/decisions/001-adopt-backlog-decisions-as-canonical-adrs.md
```

Expected: No output. If `<TASK_PATH>` is found, replace it with the actual Backlog task path before continuing.

- [ ] **Step 3: Verify the ADR records the core decision**

Run:

```bash
rg -n "backlog/decisions|significant architectural|Backlog.md|Superpowers|Historical" backlog/decisions/001-adopt-backlog-decisions-as-canonical-adrs.md
```

Expected: Matches cover the canonical location, trigger scope, workflow integration, and historical-doc treatment.

- [ ] **Step 4: Commit the first ADR**

Run:

```bash
git add backlog/decisions/001-adopt-backlog-decisions-as-canonical-adrs.md
git commit -m "Record canonical ADR workflow decision"
```

Expected: Commit succeeds with only the first ADR staged.

### Task 4: Add ADR README And Index

**Files:**
- Create: `backlog/decisions/README.md`

- [ ] **Step 1: Create the ADR index and workflow guide**

Use `apply_patch` to add `backlog/decisions/README.md` with these sections:

```markdown
# Architecture Decision Records

Canonical ADRs for `tldw_chatbook` live in this directory.

ADRs explain why significant architectural decisions were made. Backlog tasks, Superpowers specs, and implementation plans explain what work is being done and how it is being executed.

## Index

| ADR | Status | Decision |
| --- | --- | --- |
| [ADR-001](001-adopt-backlog-decisions-as-canonical-adrs.md) | Accepted | Use `backlog/decisions/` as the canonical ADR location and require ADR checks for significant architectural choices. |

## Historical Decision Material

Some older decision material exists outside this directory. See [historical-index.md](historical-index.md). Historical entries are context, not canonical ADRs under the current immutability rules.

## When An ADR Is Required

Create or link an ADR when a task makes or changes a significant architectural choice, including:

- Storage, schema, migrations, sync, conflict policy, or data ownership.
- Provider/runtime boundaries, adapters, service contracts, or cross-module interfaces.
- Security, privacy, encryption, authentication, permissions, or data exposure.
- Dependency, framework, tooling, packaging, or runtime policy choices.
- Long-lived UX/application structure choices, such as navigation model or screen ownership.
- Decisions that reject a plausible alternative future contributors may ask about again.

Do not require an ADR for routine bug fixes, small UI polish, copy-only changes, mechanical refactors that preserve existing boundaries, test-only changes, or direct implementation of an existing ADR.

## Workflow

1. Read relevant ADRs before implementation planning.
2. Add an ADR check to every implementation plan:
   - `ADR required: yes/no`
   - `ADR path: backlog/decisions/NNN-short-title.md or N/A`
   - `Reason: brief explanation`
3. If an ADR is required, create it before implementation starts.
4. Link ADRs from Backlog task plans, Superpowers plans, and implementation notes when relevant.
5. At closeout, record ADRs created, superseded, or followed.

## Immutability

Accepted ADRs are immutable except for typo fixes, link repairs, or metadata that does not alter the decision. If a decision changes, create a new ADR and mark the old one as `Superseded by ADR-NNN`.

## Naming

Use numeric filenames:

- `000-template.md`
- `001-short-title.md`
- `002-short-title.md`

Do not reuse numbers.
```

- [ ] **Step 2: Verify the README includes the required workflow language**

Run:

```bash
rg -n "ADR required: yes/no|When An ADR Is Required|Immutability|Historical Decision Material|Do not reuse numbers" backlog/decisions/README.md
```

Expected: All required guide sections are present.

- [ ] **Step 3: Commit the README**

Run:

```bash
git add backlog/decisions/README.md
git commit -m "Document ADR workflow index"
```

Expected: Commit succeeds with only the README staged.

### Task 5: Add Historical ADR-Like Index

**Files:**
- Create: `backlog/decisions/historical-index.md`

- [ ] **Step 1: Create the historical index**

Use `apply_patch` to add `backlog/decisions/historical-index.md`:

```markdown
# Historical ADR-Like Material

This file indexes decision material that predates the canonical ADR workflow in `backlog/decisions/`.

These documents are useful context, but they are not canonical ADRs under the current immutability rules unless a future canonical ADR explicitly imports or supersedes them.

## Feature-Specific ADR Files

- [Higgs ADR-001: Backend Architecture](../../Docs/Development/TTS/Higgs-ADR-001-Backend-Architecture.md)
- [Higgs ADR-002: Backend Registration and Configuration](../../Docs/Development/TTS/Higgs-ADR-002-Backend-Registration.md)
- [Higgs ADR-003: Configuration Design](../../Docs/Development/TTS/Higgs-ADR-003-Configuration-Design.md)
- [Higgs ADR-004: Voice Profile Management Design](../../Docs/Development/TTS/Higgs-ADR-004-Voice-Profile-Management.md)
- [Higgs ADR-005: Implementation Summary](../../Docs/Development/TTS/Higgs-ADR-005-Implementation-Summary.md)

## Embedded ADR Sections

- [RAG design ADRs](../../Docs/Development/RAG/RAG-DESIGN.md#architecture-decision-records-adrs)
- [RAG testing ADRs](../../Docs/Development/RAG/RAG-TESTING-1.md#architecture-decision-records-adrs)
- [Subscriptions implementation ADRs](../../Docs/Development/Subscriptions/Subscriptions-Implementation-1.md#architecture-decision-records-adrs)
- [Chatbook UX ADRs](../../Docs/Development/Chatbook/Chatbook-UX-1.md#architecture-decision-records)
- [Chat redux ADRs](../../Docs/Development/Chat/Chat-redux.md#implementation-progress--architecture-decision-records-adrs)
- [World Lorebooks ADRs](../../Docs/Development/World-Lorebooks/Worldbooks.md#architecture-decision-record)
- [Diarization ADRs](../../Docs/Development/Diarization/Diarization-Improve-1.md#architecture-decision-records-adrs)
- [TTS improvement ADRs](../../Docs/Development/TTS/TTS-Improve-1.md#architecture-decision-records-adrs)
- [Code generation ADRs](../../Docs/Code-Gen-1.md#architecture-decision-records)

## Notes

- Historical numbering is local to each old document and does not reserve canonical ADR numbers.
- Do not edit old docs to make them fit the canonical template.
- If a historical decision needs to change, create a new canonical ADR in `backlog/decisions/`.
```

- [ ] **Step 2: Verify the historical links use tracked `Docs/` casing**

Run:

```bash
rg -n "docs/" backlog/decisions/historical-index.md
```

Expected: No output.

- [ ] **Step 3: Verify expected historical entries are present**

Run:

```bash
rg -n "Higgs ADR-001|RAG design|Subscriptions implementation|Chatbook UX|Chat redux|World Lorebooks|Diarization|TTS improvement|Code generation" backlog/decisions/historical-index.md
```

Expected: Each expected historical category is found.

- [ ] **Step 4: Commit the historical index**

Run:

```bash
git add backlog/decisions/historical-index.md
git commit -m "Index historical ADR-like material"
```

Expected: Commit succeeds with only the historical index staged.

### Task 6: Update AGENTS.md Workflow Guidance

**Files:**
- Modify: `AGENTS.md`

- [ ] **Step 1: Add canonical ADR guidance near Backlog source-of-truth rules**

In `AGENTS.md`, under `## 1. Source of Truth`, expand the project-decision bullet into this guidance:

```markdown
- Project decisions are in **`backlog/decisions/`**.
- Canonical Architecture Decision Records (ADRs) live in **`backlog/decisions/`**. Historical ADR-like material elsewhere is reference-only unless a canonical ADR imports or supersedes it.
- Before implementation planning, read relevant ADRs and decide whether the task requires a new ADR.
```

- [ ] **Step 2: Add ADR trigger rules after the Source of Truth section**

Add a new `## Architecture Decision Records (ADRs)` subsection inside the Backlog.md guidelines:

````markdown
## Architecture Decision Records (ADRs)

Use ADRs to record significant architectural decisions: what was decided, why, and what alternatives were considered.

An ADR is required for decisions involving storage/schema/migrations, sync/conflict policy, data ownership, provider or runtime boundaries, service contracts, cross-module interfaces, security/privacy/encryption/authentication, dependency/tooling/runtime choices, long-lived UX/application structure, or rejection of a plausible alternative future contributors may ask about again.

An ADR is not required for routine bug fixes, small UI polish, copy-only changes, mechanical refactors that preserve existing boundaries, test-only changes, or direct implementation of an existing ADR.

Every implementation plan must include:

```text
ADR required: yes/no
ADR path: backlog/decisions/NNN-short-title.md or N/A
Reason: brief explanation
```

If an ADR is required, create it before implementation begins and link it from the Backlog task, Superpowers plan, and final implementation notes. If an existing ADR applies, link it instead of creating a duplicate.
````

When inserting this into `AGENTS.md`, make sure the fenced `text` block does not accidentally terminate a larger existing fenced block. Place the subsection outside existing code fences.

- [ ] **Step 3: Update the typical workflow documentation**

In the existing Typical Workflow section, update the documentation-reading step so it says to read relevant decision files, not every decision file when the directory grows:

```markdown
# Read relevant documentation files in `backlog/docs/`.
# Read relevant ADRs in `backlog/decisions/`; create/link one if the task makes a significant architectural decision.
```

- [ ] **Step 4: Update final steps before Done**

Add this item to the final-steps list before documentation closeout:

```markdown
5. Completed the ADR check: linked an existing ADR, created a new ADR, or documented why no ADR was required
```

Renumber later items if needed.

- [ ] **Step 5: Update Definition of Done**

Add this DoD item:

```markdown
9. **ADR hygiene**: ADR check completed; any new or superseded ADRs are linked from the task Implementation Plan and Implementation Notes.
```

If the existing list remains numbered 1-8, renumber it consistently.

- [ ] **Step 6: Verify AGENTS.md includes the ADR workflow**

Run:

```bash
rg -n "Canonical Architecture Decision Records|ADR required: yes/no|ADR hygiene|significant architectural decision|Historical ADR-like" AGENTS.md
```

Expected: Matches show the new source-of-truth rule, trigger rule, implementation-plan gate, and DoD item.

- [ ] **Step 7: Commit AGENTS.md changes**

Run:

```bash
git add AGENTS.md
git commit -m "Document ADR workflow in agent guidance"
```

Expected: Commit succeeds with only `AGENTS.md` staged.

### Task 7: Final Verification And Backlog Closeout

**Files:**
- Modify: `backlog/tasks/task-<id> - Adopt-canonical-ADR-workflow.md`

- [ ] **Step 1: Verify all intended files exist**

Run:

```bash
test -f backlog/decisions/000-template.md
test -f backlog/decisions/001-adopt-backlog-decisions-as-canonical-adrs.md
test -f backlog/decisions/README.md
test -f backlog/decisions/historical-index.md
```

Expected: All commands exit successfully.

- [ ] **Step 2: Verify no stale lowercase tracked documentation-tree paths were introduced in ADR artifacts**

Run:

```bash
rg -n "docs/(Development|Design|Features|Parity|Code|superpowers)" backlog/decisions AGENTS.md
```

Expected: No lowercase tracked documentation-tree path references. Valid `backlog/docs/` references may remain in `AGENTS.md`.

- [ ] **Step 3: Verify no placeholders remain**

Run:

```bash
rg -n "TODO|TBD|PLACEHOLDER|FIXME|<TASK_ID>|<TASK_PATH>" backlog/decisions AGENTS.md
rg -n "\\[ \\]" backlog/decisions
```

Expected: No placeholder output from final ADR artifacts or `AGENTS.md`, and no unchecked checklist output from ADR artifacts. Existing checkbox examples in `AGENTS.md` are allowed.

- [ ] **Step 4: Verify required workflow phrases**

Run:

```bash
rg -n "ADR required: yes/no|001-adopt-backlog-decisions-as-canonical-adrs|historical-index|ADR hygiene|Canonical Architecture Decision Records" backlog/decisions AGENTS.md
```

Expected: Matches cover implementation-plan gate, first ADR, historical index, DoD hygiene, and canonical ADR source-of-truth language.

- [ ] **Step 5: Check diff whitespace**

Run:

```bash
git diff --check
git diff --check <BASE_COMMIT>
```

Expected: No whitespace errors.

- [ ] **Step 6: Self-review the complete diff**

Run:

```bash
git diff --stat <BASE_COMMIT> -- AGENTS.md backlog/decisions "backlog/tasks/task-<id> - Adopt-canonical-ADR-workflow.md"
git diff <BASE_COMMIT> -- AGENTS.md backlog/decisions "backlog/tasks/task-<id> - Adopt-canonical-ADR-workflow.md"
```

Expected: Diff is limited to the Backlog task, ADR artifacts, historical index, and `AGENTS.md`.

- [ ] **Step 7: Update the Backlog task acceptance criteria and implementation notes**

Edit the generated task file or use `backlog task edit <TASK_ID>` so:

- All acceptance criteria are checked.
- Implementation Notes mention:
  - Added canonical ADR template.
  - Added first ADR for canonical Backlog decisions.
  - Added ADR README and historical index.
  - Updated `AGENTS.md` with ADR trigger/check/DoD guidance.
  - Verification commands run.

- [ ] **Step 8: Mark the task Done**

Run, replacing `<TASK_ID>` with the actual id:

```bash
backlog task edit <TASK_ID> -s Done
```

Expected: Task status is `Done`.

- [ ] **Step 9: Commit Backlog closeout**

Run, replacing the task path with the actual generated path:

```bash
git add "backlog/tasks/task-<id> - Adopt-canonical-ADR-workflow.md"
git commit -m "Close ADR workflow rollout task"
```

Expected: Commit succeeds with only the Backlog task closeout staged.

## Plan Review

Local plan review passed on 2026-06-02. The review checked:

- The plan covers every spec artifact: template, README/index, historical index, first ADR, and `AGENTS.md`.
- The plan includes an explicit ADR gate.
- The plan does not modify global Superpowers skill files.
- The plan preserves tracked `Docs/` casing.
- Verification does not depend on automated tests for a documentation-only rollout.
- Final verification checks the complete rollout diff from the recorded base commit.

External plan-document-reviewer subagent review is not required unless the user explicitly asks for delegation, because the available subagent tool is restricted to explicitly delegated work.
