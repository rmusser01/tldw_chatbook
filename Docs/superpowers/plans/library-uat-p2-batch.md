# Plan: Library UAT P2 batch (backlog task-2858, LIB-03/09/11/12/13/14/15/17/18/19)

Branch `fix/library-uat-p2-batch` (worktree `.worktrees/library-p2`), cut from dev `6b38a13b8` (which INCLUDES the P1 arc, PR #1410).
Source: critique snapshot `.impeccable/critique/2026-08-07T01-01-42Z__tldw-chatbook-ui-screens-library-screen-py.md`; findings observed at dev `6ffa56516` — TWO DAYS and one large Library arc ago. **Every item must be re-verified at HEAD before implementing; several may be partially or fully fixed by the P1 arc** (footers/F1 were reworked by task-2856; export strings by task-2857; previews by task-1993). A finding that no longer reproduces gets a recorded delta, not a blind fix.
The backlog task file `backlog/tasks/task-2858 - Library-UAT-P2-batch-routes-receipts-viewer-notes-rail-widths.md` holds the binding ACs; the four plan tasks below partition them. The LAST task checks any remaining ACs and flips the backlog task Done.

## Global Constraints

- **Python/tests**: `/Users/macbook-dev/Documents/GitHub/tldw_chatbook/.venv/bin/python` for everything; pytest is the ONLY entry point that may import app modules (a bare `python3 -c` import writes to the LIVE config). Targeted tests only + `--collect-only -q` over `Tests/Library` as collection sanity. "no tests ran" = failed gate; read the passed count. Foreground Bash only — NEVER end your turn waiting on background work.
- **TDD**: failing test first (state the RED evidence), then green. Rendered-geometry assertions (region.width/offset) for anything layout-visible — the P1 arc proved headless DOM queries miss off-screen/oversized widgets.
- **CSS**: never edit `tldw_chatbook/css/tldw_cli_modular.tcss` (generated). Edit sources under `tldw_chatbook/css/`, regenerate via `.venv/bin/python tldw_chatbook/css/build_css.py`; source and bundle must land in the same commit. Widget `DEFAULT_CSS` (in .py) needs no bundle step. `outline: heavy` on 1-row compact widgets EATS the label — prove labels readable when focused with ANSI evidence.
- **Recompose discipline**: in-place updaters must own every conditional compose() owns; display-managed always-mounted widgets over recompose on hot paths; a new state/distinction must be learned by EVERY aggregating surface.
- **Bindings/footer honesty**: multiple same-key bindings resolve via `check_action` in declaration order; a binding on a shared base is advertised everywhere — gate with `check_action`. Footer/F1 advertise only keys that work on the current surface. The Library Escape matrix (6 gates) and `_register_footer_shortcuts` (compose_content-centralized + per-mode sets) shipped in the P1 arc — extend those seams, never fork them.
- **String sweeps**: when renaming/removing user-facing copy, closure evidence is a plain `grep -rn -i` over the owning trees with every hit fixed-or-justified — pattern-list scripts have provably missed strings that reach the UI one hop away (exception → mark_failed(error=…) → queue row).
- **Live verification (required per task)**: from the worktree root, unique socket `p2T<N>lib$RANDOM`, scratch profile:
  `mkdir -p /tmp/p2T<N> && printf '[general]\nusers_name = "sdd_p2t<N>"\n[first_run]\nsetup_started = true\nsetup_completed = true\n' > /tmp/p2T<N>/config.toml`
  `tmux -L <socket> new-session -d -x 170 -y 50 'TLDW_CONFIG_PATH=/tmp/p2T<N>/config.toml /Users/macbook-dev/Documents/GitHub/tldw_chatbook/.venv/bin/python -m tldw_chatbook.app'`
  Cold start ~12s (don't navigate during it). Palette nav via `send-keys C-p` + exact entry text. Ctrl+digit CANNOT be sent via tmux. Clicks: SGR press+release, COL by CHARACTER index (python str.find), never byte offsets. Verify pane identity (`ps eww <pid>` shows YOUR config) before trusting captures. Prove focus/selection with `capture-pane -p -e` ANSI attributes. Cleanup: C-q, kill-server, `rm -rf ~/.local/share/tldw_cli/sdd_p2t<N>`.
- **Backlog hygiene**: task-2858 is already In Progress with this plan recorded. Each plan task checks off ONLY the ACs it owns (edit the task file), appends its part of the Implementation Notes; plan Task 4 flips status Done if all ACs are checked.
- **Git**: commit per task, message ends `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; `git add` SPECIFIC paths only; NEVER stash; NEVER push (controller pushes).
- **Docs**: UI-visible changes update the matching `Docs/User_Guide/` page or its "Verified against" stamp (stamp convention on this branch: the branch-base commit `6b38a13b8`).
- **Known ambient test debt (do NOT fix, do NOT count against yourself)**: task-3022 tracks ~7 fixture-bypass failures (`_library_ingest_preflight_generation`), `test_landing_footer_advertises_the_landing_keyboard_story`, and `test_shared_form_and_native_inputs_use_thin_non_semantic_focus` — A/B against clean HEAD before blaming your change on anything else.
- **Key code map**: `tldw_chatbook/UI/Screens/library_screen.py` (~19k lines), `tldw_chatbook/Widgets/Library/*.py`, `tldw_chatbook/Library/*.py` (state modules), `UI/Navigation/screen_registry.py` + `shell_destinations.py`, app-level nav in `app.py`. Tests: `Tests/Library/`, `Tests/UI/`.

## Task 1: Entry routing and canvas restoration (LIB-03 → AC#1)

Backlog AC#1: "Library landing is route-independent (one canonical landing) or intentionally routed with the difference stated on-screen; revisits restore the last canvas or intentionally reset (decision recorded)."

Observed at `6ffa56516`: palette "Library" landed the Import canvas (toast "Opened Library to ingest content"); palette "Switch to Library" landed the hub; a Home→Library round trip reset the previously-visited canvas (Search/RAG) back to Import. Re-verify all three at HEAD first — the P1 arc's task-2851 touched `_LEGACY_ROUTE_LIBRARY_NAV_CONTEXT` / `LIBRARY_NAV_MODE_TO_ROW_ID` and may have shifted routing.

Direction (deviate only with recorded reason): EXPLICIT deep links (palette "Library: Add content…", "Open Media Library"→Library Media, "Library — Skills", Study staging etc.) keep landing their specific canvas — they state their destination in their own label, which satisfies "intentionally routed with the difference stated". The GENERIC entries ("Switch to Library", tab click, bare `NavigateToScreen("library")` with no nav-context) must all land ONE canonical surface: the last-visited canvas if the screen instance/session remembers one, else the hub. Revisits therefore RESTORE (decision: restore-over-reset — a workbench should not lose your place; record it in the task notes). Investigate where the default row/canvas is chosen on mount (grep `LIBRARY_NAV_CONTEXT_MODE`, the default `_library_selected_row_id` assignment, and any "default_tab"/boot nav-context path) and whether a remembered-canvas field already exists (the collapse-prefs persistence suggests a per-session state home).

Tests: pilot round trips for (a) generic entry → hub on first visit, (b) deep link → its canvas, (c) visit Search/RAG → leave → generic re-entry → Search/RAG restored. Live: same three flows.

## Task 2: Honest advertisement and export feedback (LIB-09, LIB-11, LIB-12 → AC#2, AC#3)

Backlog AC#2: "F1 and footers advertise only keys that work on the current surface." AC#3: "The Export button is never a silent no-op, and a successful export leaves a durable on-canvas receipt with the output path."

- LIB-09 (re-verify FIRST — likely partially fixed): at `6ffa56516`, F1 on the Media canvas was titled "LibraryScreen Shortcuts" and listed skills/evidence keys (`ctrl+s: Save skill`, `escape: Back to skills list`); the media-viewer footer advertised `u` (inert there; works on Search/RAG). The P1 arc added check_action gates to every Escape binding and Textual's key panel filters on check_action — so the F1 contamination may be gone for gated bindings but may persist for any UNGATED binding on the screen (audit the full BINDINGS list: every binding either works everywhere on the screen or carries a check_action gate). The `u` binding: find its owner and gate/advertise it accurately.
- LIB-11: Export canvas with nothing to export / no destination — clicking "Export bundle (.zip)" gave zero response. Fix per house style: disabled-with-reason (the disabled-controls-say-why principle; see Collections' enable-Create guidance for the idiom) — reason text adjacent to the button naming the blocker ("Nothing to export in this scope." already exists as a line — wire the button's disabled state + tooltip to the same predicate so a click can't be swallowed silently).
- LIB-12: a successful export writes the zip but the canvas is pixel-identical afterwards. Add a durable "Last export: <path> · <relative time>" line on the Export canvas (display-managed Static, in-place updated; survives leaving/re-entering the canvas within the session at minimum — persist further only if an obvious existing seam offers it).

Tests: BINDINGS audit test (every binding gated-or-universal); disabled-reason rendering; receipt line appears after a real (temp-dir) export and shows the path. Live: F1 on Media, viewer footer, empty-export click (disabled + reason), real export → receipt.

## Task 3: Content surfaces — media viewer markdown and note lifecycle (LIB-13, LIB-14 → AC#4, AC#5)

Backlog AC#4: "The media viewer renders markdown (with a raw toggle) for markdown media." AC#5: "Blank notes no longer commit literal 'Untitled' rows that require hand-deletion; version stamps change only on content saves."

- LIB-13 (re-verify FIRST — task-1993 shipped front-matter-aware markdown parsing for notes/library previews since the observation): at `6ffa56516` the viewer showed literal `#`/`##`/table pipes while Notes Preview rendered properly. If still raw: render markdown-typed media content with the SAME renderer Notes Preview uses (find it — do not add a new markdown dependency or a second pipeline; the console-markdown program's helpers exist) behind a Rendered|Raw toggle (text-labeled strip, Rendered default for markdown types, Raw default for everything else). Escaping lesson from the RAG arc: escaping must be the TERMINAL step of any display pipeline — reuse the existing safe path, never re-order around it. Mind the per-item content search ("Match N of M") — if it targets the raw text, searching must either operate on the visible mode or state which text it searches.
- LIB-14: "Blank note" instantly commits a DB row before typing; the title is literal text "Untitled" (typing produced "UntitledAtlas follow-ups"); the version stamp bumped from clicking Preview. Fix: (a) title becomes a placeholder or select-all-on-focus so first typing replaces it; (b) either create-on-first-edit or garbage-collect an untouched blank on exit (pick the smaller-diff option that the note-creation seam supports; the optimistic-locking versioning lives in ChaChaNotes — do NOT change DB semantics, change when the UI commits); (c) Preview/view toggles must not bump the version (find what write the toggle triggers and stop it — version on content save only).

Tests: markdown render + raw toggle (assert rendered output lacks literal `#` for a heading fixture and the toggle restores it); blank-note flow (no DB row until first edit OR untouched row GC'd — assert via the real in-memory DB); version stable across Preview toggles, bumps on save. Live: open the markdown fixture in the viewer both modes; create a blank note, type a title cleanly, toggle Preview twice, check the version stamp.

## Task 4: Rail determinism, input traps, widths, folder-notes copy (LIB-15, LIB-17, LIB-18, LIB-19 → AC#6, AC#7, AC#8) + close out task-2858

- LIB-15: rail gloss/count lifecycle is nondeterministic ("Collections — item sets" → "(0)" → bare → "(1)"; some rows keep glosses after visits, some lose them). Find the gloss/count render rule in `library_rail.py`/shell state and make it ONE deterministic rule for ALL rows (recommend: gloss always shown when it fits at current width AND count always shown once known — but read the code's intent first; if the drop-gloss-after-first-visit behavior is deliberate teaching decay, make it uniform and document it). Test pins the rule across visit/leave/re-enter for two different rows.
- LIB-17: clicking into a prefilled query Input lands the cursor at position 0 (typed text PREPENDS — B produced "quokkazzqxv"); the rail search box retains stale queries across screen switches. Fix: cursor-to-end (or select-all) on click/focus into prefilled search Inputs (check whether this is a Textual default quirk our Input subclass can correct — the rail search already has a second-`/` select-all re-arm, extend that seam); clear (or select-all) the stale rail query on screen re-entry so typing replaces rather than appends. Decide clear-vs-select-all by what the `/` seam already promises and record it.
- LIB-18: width sweep (re-verify at HEAD; P1 arc did not touch these): at ≤120 rail row labels truncate mid-word (`Conversa... (0)`, `Flash... due: 0`); at ≤100 the footer's screen-specific keys vanish behind a leading `…`; at 80 the nav bar hard-cuts "⌃6 Watc" mid-word. In scope per AC#8: NO rail row label truncates mid-word at 120/100/80 (short-label fallbacks or width-aware gloss/count dropping — coordinate with your LIB-15 rule so they compose). The footer and nav-bar items: footer = order the screen-specific keys FIRST or compact the hint set at narrow widths if the footer widget offers a seam (task-2860 owns the F6-filter bug — don't collide); nav-bar mid-word cut is shared chrome — fix ONLY if a small change in the nav's own overflow logic covers it, else record it as out-of-scope with the file:line for a follow-up.
- LIB-19: Database mode, Files mode, and the Sync sub-canvas are three folder-notes concepts never related anywhere. Smallest honest fix: one sentence of placement copy on each surface ("Files mode edits a folder directly — unlike Sync, which mirrors a folder into the Library.") — match DESIGN.md voice, no new UI structure.
- CLOSE-OUT: after your items, verify EVERY AC checkbox in task-2858 reflects reality (including Tasks 1-3's), append the consolidated Implementation Notes summary, flip status Done.

Tests: rail-rule pin; cursor/select-all behavior; width sweep assertions at 120/100/80 (rendered-geometry or string assertions on the captured rail); copy presence. Live: the three-width sweep (fresh sessions at 120×35, 100×30, 80×24), search-input click-then-type, the three folder-notes surfaces.
