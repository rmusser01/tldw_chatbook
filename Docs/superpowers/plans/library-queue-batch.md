# Plan: Library queue batch (tasks 14903, 4026, 14901, 4025, 14900, 14902)

Branch `feat/library-queue-batch`, cut from dev `190eb1aad`. Six backlog items — the full open Library queue after the honesty+accessibility batch (PR #1488). Six plan tasks in dependency order: the crash guard leads (HIGH); the trash contract (4026) must land before the Trash surface (4025) because a user-visible Trash makes the implicit un-trash contract untenable; the reversibility rule (14901) informs 4025's copy and receipts; the two UX redesigns (14900, 14902) close.

Each task owns its backlog file end-to-end (In Progress + Implementation Plan before code; ACs checked + Implementation Notes + Done after).

**Standing rule (5 saves and counting): re-verify every finding at HEAD before implementing.** Dev moves fast; close dissolved findings on evidence.

## Global Constraints

- **Python/tests**: `.venv/bin/python` from the worktree root; pytest is the ONLY entry point that may import app modules — a bare `python` heredoc importing the TEST HARNESS is NOT conftest-isolated and has leaked probe input into the live user config (see `backlog/docs/lessons-live-verification.md`, task-4023 incident). Targeted tests + `--collect-only -q` sanity; "no tests ran" = failed gate. **Foreground Bash only — NEVER end your turn waiting on background work**; if a suite exceeds the tool timeout, split per-file or per `-k` selector.
- **TDD**: failing test first, RED reproducing the OBSERVED EFFECT. Contrast/colour claims need ANSI decode (`capture-pane -e`); a colorless capture is not evidence about a colour mechanism.
- **Real DB for DB-layer claims** — file-backed SQLite, never mocks. Chunked-content paths must include `chunks` in at least one test (the task-4022 Critical was invisible because every test omitted them).
- **CSS**: never edit the generated bundle; source tcss → `build_css.py` → `check_bundle_sync.py`. `App.CSS_PATH` outranks widget `DEFAULT_CSS` — prove fixes at the tier that wins live.
- **Recompose discipline**: any conditional a compose branch owns, the in-place updater must own too. Sweep `.label =` writes as well as `.disabled =` writes — the #1488 whole-branch Important was a patcher rewriting a label without touching the flag.
- **Marker/grammar vocabulary is now CONVENTION** (task-4023 AC#5, recorded in `library_shell_state.py` comments): leading `▸ ` = selected row; `(selected)` = strip option; `┃…┃` = focus; header `▸/▾` = disclosure; trailing ` ⇄` = cycle control; `○` prefix + reason tooltip = disabled. New surfaces MUST conform — extend `library_cycle_label`/`library_disabled_action_label`/the one footer builder, never fork. Footer/F1 share `_library_footer_shortcuts_for_current_state()`.
- **Live verification** per UI task: unique socket `lqT<N>lib$RANDOM`, scratch `/tmp/lqT<N>`, `users_name = "sdd_lq<N>"`, `[first_run] setup_started/completed = true`; clicks by CHARACTER index; `capture-pane -p -e` for styling proof; cleanup (C-q, kill-server, rm -rf ~/.local/share/tldw_cli/sdd_lq<N>). After ANY probe that could write state, grep the live config for the probe's own inputs.
- **Git**: commit per task; message ends `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; `git add` SPECIFIC paths; never stash; never push (controller pushes). Worktree path explicit in every git command.
- **Docs**: UI-visible changes update the matching `Docs/User_Guide/` page (stamp `Verified against dev @ 190eb1aad` + date).
- **Backlog CLI trap**: `backlog task edit --notes` REPLACES the whole section — hand-edit task files instead.
- **Known ambient on dev**: ~45 `test_library_shell.py` 60x20-geometry failures; 2 `test_destination_shells.py` [mcp|tools_settings]; skills shadow-name/video-command failures; `Tests/UI` has a known hang shape (task-14912). A/B against clean base before blaming your change.

## Task 1: Crash guard — text-selection MouseDown during relayout (task-14903, HIGH)

Read `backlog/tasks/task-14903*.md`. The app died once, live: a MouseDown on the Search/RAG canvas's `#library-rag-query-quiet-line` Static ~1s after a 50→24-row resize hit `textual/screen.py:1914` `<text-selection begin>` with `container = None` → unhandled AttributeError → app terminated. Not deterministically reproducible by replay; the window is a click dispatched while the clicked widget's ancestor chain is being replaced by recompose/relayout.

1. **Reproduce synthetically**: drive a `MouseDown` into a screen mid-recompose in a pilot test (e.g. monkeypatch/patch the timing seam, or construct the state where `get_widget_at` resolves a widget whose selection container is None — read Textual 8.2.x `screen.py` `_forward_event`/text-selection begin path to find the exact None source) OR, if the race genuinely cannot be forced through public seams, a unit-level reproduction that calls the failing code path with the crash-state inputs is acceptable; the AC allows "conclusively attributed with a written analysis" as the fallback — but try the reproduction first and show the attempts.
2. **Guard**: the app must survive the click. Prefer the narrowest App/Screen-level seam: an override or wrapper that catches exactly this signature (AttributeError from the text-selection begin path when the container resolves to None) and drops the event, NOT a blanket exception handler that would swallow real bugs. Check whether newer Textual 8.x patch releases fixed it (read the changelog/source in .venv and on GitHub if cheap) — a pinned version bump that fixes it beats a workaround, but do NOT bump the pin speculatively; any bump needs the full ambient battery A/B'd.
3. File the upstream-report text into the task notes (signature + analysis) so the owner can submit it.

## Task 2: The overwrite=True trash contract (task-4026)

Read `backlog/tasks/task-4026*.md`. Pre-existing: `_media_payload` hardcodes `is_trash: 0, trash_date: None, deleted: 0`, so `overwrite=True` against a trashed row silently un-trashes it; task-4022 made non-overwrite restore opt-in (`restore_trashed=False`), throwing this into relief; `Media/local_media_reading_service.py:1836`'s create-reading-item path is the named caller.

**Decide the contract: resurrection requires an explicit decision.** Direction (justify or revise in your plan): `overwrite=True` alone should NOT resurrect — gate the un-trash fields in `_media_payload` on `restore_trashed` (the same opt-in), so the two flags compose: `overwrite=True, restore_trashed=False` updates content but LEAVES trash state; both True = full restore-and-overwrite. Beware: task-4022's tests pin some `overwrite=True` un-trash behaviour (`TestRestoreTrashedIsOptIn`, the explicit-clear test uses `restore_trashed=True, overwrite=True`) — read them first; changing the contract means REWRITING the pins that assert resurrection-by-overwrite-alone, with the task file documenting the change. Audit EVERY `overwrite=True` caller (grep all of `tldw_chatbook/`) against the decision; fix or justify each in the notes. Real-DB pins for: trashed+overwrite (stays trashed, content updated — or whatever is decided), trashed+overwrite+restore_trashed (restored), chunked variant of at least one.

## Task 3: One reversibility rule (task-14901)

Read `backlog/tasks/task-14901*.md`. Three reversibility stories ship on one screen today: blank-note GC (silent, no undo), bulk media delete (receipt + Undo, task-4022), single media delete (confirm then silence). Write THE RULE first, as a decision file (`backlog/decisions/` — follow the existing ADR naming there), then make the surfaces conform:

Direction to draft against (revise with reasons if the audit disagrees): **destroying persisted user data always leaves a receipt naming the recovery path** (Undo while present, Trash after — Task 4 builds the surface; write the rule so it names Trash as the durable story and Task 4 as its implementation); single media delete gets the SAME receipt/Undo pattern as bulk (it is one-item bulk — reuse `_undo_library_media_bulk_delete`'s seam, do not fork a second undo path); blank-note GC is the NAMED EXCEPTION (it destroys only never-touched session artifacts, nothing user-authored — state this in the rule with the guard conditions from task-4021); unsaved prompt/skill drafts discard-with-confirm and are NOT receipted (nothing persisted is destroyed) — but check what persisted prompt/skill DELETE does today and make it conform or name it. Notes deletion (the real kind, if reachable) must match the rule. Keep scope honest: this task ships the rule + single-media-delete receipt + any one-line copy fixes; anything structural it uncovers beyond that gets filed, not half-built.

## Task 4: The Trash surface (task-4025)

Read `backlog/tasks/task-4025*.md` (all four ACs). A browsable, restorable Trash: reachable from the Media canvas (pick the mechanism that fits the existing grammar — a `type:` filter value cycles through the shared cycle control; a rail entry adds a row; judge against the rail's existing section logic and say why), listing every `is_trash=1` item, restore per item via the existing `restore_from_trash`/`restore_media_item` seam (currently unwired), list + rail counts updating in place, delete-confirmation copy (bulk AND single) updated to point at the surface, and the FTS question answered EXPLICITLY: trashed items still surface from full-text search today (pre-existing, symmetric); decide whether search results say/filter trashed state and implement the decision. Conform to: marker vocabulary, footer/F1 seam, F-018 disabled reasons, the Task 3 rule (restore is the durable recovery path the receipts point at), and the Task 2 contract (restore from Trash is an explicit `restore_trashed` operation). Live-verify AC#4's full cycle: delete → dismiss receipt → find in Trash → restore → verify back in the list with counts right.

## Task 5: Media side-by-side layout (task-14900)

Read `backlog/tasks/task-14900*.md` (all three ACs). At wide widths the Media canvas renders list | preview/detail side by side (the Collections workbench `#library-collections-workbench` is the in-house precedent — read its breakpoint + CSS approach first and reuse the pattern); below the breakpoint the stacked layout is preserved. Keyboard traversal works in both layouts and the footer advertises it honestly (the shared seam); Select mode + bulk toolbar fully usable in both. Interacts with Task 4: the Trash view lives in this canvas — build on the post-Task-4 shape. Live-verify at ≥3 widths (wide/breakpoint-edge/compact) including select mode and viewer entry, keyboard-only.

## Task 6: Cycler option menus (task-14902)

Read `backlog/tasks/task-14902*.md` (all three ACs). The Notes Sort control's choice-strip (press → one-row strip with ✓ on the active option) is the in-house discoverable pattern. Converge every Library cycler on it (media type — including any Trash value Task 4 added; prompts sort/collection; skills sort + editor toggles; export quality; Search/RAG mode): pressing shows the full option set on screen, the user can pick directly, active option carries a non-colour marker consistent with the vocabulary (`✓` or `(selected)` — match the Notes strip), footer/F1 advertise it where keyboard-reachable. Retire the per-press cycle where it no longer earns its place — but if a surface has a genuine one-press-toggle case, keep it and say why. Extend the shared builders (`library_cycle_label` family) — a second labelling path is a regression against AC#5's one-grammar contract. Live-verify at least three cyclers including keyboard path.
