# F1 — Library/Home follow-ups fix batch — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Anchors below come from the 2026-07-10 inventory (exact file:line at branch point 02da5560; grep symbols, lines drift).

**Goal:** Ten small, independently testable fixes carried from the H1–L3b redesign. Branch `claude/library-followups` off `origin/dev` (02da5560), worktree `.claude/worktrees/library-followups`.

**Global constraints:** house rules as prior phases — explicit-path staging; Fable 5 co-author line; RED-first for behavior changes; bounded polls in pilots; `escape_markup` for user text in labels; venv pytest with isolated test HOME; CSS via source + `./build_css.sh` when touched.

## Group A — ingest/notes/media small fixes

**A1. Server Jobs rename (real pre-existing bug, server-mode).** `UI/MediaIngestWindowRebuilt.py` calls scope-service methods that don't exist. Rename 6 call sites to the REAL `MediaReadingScopeService` methods (params are 1:1 identical): `:854 submit_media_ingest_jobs→submit_ingest_jobs`; `:941,:992 getattr(...,"stream_media_ingest_job_events")→"stream_ingest_job_events"`; `:1032 list_media_ingest_jobs→list_ingest_jobs`; `:1069 cancel_media_ingest_jobs_batch→cancel_ingest_batch`; `:1117 cancel_media_ingest_job→cancel_ingest_job`. Fix the masking fake: `Tests/UI/test_media_ingest_window_rebuilt.py:25-28` renames its method to `submit_ingest_jobs`; ADD a contract test asserting every panel-called method name exists on the real `MediaReadingScopeService` class (`getattr(MediaReadingScopeService, name)` for the five names) so the mismatch can never silently return. Do NOT touch `ServerMediaReadingService`/API-client alias names (legitimately theirs).

**A2. Honest XML copy.** `Local_Ingestion/local_file_ingestion.py`: remove `XML,` from the unsupported-type error string (`:86`), drop `'xml'` from `get_supported_extensions()` (`:102`) and from the `detect_file_type` docstring (`:43`); ALSO remove the `.xml/.rss/.atom → 'xml'` detection branch (`:64-65`) so `.xml` falls to the honest unsupported error instead of the not-implemented raise at `:372-374` (keep that raise as a defensive backstop). Test: ingesting an `.xml` yields the unsupported error NOT mentioning XML; `get_supported_extensions()` has no xml key.

**A3. Open-on-deleted-media notify + fallback.** In `_refresh_library_media_detail` (`library_screen.py:~2408`), at the still-current re-check (`:~2456-2458`): when the fetched detail is None AND `media_id == self._selected_media_id` AND the view is "viewer" → `notify("Media item is unavailable.", severity="warning")` (mirror the conversations copy shape at `:6727-6729`) and fall back `_library_media_view = "list"` before the recompose. Pilot: Open on a media row whose record was deleted between done and click (delete via the DB then invoke `_open_library_item_by_id("media", id)`) → notify fired, list view, no empty viewer.

**A4. notes_create stale-form reset.** `_apply_navigation_context_state` notes_create branch (`:751-757`): call `self._reset_library_note_editor_state()` first, then re-assert the create-note row/view fields the branch sets (the reset flips view to "list" — the branch must set its target state AFTER, mirroring `_open_library_item_by_id`'s reset-then-set at `:6704`). Pilot: dirty the editor, apply notes_create nav context → editor state reset (mirror the ingest_media reset pilot).

## Group B — the exc_info codemod (repo-wide)

**B1.** Empirically confirmed: loguru drops `exc_info=True` silently; only `logger.opt(exception=True).<level>(...)` captures tracebacks. ~1,320 occurrences. Write a Python codemod script (regex-based, run from the worktree; the script itself goes to `/Users/macbook-dev/.claude/jobs/56bbd923/tmp/`, NOT committed) that transforms ONLY loguru sites:
- Classify per line-prefix: `logger.<level>(` = loguru candidate; `logging.<level>(`/`std_logger` = stdlib, SKIP. Skip files whose only logger is stdlib (inventory list: `Event_Handlers/LLM_Management_Events/*`, `chat_streaming_events.py`, `worker_events.py`, `app_lifecycle.py`, `llm_nav_events.py`, `Notes/Notes_Library.py`, `UI/Screens/settings_screen.py`, `UI/Screens/stats_screen.py`, `Widgets/enhanced_settings_sidebar.py`, `Widgets/Chat_Widgets/chat_message*.py`, `LLM_Calls/LLM_API_Calls_Local.py`, `Local_Inference/*`, `RAG_Search/chunking_service.py`, `TTS/TTS_Generation.py`, `Web_Scraping/Article_Extractor_Lib.py`, `Utils/paths.py`, `Utils/Logging_Config.py`). Skip `.backup`/`.bak`/`.md` files and `Tests/`.
- Transform single-line `logger.X(args..., exc_info=True)` → `logger.opt(exception=True).X(args...)`; multi-line calls: locate the opening `logger.X(` line for a continuation-line `exc_info=True,?` — remove the kwarg line and rewrite the opener. `logger.exception(...)` sites already capture — if any carry a redundant `exc_info=True`, just drop the kwarg.
- Handle `exc_info=exc`/`exc_info=e` variants → `opt(exception=exc)`.
- After transform: `grep -rn "exc_info" tldw_chatbook/ --include='*.py'` — every survivor must be a stdlib-logging line or an intercept sink (list them in the report); `python -c "import tldw_chatbook.app"`; compile-check every touched file (`python -m compileall` on the list); run the broad suite (Tests/Library, Tests/Home, Tests/UI/test_library_shell.py + test_home_screen.py + gate16) plus 2-3 suites covering the highest-churn files (`Tests/ChaChaNotesDB/`, `Tests/Media/` if they exist — check).
- Manual-check files flagged by inventory (`LLM_Calls/Local_Summarization_Lib.py` — its `logging` name binds from Utils.Utils; `Local_Ingestion/API_Endpoint_Sample.py`) — inspect by hand, transform only if genuinely loguru.
- Commit message: `fix(logging): capture tracebacks at loguru sites — exc_info kwarg is a no-op`.

## Group C — Home/Library polish + persistence + flake hardening

**C1. Home Route line label.** `Home/dashboard_state.py:774` `f"Route: {item.detail_route}"` and `:752` `"Route: study"` → human labels. `UI/Navigation/shell_destinations.py` is a leaf (no cycle): move/reimplement the label lookup as a small pure helper (relocate `home_screen.py:_home_route_label`'s logic + `HOME_ROUTE_LABEL_OVERRIDES` into dashboard_state or a shared leaf module; `_home_route_label` currently has ZERO callers — delete the orphan from home_screen after). Line becomes `Opens: {label}` (e.g. `Opens: Library`, `Opens: Study`). Re-anchor Home tests asserting `Route:`.

**C2. Scroll Evidence into view when results land (L3a C3).** In `_refresh_library_rag_results_widgets` (`library_screen.py:~7024`), after the rebuild (`:~7044`): when results are non-empty and the trigger was a results-arrival (thread a flag from `_apply_library_rag_search_outcome` or detect results-presence change), `self.query_one("#library-rag-results-heading").scroll_visible(animate=False)` guarded try/except NoMatches. Precedents: `chat_message_manager.py:160`. Pilot: rail-submit with results → heading region visible (geometry check idiom from L3b if cheap, else assert scroll offset changed).

**C3. Conversations prepend vs snapshot race.** Preserve the pinned selection across wholesale snapshot replace: in `_apply_local_source_snapshot` (`:~831-853`), before `self._local_source_records = records` (`:840`) — if `_selected_conversation_id` is set, present in the OLD conversations records, and MISSING from the incoming ones, carry that record over (prepend into the new tuple). Pure in-memory merge; no async. Unit-style pilot: open an out-of-snapshot conversation, force a snapshot apply without it → selection + record survive.

**C4. Rail-state restart persistence (Library + Home).** Same bug class as search history (`load_settings()` carries no `library`/`home` tables). Apply the `_load_library_search_history` fallback template (`library_screen.py:3743-3756`, 1-arg dotted `get_cli_setting`): `_library_rail_preferences` (`:3690-3700`) falls back to `get_cli_setting("library.rail_state")` → `["sections"]`; `_home_rail_preferences` (`home_screen.py:240-250`) falls back to `get_cli_setting("home.rail_state")` (import get_cli_setting there if absent). Tests: monkeypatch `get_cli_setting` in each module namespace (the L3a test pattern incl. the autouse-stub hazard — check `test_library_shell.py`'s existing autouse fixture and extend it to stub the new fallback too, else cross-test disk leakage returns); assert precedence (app_config wins when present).

**C5. Flaky-pilot hardening.** (a) `test_library_shell_search_history_row_reruns_query` (`test_library_shell.py:~766`): wrap the final input-value assert in the bounded-poll idiom. (b) `test_library_surfaces_study_workflow_entry_points` + siblings (`test_product_maturity_phase3_knowledge_entry.py:38-107`): replace fixed `pilot.pause(0.1/0.2/0.3)` with bounded poll-until-selector loops (range(150)/0.02 idiom). (c) `test_library_shell_notes_sync_now_calls_recording_service_with_chosen_enums` (`:~6503`): between the two toggle presses, poll until the pressed toggle's re-mounted label reflects the chosen enum before the next press. No behavior-weakening: all original assertions stay.

## Verification & gate

Full suite set per group + a combined final gate (the L3b gate set + `Tests/UI/test_media_ingest_window_rebuilt.py` + the phase3 knowledge-entry file). Visual QA: only C1 (Home line) and A3 (notify) change pixels — capture Home canvas + the media-unavailable notify in the served TUI; present with the PR (fix-batch gate is lighter than a phase gate).
