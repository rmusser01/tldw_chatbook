# Media UX fix wave 5 — PR J (critique #5 polish batch, task-31635) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the seventeen minor observations from critique #5 on Library ▸ Media, each either fixed with a pin or declined with a one-line reason recorded in task-31635.

**Architecture:** Three batches by surface, so each is one review unit: Reader/Find copy and controls (Task 1), Trash and select-mode chrome (Task 2), list/review-set/Import residue (Task 3). No new components; every change is a label, a gate, a one-line copy, a CSS rule or a focus target in code that already exists.

**Tech Stack:** Python 3.12, Textual 8.x, pytest; `_painted` (`Tests/UI/test_library_media_render_fixes.py`), `Tests/UI/test_library_media_trash.py`, `Tests/UI/test_library_multiselect_media.py`, `Tests/UI/test_library_media_reader_flow.py`, `Tests/UI/test_review_set_walker.py`, `Tests/UI/test_library_ingest_canvas.py`.

**Spec:** `backlog/tasks/task-31635 - Library-media-critique-5-polish-batch.md` (AC#1: each item fixed or declined with a reason; AC#2: pins); critique #5 "Minor observations".

## Global Constraints

- CSS TRAP (PR F Task 2): `tldw_chatbook/css/screen_agentic_library.tcss` is GENERATED by `python -m tldw_chatbook.css.build_css` from `tldw_chatbook/css/components/_agentic_terminal.tcss`; edit the component source, rebuild, commit the regenerated sheet and bundle, `check_bundle_sync` exit 0.
- Worktree `/Users/macbook-dev/Documents/GitHub/tldw_chatbook/.claude/worktrees/media-wave5-j`, branch `fix/media-wave5-j` off dev. Every command: `cd <worktree> && PYTHONPATH=<worktree> /Users/macbook-dev/Documents/GitHub/tldw_chatbook/.venv/bin/python -m pytest … -p no:cacheprovider`; absolute paths; UI test files in separate processes; every Bash call begins with the explicit `cd` and `git branch --show-current`.
- This PR lands LAST in wave 5; before opening it, merge `origin/dev` (which by then carries PRs E-I) and re-run every touched test file. Do not duplicate anything PR E (receipt/Undo/Retry), F (focus), G (failure callouts), H (widths/More/rows) or I (row markers) ships — if an item below turns out to be covered, decline it with "shipped by PR <x>".
- Compare failures against the base before claiming them (known: `test_library_ingest_canvas.py::test_progress_detail_paints_below_row…`, `test_library_ingest_retry_last` flake, the `test_library_shell.py` census in task-31249).
- No new `logger.*`. After any TCSS/`BUNDLED_CSS` edit: `python -m tldw_chatbook.css.build_css` + `python tldw_chatbook/css/check_bundle_sync.py` (exit 0). Five-key/seven-key contract as PR I left it; review-set state code read-only except the one ruling in Task 3; no new toolbar buttons; the Items-pane 36-cell floor and PR D's row positions unchanged.
- Declines are allowed only with a reason that names the trade-off; "out of scope" alone is not a reason. The final report lists all seventeen with fixed/declined.
- Live verification: tmux (`t() { tmux -L w5j "$@"; }`), real config, ONE app instance, seeds via `MediaDatabase.add_media_with_keywords` with salted content, cleaned with `soft_delete_media`.
- TDD per task; commit per task with the trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; backlog task files are flipped by the controller.

---

### Task 1: Reader and Find copy and controls (items 1, 5, 12, 13, 14)

**Files:**
- Modify: `tldw_chatbook/Widgets/Library/library_media_content.py` / `tldw_chatbook/Library/library_media_viewer_state.py` — (1) the Find counter reads `Match 1 of 3` (grep the f-string producing `matches`); (5) `◀ Prev` / `Next ▶` render through `library_disabled_action_label` and `disabled=True` when the match count is 0 (mirror the pager's `○ Previous` / `○ Next` convention).
- Modify: `tldw_chatbook/Widgets/Library/library_media_viewer.py` — (12) the empty-Reader placeholder `Select a media item to read it here.` (two sites) becomes `Nothing loaded — the list could not be loaded.` when the browse controller carries a failure state and `Select a media item to read it here.` otherwise; (13) when `viewer.is_markdown` is False for `article`/`document`, render the mode strip's slot with a one-line note `Rendered view is for Markdown and transcripts` instead of omitting the strip silently (text only, no control).
- Modify: `tldw_chatbook/css/components/_agentic_terminal.tcss` (source; `screen_agentic_library.tcss` and the bundle are regenerated by build_css) — (14) the rendered Markdown H1 aligns left with the reading column (find the `text-align: center` rule that applies to the Markdown H1 inside `#library-media-viewer-content-markdown` and scope it out); rebuild the bundle.
- Test: `Tests/UI/test_library_media_reader_flow.py` (counter copy; Prev/Next disabled at 0 matches with the `○` label; placeholder copy under a failure; the not-Markdown note for `article`), `Tests/UI/test_library_media_render_fixes.py` (painted H1 left-aligned: the H1's first glyph column equals the body's first glyph column).

- [ ] Step 1: failing tests (five).
- [ ] Step 2: run; confirm.
- [ ] Step 3: implement; rebuild CSS; `check_bundle_sync` exit 0.
- [ ] Step 4: run `test_library_media_reader_flow.py`, `test_library_media_render_fixes.py`, `test_library_media_reader_shell.py`, `test_library_shell.py -k "find or match or placeholder"` (compare to base).
- [ ] Step 5: live 235x52: Find with 0 matches shows `○ Prev` / `○ Next`; an article shows the not-Markdown note; a Markdown doc's H1 is left-aligned.
- [ ] Step 6: commit `fix(library): Reader and Find polish — counter, Prev/Next gating, placeholder, Markdown note, H1 alignment (task-31635)`.

---

### Task 2: Trash and select-mode chrome (items 2, 3, 4, 6)

**Files:**
- Modify: `tldw_chatbook/Widgets/Library/library_media_trash_canvas.py` — (2) the permanent-delete confirmation formats `trash_date` through the same relative formatter the row uses (`trashed 6h`), with the absolute date in a tooltip; (3) `Restore` and `Delete permanently` are separated by a two-cell gap and the destructive one carries the `ds-danger`/error class the delete row uses elsewhere (grep how the select-mode `Delete` is classed), and both get keys: `r` restore, `x` delete permanently, advertised in the Trash footer chips (register through the existing footer-shortcut seam).
- Modify: `tldw_chatbook/Widgets/Library/library_media_canvas.py` — (4) buttons must not shift when their enabled state flips: `library_disabled_action_label` already prefixes `○ `; pad the ENABLED label with a leading space of the same width so `Delete` and ` Delete` share a column (apply to the select-mode row buttons only, painted-pinned); (6) with a failed load, `Export…` and `Trash` render disabled with reasons like `Select` does (route them through `_gate_stale_action` or the failure gate).
- Test: `Tests/UI/test_library_media_trash.py` (relative date in the confirm; keys `r`/`x` act; painted gap and class), `Tests/UI/test_library_multiselect_media.py` (painted: `Delete` column identical before/after the first selection), `Tests/UI/test_library_media_render_fixes.py` (failed load → `○ Export…` / `○ Trash` with tooltips).

- [ ] Step 1: failing tests (six).
- [ ] Step 2: run; confirm.
- [ ] Step 3: implement.
- [ ] Step 4: run `test_library_media_trash.py`, `test_library_multiselect_media.py`, `test_library_media_render_fixes.py`, `test_library_media_toolbar_adapt.py`, `test_library_shell.py -k "trash or select"` (compare to base).
- [ ] Step 5: live 235x52 and 100x30: Trash confirm shows `trashed 6h`; `r` restores; select one item and watch `Delete` stay put.
- [ ] Step 6: commit `fix(library): Trash and select-mode chrome polish — dates, keys, stable button columns, gated Export/Trash (task-31635)`.

---

### Task 3: List, review-set and Import residue (items 7, 8, 9, 10, 11, 15, 16, 17)

**Files:**
- Modify: `tldw_chatbook/Widgets/Library/library_media_canvas.py` — (7) the `Sets` mode-bar entry stays visible on a zero-result filter (it is navigation, not a result), disabled with a reason if it cannot open; (8) a filter that yields exactly one result no longer auto-loads it into the Reader — RULING: decline instead if the auto-load is an intentional, tested behaviour (grep for its pin); if declined, add one line of footer copy `1 result · Enter opens` so it is at least announced.
- Modify: `tldw_chatbook/UI/Screens/library_screen.py` / `tldw_chatbook/Library/review_set_state.py` — (9) `]` advancing marks the CURRENT item reviewed as a side effect (`mark_done_backing_id` in `advance`); RULING for this PR: keep the behaviour (it is the set's contract since task-31233) but make the banner say it: `] next (marks reviewed)`; a separate task may revisit. (10) the banner readout updates when a row inside the set is clicked (the walker's cursor already moves — sync the banner from the same seam). (11) while the Trash list is open the Reader shows a `Showing a Media item · not in Trash` line under its title, or is cleared — pick the cheaper honest option and say which.
- Modify: `tldw_chatbook/Widgets/Library/library_ingest_canvas.py` — (15) the path field uses the one-row `▊…▎` idiom the metadata fields use (or the metadata fields adopt the thick box — pick the form's majority idiom); (16) `Open manager` in the Reader's More row is indented like its siblings (PR H makes More one row; if PR H has landed, verify and decline as shipped).
- Modify: `tldw_chatbook/UI/Screens/library_screen.py` — (17) the `Reviewing N items.` toast at 100x30 overlaps the Reader border: shorten its timeout or render it as the banner's first frame instead of a toast (the banner already says `Reviewing: …`) — RULING: drop the toast when the banner is visible.
- Test: `Tests/UI/test_library_media_render_fixes.py` (Sets present on zero results; Reader note over Trash), `Tests/UI/test_review_set_banner.py` (banner follows a click; `]` chip wording; no toast at 100x30 when the banner paints), `Tests/UI/test_library_ingest_canvas.py` (one field idiom, painted), `Tests/UI/test_library_media_reader_shell.py` (More row indentation or the decline note).

- [ ] Step 1: failing tests (per item; declines get a note, not a test).
- [ ] Step 2: run; confirm.
- [ ] Step 3: implement.
- [ ] Step 4: run `test_library_media_render_fixes.py`, `test_review_set_banner.py`, `test_review_set_walker.py`, `test_library_ingest_canvas.py`, `test_library_media_reader_shell.py`, `test_library_media_trash.py`, `test_library_shell.py -k "review or sets or ingest"` (compare to base).
- [ ] Step 5: live 235x52 and 100x30: zero-result filter keeps `Sets`; Review these at 100x30 shows no overlapping toast; click a row in the set → banner updates.
- [ ] Step 6: commit `fix(library): list, review-set and Import polish (task-31635)`.
