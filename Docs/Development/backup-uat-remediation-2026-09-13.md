# Backup UAT remediation and repeat — 2026-09-13

**Acceptance is still in progress.** This report follows the [failed original walkthrough](backup-uat-2026-09-13.md). It does not replace or erase those results. Work remains confined to local backup and recovery on [PR #2642](https://github.com/rmusser01/tldw_chatbook/pull/2642), TASK-32562.

## Current checkpoint

The latest [Windows support run789960](https://github.com/rmusser01/tldw_chatbook/actions/runs/34867573446) completed **42/42 native checks and186/187 product checks**, with no skips, in1788.551seconds. Console capture, Settings capture and both fresh-process recovery handoffs pass. The sole failure is Library capture: its second inventory observes an opened SQLite main/WAL state that differs from the preceding metadata check, before copying begins. The primary Notes owner becomes unavailable and the existing scope check refuses capture. The changed member, field and writer are not yet identified. All118 artifact hashes, both installed-package receipts and2474 Python Git blobs per receipt verify. The bounded failure-observer extension passes63 root and25 independent checks; it retains only member/field names from already-caught state and makes no new filesystem observations. Ruff and compilation pass; Bandit adds11 test assertions and no nonassert findings. Native Windows execution is pending. [Observer receipt](backup-uat-remediation-evidence-20260913/snapshot-field-observer-checkpoint-20260914.json). [Verification](backup-uat-remediation-evidence-20260913/windows-789960-support-verification-20260914.json), [independent causal review](backup-uat-remediation-evidence-20260913/windows-789960-library-causal-review-20260914.md).

PR #2642 remains open against `dev`, unmerged. Acceptance remains in progress because native Windows failures and one failure in the latest Linux repeat are unresolved. The completed macOS keyboard journey below is a platform-specific acceptance result.

The immutable `4a50a22cff` keyboard UAT completed the ordinary first-time and experienced-user workflows: onboarding, original-note creation, Complete backup, untouched credential Abort, reviewed replacement, incoming-note readback, new-note save, fresh later-copy handoff, explicit credential omissions, validated later rollback, original-note readback, encrypted new safety-copy inspection, and another Complete backup. All sessions quit normally. The saved-evidence audit verified three archives, 457 payload hashes, seven note-state relationships and all 5,349 installed-file hashes. Recorder/navigation mistakes and the narrowed password-recording assurance remain documented. No installed source or configuration repair was used. [Keyboard report](backup-uat-remediation-evidence-20260913/keyboard-4a50-completion-20260914.md), [audit](backup-uat-remediation-evidence-20260913/keyboard-4a50-evidence-audit-20260914.json).

The [4244 Windows support run](https://github.com/rmusser01/tldw_chatbook/actions/runs/34856938381) completed with 42 native passes and 158/161 product passes, no skips. The three failures were an ordinary handoff timeout, a Settings navigation wait followed by a missing-button timer exception, and a Library capture refusal because its SQLite source changed during preview. Library's consistency check correctly refused that changing source; its writer is not yet identified. The artifacts do not establish that the timer caused the initial Settings wait. All 104 artifact hashes and both installed receipts verify, including 2,474 Python Git blobs per receipt. [Verification](backup-uat-remediation-evidence-20260913/windows-4244-support-verification-20260914.json), [causal review](backup-uat-remediation-evidence-20260913/windows-4244-mounted-causal-review-20260914.md).

The [562530 Windows support repeat](https://github.com/rmusser01/tldw_chatbook/actions/runs/34863186382) completed **42 native passes and161/165 product passes**, no skips, in2357.521seconds. All109 artifact hashes and both2,474-Python installed-source receipts verify. Settings now completes F4 navigation but fails exclusive-lease admission; Console refuses a changing SQLite preview; Library retires all owners and is still rediscovering held capture inventory when its420-second test expires. These are distinct failures with no evidence-supported product fix yet. The test observer now retains exactly one slowest completed group detail alongside its rolling16 records, so a later run can distinguish caller/worker CPU from whole-process CPU without losing the maximum. Root9, independent9 and two independent nested/concurrent probes pass; original test ASTs are unchanged, Ruff is clean, and Bandit adds only assertions. This cannot reconstruct the missing historical detail. [Observer receipt](backup-uat-remediation-evidence-20260913/slowest-group-observer-checkpoint-20260914.json). The following789960 result supersedes its current failure counts. [Verification](backup-uat-remediation-evidence-20260913/windows-562530-support-verification-20260914.json), [causal review](backup-uat-remediation-evidence-20260913/windows-562530-mounted-causal-review-20260914.md).

The Settings timer defect is reproduced with an actual mounted rail and a queued callback after descendant removal. A narrow `NoMatches` guard now skips that obsolete tick, and the next tick queries and updates the current button. Root five focused checks, three related rail lifecycle checks and the independent regression pass. Additional legacy UI fixture failures reproduce against the baseline before reaching the changed callback. The installed macOS Settings capture passes in 63.72 seconds, with all 2,474 installed Python sources matching the corrected working tree and all 2,866 installed files preserved. Ruff's seven pre-existing findings are unchanged; Bandit adds only three test assertions. No backup guard, timer interval, or deadline changed. Fresh native Windows acceptance is still required. [Timer verification](backup-uat-remediation-evidence-20260913/console-progress-timer-checkpoint-20260914.json), [independent review](backup-uat-remediation-evidence-20260913/console-progress-independent-review-20260914.md).

A separate measured startup caller read nine context settings through nine native acquisitions. Only that contiguous comprehension now shares the existing exact-source config operation; all getters and their order remain, and parsing occurs after the operation retires. Native tests verify current values, fresh next-call values, pause/selector refusal, persistence-error handling, parser-error isolation and restoration of enclosing config/database operations. Root11 and independent11 cases pass; related44 cases pass. All three installed macOS mounted capture routes pass in182.84 seconds, with2,474 source matches and2,866 installed files unchanged. Alternating native single-call measurements reduce acquisitions9→1; this is not a Windows latency result. Ruff and Bandit finding counts match the baseline. [Verification](backup-uat-remediation-evidence-20260913/context-policy-lifetime-checkpoint-20260914.json), [review](backup-uat-remediation-evidence-20260913/context-policy-independent-review-20260914.md).

Earlier failures remain evidence: [b560 Windows support](https://github.com/rmusser01/tldw_chatbook/actions/runs/34847402738) passed 152/154 with two admission timeouts. [081176 replacement](https://github.com/rmusser01/tldw_chatbook/actions/runs/34841087737) reached its existing 80-minute phase limit with two passes, one failed default later workflow and 28 unfinished cases. That child reported successful, validated rollback before its parent timed out during the remaining workflow. The [9aff replacement repeat](https://github.com/rmusser01/tldw_chatbook/actions/runs/34859131771) reached the same80-minute phase limit after **three product passes**: explicit F9 replacement, default replacement/later rollback, and nested Persona reopen/later rollback. Twenty-eight cases have no terminal result. The later child completed catalog, safety-copy inspection, app closure and loop closure8.218seconds after validated rollback, with896.593seconds total recorded child time. All69 artifact hashes and both installed receipts verify. The same31-case coverage is now partitioned into two bounded jobs: explicitF9+Persona21 and defaultreplacement+Evals10. Actual collection proves exact union/no overlap; each job retains an installed-wheel workflow. All36 existing CI contract checks pass, Ruff/Bandit are clean, and independent review approves. No case or native10/product80/job90-minute deadline changed. Native completion of the partitions remains pending in [run34869269028](https://github.com/rmusser01/tldw_chatbook/actions/runs/34869269028), exact `ec139a0241`. [Partition proof](backup-uat-remediation-evidence-20260913/windows-replacement-partition-verification-20260914.json), [review](backup-uat-remediation-evidence-20260913/windows-replacement-partition-review-20260914.md). [Verification](backup-uat-remediation-evidence-20260913/windows-9aff-replacement-verification-20260914.json). The same observed installed macOS default workflow passed in 377.25 seconds. [Later observer receipt](backup-uat-remediation-evidence-20260913/later-child-observer-checkpoint-20260914.json).

The exact `b534370982` macOS selection passed all 74 product checks in 1196.024 seconds, with no failures or skips; all 109 artifact hashes and four installed receipts verify. Subsequent changes and their narrower repetitions are identified above rather than attributed to that earlier run. [Mac verification](backup-uat-remediation-evidence-20260913/macos-b534370-verification-20260914.json).

The fresh Linux `4a50a22cff` repeat completed **76/78**, with two failures, no skips or unfinished cases. Zero-based public positions36 and49 identify Console capture/resumed writes and nested Persona reopen/later rollback. Encrypted/unencrypted restore, explicit replacement and the default later-rollback workflow passed. Only numeric pytest-hook status was read; failure causes remain unknown and the separate diagnostic-log disclosure request remains pending after prior automatic-review rejections. The subsequent `86ad9993dc` repeat completed **77/78**, no skips or unfinished cases: all three mounted capture/resumed-write routes passed; only nested Persona reopen/later rollback failed. The existing numeric-only output does not establish its cause. Product sources are unchanged through `29baddc035`. [Latest Linux receipt](backup-uat-remediation-evidence-20260913/linux-86ad-numeric-repeat-20260914.json). [Numeric receipt](backup-uat-remediation-evidence-20260913/linux-4a50-numeric-repeat-20260914.json).

Windows startup [34864871343](https://github.com/rmusser01/tldw_chatbook/actions/runs/34864871343), exact `86ad9993dc`, still failed all four cases at the unchanged60-second limit. Terminal stacks show native backup-authority work in LLM/discovery and deferred Collections paths. They do not prove a single slow call, deadlock, or Windows benefit from the local9→1 acquisition correction. [Causal review](backup-uat-remediation-evidence-20260913/windows-startup-86ad-causal-review-20260914.md). Its retained same-observer diagnostic passed pinned dev in52.87seconds and again timed out on the candidate. The candidate's last incomplete sample records260 acquisitions and138,576 native opens; missing per-method counts are unknown, not zero. [Diagnostic review](backup-uat-remediation-evidence-20260913/windows-startup-86ad-profile-review-20260914.md). Measured proposals that lost fresh identity or authority checks remain rejected; no permission cache or timeout increase was introduced.

A real native process probe showed the handoff test could leave its restarted child running after timeout. The test now uses file output and the existing PID/create-time observer under the original70-second deadline. On parent failure it retires only an already identified matching child and preserves the primary error; an absent or mismatched receipt grants no cleanup authority. Root and independent11-case runs pass, including timeout/nonzero/mismatch/cleanup-error cases. Actual installed handoff/later and related checks passed7/7 in45.25seconds before the final exception-only cleanup adjustment; all2,474 installed Python sources match and all2,866 installed files remain unchanged. Ruff is clean and Bandit adds only test assertions. Production launcher behavior is unchanged, and native Windows acceptance remains pending. [Checkpoint](backup-uat-remediation-evidence-20260913/handoff-file-observation-checkpoint-20260914.json), [independent review](backup-uat-remediation-evidence-20260913/handoff-file-independent-review-20260914.md).

### Earlier checkpoint history — retained

The PR remains open against dev, unmerged. The latest reviewed corrections address Console configuration lifetimes, later rollback of nested Persona artwork, and discovery of the canonical evaluation definition after rollback. All earlier requester-approved actions are applied. A separate measured startup module-count exception is awaiting an explicit decision under ADR-097; those limits remain unchanged.

The latest product revision passed 57 focused regressions and all three fresh installed macOS capture routes. Actual SSH Linux verification passed 74/74 with no failures or skips, including all three capture routes and first-note backup/handoff. Sixty downloaded artifact hashes and two installed receipts were verified. Independent reviews approved the Notes initializer cleanup and same-pause cache-retirement retry.

Windows replacement [34816559624](https://github.com/rmusser01/tldw_chatbook/actions/runs/34816559624) tests product `9b7316d2f4`. Replacement passed 42 native checks and both product workflows, including full default replacement/later rollback; all 52 downloaded artifact hashes verified. Windows support [34817971803](https://github.com/rmusser01/tldw_chatbook/actions/runs/34817971803), exact `4becc2872c`, exhausted its 80-minute outer product limit: 42 native checks passed; product collected 549, with 467 passes, eight failures and 74 cases without terminal results. No product JUnit was produced. All 234 artifact hashes and the source/installed receipt comparisons verified. Seven prior failures now pass; five GUI cases and three newly enabled ACL cases fail, while the 1,800-file case was not reached. A focused nine-case diagnostic retains the existing individual test limits; the complete support selection has a reviewed finite 120-minute observation allowance and 135-minute job allowance. No final all-platform acceptance is claimed.

The fresh immutable `84cf0c3414` keyboard repeat passed onboarding, original-note creation, recovery handoff, credential-review Abort, ordinary Console/Library restart and original-note visibility. Replacement retry completed with restoration validated. Ordinary reopening, exact incoming/new-note readback and normal Quit passed; the binding fingerprint matches the published config. Later rollback review nevertheless failed with `backup_operation_failed`. A preview-only diagnostic identified `local_snapshot_absence_unclassified`: an authenticated new publication is a nested `persona.assets` pack, while the later-rollback classifier only handles new top-level builtin/evaluation roots. All 1,787 fixture files retained their exact bytes and native identities. A native regression and narrowly scoped classification correction are under review. Library overview also hit its five-second aggregate deadline during a measured6.636-second UI stall; Notes subsequently loaded/read/wrote, and backup causality for the overview stall is not yet established. Its preceding artifact completed replacement/reopen/new-note/Quit but failed later review because access-time changes invalidated the config binding refresh; that cause is fixed and independently regression-tested. Historical results and corrections are retained below.

Latest preserved receipts are in the [native and keyboard evidence index](backup-uat-remediation-evidence-20260913/native-and-keyboard-checkpoint-20260914.json). Fresh installed macOS explicit/default replacement and later rollback passed both cases in 434.79 seconds; both package receipts match current product sources. The first disposable Library comparison loaded ordinary/restored overviews in 0.36/0.78 seconds and does not explain the keyboard timeout. A comparison with replacement history is underway.

The measured Console delay now has a reviewed correction: one existing configuration operation spans each synchronous control-bar refresh, with all nested native checks retained. UI-only exceptions propagate after the scope closes and do not mark configuration persistence failed; real config failures still block backup drain. Thirteen focused cases and ten independent checks passed, with no new Ruff/Bandit findings. The [exact source receipt and review](backup-uat-remediation-evidence-20260913/console-config-sync-report.md) distinguish successful preliminary installed runs from the still-required final artifact repeat.

The nested-pack correction retains installed-owner discovery through existing private main/WAL snapshots. Direct discovery changed live SHM timestamps and invalidated its own plan; corrected repeated and held previews preserve main/WAL/SHM bytes and non-access-time metadata. Held validation reads only the canonical selected config, core database and included artwork files. Its final native lifecycle passed in 254.31 seconds: capture, credential-review Abort, replacement, ordinary reopen/new-note write, later rollback, encrypted current-note/artwork readback, restored original artwork graph, Complete inventory, fresh ordinary restart with the original note, and another actual Complete backup. The subsequent canonical-Evals correction omits only the unchanged native default from retained-extra classification; all nine Evals retention checks and an independent repeat pass. The aggregate Complete assertion was retained. Fresh immutable keyboard and native Windows/Linux repetitions remain required.

Current PR CI also needs final verification. Fast Lane at `ae958f6f9a` passed 1,128 checks and failed one worker-cleanup test; the derived-artifact job failed solely because that prerequisite failed. The exact test reproduced locally and its reviewed event-synchronization correction passed all 23 capacity checks plus an independent repeat. Startup module counts still exceed the existing guards: current candidate 669 at import and 1,022 at UI-ready versus latest dev `4631b60f8d` at 643 and 975 using the same private-profile probes. No budget has been changed. Four Windows GGUF app cases also hit their existing 60-second test deadline; these failures remain under causal assessment and are not acceptance passes.

## Method

Persona acceptance uses an immutable installed wheel, the ordinary `python -P -m tldw_chatbook` entry point, and keyboard events in real tmux terminals. Fresh first-time profiles traverse the actual six-step wizard. No Textual Pilot, widget-value assignment, test-mode environment, pre-completed onboarding, or product patch is used for a claimed keyboard pass. Synthetic homes and credentials keep these tests separate from personal data.

Terminal text and ANSI frames, keyboard records, archive hashes, and read-only database/file checks are the evidence. Desktop pixels and screen-reader accessibility have not been evaluated. Native automated macOS, SSH Linux, and GitHub Actions Windows tests are separate from the keyboard persona walkthroughs. Diagnostic and fixture-assisted results are identified explicitly.

## Corrections made

| Original or repeated finding | Correction and verification |
| --- | --- |
| Ordinary `config.toml.lock` prevented Complete classification | Recognize only the exact installed private empty lock; unsafe or unrelated files still refuse. Native inventory and actual default-profile review checks cover this. |
| Live Console, Library, and first-launch backup/restore could not settle | Correct the installed retained Library state contract and retire newly acquired native file-backed caches at the measured finite Console, Library, and Home worker boundaries. Borrowed, custom, in-memory, foreign-thread, active-operation, and transaction lifetimes remain protected. Installed capture verifies archived note content and subsequent live writes; installed first-launch restore verifies recovered note content. |
| Ordinary polling could crash during live registry publication | Read the existing native registry under its shared lock. A live publisher completes before inspection; abandoned durable intent still refuses. Bootstrap does not create or repair files. Deterministic native concurrency and unsafe-lock tests cover both paths. |
| Restore required internal hashed directory entries | Request one local base and display name per profile, plus independent external locations, and derive installed owner paths. Preserve shared stores and unused optional selectors; reject invalid private parents before confirmation. |
| F9 and generic recovery feedback were misleading | Document F4 Settings and the command palette; show bounded corrective failure guidance, reviewed Partial coverage, and the contents/counts of nonempty extraction groups. |
| Larger recovery records could not be reopened | Bound recovery descriptors/journals separately from the unchanged bootstrap limit, read in chunks of at most 64 KiB, and refuse excessive records before publication. Tests cover short reads, exact limits, growth, reopening, and actual 1,800-file publication. |
| Opening a restored app reported failure after ordinary Quit | Avoid defaults-only writes during shutdown and first Console mount for the exact unchanged process-bound recovered configuration. The fresh keyboard restore/Open/normal Quit repeat now passes on tree `580c657bba5e05e185f8f62243c17f4c16eb28e6`, with verified receipt, unchanged config/note and all installed hashes. A separate legitimate-settings-edit continuity issue remains under correction. |
| Replacement of the default profile rejects internal inspection storage | Recognize only the untouched synthetic config container around exact excluded default recovery stores. Bind the actual application namespaces without granting authority over the control parent. Native replacement, untouched abort, later rollback, new-copy inspection, and namespace checks passed in 328.26 seconds; 31 focused regressions passed independently. Keyboard repeat remains pending. |
| Saved, closed Library note falsely prevents recovery handoff | Recognize the installed Library note work pane and the current prompt mutation field. Dirty, saving, conflicting and unknown editor states still refuse. Independent tests: 83 passed; installed first-note capture and guarded handoff: two passed. The latter checks shutdown/request creation and does not claim an external CLI restart. |
| Required safety-file selection needs excessive keyboard navigation | An unavailable dependency review focuses the explicit selection button; selecting the required files focuses Review restore. Selection still invalidates the plan and requires another review and confirmation. Two regression cases failed before the correction and passed afterward; both also passed independently. |
| Bound profiles refuse installed preferences writers; duplicate sidebar methods bypass the guarded writer | Exact installed emoji, sidebar and runtime policy operations use the existing configuration lease and hold their registry guard until native IO retires. Custom paths and invalid-parent refusals remain intact. Nine later duplicate sidebar definitions were removed. Independent checks: 85 passed; an installed Console capture, archive readback and resumed-write regression passed in 47.96 seconds. Repeated capture of a newly created sibling still needs the separate correction described below. |

## Power-user keyboard results

These results use product `c5c05181d834d4b7596107a154216a330af8261d`, installed at `/private/tmp/chatbook-backup-uat-repeat-20260913-07b3zfn3`. The later quit correction is being repeated separately on a fresh build.

| Action | Result and evidence |
| --- | --- |
| Missing or mismatched encryption passwords | Pass: explicit refusal before capture. |
| Two-profile encrypted backup with credentials and external research | Pass after correcting a fixture that had never initialized its second profile through normal startup. The original fixture refusal is retained. Null keyring scopes were explicitly reviewed and omitted. |
| Wrong archive password, then correct password | Pass: wrong password refused; correct inspection verified 3,868 files and 54,226,805 payload bytes. |
| Restore two profiles and the shared research folder | Pass: restoration validated; all 1,800 research files match their source SHA-256 values, and source files remain unchanged. |
| Recover a nonempty Notes/chats group | Pass: five extracted files, 21,667,840 bytes, each matching the verified manifest. |
| Optional credential inclusion | Pass for the synthetic provider credential retained inside the authenticated encrypted archive. Restored configurations are sanitized, the encrypted recovery archive is retained, and 42 unavailable keyring scopes were explicitly acknowledged. This does not claim recovery of omitted keyring credentials. |
| Open restored profile and quit normally | Failed on this revision: the child mounted Settings, but shutdown rewrote the config and invalidated its fingerprint. The correction has independent review and regression coverage; fresh keyboard repeat is pending. |
| Cancel an active capture | Pass: Running → Cancellation requested → Cancelled. No output archive; 1,802 source/config file hashes unchanged. |

Encrypted archive SHA-256: `86324ee519464336a064867bd37d6bc499fcb88d20e8290df31b5a947cfeee9e`.

## First-time keyboard repeats

The `53e3c8d0359f547b3ce1e5b4d5fc827fe67df308` and `04029dd08beb82a56dde576332cc8a57da7f63d3` repeats completed real onboarding, saved a note, found backup through F4/command palette, and obtained Complete review. Capture still failed. Those failures exposed the retained Library and Home worker cases; they are not counted as acceptance passes.

A fresh immutable build of `a743fd473818481797e5133f05daa9e85c749cd2` repeated onboarding → saved note → Complete review, but capture still failed. A separate metadata-only diagnostic identified the remaining lease: Home active-work refresh → unread notification count → the installed notification database. The exact finite callback correction subsequently passed the installed full first-note regression. Historical failure evidence remains at `/private/tmp/chatbook-backup-uat-repeat-20260913-ydouf8av`.

The fresh merged repeat uses Git tree `f69a11ecb0720db043d9e89616ff46dc789f8bad`, wheel SHA-256 `2cb2fff7cf0612c6e8f29593d288dc93db445a093d14969abe0f185fe037b1ec`, and evidence root `/private/tmp/chatbook-backup-uat-repeat-20260913-kk6_dews`. This is a staged tree, not a final commit. All 5,347 installed file hashes were checked against its artifact receipt.

| Fresh keyboard journey | Result |
| --- | --- |
| Six-step onboarding → first saved note → Complete review → capture | Pass: archive verified, with the exact saved note checked read-only in both source and archive. Archive SHA-256 `cf386124bb7c0daf46ffe0a127454713bde3d83d677a88dc6cf289def7406114`. |
| Resume ordinary note editing after capture | Pass: keyboard edit and Escape autosave verified in the live database. |
| New installation → Welcome Restore → inspect → one base/name → review → confirm | Pass: restoration validated; archived note payload matches the restored database. An overlapping parent was refused first, then an independent private parent succeeded. |
| Open restored app → ordinary Quit | Failed: child mounted Console, but the parent reported `backup_operation_failed` and marked the profile `recovery_required`. The exact cause is under investigation; an Evals warning alone is not treated as the cause. |

Disk exhaustion interrupted the terminal recorder before several keyboard actions. Those actions were not sent; affected steps resumed after reclaiming reproducible, task-owned build inputs. These interruptions are not counted as product failures or acceptance passes.

The subsequent Console correction passed a new independent restore and actual keyboard Open/Quit journey on tree `580c657bba5e05e185f8f62243c17f4c16eb28e6`. The parent returned a successful Needs setup result and kept the profile restoration_validated; the native receipt, exact note/config bytes and 5,347 installed hashes verified after exit. See [the curated acceptance report](backup-uat-remediation-evidence-20260913/newcomer-c8-open-quit-report.md). The f69 failure above remains historical evidence.

## Additional acceptance findings under correction

- Default-profile replacement's credential review and actual Abort passed, with both original notes unchanged. After acknowledging all 21 unavailable synthetic keyring scopes, replacement failed before safety-archive creation. Read-only reconstruction proved `unknown_producer_dependency`: the safety copy contains the notes/chat database but omits a preserved persona-artwork dependency. A second actual Abort succeeded; database and configuration hashes remained identical to the first Abort. The correction will require explicit safety-file selection during review, before staging or pending recovery state.
- Actual mounted rail and theme edits persist, but then invalidate the restored profile's config fingerprint and recovery-catalog open result. Two focused real-app regressions reproduce this separately from the now-passing no-edit Quit. The repair must preserve namespace roots and activation restrictions while recognizing successful installed configuration writes; external modifications must not gain enrollment.

The safety-copy dependency correction now requires an explicit **Select required safety-copy files** action and another review. It passed independent review (nine focused tests) and an installed macOS default-profile replacement and later rollback in 328.90 seconds. The keyboard repeat on immutable tree `fe62a5aa319969ae2f21403ab3ca61059a8e3707` verified that explicit selection makes the plan available. Confirmation then failed with `backup_operation_failed` after staging and before journal or safety-copy creation. Read-only diagnosis proved the absent incoming UI preferences file cannot acquire publication scope without covering the protected config parent. The original saved note/database and all installed artifact hashes remain unchanged. See the [preserved failure and provenance](backup-uat-remediation-evidence-20260913/replacement-fe62-report.md). This attempt is not a replacement pass.

That keyboard repeat exposed a further problem before it could start: after the earlier successful untouched Abort, ordinary startup of the original default profile fails with `storage_scope_not_enrolled`. Its saved file-only binding does not cover the configuration writer's companion files. Original database/configuration bytes still match the post-Abort receipt. The native replacement/rollback test stays in recovery mode and did not exercise this normal restart, so its pass does not resolve the new failure.

A repair for exact configuration companion operations and verification of the existing bound user-data directory passed independent review and 24 tests. It holds the existing selector lease and registry lock for the native operation without enrolling the config parent or changing persisted fingerprints. Full startup then exposed the first binding's omission of ordinary temporary files in the user-data folder.

The first-binding correction proves and binds the existing installed data directory, with no control-parent coverage or automatic expansion of older bindings. It passed 75 focused tests and 76 independent checks, including shared-profile compatibility. Ordinary CLI construction, app mounting, saved-note readback and clean return now have explicit native checkpoints after Abort, replacement and later rollback. All three passed in the source-only workflow, but its first child used the actor-scaffold correction before the final shared-profile adjustment; a final immutable-artifact repeat remains required.

The separate runtime writer and duplicate-method corrections now have independent approval. The installed Console regression passed on wheel SHA-256 `a724c742380d09f82365acec09db0e5aef61e062e9fd4cf9b4d70e17fa355eac`; all four changed product files matched their reviewed hashes and the package fixture verified its 2,865 installed files after the test. This automated test is distinct from keyboard acceptance. A further native check reproduced a bound-profile failure: saving a previously absent runtime-policy file succeeds, ordinary capture registers it in a new namespace outside that profile's binding, and the next save refuses. Repeated-capture continuity and publication of absent config files remain unfinished.

The proposed fingerprint-only settings-continuity repair remains unapplied pending the specific approval requested after automatic review rejected that persistent-boundary change.

## Actual interruption and restart

A fresh independent profile on tree `580c657bba5e05e185f8f62243c17f4c16eb28e6` passed actual capture interruption → ordinary restart → keyboard retry. The verified retry archive contains all 1,800 research files and the exact saved note; original files and note rows were unchanged, and no incomplete archive was published. All 1,936 payloads and 5,347 installed file hashes were checked. The sole config change on restart was the proven ordinary first-Console default layout initialization. See the [acceptance report](backup-uat-remediation-evidence-20260913/power-interruption-fresh-report.md) for hashes and the earlier inconclusive attempt preserved separately.

## Native platform evidence

| Platform / product | Result |
| --- | --- |
| macOS, Library correction `04029dd08b` | Installed Library → F4 → Complete capture → archive readback → resumed write passed. Independent retained-state/lifecycle check: 102 passed. |
| macOS, Home/quit/registry corrections through `a743fd4738` | Installed first-launch restore passed; 23 profile-open/config-persistence checks passed; installed Console, Settings, and Library capture routes all passed. Independent registry review ran 38 passing tests. |
| Linux SSH, `04029dd08beb82a56dde576332cc8a57da7f63d3` | 113 tests passed, no skips, 227.28 seconds. Includes installed Console/Settings/Library capture and actual 1,800-file publication. Exact source patch receipt retained on the host. |
| Linux SSH, `a743fd473818481797e5133f05daa9e85c749cd2` | 114 tests passed, no skips, 338.79 seconds, including new Home, registry, first-launch restore, quit, worker lifetime, and native capture checks. These do not substitute for the failing full keyboard onboarding-to-note path. |
| Windows, `d0882f84b0` | Plain, encrypted, encrypted-credential, two-profile roundtrip, replacement, and later rollback selections passed. Support: 191 passed and two failed (live registry refusal and the 1,800-file test deadline). [Run evidence](https://github.com/rmusser01/tldw_chatbook/actions/runs/34785915153). |
| Windows, `ad88df3415049134bac230314ca244f48c6510d0` | Failed: 42 native filesystem checks passed; product checks had 258 passes and 11 failures. Failures comprise three mounted capture routes, one public-lock fixture, six profile-open cases and the actual 1,800-file publication deadline. Bounded diagnostics and a Windows ACL fixture correction have been reviewed; the new native Windows run remains pending. No current-tree Windows pass is claimed. [Run evidence](https://github.com/rmusser01/tldw_chatbook/actions/runs/34788128200). |
| Linux SSH, merged tree `f69a11ecb0720db043d9e89616ff46dc789f8bad` | Exact 16,248 source blobs verified. Lifetime/capture/open checks: 104 passed, no skips, 428.85 seconds. Schema/replacement-admission/crash checks: 139 passed, no skips, 143.95 seconds. Default-profile replacement/later rollback: failed at the fresh replacement subprocess's 150-second deadline; investigation remains open. |

Earlier Linux native capture, encrypted controls, replacement, and later rollback also passed on `c5c05181…`; the later rollback repeat used the committed test-only measured first-preview wait budget. These older results do not substitute for checking the new corrections.

## Remaining acceptance work

- Resolve and verify the remaining native Windows backup-workflow and startup failures.
- Verify the latest product corrections on Linux and diagnose any remaining failures within the approved diagnostic access.
- Publish final native evidence with the PR. The immutable macOS keyboard replacement/later-rollback journey is complete; its exact-revision receipt and limitations are above.

The requester subsequently approved the two exact path-inventory comparison entries, the two-file Linux diagnostic transfer, and configuration-write binding continuity for both default and restored profiles. Their execution and verification are recorded below.

No merge has been performed. The requester's PR Change summary remains unchanged.

### Bound preferences capture correction

The three installed config siblings now retain their existing config exclusion scope during capture. Independent native testing passed all 15 cases in 83.28 seconds, including actual Complete backup followed by another owner write/readback for sidebar state, recent emoji and runtime policy. The shared-parent case drains a real writer under the other bound selector, then captures only the requested profile; registry and binding contents remain unchanged. The final strengthened scope-change assertion passed in 24.73 seconds. Existing capture/SQLite regressions passed 48 cases in 60.73 seconds.

The replacement/rollback correction now has independent approval. The committed originating plan, manifest and publication evidence establish which exact installed preferences file was originally absent, including when imported and local profile IDs differ. This permits later rollback to capture the current file in its safety copy and then remove it. Default-layout native lifecycle coverage passed 22 cases in 112.38 seconds, including real process interruption before/after activation-pair publication, both fresh-process recovery actions, foreign-intent refusal without changes, and ordinary writes by all three installed owners afterward. Independent review passed 51 checks across scope validation, explicit bootstrap roots, default interruption recovery, retirement and the corrected recovered-media fixture. No config-parent namespace was enrolled and no placeholder file was created.

A separate config-only isolated restore completed recovery but failed ordinary Open because its user-data child did not yet exist. The failure reproduced on the preceding source baseline. The correction returns that missing child to the existing admitted directory-creation path only when an existing bound directory already contains it and its parent is private. No root is added. Companion and actual isolated recovery/Open checks passed 29 cases in 17.54 seconds, including a fresh app writing and reading a native note; independent checks passed six cases. Ruff and Bandit reported no findings for this correction.

These source-native checks do not replace final installed keyboard or Windows/Linux repeats. A fresh installed keyboard walkthrough on immutable tree `336c272dab58d2116055d3f335437e5f46af60c7` is in progress; that artifact includes publication/recovery fixes but predates the config-only missing-child correction.

### Filename review correction

The fresh keyboard repeat entered `first-complete.zip`. Review showed Complete coverage, then Create failed with `invalid_backup_suffix` and generic guidance. The unchanged capture suffix predicate is now also checked before service preview and operation startup. The form and bounded error message name `.tldw-backup.zip` and the encrypted `.tldw-backup.zip.age` suffix. Eight regressions passed after reproducing the service/UI failures; independent review passed 13 checks, including valid plain/encrypted roundtrips and credential protection. Ruff and Bandit were clean. This adds no automatic filename rewriting or archive format change.

The immutable `336c272d` keyboard repeat succeeded after correcting the filename: all 135 payloads (26,860,280 bytes) and the saved note were verified in archive SHA-256 `e11915a78c0d0b6029de6300851d3bd5e49c21e28dd25374336fe62d2f5bd13c`. It then completed the actual Settings handoff to a fresh recovery process. All 5,347 installed file hashes remained unchanged. Replacement review is in progress.

The first immutable `336c272d` Abort attempt is **inconclusive**: UAT verification opened the live SQLite database in read-only mode after replacement reached its prepared state. SQLite can change its shared-memory sidecar even through that connection. The preserved artifact proof identified exactly that sidecar as changed and refused recovery; main database bytes remained unchanged. This must not be counted as a product Abort failure or used to relax recovery checks. A clean repeat will perform no SQLite opens while replacement is pending.

Fresh installed `336c272d` probes passed all three credential-review→Abort cases in 2.56 seconds: absent/present default config siblings with the actual null keyring, and a WAL-backed credential refusal preserving exact database/WAL/SHM/config bytes and native identities. No SQLite connection occurred while prepared. The contaminated fixture remains preserved; no product change was made for that refusal.

The latest immutable `ab24f6b0c2690f7705bdcddf46a941104541fe07` package separately passed installed checks for config-only missing-user-directory creation and both plain/credential-encrypted filename review refusals. All 5,347 installed file hashes remained unchanged; wheel SHA-256 `198a7494d194b553e1bba81d01adf401ae5e6590c93f9f310fc0659e489ad8bb`. These three checks use native child processes and the Textual test harness, not actual keyboard automation.

The clean immutable `ab24f6b0` keyboard repeat passed credential-review → Abort → normal startup → original-note readback → normal Quit. Abort reported success and removed the pending recovery entry. The original note appeared in Library and its exact title/body matched a byte-copied database snapshot; verification never opened the live SQLite database while prepared. See the [clean Abort receipt](backup-uat-remediation-evidence-20260913/replacement-ab24-clean-abort-reopen.json). Acknowledged replacement and later rollback remain in progress.

After that normal restart and Quit, the same keyboard session acknowledged all 21 synthetic keyring omissions and repeated replacement review. Review refused with `target_unverified`; no new replacement started. The existing default binding now has a stale configuration fingerprint and `activation: null`. This is a separate default-binding case from the pending restored-profile settings-continuity proposal, which currently handles only activated bindings. Independent read-only causal review is underway; the original saved note remains verified. See the [refusal and binding receipt](backup-uat-remediation-evidence-20260913/replacement-ab24-post-restart-binding.json).

Independent read-only review confirmed the retry cause: automatic normal Console startup adds only `console.rail_state` to the TOML, invalidating the unactivated binding fingerprint. The installed MCP recovery check then raises `mcp_recovery_binding_changed`; inventory cannot publish an included target config, so destination review refuses `target_unverified`. This is a configuration-write continuity defect, not an Abort failure. Two new native tests reproduce it for explicit and implicit configuration selectors (two expected failures in 2.11 seconds). Those RED tests remain with the unapplied continuity proposal; no product repair or authority record modification has been made.


### Approved configuration-write continuity repair

The installed configuration writer now refreshes only the fingerprint of the same verified existing profile after successful private publication. Both unactivated default profiles and restored profiles retain their exact storage roots, namespace membership and activation association. External preimage changes, substituted publication/authority identities, failed writes and interrupted writes cannot gain a refreshed binding. Native reader closure now retires the same tracked descriptor it opens.

Independent review found and reproduced a cross-profile race in the first repair: an unrelated successful profile write invalidated the current transaction. The corrected check verifies the selected profile and association while retaining global pending-recovery and registry checks. A real two-process regression confirms both disjoint writes and both bindings remain valid. The final focused run passed 39 checks in 56.81 seconds; the two duplicated mounted-edit cases were then consolidated into the existing profile-opening suite, whose 27 checks passed in 103.05 seconds. Independent review approved all eight frozen source/test hashes and separately passed 32 companion/bootstrap checks, five persistence checks and the native concurrency reproduction.

The approved two literal-comparison inventory entries are applied. The path census passes with 54 occurrences matched by 51 approved exceptions; the persistent diagnostic inventory also passes. Ruff is clean on the new helper/bootstrap and new tests. Bandit reports no findings in the helper/bootstrap/raw/private-path scope and exactly the same three existing config findings as the staged baseline.

The two approved Linux diagnostic files were transferred and their exact hashes verified on the native host. The bounded default replacement diagnostic passed against the preserved `f69a11ec` installed package with all 2,865 installed files unchanged. Its earlier finalization failure did not reproduce; no deadline or product rule was changed. The full default replacement/later-rollback repeat also passed on the preserved Linux build, with all 2,865 installed files unchanged. The bootstrap suite passed 31 checks in 4.01 seconds. Current-build platform/keyboard acceptance remains in progress.

The Windows ordinary-reopen observer passed the full explicit replacement/Abort/reopen workflow in165.12s. A separate full default workflow reached later rollback, then exceeded its existing first-preview15-second test wait; that result remains a failure, and the deadline is unchanged. Runtime Notes-handle metadata passed all three installed mounted capture routes locally in156.02s and six independent observer cases plus a native transaction-preservation probe. Both diagnostic edits are test-only; Ruff passes and Bandit adds no findings.

The later rollback refusal after keyboard replacement was traced to ordinary Quit: the installed config writer published merged defaults, then reading its new binding record changed only the file's access time. Whole-stat equality rejected that unchanged record and left the old fingerprint. The preserved fixture and a separate native reproduction prove this sequence. The correction excludes access time while retaining device/inode, mode, link count, ownership, size and nanosecond modification/change times, plus exact contents and existing authority checks. Independent review approved; 61 binding/activation tests and 26 activation-pair/default lifecycle tests pass. The original installed artifact still fails both new writer regressions with the corrected observer. Product-scope Bandit reports zero findings; two pre-existing control-record Ruff findings are unchanged. A new immutable keyboard repeat remains required.

The exact `e3b022f82c` Console diagnostic passed on Linux in a fresh private fixture; its Notes connection retired and all leases drained. This single pass does not resolve the earlier intermittent Console timeout. Windows run `34810093007` still has four successful product selections, two failed ordinary-reopen selections and support running; diagnostic run `34812642484` is queued behind it. No current all-platform acceptance claim is made.

Windows support subsequently completed: 42 native checks passed; product checks reported 508 passed, 13 failed and 3 skipped. Five failures exposed test-platform assumptions: Windows ACL damage requires native ACL changes, held directory handles prevent the attempted rename, and symlinks produce the adapter's exact reparse refusal. The corrected fixtures retain unchanged-data/authority assertions and prove the formerly blocked rename works after release. First-note tests now retain their bounded diagnostic logs and wait for the actual wizard container. These test-only changes passed independent review, 40 portability checks and four first-note checks, including both installed workflows. No deadlines or product refusal rules changed. Windows repetition is still required.

A separate real SQLite lock probe reproduced a Notes connection-initialization leak that prevented backup pause from draining. The getter now closes only its newly opened, unpublished connection on setup failure; successful native close releases its lease, while a failed close retains the lease and original setup error. Eleven caller-retirement checks pass, including two new native lock regressions; six independent cleanup/borrower checks pass. Eight broader participant-lifetime failures reproduce unchanged against the prior installed package. Bandit introduces no findings in the changed Notes scope. A local installed capture run passed Console and Library but timed out in Settings; a preserved Settings repeat passed in 52.20 seconds. These results do not establish that the intermittent capture race is fixed.

Linux at `84cf0c3414` passed 90 of 91 checks, including config-binding, activation interruption, default replacement and later rollback; Library capture timed out. Its new metadata shows an active worker correctly prevented initial retirement of the main-thread Notes cache, then finished while the cache remained held. The one-shot retirement coordinator never revisited that now-idle cache. A minimal repeat of the existing checked retirement operation within the existing deadline is under implementation after independent design review.

The cache-retirement retry is implemented and independently approved. Two native regressions prove a completed worker permits subsequent retirement while a held worker remains protected; the combined runtime/caller scope passes 24 checks. All three fresh installed macOS capture routes (Console, Settings, Library) pass in 153.90 seconds with the fixture and logs preserved. The exact product change repeats the existing eligibility check within the same pause and deadline.

Windows diagnostic run `34812642484` confirms both ordinary reopens were progressing through constructor stages when the 45-second test observer killed them. The reviewed Windows-only observer now allows 135 seconds, matching the existing recovered-profile Open check; the enclosing 600-second F9/restart budget and all application deadlines remain unchanged. This adjustment does not apply to the separate profile-opening tests. Native Windows verification remains pending.

Native Linux verification of `9b7316d2f423fe43ffff9c046b1a76c4f4defe06` passed all 74 tests with no failures or skips: new native runtime/caller regressions, Console/Settings/Library installed capture, first-note capture/handoff, companion/sibling fixtures and config-binding writes. Sixty sanitized artifacts were retrieved and all recorded SHA-256 hashes verified; two installed package receipts are retained. The fixture remains on the SSH host at `native-retirement-9b7316-qc7gvh2c`. This is a single full affected-scope repeat backed by the deterministic worker-race regression.

Windows UAT observation fixes are independently approved and locally verified. Only the actual app's first default Textual screen barrier receives a 135-second Windows observation ceiling; the original barrier, result, exception, cancellation and subsequent calls remain intact. Windows profile test bounds are consistently nested (child240/service255/outer300), while non-Windows45/50/70 and product deadlines remain unchanged. The 1,800-file Windows benchmark receives600 seconds; its full publication/readback assertions and existing phase diagnostics remain unchanged. Four fixed mounted-entry timing records distinguish constructor and initial yield. Local genuine workflows plus helper tests passed14/14 in120.24s; an independent11-case check and the combined27-case helper/sibling check pass. These are test observation limits, not startup-performance acceptance claims.

The three prior Windows sibling unsafe-parent skips are removed. The test now saves the original native DACL, grants Everyone read access, requires the existing guard's refusal, restores and compares the exact DACL, and verifies private parent identity and unchanged files. POSIX continues using its original mode transition. Actual Windows execution of this fixture remains pending; qualification skip accounting is unchanged.

Native macOS full replacement verification at exact `4becc2872c` passed both workflows without retries or deadline edits: explicit128.22s and default replacement plus later rollback286.43s, total434.79s including builds. Both wheels match all2474 Python source files; preserved metadata records hashes and final ordinary reopen/note-read completion. Windows native replacement/default later-rollback at9b7316 likewise passed both product workflows (555.905s and1201.657s); observed ordinary reopen completion ranges42.765–55.563s. The prior45s test ceiling had censored real successful completion.


## Approved startup-count exception and exact macOS verification (2026-09-14)

The owner explicitly approved the ADR-097 exception: import limit660→686 and warm UI-ready limit975→1022. The limits preserve the measured dev headroom (dev643/975, candidate669/1022). The same commit appends both ADR ledger rows and refreshes only the two module snapshots through the official generator without `--force`. All11 targeted guards pass in27.96s; independent review verifies that timing limits, absent-family guards and other executable code are unchanged. Ruff retains2 existing findings and Bandit39 existing test-scope findings, with zero new findings. Evidence is indexed by `approved-startup-budget-checkpoint-20260914.json`.

Exact `9e5914fca140e762b66ec4a7beb77cf2af450ca0` macOS native verification passed63/63 with no skips in1256.57s, including installed explicit/default replacement, later rollback, mounted captures and the Persona/Evals corrections. All109 artifact hashes and four installed receipts were verified; each receipt matches2474 Python source blobs from that exact commit. This is native automated verification; the separate immutable keyboard repeat remains in progress.

Windows focused run34825631919 completed:42 native passes; product2 passes/7 failures/0 skips. Mounted Library capture and1,800-file publication pass. Complete tracebacks expose a Console background-refresh pause race, Settings dependency refusal, profile-opening20s readiness waits and three ACL export comparison failures after successful unsafe-parent refusal. These are under diagnosis. The exact Linux run has one encrypted-with-credentials failure so far; diagnostic metadata access awaits owner approval after two automatic review rejections. No final cross-platform acceptance is claimed.


## Windows pause and diagnostic corrections (2026-09-14)

Native Windows evidence showed a Console worker crashing after the archive had verified successfully. A native maintenance request can arrive before the UI monitor observes it. Console now defers only a refused configuration-operation entry, retaining the visible controls and scheduling one fresh replay. Its asynchronous refresh stops before later native reads and coalesces subsequent requests behind that replay. Actual rendering, configuration and cleanup errors continue to propagate. Native regressions reproduced the entry, continuation and duplicate-work failures; all 21 final cases pass in 24.71 seconds, and an independent 8-case review passes. No storage guard, owner authority or deadline changed.

The three Windows ACL failures occurred after the intended public-parent refusal. Their test fixture now retains and restores the original native DACL using its pinned object, then compares exact owner, descriptor control/revision and ACL bytes. It no longer compares exported text files. All 27 local cases and eight independent helper cases pass; native Windows acceptance is still pending.

Settings dependency diagnostics now identify the unavailable dependency by owner, status and hashed logical IDs, with bounded output and no paths or configuration values. The optional Windows startup diagnostic compares the same fresh-process GGUF case on dev and the candidate using cProfile, retaining the original 60-second test limit and failing check. Profile timings include instrumentation overhead. Review caught an observer error-precedence issue; the corrected observer preserves the original test error/exit status even if sampling or output fails. All 41 combined observer/CI checks pass, with seven final observer cases and eight independent checks also passing.

The focused native Windows selection retains its nine original cases and adds the 21 Console regressions, for 30 cases. Its existing observation budgets are unchanged. The current installed macOS mounted repeat, immutable keyboard acceptance, Windows replacement run and Linux diagnostic approval remain pending. Evidence and independent reviews are indexed by `windows-pause-and-observation-checkpoint-20260914.json`.


### Recovery handoff and native Windows replacement checkpoint — 2026-09-14

The exact 9e5914 Windows replacement run34825668804 finished with42 native passes and11 product passes,1 failure and19 setup errors, with no skips. Explicit replacement, default replacement with later rollback, and all9 Evals checks passed. All20 Persona cases stopped before backup assertions because their seed used the existing POSIX-only Persona publisher; a portable test-only seed is under review. The54 artifact hashes and both installed source receipts were verified. The previous failures remain preserved.

The Console pause correction now passes all3 fresh installed macOS mounted capture routes in126.33s. All2474 installed Python files matched the frozen candidate product source; the wheel hash and JUnit receipt are recorded in `console-pause-installed-macos-20260914.json`. This is candidate evidence, not an exact committed-revision platform pass.

Actual keyboard UAT exposed a separate recovery restart defect: `python -c` allowed inherited cwd to precede the launching package supplied through PYTHONPATH. The minimal correction adds `-P`, as already used by isolated profile launch. A real subprocess first reproduced the competing-package import, then all15 restart tests passed after correction; independent review approved. The native regression is included in the full and focused Windows selections; the latter now collects31 cases with unchanged budgets.

The stopped keyboard fixture verified its original Complete plain archive, credential-review Abort without publication, exact original-note onscreen readback and ordinary Quit. All installed/shared resource hashes remained unchanged. Its initial Library5s timeout remains recorded; one Retry succeeded. Mixed package paths during recovery invalidate acceptance of an immutable package throughout handoff, so replacement and later rollback were stopped and require a fresh corrected wheel. `recovery-handoff-checkpoint-20260914.json` indexes the reports. Linux diagnostic metadata access remains pending explicit approval after automatic review rejected it. No all-platform completion or merge is claimed.

Final portable Persona seed verification: all20 existing lifecycle/refusal cases pass in482.20s; independent native private-posture/graph probe passes. Only test setup changed; publisher product code and all backup assertions/deadlines remain unchanged. Fresh Windows acceptance is pending.


Native Windows startup comparison d3082 is now terminal: four original full-app60s checks still fail, while31 non-UI checks and macOS/Linux jobs pass. The same fresh profiled claim-authority case passes on dev4631 in48.42s and times out on candidate; all8 snapshots identify the correct source. Candidate shows heavy repeated admission/configuration/native-filesystem work before and around initial UI. Cumulative costs overlap and profiling adds overhead; no timing exception or guard reduction is justified. Full bounded evidence is indexed by `windows-startup-dev-candidate-d3082-receipt.json`. The independently reproduced Console/Canvas lock cycle has a reviewed minimal two-method correction under TDD; Windows timeout causality for that cycle is unproven.
# Console/config native follow-up — September 14

Two independently reproduced lock-order cycles are corrected: native Canvas policy reads precede the Canvas lock, and outer checked configuration operations acquire REBUILD before FILE with cancellation checks and reverse cleanup. The raw snapshot reader uses that same checked operation. A Console tick now shares one checked configuration lifetime across its existing synchronous rail, settings-summary and control refresh; the later mode bar, asynchronous work and fresh post-await rail read remain separate. Native pause replay, source validation and UI/config error distinctions are unchanged.

The 26 new native cases and existing 21 Console cases pass across the recorded runs; each correction has independent review. All three installed macOS mounted backup cases pass (119.50 seconds), with 2,474 Python files matching current source and 2,866 installed files preserved. The same local instrumented full-app case passes before and after grouping, with native acquisitions reduced from 2,400 to 1,872 and guarded calls unchanged. These overlapping profiler timings include overhead and do not establish Windows acceptance. The 68 legacy config tests retain their identical HEAD outcomes (52 failures/16 passes); they are not counted as passing acceptance.

The exact preceding d3082 Windows support run passes native42 and product23/31, including all21 Console lifetime/retry cases, actual restart origin and 1,800-file publication. Remaining mounted3/profile2/ACL3 failures are preserved and investigated. The existing focused selection now includes the26 new regressions (57 total), without changing deadlines. Current immutable keyboard replacement validated successfully; normal use and later rollback remain in progress. Linux diagnostic reads still await the separate requested approval. Evidence: [checkpoint](backup-uat-remediation-evidence-20260913/console-config-remediation-checkpoint-20260914.json), [Windows report](backup-uat-remediation-evidence-20260913/windows-support-d3082-review-20260914.md). No merge or final acceptance is claimed.
# Primary-core discovery correction — September 14

A real concurrent ordinary note write can invalidate the first strict private database snapshot. The primary-core adapter previously reported that unverified observation as included with guessed optional dependencies, while later same-preview owner reads could succeed and report those targets unused. It now reports the exact same core as unavailable with only its known config dependency. Successful discovery, native snapshot checks, actual referenced-owner checks and capture scope validation are unchanged. A fresh stable review is still required; no automatic retry or scope exemption is added.

The correction passes22 focused cases, five independent cases and a fresh installed Library backup (47.04 seconds), with all2,474 installed Python files matching source and no new static findings. The current Windows run continues testing its already-built e683 artifact; the four new discovery cases join the existing native selection for the next corrected-revision run. [Evidence](backup-uat-remediation-evidence-20260913/core-dependency-discovery-checkpoint-20260914.json).

The immutable d308 keyboard later-rollback review subsequently succeeded and was available, but execution after full destination/password/consequence review refused with `target_changed` before an operation ID was shown. The fixture and refusal are preserved. Read-only diagnosis is checking the actual target change and the ordinary-app recovery handoff; no later-rollback pass is claimed for this run.
