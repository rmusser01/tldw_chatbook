APPROVED, bounded test-only review of the three files in uat-native-acl-final-hashes.json. Exact frozen hashes independently verified. Descriptor ownership is correct: GetSecurityInfo allocation remains live through SetSecurityInfo and post-restore observation, with LocalFree in finally. The parent object is pinned and reopened for READ_CONTROL|WRITE_DAC; restoration writes only the captured DACL and original protected/unprotected flag. Exact owner/control/revision/DACL equality refuses a different native result; no normalization or guard bypass occurs. Native handle/context cleanup preserves a preceding body error as exception context when restoration fails. Existing native public-posture/refusal/restored-private and file byte/inode checks remain. No actionable finding.

Independent8 helper tests passed; /private/tmp/uat-native-acl-independent.log. Native Windows API restoration remains CI acceptance, not established by these local mocked helper tests or POSIX sibling tests. In particular Windows may produce differing descriptor controls; the strict equality will fail visibly rather than grant a false pass. No product/source edits by reviewer.
