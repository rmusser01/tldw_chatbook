# Keyboard progress diagnostic independent review

APPROVED after resolving the caught-exception suspension finding. Four test/diagnostic/workflow files against `d2f89fbc01d49b326c14c7ef2f345d230032d0d4`; no product edits or reviewer app boots. Exact hashes: `/private/tmp/uat-keyboard-progress-independent-hashes.json`.

Independent initial24 helper tests passed1.68s. A separate real asyncio probe proved that PY_RESUME alone leaves obsolete suspended_at metadata while a selected coroutine runs a caught CancelledError handler. The final implementation handles PY_THROW too; **26 helper tests PASS0.94s**, and the original private RED probe now passes with null suspension in the running handler while preserving its return. Logs `/private/tmp/uat-keyboard-progress-independent-final.log` and `/private/tmp/uat-keyboard-progress-throw-{red,green}.log`. One existing Requests dependency warning remains.

Python3.12 rejects PY_THROW in the local event mask. The intermediate local-mask attempt is not accepted evidence. Final source correctly registers/unregisters its callback and includes PY_THROW only in the existing global event mask alongside PY_UNWIND. Both immediately filter to the finite code selection; thrown exception arguments are ignored. Normal resume and thrown-exception resume clear suspension; yielded metadata is rebuilt at the next actual suspension; return/unwind retire the active record. New tests cover ordinary resume, uncaught cancellation, caught cancellation, and caught awaited ValueError. No replacement/swallowing of the observed coroutine's return/error is introduced.

Selection occurs from collected items and exact source-file code identity, retaining only the collected GGUF function and `_mount_models`, `_settle_pilot_until`, `_close_context`. It unwraps the actual test, checks helpers against the same selected source, and adds no helper imports or broad task enumeration. Existing after-collection config selection remains unchanged. The driver passes a single node in each process. Suspension output is bounded basename/function/line, not arguments, values, exception text, frames, task names, or DOM data. Existing active/completed/error bounds and temporary live-frame cleanup remain. The suspension location identifies the selected coroutine's await site, not the deepest awaited task or executor queue contents. Coroutine elapsed/thread-CPU intervals can include other work on the same thread; they are not exclusive coroutine CPU.

`--no-profile` leaves cProfile disabled and emits an explicit false flag with empty profile tables. Finite sys.monitoring and periodic stack sampling remain, so it is reduced observation overhead, not an uninstrumented performance run. Existing monitor-slot coexistence, owned callback cleanup/retry, original return/error preservation and60-second pytest invocation tests still pass.

Workflow primary cases, assertions, dependency installation, product source and20-minute job limit are unchanged. On Windows failure, it runs llama-cpp dev→candidate, then llamafile dev→candidate, as four independent processes with the same original60-second case limit and the same current diagnostic script. Pinned dev source remains `4631b60f8dd9623fc55bf16f4a37e29fcb1240c7`; candidate is the exact checked-out workspace. Source path/selected-node environment are set before pytest imports. The baseline has all three helpers AST-identical and the keyboard test body AST-identical; current source has the pre-existing private-profile decorator/request parameter, whose already-selected node executes directly. This is not a new fixture behavior change in the reviewed diff. No failed primary result is converted to success by the continue-on-error diagnostic step.

The four diagnostic windows remain bounded but add two processes versus the old comparison; no claim is made that dependency installation or the complete job can never exhaust its existing20-minute outer limit. Terminal partial/missing outcomes must still be reported if that occurs.

Parent reports actual Mac original llama-cpp no-profile case PASS23.95s before the final PY_THROW correction, with matched/error-free test_mount/test_case suspension records. That is supplementary integration evidence for capture; final exception semantics are independently covered by the26 tests and private native probe. Windows acceptance remains pending. No outstanding actionable finding.
