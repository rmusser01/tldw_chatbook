# Workspace tray layout implementation review

APPROVED: no remaining actionable finding in the three-file implementation against `63756d61d621be37f4b5a94df9640fd565575333`. Exact reviewed SHA256 values are recorded in `/private/tmp/uat-tray-layout-implementation-review-hashes.json`.

Independent verification: all five new native subprocess cases passed in 7.34 seconds, with an isolated basetemp and the shared project venv. Log: `/private/tmp/uat-tray-independent-pytest.log`. `git diff --check` passed. Root owns the separate Ruff/Bandit comparison; this receipt does not invent results for it.

The product replaces only the recursive bottom==1 callback retry with a one-shot subscription to the owning screen's real layout signal. An optional stored signal coalesces requests; assignment occurs after successful subscription. Immediate signal delivery unsubscribes and clears ownership before queuing the ordinary fit. Unmount uses the retained signal, addressing the previously identified detached-screen lookup hazard. No inherited recompose/capture behavior is replaced. Normal height calculation, width relabel, parent scroll scheduling, resize handling, native admission, and all deadlines remain unchanged.

The tests use the actual production tray and Textual layout beneath two real screens, with actual zero child geometry after recompose. The only override counts fits and delegates the full original implementation; there are no geometry, CPU, guard, or layout-result replacements. Two covered recomposes verify callback counts stop changing. Same-size return verifies the 45-line recovery block's actual height, expanded tray bounds, non-overlapping following section, and its reachability by scrolling. Transparent-background, hidden-parent, and visible cases preserve ordinary layout behavior. Removal is followed by actual uncover/layout and checks both detachment and no further fit invocation, rather than merely relying on successful process exit. Each child must also reach the shared completion sentinel.

The bounded waits are observations of callback quiescence in small native lifecycle tests, not proposed product time limits or a Windows speed claim. Source-level coalescing is one subscriber per pending tray; these tests do not independently count Signal's private subscription list, which is unnecessary to establish the tested behavior.

Runner changes only add the five-case module to full, ordinary support (via `_PRODUCT_TESTS`), restore-diagnostic, and native-close-diagnostic. Plain remains its existing actual plaintext journey; other indexed selections and their deadlines are unchanged. The separately enumerated support-diagnostic tuple is unchanged; this distinction was reported to the root reviewer.

Scope limit: this is a real small Textual lifecycle regression, not a complete installed backup journey or proof that the preserved Windows Settings/Library timeout has been resolved. The original stacked-screen RED proves the callback loop; new native Windows and installed backup results must independently establish their outcomes.

## Final four-file refinement

Approval retained. The layout test's final import ordering was inverted back to the reviewed ordering and reproduced the exact previous SHA256 `73c93f991f1af180b43d6d2a8e4d1a8aa107878d3052549847b07751286e5e83`; no other bytes changed. Current layout-test hash is `ee96e9b4eea68e247be9dfd41a85a8282da0fcba99f0ca5c434e601c378f3a07`.

Also reviewed the eight fixed, flushed `guidance_phase=` print sites in `test_console_guidance_readiness.py`. Parsing the embedded child script, removing only these exact literal checkpoint expressions, and comparing ASTs reproduces the HEAD script exactly; the surrounding test-module AST is also identical. There are seven unique phase labels, with `assertions_complete` intentionally present in the two mutually exclusive routes. No arguments, settings, paths, error messages, or application values are printed. Original calls, assertions, exception handling, and deadlines remain intact. Flushing makes the last completed checkpoint available in subprocess timeout output. `mounted` establishes run_test context entry; `assertions_complete` establishes route-body completion before context teardown, not final test success; `shutdown_complete` is after asyncio.run but before the retained final no-network assertion and completion sentinel.

The four-file receipt has been refreshed. Product and runner hashes are unchanged. No apps or tests were rerun for these refinements, as requested while the root runs installed workflows. Root reports product Ruff findings unchanged at two, product Bandit zero, and clean runner/new-test static results with assertion-only test findings; these are explicitly root-reported checks rather than independent reruns. The omission from support-diagnostic was confirmed intentional.
