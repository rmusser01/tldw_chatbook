# Actual capture interruption and restart acceptance

TASK-32562. **PASS for the fresh independent power-interruption HOME.** Immutable installed tree `580c657bba5e05e185f8f62243c17f4c16eb28e6`, wheel SHA256 `c9e03643b30fae1671c778c2011398d1bcac4b76e83c5c3c3689e498b9f679ea`. All 5,347 installed file hashes remain unchanged.

The fixture used the ordinary installed `python -P -m tldw_chatbook` entrypoint under a new HOME in `/private/tmp/chatbook-backup-uat-repeat-20260913-c8we5yz9/power-interruption`. Actual keyboard actions used Welcome → Skip setup → confirm, declined online model checks, created a Blank note through the New Note palette command, typed its title/body, and autosaved with Escape. No precompleted configuration, Pilot, widget assignment, product patch, injected fault hook, deadline change, or recovery-state cleanup was used.

The saved note is `Interrupt backup survival note`, body `Saved before interruption. C8-INDEPENDENT-1800-20260913.`, id `3f5b7b6f-a4c0-4302-b152-f2deed3a4766`. The existing 1,800-file research folder from the prior power fixture was selected through the ordinary folder picker and reused without modification. All file hashes, the saved Notes rows, and config hash were recorded before capture.

The actual first review showed **Complete coverage**, **Partial — acknowledged** for the external file selection, and available capacity. Separate read-only installed preview confirmed exactly one profile, no issues, and no unavailable/unsupported/missing required sources. The UI then showed **Running: capturing**. Only the verified app PID `71416` was sent SIGKILL; the process receipt records the command and timestamp. This establishes interruption during the UI's capturing phase; no claim is made about which internal copy instruction had executed.

Immediately after interruption, the output `interrupted.tldw-backup.zip` did not exist, and all 1,800 research files, saved Notes rows, and config bytes were unchanged. Ordinary restart mounted Console normally without deleting or repairing any pending/catalog/profile state. The note and all 1,800 files remained unchanged. First Console mount serialized its normal default rail layout: removing that single added table and using the existing TOML serialization reconstructs the exact original config SHA256. The disclosed startup-only config delta is preserved in its evidence; config bytes are not claimed unchanged across this first Console mount.

The keyboard retry selected the same research folder, acknowledged Partial coverage, and used `retry.tldw-backup.zip`. The UI reported **Archive verified**. Archive SHA256: `58aa245978fd9b43a396516b4771806250902ff2c994da54ea9f42f44d5faa5c`; manifest SHA256: `5553438009df61f13f83b73a46eb4b9f0dfde1c82e018d5730056b4ef2b10002`. It contains 1,936 verified payloads, is classified Partial because of external files, and excludes credentials. Byte-readback verifies all 1,800 archived research files against the pre-interruption baseline, the exact archived note, unchanged live research files and Notes rows, no additional config change, and no completed interrupted archive.

## Earlier attempt preserved separately

An earlier actual interruption used the old power HOME whose all-known-profiles catalog included two old restorations. Its retry failed because the earlier first restored profile already had a changed retained evaluation/config binding (`eval_retained_binding_changed`). That run is **not counted as a passing interruption/retry test** and does not establish interruption causality. Its failed terminal frame, selected-profile coverage, process receipt, and discovery summary remain preserved. No stale catalog, old profile, or pending record was modified to make the fresh run pass.

The curated provenance index identifies each small evidence file and its original immutable artifact path. Full action/ANSI logs and 1,800 per-file baseline hashes remain in the original artifact; no password logs or package artifacts are copied here. The parent-owned replacement scenario and other tmux sessions were not touched.
