# Windows ec139 Persona partition causal review

**One shared setup failure caused all 19 errors; the negative test bodies did not execute.** The partition has 42 native passes and two product passes: explicit F9 replacement (683.954s) and the full nested-Persona reopen/later-rollback lifecycle (1565.594s). Controller verification reports 45 artifact hashes and one installed receipt with 2474 matching Python files. All 19 retained failure bodies independently match the same `persona-negative0` fixture, `_add_note_with_cursor`, and `sqlite3.OperationalError: database is locked`.

Exact tested source: `ec139a0241f2de0b8c5da62916e25157f9ac4b35`. First traceback: `/private/tmp/uat-windows-ec139-persona/junit-failures/6b5d77b5526523fc.txt`. The shared module fixture is `test_created_persona_subtree_rollback.py:383–385`; `_prepare_native` performs sequential source/target seed, source capture, replacement, and ordinary reopen (`:265–293`). It fails only during that final reopen child, before yielding to the nine malformed-graph and ten ancestor-exclusion cases.

The reopened app enters its real `run_test` lifetime and successfully lists notes and verifies incoming/original-note expectations (`_REOPEN`, test file `:151–153`). Its next direct native `app.chachanotes_db.add_note` call (`:154`) fails at **INSERT INTO notes**, before note-link updates: `ChaChaNotes_DB.py:17513–17514 → :17494 → base_db.py:468`. The terminal exception comes from SQLite's actual execute after the cursor's use reservation was acquired. It is not an admission refusal, archive validation failure, unsupported Persona format, or a subprocess timeout. The child exits 1; parent `_child` reports that exit without retry (`test_config_sibling_capture.py:117–140`).

Ownership/lifetime evidence is limited but useful. The recorded Notes initializer thread commits its schema/setup transactions, performs the WAL checkpoint, and closes its connection at 17:21:39.985. The main thread later reopens and commits another transaction at 17:21:54.312. These records do not support blaming that retired initializer cache, and do not prove that no other Notes connection subsequently owns a transaction. Startup warnings and a later conversation-list log identify activity, not a SQLite lock owner. The fixture's preceding list uses `fetchall` (`ChaChaNotes_DB.py:16745–16746`); source alone does not show an unconsumed result introduced by that read.

`add_note` normally requests the existing deferred transaction context. The manager can also nest within or borrow an existing transaction (`ChaChaNotes_DB.py:24000–24038`), so this traceback does not establish which branch the failing call used. The cursor releases its use reservation on an execute exception (`base_db.py:469–471`); the transaction manager rolls back manager-owned work and preserves borrowed ownership according to its existing contract. No retained transaction-depth/borrow-state, SQLite extended error code, owner stack, or timed connection snapshot exists at this failure; `faulthandler.log` is empty. The SQLite error text cannot distinguish ordinary writer contention from a stale-snapshot write upgrade. A specific blocking writer, a leaked native lease, and cleanup success cannot be inferred.

The passing full nested lifecycle in this **same partition** uses the identical `_prepare_native` and `_REOPEN` code in a separate profile, and passes the new-note write, later safety-copy readback, restored reopening, and subsequent capture. That directly rules out a deterministic inability to perform this seed/reopen sequence, but neither dismisses the failed write nor proves the defect belongs only to the fixture. Both Persona children intentionally import the exact source checkout through `_child`'s `PYTHONPATH`; the partition's separate installed receipt must not be represented as an installed-package assertion inside these source-based Persona children.

**Smallest next evidence, if authorized:** observe only the existing Notes transaction entry/exit and the failed add-note boundary in this shared fixture, retaining bounded fixed callback labels, thread/connection ordinals, managed depth/borrowed/native-transaction flags, and the SQLite numeric extended error code, with a bounded contemporaneous thread snapshot on failure. Preserve the original methods, return/error identities, write, assertions, and deadlines. This would distinguish an active competing transaction from a borrowed stale snapshot before selecting a correction. No product change, artificial settling wait, cache close, retry, timeout increase, or guard relaxation is justified yet.

No test rerun, database open, repository edit, native fixture mutation, or Linux-log access was performed.
