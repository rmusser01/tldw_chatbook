# Windows focused e683 verification

Run 34835166453 completed with failure at clean exact e683efb86433d3598a99e31033da38f778b59896. All 48 artifact SHA entries verified. Installed receipt has 2,866 files and matches all 2,474 comparable Python Git blobs (32 exact, 2,442 Windows CRLF-only). Downloaded receipts are verified; the remote install itself was not newly re-read. Native: 42 passed, zero skipped, 1.811s. Product: 57 collected, 50 passed, seven failed, zero skipped, 2,093.791s. JUnit and summary agree. Full failure bodies, decoded child stderr, and complete case accounting are adjacent.

Passing groups: Console configuration/pause/replay 21; Canvas lock order 14/15; native configuration lock order six; new pre-control projection five; mounted Console and Settings; native restart import origin; 1,800-file publication. These are exact-revision Windows passes, not proof that cold startup cost is resolved.

Seven failures:

1. Canvas stale-true-policy effect test (8.833s): `run.create_canvas` fails before the exercised race. Native compiler.py:774 rejects `selected.executable == False` with CanvasCompileError/runtime-profile unavailable. The other 14 concurrency/refusal cases pass. Determine the legitimate fixture runtime profile before changing the effect test; no Canvas product capability expansion follows from this setup failure.
2. Mounted Library (406.304s): inventory is unchanged, Complete, no unavailable dependencies. Runtime observer records settle_stage/settle_producers `runtime_work_not_settled` and runtime_resume with startup_retired=False. Then capture_service.py:220/admission.py:600 times out waiting for its exclusive lease. Storage diagnostic unavailable; candidate hook names are recorded, but the exact false-returning hook is not. Next diagnostic should identify that existing drain hook with fixed name/elapsed/result while preserving its semantics; no speculative cache or timeout change.
3. Profile console_quit (176.425s): exact TimeoutError at embedded child mount line89, the existing 20s recovery-check wait. Wait begins93.218s; actual reads+receipt occur142.359–147.015s. No normal-quit phase.
4. Profile console_edit (149.843s): same timeout; wait83.875s, actual reads+receipt127.813–132.641s. These remain startup scheduling/latency failures despite grouped config reads; no new exact blocking owner is established.
5–7. Native unsafe-parent emoji/runtime/sidebar (4.044/3.965/4.037s): restoration metadata is identical across owners: owner_equal=True; original/restored control36868/37892 (0x9004/0x9404), XOR1024; revision1/1; ACL lengths72/72; acl_equal=True. Only control bit0x0400 differs. Microsoft's SECURITY_DESCRIPTOR_CONTROL documentation names it SE_DACL_AUTO_INHERITED: https://learn.microsoft.com/en-us/windows/win32/secauthz/security-descriptor-control . The metadata establishes a descriptor-control restoration mismatch, not changed owner or DACL bytes. Native restoration equality remains unchanged; do not silently mask this bit without an independently reviewed fixture-only policy or exact restoration strategy.

Both original immutable runs were left intact; no dispatch, cancellation, source changes or deadline changes were made in verification. The d308 replacement run is separately reported under /private/tmp/uat-windows-d3082e-replacement/review.md.
