# Config-operation startup observer independent review

APPROVED. Three-file test/diagnostic scope against `8db7465e6110726307f2d5ef745646e00f939a9f`; no product edits or app boots. Exact final file hashes: `/private/tmp/uat-config-operation-independent-hashes.json`.

Independent two observer modules: **20 PASS in0.89s**, one existing Requests dependency warning. Log `/private/tmp/uat-config-operation-independent.log`. Additional finite private probe: **4 PASS** for genuine stack-depth truncation at25, OSError and RuntimeError during frame capture, and an absent live thread frame. Original worker return identity, active retirement, and monitoring slot cleanup remained correct. Source/log: `/private/tmp/uat_config_operation_review_probe.py`, `/private/tmp/uat-config-operation-private-probes.log`.

Selection is confined to the already-loaded config module after collection. The optional `_config_participants.operation` is unwrapped to its actual code and required to match the exact selected source path. It does not import the participant module. A foreign path records ValueError and leaves monitoring unclaimed; absence retains the original five-function selection. Existing selection tests cover selected, foreign and absent variants. Product functions are not wrapped or replaced; the original PY_START/RETURN/YIELD/UNWIND registration and owned-slot cleanup protocol remain intact.

The first operation yield distinguishes completed scope entry from setup, which includes native admission and config-lock acquisition. It is not pure mutex-acquisition timing. The existing record bounds remain32 active,16 completed, one slowest per finite function label,4 errors. The snapshot adds at most25 basename/function/line coordinates per active record, with each string capped128. Internal Python thread identifiers are used only to choose the live stack; output retains the existing native thread identifier. No arguments, locals, returns, exception messages or path directory components are read or emitted. Temporary frame references are explicitly dropped in finally; none enters active/completed/result state.

Active timing rows are copied under the existing observer lock; live frame capture happens after releasing it. This avoids holding that lock while traversing stacks. It also means the operation can finish and the thread can progress before its frame is sampled. Coordinates are evidence of that thread's sampled location, not an atomic operation/lock-owner proof. Multiple selected active frames on one thread can correctly receive the same current stack, and the25-frame cap can omit an outer selected frame. Empty/missing or partial coordinates and bounded class-only capture errors must be treated as unavailable detail, not a lock-state inference. Ordinary capture failures preserve the snapshot and worker outcomes in the independent probes.

Existing tests continue to cover monitoring-slot coexistence, callback errors, original return/error identity, generator unwind, cProfile coexistence, and cleanup failure with retained ownership/retry. No event registration, cleanup behavior, native calls, deadline, import cap, or observed test assertion was changed. Runner diff adds only the existing installed Settings case beside Library in the existing native-close selection; no mode or node was removed.

The observer can supply the previously missing contemporaneous caller context. It does not itself establish the remaining Windows startup cause or measure unprofiled performance. Windows acceptance and root's subsequent actual-app verification remain separate evidence.

Final naming-only refresh: confirmed the three dummy-variable identifier changes secret→marker by inverse-substitution SHA256 equality to the previous reviewed test bytes. Diagnostic and runner hashes unchanged. No executable behavior change; unchanged tests were not rerun. Final test SHA256 `5a52e219690d4a12e983b7fcddab642d2c460f999dbf15ee3838a4e2cb23d695`. Parent reports actual Mac app15.88s PASS with two source-matched profiles, bounded25-frame active sample and zero final active records; this is supplementary parent evidence, distinct from independent unit/probe checks.
