# Final guidance and layout fixture review

APPROVED, test-only, against92f8f9224bc3cadfc22ba9fe997597b1ce89a05a. No actionable findings in the two final files. Exact SHA256 values: `/private/tmp/uat-final-fixture-independent-review-hashes.json`.

Independent final layout run:8PASS11.02s, no failures/skips (`/private/tmp/uat-bounded-quiescence-independent.log` and XML). All real child processes completed. The approved guidance probe previously passed both unchanged routes independently in12.13s; the promoted embedded script now matches that probe's AST exactly. Root's final combined run is10PASS20.70s (`/private/tmp/uat-final-fixture.log`).

AST checks verified the two guidance test entry points and all outer module code are unchanged, and all57 original guidance assertions remain. Only the approved fixture wrapper changes: real TldwCli/app context replaces unrelated full Settings mounting; real unmounted ChatScreen/store/readiness calls remain; finally awaits actual app._shutdown and preserves its fatal-error check. Whitespace changes in the embedded string are non-executable. Real config writes/admission/session convergence, exception checks, no-network assertion, full shutdown and45-second child ceiling remain. This does not identify the2031d Windows timeout cause: both old cases reached mounted, and the unknown subsequent operation stays unproved.

The bounded layout script differs only by a two-second cooperative timeout around `while section._reconcile_scheduled: await pilot.pause()`, immediately before the unchanged no-pending assertion. All16 original assertions and eight outer cases are unchanged. This tests finite eventual quiescence after geometry, scrolling, hint, focus and allocation have already passed, rather than requiring exactly three earlier pilot pauses to consume every callback. Installed Pilot.pause performs a final synchronous timer update that may enqueue another callback; the existing `_settle` is exactly three such pauses, not a promise that all subsequently queued work is consumed. No pending flag or callback is manipulated, no timeout exception is caught, and the final40ms no-new-reconcile assertion remains. Earlier covered/no-loop and unmount checks still fail immediately under their original conditions. The original45-second subprocess ceiling is unchanged, so the new inner wait cannot expand total child observation indefinitely. This is not a new product performance deadline or a fixed Windows convergence-time claim.

Verified static comparison `/private/tmp/uat-final-fixture-static.json`: extracted guidance Ruff remains I001×3/B023×4 and Bandit57B101; bounded Ruff0/Bandit16B101; no added non-assert security findings. Outer Ruff/format results are root-reported; independent git diff --check is clean. No application source or native guard changed.

Limits: local native and AST evidence approve fixture alignment and bounded quiescence observation. Root reports current2031d Windows actual Settings/Library passes separately, with two guidance timeouts and one pending-flag failure still preserved for that tested revision. These final test corrections are not yet a passing Windows receipt and do not resolve the separate startup performance failures by inference.
