# Windows 05e native-close qualification review

Run34914057022, exact05e07702abedcd3b1dac1058385dddb6f880c343, terminal failure. All42 native platform cases PASS. All13 selected product cases reached terminal outcome: eight DB-status lifecycle cases PASS; original four TTS native-close cases PASS; installed Library capture FAIL. No skipped or unfinished selected cases. Product phase467.352s; Library testcase381.498s. This is an actual service failure, not the420s parent timeout.

Verified all20 indexed artifacts. Clean source receipt matches exact revision. One installed receipt contains2867files; all2475 comparable Python files match source receipt and exactGit blobs (2443 CRLF conversions,32 exact; zero mismatches). Wheel SHA2562a68112a517bf5af9ca51be67353c4389e3b2ada96634c07fbff24f21a2b8ad7. Verification JSON, Git proof, case accounting and extracted JUnit failure live under /private/tmp/uat-windows-05e077-native-close; compact verified-summary.json accompanies them.

Library returned state=failed, phase=capturing, issues=(admission_timeout,). The original AdmissionTimeout traceback is recovery_service.run283→backup1164→capture_service.capture220→Admission._admit600→_lock173→_check188. This is the exclusive namespace-lease acquisition inside the existing60s maintenance boundary. Both preceding inventory observations completed with no unavailable dependencies, no scope delta and no truncation. No held capture-inventory completion is recorded. The test then fails its original succeeded assertion; archive-content and resumed-write assertions are not reached. No backup or resumed-writes success is claimed.

Entry phases: constructor begins0.016s/completes40.766s; mounted context yields94.985s; service close begins and completes333.782s. Existing body/child failure records retain the assertion; no wait-failure record appears. Service close did not cause this failure. No runtime-settlement artifact/events were retained, so the new manager hook is not evidenced as reached or failing drain. This is a different boundary from the older Scheduler false-drain and changing-snapshot failures.

Four bounded stack samples show actual DB-status collector thread7272 progressing through guarded config admission (startup_permission, scope, companion guard and final pause_requested). Scheduler heartbeat thread2156 waits at config operation343; core graph reader7728 initially waits for acquisition initialization, then progresses. Capture thread5944 is waiting for exclusive namespace lease in the first two snapshots and no longer executing capture in later snapshots. These observations establish actors and progress, not a deadlock, manager late-arrival timing, or which exact held namespace prevented capture.

The slowest recorded pause_requested group (local-pause poll thread8032,21namespaces/21roots) takes80.3779665s wall with0.265625s own CPU while process CPU advances80.71875s; nested21 _tokens calls account80.3747054s inclusive. Do not add nested timings or attribute80s to pure overlap-loop CPU. A later56.625s sample window shows mainthread8028 using52.8125CPU seconds and process56.84375s. One main-thread sample is Console workspace height fitting; others show Textual message dispatch/poll. The five-second loop profiler is instrumented and dropped230093calls, so it does not establish one complete hot callback or a safe optimization. No Windows speed or cause claim follows from the eight passing lifecycle cases.

Conclusion: the bounded DB-status lifecycle correction is qualified by all eight actual Windows native cases; the installed Library end-to-end acceptance remains failing at pre-settlement native admission. Preserve native checks and existing limits. The next causal work would require correlating this pre-settlement poll delay/main-loop work with exact retained authority and hook arrival; this review makes no new instrumentation, rerun or code proposal. No repository edits or new workflow execution.
