# Dev698 merge: bounded independent review

No concrete missed semantic conflict found in the reviewed auto-merges. This is source compatibility review, not merged runtime acceptance. Reviewed Git object treee6c6498a6f80e4e1fb02dfde0d18d4c4dcbee98e against ours29baddc035 and dev698c5fa9e81d6830e083e7182555fa90aaf732b4; no working-tree edits, merge, network or heavy tests.

## Compatibility findings

- `app.py`: upstream adds only empty-screen-stack checks to Persona overlay rebuild/scheduling. Backup request/restart, guarded shutdown and native lifetime methods remain identical to ours. The guard avoids dereferencing self.screen after the last screen is removed and is compatible with ordinary-to-RecoveryApp shutdown.
- `library_screen.py`: delegating signatures now match upstream controller's failure_line and tuple-return Console handoff; navigation notices no longer emit the removed graduation toast. Backup live-read retirement and recovery handoff methods are unchanged. Related controller/state/session changes preserve dirty/saving/conflict session state; they clear terminal presentation tokens, retain running tokens and render saved time locally while persisted timestamps remain UTC. The new workspace-link/compensation operations call existing registry APIs; no direct SQLite or admission bypass was introduced by the merge. Actual save/close→backup and Console handoff should be exercised together after merge because they now share those upstream presentation paths.
- `FirstRunSetupWizard.py`: the previously added Restore a backup control/handler remains intact. Upstream empty-key Enter calls skip_without_key then the existing next/commit path; ready environment/local-provider selections are preserved. Nonempty-key probe behavior remains unchanged. This does not remove the backup entry or publish new recovery authority, but deserves the exact new skip test plus the existing backup entry test.
- `Notes/note_import_receipts.py`: our lexical selected path, memory flag, db_path property and NotesDeviceStateStore connection/transaction delegation are preserved. Upstream source-level multi-record matching reads all existing payload rows through the same transaction, with ordered contiguous indices, state/reason/retry/digest/ID/version checks. Matching helper and payload_count model/planner changes all arrived together. There is no schema-version/catalog migration in this change and no new receipt file/root: existing Notes-device exclusion/owner rules continue to apply. A clean merge would have been wrong if the planner/model half were absent; they are present in this tree.
- `pyproject.toml`: version0.2.2 and removal of the native voice companion from speech_recording are coherent with the new app-only release tests and installed-distribution probe. Ordinary audio dependencies remain. Existing backup helper/distribution scope is retained. Previous0.2.1 wheel receipts cannot qualify the new merged wheel: use the current installed-package fixture when doing acceptance. No Backup_Recovery hard-coded0.2.1 or removed graduation-toast expectation was found in its test/product scope.
- Installed-distribution test retains the original immutable installed-tree probe and adds an independent app-only voice metadata/gate check. First-run contract retains its backup tests and appends the empty-key case. No existing backup assertion was replaced by an upstream weaker substitute.

Method-level AST comparison of all six explicitly auto-merged Python files found no method whose implementation differed from both parents. This is evidence that method bodies were retained from one side rather than silently spliced, not proof of all cross-method behavior. All29 changed Python blobs parse, and merged TOML parses with version0.2.2 and the intended speech_recording dependency removal. Backup_Recovery source, Windows native files and NotesDeviceStateStore have no tree diff versus ours.

## Known conflict remains substantive enough to regenerate

The preview still contains conflict markers in production-diagnostic-inventory.json summary task492/task494 counts; it is not a final usable tree. Upstream also changes the Library controller diagnostic row10→12 calls and its digest556dd220325841915afd→54d9aaab79b70689ed97. That matches the newly added type-only workspace-link/rollback diagnostics and changed Console failure log. Preserve/regenerate from actual merged source, not just choose one summary side. Root owns the existing checker and conflict resolution.

## Scoped verification after root completes merge

1. Notes native/data contracts: `Tests/Notes/test_note_import_receipts.py`, `test_note_import_planner.py`, `test_note_import_executor.py`; plus existing operational-owner cases for current Notes-device exclusion/unsafe entries and registered core+Notes physical identity. This checks multi-record old receipts without changing backup schema or owner qualification.
2. Library session/handoff: `Tests/Library/test_library_notes_session.py`, new `Tests/UI/test_library_notes_w4_console_handoff.py` and `test_library_notes_w4_data_truth.py`; existing `Tests/Backup_Recovery/test_unsaved_editors.py` closed/dirty/saving/conflict cases. Then one current-wheel `test_saved_closed_library_note_can_handoff_to_recovery` and mounted Library Complete/resumed-write case, sequentially when native testing is free.
3. Wizard: new `test_enter_on_an_empty_provider_key_advances_with_a_visible_skip`, existing backup-entry control coverage and one current-wheel first-run-note capture. Preserve the separate ready env/local/nonempty-key expectations; no automatic credential staging assumption.
4. Packaging: focused `test_app_only_voice_release.py` and existing backup helper distribution checks, followed by the changed actual `test_installed_wheel_loaders_entry_points_and_assets_are_immutable` when a wheel is built. Re-run source/version/installed-byte receipts against that exact merged artifact.

These are targeted merge-boundary checks, not a request to rerun full heavy app suites concurrently. Existing startup/performance/Windows failures remain separate unresolved evidence; this merge does not qualify them as fixed.
