# Actual keyboard backup/restore acceptance — immutable 4a50

Accepted on macOS using the ordinary installed application in a real tmux TTY,150×48. This report covers this fixture only; it does not claim all-platform acceptance.

- Fixture: `/private/tmp/chatbook-backup-final-uat-20260914-v2wzmm74`
- Commit: `4a50a22cffd5ae0ca029dad4c1e10eada8a2c3a5`
- Product tree: `1152efce49b3e1335aab350428e121b7beadb89a`
- Wheel SHA256: `6f74636d827cd96b4fabd92763670f502cc38aedc71334d6f39b778eb06d7e1b`
- All5349 installed files match the initial receipt after completion (`final-installed-verification.json`).
- Normal inherited cwd remained `/private/tmp/chatbook-backup-pr-20260912`; unchanged launcher uses `python -P -m tldw_chatbook`. Recovery handoff used the installed package. Old fixtures were untouched.

## Completed observed journey

1. Actual six-page onboarding, original note creation and Saved indication. Original title `Final original note 4a50a2`; body `Restore this original after later rollback. FINAL-4A50A2-20260914.` Initial Complete plain archive verified and sealed-note readback passed (`original-archive-readback.json`).
2. Actual incoming-archive inspection and recovery handoff. Required safety sources selected before credential omissions. Untouched Abort exercised; ordinary original-note readback and Quit0 passed (`after-abort-readback.json`).
3. Root assisted the final reviewed replacement confirmation after verifying150 restore/88 retirement paths inside this fixture. Replacement operation `9c9b6226408c40d9b35d20b207d9ff15` reached Restoration validated (`replace4a-resume-result-03`).
4. Ordinary incoming note readback showed `Novice backup acceptance note` and exact NOVICE-REPEAT-20260913 body with Saved (`readback4a-056-read`). New note `Final new note 4a50a2` / `Preserve this later state in the new safety copy. NEW-4A50A2-20260914.` showed Saved (`newnote4a-024-saved`). Normal Quit0; copied database verified incoming/new state (`after-new-note-readback.json`).
5. Ordinary-app later-copy selection offered recovery handoff; actual guarded shutdown opened a fresh RecoveryApp copies list (`later4a-007-handoff`). Fresh review succeeded. First attempt stopped for unreadable credentials, operation `d2ef9b1ff3254708bc41b7f696be843c`; actual untouched Abort succeeded (`later4a-027-aborted`).
6. All8 actual later-copy credential omissions were individually keyboard-focused and green-selected, then verified retained after fresh review (`later-explicit-omissions.json`, `029-ack-*`, `036-retained-*`). This profile offered8 later omissions, versus21 in forward replacement. Final reviewed93 restore/147 retirement entries exactly matched the prior reviewed mutation paths; all remain under `/private/tmp/chatbook-backup-final-uat-20260914-v2wzmm74/power` (`later-final-destination-check.json`). Shared installed sources/control were preserved, never chosen as mutation targets.
7. Final actual later rollback reached Restoration validated, operation `fb3bbb20a3f34d058b57ed85276538b2` (`later4a-045-final-result`). Recovery exited0. Ordinary Home showed Notes1; actual original note body/Saved readback passed (`restored4a-007-original-readback`), normal Quit0. After-Quit copiedDB contained exactly the original ID/title/body, with incoming/new notes absent (`after-later-rollback-readback.json`).
8. New encrypted safety copy inspected through actual UI: Archive verified,186files,coherent,credentials rollback (`final4a-008-inspect`). Separately, installed crypto decrypted only this sealed archive to private evidence; hashed sealed Notes exactly matched the pre-rollback copied current state (`new-safety-readback.json`). Encrypted SHA256 `99a6cc5afccb0b7fdff0aee617829582bd2db03f1e6699151bf8dc69b3037f26`.
9. Subsequent actual Create backup review showed Complete coverage with Partial acknowledgment unchecked (`final4a-018-create`). Completed Archive verified (`final4a-020-result`). Sealed archive136files/coherent, exclusions onlyunused/intentionally_excluded, exact original sole note. SHA256 `b4c4485ea7b00091aca0f16d2ed9edca34352394717e6f2f7f4ce54f07f8baac` (`final-archive-readback.json`). Final ordinary Quit0.

## Method and preserved observations

UI claims use actual keyboard events only, with before/after text+ANSI and actions.jsonl. No Pilot, test mode, widget assignment, onboarding preseed, installed edits, config/binding repair, or live SQLite query was used. Database verification reads private copies made after normal Quit or hashed sealed archive payloads. Password entry in the retained procedural record used stdin/tmux buffers. The evidence audit found neither current retained password value in the 540 action records or saved launch environment. The generic controller itself does not redact type arguments, and overwritten historical passwords plus complete historical process arguments/environments are unavailable, so an absolute never-recorded claim cannot be independently established.

Root's first post-replacement recording redirected stderr, where Textual renders; that recorder error was corrected with a new ordinary launch, not an application change. Both sessions were preserved and exited normally.

Ordinary note navigation showed a collapsed/blank Notes list after pane actions. Actual Home Resume note opened the existing blank note and allowed the required save. One additional blank note resulted from my mistakenly typing expected filter text while no visible input was focused; it remained honest current state and is preserved in the new safety copy. This is not claimed as a backup failure or silently removed. Subsequent original restoration contains only the original note.

The fixed21-count observer stopped safely when the ninth Tab reached the new-password field: the actual later form contained8 omissions. All8 were verified selected; no hidden or absent checkbox was toggled. The first sealed-archive readback script used `owner` instead of manifest `owner_id`; decryption succeeded, that diagnostic KeyError is retained in `new-safety-decryption.log`, and corrected schema-based readback passed without rewriting the archive.

No remaining backup/restore acceptance blocker was observed in this completed journey. Independent Windows/Linux evidence remains owned by the parent task.
