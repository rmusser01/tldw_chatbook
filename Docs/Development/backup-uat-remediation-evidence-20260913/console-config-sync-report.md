# Console synchronous config lifetime — TASK-32562

Frozen scope: `tldw_chatbook/UI/Screens/chat_screen.py` (+25 lines) and new `Tests/Backup_Recovery/test_console_config_sync_lifetime.py` (10 native cases). Exact hashes are in `console-config-sync-hashes.json`.

The existing synchronous control-bar refresh now runs under one existing `config_participants.operation(config)`. The original body is retained unchanged in a private held method; AST comparison confirms every original statement/order, and neither wrapper/body awaits. Both the coalesced callback and the async Console worker's synchronous call use this boundary. Nested readers still check the same installed source, selected path, current raw operation, pinned parents and native holds. Each subsequent refresh takes a fresh operation and reads current settings. No visual tokens, settings cache, guard/deadline, binding, schema or ownership changes.

UI-body exceptions are held until the native operation exits normally, then the identical exception is raised with its existing traceback. This avoids falsely marking config persistence as failed. Genuine nested config failures retain their existing `config_failed`/persistence-error state; no error flag is cleared. If cleanup itself fails, that exception propagates with the original body exception as its cause. This detail was required by a native RED: an initial direct-with draft marked a UI-only ValueError as a config persistence failure, making the real config participant refuse drain even after a valid refresh.

Validation:

- Native original-boundary RED:3 failures/3 passes after correcting a fixture-only ChatScreen delegation setup error. Log `/private/tmp/uat-console-config-sync-red2.log` (actual final summary time in log).
- UI-only error→valid refresh→native participant drain RED:1 failure1.94s, `/private/tmp/uat-console-config-error-red.log`; expanded error matrix2 failures/2 passes5.27s, `/private/tmp/uat-console-config-errors-red.log`.
- Final10 native cases plus existing3 prompt-count cases:13 passed13.53s, `/private/tmp/uat-console-config-sync-final.log`. Covers bound/unbound current config, next-call changes, exact errors and cleanup chaining, pre-existing/genuine native write failure retained, pause-before refusal, admitted pause continuation/drain then new-operation refusal, selector changes, unrelated Notes owner lifetime.
- Independent final review APPROVED:10 passed13.78s, /private/tmp/uat-console-config-sync-final-independent-review.md. Reviewed product hash a142bd431219 differs from final d655157e95f1 only by the required local Ruff explanation on the deferred/re-raised BaseException; test hash unchanged.
- Actual installed Console Complete capture, archive byte readback and resumed live write:1 passed47.95s, `/private/tmp/uat-console-config-mounted.log`. This package predates the final exception-path correction (and only its helper annotation changed before the next wheel).
- Fresh installed full default-history workflow succeeded: Complete capture→F9→credential review→Abort→ordinary reopen→second acknowledged replacement→restoration_validated→ordinary Console/Library. `/private/tmp/uat-library-replacement-history-green/driver.log`, result and phase/stack logs adjacent. Installed files passed the fixture's end-of-run unchanged-byte verification. This package includes the held boundary but predates the final exception-path correction; it is not claimed as final-exact installed acceptance.
- History observation: baseline navigation13.4224s/snapshot2.3518s versus held-boundary8.7449s/0.9205s. Both snapshots succeeded; one comparison is diagnostic evidence, not a controlled performance benchmark. The original keyboard five-second timeout did not reproduce. Full onboarding content and incoming nested persona pack from the keyboard fixture are absent. Original report: `/private/tmp/uat-library-replacement-history/review.md`.
- Existing Console maintenance selection:4 pass/2 failures; both failures reproduce on the unchanged HEAD ChatScreen via disposable native import overlay, `/private/tmp/uat-console-config-baseline.log`. Those tests stub the changed callback and fail elsewhere in the existing async worker.
- Existing control-bar coalescing UI tests:2 setup errors (`raw_source_selection_changed`) before application construction, repeated identically with unchanged HEAD module: `/private/tmp/uat-console-config-ui.log` and `/private/tmp/uat-console-config-ui-baseline.log`. Reviewer independently observed the same setup error in a hidden-inspector case. No baseline tests were modified or disabled.
- Ruff baseline/current208/208 with zero new findings (one existing F811 message's embedded line number shifts); new test file clean. The deliberate broad body-exception retention has a local explanation because the exact error is re-raised after owner cleanup.
- Bandit baseline/current9/9, zero new findings; test file has no findings. Logs `/private/tmp/uat-console-config-{ruff,bandit}-{baseline,final}.json`. `git diff --check` passes.

Design-language.md and relevant native config/test lifetime lessons were read. Plan: `console-config-sync-plan.md`. Root owns final integration, Windows support selection and actual keyboard acceptance; no commit/push/dispatch performed by this agent.
