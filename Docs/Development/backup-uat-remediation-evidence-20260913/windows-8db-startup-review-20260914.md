# 8db Windows startup independent evidence review

Actual TASK2062.2 GGUF source run **34923085663**, job **104235243302**, checked-out source `8db7465e6110726307f2d5ef745646e00f939a9f`. This is not DerivedArtifacts run34923085658 or PerfGuard34923085662. Read-only local review; no tests/runs/code changes. Log SHA256: `abd97c1c6739f7cd35adf6cf014e0b367deb351a696d503493b2ca7e4f263d3f`. Compact metadata: `/private/tmp/uat-8db-startup-independent-summary.json`.

31 non-UI cases passed in 9.69s. All four full-app primary cases timed out at their unchanged 60-second limit: claim-authority/recompose, external-copy geometry, and the llamacpp and llamafile keyboard cases. They have no successful completion receipt. The diagnostic pinned dev source `4631b60f8dd9623fc55bf16f4a37e29fcb1240c7` passed the claim-authority case: **46.87s actual call**, 2.68s teardown, **65.15s entire pytest session**, one SQLite privacy warning. The overall 65.15s is not a >60s passing test-call duration; collection/session costs and per-test timing are different boundaries. Candidate diagnostic timed out.

There are **nine profiles**, five dev and four candidate, all `source_matches=true`, empty observer/config error arrays and zero dropped-active records. Dev emitted an additional terminal profile. These are checked code-origin receipts, not installed-wheel manifests. Last dev observer elapsed68.656s also has a different boundary from pytest's 65.15s.

At candidate cutoff the main thread9656 is synchronously blocked in `CompactModelBar.compose:46 → get_cli_providers_and_models:8719 → guarded load_settings → config_participants.operation:343`. Exact-source343 is the timed acquire loop over REBUILD then FILE. The stack does not reveal which of those two locks is currently being acquired. This is an actual config-lock wait during widget composition; it does not establish a deadlock or its lock owner.

All eleven final thread stacks were inspected. Five executor threads are idle on work_queue.get; asyncio_6/4680 is in Textual Windows native sleep; asyncio_3/4040 has only four native-file/open/info frames, with no retained higher-level caller. UI-stall persistence waits on its queue, storage admission waits on its stop event, and the profile sampler waits on its timer. The four-frame worker sample cannot authenticate a watcher/writer identity, a complete contiguous native call chain, or ownership of the main thread's lock. Do not reconstruct omitted callers from neighboring frames or an earlier run.

The last ConfigTimings sample (03:04:47.057, elapsed61.0s) precedes the terminal stack by about6.08s. It shows **15 user-directory calls, all completed, no active operation**, and no transaction/config_lock/publication/settings_rebuild counts. Those labels instrument five exact functions; ordinary guarded-read operations are not themselves a tracked transaction. Their absence therefore does not mean no thread owns REBUILD/FILE. Completed user-directory wall median0.489254s and thread-CPU median0.484375s; the longest was5.139337s wall/.5s threadCPU. That outlier demonstrates a substantial non-own-CPU interval, not its particular waiting mechanism.

The dev completed transaction was0.697686s; its config lock was0.697546s (.076807s acquisition/.620739s body-plus-release), publication .058027s. There is no completed candidate transaction to compare with it. Candidate inclusive totals at the last sample include constructor22.862s, 271 acquire_storage calls27.888s, 1,384 registry calls17.659s, and69,976 fstat calls28.975s. These nested/incomplete profiled totals must not be added, divided into claimed speedups, or treated as equal completed work versus dev.

Compared with276: that final main stack was the synchronous Canvas watcher in native admission; 8db's final main stack is widget composition waiting for a config lock. The earlier276 timing sample had a transaction/config_lock still running before first yield, then no corresponding active worker at the later stack. 8db has no such tracked transaction sample at all. Offloading the periodic watcher removes its known synchronous loop-starvation path, but this run still fails startup; the evidence neither establishes the remaining owner nor proves a new ownership defect. The smallest missing causal evidence is the current REBUILD/FILE owner and its complete caller at the same time as this wait. No source correction, guard relaxation, caching, timeout change, or layout patch is justified from these incomplete owner samples.

The separate actual installed macOS Settings backup geometry control has24 samples over34.101s wall/13.562s mainCPU, at most6 reconcile calls per section; parent reports no callback growth during paused capture. Its preserved SHA256 is `11319efa018a17b0a9b23b70a9140690f32a0e462fcdf4d2835452363129f208`. It did not reproduce the proposed retained-screen loop. Windows geometry/callback evidence is still required to attribute Windows backup waits to that mechanism. The current startup stack is not in that layout loop.
