# d2f89 Windows startup independent classification

Run34924813706/job104240510243, exact `d2f89fbc01d49b326c14c7ef2f345d230032d0d4`. Independent full-log SHA256 `97b5ab774c280baa942d6c41175ba7a9674be544aea11ec2091b071fd71c1138`. Extracted profile SHA256 `2141d426c61bc72a8a7b737d322a4a17665541021057de68dc0dac398a75eef5`; all nine extracted objects exactly match the JSON in the log. No edits, new tests, remote reads or app boots. Numeric evidence: `/private/tmp/uat-d2f89-startup-independent-summary.json`.

**Actual primary outcomes differ from8db:**31 non-UI PASS7.52s. Claim-authority/recompose PASS51.20s call/56.08s session. External-copy geometry PASS46.38s call/49.86s session. Only the supported-width keyboard variants `[llamacpp-llama-cpp]` and `[llamafile-llamafile]` time out at60s. No product source changed from8db to d2f89, so changed outcomes alone do not establish a product speedup.

Pinned dev diagnostic passes34.33s call,2.55s teardown,43.90s session,2 warnings. Candidate diagnostic repeats the **claim-authority case that passed without profiling**, but times out under the unchanged60s limit. Thus the candidate diagnostic is not a second unprofiled primary failure, and its timings include observation overhead and different work completion. Nine profiles comprise4dev/5candidate, all source-matched, no observer/config errors and no dropped active records. Dev intentionally has no participant-operation selector; its five-function records are not missing candidate-equivalent data.

## Primary keyboard timeouts: what is actually known

Both app constructors reached their startup summary before timeout: llama-cpp 4.729s, llamafile 17.088s. At both cutoff snapshots main is in the asyncio Windows selector and **all eight executor threads** are in Textual `_win_sleep.wait_inner`/`WaitForMultipleObjects`; storage admission waits on its stop event. None of these primary worker stacks is in native config acquisition. This is materially different from the candidate diagnostic's native-admission samples.

The unchanged test `_mount_models:94–139` constructs the app, awaits `run_test.__aenter__`, pushes LLMScreen, pumps Pilot, and waits for deferred provider views. The case then traverses selectors, overlays, screenshots, refresh, external browse and controls using repeated `pilot.press`/`pause`; finally it awaits `_close_context:142–143`. Pilot waits for screen processing before its pause/press operations. Thread dumps do not expose the suspended test coroutine/await chain, so they do **not** prove LLMScreen mounted, a particular modal opened, all keyboard assertions completed, or cleanup began. Constructor completion rules out being stuck in that synchronous constructor at cutoff; it does not prove full UI readiness.

Textual's existing Windows timer implementation queues both blocking timer waits and its cancellation signal through the same default executor (`_win_sleep.py:93–112`). Eight occupied timer workers therefore provide a concrete contention lead. Missing timer due times, coroutine/cancellation state and queue contents prevent declaring executor deadlock or tying the test to a particular timer. Earlier native config cost may consume part of the budget, but is not the sampled terminal work of these two primary failures. The smallest next evidence is bounded test phase/await-chain metadata at cutoff covering mount, Pilot interaction and cleanup, with original waits/executor behavior unchanged. Do not repair these failures by assuming the diagnostic's SID sample is their cause.

## Profiled candidate: native admission is now identified

At25.765s, main3424 traverses `_build_local_skills_stack → get_user_data_dir → _default_data_root_lock → config operation → registry` in native guard entry; active outer/inner operation labels refer to different frames on that thread. At39.859s worker1408 traverses prompt-history path selection→get_user_data_dir→private-parent admission inside a config operation, while watcher3264 and main3424 are sampled waiting at the operation's timed REBUILD/FILE acquire loop343. This supports real contention in this observation; non-atomic samples do not independently prove which mutex or continuous ownership interval.

At53.625s main operation age3.745729s is sampled at SID conversion/private-parent checking under provider/default projection in the inspector. At67.437s a **different** main operation age3.958146s is sampled in SID/private-parent checking under attach/resume→transcript guidance→setup card→settings readiness. Both are before the operation's first yield, at native entry rather than the earlier ambiguous mutex wait. The age is for the entire operation, not3.75/3.96s spent on SID conversion; frames are non-atomic. The late sample is interleaved with timeout reporting, not a completed test receipt.

Latest counts545 config operations,23 user-directory calls and one completed transaction/config_lock/publication/rebuild. Candidate transaction1.558997s; config lock1.558888s (.923035entry/.635854body+release). Dev transaction .581661s/config lock .581565s, but these include different profiled lifecycle work. Candidate sampled totals include680 acquire_storage calls37.416s inclusive,363,436 open_handle calls17.477s,200,353 security calls9.955s and912,391 sid_string calls3.348s. These nested totals cannot be added or converted into an expected startup saving.

Source confirms every `_stat_handle` obtains current native security, and `security` converts the fresh owner and each supported ACE trustee using `sid_string`, freeing native allocations. That is a concrete hot inspection boundary, not evidence of redundant authority checks or permission caching safety. A same-fresh-descriptor native decoding/call-count proof preserving exact owner/DACL interpretation and every freshness check would be a defensible bounded evaluation before any correction. There is no source-backed patch recommendation from this artifact alone, and such work would not explain the two primary timer-wait cutoffs without their missing coroutine evidence.
