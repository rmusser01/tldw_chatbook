Read-only five-case causal review, Windows4becc run34817971803

Exact five cases are the first five lines of failed-eight-nodes.txt. Full eight additionally includes the three unsafe_parent cases; ACL reviewer owns those. No product JUnit means final mounted failure tracebacks and per-case durations are unavailable. Do not infer an admission failure merely from a FAILED log line.

1. Mounted console: real constructor40.422s; mount yields97.531s. Capture review records all five discover/capture stages, unchanged scope, no inventory delta. Runtime retirement removes main Notes/native caches, ending0 live leases after resume. Last retained worker stack is archive_reader.acquire:514, the readback archive os.write; the test reaches this only after service success/complete assertions, resumed write, and live note readback (test_mounted_console_backup.py:84–95). Thus this case got past the formerly suspected live-capture admission failure. It failed during or after independent archive readback/cleanup, but terminal exception is not retained. The Console sync change may reduce startup cost, but does not change that archive acquisition path; no claim it fixes this failure.

2–3. Mounted settings/library: constructors40.781/41.000s, mount yields98.015/97.797s. Neither has capture-review or runtime-settlement output. Last four main-thread samples show synchronous Console config reads and native Windows path/security/admission checks. Settings two samples are inside _sync_console_control_bar, later samples in _current_console_rail_state and _sync_native_console_transcript. Library three are inside _sync_console_control_bar, last in _current_console_rail_state. These are progressing synchronous reads, not evidence of a stuck lease. Final Console lifetime correction directly addresses repeated raw scopes inside the first method; it leaves the other two paths unchanged. No terminal traceback means exact guard wait/navigation/outer deadline boundary remains unresolved.

4. console_quit (fixture act8): construct27.203s, mount93.094s, recovery-check wait begins93.094s. Actual _check_local_content worker starts169.719s and returns173.625s. No recovery_check_wait_complete/receipt_read_complete/quit phase follows; parent logs native subprocess.TimeoutExpired at the unchanged Windows240-second child bound. Last four stacks alternate sync Console settings summary/control-bar/transcript/rail-state getters with native config admission. One of four is within the corrected control-bar body; three are outside. The75-second gap before local-read worker entry and subsequent missing event-loop continuation show scheduling delay around successful local content, not a proven broken Evals owner. The logged Evals warning is not terminal local-read failure.

5. console_edit (fixture act9): construct27.031s, mount93.968s, recovery wait starts93.968s; local reads174.093–179.140s succeed. Child error metadata records TimeoutError in mount at timeouts.__aexit__, matching unchanged20-second recovery-check wait in test_profile_open.py:87–92. No quit/rail edit phase is reached. Two of four retained main-thread samples are in corrected control-bar work; the other two are on_screen_resume cost projection and consume_pending_console_provider_intent. A next run can test whether reduced repeated admission cost is enough; current evidence cannot prove all source scheduling costs are corrected.

profile_open.acknowledge_mounted at153–175 awaits to_thread(_check_local_content) and sets _recovery_open_checked in finally on the loop. Successful worker completion alone therefore cannot satisfy the waiting UI before the loop gets scheduled. This explains why receipt-content reads can pass while these observation waits fail. There is no basis here to increase product timers, broaden config scope across awaits, cache authorization, or optimize unrelated surfaces.

Minimal fixed Actions rerun design: add one finite diagnostic selection containing exactly failed-eight-nodes.txt, reusing the existing native42 phase, installed package builder, source/hash receipts, environment and log collector. Keep existing per-case420/300/35-second bounds and current80-minute outer/90-minute job for this smaller selection. Five GUI maxima total31minutes, ACL maxima1m45s, plus existing build/setup/teardown overhead; well below80 unless new independent failure. Do not introduce arbitrary node inputs or support sharding. Completed pytest will retain all eight final tracebacks/JUnit instead of losing them to a long support run. Existing mounted child should additionally retain fixed phase names after gate/navigation/preview/capture/resumed-write/acquire/exit and _error_metadata for any BaseException or parent TimeoutExpired, preserving identical returns/errors and original cleanup; this only closes the current evidence gap. Profile child already records useful phase/error metadata, so no deadline change is justified. Root can include the separately unreached1800 node as an explicitly named ninth case if desired; it is not one of this run's eight observed failures.
