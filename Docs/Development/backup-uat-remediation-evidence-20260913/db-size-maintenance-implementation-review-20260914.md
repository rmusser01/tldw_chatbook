# DBStatusManager lifetime independent review

APPROVED for the scoped three files. No actionable finding.

The entry gate runs before any await/submission and therefore rejects already queued update deliveries. The caller count is incremented on the event loop before submission, and remains active through the original cache assignment/change-gated logging and exception handling. Closing intake cannot interleave with that initial synchronous admission segment. The separate strong task set plus shield retains the actual to_thread completion after caller cancellation; cancellation propagates, cache publication does not occur, and drain/resume continue to refuse while that worker remains live. The done callback observes an abandoned exception without changing ordinary awaiting behavior. Drain cancellation propagates and does not reopen admission; unsuccessful resume leaves the existing closed-hook stack intact.

The Event clear/check/wait sequence has no await between its state check and clear, so event-loop-only updates cannot lose a completion notification at that boundary. Worker completion and caller completion independently wake drain; neither a finished worker with unpublished results nor a canceled caller with a still-running thread is prematurely declared settled. Existing getter/formatter/error behavior and periodic scheduling are unchanged.

RuntimeMaintenance binds the original methods of the exact installed manager class at the existing producer level, and includes that exact manager identity in the owner snapshot. An instance hook override is not accepted as the hook implementation, foreign types refuse, and replacement after settlement is covered by the existing runtime_owners_changed checks. Resume uses the existing reverse hook order. No native admission/permission checks, deadlines, caches, executors or timer cadence were changed.

Independent verification: all eight new native cases PASS in 8.30 seconds, using the shared project virtualenv and unique /private/tmp/uat-db-status-independent-fixtures. Log /private/tmp/uat-db-status-independent.log; XML /private/tmp/uat-db-status-independent.xml. Cases cover late delivery, normal completion, caller cancellation, collector error, canceled caller plus later collector error, canceled drain, original hook/foreign-owner qualification, and refusing drain while intake remains open. They use real bound profile/config/native database paths and original collection after a finite worker gate. The late test invokes the delivered update directly; the preceding disposable native probe separately verified the actual stored Textual timer callback/call_later path. Existing ten DB-size tests and installed app workflows are parent-owned evidence, not independently rerun here.

Limits: these focused tests do not execute the whole app hook graph or Windows startup. They demonstrate the measured late-arrival lifecycle repair, not attribution or a performance fix for the Windows drain deadline. No repository edits made during review. Exact reviewed hashes are in /private/tmp/uat-db-status-lifetime-hashes.json.

## Runner-only follow-up

APPROVED. Exactly four literal selection additions: the eight-case DB-status module once in the shared full/support list, once in support-diagnostic, and that module plus the installed Library mounted node in existing native-close-diagnostic. Removing only these four AST entries reproduces HEAD exactly, so no execution code, selection names, arguments, budgets or prior nodes changed. The retained collection log actually contains **13 unique cases**, not 12: four existing TTS close cases (success/live_before/live_after/evidence), eight DB-status cases, one Library case. No duplicates or dropped prior cases. This corrects only the reported count. No further native tests were run. Runner SHA256: 04d9b572b3b12f2671aa6414d997d7f276cfa71809db9625a2efba471dd30b53.
