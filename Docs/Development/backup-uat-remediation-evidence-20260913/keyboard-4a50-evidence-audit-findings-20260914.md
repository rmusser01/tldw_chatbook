# Saved-evidence audit

All three cited archive hashes match. All 457 sealed file-record payload hashes, seven saved-note relationships, and 5,349 installed-file hashes pass. The prior final receipt records five completed sessions, all exit0; this audit did not query or drive the application.

The password claim was narrowed in the completion report. Neither of the two current retained password values appears in 540 saved action records or the saved launch environment. However, generic `uat.py type` would put its value in process arguments and the action log; no redaction is enforced. Complete historical process records and overwritten prior password values are unavailable. This is a limit on the assurance claim, not evidence that a password was exposed. No password value or digest is included in this audit.

An initial audit attempt assumed the after-Abort readback used the later `rows` key; its actual key is `notes`. That diagnostic stopped before any artifact or report write. The completed audit uses the recorded schema.
