# Windows 789960: sole mounted Library refusal

Read-only review of exact tested Git source and retained artifacts. Parent reports verified native42PASS and product187=186PASS/1FAIL, with all other previously failing cases passing. No repository/fixture edits, tests, network, Linux logs or live SQLite access.

**Exact cause:** the first preview inventory is complete. During capture's second inventory, _PreviewScope.sqlite_target raises preview_sqlite_changed at storage_admission.py1504. On exact789960 this is the INITIAL comparison of the opened main/WAL descriptor's(dev,ino,size,mtime_ns,ctime_ns) against the earlier _source_state tuple. The mismatch is detected before copying that member. This is different from562530 Console's line1530 post-copy count/descriptor check; the reason string alone obscures that distinction. Neither check includes atime.

The error makes primary Notes included→unavailable. Two excluded SQLite transient rows disappear; dependency/shared-group metadata changes, and seven dependent rows lose their Notes target. capture_service.py180 rediscovery therefore produces a different approved review digest and line185 raises CaptureReviewRequired(scope_changed). The JUnit terminal state is failed/capturing, review_required/scope_changed. No runtime-settlement or recovery-failure record exists: this is pre-maintenance review refusal, not an observed lease/owner-drain failure.

The failing member, changed field and writer remain unrecorded. One sampled worker is character_context._read_database_scope_metadata338→get_local_authority_id3742→transaction/get_connection. The method's actual SQL is a SELECT; the sample catches connection acquisition, not a proved write or checkpoint coincident with this mismatch. It does not justify naming that reader as the writer. Concurrent mutation, replacement or native metadata changes cannot be distinguished from the five-field inequality alone.

Admission records show2221/2221 completed groups,0 errors,6.439s inclusive total and0.474s maximum; tokens4745/4745,0 errors,12.206s inclusive total. This run does not reproduce562530's88s/46s group outliers. Its bounded loop sample is5.402s wall/4.359s thread CPU; it does not identify the source-file actor. No performance conclusion or speculative fix follows.

## Smallest additional evidence

Approve a test-only extension to the existing snapshot error wrapper, using the already-caught traceback and no new filesystem/native reads. Match the exact original sqlite_target code object and known raise site. For1504, inspect only suffix/expected/opened; for1530, only suffix/expected/observed/count. Emit fixed phase(opened_state/post_copy), member(main/wal), and changed field names or booleans(dev,ino,size,mtime_ns,ctime_ns; count only for post_copy). Validate exact primitive shapes: suffix is a built-in string equal to empty or -wal, tuples contain exactly five built-in integers, and opened is the native built-in os.stat_result. Retain the existing bounded failure record count and original exception/traceback under any extraction failure.

Do not infer phase from whether a local exists: opened/observed/count can survive a previous loop iteration. Unknown code site/type must yield unavailable metadata. Do not infer journal membership: this copying loop handles only main/WAL; the final _source_state comparison has no retained new tuple from which to extract a journal field without another read. Do not emit paths, raw tuple/stat values, contents, exception text or unrestricted locals. The resulting evidence narrows the mismatch and may still leave the writer unknown; it is not permission to skip/retry/weaken the stable-image guard.

Evidence root: /private/tmp/uat-windows-789960-support/backup-platform-windows-2022-py3.12-support-diagnostic-7899608e0674957e39bb99587477f5d534fade0b/test-logs/product-pytest/test_mounted_console_complete_2/home. Sole JUnit: /private/tmp/uat-windows-789960-support/junit-failures/344f17c907ca7f60.txt.

Design refinement approved after discussion: source-line/AST parsing is unnecessary. Require the actual terminal traceback frame itself to be original_snapshot.__code__ and the exact built-in ValueError/reason. Then accept only a comparison that proves the current failure: opened tuple !=expected means opened_state/before_copy; otherwise exact integer count>expected size means copy_growth; otherwise a valid observed tuple mismatch or count!=size means after_copy. Earlier iterations passed all three comparisons, so their locals cannot independently prove a current opening failure. A final-source-state failure leaves the last successful comparisons equal and must produce unavailable metadata. Do not select merely a matching ancestor frame, which could retain locals from a nested helper exception. Native two-member main→WAL cases should explicitly test stale observed/count presence. This refinement supersedes the earlier requirement to identify a known raise site while retaining all privacy/shape/error-preservation constraints.
