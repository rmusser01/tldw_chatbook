# Final Canvas backup maintenance independent review

APPROVED for the exact six product files and two test files recorded in /private/tmp/uat-canvas-policy-maintenance-final-hashes.json. Earlier hook-only finding and disposable failing-behavior proof remain retained in the prior implementation review; this approval applies to the updated transient distinction and downstream wiring.

Only exact RecoveryRequired with args=('storage_locally_paused',) returns runtime's private unavailable None. It never enables an effect, does not latch permanent disable, and does not erase an existing latch. Genuine false, other RecoveryRequired codes/subclasses and other ordinary errors remain fail-closed and sticky; BaseException control flow is not swallowed. Watcher retries transient refusal, ends naturally during coordinated closure, and apply detaches/cleans only a true permanent disabled/disposed state. Resume starts a fresh watcher and actual effect checks continue reading current checked configuration. No cached authority or previous enabled value grants access.

The lock-free policy read plus monotonic maintenance generation rejects reads crossing close, including full close/resume; existing final locked bind/materialize checks reject allocation while closed. This avoids the rejected native-lock-across-reader contract and preserves existing stale-read contention tests. Explicit disable and disposal still win. The exact original runtime hook is in the producer graph; runtime identity was already in owner snapshot.

Both downstream consumers consult the new optional disabled predicate only AFTER actual availability is false, solely to choose whether that denial is permanent. False remains denial regardless of predicate result. Missing predicate and predicate exceptions/non-false results preserve previous sticky behavior; CanvasToolProvider explicit enabled=False remains sticky. Runtime passes both callbacks to its ordinary ChatController only when providing the default enabled callback, preserving custom-reader semantics. ChatController forwards both to actual per-turn provider construction. Actual MessageController passes its matching runtime predicates to its retained action service. Ephemeral transcript action services do not retain state between projections. No permission/read check is skipped.

Accepted async revocation cleanup is retained and shielded from caller cancellation; only successfully completed cleanup tasks leave the set. Failed/canceled tasks prevent maintenance resume, and normal disposal awaits retained tasks. The watcher is not canceled by maintenance close, avoiding abandonment of detached gateway/authority cleanup. No native lock is held over config reads or cleanup awaits. Repeated direct apply calls cannot recover a latched state or create new native authority; dispose-while-paused cannot restart its watcher.

Independent verification:12native tests PASS13.03s, /private/tmp/uat-canvas-maintenance-independent-final.log; unique /private/tmp/uat-canvas-maintenance-independent-final-fixtures. Includes actual bound profile/config refusal, coordinated/uncoordinated pause controls, same retained message service and provider deny→resume, explicit disable persistence, external disable, disposal, accepted cleanup/canceled caller/canceled drain, and a thread policy read spanning both closed and resumed generations. The uncoordinated local-pause case proves the exact same refusal code as native gate contention; it is not a separate external-gate timing reproduction. Previous original native-pause tests change only the erroneous permanent-latch expectation, retaining effect refusal, binding/authority identity and existing error assertions. Parent owns15existing native-order tests,8kill-switch tests and final installed workflows; those are not independently rerun or claimed here.

No remaining actionable findings in this scoped review. No source edits or full-app runs by reviewer. Windows/full installed acceptance and static receipts remain parent-owned requirements; no startup-performance attribution.
