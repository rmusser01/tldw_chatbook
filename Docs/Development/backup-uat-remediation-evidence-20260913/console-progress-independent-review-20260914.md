# Console progress timer independent review

APPROVED for the reproduced missing-descendant timer failure. No actionable finding in the three-file scope. Exact hashes: /private/tmp/uat-console-progress-independent-hashes.json.

The product catches only NoMatches from the exact Button query and returns before reading progress when no destination exists. It re-queries on every tick, so a recreated button receives current state. State-provider, label setter, navigation callback and count comparison exceptions remain outside the catch. For a live button, label update, changed-count navigation refresh and final count assignment retain their existing sequence. No timers, worker cadence, permissions or backup deadlines change.

The new test uses an actual mounted ConsoleLeftRail, its real interval timer and a real descendant removal. It delivers the unchanged callback through the message pump in the missing-child interval, then recomposes and verifies that the current button receives the label. Thus it does not merely mock query failure or accept a dead UI. It models the demonstrated lifecycle gap; it does not claim to reproduce Windows F4 scheduling or its initial Pilot timeout. The runner adds only this module to the existing support-diagnostic selection.

Independent regression: 1 passed in2.46s, /private/tmp/uat-console-progress-independent.log. Additional whole existing progress-owner module:3 passed/4 failed in6.17s; all four failures are raw_source_selection_changed in config setup before the relevant app construction. An isolated pytest process replacing only the changed callback with its exact HEAD AST reproduces the same3 pass/4 fail and same refusal, /private/tmp/uat-console-progress-owner-baseline2.log; current log is /private/tmp/uat-console-progress-owner-independent.log. The first attempted baseline import happened before pytest setup and refused recovery_scope_uncertain; it is retained separately and was not counted as a comparison.

Author reports native lifecycle RED before the guard and5 focused passes afterward. This review independently confirms the new real-rail regression, not the author's entire selection. Local installed Settings acceptance is parent-owned and Windows repeat remains required. The initial Windows F4 WaitForScreenTimeout is not proven fixed by eliminating the later stored NoMatches exception. No product or test source files were edited by reviewer.

Follow-up narrow attribution: the author's related-rail selection was3 pass/1 fail. Independently restoring the exact HEAD progress callback in a disposable pytest process and running only `test_character_header_missing_during_prepare_does_not_latch_reconciliation` reproduces1 failure (51 deselected) in2.31s. It fails at `ConsoleHarness(_build_test_app())` with raw_source_selection_changed before rail construction. Evidence: /private/tmp/uat-console-progress-character-baseline.log. No broader sweep or product edits; timer approval remains unchanged.
