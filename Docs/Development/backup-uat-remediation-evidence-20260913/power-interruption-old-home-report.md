# Interruption run using the prior power HOME

TASK-32562. This run is preserved as **inconclusive for full interruption/retry acceptance**. The interrupted app used immutable tree 580c657bba5e05e185f8f62243c17f4c16eb28e6 but inherited the earlier 07b3zfn3 power HOME and its restored-profile catalog.

Actual keyboard capture reached Running: capturing; only verified PID 62808 was sent SIGKILL. No interrupted.tldw-backup.zip was published. All 1,800 external research file hashes, saved Notes rows (zero rows), and current config bytes matched the baseline immediately after interruption and after ordinary installed restart. Restart mounted Settings normally without cleanup or a recovery bypass.

The keyboard retry selected a new output, the same external research folder, and Partial acknowledgement. It failed with review_required: config_discovery_failure, dependency_unavailable, unavailable, unsupported. Its reviewed all-known-profiles selection includes the current profile and two earlier isolated restorations.

Separate read-only installed discovery identifies exactly the earlier restore-locations/one/config/config.toml as unsupported with discovery_failure. Its retained evaluation adapter raises eval_retained_binding_changed because bootstrap._binding no longer matches its retained activation binding. The prior actual power Open/Quit failure had already left that restored profile recovery_required. No evidence establishes that this interruption caused the binding failure; the first capture may already have been unable to complete. No catalog, profile, pending record, product file, or source fixture was repaired or removed.

A clean independent HOME is needed for a conclusive interruption→restart→successful retry test. Original before/after terminal frames, signal receipt, source hashes, and read-only discovery evidence remain under this artifact's evidence directory.
