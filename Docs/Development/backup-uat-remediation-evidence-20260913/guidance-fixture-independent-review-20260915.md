# Unmounted guidance fixture: independent implementation experiment

APPROVED as a correction to the fixture's scope, without a Windows timeout-cause or startup-performance claim. Exact probe/current-source hashes and results are in `/private/tmp/uat-guidance-unmounted-independent-review-hashes.json`. No repository file was edited.

Independent execution of both routes through the original shared `_run` helper, retaining its45-second child limit, passed2cases in12.13s. `/private/tmp/uat-guidance-unmounted-independent.log` and XML retain the result. Each real child reached all seven fixed labels through assertions_complete and shutdown_complete and the original completion sentinel. No app/test process remains running.

AST verification establishes that the actual assertion body, `check_display_pairs`, all observation helpers, all setup/config writes and final no-network assertion are identical to the repository script. The entire script changes only the `app.run_test` wrapper to `app._context`, the honest context_ready label replacing mounted, and the explicit finally that awaits real `app._shutdown()` and raises a retained `app._exception`. Neither route's early return skips finally. No fake app, config, runtime, store, session, provider-readiness result, native guard, geometry or timing result was introduced. The existing two output-consumer observations remain as before.

Native ownership remains real: both `app.py` and `config.py` call `storage_admission.admit_startup` at module entry before runtime/config initialization. The same real TldwCli constructor loads config and owns Notes/runtime/services; ChatScreen constructor wires actual controllers; the unchanged first screen store lookup preserves the empty-store assertion and real session/default convergence. Removing destination mounting does not remove this import-time native enrollment. Real Settings writes, fresh readback, explicit-session preservation, independent fresh helper calls, later fresh send guard, exact original exceptions and memo retirement all execute unchanged.

Cleanup uses the same TldwCli `_shutdown` entry point that run_test uses. It drains real app-owned lifecycles and reaches Textual/app teardown; the candidate does not copy the video fixture's partial owner/TTS close or suppress cancellation. Actual no-mount teardown completed on both native cases. No run_test-created app task exists to await in this fixture. This is not a full alternate lifecycle framework or proof for arbitrary application features.

Causal boundary: the preserved2031d Windows logs reached mounted and then timed out before assertions_complete. They do NOT prove mounting itself exhausted the budget, nor identify the pending body operation or writer. The new experiment therefore does not establish why that run failed or that a product defect was fixed. It is appropriate independently because these tests already constructed a second unmounted ChatScreen and replaced its final UI consumers; full Settings mount and unrelated background activity were incidental setup, not their assertion target. The new fixture explicitly tests real native config/session/readiness semantics without claiming mounted rendering, keyboard interaction or background-service integration. Existing installed mounted backup workflows and unchanged60-second startup tests retain those separate responsibilities. A native Windows run of the promoted fixture still needs its own result.
