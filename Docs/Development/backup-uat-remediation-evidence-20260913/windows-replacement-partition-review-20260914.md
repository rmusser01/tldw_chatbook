# Independent replacement partition review

APPROVED at the two hashes below. No actionable finding. This approves scheduling and exact coverage preservation, not native Windows completion or elapsed-time acceptance.

Reviewed diff against7899608e06: runner adds only the two literal selection tuples; workflow expands only the full/replacement-diagnostic replacement leg into those two selections. Independent AST comparison proves the runner is otherwise identical after removing those exact entries. Independent YAML comparison proves the workflow is otherwise identical after reversing the exact matrix substitution. Original full/aggregate replacement selections, dispatch gates, dependencies, native code, test assertions, error handling and artifact validation remain unchanged.

Root's actual network-blocked collection used fresh private environments and pytest collection hooks, returning exact node IDs with successful collection exits. I inspected the probe and independently checked the resulting receipt against current file hashes: aggregate31, Persona partition21, default/Evals partition10; no duplicate IDs, empty intersection and exact union. All19 Persona refusals remain together, preserving their module-scoped native fixture. No test/deadline/parameter was changed.

Each partition includes a test whose signature requests the existing native_package fixture: explicit F9 in Persona, default full/later lifecycle in default/Evals. Thus the existing installed-receipt requirement remains meaningful in both jobs; no source-only qualification exception was introduced. The runner's original failure on absent installed receipts remains intact.

Native qualification stays10min, product phase80min, job90min, with existing per-test/child deadlines unchanged. Support retains its separate120/135 limits. Unique matrix selection names flow through the existing job/artifact names; independent jobs have separate runner.temp roots. Fail-fast:false and always-upload preserve evidence from both. All other selection routes are byte-equivalent.

Verification performed independently: finite AST/YAML/receipt/signature probe passed; no full/native tests, network or repository edits. Root owns the existing CI contract, lint/security and subsequent Windows runs. Retained9aff exhaustion supports the partition but does not prove the future21-case leg fits; both legs still must finish with terminal results and verified receipts to claim31-case acceptance.

Hashes:
- Tests/Backup_Recovery/run_platform_product.py: ad93fe81385cbf01fda160cf8189d52f39c3860466047f26e9f5f7b88c9a24a6
- .github/workflows/test.yml: f53126f992cb70a96d273d7e782bbf6d43285ba9bdc8ca55d16227ff7c167fb6

Machine receipt: /private/tmp/uat-replacement-partition-independent-hashes.json. Root collection evidence: /private/tmp/uat-replacement-partition-verification.json and adjacent .log.
