# Startup config timing observer independent review

Runtime safety/privacy APPROVED for the three files in adjacent review-hashes.json. Independent focused tests:15PASS0.69s, /private/tmp/uat-startup-config-independent.log, unique /private/tmp/uat-startup-config-independent-fixtures. No app boots, source edits or native guard replacement.

One measurement-label clarification is needed before interpreting output: config_lock.wait_seconds measures generator PY_START→first PY_YIELD. Actual _config_write_lock includes checked config operation admission and settings-rebuild/config-file/interprocess lock setup/acquisition before yield. It is composite acquisition cost, not isolated mutex wait. hold_seconds spans yield→return/unwind and includes body plus lock/native-scope release/revalidation. Rename to acquisition/body-and-release or document these exact semantics next to fields. No contention or native-I/O attribution follows from these composites alone. Parent notified.

Monitoring claims only unused tool3; failed claim preserves an existing tool. After successful claim it enables only exact selected code objects locally, plus globally required PY_UNWIND. Unwind callback ignores unselected code before reading metadata or taking the observer lock; it receives but never examines the exception, and the transient frame is used only for id, never locals/args. Both local and global event registrations/callbacks are disabled before releasing ownership. Failed cleanup retains ownership for retry instead of falsely declaring a released slot. Callback ordinary errors retain only last4class names and preserve the original product return/error; monitoring does not replace cProfile's hook. Existing main finally still disables cProfile/stops sampler/emits final record and preserves original pytest outcomes. No deadlines changed.

Plugin attaches after collection without importing config, finds already loaded config, unwraps existing wrappers solely to select original code, and verifies each selected source filename against the selected source tree. It never substitutes product functions or changes their identities. Five fixed labels bound counts/slowest maps; active frames capped32, completed16, errors4. Frame identity separates concurrent and nested frames; config-lock yield records acquisition while retaining its frame until normal return or unwind. No values, path strings, SQL, configuration or exception messages are emitted. Labels and native thread IDs/durations only. Completed CPU deltas assume these selected synchronous config functions/contextmanager invocations remain on their executing thread, as the actual source does; no active cross-thread CPU estimate is fabricated.

The new tests exercise generator yield/return/unwind, worker exact return/error identity, metadata privacy, callback error isolation, occupied monitoring slot, cProfile coexistence, bounded completed/slowest retention, and failed cleanup ownership/retry. Existing tests preserve original pytest return/error through output/start failures. No broad performance claim is warranted: global unwind dispatch and observer locking introduce measurable instrumentation overhead, even though output is bounded. Parent's actual native bound/unbound public-write probes remain separate validation; no Windows performance result claimed here.

## Final label correction verified

Final APPROVED. The only delta from the independently tested15PASS bytes is acquisition_seconds/body_and_release_seconds field names, body_and_release active phase, corresponding test expectations, and three comments explicitly explaining composite costs. Reversing those exact string/comment changes reconstructs all three previously reviewed SHA256 values byte-for-byte. No execution, timing, ownership or cleanup change; no additional test/app run required. Updated exact hashes are in the existing hash receipt. Parent native bound/unbound actual public config-write/readback proof reports both PASS in /private/tmp/uat-startup-config-native-final.log; it predates labels only and is separately attributed. The first throwaway harness read-helper misuse was corrected and is not treated as a product failure.
