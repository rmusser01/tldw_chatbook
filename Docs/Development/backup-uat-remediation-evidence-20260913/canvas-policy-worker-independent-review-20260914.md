# Periodic Canvas policy worker independent review

Base: `27603954403930eb1ee9551995aef784dd97b9c8`. Scope: ConsoleRuntime, new native policy-worker tests, and three existing runner selection entries. No product edits or app boots by reviewer.

APPROVED on the inspected lifetime contract. Final independent six native cases passed in 5.93s (`/private/tmp/uat-policy-worker-independent-final.log`), with one existing Requests dependency warning. Initial five also passed in 4.78s. No remaining actionable finding.

The parent native RED establishes event-loop starvation: a real config writer's release is scheduled on that same loop, and the original synchronous watcher prevents it until the external watchdog releases the lock. This supports moving this particular periodic read. It does not establish a Windows startup speedup or resolve the complete 60-second startup failure.

The whole `_canvas_enabled` predicate is the correct offload boundary (console_runtime.py:2839,2996). Its pre/post maintenance-generation, disposal and latch checks remain around the original reader; temporary `storage_locally_paused` remains distinct from permanent disable. Offloading only `_read_canvas_enabled` would require recreating this protocol across an await. Existing direct authority paths already permit worker-side predicate invocation. Injected readers consequently must tolerate that existing worker usage; no thread-affine callback guarantee is present. Ordinary reader Exceptions still fail closed and BaseExceptions still propagate.

The single runtime-owned task is shielded from watcher cancellation. Repeated starts retain the sole active watcher; a replacement watcher can consume the same outstanding read. The field clears only after successful terminal observation. Close-admission still advances the generation before draining. Drain includes the actual reader; resume refuses it while unfinished (2902–2935). The worker therefore cannot be hidden by cancellation of its waiter. Disposal shields and retains the reader before subsequent owner teardown (4526–4528), including when the disposing caller is cancelled. It is not added to the later generic cancellation list, which would incorrectly equate cancellation of an executor waiter with worker completion.

The false-policy result sets the existing monotonic latch before `apply_canvas_policy`; its synchronous predicate then short-circuits, so this path does not perform a second native config read on the loop. Action-time permission checks, .25-second polling interval, idle external-disable observation, cleanup ownership, and restart-required latch remain intact. This retains the explicit no-preview watcher contract tested at Tests/UI/test_console_runtime_ownership.py:2294.

The six new tests exercise real bound config and native writer ownership: UI release, maintenance retention, cancelled watcher, cancelled drain, and cancelled disposal, with raw-operation retirement and resumed authority checks. The final separate restart case cancels the watcher with admission open, starts twice, verifies exact retained reader identity and one native invocation, releases the real writer, and proves a subsequent periodic read occurs. This closes the initial coverage gap without changing product code. All tests retain native checks and verify no raw operations remain. No broader suite or diagnostic mechanism was needed.

Runner additions retain all prior nodes and only add this module to full, support-diagnostic and native-close selections. Windows acceptance remains pending. Hashes are in `/private/tmp/uat-canvas-policy-worker-independent-hashes.json`.

Parent-reported supplementary checks (not rerun by reviewer): 34 related Canvas cases passed in 30.72s; Ruff baseline/current 31 product findings, zero runner/new-test findings; Bandit unchanged three product findings. These are distinct from the independent six-case receipt. Installed route and Windows startup acceptance remain separate pending work.
