# Independent retained-failure diagnostics review

APPROVED, no actionable findings. Read-only five-file delta against pushed db4247b8db. Exact scope/hashes are /private/tmp/uat-failure-diagnostics-independent-hashes.json. Independent focused tests: 30 passed in 0.80s, /private/tmp/uat-failure-diagnostics-independent.log. No heavyweight/native application workflow was repeated.

Runtime: original hook sequence, arguments, calls, return and exception behavior remain. Observer snapshots at most 64 original list/tuple hooks before invocation, searches at most 64 traceback frames for exact original_stage code identity, reads only that frame's hook, and proves membership with is (no equality callbacks). failed_hook is explicitly a hook label, not a claim which close/drain phase threw. Its qualified name is capped160; elapsed time is monotonic. Metadata exceptions/cancellation are recorded for the existing stop-time diagnostic failure contract while the original stage exception is re-raised unchanged. Objects/frames/owner data are never persisted. Tests exercise the real existing _settle_stage with second-hook false result, throwing close/cancelled drain, 65th-hook omission, no extra equality, and failing metadata.

Later-child: existing asyncio.run is surrounded only by an error handler that records bounded existing _error_metadata then re-raises. Parent records launch/timeout errors before re-raising and numeric nonzero exit before the unchanged assertion. Record helper catches its own diagnostic failures, including cancellation, so these cannot replace the underlying error. Native subprocess900/300 and all UI/service timeouts, network checks and behavior assertions remain byte-for-byte unchanged. No commands, passwords, exception messages, locals or captured-output contents enter the new metadata. Basename/function/line bounds remain those of existing _error_metadata. Immediate *.json.log files pass the existing sanitized collector test; no global plugin or suite-finalization dependency is added.

Limits: a parent/test process killed while still executing cannot manufacture a final error record. These changes preserve already-observed exceptions/nonzero exits before later suite timeout; they do not establish the unresolved native Library hook or default later failure cause until rerun. No product, authority, callback wrapping, permission, deadline or failure-normalization changes are present. Runner final addition reviewed: only the two diagnostic test modules were added once each to existing _PRODUCT_TESTS and _SUPPORT_DIAGNOSTIC_TESTS; no suite mode, collection filters, timeout or execution behavior changed. Both entries resolve to the independently passing tests. Parent reports focused collection130 in0.46s. Final test comment-only nosec clarification reviewed; executable behavior unchanged. Hash receipt refreshed for final six-file scope.

Final B404 annotation reviewed: subprocess is used only for synthetic TimeoutExpired/CompletedProcess values in the new unit test; its workflow launch is monkeypatched. The comment/annotation changes no executable statements. Updated final test hash 4a036d7200dc0d8e2013f50e1c713fb1203e21af9510481e352dfbee43b61520.
