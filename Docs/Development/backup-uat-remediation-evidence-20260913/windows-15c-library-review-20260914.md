# Windows15c Library causal review

Verdict: the retained evidence shows an unfinished native capture, already inside its exclusive maintenance inventory discovery, when the 420-second subprocess ceiling expired. It does not show a completed backup followed solely by slow ordinary shutdown. No specific new product exception is established; the exact terminal child exception and coroutine awaiting cleanup are not retained.

Reviewed source: `15c1541f77780c7897a11ba136d738bad679e9b4`; existing run `34881797168`. Parent verified artifact and installed-source receipts. This review used only retained local evidence and exact Git source; no new run, product edit, or limit change.

## Observations

- Exact node `test_mounted_console_complete_capture_and_resumed_writes[library]`: JUnit duration420.224s; parent `subprocess.TimeoutExpired`, actual timeout420. The child has no `mounted-child-failure.json.log`; JUnit retains no separate child stdout/system-out for this node. Embedded script markers in the exception command are not evidence that those markers executed.
- Entry: constructor complete22.719s, mounted context yielded52.079s. Initial screen entry succeeded.
- Capture-review log has two completed observations: initial preview and pre-maintenance rediscovery. Both scope_changed=false, empty deltas and unavailable-dependency lists, no truncation. There is no completed third, held-discovery observation and no snapshot-failure diagnostic event.
- Runtime settlement: five stage-begin events followed by cache-retirement before/after and resume. No failed_hook, stage exception, pending operation or raw operation is recorded. Eight live leases fall to the startup lease after retirement and then zero at resume; Notes cached handle was registered/alive, exact and not in a transaction before retirement. Thus the observed local retirement succeeded.
- `runtime_resume` reports startup_retired=true and `unrecognized_runtime_issue`. This is NOT evidence of a runtime error: the observer maps any non-whitelisted value, including the normal None, to that label. `monitor_app` always enters resume in finally after successful startup retirement; it intentionally waits for the exclusive capturer before reopening producers.
- All four rolling stack snapshots retain worker5860 inside `capture_service.capture:221 -> _discover_capture_inventory:1640 -> inventory.discover:581`. Its observed leaves advance through private SQLite verification, file inventory, capture-path ownership checks, and Windows ACL/SID verification. The last snapshot is `recovery_core.discover:62 -> connect_private_sqlite -> verify_trusted_directory -> Windows security/sid_string`. This is concrete ongoing held discovery, not a `RecoveryService.close` stack and not a demonstrated stationary lock cycle.
- Readmission thread5500 waits at `storage_admission._reacquire_paused_startup:452 -> Admission.normal -> _admit:577 -> Windows flock` in all four snapshots. Source explicitly requires waiting behind the native exclusive gate. The capture worker is still executing under that gate; its stack does not show waiting on readmission. No reversal/cycle is proven.
- Worker576 is in `RecoveryService.wait -> future.result` in the first two snapshots and ordinary executor wait thereafter. The test's inner service.wait limit is240s; its finally prints status, then exceptional unwinding must leave the mounted app before reaching the outer service.close. The transition is compatible with inner timeout and retained monitor cleanup while capture remains busy, but missing stdout/coroutine metadata prevents claiming the precise unwind step. Later successful capture/readback/resumed-write assertions are unproven.

## Cost evidence and limits

The bounded admission observer reports2,191 completed `_groups` calls and4,966 `_tokens` calls, zero recorded errors. Inclusive wall totals are38.228s and88.778s respectively; these overlap and must not be added. The slowest sampled group has20 roots, 20 token calls,31.8646s wall,0.5625s thread CPU and32.4063s process CPU; almost all its inclusive wall lies inside `_tokens`. This does not isolate a slow pure lexical overlap loop or prove that one thread's computation consumed31.9 CPU seconds. The loop profile is instrumented6.211s elapsed/4.031s main-thread CPU; three Canvas-policy callbacks consume3.703s inclusive. None of these instrumented samples is a normal-runtime benchmark or enough to attribute the full420s to one operation.

## Distinction from prior failures / next evidence

The prior65a Scheduler false-drain and789 changing-snapshot cases are distinct failure boundaries. Here retirement completes and both completed inventories agree; neither earlier mechanism appears in this retained receipt. Do not apply either earlier attribution to this run.

The smallest remaining diagnostic gap is fixed phase/timing/status receipts at service.wait expiry, before/after mounted-context exit, and before/after service.close, plus bounded per-owner held-discovery entry/exit timings if the existing observer can supply them without extra source reads. The existing stack evidence already identifies the unfinished operation; a timeout increase, skipped native validation, or speculative guard/cache change is not justified by this review. Native discovery correctness is not disproven, but this test has not established completion within its current bound.

Artifacts: Library home is `/private/tmp/uat-windows-15c-support/backup-platform-windows-2022-py3.12-support-diagnostic-15c1541f77780c7897a11ba136d738bad679e9b4/test-logs/product-pytest/test_mounted_console_complete_2/home`; root and exact hashes are recorded below. JUnit failure is `/private/tmp/uat-windows-15c-support/junit-failures/344f17c907ca7f60.txt`.
