# Independent Canvas checkout preservation review

APPROVED. Two-file scope only: .gitattributes and appended native regression in Tests/Canvas/test_profiles.py. Frozen hashes match /private/tmp/uat-canvas-attributes-hashes.json. Independently derived the actual catalog-selected two-manifest output/library union and verified exact equality to all ten new -text paths. No wildcard, omitted closure member, extra protected file, asset-byte edit, product/profile change or existing age_worker attribute change.

The regression uses actual repository resources and actual git checkout-index under core.autocrlf=true in a disposable repository. It stages with autocrlf=false, removes working resource files before checkout, and invokes the unchanged strict loader for both catalog profiles, checking executable selection and retained runtime assets. Its derived closure avoids duplicating the ten-rule list. Only resource location is redirected; no hashes, manifests or capabilities are mocked. Local Git config is isolated from inherited GIT variables/global/system files; no network or source-repository mutation occurs. Catalog conversion remains intentionally permitted because it is parsed rather than byte-hash checked.

Author evidence: true checkout RED, then 44 passing profile/stale-policy cases. Parent independently runs the same focused selection, so this review did not rebuild or repeat tests. Native Windows acceptance of the committed attributes remains pending; existing receipts conclusively showed the CRLF corruption this correction addresses. No actionable findings.
