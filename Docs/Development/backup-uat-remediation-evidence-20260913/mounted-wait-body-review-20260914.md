# Mounted wait/body diagnostic final independent review

APPROVED, no actionable findings in final two-file diff against b416cf4c1a. The added immediate service.wait handler resolves the specific observation gap identified in the previous review; it records the exception before observer/status cleanup and Textual mounted-context exit. Product sources are unchanged.

Independent validation: all43 cases in test_later_failure_diagnostics.py passed in0.83s, exit0, /private/tmp/uat-mounted-wait-independent.log; explicit disposable basetemp /private/tmp/uat-mounted-wait-independent-fixtures. One dependency warning, no failures. Actual child compiles. Complete parent/child AST equivalence after removing precisely the two added diagnostic handlers and two fixed phase calls proves all earlier calls, assertions, await boundaries and deadlines unchanged. Diff check clean. No full app test was run by this reviewer.

The new wait test extracts the actual nested try, including its unchanged real asyncio.to_thread call, added handler, and observer/status finally. It substitutes the service only to cause a controlled TimeoutError or cancellation and records the actual exception reaching the boundary before forwarding it to the real record_failure. The stop callback observes the on-disk metadata before cleanup, proving order rather than merely checking a later file. The final exception is the exact boundary object, unless status cleanup raises, in which case existing cleanup precedence remains unchanged. Using the boundary object correctly accommodates asyncio Future reconstruction of a worker TimeoutError; the handler and cleanup under review are not replaced. The prior five body scenarios still exercise actual extracted handler/phase/finally with real blocking worker cleanup, success, cancellation, close failure and diagnostic OSError.

Both failure files use the existing bounded class/native-code/code-frame metadata helper, without exception text, source contents or locals. The separate mounted-wait-failure.json.log preserves originating wait evidence even if subsequent cleanup causes the outer body record to describe a different error. Existing collector tests prove both fixed .json.log names are retained. The original awaited service.close still runs once; begin/complete phase labels and elapsed values are fixed, with existing OSError-tolerant phase writes. No error is cleared, no native operation is canceled differently, and no retry or timeout is added.

Limits: this directly observes exceptions from service.wait, including the suspected240-second wait expiry. It does not establish that15c actually expired there; native Windows rerun is still required. Exceptions elsewhere inside app.run_test can still block in Textual __aexit__ before reaching the outer body handler. This is deliberately a narrow wait observation, not arbitrary-body tracing. Parent reports the earlier pre-service-close version's actual installed Library run passed in60.50s (/private/tmp/uat-mounted-body-installed.log); that is not a final-byte native execution claim. The final incremental wait branch is independently exercised by the43 focused tests.

Preserved prior evidence: initial body RED /private/tmp/uat-mounted-body-red.log; prior independent39-pass review /private/tmp/uat-mounted-body-independent.log; wait RED /private/tmp/uat-mounted-wait-red.log. Parent's first wait GREEN had a test-only mistaken pre-Future identity assumption, corrected to verify actual boundary identity; no product regression is inferred.

Exact reviewed SHA256:
- `Tests/Backup_Recovery/test_mounted_console_backup.py`: `db86c4c42d4612fc5e9681e8f74b77e17f77210c649fe799b820b13fa0cafdcc`
- `Tests/Backup_Recovery/test_later_failure_diagnostics.py`: `5ae7684ed70989891b37a0e6b258a16de2e1381641d6be78100c4f1841ac094f`
