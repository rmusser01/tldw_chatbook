# Native config operation lock-order implementation review

APPROVED at the exact three hashes in /private/tmp/uat-config-native-order-hashes.json, independently rehashed: config_participants37f9e4fe, config99aca341, test845651a0. No actionable findings.

The outer checked operation now acquires the existing REBUILD then FILE RLocks through the unchanged bounded acquisition/check loop. ExitStack records only successful lock acquisitions, releases FILE then REBUILD, and releases a partial REBUILD acquisition if FILE waiting is refused. The existing nested operation, native raw checks, cache/failure handling and repository operation suspension/restoration are unchanged. The raw-editor snapshot now enters the same checked operation before resolving its path and retains its nested _config_write_lock/target validation, removing the independently identified FILE-first edge without a new guard path or decorator.

Native regressions use the real public runtime_capture_policy override-cache miss and both real runtime/serialized snapshot functions; only lock getter entry is observed, returning the exact native RLock. Existing source-byte invariants remain. Both lock waits refuse a native pause without entering the body, leaking locks/acquisitions or setting the persistence failure flag. Real repository operation nesting verifies exact exception identity, failure flag semantics, lock retirement and restored core operation after success/failure.

Independent6PASS4.36s: /private/tmp/uat-config-native-order-independent.log. The prior unmodified native cycle is preserved separately (/private/tmp/uat-config-rebuild-file-order-review.log). Parent owns combined Canvas/Console and broader config regression/static validation, so this verdict covers the bounded reviewed implementation and independent six cases rather than claiming those pending suites passed. No platform timeout/performance claim; native Windows acceptance remains separate on the next integrated revision.
