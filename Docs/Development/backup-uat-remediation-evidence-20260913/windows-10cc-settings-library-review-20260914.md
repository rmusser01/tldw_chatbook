# Windows 10cc Settings/Library independent review

Run34889283757, exact source `10cc230016610db91771b68b82945667084d9a0b`: **42 native passed;293 product outcomes,291 passed/2 failed**. Settings failed271.562s; Library passed161.441s; Console189.828s is outside this review. Independent rehash verified all **165 artifact SHA256 entries**. Existing source/Git verification records two installed2867-file receipts with2475 matching Python Git blobs each, no mismatches.

Artifact root: `/private/tmp/uat-windows-10cc-support/backup-platform-windows-2022-py3.12-support-diagnostic-10cc230016610db91771b68b82945667084d9a0b`. Relevant homes are `test-logs/product-pytest/test_mounted_console_complete_1/home` (Settings) and `_2/home` (Library). I inspected exact tested source; Scheduler loop and storage admission match current files byte-for-byte. No tests, product/fixture edits, remote actions or Linux reads were performed.

## Settings: exact failed gate, unfinished workers

F4 completed, preview was Complete, and capture rediscovery produced a second unchanged inventory with no unavailable dependencies. The real backup then failed before entering its held capture body. `mounted-recovery-failures.log` preserves `AdmissionTimeout`: `capture_service.capture:220 → admission._admit:600 → _lock:173 → _check:188`, while acquiring an exclusive namespace lease. Body and final child observers both retain the result assertion (`<string>:83`), not an unrelated WorkerFailed. Service close returned at268.156s from the recorded entry clock.

New causal evidence beyond15c: after four settlement-stage begins, `mounted-runtime-settlement.log` identifies **`SchedulerLoop._maintenance_drain`**, elapsed **30.0s**, with `runtime_work_not_settled`. The producer-settle failure propagates, runtime resumes with `startup_retired=false`, and capture subsequently has the lease timeout. Cache-retirement events were never reached. Initial stage snapshots had ten leases, one startup hold and zero active/pending operations; those initial counts must not be treated as terminal state. Failure-time storage observation was **unavailable**, not zero.

The retained thread evidence narrows unfinished work:

- Thread5496: `_record_heartbeat:568 → default_heartbeat_path:165 → guarded config operation → native acquisition`. Its samples progress through bootstrap permission, same-root initializer waiting, then native qualification. At failed drain its scope has1.718s active time, after an initialization-body maximum18.453s and authority-open maximum12.968s; these inclusive phases must not be added.
- Thread4616: `console_trace_maintenance.run_batch:753 → Notes transaction entry → native repository/connection acquisition`. One sample waits in `storage_admission.initializing:244`, another progresses through scope observation, and another reaches native connection acquisition. At failure its current initializer-entry wait is28.109s.

Exact Scheduler `_offload:233–247` retains the real thread task until completion; `_maintenance_drain:213–225` waits for task/offload sets. Tick finalization deliberately awaits `_record_heartbeat:526–549`, whose default path resolves config. Trace maintenance begins a real immediate Notes transaction before its batch body. Storage `initializing:215–251` serializes same-root initialization; line244 is its condition wait, not a pause-check result. These frames establish concurrent admitted heartbeat/config and trace-maintenance/Notes work at the failed Scheduler drain. They do **not** identify the initializer leader at the exact failure, prove these two threads are mutual waiters, or establish a lock cycle/leaked cache. Both make progress across samples.

The slowest completed group (`_admit:541`,21 roots) took66.644039s wall,0.21875s own thread CPU and67.234375s process CPU; its21 token calls account for66.641936s inclusive. All2103 measured grouping calls completed with zero errors. This is not66 seconds of grouping-algorithm CPU or proof of a particular native call blocking for that interval. The actual exclusive-lease failure is separately captured above.

Compared with15c Settings: both reach Complete preview/capture rediscovery and fail exclusive admission;15c lacked a runtime settlement record. The new exact Scheduler failure is additional evidence, not retroactive proof that15c failed at that same hook.

## Library: actual mounted lifecycle passed

JUnit's passing result covers the unchanged installed-driver assertions, not just navigation: construct/mount and boot-gate readiness; saved note; navigation to actual `LibraryScreen(mode='notes')` and `_library_loaded`; F4 Settings; Complete preview; successful complete backup; native pause/runtime-maintenance release; a new resumed note write; live readback of both notes; acquired archive with `consistency='coherent'`; archived Notes bytes contain the pre-capture note and exclude the post-capture note; service close, clean app exit and no blocked network attempts. The driver asserts imported package identity against the installed wheel.

Its five capture inventories are unchanged and have no unavailable dependencies. Five settlement stages proceed; cache retirement changes live leases **8→1**, leaving only startup, then runtime resumes with **startup_retired=true,0 leases/0 startup/0 operations/0 pending**. No runtime stage, recovery or body/child failure record is present. The resume observer's `issue='unrecognized_runtime_issue'` is an allowlist fallback label; it is not a recorded exception or evidence that this passing capture failed. The successful source assertions and completed retirement provide the interpretation.

Library reached mounted yield55.203s and service close157.906s, with JUnit total161.441s. Its slowest completed group was0.389603s (20 roots); all2157 groups completed without errors. This success preserves a complete native mounted acceptance result for this attempt, not a guarantee that prior Library failures or the separate Settings race are fixed.

## Minimum justified next causal test

No production change follows solely from these artifacts. The existing bounded Scheduler/native-drain reproduction should, if it does not already, include the **actual trace-maintenance immediate Notes transaction alongside the real heartbeat default-config path**, with event gates only at native initialization entry/exit. Identify the real leader/contender and retain task ownership, trigger existing close/drain, then release native work within the unchanged deadline and check real completion/retirement/resume. A failing supported interleaving can justify a specific correction; holding an artificial event past the deadline only demonstrates expected drain refusal. Do not replace the default reader, precompute/cache the heartbeat path, drop an unfinished task, suppress a native check, add automatic retries or increase deadlines. This is a proposed focused reproduction, not a claim it has already reproduced this Windows failure.
