# Nested Persona pack later rollback

**Final acceptance passed:** `/private/tmp/uat-persona-eval-final-lifecycle.log`, 1 passed in 254.31 seconds. The subsequent Evals correction and full ordinary-reopen/next-backup proof are described below and in `canonical-eval-retention-report.md`. All four implementation/test files are frozen in `nested-persona-final-hashes.json`; no commits or remote actions by this agent.

TASK-32562; owned scope: `later_rollback.py` and new `test_created_persona_subtree_rollback.py`. No immutable keyboard fixture changes.

The real default-profile failure was `local_snapshot_absence_unclassified`: a committed new nested `persona.assets` pack has a source-profile logical ID while current discovery uses the destination-profile ID. Existing handling intentionally covered only inactive top-level roots. The correction authenticates the nested source/current native graph and maps IDs without rewriting current root/dependency records. A separate transient proof permits only already-existing semantic ancestor directory metadata; actual ancestor publication and subtree overlap still refuse.

## Evidence and current checkpoint

- `/private/tmp/uat-nested-persona-red2.log`: genuine native RED, one failure in 109.17 seconds after Complete capture, credential-review stop/untouched Abort, replacement, and ordinary mounted new-note save. Later preview failed `local_snapshot_absence_unclassified`.
- `/private/tmp/uat-nested-persona-green.log`: first product attempt reached the existing ancestor-overlap refusal. Native plan evidence proved canonical root and packs parent are retained directory metadata, not publication units. Root and independent reviewer approved the finite ancestor proof.
- `/private/tmp/uat-nested-persona-lifecycle-green.log`: second product attempt passed genuine later preview and all nine topology negatives; execution refused `target_changed`. Overall 9 passed, 1 failed in 261.32 seconds.
- Exact repeated-preview comparison found **only `target_fingerprint` differed**; target graph and every other field matched. Existing current ChaChaNotes `-shm` mtime/ctime changed; bytes, inode, mode and size remained stable. A transparent native `_Assets._references` observer proved the added full owner discovery caused this through its live read-only SQLite query. Logs `capture-plan-diff.log`, `capture-fingerprint-diff.log`, and `capture-sqlite-observation.log` under the preserved lifecycle fixture's target directory.
- Product edits paused for this third correction boundary. Existing `_preview_reads` uses a verified private main/WAL copy and refuses hot journals; existing held `capture_scope` provides the equivalent. Root/reviewer are assessing reuse, with no live immutable-WAL bypass or fingerprint exemption.
- `/private/tmp/uat-persona-overlap-negatives.log`: nine direct overlap negatives passed. Ancestor-file produced the correct native `RecoveryRequired(storage_admission_unavailable)` rather than the test's expected ValueError/OSError; expectation now accepts only that exact case/code. Direct rerun in the same preserved native fixture passed; `capture-ancestor-file.log` ends `NATIVE_PERSONA_OVERLAP_REFUSED`.

The extra fixture attempt to publish another Persona version was removed after source tracing showed it creates a new pack and used a source form rejected by the installed bound-source contract. It was not a correction to the backup product. The intended ordinary post-replacement note write remains. Final lifecycle assertions now require the encrypted next safety copy to contain that exact note and incoming pack bytes, and the old target pack's exact original bytes/graph to be restored.

At that intermediate checkpoint, Windows observation budgets reused existing seed/F9/open/later ceilings; product deadlines were unchanged. The enclosing lifecycle bound was 3,600 seconds on Windows and 720 elsewhere. Full lifecycle acceptance was still pending; final results and the added reopen/capture phase bounds appear below.

## Existing-mechanism reassessment

Root and independent reviewer approved retaining full native `owner.discover`, with the existing `_preview_reads` verified private main/WAL image for non-held review and the existing capture scope for held review. No storage-admission change, live `immutable=1`, timestamp exemption, or fingerprint relaxation was made. The exact finite read set is the canonical selected config, canonical core database, and included same-root Persona files; config is a dependency of both root/core. Native binding and capture identity validation remain mandatory.

`/private/tmp/uat-persona-live-source-red.log` reproduced the explicit new assertion `native review mutated live SQLite source`. `/private/tmp/uat-persona-live-source-green.log` then passed two equal genuine previews, checking source main/WAL/SHM bytes, device, inode, size, mode, mtime and ctime before/after (atime intentionally unasserted). The first held run exposed the omitted config read (`capture_source_outside_scope`), preserved at `/private/tmp/uat-persona-final-native.log`; root/reviewer approved adding this exact dependency, with no generic fallback. All retained helper reads were enumerated before rerun.

Fresh full lifecycle run is `/private/tmp/uat-persona-complete-lifecycle.log`. Existing builtin later execution and changed-file refusal: **2 passed in 69.80 seconds**, `/private/tmp/uat-persona-existing-later.log`. Ruff check/format and scoped diff check pass; product Bandit has zero findings. The new test file reports only three B101 pytest assertions. Current candidate hashes: product `e1cdbbc8bdf90801df15b47ab81ad5dabdfd2bcf578d2796c00fd68bbc5586f2`, test `02f591943ab6033c98c42fb8dc15f01b550ba3687e8df7bfd742b02c90b132da`.

## Final reviewed outcome

- Nineteen topology/overlap negatives passed in `/private/tmp/uat-persona-final-native.log` (that run preserves its one earlier held-config omission failure). The corrected genuine lifecycle separately passed on the final assembled product.
- Exact source-stable main/WAL/SHM proof holds across ordinary and held re-review. Actual later rollback committed; its verified encrypted safety copy contains the post-replacement note and current incoming pack bytes.
- Original target-pack bytes and its exact native logical IDs/dependency graph were restored; the canonical semantic root and packs parent survive. The original target pack is **restored**, not claimed to have survived forward replacement unchanged.
- Aggregate Complete inventory passed after the separate six-line canonical Evals retained-extra correction. Fresh ordinary mounted restart then read the original target note, found neither incoming-source note nor post-replacement note, and exited normally. A fresh child produced another actual Complete backup.
- Final lifecycle: **1 passed in 254.31 seconds**, `/private/tmp/uat-persona-eval-final-lifecycle.log`; individual terminal markers are in its target child logs. No deadline adjustment corrected a product failure; existing per-phase observation ceilings remained, with enclosing test bounds increased only to cover the added required reopen/capture phases (Windows 4,200, others 900).
- Independent Persona review approved frozen product `e1cdbbc8…`, with six independent native negatives and final lifecycle assertions reviewed; `/private/tmp/uat-persona-independent-review.md`. Independent Evals review approved exact product/test hashes with 9 passes; `/private/tmp/uat-eval-canonical-independent-review.md`.
- Final test hash is `679c1d533a4d3dfc70e67e41cf4c223ad49b88da1f6e4ed0803b95088b0f1e57`. `nested-persona-final-hashes.json` is the authoritative four-file freeze receipt. Product Bandit zero; new test findings are pytest B101 only; Evals' three Ruff warnings are byte-equivalent baseline findings. No unresolved finding remains in these bounded corrections. Native Windows/Linux final-head acceptance is controller-owned and is not claimed by this macOS evidence.
