# Windows startup 043d independent review

**Acceptance remains failed.** Run34883779948/job104109237775 checked out exact `043d2cb639eb6ca4a06d569425850dfff76de2d8` (log lines36/204). The completed job log contains31 passing non-UI cases (10.09s), then four separately collected full-app GGUF cases, each timing out under the unchanged60s limit. Primary step exits1 at1905. The comparison executes pinned dev4631b60f and candidate using the same observer/node: dev **1 passed52.89s**, actual test call38.19s (2106–2109); candidate times out and the diagnostic shell exits1 (2406–2407). A continued diagnostic step does not mean the candidate passed.

Evidence: `/private/tmp/uat-windows-startup-043d-job.log`; bounded parsed eight-profile receipt `/private/tmp/uat-windows-startup-043d-profile-summary.json`, including log SHA256. File includes post-job cleanup, not a partial download. All eight `source_matches` values are true and all observer-error lists empty. This validates imported package location for source-checkout execution; it is not an installed-wheel hash receipt. Observer source is byte-identical to86ad. The inspected ChatScreen and ConsoleRuntime source files are also byte-identical to86ad.

## Actual terminal work

| Full-app case | Main-thread path at its60s timeout |
|---|---|
| claim authority/recompose | Controller/core-state sync → provider selection → ensure session/store → Canvas binding gate → native config acquisition/fresh bootstrap observation (821–873). |
| external-copy keyboard/geometry | Post-attach reconciliation → roleplay/identity refresh → settings summary → provider selection → readiness `load_settings` → native qualification (1160–1204). |
| provider width `[llamacpp-llama-cpp]` | Same post-attach settings/readiness path → native qualification/stat/security (1489–1535). |
| provider width `[llamafile-llamafile]` | Post-attach identity/core-state sync → ensure store → Canvas gate → config operation lock acquisition (1876–1902). |

These snapshots show startup/post-attach synchronous policy/config work, not a measured60-second individual call or proven deadlock. The diagnostic candidate separately remains inside initial composition at cutoff: `compose_content → summary → readiness → ensure_session/store → bind_canvas_native_view → Canvas getter → config/native admission` (2354–2404). Do not conflate profiled initial-compose progress with the primary unprofiled post-attach snapshots. Some displayed source statements in sampled native frames are misaligned with adjacent frame names, so function/stack identity is stronger than the displayed statement.

The primary constructors completed in7.582/7.254/7.501/7.543s. The profiled constructor's own completed interval is **15.724s candidate vs0.548s pinned dev**. This is a comparable completed lifecycle boundary under the same observer, showing substantial candidate startup overhead, but the constructors differ in implemented work. It does not isolate a particular correction or predict unprofiled savings. Both final profile samples describe different lifecycle endpoints: dev completed the test; candidate did not.

## Bounded profile findings

Samples: dev13.422/31.313/49.531/55.938s; candidate14.141/28.672/44.469/60.203s. Last candidate records323 `acquire_storage` calls/28.106147s inclusive,411 raw-scope calls/25.795269s,98 CLI-load calls/21.927626s,1647 registry calls/14.659042s,168881 native opens/4.700787s self, and94449 security calls/3.649876s self. The caller chain is now explicitly retained:29 `_ensure_console_chat_store` calls/12.099595s inclusive and29 Canvas view binds/12.098280s;33 live Canvas policy reads/14.902255s inclusive. This is stronger evidence for the actual binding→getter chain than guessing from generic acquisition totals.

These are cumulative main-thread cProfile aggregates, not additive durations. Generator/coroutine resume counts are not invocation counts: `compose_content` reported10 profiler entries cannot establish10 distinct initial compositions. Native acquisition/frame timing includes nested work; live entries may not yet be fully accounted. Earlier86ad260 acquisitions/34.114s and17 Canvas reads/13.846s cannot be treated as an improvement or regression against current323/28.106s and33/14.902s: endpoints represent unequal unfinished workloads, despite the observer being unchanged. Missing entries outside the top20 are unknown, not zero. No Windows per-method grouping speedup is established.

## Correction to the preceding lifetime review

My `/private/tmp/uat-startup-settings-lifetime-review.md` incorrectly said cached `load_settings()` avoids native acquisition. **That statement is withdrawn.** Exact source `config.py:1747` decorates `load_settings` with `@_config_participants.guarded`; its body’s cache-hit return occurs only after wrapper entry. `config_participants.py:400–419` enters the real config operation first. The current primary readiness timeout confirms that wrapper is reached from `chat_screen.py:7615`. Repeated cached calls can therefore repeat acquisition unless an existing same-config operation is already held.

This corrects one premise but does not establish an adjacent getter-only group in the actual terminal chain. `_build_console_provider_selection` already enters a per-derivation memo; `:9613` resolves app config, then `:9614` obtains a live store, `:9616` ensures session settings, and the nested projection resolves the memoized readiness mapping. `_ensure_console_chat_store` may construct/recover persistence or update workspace context and bind Canvas callbacks. Session ensure may create or update session state. Combining these paths would cover real owner/session/binding effects, not merely consecutive checked getters. The one default Canvas gate itself still contains one config load. No removal of a repeated check across effect boundaries is approved.

There is one distinct nearby *candidate for separate measurement*, not a demonstrated startup fix: `ConsoleSessionController._default_console_session_settings:3490` calls effective provider/model resolution (which reaches readiness config) immediately followed by readiness config again, before the default-settings parser. With ordinary callbacks, outside an existing derivation memo, those can be two checked reads. But the current measured initial path calls `_ensure_active_console_session_settings → _blank_console_session_settings`, which projects the app-owned mapping; the default helper is not identified in the terminal stack or bounded profile. Do not attribute the observed29 binds to that two-read helper or invent a repeated Canvas loop as proof.

If the nearby default helper is considered, the minimum prerequisite is one exact native invocation with actual callback wiring and config getters, compare ungrouped/grouped native acquisition count and identical provider/model/default output, prove fresh independent calls and current selector/pause behavior, and leave parsing outside. Readiness currently catches refresh exceptions and falls back to the app snapshot; an outer operation can raise before that catch, so entry/error semantics must be explicitly preserved, not assumed. No such before/after evidence was produced in this read-only review, and it should not be promoted as the explanation for this timeout.

The concrete current causal lead is repeated live config admission within store/view binding and post-attach settings derivation, with known caller names. No bounded correction to those mixed-effect paths is proved. Prior no-cache/no-skip/native-lifetime constraints still apply; no timeout change, arbitrary loop benchmark, broad summary wrapper or new run is recommended on this artifact alone.
