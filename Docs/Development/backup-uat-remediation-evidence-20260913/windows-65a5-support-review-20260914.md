# Windows 65a5 support causal review

Exact revision `65a5c152b31e77f33506fceac5ec5921b61ef040`: **42 native passed; 197 product outcomes = 192 passed, 5 failed, zero skips/missing outcomes**, product duration 2409.575s. Existing verifiers confirm 124 artifact SHA entries, clean source and two installed receipts with 2474 matching Git Python blobs each (recorded exact/Windows-CRLF comparison). Unit diagnostics are source tests; mounted/handoff drivers explicitly use the installed package. This review read those receipts and failure artifacts without rerunning tests or changing source.

Artifact: `/private/tmp/uat-windows-65a5-support/backup-platform-windows-2022-py3.12-support-diagnostic-65a5c152b31e77f33506fceac5ec5921b61ef040`

| Failure | Duration | Established nearest cause |
|---|---:|---|
| Actual handoff `[handoff]` | 115.487s | Parent subprocess exceeds its 70s observation; case duration also includes setup. |
| Actual handoff `[later]` | 70.046s | Same 70s parent subprocess timeout. |
| Mounted Console | 289.657s | Backup reaches `succeeded / archive_verified`; app lifecycle subsequently exits through `WorkerFailed(RuntimeError('raw_source_helper_not_supported'))`. |
| Mounted Settings | 204.488s | `pilot.press('f4')` raises `WaitForScreenTimeout`, before preview. |
| Mounted Library | 396.057s | Scheduler drain returns false, then capture's exclusive lease acquisition ends in `AdmissionTimeout`. |

Both handoff output files show initial installed application construction/mount activity and the deferred Buddy warning, without a retained fresh-recovery completion or exception. The parent error is in `test_home_citation_retirement._run`'s original subprocess wait, not a proven descendant-pipe wait. The exact source caller still supplies 70s (`test_recovery_restart.py:306–318`). These logs cannot locate the final startup callback or establish that either fresh handoff occurred. No timeout increase is proposed.

Console has five unchanged inventories, including both under-maintenance capture inventories. Its stdout explicitly records archive verification. The tail-limited Rich traceback retains a raw operation with `route='config'`, but loses the originating helper frame; the exact private helper/selected relationship cannot be recovered. `Utils/private_paths.py` has multiple exact helper-refusal sites, so naming a particular writer or proposing an allowlist change would be speculation. The worker error still makes this a failed mounted lifecycle; archive verification alone is not a complete acceptance pass.

Settings reaches `mount_yield` at94.766s and prints `MOUNTED_BEFORE_SETTINGS`, without `MOUNTED_AFTER_SETTINGS` or a capture-review log. Final main-thread samples are in `_watch_canvas_policy → get_canvas_execution_enabled → config_participants.operation → native acquisition` (including lock waiting/authority validation). This establishes synchronous config activity near the failed screen barrier, not a measured single callback duration or proof of a layout loop. No missing progress-button exception appears.

Library reaches its loaded view, F4, Complete preview and capture rediscovery with unchanged inventories. The new exact failed-hook record names **`SchedulerLoop._maintenance_drain`**, elapsed29.969s, returning `runtime_work_not_settled`; startup retirement remains false. Capture then fails at `admission.py:600 → _lock:173 → _check:188`. Exact source drain waits for scheduler task/offload sets (`Scheduling/scheduler/loop.py:213–225`); offload retains its task until real completion (`:233–247`). Retained worker stacks identify `_record_heartbeat:568 → default_heartbeat_path:165 → guarded config acquisition`. Two samples wait at `storage_admission.initializing:244`; the last progresses into authority checks. That wait is same-root initialization serialization, **not itself a local-pause check**. The stage failure records another initialization waiter at29.875s and the heartbeat thread's completed initialization-body maximum28.969s. This is concrete unfinished scheduler-heartbeat work consistent with the failed drain. It does not prove the exact contending initialization leader, a leaked cache, or a lock cycle. Storage snapshots at failure are unavailable and must not be interpreted as zero leases.

The slowest completed Library group is `_admit:541`,21roots: **79.803672s wall,0.15625s caller-thread CPU,79.96875s process CPU**, with all21 token calls completed. Thus it is not79 seconds of overlap-loop CPU on that worker. Total2276 groups completed without errors (106.156131s inclusive). Final rolling CPU samples show the main thread consumed46.796875 CPU seconds over52.125 wall seconds, but their window does not cover the whole slow group; exact attribution is unavailable. The separately sampled five-second loop profile contains two Canvas policy reads at3.84375s inclusive, not evidence of an80-second policy call. Console/Settings slowest groups are0.684414s/0.035686s, respectively. Inclusive nested timings must not be summed.

**No SQLite snapshot-change event occurred in this run.** All six new main/WAL opening/growth/post-copy race tests passed natively. Mounted Console's five and Library's two inventories are unchanged; Settings never previewed. There is therefore no new changed member/field or unknown-writer claim to make for these failures. Prior789960 Library `preview_sqlite_changed` remains separate evidence.

Other previously problematic coverage passes: both mounted profile-open Console cases (163.122s/159.497s),1800-file isolated publication (419.009s), all three native unsafe-parent ACL cases, strict Canvas CRLF checkout, and safe-path restart. No production correction is established by this review. The strongest follow-up is a bounded reproduction of the measured scheduler heartbeat/config-admission/drain interleaving; Console needs preserved fixed exception frames before terminal tail truncation if its exact unsupported helper is to be identified. No guards, caches, deadlines, Linux evidence, or live UAT fixtures were changed/accessed.
