# Bounded runtime and later-child failure diagnostic review

Runtime false-drain attribution: APPROVED with the precise identity/phase conditions below. Read at most 64 traceback nodes, match only f_code is original_stage.__code__, and inspect only that frame's hook local. Require it is identical to one of the original supplied hooks (bounded 64). Emit only a bounded callback qualified name plus monotonic stage elapsed. Keep the hook and frame local to the exception handler; persist no object, owner, frame, other locals or data. No hook wrapping/replacement or callback invocations are needed.

Name this metadata failed_hook rather than failed_drain unless the traceback terminates at the original_stage frame and the error is the exact runtime_work_not_settled refusal. _settle_stage also has the hook local when a close or drain raises, so code/frame identity alone does not prove a false drain. A final-frame check plus existing exact issue establishes the original stage's direct false-result raise. Metadata failure must preserve the pending original exception and existing later observer-stop failure reporting.

Tests: two hooks with first drain true/second false identify only the second; original order/results and exception identity remain; close failure and throwing drain are not mislabeled as false-returning drain; unmatched/foreign hook yields no attribution; bounded name output excludes private exception/local data; observer output failure preserves the original failure. Existing test_runtime_observer_preserves_false_drain_and_reports_candidate_hook is the smallest starting point.

Default later child: no global pytest plugin is needed. Guard the existing asyncio.run(main()) entry in the embedded _LATER script: on BaseException, immediately write only existing _error_metadata to a unique *.json.log then re-raise the same exception. Preserve original error if diagnostic output itself fails. Additionally guard the existing parent subprocess.run in test_f9_later_rollback_requires_explicit_credential_review: persist the same bounded metadata on launch/TimeoutExpired exceptions before re-raising. On a nonzero CompletedProcess, persist a fixed child_exit event and numeric returncode before the unchanged assertion; never serialize the command, args, captured content or CompletedProcess repr.

The parent guard is essential: the d308 default child log ended after changed_acknowledgement_reviewed_again without traceback; an externally imposed 900s child termination cannot run its own exception handler. Since that test had already reported FAILED before the later enclosing phase kill, immediate parent failure metadata would have survived through the existing *.log collector even though JUnit/final pytest summaries did not. A process killed directly by the enclosing phase while still running remains an incomplete case; this does not manufacture a terminal cause.

Add focused synthetic timeout/nonzero/child-raised/write-failure tests with unchanged timeout values and exception identity. Reuse the current collector suffix and error helper. Bounded fixed before-confirmation/before-native-wait markers could distinguish the existing last checkpoint if needed, but no new watchdog or deadline is justified. No product/runner/plugin changes are proposed by this review.
