# Windows 15c Settings capture failure

Exact run34881797168/source15c1541f77780c7897a11ba136d738bad679e9b4. The Settings case fails in204.973seconds. Native phase42/42 and aggregate product272/275 pass; the Settings result is not a passing backup.

The first preview and capture-time inventory both report no scope change, unavailable dependencies or truncation. Actual backup starts, then reports state failed/phase capturing/issue admission_timeout. The retained original error is AdmissionTimeout at admission._admit:600 entering an exclusive namespace lease through _lock:173/_check:188. That is before the later capture body and its final owner snapshot; no archive verification or resumed-write success is established. There is no Settings runtime-settlement record, so no particular drain hook is proved false. The new top-level child record preserves the assertion failure at the actual result check; it is not a WorkerFailed.

The slowest completed pause_requested grouping covers21 namespaces/21 roots,65.5720622seconds wall,0.3125seconds own thread CPU and66.171875seconds process CPU. Its21 token calls account for65.5702497seconds inclusive. This provides evidence of a delayed native pause observation, not65seconds of CPU in the grouping algorithm. The retained window where both main5132 and observer6532 exist is51seconds: main consumes50.5seconds CPU, observer0.203125seconds. That window starts after the slow group's beginning and is not a full-window CPU attribution.

Stacks include the capture worker awaiting the exclusive namespace lease, the native pause observer inside _tokens/pause_requested, and an ordinary trace-maintenance worker entering guarded Notes storage observation. Main-thread snapshots progress through config/native work, message dispatch and finally the event-loop wait. The separate5.6844588second loop profile records5.140625seconds thread CPU but drops460176 calls. Its small retained callback table and inclusive generator timings do not identify a single causal hot callback or prove a deadlock. The main CPU workload can delay the observer, but the artifact does not establish whether GIL contention, particular native calls or a specific application loop is responsible for the whole delay.

This differs from prior65a Library evidence where a named Scheduler drain returned false. It also differs from the current Console preview WAL refusal. Preserve the unchanged native admission deadline and all source/gate checks. No poll-algorithm optimization, guard cache, wider scope, application pause bypass or timeout change is justified by this artifact alone.

Evidence files are under `/private/tmp/uat-windows-15c-support/backup-platform-windows-2022-py3.12-support-diagnostic-15c1541f77780c7897a11ba136d738bad679e9b4/test-logs/product-pytest/test_mounted_console_complete_1/home/`. Bounded timing receipt: `/private/tmp/uat-windows-15c-settings-summary.json`. Root verified146artifact hashes and both immutable installed receipts/2475sourcePython Git blobs each. This report adds no source or test changes.
