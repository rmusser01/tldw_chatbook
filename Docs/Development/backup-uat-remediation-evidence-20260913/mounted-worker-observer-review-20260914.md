# Mounted observer independent final review

**Approved; no actionable findings.** Reviewed four test-only files against `e4f8aa7a62ea9800e3867d47036fafb4a1ec6b5f`. Exact hashes and independent AST checks are recorded in `/private/tmp/uat-mounted-observer-final-review-hashes.json` and match the author's final static receipt.

`record_failure` adds only one optional bounded metadata record for an exact installed Textual `WorkerFailed`. It reads the stored inner exception directly, rejects missing/non-exception/self references and subclasses, and does not recursively follow nested wrappers. Original messages, arguments, private values and traceback locals are not serialized. Optional extraction failure retains the root record; the existing outer best-effort boundary prevents diagnostic failures from replacing the application failure. No observation is added to successful calls.

The mounted entry records its immediate failure and bare-raises. Existing `stop_observer(diagnostics.close)` preserves an active original error or cancellation through observer cleanup; a cleanup error without a primary still fails. All mounted `main()` calls/assertions and its complete AST are independently identical to HEAD. Application, service and subprocess deadlines remain unchanged. The real small Textual App regression exercises its actual exit-on-error worker path and proves the retained terminal callback frame. Entry-AST tests verify original object and terminal traceback identity, write/cleanup combinations, cancellation and success. Unknown/malformed worker values, non-recursion, optional metadata failure, message exclusion and both collector filenames are meaningfully covered.

The runner adds only the existing `test_merged_service_producer_maintenance.py` module to full and support-diagnostic selections. Independent tuple comparison confirms all original nodes and ordering retained. The completed actual support collection log reports **275 tests**, including exactly **36** cases from that added module. No new runner mode or platform skip is introduced.

Independent validation: **33 passed in 2.42s**, `/private/tmp/uat-mounted-observer-independent.log`, private basetemp `/private/tmp/uat-mounted-observer-independent-pytest`. Author evidence reviewed separately: **152 passed in 8.42s**, `/private/tmp/uat-mounted-observer-final.log`; support collection **275 in 3.58s**, `/private/tmp/uat-mounted-observer-support-collection.log`. Static comparison reports Ruff zero; Bandit 18→36 assertion findings, zero non-assertion findings. One existing dependency warning occurred in pytest.

This approves the observation implementation, not a Console/Scheduler product correction or native Windows acceptance. No TldwCli boot, full application suite, repository edit, remote action, or change to the running Windows replacement attempt was performed during this review.
