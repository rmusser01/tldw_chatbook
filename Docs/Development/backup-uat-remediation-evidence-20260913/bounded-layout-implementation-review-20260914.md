# Bounded section layout wake: final independent review

APPROVED. No remaining actionable finding in the final four-file correction against a523. Exact base and SHA256 values are recorded in `/private/tmp/uat-bounded-layout-implementation-review-hashes.json`.

Independent native validation: eight wrapped/native × covered/transparent/hidden/removal cases PASS in11.90s, no failures or skips (`/private/tmp/uat-bounded-independent-final.log`, isolated basetemp). Root's combined guidance/provider/tray/bounded run also reports19PASS61.71s (`/private/tmp/uat-bounded-final.log`). Independently parsed all eight original geometry/scoped-contract XML receipts referenced by `/private/tmp/uat-bounded-compat-corrected.log`: each has one passing case and no failure/error/skip.

The final correction preserves original `request_reconcile()` behavior whenever the owning screen is current, including Textual's live background screens. Only a covered, non-current owner waits for its own layout signal. The three geometry-mismatch paths retain their original height, scroll, focus and hint mutations. A single stored signal retains scheduled/full-retry ownership; old queued callbacks cannot consume that wait. Delivery unsubscribes and clears the stored signal before queuing the existing virtual `_run_scheduled_reconcile`, preserving `_ContextBoundedSection` demand comparison/allocator escalation. New full requests demote scoped work; scoped requests cannot narrow an existing full wait. Unmount cleans through the stored signal and clears scheduled/scoped state. There is no blanket hidden-screen return, polling, timer, guard change or new deadline.

Compatibility finding and resolution remain explicit: the initial always-signal candidate failed the existing one-pause allocation/hint assertion at `test_console_bounded_section.py:497`, while exact HEAD passed1case2.02s in the same native-profile setup. Its viewport height/focus had updated but its hint had not. Restricting the signal wait to non-current owners restored the original visible scheduling. The exact unchanged failing case and seven related geometry/scoped contracts now pass. No assertion, wait or test deadline was relaxed.

The new tests use actual `_ContextBoundedSection`, real wrapper/native WorkspaceTree viewports, stacked screens and genuine content/layout changes. The observer counts reconciles and delegates the original method; geometry, CPU, native checks and results are not replaced. They prove covered callback quiescence despite repeated requests, full-request demotion, same-size recovery, viewport cap accounting, total height6 including the native hint, retained scroll offset4, hint/focusability and allocator notification. Removal is followed by uncover/layout and verifies detachment and no further reconciliation. The dedicated existing scoped-only test supplies the complementary allocator suppression/demotion proof; the new capped cases do not replace it.

Guidance observation is limited to the existing eight fixed phase sites. Excluding only checkpoint plumbing, both embedded script and outer module AST equal HEAD. The helper preserves stdout and appends the same literal labels to HOME/guidance-phases.log, closing each write; new file OSError does not replace the original outcome. Seven chronological labels occur on either route, with assertions_complete at mutually exclusive sites. No settings, paths, IDs or error text are emitted. The final no-network assertion and child completion sentinel remain after shutdown_complete. Root reports actual complete phase files and collector retention of both logs. No fixture was shrunk and the inherited45-second bound is unchanged.

Runner adds only the eight-case module to full, ordinary support, restore-diagnostic and native-close-diagnostic. Plain, support-diagnostic and other finite selections/limits remain unchanged. `git diff --check` is clean. Root-reported static checks: product Ruff4→3/Bandit0; guidance extracted-script Ruff unchanged7 and Bandit57 assertions only; new native test16 assertion-only findings. These static results were not independently rerun.

Limits: the small real Textual lifecycle regressions establish the layout correction and original contract compatibility. They are not full installed backup journeys and do not establish that every preserved Windows backup timeout or startup regression is resolved. Current Windows results must be reported separately with their exact source provenance.
