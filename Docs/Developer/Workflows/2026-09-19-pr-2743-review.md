# PR #2743 rebase and Qodo review

Request: rebase onto latest `dev` and address Qodo's posted findings. No merge
was requested. Original remote head: `84d29dac1473ff784a5d394954b9a6670d40d786`.

ADR required: no new ADR. Existing ADR-138 governs session-only execution,
accepted cancellation and physical settlement; ADR-029 governs private data.
The approved first-run plan explicitly sends `stream: False`. This follow-up
does not add execution storage, SQLite ownership machinery or model streaming.

## Review dispositions

| Qodo finding | Disposition |
| --- | --- |
| 1. Backup and Restore entry removed | Rebase retains dev's Settings registration and `action_backup_restore`. |
| 2. Cancelled navigation loses save observation | Rebase retains eager `_retain_unfinished_flush` before the shielded wait. |
| 3. Notes close interrupts managed writes | Rebase retains dev's `_core_closing` guard. No new SQLite implementation. |
| 4. Failed quit leaves workflows fenced | Intentional ADR-138 boundary: failed or unfinished physical drain cannot reopen admission. The existing error notification reports the drain failure; authoring and Console remain usable. Successful settlement followed by Stay can admit a new run, never revive the cancelled run. Moving the accepted flag after `close()` would violate this distinction. |
| 5. Model output appears only at completion | Intentional approved slice: the first-run implementation plan specifies a bounded non-streaming request. Adding an incremental transport/projection contract is not a review correction to this delivery. Final-response validation, limits and cancellation remain intact. |
| 6. Null Note creation error lacks context | Added bounded SHA-256 references for the user and supplied Note attempt ID; absent IDs are labeled `generated`. No title, content or raw caller-supplied identifier is added to this error. Hashes are correlation aids, not a claim of anonymization. |
| 7. Public Run controls lack API docs | Added concise Google-style constructor, composition, lifecycle, action and event docs, with Args/Returns/Yields/Raises where applicable. |
| 8. Failed DB setup leaks unpublished handles | Rebase retains dev's close/unregister/authorization cleanup and failed-close evidence. |
| 9. Notes tree actions fail to repack on resize | Rebase retains `_tree_actions_need_repack` in both resize paths, alongside the workflow nested-mount fix. |

## Rebase and verification

The initial rebase onto `e3ea3b42fbe55d3fead27b9a194d97c8750c913e`
resolved two app lifecycle hunks by retaining both sides: workflow settlement
precedes the newer shutdown services, and the pre-unmount workflow guard remains.
Independent testing lessons from both branches were retained. Range-diff showed
the other implementation patches unchanged. Rebuilding CSS removed the comment
that the newer builder omits from generated bundles.

The newer dev config source is process-lived. The first bridge regression
errored during fixture setup before reaching the log assertion. Unit Notes/session
fixtures now supply their unrelated migration default explicitly. The log test
then failed for the intended missing correlation and passed after the fix.
Mounted tests reuse dev's existing `private_profile_test`, retaining their actual
config writes, app controls, temporary DBs and original assertions. The real
constructor composition check moved unchanged from the separately bootstrapped
ProductionApp module into that fresh-process UI module. A partial config-module
reinstall was tested, failed on a retained TTS import, and removed; no registry
reset or production ownership bypass remains.

Pre-final-base targeted results (overlapping groups are not additive):

- Local effects and null-ID unit check: **77 passed**, including two new
  correlation cases and the existing private payload canaries.
- Session/admission, app quit guard and token/bundle governance: **107 passed**.
- Mounted workflow controls: **57 passed**; relocated composition case separately
  **1 passed**. Final combined file verification is recorded below when complete.
- Real localhost HTTP / edited Local Note integration: **2 passed, 1 live-UAT
  skip**. Initial socket bind was sandbox-denied; the same loopback-only checks
  passed with the required execution permission.
- Upstream navigation-flush, managed Notes-close, failed setup retirement and
  dark/light Notes toolbar shrink cases: **6 passed**.
- Eleven unchanged ProductionApp lifecycle cases passed; the original twelfth
  case was the moved constructor check, not a removed assertion.
- Six focused source/test files pass Ruff and formatting. Legacy Notes source
  retains **115** Ruff findings and its unit module **3**, identical to HEAD's
  baseline by rule/message counts; no broad cleanup was performed.
- Scoped independent read-only review reported no actionable findings.

No full repository suite, new real-model request, dependency installation or merge
was performed. Earlier live llama.cpp UAT remains historical evidence, not a
claim of a new live-model run after rebase. The existing standalone live launcher
now identifies its fresh-profile child before imports and preserves the prepared
profile on restart rather than rewriting it.

The unrelated TASK-32601 edit was saved in an exact path-specific stash and
restored after rebase; `.uat-workflows-9NUT5t/` was left untouched.

### Final fetched base and verification

Final base: `cccf0acdad8e939a55cb003588ff1406cef5d1f4` (latest remote `dev`
at the final fetch). Source/test head: `12fb82b13dd3a3f8769d4257f9da41383d1b37eb`.
All twenty patches were unchanged by this last rebase according to range-diff;
the newly included dev changes concern MCP Audit navigation, not these fixes.

Fresh post-rebase runs: **58 UI passed** (120.67s), **11 ProductionApp lifecycle
passed** (11.64s), **2 real-HTTP integration passed / 1 opt-in live skip** (36.87s).
These are three separate targeted invocations, not a full repository sweep.
The test logs were retained in the private PR working directory; the case names,
original failed attempts and scoped review dispositions are summarized above.
The new-head Qodo follow-up remains a separate remote review gate.
