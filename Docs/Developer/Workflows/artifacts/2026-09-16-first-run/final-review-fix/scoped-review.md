**[P1] Open Note can deadlock the app loop against a later Note worker — ADDRESSED.** `tldw_chatbook/UI/Screens/workflows_screen.py:291` now requests the existing owner guard with `blocking=False`. `tldw_chatbook/Notes/Notes_Library.py:232` raises on contention before entering the protected body. The app loop therefore cannot wait for the mutex held by the Note worker awaiting that loop. The default remains blocking for existing worker callers; successful acquisition retains cache-object identity validation and releases in `finally`, including validation/body exceptions. Failed acquisition cannot release another caller's lock.

### New Breakage in the Fix Diff

None identified: no new Critical, Important, or Minor findings in the four scoped code/test paths.

- Captured identity check: `tldw_chatbook/UI/Screens/workflows_screen.py:267` rejects missing/stale displayed run and Note identities before navigation. Lines 278–285 preserve the captured scope, user, owner, path, client, and file-availability checks; the owner guard additionally validates the exact cached DB object. The synchronous handler posts the captured current Note ID without an intervening await. Changed/unavailable routes still fail closed at line 304.
- Busy/retry check: `tldw_chatbook/UI/Screens/workflows_screen.py:301` reports transient contention and returns control to the loop; it introduces no wait, automatic retry, owner, or infrastructure. Queued events pass through the same handler at line 1260, so button state is not the safety boundary.
- Status-cleanup check: `tldw_chatbook/UI/Screens/workflows_screen.py:294` clears `_error` and refreshes status only after successful destination validation, before posting navigation. The inspected `_show_status` path updates existing UI state without acquiring the Notes mutex. Failed validation/contention still sets its appropriate message. `Tests/UI/test_workflows_run.py:606` returns to the retained Workflows screen after exact-ID navigation and checks compositor-painted output for stale busy copy.
- Liveness/settlement check: `Tests/UI/test_workflows_run.py:471` covers two real sequential Note effects, a queued captured first-result event during second-worker lock ownership, busy/retry rendering, loop heartbeat, Quit/Stay, optional Cancel, retained physical work, eventual lock release, exact contents and distinct IDs. The completion variant rejects the stale first-result event; fresh retry opens the current confirmed result. `Tests/UI/test_workflows_run.py:621` covers seven changed-destination cases. `Tests/Workflows/test_local_steps.py:405` covers nonblocking contention, unchanged cache/lock ownership, successful retry, default blocking behavior, and exception release in both modes.

### Checks and Evidence

- Reviewed the supplied complete re-review rubric, fix brief, original finding/report, both-commit fix report, and the complete 17,326-character source companion once. Review scope: base `766e2ca43222703a9b07fe67cbcb9dcf3a75811f` through head `bcc8290dd2d883893c781a83a3e5cd0f13f27eb5`, comprising `cdc7c57909a76cafc451757fb99f9aaa2f8719d4` and `bcc8290dd2d883893c781a83a3e5cd0f13f27eb5`.
- Read-only package audit: the full retained package is 528,704 bytes; its four non-artifact diff blocks match the source companion exactly. All nine added artifact bodies match the corresponding retained files exactly. Inspected relevant failure assertions/summaries and static attribution; did not repeat legacy diagnostics line by line.
- Initial `final-review-fix/red.txt`: 5 failed / 1 passed. Both UI failures explicitly record a contended blocking acquire on the app loop; three failures identify the absent `blocking` argument. This is a bounded test-only detector around the original mutex, not an intentional production-loop freeze or a runtime frequency measurement.
- Initial `green.txt`: 13 passed. Initial `covering.txt`: 229 passed across UI, local-step, lifecycle and Notes coverage. Separate `production-lifecycle.txt`: 12 passed. These precede the status-cleanup follow-up and are not final-head rerun claims.
- Follow-up `retry-status-red.txt`: 2 failures show the actual busy sentence still painted after successful navigation and return. Final-code `retry-status-green.txt`: 9 passed / 48 deselected, covering both retry/status variants and seven destination-rejection cases. Each recorded run retains the existing Requests dependency warning.
- Controller-owned `Docs/Developer/Workflows/artifacts/2026-09-16-first-run/controller-postfix-integration.txt`: 2 passed / 1 live-only skip / 1 warning, exit 0, with real app/services/SQLite and an owned HTTP test peer. This is evidence at `cdc7c57909`, before the two status-clear lines; the supplied provenance reports no localhost:9099 POST. It is neither independent reviewer execution nor post-follow-up integration evidence.
- Subsequently inspected controller-owned `Docs/Developer/Workflows/artifacts/2026-09-16-first-run/controller-final-covering.txt`, explicitly recorded at exact reviewed head `bcc8290dd2d883893c781a83a3e5cd0f13f27eb5`: 11 passed / 1 opt-in live skip / 1 existing warning in 38.16s, exit 0. The recorded command combines both multi-Note UI variants, seven queued-destination cases, and two actual-app joined HTTP/Notes cases. Its provenance states isolated bootstrap before application imports, owned HTTP peer, and no localhost:9099 POST. This adds final-head integration evidence without changing the earlier runs' provenance or expanding the source review.
- `static-delta.json` records no introduced lint or formatter findings in all four files, with Notes retaining 115 lint findings and 14 formatter edits. Raw current outputs confirm 115 lint findings and only Notes requiring formatting. The new controller final output records successful Ruff check and format check for `workflows_screen.py`, `test_workflows_run.py`, and `test_local_steps.py` (three files already formatted). Follow-up whitespace success remains recorded in the implementation report. No static checks were rerun here.
- The implementation report is also preserved at `Docs/Developer/Workflows/artifacts/2026-09-16-first-run/final-review-fix/implementation-report.md`; this added evidence copy and the controller final output are outside the original nine-artifact committed package inventory.

### Out-of-Scope Observations

None newly identified. Previously disclosed Skills baseline failure, legacy static/dependency warnings and native PTY limitations remain outside this fix review; this verdict does not requalify them.

### Verification Limits

This is source and retained-evidence review, not fresh execution. No tests, app/core imports, live calls, profile reads, subagents, checkout/index/HEAD/config mutations, push or merge were performed. Unchanged context was read only to resolve captured-event and status-refresh risks. Package consistency does not independently establish runtime provenance or current repository HEAD. The sole write is this scratch report.

### Verdict

**Fix round: All findings addressed, no new Critical/Important breakage.** The sole P1 is closed within this scoped two-commit wave, including transient-status cleanup. No further probe is requested on the reviewed evidence.
