## C3 — partially_true

**Corrected:** Settings 'Test Provider' is a deliberately local readiness check for cloud providers (TASK-191, TASK-386 visible guidance at settings_screen.py:16936-16941, TASK-30011 AC#2; pinned by Tests/UI/test_settings_configuration_hub.py:4622). For OpenAI it reports 'OpenAI configuration is complete. Credential is present; provider acceptance has not been tested.' (15247-15251), so a fake key passes. Whether a button labelled 'Test Provider' should do only this is a UX/labeling question, not a code defect. It is true that the result is one ' | '-joined line mixing config keys and status (15325/15332). There is a real defect on the URL-based live-probe path only (not OpenAI). `_provider_readiness_test_report` (not '_findings') appends prior evidence at 15150, and `_apply_provider_endpoint_probe_outcome` appends the fresh evidence again at 15462-15469. A re-test after an earlier failed probe on the same draft identity therefore shows both 'model listing failed (...)' and 'model listing reached', plus a duplicated 'generation not tested'.

**Root cause:** tldw_chatbook/UI/Screens/settings_screen.py:15143-15150 (stale evidence baked into `detail`) combined with :15458-15469 (fresh evidence appended to that same `detail`), wired together by action_settings_test_category at :30801-30824

**Evidence:** (1) Cloud keys are not verified, and that is by design. settings_screen.py:15343-15363 `_provider_live_probe_base_url` returns "" for any provider outside URL_BASED_PROVIDER_KEYS (Chat/provider_endpoint_contract.py:23-40, which does not include openai). action_settings_test_category (30800-30832) then shows only the local readiness line. The intent is written down in several places: TASK-191's description ("the Settings 'Test' action is a local readiness check by design"), the button tooltip at 16926-16932 and the visible guidance Static at 16936-16941 (TASK-386: "Runs a local readiness check; URL-based local providers also get a short live endpoint probe"), and TASK-30011 AC#2 ("Ready to send ... never implies provider acceptance"). The behaviour is pinned by Tests/UI/test_settings_configuration_hub.py:4622 `test_settings_provider_test_skips_probe_for_cloud_providers`. The claim also misquotes the verdict. The cloud branch at 15247-15251 reads "OpenAI configuration is complete. Credential is present; provider acceptance has not been tested.", and the toast at 15310-15313 adds "Live generation has not been tested." (the test at :4652 pins that toast). (2) The pipe-delimited line is TRUE. `_build_provider_readiness_findings` puts the verdict prose, `model=`, `api_key_source=`, `<ENV>=<redacted>`, the endpoint summary and `configuration=complete` into one list and joins it with " | " (15258-15307, joined at 15325/15332). (3) The double append of evidence is TRUE, but the claim names the wrong method and the wrong path. No `_provider_readiness_test_findings` exists; the first append is in `_provider_readiness_test_report` at 15143-15150. That detail is passed as-is to `_provider_endpoint_probe_worker` (30817-30824), and `_apply_provider_endpoint_probe_outcome` appends fresh evidence to it again at 15458-15469. `ProviderTestEvidenceStore.begin` (Chat/provider_test_evidence.py:593-620) resets the stored endpoint to 'testing', but the stale copy is already baked into `detail`. So a second Test on the same draft identity (no semantic edit bumps draft_generation, 13320-13331) after an earlier failed probe produces "...model listing failed (connection error)..." followed by "...model listing reached...". The in-flight line at 30806 likewise shows the old failure next to "model listing checking". 'generation not tested' also appears twice. This only happens on the URL-based probe path: in Settings the evidence store is only written by begin() at 30814 (the store is created per screen at 2949). So an OpenAI test never gets this contradiction, and the claim wrongly joins it to the OpenAI example. Both appends came in commit 939dee8dc2 (#2365). No test covers repeated runs; the existing pins at Tests/UI/test_settings_configuration_hub.py:4528/4571/4618 cover only single runs.

## C4 — partially_true

**Corrected:** In Settings > Providers & Models, the Discover-models SelectionList only feeds 'Save selected', which appends the IDs to the provider's model list (settings_screen.py:17103-17106, 14649-14668). Selecting a discovered model never replaces an already-set default model, and no one-click 'use as default' exists. The user is not forced to retype the ID, though. TASK-369 (Done) added typeahead completion of discovered IDs on the Model field (14748-14763, attached at 16806), and Save selected auto-fills an empty Model field with the first saved ID (14775-14808, called at 14885). Both are pinned by Tests/UI/test_settings_provider_test_draft.py:1180/:1193. The remaining friction applies only when a model is already set: the user must overwrite it by typing (with prefix completion), because Save selected deliberately leaves a non-empty field untouched.

**Root cause:** tldw_chatbook/UI/Screens/settings_screen.py:14775-14791 (`_model_to_activate_after_save` keeps a non-empty field by design) and 28984-28997 (selection handler only records the selection set); Model field defined as a plain Input at 16801-16807

**Evidence:** TRUE: 'Save selected' (settings_screen.py:17103-17106, tooltip 'Append selected discovered model IDs to the local provider list.') calls `_save_selected_discovered_provider_models`, which persists the IDs and appends them to `app_instance.providers_models` via `_append_saved_discovered_models` (14649-14668). Ticking an item in the SelectionList (handler 28984-28997) only records `_model_discovery_selected_model_ids` and never touches the Model field. There is no 'use as default' action. The Model field is a free-text Input (16801-16807). WRONG: the user does not have to retype the ID. TASK-369 ('Let users pick the Active Model from discovered models instead of retyping from memory', status Done) added two mitigations. (a) The Model Input carries a `SuggestFromList` typeahead of the discovered IDs (`_model_field_suggester` at 14748-14763, attached at 16806, refreshed after discovery at 14732 and after save at 14890); save does not clear `_model_discovery_models`, so the typeahead remains. Typing a prefix completes the full ID. (b) After a successful Save selected, an EMPTY Model field is filled automatically with the first saved ID (`_model_to_activate_after_save` at 14775-14791, `_activate_saved_model_if_field_empty` at 14793-14808, called at 14885). A field that already has a value is left alone on purpose ('A field the user already set is left untouched', TASK-369 notes). Both are pinned by Tests/UI/test_settings_provider_test_draft.py:1180 and :1193. The gap that remains is narrower than claimed. When the Model field already holds a value (for example the provider default staged on provider change at 28214-28227), neither selecting nor saving a discovered model replaces it. The user must edit the field, with typeahead help.

## C1 — partially_true

**Corrected:** By design (ADR-095, TASK-22515 AC#10, and Docs/User_Guide/settings.md:244-246 and 546-547): a Providers & Models save does not rebase an already-open Console conversation; only new blank chats pick up the new defaults. The real UX gaps are these:
(a) The Settings save copy has no scope ('Provider settings saved.' at settings_screen.py:30215, and the toast at 30242). Nothing in the UI says the change applies to new chats only.
(b) The task-177 refresh of an untouched chat (session.py:3783-3786) runs only when the old provider is blocked. An untouched chat on a keyless provider such as llama.cpp, which reads Ready even when down, is never converged.
First-run setup IS meant to update the open chat. 'Start chatting' stages a CONSOLE_FIRST_CHAT handoff that refreshes the untouched session or opens a new one with the new defaults (FirstRunSetupWizard.py:9936-9998, session.py:1325-1560, pinned by Tests/UI/test_console_session_settings.py:2856 and following). The old provider can reach the first message only when setup exits through Home/Library/Notes/skip (no handoff is staged) with a send-capable untouched chat already open, or when the config-revision check (session.py:1353-1360) releases the handoff. P0 is overstated.

**Root cause:** Missing scope copy: tldw_chatbook/UI/Screens/settings_screen.py:30215 and 30242-30244. Untouched chats never converge while the old provider reads Ready: tldw_chatbook/UI/Console_Modules/session.py:3783-3786. First-run handoff is gated to TAB_CHAT only: tldw_chatbook/UI/Wizards/FirstRunSetupWizard.py:9936.

**Evidence:** The main behaviour is BY DESIGN and is documented. ADR-095 (backlog/decisions/095-conversation-owned-console-generation-settings.md:23-27) says blank new chats, including 'the initial pristine Console chat after startup', use the saved global defaults, and 'Existing/open conversations do not rebase'. Its Consequences section repeats this: 'Existing/open and explicitly source-owned conversations remain unchanged'. TASK-22515 AC#10 says the same. The User Guide says it twice: Docs/User_Guide/settings.md:244-246 ('Existing Console chats retain their own session selection; new chats inherit the saved defaults.') and settings.md:546-547 ('Global sampling fallbacks reach new or default sessions, not a conversation already open.').

How the code does it. A Providers & Models save patches app.app_config in memory (settings_screen.py:30202-30207). Only the Console's own Make-default action increments console_new_chat_default_generation (chat_screen.py:3203-3204); this save never does. Ctrl+T blank chats read app.app_config (session.py:3679-3684), so a new tab gets the new values. The only path that refreshes an open chat is the task-177 recovery, _maybe_refresh_stale_default_console_settings (session.py:3742-3825). It returns early when the chat predates a newer default generation (3758-3762), has user work or edited settings (3775), or is already send-capable (3783-3786). So an untouched chat on a keyless provider that reads Ready (llama.cpp, see C2) keeps its old provider/model and sampling values.

The 'no applies-to messaging' detail is TRUE in the UI. The save result says only 'Provider settings saved.' (settings_screen.py:30215), the toast says 'Provider and model settings saved.' (30242-30244), and the Provider field guidance says 'Selects the provider used for Console generation defaults.' (15676-15680). No in-UI copy says the change applies to new chats only; that is stated only in the docs.

The first-run part is WRONG as a statement of intended behaviour. The wizard's 'Start chatting' exit (#setup-exit-chat, FirstRunSetupWizard.py:7503-7510) runs _finalize, which stages a CONSOLE_FIRST_CHAT handoff (9936-9938, 9943-9998). The handoff targets the untouched active Console session through eligible_console_first_chat_session_id (session.py:1210-1237), or reserves a new session if there is none. The Console consumes it when mounted or resumed (chat_screen.py:16722, 23671; session.py:1325-1560). It either refreshes the untouched session to the new defaults (session.py:1506-1516) or creates the reserved session with them (1452-1462). This was added in commit f66b75357c ('feat: hand first-run defaults to Console safely'), and tests pin it (Tests/UI/test_console_session_settings.py:2856 and following, 3861 and following).

The handoff is skipped in two cases. First, it is staged only for exit_route == TAB_CHAT (FirstRunSetupWizard.py:9936), so the Home, Library and Notes exits and skip (exit_route None, 9610) do not stage it. Second, it is released without effect if the runtime config generation changes before the Console consumes it (session.py:1353-1360, message 'Provider settings changed before Console opened'). If the reviewer really saw the old provider after setup, it was one of these two edge cases combined with an already-mounted, send-capable untouched chat, not the designed flow.

## C2 — partially_true

**Corrected:** 'Ready' / 'Ready to send' in the Console for a llama.cpp endpoint that has not been tested is intentional. Readiness is config-only by design (TASK-30011 AC#2, ADR-114); it means 'no known local blocker', not 'the server is reachable'. llama.cpp is keyless and defaults to 127.0.0.1:9099, so it always reads Ready once a model is set. The real defect is narrower: the connection-test evidence is local to the modal (console_settings_modal.py:1304) and the Settings screen (settings_screen.py:13313-13318). The Console header's readiness call (chat_screen.py:9865-9869) passes no evidence, so a KNOWN refused connection never turns the header into 'Not ready — connection refused'. That breaks TASK-30011 AC#6 (surfaces must not overclaim one another). build_console_settings_readiness already maps refused evidence to endpoint_unreachable, so the missing piece is sharing that evidence (for example, one evidence store at app or session scope keyed by ProviderDraftIdentity).

**Root cause:** tldw_chatbook/UI/Screens/chat_screen.py:9865-9869 (Console readiness called with no evidence or current_identity) and tldw_chatbook/Widgets/Console/console_settings_modal.py:1304 (evidence store local to the modal and dropped when it closes). The config-only 'Ready' baseline is intended: tldw_chatbook/Chat/console_session_settings.py:1649-1690 and tldw_chatbook/Chat/provider_readiness.py:98 and 316-319.

**Evidence:** The core mechanism is CONFIRMED. The Console computes readiness in chat_screen.py:9853-9869 (_active_console_settings_readiness_uncached). It calls build_console_settings_readiness(effective_settings, app_config=..., active_run=...) with no evidence and no current_identity. With no evidence, ProviderReadiness.snapshot sets endpoint='not_tested' (provider_readiness.py:316-319). The endpoint-unreachable blocker branch (console_session_settings.py:1649-1650) therefore never fires, and the result is label 'Ready' (1686) and the presentation 'Ready to send' (console_settings_summary.py:154-158). llama.cpp is in KEYLESS_PROVIDER_KEYS (provider_readiness.py:98), and 127.0.0.1:9099 is its built-in default (console_provider_gateway.py:195; settings_screen.py:1420). A configured model alone is therefore enough for Ready, whether or not the server is up.

Test results do not reach the Console, also confirmed. The Console settings modal creates its own ProviderTestEvidenceStore (console_settings_modal.py:1304) and passes that evidence only to its own readiness call (6869-6875). The Settings screen likewise keeps a separate, screen-local store (settings_screen.py:13313-13318). chat_screen.py has no reference to ProviderTestEvidence or any evidence store. So a failed modal test can make the modal say Not ready while the Console header keeps saying Ready.

Part of this is BY DESIGN. TASK-30011 AC#2 says 'Ready to send means no known local blocker and never implies provider acceptance or successful generation'. The detail copy reads 'A send attempt is permitted with these settings.' (console_settings_summary.py:158). ADR-114 says 'Readiness is not process liveness'. Showing Ready for an endpoint that has never been tested is therefore intended: readiness is config-only and there is no background probe.

The defect is that the header ignores a KNOWN failed test. TASK-30011 AC#4 requires distinct outcomes for refused/timeout failures. AC#6 requires that 'the Console rail, setup card, Conversation settings modal, and Settings provider test consume compatible structured facts and cannot overclaim one another'. The readiness function already supports this: tests pin blocker 'endpoint_unreachable' / 'retry_connection' when refused evidence is supplied (Tests/Chat/test_console_session_settings.py:1169-1182). Only the Console header path never supplies it.

## C5 — partially_true

**Corrected:** In ConsoleSettingsModal, Inputs and non-footer Buttons are 3 rows. Inputs get this from MODAL_CONTROL_HEIGHT=3 (console_settings_modal.py:168, 1066-1071) and _agentic_terminal.tcss:43-52. The Buttons are borderless because of the global Button {border:none}, so each shows 1 label line and 2 blank rows. Selects are 1 row, because the app-sheet rule _agentic_terminal.tcss:54-62 overrides DEFAULT_CSS, but their rows are still 3 tall (label min-height 3 plus the leaked Select margin-bottom 1). The Settings screen does the reverse: 1-row input rows (_settings.tcss:369) and 3-row select rows (_settings.tcss:465). A collapsed Collapsible costs about 7 rows because of three leaked global rules: the title height 3 (_conversations.tcss:298-303), the tall border (_evaluation_unified.tcss:59-63) and Textual's padding-bottom 1, plus the section margin. Short numeric inputs are about 125 columns wide at 211 columns, set by the 75% max-width. The '▼ more — scroll for the rest' hint only tests overflow (console_settings_modal.py:3861-3866) and is never re-evaluated on scroll, so it still shows at the bottom of the scroll.

**Root cause:** css/features/_conversations.tcss:298-303 (+ _evaluation_unified.tcss:59-63, _conversations.tcss:313-316); console_settings_modal.py:1066-1071 and css/components/_agentic_terminal.tcss:43-62; console_settings_modal.py:3850-3866 (_sync_fold_hint has no scroll_y check and no scroll trigger)

**Evidence:** Checked at c4225b5d38. Confirmed parts: (a) Inputs are 3 rows. console_settings_modal.py:168 sets MODAL_CONTROL_HEIGHT=3, :1066-1071 is the DEFAULT_CSS rule, and the app-sheet rule css/components/_agentic_terminal.tcss:43-52 (bundle tldw_cli_modular.tcss:2477) also sets height 3. Tests/UI/test_console_session_settings.py:4555-4560 pins the constant. (b) Every Collapsible title is 3 rows because of the unscoped rule css/features/_conversations.tcss:298-303 (`Collapsible > CollapsibleTitle { height: $ds-size-3 }`, bundle :11664). No console-scoped override exists. _agentic_terminal.tcss:1741-1744 already names this rule as a leak (task-2043) but fixed it only for LibraryIngestCanvas. (c) The fold hint ignores scroll position. _sync_fold_hint (console_settings_modal.py:3850-3866) shows the hint whenever `virtual_size.height > container_size.height` and never reads scroll_y. None of its callers (:2709, :2716, :3216, :3361, :3574, :3672, :3677, :3892) runs on scroll. The sibling ConsoleBoundedSection does watch scroll_y (console_bounded_section.py:234). Tests pin only 'hint shows while overflowing' (test_console_resize_reflow.py:846-856, test_console_context_controls.py:980-985). No test or ADR pins 'shows at the bottom'. (d) The inversion is real. The Console modal has Input 3 rows (_agentic_terminal.tcss:43) and Select 1 row with border none (_agentic_terminal.tcss:54-62). The Settings screen has .settings-input-row height 1 (_settings.tcss:369-374, with .settings-compact-input) and .settings-select-row height 3 (_settings.tcss:465-470). Wrong or overstated parts: (1) 'every Input/Select/Button is 3 rows' contradicts the claim's own last sentence. In Textual 8.2.8, app CSS always outranks DEFAULT_CSS: styles.py:998-1003 puts default_rules=0/1 first in the specificity tuple. So `ConsoleSettingsModal Select {height:1}` (_agentic_terminal.tcss:54) beats DEFAULT_CSS height 3, and Selects are 1 row. A Select row still takes 3 rows, because .console-settings-modal-label has min-height 3 (_console_panels.tcss:67-75) and the leaked `Select { margin-bottom: 1 }` (_conversations.tcss:313-316) adds a row. Footer buttons are forced to 1 row with h-1 (console_settings_modal.py:3308-3313). Other Buttons are 3 rows but borderless because of the global `Button { border: none }` (components/_buttons.tcss:12-15), so each shows one label line and 2 blank rows. (2) The collapsed-section cost comes from three leaked global Collapsible sources, not just _conversations.tcss: the title height 3 there; `border: tall` from features/_evaluation_unified.tcss:59-63 (which overrides the round border at components/_widgets.tcss:13-19); Textual's default Collapsible padding-bottom 1; and the section margin-top 1 from .console-settings-modal-section (_console_panels.tcss:42-45). That totals about 7 rows of footprint (6 of box) per collapsed title. The model view has 3 of them (:1954, :2139, :2168). (3) Input width: the wide tier is 85% of 211 = 179 columns (_console_panels.tcss:200-203, cap 196). The .console-settings-control max-width of 75% (_console_panels.tcss:77-81) puts Advanced-generation inputs at about 125 columns by CSS arithmetic, not 136. The size problem is real; the exact number is not reproducible from code. (4) 'Only the Connection block visible at 211x44' is plausible (the modal is 94% of 44 = 41 rows, so the body is about 27 rows, and the Connection block is about 25 or more rows) but cannot be verified without running the app.

## C6 — partially_true

**Corrected:** The quick popover is 170x32 at both 235x52 and 211x44. The width is capped on purpose (console_model_popover.py:181-184, pinned by test_console_model_popover_geometry.py). Its 2x2 action grid uses grid-rows: 3 3 (:253-261) for compact 1-row buttons (:634-650), so the footer spends about 8 rows (9 counting bottom padding) on 2 rows of labels. That pushes the Temperature input to the fold of a roughly 20-row body, where its last row is clipped. Max tokens is shown read-only by design: ADR-095 limits the quick mask to temperature and streaming. No explicit initial focus is set, so Textual's AUTO_FOCUS='*' focuses the scroll body #console-model-popover-body instead of a control.

**Root cause:** tldw_chatbook/Widgets/Console/console_model_popover.py:253-261 (grid-rows 3 3 with compact buttons at :634-650) and :682-687 (on_mount sets no initial focus; falls through to Textual AUTO_FOCUS='*')

**Evidence:** Checked at c4225b5d38 (console_model_popover.py). Confirmed: (a) Footer waste. The rule at :253-261 sets `height: 6; margin: 1 0 0 0; grid-rows: 3 3`. The four buttons are compact=True (:634-650), and Textual's Button is height:auto with a borderless compact style, so grid.py:332-348 places each as a 1-row box in a 3-row cell. The footer is the fold hint (:248-251, 1 row, shown on overflow) + a 1-row margin + a 6-row grid = 8 rows. Only 2 of those rows carry button labels; the '9' counts the container's bottom padding (padding: 1 2 at :162-171). Tests pin only button order and reachability (test_console_resize_reflow.py:283-310, test_console_context_controls.py:1006-1011), not row height. (b) Initial focus lands on the scroll body. on_mount (:682-687) does not focus anything, and chat_screen.py:5527-5550 pushes the popover with no follow-up focus. Neither the app nor the popover sets AUTO_FOCUS, so Textual's App.AUTO_FOCUS='*' (textual/app.py:396, applied at screen.py:1485-1500) focuses the first focusable node in DOM order. That node is the VerticalScroll #console-model-popover-body (:454), since ScrollableContainer has can_focus=True (containers.py:32). (c) Height is 32 at both terminal sizes: height 100% with max-height 32 (:165-167). (d) Temperature sits at the fold. The body is about 32 - 2 border - 2 padding - 8 footer = 20 rows. Before Temperature come: the header (1 row + margin 1, _console.tcss:1843), scope and durability (2), the Provider label with its margin (2), the provider Select (3 + leaked margin-bottom 1 from _conversations.tcss:313-316), the Model label (2), ModelSearchPicker (3 + status 1) and the Temperature label (2). That puts the input on body rows 18-20, so its last row is clipped rather than wholly below the fold. Wrong or by-design parts: (1) The width is 170, not 172. The wide tier is `width: 85%; max-width: 170` (:181-184), which gives 170 at both 211 and 235 columns. The cap is deliberate: a comment at :172-180 says it sits below the modal's 196 so the quick surface stays lighter. Tests/UI/test_console_model_popover_geometry.py:150-154 pins it (region.width == min(170, 85%)). (2) 'max tokens is only read-only' is by design. ADR-095 (backlog/decisions/095-conversation-owned-console-generation-settings.md:74-79) fixes the quick field mask to temperature and streaming, matching QUICK_MODEL_DEFAULT_FIELDS in Chat/console_settings_apply.py:19. The popover shows 'Response max' as a Static context row (:531-536), and editing it belongs to Full settings. (3) '18 of 32 rows blank' cannot be verified without a live capture. Code-level sources of blank rows: 2 padding rows, the header margin, label margin-top 1 each (:193-196), the leaked Select margin-bottom, the footer margin, and 4 empty grid rows.

## C7 — partially_true

**Corrected:** P1 (the model part): the Console popover and the full settings modal both rebase a provider switch with model=None, and resolve_effective_chat_configuration (Chat/console_session_settings.py:1269-1275) then fills the global chat_defaults.model before the target provider's own model. Any switched-to provider inherits the global default model, whatever the previous provider was, e.g. a local model under Anthropic when the user's default is local. The shared ModelSearchPicker (popover and modal) never marks or pre-highlights the committed model in its results (model_search_picker.py:643-644, :697-702, :868). P3: status chips show raw provider keys or the raw chat_defaults spelling (chat_screen.py:9826-9831, console_display_state.py:776), while the pickers use provider_display_name. Unmapped keys such as local_onnx and local_transformers render raw. Not defects: the chip cap of 25 model characters is user-requested and pinned (TASK-1671, _console.tcss:253). Selectable legacy aliases are intentional (task-180), labelled '(legacy alias)' and grouped last.

**Root cause:** tldw_chatbook/Chat/console_session_settings.py:1269-1275 (chat_defaults.model outranks target-provider model on explicit provider override); tldw_chatbook/Widgets/model_search_picker.py:643-644,697-702,868; tldw_chatbook/UI/Screens/chat_screen.py:9826-9831 + tldw_chatbook/Chat/console_display_state.py:776

**Evidence:** (a) MODEL CARRY-OVER, true on both surfaces but the cause is wrong. Both surfaces rebase with model=None through the controller: modal _switch_provider -> _rebase_to(provider, None) (Widgets/Console/console_settings_modal.py:5372-5373); popover _provider_changed -> _rebase_to(provider, None) (Widgets/Console/console_model_popover.py:1101); both wired to controller.rebase_console_settings_draft (UI/Screens/chat_screen.py:3421, :5543-5545). That calls build_target_default_console_session_settings -> resolve_effective_chat_configuration (Chat/console_chat_controller.py:12982-12986). Its model chain is session model -> chat_defaults.model -> provider model/api_model/default_model (Chat/console_session_settings.py:1264-1281). chat_defaults.model is applied with no check that the explicit provider matches chat_defaults.provider. So the switched-to provider inherits the GLOBAL default model, not the previous provider's model. The two match only when the previous provider is also the default provider. The shipped default is OpenAI/gpt-5.6-terra (config.py:4612-4613), so llama_cpp->Anthropic would give gpt-5.6-terra. This is not by design: the popover comment for TASK-364 says 'a stale model from another provider must not linger' (console_model_popover.py:1096-1098). Tests pin only the same-provider precedence (Tests/Chat/test_console_session_settings.py:160-170). No test covers a cross-provider rebase with model=None (Tests/Chat/test_console_settings_apply.py:768-775 asserts only the provider). The modal's non-rebaser fallback _model_for_provider (console_settings_modal.py:7212-7232) does prefer the provider's own model, but production always passes a rebaser. (b) NO CURRENT-MODEL MARK, true but not modal-only. Popover and modal share ModelSearchPicker. Options render as plain ids (Widgets/model_search_picker.py:643-644 and :697-702). Down highlights the first enabled option, not the current one (:866-875). The committed model appears only in the input value (:183-185). (c) CHIP TRUNCATION, true but by design and independent of width. .console-control-chip has a fixed max-width: $ds-size-34 with ellipsis (css/features/_console.tcss:253). TASK-1671 (Done, user-requested: 25 chars of the model name) set it, and Tests/UI/test_console_shell_chip_actions.py:83-92 pins it. Focus lifts the cap (_console.tcss:296). (d) NAME DRIFT, true. The status chips use raw spellings: provider_display = selection.provider (a raw config key) or the persisted chat_defaults.provider string (chat_screen.py:9826-9831; e.g. 'OpenAI', or a user's 'Llama_cpp'), rendered as 'Provider: {raw}' (Chat/console_display_state.py:776). The picker, popover and modal use provider_display_name (console_provider_picker.py:235, console_model_popover.py:380, console_settings_modal.py:7075). (e) ALIASES, partly by design. task-180 intentionally keeps legacy aliases selectable, sorted last in 'Custom & legacy aliases' and labelled '(legacy alias)' (Chat/provider_catalog.py:26-31, :55-69). The real residue: default [providers] keys local_onnx and local_transformers (config.py:4150-4151) have no PROVIDER_DISPLAY_NAMES entry, so provider_display_name falls back to the raw key.

## C8 — partially_true

**Corrected:** P2: for Anthropic the Console modal shows Min P, Seed, Presence and Frequency like supported fields. PROVIDER_PARAM_MAP['anthropic'] omits them and project_chat_handler_kwargs drops them silently. The root cause is _supported_console_settings_fields (console_chat_controller.py:774-802), which treats all sampler fields as universal, so only the reasoning and thinking controls get support gating. Esc (and backdrop) on ConsoleSettingsModal discards an edited draft with no prompt: _request_settings_close (:4041-4054) guards only memory reset and active compaction. This conflicts with ADR-031's task-16211 'never discards by itself' rule. F6 on Settings shows 'No workbench pane focus target is available.' because SettingsScreen has no pane-cycle handler (app.py:7750, :20175-20186). This is already documented as a known limitation in Docs/User_Guide/settings.md:877. The Settings rail focus cue is a background swap of about 1.1-1.2:1 plus bold (_settings.tcss:198-203). A focused active row looks identical to an unfocused active row (:205-209), and tests pin that.

**Root cause:** tldw_chatbook/Chat/console_chat_controller.py:774-802 (+ Chat_Functions.py:1431-1435 silent drop); tldw_chatbook/Widgets/Console/console_settings_modal.py:4041-4054; tldw_chatbook/UI/Screens/settings_screen.py:2731 (no action_focus_next_workbench_pane) with fallback at app.py:20175-20186; tldw_chatbook/css/features/_settings.tcss:198-209

**Evidence:** (1) UNSUPPORTED SAMPLERS, confirmed. PROVIDER_PARAM_MAP['anthropic'] (Chat/Chat_Functions.py:281-300) has no minp, seed, presence_penalty or frequency_penalty. project_chat_handler_kwargs forwards only mapped keys (Chat_Functions.py:1431-1435), with no warning or log. The Console path maps min_p->minp and the rest into chat_api_call (Agents/agent_service.py:269-275; gateway console_provider_gateway.py:6461-6465). The modal renders Min P, Seed, Presence and Frequency unconditionally (Widgets/Console/console_settings_modal.py:1976-2017). Only the reasoning and thinking controls get support indicators or the 'no effect on this provider' treatment (_GENERATION_CONTROL_INPUTS :846-852). Deeper cause: _supported_console_settings_fields (Chat/console_chat_controller.py:774-802) treats every sampler field as universally supported (FULL_MODEL_DEFAULT_FIELDS minus provider-specific ones), so nothing downstream can know. TASK-83 made reasoning controls visibly unavailable (AC #6) but never did the same for sampler fields. (2) ESC DISCARDS THE DRAFT, confirmed. Escape binds request_safe_cancel (console_settings_modal.py:1140) -> _perform_safe_cancel -> _request_settings_close (:4037-4054), which guards only an open guard, _memory_reset_token and _compaction_is_active, then dismiss_safe_once(None). There is no dirty-draft check. The only draft snapshot (capture_suspended_draft :2958) serves the credential handoff, not Esc. Tests pin Esc/Cancel -> None only with an unedited draft (Tests/UI/test_console_session_settings.py:5254-5277, :5302-5325). ADR-031's task-16211 refinement (backlog/decisions/031-tui-keybinding-and-footer-hint-conventions.md:18) says 'a generic gesture never ... discards by itself', so this arguably breaks the ADR rather than being sanctioned by it. (3) F6 TOAST, confirmed, and a documented known limitation rather than a hidden bug. The app binds f6 with show=True (app.py:7750). action_focus_next_workbench_pane falls back to the toast (app.py:20175-20186). SettingsScreen(BaseAppScreen) (UI/Screens/settings_screen.py:2731) defines no handler; only chunking_lab, chat, library, personas and workflows do. Docs/User_Guide/settings.md:877 already says 'F6 does nothing on this screen'. (4) RAIL FOCUS, mostly confirmed, with detail corrections. At _settings.tcss:198-203 focus only swaps $ds-surface-panel for $ds-surface-raised and adds bold. The contrast is 1.107:1 in agentic_terminal (#0D1626 vs #142033, css/Themes/themes.py:1561-1562) and 1.22:1 in the default textual-dark ($panel #242F38 vs $surface #1E1E1E, where the focused fill is DARKER than unfocused). Bold remains a weak second cue, so 'invisible' overstates it. Worse, the focused ACTIVE row is identical to the unfocused active row (:193-197 vs :205-209), and tests pin that selected-state contract (Tests/UI/test_settings_configuration_hub.py:4953-4968, Tests/UI/test_non_obscuring_focus_contract.py:1140-1152).
