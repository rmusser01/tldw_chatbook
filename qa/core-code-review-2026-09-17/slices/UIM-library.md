# UIM-library — tldw_chatbook/UI/Library_Modules/ (46 files), 47,026 lines

Reviewed at worktree `/Users/macbook-dev/Documents/GitHub/tldw-review` HEAD d8fb4053f9 (origin/dev has since moved to e89f28d751; not reviewed).

## Coverage
| file | lines | read in full / sampled (which ranges) / mechanical only |
|---|---|---|
| library_notes_controller.py | 6365 | **read in full** (1–6366, five passes) |
| library_prompts_controller.py | 5063 | **sampled deep**: 779–4180 read line-by-line (the whole moved-body region through the save/delete/history clusters); 1–779 (module docstring + `__init__` + property-forward block) and 4180–5063 (conflict-resolution tail) by targeted extract only — every `self.refresh(recompose=True)`, `run_worker`, `except`, `query_one`, service-call and timer site enumerated by AST across the whole file |
| library_media_controller.py | 4763 | **sampled deep**: 1130–4763 read line-by-line (entire property-forward + moved-body region); 1–1130 (module docstring + `__init__`) by targeted extract only. Same AST enumeration over the whole file. |
| library_skills_controller.py | 3145 | **mechanical + targeted**: AST/grep sweeps only (all `run_worker`, `query_one`, `set_timer`, `except`, `_sync_library_canvas`, `refresh(recompose=)`, service-offload counts, import resolution); no line-by-line read |
| library_ingest_controller.py | 3078 | **mechanical + targeted**: same sweeps, plus the debounce-timer site (1650–1665), the preflight cancel/invalidate pair (2039–2065) and the worker/FS-call census read in place |
| library_notes_sync_controller.py | 2380 | **mechanical + targeted**: same sweeps, plus `abandon_setup` (1165–1180) and its runtime counterpart `Notes/notes_sync_runtime.py:2379–2401` read in place |
| library_rag_search_controller.py | 1915 | **mechanical + targeted**: same sweeps, plus the two unguarded-`query_one` refresh functions (1640–1690, 1815–1870), the scope-recovery mirror (1590–1632) and their screen-side dispatcher (`library_screen.py:35060–35135`) read in place |
| library_conversations_controller.py | 1800 | **mechanical only** (the AST/grep sweeps listed below) |
| library_collections_controller.py | 1747 | **mechanical + targeted**: sweeps, plus the four unguarded-`query_one` clusters mapped to their enclosing functions |
| library_export_controller.py | 1448 | **sampled deep**: 1–450 (docstring + `__init__` contract) and the marshal/worker/path region (930–1130, 1355–1420) read line-by-line; middle builder region mechanical |
| canvas_sync.py | 1084 | **read in full** |
| library_character_repair_controller.py | 502 | **read** 300–502 (compose + every handler); 1–300 (controller/state machine) by targeted extract |
| the other 34 files < 1000 lines | ~11,800 | **mechanical + candidate-row sampling**: every one appears in at least one AST/grep sweep below; the ones carrying a candidate row (`skill_import_choice_modal.py`, `prompt_collection_manager_modal.py`, `library_media_browse_controller.py`, `screen_helpers.py`, `library_browse_route_swap.py`, the four `_next_request_token` browse controllers, `library_snapshot_cache.py`) had the named region read |

**Mechanical sweeps run over all 46 files** (evidence for every "N of M" below): ruff `--select E9,F63,F7,F82`; AST census of every `query_one` call vs. its enclosing `try`/handler (228 total, 40 unguarded) plus each unguarded call's enclosing function; AST census of every function-body `import` with `importlib.util.find_spec` resolution (36, all resolve); `rg run_worker\(` (96 sites) cross-referenced with `thread=True` (4) and `to_thread|run_in_executor` (25); `rg set_interval|set_timer` (6 real call sites, all read); `rg except Exception` + following line; `rg '^\s*self\.refresh\(recompose=True\)'` per file; `rg _sync_library_canvas\(` per file; `rg re\.compile`; `rg get_cli_setting|load_settings\(`; `rg threading\.|Lock\(`; `rg '\.plain\b|str\((\w+\.)?label\)'`; AST scan for `_`-prefixed names crossing the package boundary in both directions; `Tests/Architecture/test_library_modules_size_ratchet.py` run targeted.

## Findings

### P2 [D1] — a swallowed read error silently disables the ONLY optimistic-concurrency check on the Prompt save path, turning a conflicting save into a last-writer-wins overwrite
- Where: `tldw_chatbook/UI/Library_Modules/library_prompts_controller.py:3116-3146` (the pre-read and its `except Exception: fresh = None` at `:3124-3125`); contract stated by the method's own docstring at `:2946-2953`
- Evidence: read only — see UNVERIFIED. The chain is unambiguous in three lines. `_save_library_prompt`'s docstring states that the write seam "always re-derives the version to bump from a fresh read inside its own transaction, so it cannot detect 'this editor's cached version is stale' by itself. **This method does that staleness check itself**, via a fresh `get_prompt` read, BEFORE attempting the real write." That read is wrapped `try: fresh = await …get_prompt(…) except Exception: fresh = None` with **no logging at all**; `fresh_version = fresh.get("version") if isinstance(fresh, Mapping) else None` (`:3132`) is then `None`, so the guard `if fresh_version is not None and …` (`:3133-3137`) is False, `_enter_library_prompt_conflict` is skipped, and control falls straight through to the real `save_prompt` at `:3149`, which bumps whatever version it finds. The sibling pre-check at `:3079-3088` (`except Exception: candidate = None`) degrades the same way but is harmless — it only skips a name-collision pre-classification the unique-name constraint re-raises on the write.
- Why it matters: the stated design is that the editor, not the DB, detects a stale version. A transient read failure (lock/timeout/closed handle — the write is a separate call that can still succeed) therefore converts "another writer changed this prompt, show the conflict banner" into a silent overwrite of that writer's edit, logging nothing an operator could see afterwards.
- Recommended correction: on a failed pre-read, refuse the save rather than proceeding — surface the existing "Couldn't load the selected Prompt… retry" copy (`_apply_library_prompt_detail_failure`, two hundred lines up) and log with `logger.opt(exception=True).warning`. A read that did not answer must not read as "nothing changed"; that is the distinction `_load_library_note_backlinks` already makes explicitly (`library_notes_controller.py:3498-3500`: "A lookup that did not answer must not read as 'no notes link here yet'").
- Size: S · ADR: no · Confidence: inferred
- Pinning test: none found — no test in `Tests/UI/test_library_prompt*.py` forces the pre-read to raise
- Already covered: none

### P2 [D2] — the Media Reader rebuilds the review-set banner from an unmemoised whole-set load on every viewer sync; measured 3.25 ms per call at the 500-item cap, and the cost is per-CALL not per-item, so every review-set user pays it
- Where: `tldw_chatbook/UI/Library_Modules/library_media_controller.py:3913` (`_sync_library_media_viewer_state`, the unchanged-compare) and `:3754` (`_build_library_media_reader`, the route build). Both call the late-bound `_active_review_set_banner`, whose body is `tldw_chatbook/UI/Screens/library_screen.py:31169-31220` → `service.get_active_review_set()` at `:31186`.
- Evidence: `cd $WT && source $SCRATCH/env.sh && PYTHONPATH=$WT $PY - <<EOF` building a real `LibraryCollectionsDB` + `ReviewSetService`, creating a 500-item and a 50-item set, and timing 60 calls each → `50 items: median 2.59 ms  p90 2.93 ms  max 3.42 ms` / `500 items: median 3.25 ms  p90 3.78 ms  max 16.86 ms`. Per-`]`-press call count, traced: `action_library_media_next_item:3241` → `_select_library_media_adjacent_item:3249` → `_walk_active_review_set` (screen `:30953`, load #1) and, when the walk does not consume the key, `_select_library_media_reader_row:2171` → `_sync_library_media_viewer_or_recompose:4295` → `_sync_library_media_viewer_state:3848` → `:3913` (**load #2 — this file's share**). `:3754` is NOT on the in-place sync path: the changed branch at `:3980-4058` patches viewer attributes and calls `viewer.refresh(recompose=True)`; the builder is reached only from compose/route-swap (`library_screen.py:15286`, `library_browse_route_swap.py:204`), so a Reader ENTRY pays a third load.
- Why it matters: the banner is rebuilt from storage on a path whose sibling on the same class is already memoised. `_decorate_with_review_state` (`library_screen.py:16100-16127`) caches `get_active_review_set()` against `service.revision` with a comment saying exactly why ("this runs at every one of the ~30 viewer-flip sync sites — a whole-set load to stamp at most a page"); `_active_review_set_banner`, 15,000 lines later, does the identical load with no cache. The measurement also corrects the mental model: the cost is dominated by `read_transaction` setup, not by the 500 rows, so a user with a 12-item set pays 2.6 ms of the 3.2 ms — this is not a large-set-only problem.
- Recommended correction: reuse the existing `(service.revision, value)` memo shape for the banner. Canonical home is `library_screen.py` beside `_review_done_map_cache`, not this slice — this file's share is the call site. **This confirms and bounds the UI-library P1**; fix it there, not twice.
- Size: S · ADR: no · Confidence: verified (measurement); the per-press call count is verified by call-graph read
- Pinning test: none
- Already covered: UI-library P1 (same defect; this is its measurement and this file's share)

### P2 [D3] — the Prompts controller is the one Library cluster where whole-screen recomposes outnumber the canvas-scoped sync the performance audit introduced, 16 to 8
- Where: `tldw_chatbook/UI/Library_Modules/library_prompts_controller.py` — `self.refresh(recompose=True)` at `:1652, 2586, 3353, 3408, 3905, 4155, 4358, 4394, 4477, 4570, 4661, 4733, 4778, 4880, 4925, 4968`
- Evidence: `rg -c '^\s*self\.refresh\(recompose=True\)' tldw_chatbook/UI/Library_Modules/` → prompts 16, export 5, media 3, ingest 3, unavailable_navigation 2, notes 1, collections 1, prompt_collection_manager_modal 1. `rg -c '_sync_library_canvas\(' …` → notes 40, skills 16, media 8, conversations 8, **prompts 8**, rag 4. So notes is 40:1 and skills 16:0 in favour of the targeted seam while prompts is 8:16 against it. The seam's own docstring (`canvas_sync.py:516-537`) records what a whole-screen recompose costs — "a whole-screen remove/remount of the nav bar, footer, ~20-row rail, and 50-100-row canvas" — and cites `Docs/Design/2026-07-16-performance-audit.md §P1 B2` (file present) as why the conversion exists. Every one of the 16 sites is in a cluster the same file already syncs canvas-scoped elsewhere (`_sync_library_prompt_selection:1131`, the import open/close pair `:1600/:1622`, the detail retry `:1573`, the browse-result projection `:1351`).
- Why it matters: it is not a hot path today (each of the 16 is one user gesture — duplicate, convert, undo, receipt dismiss, conflict entry/resolution, a file-picker callback), which is why it has survived; but it is the pattern that produced the audit's finding, on the one cluster that never finished converting, and each recompose tears the rail and footer down beside an editor holding unsaved text. `:1652` (`browse_callback`) is the clearest single case: it recomposes the whole screen to store one string into `_library_prompts_import_path`.
- Recommended correction: convert the 16 to `_sync_library_canvas(self, "prompts", then=…)` in one PR, in the shape the same file's 8 existing call sites already use — what the notes and skills series did. No redesign: the helper, the follow-up ordering and the fallback all already exist.
- Size: M · ADR: no (`backlog/docs/library-decomposition-recipe.md` §1 per-subsystem PR series is the shape) · Confidence: verified
- Pinning test: none (the size ratchet measures lines, not recompose discipline)
- Already covered: none

### P2 [D3] — §17 controller-file size governance is red for 16 of 27 `_BUDGETS` rows at the reviewed SHA; both >5k controllers are over their pins (notes +1065, prompts +65)
- Where: `Tests/Architecture/test_library_modules_size_ratchet.py:135-576` (`_BUDGETS`), measured files under `tldw_chatbook/UI/Library_Modules/`
- Evidence: `cd $WT && source $SCRATCH/env.sh && PYTHONPATH=$WT $PY -m pytest Tests/Architecture/test_library_modules_size_ratchet.py -q -p no:cacheprovider` → `16 failed, 27 passed`. Rows: notes_controller 6365 vs 5300 (+1065); prompts_controller 5063 vs 4998 (+65); ingest 3078/2721 (+357); notes_sync 2380/2024 (+356); media_browse 720/371 (+349); note_import 793/602 (+191); export 1448/1307 (+141); media 4763/4670 (+93); conversations 1800/1738 (+62); collections 1747/1689 (+58); skill_import 805/760 (+45); conversation_reader 969/943 (+26); unavailable_navigation 840/817 (+23); rag_search 1915/1898 (+17); navigation 202/198 (+4); skills 3145/3142 (+3). The test's last commit is `18a8369bf1 2026-09-11`; the notes controller was modified by 11 commits after that (wave-5 PRs #2694–#2702, 2026-09-13..15) with no re-pin. `git show c8e2a913e6:…/library_notes_controller.py | wc -l` → 5816 at the "re-pin" commit itself vs its 5300 row, and media_browse was 720 at that commit vs its 371 row — two rows were already stale when pinned. `.github/workflows/test.yml:121` runs `pytest Tests --ignore=Tests/UI`, so this test is in the core CI shards.
- Why it matters: the ratchet is the only mechanism §17 of the recipe relies on to stop the decomposed controllers regrowing into god modules; a 16-row-red ratchet that CI tolerates is inert, and `library_notes_controller.py` has regrown 20% past its pin in four days.
- Recommended correction: re-pin all 16 rows at their measured counts in one commit per the test's own dated guidance, and — since the notes controller is now the largest file in the slice at 6365 — open the §1 per-subsystem move series for it. Not a redesign; the recipe already prescribes the shape.
- Size: S (re-pin) · ADR: yes (`backlog/docs/library-decomposition-recipe.md` §17 governs; no new ADR) · Confidence: verified
- Pinning test: `Tests/Architecture/test_library_modules_size_ratchet.py::test_controller_does_not_grow_past_its_budget[...]` — states the requirement (it is the test that is red)
- Already covered: none for the re-pin; task-32199 (test-health after the notes decomposition) is adjacent — cite it there rather than filing a duplicate

### P2 [D1] — the lasting-sync abandon path is the only `except Exception: pass` in the slice with no diagnostic at all, and the awaited call it swallows sits BEFORE the six `pop()`s that release the root's lease
- Where: `tldw_chatbook/UI/Library_Modules/library_notes_sync_controller.py:1175-1178` (`abandon_setup`); the swallowed body is `tldw_chatbook/Notes/notes_sync_runtime.py:2379-2400`
- Evidence: read only — see UNVERIFIED. `abandon_setup` does `try: await self._runtime.abandon_setup(root_id) except Exception: pass` — no `logger` call of any kind, unlike the other five `except …: pass` sites in this slice, each of which carries a comment naming a specific narrow exception (see Verified-fine). In the runtime, `abandon_setup` → `_release_setup_authority`, whose FIRST statement is `await self._maintenance_offload(self._coordinator.close_admission, lease, …)` (`:2392-2394`) and whose next six statements pop `self._leases`, `_admissions`, `_admission_reasons`, `_setup_reviews`, `_root_paths`, `_root_status` (`:2395-2400`). A raise out of the offload therefore skips all six pops, leaving the runtime holding a provisional lease and admission for that root, and the controller swallows the evidence.
- Why it matters: this is the "the user pressed Back out of Add-from-files" path. A failure leaves an invisible held lease on a folder the user believes they abandoned, and nothing — no log line, no notice, no state — records that it happened.
- Recommended correction: log it (`logger.opt(exception=True).warning`, metadata only — a root path must not reach the sink; the file already follows that rule elsewhere), and release the in-memory maps in `_release_setup_authority` from a `finally` so a failed coordinator call cannot strand them.
- Size: S · ADR: no · Confidence: inferred
- Pinning test: none · Already covered: none

### P3 [D3] — the notes controller's `TYPE_CHECKING` block lost one import in the decomposition move: `NoteImportExecutor` is an undefined name in a return annotation — the one ruff fatal in the whole Tier-1 baseline
- Where: `tldw_chatbook/UI/Library_Modules/library_notes_controller.py:5009` (annotation), block at `:621-623`; the module it moved from still has it, `tldw_chatbook/UI/Screens/library_screen.py:572`
- Evidence: `ruff check --select E9,F63,F7,F82 tldw_chatbook/UI/Library_Modules/` → `library_notes_controller.py:5009:10: F821 Undefined name 'NoteImportExecutor'`. Harmless at runtime: `from __future__ import annotations` is at `:501`, and `cd $WT && source $SCRATCH/env.sh && PYTHONPATH=$WT $PY -c "import …library_notes_controller"` → `import OK` (the class resolves). The runtime import lives function-locally at `:5013`. `rg get_type_hints tldw_chatbook/` → 2 hits, both `MCP/gateway_runtime.py`, so nothing ever evaluates this annotation.
- Why it matters: it is the single fatal-class ruff hit in the entire Tier-1 baseline, so it costs every future lint run a documented exception; one line removes the exception permanently.
- Recommended correction: add `from ...Notes.note_import_executor import NoteImportExecutor` to the `if TYPE_CHECKING:` block at `:621`, matching `library_screen.py:572`.
- Size: S · ADR: no · Confidence: verified
- Pinning test: none · Already covered: none

### P3 [D3] — a two-press destructive confirmation is branched on the rendered Button label, with the authoritative flag 200 lines up in the same file
- Where: `tldw_chatbook/UI/Library_Modules/library_character_repair_controller.py:437` (`if button.label.plain == "Repair":`), label writers at `:392, 410, 452, 488, 493`; the real flag is `self._confirmation_requested` (`:82, 107, 188, 207, 215, 221, 243`)
- Evidence: `rg -n '\.plain\b' tldw_chatbook/UI/Library_Modules/` → exactly two hits; the other (`canvas_sync.py:250`) is a COMMENT forbidding this exact read ("both `.plain` and Textual 8's `str(Content)` return RENDERED text, stripping the escape_markup()… PR #665 review"). The destructive commit is NOT bypassable: `prepare_confirmed_repair` (`:236-249`) re-checks `if not self._confirmation_requested … return None`, and the handler's else-branch (`:448-452`) resets the label and returns on `None`. So a mis-read costs a silently dead first press, not an unconfirmed repair.
- Why it matters: it is the read the sibling module documents as unsafe, applied to a confirmation gate. Today the two labels are code-set literals so nothing is rendered away; the moment this button joins the marker family (`library_disabled_action_label` prepends "○ " to every other gated Library action) `.plain` returns `"○ Repair"`, the comparison silently fails, and the first press does nothing with no feedback.
- Recommended correction: expose `confirmation_requested` as a read-only property on the controller (the flag exists; `request_confirmation()` cannot be reused because it also SETS) and branch on it.
- Size: S · ADR: no · Confidence: verified
- Pinning test: none · Already covered: none

### P3 [D4] — `_next_request_token` and the modal-dismissal clone are re-rolls of surfaces that already have a canonical home; the skills/prompt browse-controller pair is one generic controller written twice
- Where: `_next_request_token` at `prompt_history.py:79`, `prompt_collections.py:157`, `library_skills_browse_controller.py:185`, `library_prompt_browse_controller.py:137` (+ `Widgets/Console/console_side_chat_modal.py:205` as `_next_request_id`); the dismissal clone at `skill_import_choice_modal.py:100` (`cancel`) and `prompt_collection_manager_modal.py:639/643` (`_cancel_button` / `_perform_safe_cancel`); the browse pair at `library_skills_browse_controller.py:101/204/240/401` vs `library_prompt_browse_controller.py:77/149/176/277`
- Evidence: read (not just grepped). The excerpt's `dup_verbatim` rows report 5 copies of the token helper and a 30-copy / 14-copy dismissal cluster spanning 24 and 13 files. `Widgets/modal_dismissal.SafeModalDismissMixin` has 79 importers and is the canonical dismissal seam; `prompt_collection_manager_modal.py` names its own helper `_perform_safe_cancel`, i.e. it re-rolls the mixin's contract under the mixin's own vocabulary. The skills/prompt browse pair carries four byte-identical method pairs (`pager`, `dispatch`, `request`, `invalidate`).
- Why it matters: consistency only — no behavioural drift found in any copy read; each `_next_request_token` is `self._token += 1; return self._token` against its own counter, and the two dismissal sites dismiss with the same argument shape. The browse pair is the stronger signal: two files, one contract.
- Recommended correction: leave the token helpers (3 lines each; no canonical home worth creating). Adopt `SafeModalDismissMixin` in the two modals. The browse-controller pair deserves a shared base, but that is a decomposition-series decision, not a review recommendation.
- Size: S (modals) / M (browse pair) · ADR: no · Confidence: verified
- Pinning test: none · Already covered: none

## Candidate dispositions
| candidate (file:line pattern) | confirmed / retired (why) / unverified (check) |
|---|---|
| ruff F821 `library_notes_controller.py:5009 NoteImportExecutor` | **confirmed** — P3 above |
| id_keyed_dict `library_notes_controller.py:2736 owners[id(owner)]` | **retired** — see Retired |
| dotted_section_setting `library_notes_controller.py:5163 library.notes_sync` | **retired** — `browse_start_directory(get_cli_setting(...))` with a validated fallback chain; confirmed by reading `_library_notes_sync_browse_location:5151-5165` |
| strftime `library_notes_controller.py:3597` / `library_notes_sync_controller.py:901` `%Y-%m-%d %H:%M` | **retired** — both are DISPLAY labels for a file mtime, built tz-aware then localised, matching `_absolute_local_label`'s spelling by design (docstring `:3589-3593`). Not the SQLite `%f` shape. |
| plain_readback `canvas_sync.py:250 .plain` | **retired** — a COMMENT forbidding the read, not a read |
| plain_readback `library_character_repair_controller.py:437 .label.plain` | **confirmed** — P3 above |
| except_exception_pass `library_export_controller.py:1102/1114/1124` | **retired** — all three are `call_from_thread` marshals with a comment naming the exact shutdown races (`RuntimeError` / Textual's `NoApp`); a worker thread must not die on teardown |
| except_exception_pass `library_ingest_controller.py:2047` | **retired** — `worker.cancel()` on a worker that may already have finished; the generation bump beside it (`_invalidate_library_ingest_preflight:2051-2064`) is the real fence and it is unconditional |
| except_exception_pass `library_prompts_controller.py:4024` | **retired** — `self.workers` on an unmounted screen raises `NoActiveAppError`/`LookupError`; the app-owned manager is still scanned |
| except_exception_pass `library_notes_sync_controller.py:1177` | **confirmed** — P2 [D1] above |
| except_exception_return (7 files, 13 sites) | **retired** — every one read; all are `query_one`/service-probe guards returning a typed "unavailable" value, each with a logger or a comment. No persistence write is among them. |
| run_worker_coroutine (12 files; 96 `run_worker` sites overall) | **retired** — see Verified-fine. No coroutine worker in the slice performs sync sqlite or blocking file I/O on the loop. |
| `library_notes_controller.py:5170` `run_worker(lambda …)` | **retired** — carries `thread=True` (`:5178`). Without it Textual 8 raises `WorkerError("Request to run a non-async function as an async worker")` (`textual/worker.py:342`), so the shape is not merely fine, it is required. |
| set_timer/set_interval (6 sites) | **retired** — see Verified-fine |
| try_import_guard `library_media_controller.py:2516`, `library_skills_controller.py:925` | **retired** — `:2516` is a function-body import of the first-party `Widgets/Library/library_media_image_preview` inside a worker coroutine whose one broad `except Exception` logs and degrades the preview only; not an `optional_deps` bypass |
| function_body_import (9 files, 36 imports) | **retired** — all 36 resolve; `importlib.util.find_spec` over every one returned `unresolvable: 0` |
| dup_verbatim `_next_request_token` ×4 + `cancel`/`_cancel_button` cluster + the 4 browse-pair methods | **confirmed as P3 [D4]** above (no behavioural drift found) |
| dup_shape `_notify_library_*_warning` ×9 (3 files), `_mirror_library_*_reader_preference` ×5 | **retired as not-worth-filing** — the notify family is a 4-line `getattr(app_instance,"notify")` + `notify(msg, severity=…)`; the mirror family differs per subsystem in its config section AND, for Folder-Files, in its config KEY (`files_tree_open` vs `items_open`, `library_notes_controller.py:2598`). A shared helper would need the section map passed in — the same five lines. |
| dup_shape `_skills_context_or_none` / `_prompts_count_or_none` / `_notes_true_count_or_none` | **retired** — all three read; each returns a different type and a different fallback story, and each docstring states its own degrade contract. Same shape, different contracts. |
| dup_shape `_consume_library_media_find_focus` / `_consume_library_skill_scroll_pending` / `consume_release_click` | **retired** — a 3-line read-and-clear one-shot token; three different tokens, no shared home worth creating |
| legacy_markers (13 files, 56 hits) | **not examined** — no candidate row named a line; the markers are `task-NNNNN`/`TASK-NNNNN` provenance comments, this codebase's standard annotation |
| `screen_helpers.py:194 _record_value` (5-file `_field`/`_value` cluster) | **retired** — read; a 3-line Mapping-or-attribute reader. The five copies read three different shapes (`Mapping` only, `Mapping`+attr, dataclass) and none has drifted in a way that reaches storage or the wire. |
| `library_media_browse_controller.py:99 _PATH_TOKEN_PATTERN = re.compile(` | **retired** — module level, not a function body |
| `library_rag_search_controller.py:1663/1670/1849/1850` unguarded `query_one` | **unverified (check)** — see Left UNVERIFIED |
| `library_collections_controller.py` 12 unguarded `query_one` (`:760/763/766/769/1022/1027/1032/1035/1372/1375/1439/1461`) | **retired** — mapped to `_capture_library_collection_quick_capture_draft`, `_library_collection_capture_filter_request` and three `async save/link_library_collection_capture_*` handlers. All five run from a press ON the widgets they query, in the same synchronous turn or one guarded `await` later. |
| `library_character_repair_controller.py` 21 unguarded `query_one` | **retired** — every one is inside the `ModalScreen`'s own composed subtree while that modal is mounted; `_apply_owned`'s post-await queries (`:473-493`) are additionally gated on `self._operation_token is token and self.is_mounted` (`:481`, `:488`) |
| seed_name `library_character_repair_controller.py:499 _cancel` / `:496 set_timer(0.01, refresh.focus)` | **retired** — `Widget.focus()` defers through `app.call_later` and catches `NoScreen` (`textual/widget.py::focus`), so a detached target is a no-op, not a raise |
| `library_browse_route_swap.py:220`, `prompt_collection_manager_modal.py:475`, `skill_import_choice_modal.py:86/93` unguarded `query_one` | **retired** — `:220` is inside `_apply_library_notes_source_strip`, reached only from the swap that just mounted the strip; the other three are modal `on_mount`/composed-subtree reads |
| seed_name `_safe_text` ×4 (`media:1081`, `rag:630`, `prompts:726`, `ingest:885`) | **retired** — all four are the SAME class-level binding of one shared screen helper, the shape the conversations controller's docstring documents; not four implementations |
| `library_prompts_controller.py:1652` `self.refresh(recompose=True)` in a file-picker callback | **confirmed** — folded into P2 [D3] as its clearest single site |

## Verified-fine
- **No coroutine worker in this slice runs sync sqlite or blocking file I/O on the event loop**, despite 96 `run_worker(` sites and only 4 `thread=True`. Every one is either (a) awaiting a scope-service method through `self._run_library_service_call(..., isolate_in_worker=True)` — the seam's own thread offload, 25 sites across 12 files — (b) awaiting an already-async coordinator (`_library_notes_sync_controller.*`, `_library_note_import_controller.check()`, `_library_prompt_history_controller.restore`), or (c) a `@property` pass-through to a screen-resident body outside this slice (`library_notes_controller.py:1161/1293/1329/1261` — `_create_library_note`, `_refresh_library_note_detail`, `_save_library_note`, `_locate_library_notes_tree_target` are forwarders, not implementations). The one genuinely blocking call found inside a coroutine, `os.stat`, is already wrapped: `library_notes_controller.py:3569` → `await asyncio.to_thread(self._note_file_written_label, path)`. The one zip read (`library_export_controller.py:1051 _read_export_artifact_facts`) runs on a worker THREAD — its caller carries `asyncio.run(  # policy-exception: worker-thread loop` at `:1054`.
- **All six `set_timer` callbacks are safe.** `library_media_controller.py:1493/2224/2291`, `library_prompts_controller.py:1385` and `library_ingest_controller.py:1655` all fire generation/token-fenced callbacks (`_expire_library_media_return_settlement:1581-1585`, `_dispatch_library_media_detail_request:2155-2161`, the prompts `dispatch` closure's `request_token`) whose first statement is the fence; none does an unguarded `query_one`. `library_character_repair_controller.py:496` is retired above.
- **`library_notes_controller.py:2741-2752`'s `self.watch(owner, "scroll_x"/"scroll_y", partial(...))` does not leak.** Textual stores watchers on the WATCHED object (`textual/reactive.py::_watch` → `setattr(obj, "__watchers", {})`), not on the watcher, so an unmounted scroll owner takes its watcher list with it. The `owner._library_notes_scroll_observed` flag is load-bearing, not redundant: Textual's own de-dup is `any(callback == callback_from_list …)`, which cannot match two distinct `functools.partial` objects.
- **48 `_`-prefixed names cross the `Library_Modules` → `Screens` package boundary** (8 `from ..Library_Modules.<mod> import _name` statements in `library_screen.py`: `:511, 579, 743, 818, 836, 837, 843, 11223`). This is exactly the D3 "private helpers imported across packages" smell and is **out of scope by contract**: `Tests/Architecture/test_library_support_layer_surface.py:33 _SURFACE = {…}` pins the screen's module surface by exact name, and the decomposition recipe's PR-0a re-export contract is what requires the moved names to stay reachable under their original spelling (`canvas_sync.py:7-8` states it; the recipe's §12/§20 record two series that tripped it). Nothing in `Library_Modules` imports a private name from OUTSIDE its own package (AST scan: 0 hits).
- **No module-level `threading.Lock` + raw SQL anywhere in the slice.** The only `threading` uses are `threading.Event` cancel flags (`library_note_import_controller.py:137/675/698`, `library_export_controller.py:930`, `library_export_state.py:131`) and one `threading.Lock` field on a state dataclass (`library_ingest_state.py:118 backend_save_lock`) — all correct.
- **`_note_word_count` (`library_notes_controller.py:3190-3205`) compiles no regex per call** — `re.finditer(r"\S+", content)` uses the module cache, and the docstring records why it is not `len(content.split())`. The only `re.compile` inside a function body across the slice is a docstring quotation (`screen_helpers.py:137`); the real one (`library_media_browse_controller.py:99`) is module-level.
- **`get_cli_setting` is read once per user gesture, never per keystroke, anywhere in this slice.** Only two live call sites exist in 47k lines (`library_notes_controller.py:5163`, in a folder-picker opener; the seven `library_rag_search_controller.py` hits are all prose in a module docstring explaining a monkeypatch-routing constraint). The sibling finding's ≈11 ms per call therefore costs at most one folder-picker open here.
- **`canvas_sync._sync_library_canvas`'s blanket `except Exception` (`:1007`) is a decision, not a smell** — its comment records that narrowing to `(NoMatches, QueryError)` was implemented, measured and reverted because `Tests/UI/test_library_canvas_sync_defects.py::test_strict_failure_retry_retains_original_semantic_focus` forces a `RuntimeError` out of `canvas.sync_state` and requires the swallow (`assert attempts == 2`). A pinning test whose name states the behaviour.
- **`canvas_sync._apply_library_row_toggle` (`:248-256`) is the codebase's best-documented refusal of the `.plain` readback** — it rebuilds a row label from a compose-time stash precisely because `.plain` and `str(Content)` strip `escape_markup`. It is the positive control for the P3 finding above.
- **`library_media_controller.py:4191 handle_library_media_open_original`** hands a stored URL to `webbrowser.open` behind `url.startswith(("http://","https://"))`. Not an `Utils/egress` bypass: nothing is fetched by the app; the scheme allowlist blocks `file:`/`javascript:`. Synchronous, but one explicit user gesture.
- **`library_export_controller.py` validates its dialog-returned destination through the canonical helper** — `from ...Utils.path_validation import validate_path_simple` (`:257`), applied at `:1380` and `:1382`. No inline traversal check anywhere in the slice.
- **`library_prompts_controller.py:4163 _await_library_prompt_durable_call` swallows `CancelledError` deliberately** ("Drain an admitted Prompt write even if its worker is cancelled") — durability chosen over cancellability, stated in the docstring. Not a hang: each `Task.cancel()` delivers exactly one `CancelledError`, after which the re-`await asyncio.shield(task)` suspends normally.

## Retired
- **`library_notes_controller.py:2736 owners[id(owner)]` — symptom real (an `id()`-keyed dict), cause wrong, retired.** The dict is FUNCTION-LOCAL, built and consumed inside `_install_library_notes_scroll_observers` (`:2732-2752`); it maps `id(owner) -> owner`, i.e. it holds a strong reference to every key's own object for the dict's entire (single-statement) lifetime, so no id can be recycled while it is live. It de-duplicates the ≤8 region selectors that can resolve to one widget; it is not a cache keyed across recompose. Matches the sibling finding that all seven such maps elsewhere are safe.
- **"The Media Reader loads the review set 2–4× per `]` press" — over-counted; retired to the measured 2.** `_build_library_media_reader:3754` is NOT on the in-place sync path: `_sync_library_media_viewer_state`'s changed branch (`:3980-4058`) patches viewer attributes and calls `viewer.refresh(recompose=True)`; the builder is reached only from compose and the route swap. Steady-state traversal is 2 loads (walk + banner), Reader entry is 3. `_decorate_with_review_state` is memoised and contributes 0.
- **"A `run_worker(lambda …)` with no `thread=True` at `library_notes_controller.py:5170` would raise `WorkerError`" — raised, then retired on reading two lines further.** `thread=True` is at `:5178`.
- **"Prompts' whole-screen recomposes remount the armed editor and re-dirty a just-saved prompt" — raised from the file's own warning (`:2415`, `:3286-3294`), then retired.** Every editor-side recompose site sets `_library_prompt_editor_armed = False` before the recompose and re-arms through `call_after_refresh(self._arm_library_prompt_editor)` after it (`:3351-3354`, `:3406-3409`, `:3903-3906`, `:2584-2587`). The hazard is real and they handled it; only the cost survives, as P2 [D3].
- **"The character-repair confirmation can be bypassed by a mis-read label" — retired with evidence.** `prepare_confirmed_repair` (`:236-249`) re-checks `_confirmation_requested` and returns `None`, and the caller's `if admitted is None: button.label = "Repair"; return` (`:449-452`) resets. The cost is a dead press, not an unconfirmed repair — filed at P3 accordingly.
- **"`_read_export_artifact_facts` decompresses a zip manifest on the event loop"** — retired: its one caller (`:1051`) is inside the export worker-thread function, evidenced by the `asyncio.run(  # policy-exception: worker-thread loop` three lines below it.

## Left UNVERIFIED
| claim | why not verified | literal command to run |
|---|---|---|
| P2 [D1] prompt-save concurrency check: a raising `get_prompt` pre-read lets the save proceed and overwrite a concurrent version bump | Constructing a `LibraryPromptsController` needs ~100 keyword dependencies; the screen-level path needs a mounted `LibraryScreen`. Not reproducible under the read-only/no-app rules at proportionate cost. The claim is a three-line control-flow read (`:3124` → `:3132` → `:3133`), not an inference from an aggregate. | `cd $WT && source $SCRATCH/env.sh && PYTHONPATH=$WT $PY -m pytest Tests/UI/test_library_prompt_save.py -q` after adding a test that patches `prompt_scope_service.get_prompt` to raise on the staleness call while `save_prompt` records, then asserts `_enter_library_prompt_conflict` ran |
| P2 [D1] notes-sync abandon: a raising `close_admission` strands the root's lease and admission maps | Same — needs a runtime whose coordinator fails; `_maintenance_offload`'s own failure semantics were not exercised | `cd $WT && source $SCRATCH/env.sh && PYTHONPATH=$WT $PY -m pytest Tests/Notes/test_notes_sync_runtime.py -q` after adding a test whose `coordinator.close_admission` raises, then asserting `runtime._leases` no longer holds `root_id` |
| `library_rag_search_controller.py:1663/1670/1849/1850` unguarded `query_one` can raise `NoMatches` out of a worker if the Search route is left mid-refresh | The screen-side dispatcher (`library_screen.py:35080-35092`) re-checks route + panel presence INSIDE `panel_refresh_lock` and its comment states this exact hazard ("Every `query_one` below would then raise `NoMatches` out of whichever worker called us"). The residual gap is only the window between that one re-check and the ~6 awaited sub-refreshes that follow, each containing several `await widget.remove()`/`mount()` yield points. Whether a route change can land in that window needs a live drive. | `.claude/skills/verify/SKILL.md` recipe: `tmux -L verify new-session -d …`, open Library ▸ Search, run a query returning ≥50 results, press the rail's Media row during the results paint, then `tmux -L verify capture-pane -p` and check the log for `NoMatches` / an app exit |
| `library_skills_controller.py` (3145) and `library_conversations_controller.py` (1800) carry no finding | Covered by the mechanical sweeps only (no line-by-line read). Both are clean on every pattern class swept: 0 unguarded `query_one`, 0 `self.refresh(recompose=True)`, 13 and 7 service offloads respectively, all function-body imports resolve, no timers, no `.plain`. A reading-level defect in either would not have been found. | `cd $WT && source $SCRATCH/env.sh && PYTHONPATH=$WT $PY -m pytest Tests/UI/test_library_skills_wiring.py Tests/Architecture/test_library_collections_wiring.py -q`, plus a line-by-line read of both moved-body regions |
| `library_prompts_controller.py:4180-5063` (the conflict-resolution tail, 6 of the 16 recompose sites) and `library_media_controller.py:1-1130` / `library_prompts_controller.py:1-779` (docstring + `__init__`) | Read by targeted extract only, not line-by-line; the AST sweeps cover them for every pattern class but not for logic defects | a line-by-line read of those three ranges |
