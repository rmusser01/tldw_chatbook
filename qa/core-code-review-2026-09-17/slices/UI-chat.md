# UI-chat — tldw_chatbook/UI/Screens/chat_screen.py, 25215 lines

Worktree `/Users/macbook-dev/Documents/GitHub/tldw-review` @ d8fb4053f9. Probes live in `<SCRATCH>/probes/` (review evidence only, not repo tests).

## Coverage
| file | lines | read in full / sampled (which ranges) / mechanical only |
|---|---|---|
| tldw_chatbook/UI/Screens/chat_screen.py | 25215 | **Read in full** (lines 1–25215, sequential ~1400-line chunks). Mechanical passes on top: AST census (class/method/duplicate-def), AST resolution of every function-body import, AST list of every `run_worker` call + kwargs, grep of `set_interval`/`set_timer`/`except Exception`/`recompose`/`id(`/class attrs. Supporting files opened only to settle claims: `UI/Console_Modules/left_rail.py` (445, 2005–2330, 2662), `UI/Console_Modules/video.py` (968–984, 1296–1312), `UI/Console_Modules/wiring.py` (800–812, 1074–1081), `UI/Console_Modules/workspace.py` (506–647, 4861), `Chat/console_session_settings.py` (763–775, 1780–1850), `Chat/console_chat_store.py` (10201–10360), `Chat/console_display_state.py` (285–291), `Tests/Architecture/test_screen_size_ratchet.py`, `Tests/UI/test_console_model_section*.py`, `Tests/Architecture/test_timer_path_static_update_inventory.py` (195–240), `.github/workflows/test.yml` (112–132). |

### Structural facts (mechanical, worktree d8fb4053f9)
- `ChatScreen` spans lines 1758–25206: **762 method defs, 728 unique names** (34 duplicates: 32 are legitimate `@property`/`@x.setter` pairs at 9954–10847; **2 are real shadowed re-definitions** — finding 4). 17 module-level functions; module-scope imports end at line 696.
- **Function-body imports: 133** (AST, nested functions included; the excerpt counted 113). Every `tldw_chatbook.*` target exists on disk; the stdlib/third-party ones verified importable (`textual.app.ScreenStackError`, `textual_image.widget.Image as _GraphicsImage`, `Character_Chat.Chat_Dictionary_Lib as cdl`, `pydantic.ValidationError`, `aiofiles`). **None targets a module that no longer exists.** Classification: 5 redundant with a module-scope import (5205 `ConsoleLeftRail`, 7067/7095 `asyncio`, 7088/7123 `Path`); 2 look redundant but are TYPE_CHECKING-only at module scope so are real runtime imports (8517 `ConsoleEnvironmentController`, 8574 `EnvironmentSnapshot`); the remaining ~126 are ADR-097 boot-ratchet deferrals documented in-line (`_conversation_settings_modal_module`, `console_conversation_action_menu` ×7, `console_workspace_action_menu` ×5, `console_environment_state` ×5, `attachment_core` ×4, …) or cycle-breakers (`Console_Modules.wiring`, `change_review_screen`).
- **`run_worker`: 75 calls** (AST; excerpt said 68). Every `exclusive=True` carries a `group=` (0 violations). 1 `thread=True` (11568). 5 pass a bound method uncalled (14096, 14152, 17677, 24431, 24447 — Textual accepts a callable). 4 use `exit_on_error=False` (8524, 19641, 20691, 22864). The rest are coroutines.
- **Coroutine workers whose body does sync sqlite/file I/O on the loop** (item 1): audited every coroutine body. Off-loop discipline is good — durable reads/writes go through `asyncio.to_thread` (7071/7097 markdown render + paginated read, 10135 image prep, 12445 character options, 12824 card read, 14461/14532/14553/14594/14620/14648 dictionary/world-book, 18014 citation counts, 20285/20326/20361 attachments, 20656/20714 turn undo, 22918/23017/23043 annotations, 23709 watchlists) or `@work(thread=True)` (13036/13092/13206/13308 config writes). The residual on-loop sqlite in this file: (a) `_console_conversation_state` 6619 + `_console_target_has_messages` 6997 — two PK reads per row-menu open inside the `_open_console_*_action_menu` coroutine workers (documented, bounded); (b) 19882 `controller._durable_context_snapshots(session_id)` called synchronously from `_console_rewind_summary_disabled_reason` when `/rewind` opens — **cite CHAT-controller P2** (one more caller of that sync point-read path); (c) 7138 `mkdir` on the loop before an `aiofiles` write (trivial). No coroutine worker here runs a whole-table sqlite read on the loop; the screen never calls a controller durable-write path directly (no `_run_durable_db_call` in this file).
- **Size ratchet EXISTS** at `Tests/Architecture/test_screen_size_ratchet.py` (not `Tests/UI/`): row `("ChatScreen", 16966, 563)` + `_TASK_22507_4_CHAT_SCREEN_BASE = (20099, 633)`. It is **RED on dev**: `$PY -m pytest Tests/Architecture/test_screen_size_ratchet.py -q` → `3 failed, 2 passed` (`assert 25215 <= 20099`; the library_screen row is red too). `.github/workflows/test.yml:121` core-tests runs `pytest Tests --ignore=Tests/UI`, which collects it. See finding 5.

## Findings

### P1 [D1] — The left rail's Model section (Temperature / Max tokens) never updates after compose: the screen's updater queries two widgets TASK-23196 deleted, raises `NoMatches` on the first, and the real rows' writes are unreachable
- Where: `tldw_chatbook/UI/Screens/chat_screen.py:9404-9431` (`_apply_console_settings_summary_state`); compose-time render `UI/Console_Modules/left_rail.py:2281-2320`; the rail's state is set only in `left_rail.py:445` (`__init__`) with no sync method (`grep -n "_settings_summary_state" left_rail.py` → 445, 2275 only).
- Evidence: `grep -rn "console-model-section-provider\|console-model-section-model\b" tldw_chatbook/` → only chat_screen.py:9416/9420 (no widget composes them); `Tests/UI/test_console_model_section.py:31-32` and `test_console_model_section_dedup.py:40-43` assert they are ABSENT. Probe `<SCRATCH>/probes/test_probe_model_section_stale.py` (ConsoleHarness 160×44; patched `_build_console_settings_summary_state` → `temperature="9.87"`, called `_sync_console_settings_summary()`): `PROBE before='0.60' after='0.60' provider_rows=[]` → `AssertionError: row did not update`. The `try` at 9414 dies on its first `query_one` (`#console-model-section-provider …`), so the temperature/max_tokens `.update()` calls at 9422–9429 never execute; the regex parse at 9406–9411 (`re.search(r"T ([\d.]+)"…)`) is dead too.
- Why it matters: after the user changes temperature or max_tokens (Conversation settings, Alt+M popover, `/model`), the Context rail's Model section keeps the compose-time values until the rail happens to recompose — the rail shows one temperature while sends use another.
- Recommended correction (S): delete 9404–9431's provider/model queries and regex parse; write `summary_state.temperature or "—"` / `summary_state.max_tokens or "—"` (the structured fields TASK-32338 added to `ConsoleSettingsSummaryState`, already used by `left_rail.py:2281`) into `#console-model-section-temperature/-max-tokens .console-model-section-value` under their own guarded `try`; update the two pinned rows in `Tests/Architecture/test_timer_path_static_update_inventory.py:223-229` (they inventory the dead `query_one` expressions). Better shape: a `ConsoleLeftRail.sync_settings_summary(state)` next to `sync_sections`, called from `_apply_console_settings_summary_state`, so the rail owns its rows.
- Size: S · ADR: no · Confidence: **verified** (probe)
- Pinning test: `Tests/UI/test_console_model_section.py::test_model_sync_updates_rows` — its NAME asserts the refresh but its body asserts only that the row is non-empty (the compose value satisfies it), so it does not pin the behaviour. That file is red under the isolated review env for an unrelated bootstrap reason — see UNVERIFIED.
- Already covered: none (TASK-23196 removed the rows; TASK-32338 added the structured fields to the rail's compose path only).

### P1 [D2] — The 0.25 s credential-readiness poll costs ~49 ms per tick at idle: it rebuilds provider readiness outside any config-admission scope, so one tick performs six `load_settings()` admissions (~66 verified-directory opens)
- Where: `chat_screen.py:11966-11984` (`_poll_console_credential_readiness`), armed at 16805 `set_interval(0.25, …)` for the screen's whole life (`is_current`-gated), stopped only in `on_unmount` 16994 — NOT in `on_screen_suspend` (23476–23479 stop the other timers). The cost sits in `_provider_readiness_app_config` 7624–7659 → `load_settings()` → `Backup_Recovery.config_participants.operation` → `storage_admission.acquire_storage` → `private_paths._open_verified_parent`.
- Evidence: probe `<SCRATCH>/probes/test_probe_credential_poll_cost.py` (ConsoleHarness): `readiness_ms=41.4 poll_tick_ms=37.4 app_config_ms=11.7`. cProfile probe `test_probe_credential_poll_profile.py` over 20 ticks: `_poll_console_credential_readiness` cum 0.977 s (**49 ms/tick**); `_provider_readiness_app_config` **120 calls / 20 ticks = 6 per tick**, cum 0.953 s; `config_participants.operation` 120 calls; `storage_admission._acquire_storage` 120 calls cum 0.739 s; `private_paths._open_verified_parent` **1320 calls** (66 per tick) cum 0.361 s; `session._maybe_refresh_stale_default_console_settings` 40 calls cum 0.331 s. Snapshot was disk-loaded (`disk_loaded=True`), i.e. the production branch of 7646.
- Why it matters: with Console current and nothing happening, ≈49 ms of every 250 ms of event-loop time (≈20 %) re-derives readiness that almost never changes; the docstring's "cheap (cached)" claim for `load_settings()` is false under the backup-recovery admission wrapper (≈8 ms per call). The 0.2 s transcript tick was routed through ONE `operation(config)` per tick (`_run_console_config_sync` 21604) precisely to avoid this — the credential poll bypasses that seam.
- Recommended correction (S): (1) read `subscription_readiness_revision()` FIRST and return when it equals the cached revision — that is the only external signal this poll exists to notice; (2) when it changed, build readiness inside `with self._console_derivation_scope():` (memoises `app_config` for the pass: 6 → 1 admissions) or via `_run_console_config_sync`; (3) stop the timer in `on_screen_suspend` beside its siblings. Cross-slice: the per-call cost of `load_settings()` under `Backup_Recovery` admission belongs to ENTRY-config / Backup_Recovery — cite, don't fix here.
- Size: S · ADR: no · Confidence: **verified** (measured in harness; a real profile with keyring/network credentials can only be slower)
- Pinning test: none for the tick's cost; `Tests/Architecture/test_timer_path_static_update_inventory.py` inventories `.update()` receivers on timer paths, not this timer's body.
- Already covered: none (task-26834 covers tab-switch remount/CSS/title lookup, not this idle poll).

### P1 [D1] — `_open_video_with_os` is a class-body function with neither `self` nor `@staticmethod`: every production call raises `TypeError`, so "open video with the OS player" silently never works
- Where: `chat_screen.py:19648-19658`; wired at `UI/Console_Modules/wiring.py:808` (`open_video_with_os=lambda path: screen._open_video_with_os(path)`); called at `UI/Console_Modules/video.py:978` (external open after save) and `video.py:1308` (managed-open fallback when playback tools are missing) — both inside `except Exception` that logs `"Console video operation={} failed error_type=TypeError"` and returns.
- Evidence: `$PY -c` (isolated env): `ChatScreen.__dict__['_open_video_with_os']` is a plain `function` (not staticmethod); `ChatScreen.__new__(ChatScreen)._open_video_with_os(Path('/x.mp4'))` → `TypeError: ChatScreen._open_video_with_os() takes 1 positional argument but 2 were given`. `grep -rn "_open_video_with_os" Tests/` → every test injects a stub (`open_video_with_os=lambda _path: None`, `harness._open_video_with_os = fail_open`); none exercises the real method.
- Why it matters: the user asks to open a generated video externally, nothing opens, one warning line lands in the log; the feature shipped dead and every test is green because the seam is always mocked (the `lessons-testing-evidence.md` "fakes mask real signatures" class).
- Recommended correction (S): add `@staticmethod` (the body never uses `self`); give `Tests/Chat/test_console_video_controller.py` one case that wires the REAL `ChatScreen._open_video_with_os` with `subprocess.Popen` patched so the signature is exercised.
- Size: S · ADR: no · Confidence: **verified**
- Pinning test: none (all stubbed).
- Already covered: none.

### P2 [D3] — Two methods are defined twice in the class body; the first `on_button_pressed` (200 lines, an older divergent row-open implementation) and the first `_restore_collapsible_states` are silently shadowed dead code
- Where: `on_button_pressed` at `chat_screen.py:24462-24662` (dead) and `24893-25079` (live); `_restore_collapsible_states` at `24844-24870` (dead) and `25089-25115` (live, byte-identical); nine blank lines 25080–25088 between them (merge artifact).
- Evidence: AST census of `ChatScreen.body` → `on_button_pressed x 2 at [24462, 24893]`, `_restore_collapsible_states x 2 at [24844, 25089]` (every other duplicate name is a `@property`/`.setter` pair). The file's own comment at 22714 names this exact trap ("The two also must not share a method NAME: the second definition would silently replace the first"). The dead copy's `console-workspace-conversation-` branch (24554–24646: task-457(b) loading flag + TASK-717 broken-row marking inline) was superseded by `_workspace.open_console_workspace_conversation` — `workspace.py:4861` carries the same loading/broken logic through wiring 1074/1081 — so behaviour is preserved by the live copy; the first copy is dead, not lost.
- Why it matters: an edit to the first copy is inert with no error (which is how a prior handler died, per the 22714 comment); `Tests/UI/test_console_button_routing.py`'s "19 branches" characterisation and the size ratchet count the wrong method; 230 dead lines on the largest file in the app.
- Recommended correction (S): delete 24462–24662, 24844–24870 and the blank block; add one assertion to `Tests/Architecture/test_screen_size_ratchet.py`'s existing AST walk — method names in the class body are unique once `.setter` pairs are excluded (ruff `F811` would also catch it, but is outside this review's allowed lint selection).
- Size: S · ADR: no · Confidence: **verified**
- Pinning test: `Tests/UI/test_console_button_routing.py` presses real buttons, i.e. characterises the LIVE copy; deleting the dead copy cannot change its result.
- Already covered: none.

### P2 [D3] — God module whose size ratchet is red on dev and collected by the core-tests job: 25,215 lines / 762 methods against a 16,966 / 563 budget (and 20,099 / 633 for the task-22507 base)
- Where: whole file; `Tests/Architecture/test_screen_size_ratchet.py:77` and `:894`; `.github/workflows/test.yml:121`.
- Evidence: `cd <worktree> && source <SCRATCH>/env.sh && $PY -m pytest Tests/Architecture/test_screen_size_ratchet.py -q -p no:cacheprovider` → `3 failed, 2 passed` — `test_screen_does_not_grow_past_its_budget[chat_screen.py]`, `[library_screen.py]`, `test_task_22507_4_does_not_worsen_chat_screen_base` (`assert 25215 <= 20099`). `test.yml:121` core-tests: `pytest Tests --ignore=Tests/UI …` (Tests/Architecture is collected; no marker skip in the test). `grep -rl "16966\|ratchet" backlog/tasks/` → no open task names the red.
- Responsibility clusters (line ranges, all read): imports 1–696 · module constants/helpers 697–1757 (turn-undo planner 970–1093, inspector exchanges loader 1585–1696, `_ControllerState` descriptor 1698, `_active_lineage_rows` 1021) · bindings/actions/focus 1758–2160 · left-rail/terminal/inspector-section events 2173–2810 · **settings-durability + default-intent state machine 2804–3320** · **Conversation-settings modal open/suspend/return handoff 3318–4190** · **roleplay projection persistence drain 4192–4780** · workbench help/F6 panes 4782–5245 · session switcher + model popover 5245–5605 · settings submission durability + default recovery 5606–6100 · palette actions 6101–6465 · row/workspace action menus + markdown export 6465–7240 · class attrs + `__init__` 7239–7560 · config memo / provider intents / vLLM handoff 7560–8340 · context estimate / control state 8338–8500 · **Environment/Tasks section + focus restore 8512–9330** · settings summary / agent section sync 9323–9620 · provider-selection derivation + runtime-handle properties 9626–10000 · store/controller/gateway ensure + view hooks 10000–10500 · **~60 proxy properties to controllers 10528–10860** · TTS/voice/impersonate/composer menu 10854–11340 · hands-free/realtime delegations 11341–11470 · collapse toggles 11466–11620 · control state + cost chip (`_build_console_cost_state` 240 lines) + timers 11621–12000 · retrieval scope / chips / inspector push 12002–12500 · character avatar render 12502–12800 · rail prefs persistence + onboarding flags 12792–13320 · **rail state/preferences/visibility 13320–14020** · workspace context sync 14022–14180 · inspector state build 14187–14400 · dictionary/world-book appliers + 4 attach/detach workers 14390–14690 · inspector rows / live-work cards 14684–15100 · readiness copy / blockers / setup modal 15015–15650 · live-work strip/swap/center builder 15649–16070 · **`compose_content` 16071–16632 (560 lines)** · on_mount / attach reconciliation / resume startup 16633–16960 · on_unmount 16954–17010 · state serialize/restore + attachment stash 17011–17430 · handoff consumers 17433–17690 · citation counts 17683–18030 · transcript fingerprint + `_sync_native_console_transcript` 18029–18345 · run copy / mode bar / sync maintenance 18355–18500 · **`_sync_native_console_chat_ui` + tabs + 0.2 s poll 18496–18810** · send pipeline 18811–19130 · **slash commands 19131–20070** · collapse/stop/attach/paste/clipboard/chatbook 20071–20460 · change review / turn undo / approval focus 20456–20890 · canvas bridge 20892–21310 · summarize range 21327–21500 · control-bar sync / coalescing / config-sync / popup 21497–21960 · composer undo/redo + action state 21956–22270 · resize / rail-collapse notice / focus frame 22268–22450 · **`on_key` 22447–22690** · selection / side-chat / review notes / paste / mouse 22690–23300 · compact shell sync 23294–23390 · suspend/resume 23396–23660 · watchlists ops 23656–23860 · task cards / questions / park approvals / run toasts / agent chat create 23853–24460 · dead `on_button_pressed` 24462–24662 · sidebar state persistence 24664–24843 · dead `_restore_collapsible_states` 24844–24870 · live `on_button_pressed` 24893–25079 · collapsibles 25089–25206 · lazy modal loader 25209.
- Why it matters: the ratchet's own docstring says a red budget "defeats the entire mechanism"; the file grew ~8,250 lines past it. Either the Core Tests job is red on dev and PRs are landing anyway, or the check is not required — both mean the governance is not governing.
- Recommended correction (M, then per-subsystem L): re-measure and decide (the docstring forbids raising) — the honest states are "raise once with a written waiver" or "cut below budget". Extraction candidates that own no pixels and can move verbatim under `backlog/docs/library-decomposition-recipe.md` §1/§2: the settings-durability/default-intent machine (2804–3320 + 5569–6100 ≈ 1,050 lines, pure state over `app_instance` attributes), the Conversation-settings suspend/return handoff (3318–4190), the roleplay persistence drain (4192–4780), and the Environment focus-restore cluster (8512–9330). Cite the recipe's §17 size governance; do not redesign.
- Size: M/L · ADR: yes — `backlog/docs/library-decomposition-recipe.md` (§1 per-subsystem PR series, §2 field-ownership script, §17 size governance) governs the shape; no ADR in `adr_list.txt` names chat_screen's size · Confidence: **verified** (the red + collection), inferred (cluster sizes are my line counts; whether Core Tests is a *required* check is UNVERIFIED)
- Pinning test: `Tests/Architecture/test_screen_size_ratchet.py` (red).
- Already covered: none found; task-2902/task-26834 are perf tasks, not size.

### P2 [D4a] — `_apply_console_settings_summary_state` re-derives temperature/max_tokens by regex from a formatted display string although the summary state carries them as structured fields
- Where: `chat_screen.py:9404-9411` (`re.search(r"T ([\d.]+)"…)`, `re.search(r"max_tokens (\d+)"…)` over `summary_state.sampling_row`) vs `Chat/console_session_settings.py:774-775` (`temperature: str`, `max_tokens: str` — "TASK-32338: structured fields … replace regex-parsing of the formatted sampling_row").
- Evidence: `grep -n "summary_state.temperature" UI/Console_Modules/left_rail.py` → 2281 uses the structured field; the screen path still parses. Same defect family as finding 1 (its dead half), listed separately because it is a helper-exists-ignored duplication that would survive a naive fix of finding 1.
- Why it matters: two derivations of one value; the regex silently yields "—" the moment the copy changes — exactly the failure TASK-32338 documented.
- Recommended correction (S): use the fields; delete the two `re.search` calls. Canonical home: `ConsoleSettingsSummaryState` (exists).
- Size: S · ADR: no · Confidence: verified (read + finding 1's probe) · Pinning test: none · Already covered: none.

### P3 [D1] — `_active_lineage_rows` swallows a failed `get_conversation_by_id` and silently exports EVERY branch of the conversation
- Where: `chat_screen.py:1031-1034` (`except Exception: record = None` → falls back to `rows`), reached from `_console_markdown_source_messages` 7045 (inside `asyncio.to_thread`, so not a loop stall). Also 1047 `return lineage if len(lineage) == len(seen) else lineage` — both branches identical (dead conditional).
- Evidence: read only.
- Why it matters: a transient DB error turns a "clean markdown" export into a dump of regenerate siblings and unselected variants with no log line and no toast.
- Recommended correction (S): `logger.warning` in the except; collapse 1047 to `return lineage`.
- Size: S · ADR: no · Confidence: inferred (literal check: make `db.get_conversation_by_id` raise under the `Tests/UI/test_console_conversation_markdown*` fixtures and assert the export is flagged) · Pinning test: none · Already covered: none.

### P3 [D3] — Five function-body imports re-import names already bound at module scope
- Where: 5205 `from tldw_chatbook.UI.Console_Modules.left_rail import ConsoleLeftRail` (module scope line 154), 7067/7095 `import asyncio` (line 8), 7088/7123 `from pathlib import Path` (line 14).
- Evidence: AST cross-check of function-body imports against module-scope names.
- Why it matters: noise only; the other ~126 body imports are documented ADR-097 deferrals and must stay.
- Recommended correction (S): delete the five lines.
- Size: S · ADR: no · Confidence: verified · Pinning test: none · Already covered: none.

### P3 [D4a] — `_safe_text` is the 12th local re-roll of a one-line wrapper over `Utils.input_validation.sanitize_string`
- Where: `chat_screen.py:17550` (closure inside `_stage_handoff_as_console_live_work`); `grep -rn "def _safe_text" tldw_chatbook/ | wc -l` → 12.
- Evidence: grep; each is `sanitize_string(str(value or ""), max_length=…).strip()`-shaped.
- Why it matters: 12 copies of the same coercion; a bound-policy change has to be found 12 times.
- Recommended correction (S): one `safe_text(value, max_length=500)` in `Utils/input_validation.py`; cross-slice with UTILS (the seed-name row suggests the census already flagged the family).
- Size: S · ADR: no · Confidence: verified (count) / inferred (drift not diffed) · Pinning test: none · Already covered: none found.

### P3 [D3] — Boilerplate handler pairs (20 shape clones) are exact 3-line delegations with no behavioural drift
- Where: `_console_retrieval_scope_open/clear_pressed` 12127/12134, chip `OpenRequested` handlers 12141–12229, `handle_console_composer_collapse/expand` 20109/20115, `handle_console_status_collapse/expand` 20121/20127, `handle_console_turn_file_card_*` 20619/20633, `handle_console_inspector_review_*` 20613/20855, `handle_console_run_library_rag` 15775, `handle_console_save_chatbook` 20430, `_note_console_follow_intent` 19118.
- Evidence: read all 20; each is `event.stop()` + one call. Textual's `@on(MessageType)` dispatch is the contract, so a table-driven dispatcher only helps the `Button.Pressed`-by-id family (collapse/expand ×4, run-library-rag, save-chatbook, review-changes/approval) — ~8 methods, ~40 lines.
- Recommended correction: P3, only inside an extraction wave.
- Size: S · ADR: no · Confidence: verified · Pinning test: `Tests/UI/test_console_button_routing.py` covers the id family · Already covered: none.

### P3 [D4b] — Four attach/detach picker workers are a 4-copy shape clone (dictionary attach/detach, world-book attach/detach); no drift found
- Where: `chat_screen.py:14443-14682` (each: guard conversation → load rows off-thread → `push_screen_wait(Picker)` → apply off-thread → refresh summary, `finally` clears the dialog flag).
- Evidence: read all four; only the loader/apply callables and copy strings differ.
- Recommended correction (S): one `_run_console_picker_flow(load, apply, picker_factory, copy)`; the dictionary pair already routes through `Event_Handlers/Chat_Events/chat_events_console_dictionaries.py`, the natural home.
- Size: S · ADR: no · Confidence: verified · Pinning test: `Tests/UI/test_console_*dictionar*` (not read) · Already covered: none.

### P3 [D3] — `asyncio.to_thread(lambda: asyncio.run(process_attachment_*(…)))` spins a fresh event loop in a worker thread to call an async helper
- Where: 20326–20332 and 20361–20363 (`Chat/attachment_core.process_attachment_bytes/path`).
- Evidence: read only.
- Why it matters: works, but an async API only ever called via `asyncio.run` in a thread is a sync function wearing `async def`; cross-slice with the CHAT reviewer of `attachment_core`.
- Recommended correction (S, cross-slice): expose sync variants in `attachment_core`; call them via `to_thread` directly.
- Size: S · ADR: no · Confidence: inferred · Pinning test: none · Already covered: none.

## Candidate dispositions
| candidate (file:line pattern) | confirmed / retired (why) / unverified (check) |
|---|---|
| dotted_section_setting `:10877 dictation.spoken_feedback` | **retired** — resolves since TASK-1771 (brief); not flagged |
| dup_shape 12161/12181/12194/12202/12224/15775/20430/20613/20855 (chip/button delegations) | **confirmed P3 boilerplate**, no drift |
| dup_shape 20619 `handle_console_turn_file_card_review_requested` | confirmed P3 boilerplate |
| dup_shape 12127/12134/12141/12153/12478 (`_console_retrieval_scope_*`, chips) | confirmed P3 boilerplate |
| dup_shape 20109/20115/20121/20127 (composer/status collapse/expand) | confirmed P3 boilerplate |
| dup_shape 12173 `_console_library_chip_activated` | confirmed P3 boilerplate |
| dup_shape 7879 `_config_section` ≈ `_mapping_value` (gateway 7036, session_settings 1949) | **confirmed P3 D4b** — three 2-line dict-or-`{}` coercers, 6 call sites here, no drift (all return `{}` on non-dict); canonical home a `Utils` config helper |
| dup_shape 19118 `_note_console_follow_intent` | retired — one-line delegation kept for wiring-by-name (documented) |
| except_exception_pass 2597, 2602 | **retired** — steering-bar `clear_draft`/`show_delivery_refusal` on a mid-recompose widget; UI-only, documented |
| except_exception_pass 3553 | retired — `await pop_result` after a failed modal push; the stack is re-checked synchronously below (documented) |
| except_exception_pass 19574 | **confirmed P3** — research `search_params`/`paper_search_fn` build failure silently degrades to empty params, no log |
| except_exception_pass 24452 | retired — last-resort re-sync inside an already-logged failure branch |
| except_exception_return_per_file (19) | examined all: 1172/3532/3545/3650/6989/6998/7688–7724/12619/12623/18117 are best-effort probes (screen stack, diagnostics monitor, widget size, mounted-ids read) — **retired**; 3199 `app_config` assignment — retired; 3692 `_reopen_suspended_console_settings` swallows a modal-open failure with no log — **confirmed P3** (add a debug log); 1031 `_active_lineage_rows` — **confirmed P3 D1** (finding) |
| function_body_import_per_file (113) | **confirmed count is 133** (AST incl. nested); 0 missing targets; 5 redundant (finding); rest ADR-097 deferrals — retired |
| legacy_markers_per_file (31) | examined: docstring/comment history, `legacy_model`/`legacy_provider` variables in the provider-selection derivation (9664–9876), the `_sync_console_legacy_workspace_context_aliases` transitional mount (14108); `_serialize_console_message`/`_restore_console_message`/`_rehydrate_*` (17011–17401) are documented "no production caller since task-15860, kept for tests" — **confirmed P3 dead-in-prod shims**, retire with their tests |
| plain_readback `:13535 str(row.label)` | **retired** — `ConsoleDisplayRow.label` is a `str` dataclass field (`console_display_state.py:288`), not a Textual `Label`; no un-escaping |
| raw_1024x1024 `:863` | **retired** — `REMOTE_IMAGE_MAX_BYTES = 8 * 1024 * 1024` is a byte cap, not an image dimension |
| raw_mkdir `:7138` | **retired** — `target_path` is `validate_path_simple(candidate, require_exists=False)`'s result; the write is `aiofiles`; only the `mkdir` is sync on the loop (trivial) |
| run_worker_coroutine_per_file (68) | **confirmed count is 75**; every coroutine body audited for on-loop sqlite — see "Structural facts"; 0 `exclusive=True` without `group=` |
| seed_name__safe_text `:17550` | **confirmed P3 D4a** — 12th local re-roll over `sanitize_string` (finding) |
| strftime `:20325 %Y%m%d-%H%M%S` | **retired** — clipboard attachment filename; local time is right |
| try_import_guard 11077/11083 (`_open_console_generate_image_modal`) | retired — optional backend/style lists, debug-logged, modal still opens |
| try_import_guard 12722/12747 (`_build_character_avatar_widget`) | retired — `textual_image` graphics path with documented pixels fallback; `textual_image.widget.Image` verified present in the venv |
| try_import_guard 13101 (`_prune_console_rail_preferences`) | retired — not an import guard; whole-body `except Exception` on a `@work(thread=True)` prune that logs a warning (correct: an escaping worker exception is fatal) |
| try_import_guard 19559 (`_console_command_research`) | = except_exception_pass 19574 — confirmed P3 |
| set_interval/set_timer callbacks with unguarded `query_one` (item 5) | examined all 30 sites (11941, 16721–16805, 18797, 21514, 23604–23631, 24691): every callback guards its `query_one` with `QueryError`/`NoMatches` or touches no DOM; `_poll_transcript` runs `_sync_native_console_chat_ui`, whose DOM reads are guarded and whose teardown race is closed at 18519/18624/18656 — **retired**. Non-timer `@on(Resize)` handler `_adapt_console_workspace_to_width` 22319–22322 does four unguarded `query_one` after a guarded grid check — verified-fine (rails/handles are yielded unconditionally with the grid at 16178–16473) |
| mutable class attributes / `id()`-keyed caches / whole-screen `recompose=True` (item 8) | class attrs 7254–7276, 7573–7582, 10493, 21827/21835 are `None`/`bool`/`int` defaults — retired; `sidebar_state = reactive(dict)` uses a factory — retired; `id(request)` dict 5289 is call-local and popped in `finally` — retired; `id(transcript)` 18288 is one element of a refresh KEY (comparison only; documented at 15189) — retired; `id(repository)` 17829/17838 is a token — retired; `refresh(recompose=True)` appears once, 15910, as the documented fallback when the shell is not mounted — retired |

## Verified-fine
- `run_worker(exclusive=True)` without `group=`: **0** of 75 (AST).
- Off-loop durable I/O: the file consistently uses `asyncio.to_thread` for DB/file reads and `@work(thread=True)` for config writes (list in "Structural facts").
- Module-level `threading.Lock` at 769 guards only the `@work(thread=True)` config-file write at 13045 (`save_setting_to_cli_config`), never raw SQL — the D1 lock+SQL pattern does not apply.
- `_console_composer_or_none` 10495: memo revalidated via `is_mounted and self in cached.ancestors_with_self` on every hit — a recompose cannot serve a stale node (TASK-15454).
- `_configure_console_copy_block` 15175: cache stored ON the widget instance, so a recycled `id()` cannot alias (documented).
- `_build_console_cost_state` 11657–11902: whole-transcript re-tokenisation is memoised (`TokenEstimateCache`) and the fingerprint recompute is revision-gated (task-15451) — the tick-path cost is already bounded here.
- `_sync_native_console_chat_ui` 18496: teardown race closed three ways (entry check, teardown-scoped `except`, `finally` re-arm check) — measured-and-documented; not a swallowed error.
- `get_cli_setting("character_chat", "enable_world_info", True)` 14434 is the `(section, key)` form; `get_cli_setting("dictation.spoken_feedback", …)` 10877 dotted form resolves (TASK-1771).
- `re.search` with literal patterns at 9406/9409 and `re.fullmatch` at 23828/23844 hit `re`'s module cache — not per-call compiles (and 9406/9409 are dead anyway, finding 1).
- `store.clear_pending_attachment` (singular, 20419) is a documented alias of `clear_pending_attachments` (`console_chat_store.py:10340`) — the "Clear" control clears the whole staged list as its toast implies.

## Retired
- "68 coroutine `run_worker`s block the loop with sqlite" (the excerpt's framing): symptom real (75 coroutine workers), cause wrong — every durable read/write inside them is already `to_thread`-ed; the only on-loop sqlite is two PK reads at row-menu open and the `/rewind` snapshot read that belongs to CHAT-controller P2. Evidence: coroutine-by-coroutine read of all 75 bodies.
- "`_provider_readiness_app_config` is cached and cheap" (docstring 7631): measured 8–12 ms per call under backup-recovery admission → became finding 2.
- "`test_model_sync_updates_rows` proves the Model-section sync path works": the test asserts only non-emptiness → retired as evidence; finding 1 stands on the probe.
- Excerpt rows plain_readback / raw_1024x1024 / raw_mkdir / strftime / dotted_section: retired with reasons in the table.

## Left UNVERIFIED
| claim | why not verified | literal command to run |
|---|---|---|
| The `Core Tests` job (`test.yml:53`, which collects `Tests/Architecture/`) is a *required* branch-protection check — i.e. dev is landing PRs over a red ratchet rather than the job being advisory | branch protection is not readable from the tree; I only verified collection | `cd /Users/macbook-dev/Documents/GitHub/tldw_chatbook && gh api repos/rmusser01/tldw_chatbook/branches/dev/protection --jq '.required_status_checks.contexts'` then `gh run list --workflow=test.yml --branch dev --limit 3 --json conclusion,headSha` |
| `Tests/UI/test_console_model_section.py` is green on dev (it is red under the isolated review HOME with `Backup_Recovery.bootstrap.RecoveryRequired: raw_source_selection_changed`; my probes using the same `ConsoleHarness` from a scratchpad rootdir pass, so the failure is `Tests/conftest.py` profile handling under the review env, not the product) | could not run against the real profile (ADR-126 recovery gate) | `cd /Users/macbook-dev/Documents/GitHub/tldw_chatbook && .venv/bin/python -m pytest Tests/UI/test_console_model_section.py -q -p no:cacheprovider` (main checkout, real profile) |
| Live surface of finding 1: change temperature via Alt+M and observe the Context rail's Model ▸ Temperature row not updating | do-not-run-the-app rule | `.claude/skills/verify/SKILL.md` recipe (main checkout): `tmux -L verify new-session -d -x 160 -y 44 'python3 -m tldw_chatbook.app'`; open Context rail ▸ Model (F6 → left rail, expand "Model"), note Temperature; `alt+m`, set temperature 0.99, Enter; `tmux -L verify capture-pane -p` and compare the Temperature row |
| Finding 2's magnitude on a REAL profile (keyring-backed credentials) — harness measured 37–49 ms/tick with the null keyring | harness only | `cd <worktree> && source <SCRATCH>/env.sh && PYTHONPATH=<worktree> $PY -m pytest <SCRATCH>/probes/test_probe_credential_poll_profile.py -q -s --rootdir=<worktree> -c <worktree>/pyproject.toml`, then repeat with `PYTHON_KEYRING_BACKEND` unset against a scratch profile holding an anthropic key |
| Cost of the two on-loop PK reads at row-menu open (6619/6997) on a large user DB | not timed | `cd <worktree> && source <SCRATCH>/env.sh && $PY - <<'EOF'` building a file-backed ChaChaNotes DB with ~10k conversations (see `Tests/DB/` fixtures) and timing `db.get_conversation_by_id(cid)` + `db.get_messages_for_conversation(cid, limit=1)` with `time.perf_counter()` |
