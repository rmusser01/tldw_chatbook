# S13-canvas — validation

Baseline check: `git diff --stat 3722a85748..HEAD -- tldw_chatbook/Canvas tldw_chatbook/Workspaces tldw_chatbook/Research_Workspace tldw_chatbook/Notes/notes_scope_service.py tldw_chatbook/MCP/redaction.py scripts/vendor_canvas_mermaid.py scripts/check_canvas_mermaid_assets.py 3722a85748..HEAD` → **empty diff**. None of this slice's files changed across the 25 post-review dev commits, so every verdict below rests on reading the code as committed at review time (no drift, no FIXED expected).

## 1. P1 [D1] — `ShadowRepo._locked()` spins forever on a non-empty stale lock dir
- Verdict: CONFIRMED
- Site now: `tldw_chatbook/Workspaces/change_tracking.py:308-352` (`_locked`'s `except FileExistsError` body) — unchanged from cited `:322-349`.
- Proof: read the method directly — the `age > _STALE_LOCK_SECONDS` branch does `logger.warning(...)`, `try: repo.lock_dir.rmdir() except OSError: pass`, then bare `continue` with **no `time.sleep()` and no `deadline` check** before the next loop iteration; the sibling `except OSError: continue` on the `stat()` call has the same shape. Reachability: `grep -n "ChangeTurnTracker\|_locked(" tldw_chatbook/Chat/console_runtime.py` → `ChangeTurnTracker()` instantiated at `:3534`, matching the review's cited call site exactly. Constants confirmed: `_STALE_LOCK_SECONDS=300.0`, `_LOCK_RETRY_SECONDS=0.05` (`change_tracking.py:98,101`).

## 2. P1 [D1] — `set_folder_binding_access` unlocked RMW races the locked exclusion editors
- Verdict: CONFIRMED
- Site now: `tldw_chatbook/Workspaces/registry_service.py:2757-2775` (was `:2747-2775`, 10-line shift, same body) — no `with _BINDING_EXCLUSION_EDIT_LOCK:`; lock defined `:375`, taken by the exclusion editors at `:2625` and `:2663` exactly as cited.
- Proof: `save_runtime_binding` (`:2486-2546`) does `INSERT ... ON CONFLICT(binding_id) DO UPDATE SET ... metadata_json = excluded.metadata_json` — whole-blob replace, confirmed by reading the SQL. Caller check: `grep -n set_folder_binding_access tldw_chatbook/UI/Screens/settings_screen.py` → `:25468`, inside `@on(Button.Pressed, ...) def _settings_workspace_toggle_folder_access` — a synchronous handler on the Textual loop, not a worker. `UI/Console_Modules/workspace.py:1103-1107`'s docstring literally reads "Callers run this off the Textual event loop, matching the visit admission's registry discipline" for `set_exclusion` (which calls the locked editors).

## 3. P2 [D4] — `Workspaces/models.scrub_secret_metadata` is a weaker, drifted secret policy vs `MCP/redaction.redact_mapping`
- Verdict: CONFIRMED
- Site now: `tldw_chatbook/Workspaces/models.py:76-84` (`_SECRET_METADATA_PARTS`), `:349-376` (`scrub_secret_metadata`/`_is_secret_metadata_key`), applied `:232`. Shared: `tldw_chatbook/MCP/redaction.py:12` (`_SECRET_KEY_RE`), `:90` (`is_secret_key`), `:95` (`looks_like_secret_value`), `:138` (`redact_mapping`) — line numbers match the citation exactly.
- Proof: `_SECRET_METADATA_PARTS = ("api_key","apikey","credential","password","private_key","secret","token")` has no `passwd`/`authorization`/`bearer`, and `_is_secret_metadata_key` does substring-of-normalized matching only (no value-shape check) — a `"passwd"` key is never caught (not a substring of any part) and the match `continue`s (drops the key). `MCP/redaction.py`'s `_SECRET_KEY_RE = (?i)(token|secret|passwd|password|api[-_]?key|authorization|bearer|credential)` catches `passwd`/`authorization`/`bearer` but has no bare `key`/`private_key` alternative, so `private_key` never matches; `redact_mapping` replaces matched values with `REDACTED = "***"` rather than dropping the key. Both directions of drift confirmed by direct read, matching the review's evidence table exactly.

## 4. P2 [D1/D3] — `Canvas/`/`Research_Workspace/` have zero logging and `Canvas/service.py` discards `__context__` on re-raise
- Verdict: CONFIRMED
- Site now: `tldw_chatbook/Canvas/service.py:114-123, 324-331, 434-441, 482-489, 667-674, 688-695` (all six ladders read verbatim, unchanged); `Canvas/gateway.py:1404,1413` (`raise RuntimeError("Canvas gateway could not start") from None`, exact line match).
- Proof: `grep -rn "logger\." tldw_chatbook/Canvas/` → 1 hit, but it is inside `Canvas/mermaid/inputs.json` (`"package/dist/logger.d.ts"`), not Python code — actual Canvas `*.py` logger-call count is 0, matching the review. `grep -rn "logger\." tldw_chatbook/Research_Workspace/` → 0. `grep -rn "^\s*except " tldw_chatbook/Canvas/ | wc -l` → **167**; same for `Research_Workspace/` → **49** — both match the review's counts exactly. Each of the six ladders sets `repository_error = CanvasServiceError(...)` inside the `except` block and does `raise repository_error` *after* the try/except exits, so `sys.exc_info()` is empty at raise time and `__context__` is unset — confirmed by reading the control flow (no re-verification needed beyond the code itself, this is deterministic Python semantics).

## 5. P2 [D2] — `build_console_workspace_state` double `workspace_memberships` scan on the Textual loop
- Verdict: CONFIRMED
- Site now: `tldw_chatbook/Workspaces/display_state.py:461` and `:508` (unchanged — exact match) call `_conversation_rows_from_memberships` / `_handoff_rows_from_memberships`, whose bodies (now at `:758` and `:791`, helper functions defined lower in the file) each independently call `registry_service.list_workspace_memberships(...)`.
- Proof: `list_workspace_memberships` (`registry_service.py:1416-1435`, exact match) is `SELECT * FROM workspace_memberships WHERE workspace_id = ? ORDER BY ...` with `.fetchall()` and no `LIMIT`. `display_state.py:449-450` comment: "Filesystem status belongs to the controller's off-loop snapshot worker. A pure UI-loop state build must fail closed..." confirming the whole function runs on the UI loop. `list_workspace_source_memberships`/`list_workspace_note_memberships` (`:1438`, `:1482`) exist as paged siblings, confirming the unpaged accessor is the odd one out.

## 6. P2 [D4] — `Workspaces/models.utc_now_iso` shadows the canonical `Utils/timestamps.utc_now_iso`
- Verdict: CONFIRMED
- Site now: `tldw_chatbook/Workspaces/models.py:97-100` (exact match), imported at `registry_service.py:31-46` (`from .models import (... utc_now_iso)`) → `self._now_factory = now_factory or utc_now_iso` at `:491` (exact match).
- Proof: local `utc_now_iso()` returns `datetime.now(timezone.utc).isoformat()` — variable-width (microseconds present/omitted per value, `+00:00` offset). Canonical `Utils/timestamps.py:63` `utc_now_iso()` returns `to_utc_iso(utc_now())` — fixed-width millisecond-`Z`. Same public name, genuinely different shape, confirmed by direct read; no shared-name collision protection exists (plain module-level `def`).

## 7. P2 [process] — `check_canvas_mermaid_assets` is a tautology for 2 of 6 outputs (the consequential claim)
- Verdict: CONFIRMED — read both scripts in full, claim is precise.
- Site now: `scripts/vendor_canvas_mermaid.py:349-353` (`build()`'s `outputs[...]` dict construction, matches cited lines exactly) + `scripts/check_canvas_mermaid_assets.py:195-232` (`check_assets`/`compare_generated_outputs`).
- Proof: `vendor_canvas_mermaid.py` — `STATIC = ROOT / "tldw_chatbook/Canvas/static"`; inside `build()`, `outputs["canvas_runtime_worker_v2.js"] = (STATIC / "canvas_runtime_worker_v2.js").read_bytes()` and the identical pattern for `canvas_renderer_v2.js` — both read **directly from STATIC**, no generator, no hash of an independent source (`grep -rn "canvas_runtime_worker_v2\|canvas_renderer_v2" scripts/vendor_canvas_mermaid.py` shows these are the only two producers for those names). `check_canvas_mermaid_assets.py:check_assets(committed_dir: Path = STATIC)` — default `committed_dir` is the **same STATIC directory**. `check_assets` calls `build(isolated_inputs, rebuilt)` where `rebuilt` is a fresh temp dir; for the 2 files, `build()` still populates `rebuilt/<name>` by reading straight from `STATIC/<name>` (ignoring `isolated_inputs` entirely for these two). `compare_generated_outputs(rebuilt, committed_dir=STATIC, ...)` then diffs `rebuilt/<name>` (= bytes copied from `STATIC/<name>`) against `committed_dir/<name>` (= `STATIC/<name>` directly) — **the same file compared to a copy of itself**, so this pairwise check always passes regardless of what's actually in `STATIC/canvas_runtime_worker_v2.js` or `canvas_renderer_v2.js`. Confirms the finding's core claim precisely.
- Note: the review's own caveat is correct and not overstated — tampering with just these 2 files **is** still indirectly catchable, because `build()` also embeds `digest((STATIC / name).read_bytes())` into the regenerated `mermaid-runtime-manifest.json` (`contract["facade"]["sha256"]` / `contract["plan"]["sha256"]`), and that regenerated manifest is compared byte-for-byte against the *separately* committed `STATIC/mermaid-runtime-manifest.json` (not self-referential) — so an untouched-manifest tamper of the two `_v2.js` files fails on the manifest comparison, not the direct one. But per the review, running the documented `reproducible_command` (`vendor_canvas_mermaid.py --output-dir STATIC`, i.e. writing straight back into STATIC) regenerates the manifest to match the tampered bytes and erases that indirect signal — confirmed by reading `main()`'s `args.output_dir` default (`= STATIC`). Pinning-test duplication also confirmed: `Tests/CI/test_canvas_mermaid_asset_checker.py:15` defines its own local `EXPECTED_OUTPUTS = {...}` rather than importing the checker's, so it cannot notice the circularity.

## 8. P3 [D3] — Six verbatim copies of the repository-error ladder in `Canvas/service.py`
- Verdict: CONFIRMED
- Site now: `tldw_chatbook/Canvas/service.py:114-123, 324-331, 434-441, 482-489, 667-674, 688-695` — exact line match, all six read and are byte-identical in structure (`except CanvasRepositoryError` / `except (CharactersRAGDBError, sqlite3.Error)` / `except Exception:` ladder, same three lines each).
- Proof: `sed -n` over all six ranges — literal text equal modulo trailing `return` statement.

## 9. P3 [D1] — Startup resume discards every exception from its `asyncio.gather` fan-out
- Verdict: CONFIRMED
- Site now: `tldw_chatbook/Research_Workspace/source_association.py:545-548` and `tldw_chatbook/Research_Workspace/source_readiness.py:304-307` — exact line match.
- Proof: both are bare `await asyncio.gather(*(self.resume(...) for ...), return_exceptions=True)` with no assignment to capture/inspect the result list. `grep -n "logger\."` on both files → 0 hits, confirming "modules with zero logger calls."

## 10. P3 [D1] — `remove_`/`has_internal_research_quick_note_owner_proof` skip `_enforce_policy`
- Verdict: CONFIRMED
- Site now: `tldw_chatbook/Notes/notes_scope_service.py:2333` (`has_internal_research_quick_note_owner_proof`) and `:2361` (`remove_internal_research_quick_note_owner_proof`) — exact line match to the cited `2333-2384` range; neither body calls `self._enforce_policy(...)`, while siblings at `:2270`, `:2286`, `:2396`, `:2407` do.
- Proof: `grep -n "remove_internal_research_quick_note_owner_proof\|has_internal_research_quick_note_owner_proof\|_reconcile_quick_note_receipts" tldw_chatbook/Research_Workspace/local_adapter.py` → `remove_` called at `:497` and `:688`, exact match to the review's citation. Traced the chain: `reconcile_quick_notes()` (startup entry, docstring "Resume one bounded global page of durable Local note receipts") → `_reconcile_quick_note_receipts` (`:524`) → for `operation_kind == "create"` receipts, calls `self._reconcile_quick_note_create(receipt, receipt_ref, row)` (`:559-561`) → `_reconcile_quick_note_create` (defined `:624`) contains the `remove_` call at `:688`. Confirms the reachability claim precisely: a startup resume can reach the unenforced `remove_` without any create-gate check running first.

## 11. P3 [D4] — Small verbatim clusters (`_page_bounds` ×4, `_read_bounded`/strict-JSON hook, `_freeze_json` dict-vs-Mapping drift)
- Verdict: CONFIRMED
- Site now: `_page_bounds` at `Research_Workspace/server_adapter.py:105` and `Research_Workspace/local_adapter.py:90` — exact match to cited "105/90"; call sites `server_adapter.py:439,473` and `local_adapter.py:840,909` (review cited `registry_service.py:1449-1452,1493-1496` as the inlined pair — not independently re-verified, low-value). `_freeze_json`/`_freeze_json_value` drift: `Canvas/runtime_assets.py:103` `if isinstance(value, dict):` inside `_freeze_json` (def at `:100`) vs `Canvas/models.py:608` `if isinstance(value, Mapping):` inside `_freeze_json_value` (def at `:607`) — both line numbers match the citation exactly.
- Proof: direct `grep -n` isolating each check; the dict-vs-Mapping asymmetry is real and would leave a non-`dict` `Mapping` (e.g. `MappingProxyType`, a custom mapping) unfrozen in `runtime_assets.py`'s copy while `models.py`'s copy freezes it.

TOTALS: confirmed=11 fixed=0 wrong=0 demoted=0 promoted=0
