# S24 validation — the 31 `*_Interop/` packages (one cluster finding, per instruction)

Validated against worktree HEAD `d0face3ebe` (review baseline `3722a85748`, +25 commits). Per the slice's own
instruction this is treated as the two `###`-headed findings it declares, not expanded per-file; the numbered
narrative sections (1-4) are supporting context for the P2 finding's severity call, not separate findings.

## 1. P2 — Scope-service policy gate is applied per-method; real sibling-method gaps exist (Study import pair live-unreached, Notes pair live-reached)
- Verdict: CONFIRMED
- Site now: `Study_Interop/study_scope_service.py:1489` `import_flashcards_tsv` and `:1609` `import_flashcards_json_file` (both exact matches to the finding's line numbers); gated siblings at `:1262-1263`, `:1291-1292`, `:1473-1474`, `:1642-1643`. `Notes/notes_scope_service.py:2361` `remove_internal_research_quick_note_owner_proof` (exact match) vs gated `add_internal_research_quick_note_owner_proof` flow at `:1425-1428`.
- Proof: `sed -n '1489,1610p' study_scope_service.py | grep -n "_enforce_policy\|^    async def "` → `import_flashcards_tsv`'s 28-line body contains zero `_enforce_policy` calls; the next method (`export_flashcards`) does. Same check on `import_flashcards_json_file`'s body (1609-1631): zero `_enforce_policy` calls; the next method (`import_flashcards_apkg`) has one. `grep -n remove_internal_research_quick_note_owner_proof tldw_chatbook/Research_Workspace/local_adapter.py` → live callers at `:497` and `:688`, confirming this pair (unlike the Study pair) has a shipped caller today.
- Cross-check on the census scale: `find tldw_chatbook -name "*_scope_service.py" | wc -l` → **47** and `find tldw_chatbook -path "*_Interop/*_scope_service.py" | wc -l` → **31**, both exact matches to the finding's corrected counts (superseding the original review's "about 45"/"33 Interop"). This is strong corroboration for the AST sweep's precision on the broader "24 ungated public methods across 10 of 47 scope services" claim, which I did not fully re-derive by hand.
- Additional corroboration (mode=None default inversion, used in the same finding's severity reasoning): `grep -n "OutputsBackend.SERVER\|OutputBackend.LOCAL" tldw_chatbook/Outputs_Interop/outputs_scope_service.py tldw_chatbook/Outputs/server_outputs_scope_service.py` → `Outputs_Interop/outputs_scope_service.py:71` defaults to `SERVER`, `Outputs/server_outputs_scope_service.py:24` defaults to `LOCAL` — confirms one of the four claimed twin-pair inversions directly (the other three — Sharing, Web_Clipper, Notifications — were not independently re-checked here but follow the identical file-pair pattern).
- Note: this finding was already independently re-verified in `qa/tier2-code-review-2026-09-21/phase4-verification.md` "S24 — two claims verified"; I re-derived the same evidence from the current tree rather than trusting that entry, and it reproduces exactly.

## 2. P3 — Six `*_Interop` modules (753 lines) have zero production importers; five test-only, one fully orphaned
- Verdict: CONFIRMED
- Site now: `Research_Interop/research_models.py` (65 lines), `Sync_Interop/notes_local_store.py` (47), `Sync_Interop/notes_m1_flow.py` (126), `Sync_Interop/notes_mirror.py` (112), `Sync_Interop/sync_profile_status_state.py` (194), `Writing_Interop/writing_models.py` (209) — `wc -l` on all six reproduces the exact per-file counts, summing to 753.
- Proof: `grep -rln "\bresearch_models\b" tldw_chatbook/ --include=*.py` outside its own file → 0 hits (fully orphaned, not even test-referenced). For `notes_mirror`, the apparent importers are false positives from a duck-typed parameter name, not a module import — confirmed by reading `envelope_builder.py:39,45,132-133` (`notes_mirror: Any = None` constructor param), `envelope_applier.py:49,58,115` (same pattern), `domain_adapters/notes_m1.py:20,24,38,61` (same), and `DB/private_sqlite.py:508-509` (a string-literal ownership-policy path, not an import) — none is `import notes_mirror` or `from ... import notes_mirror`. `notes_m1_flow.py:63` similarly passes `notes_mirror=self.mirror` as a kwarg. `grep -rln notes_m1_flow tldw_chatbook/ --include=*.py` outside its own file → 0 hits; `grep -rln notes_m1_flow Tests/` → exactly `Tests/Sync_Interop/test_notes_m1_flow.py`, confirming "kept alive by its own tests."
- `grep -iln "_interop" backlog/tasks/task-32807*.md` → 0 hits, confirming "none of TASK-32807's sub-tasks names an `*_Interop` module."

TOTALS: confirmed=2 fixed=0 wrong=0 demoted=0 promoted=0
