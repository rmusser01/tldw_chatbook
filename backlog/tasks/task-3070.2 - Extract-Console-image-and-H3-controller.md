---
id: TASK-3070.2
title: Extract Console image and H3 controller
status: Done
assignee:
  - '@codex'
created_date: '2026-08-13 17:08'
updated_date: '2026-08-14 01:37'
labels:
  - console
  - image-generation
dependencies:
  - TASK-3070.1
references:
  - Docs/superpowers/specs/2026-08-13-console-decomposition-wave6-design.md
parent_task_id: TASK-3070
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Move the reviewed non-DOM image-generation and H3 lifecycle ownership out of ChatScreen.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Image/H3 move inventory is controller-owned and screen entry points are bounded delegates
- [x] #2 H3 cancellation, remount, settlement, persistence, and attachment behavior is unchanged
- [x] #3 Isolated controller tests run without mounting Textual
- [x] #4 Remote transcript-image fetch deduplication remains memory-bounded under high-cardinality URLs
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Record the immutable Wave 6 image/H3 inventory and focused baseline.
2. Add failing no-mount controller, compatibility-descriptor, direct-wiring, and H3 lifecycle contracts.
3. Move image projections, H3 ownership, ordinary generation, and generation-card actions into ConsoleImageController with named late-bound dependencies.
4. Wire screen lifecycle callers and MessageController directly to the new owner, retaining only the bounded command-registry delegate and DOM stays.
5. Run focused, architecture, mounted-regression, static, privacy, and full-suite verification; self-review; document the implementation.

ADR required: no
ADR path: N/A
Reason: This implements the already-approved Wave 6 ownership boundary and introduces no new storage, security, provider, runtime, or cross-module policy.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Extracted the reviewed 25-method image-generation/H3 cluster into ConsoleImageController and wired its named late-bound dependencies through wiring.py. ChatScreen now retains only the approved DOM stays and a three-line command delegate; MessageController generation actions call the image controller directly. Added fail-loud read/write compatibility descriptors for the three moved state attributes and no-mount controller coverage.

Preserved exact H3 cancellation-event identity, persistence-before-publication ordering, remount adoption, attachment retention/cleanup, shutdown draining, and privacy-safe failure copy. Review follow-up corrected stale ownership documentation and extended two bounded test-worker holds to avoid self-settlement during mounted DB/screen setup. No new ADR was required because this implements the approved Wave 6 boundary.

PR review follow-up replaced the controller's unbounded remote-image attempt set with a 256-entry LRU while preserving recent-URL fetch deduplication. A focused high-cardinality regression proves the oldest attempt is evicted and controller memory remains capped.

Verification: 297 focused architecture/image/H3/generation/citation tests passed; 22 pending-attachment/remount tests passed; 144 controller/action tests and 4 affected native-flow tests passed; Ruff check/format, fatal screen lint, Bandit, py_compile, diff/privacy/artifact checks passed. Full pytest fail-fast reached 1,516 passed and 1 expected skip before the unchanged origin/dev App ingest test failed on its stale two-argument unpack of the current three-argument worker call; neither production nor that test is modified by this task.
<!-- SECTION:NOTES:END -->
