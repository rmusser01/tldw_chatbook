---
id: TASK-2154.1
title: 'Console: responsive fallback below ~100 cols (LY-08, LY-09, LY-10)'
status: Done
assignee:
  - '@kimi'
created_date: '2026-08-05 00:15'
updated_date: '2026-08-05 01:29'
labels:
  - console
  - ux
dependencies: []
parent_task_id: TASK-2154
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
At 80x24 the workspace grid renders only the left rail and the transcript vanishes; at 60x18 the screen is an empty frame. Add a responsive fallback: auto-collapse the left rail below ~100 cols, single-pane layout below ~80, and preserve a status identity when the compact-height rule hides the header badge. See findings doc section 2.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Transcript stays visible and usable at 80x24 and 60x18 (UAT p4-narrow-80x24/60x18: transcript + tab strip + `Ready — type a message to begin.` render; send/reply loop verified at 80x24),Left rail auto-collapses instead of eating the grid at <100 cols (CONSOLE_RAIL_LEFT_COMPACT_COLLAPSE_COLUMNS=100, rendering override; stored pref untouched),A status indicator survives compact-height mode (#console-compact-status-marker in the control bar mirrors the hidden header badge),Existing console tests pass (Tests/UI -k console: 2161 passed + reflow expectation updated; the 2 remaining failures reproduce on HEAD and are pre-existing)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: no
ADR path: N/A
Reason: rendering-override inside the existing rail compact-collapse pattern (mirrors CONSOLE_RAIL_RIGHT_COMPACT_COLLAPSE_COLUMNS=150). No persistence-model change: stored prefs still flow through preferred_left_open/right_open; no new keys, no schema/storage/sync/security boundary touched.

1. console_rail_state.py: add CONSOLE_RAIL_LEFT_COMPACT_COLLAPSE_COLUMNS=100 (left rail force-collapses below 100 cols as a RENDERING override, stored pref untouched - mirrors the 150-col right-rail rule; matches the compose comment's own 100-col compact contract) and CONSOLE_SINGLE_PANE_COLUMNS=84 (below 84 = single-pane: 13+56+11+2 grid border = 82 is the smallest width where handle mode still fits). ConsoleRailState gains left_forced_collapsed + single_pane; build_console_rail_state computes both from available_columns; add console_rail_width_band() helper (single-pane/narrow/standard).
2. chat_screen.py compose_content (~14473-15028): handles hidden when their rail is open OR single_pane; main_column min_width = 0 when single_pane else 56; update the compact-contract comment.
3. chat_screen.py _sync_console_rail_visibility (~12195): apply the same handle + main-min-width rules post-compose so sync ticks and live resizes converge to the compose layout.
4. chat_screen.py: new @on(Resize) handler _adapt_console_workspace_to_width - width rules today only evaluate when rail state is (re)built (compose / sync tick), and a pure terminal resize triggers neither; guard on width-band change (tracked on __init__ attr) then _sync_console_rail_visibility_if_changed(_current_console_rail_state()).
5. LY-10: console_control_bar.py compose adds Static#console-compact-status-marker as first child of #console-control-action-row (no _workbench_action_id so sync_actions leaves it; display:none default, Python-driven, no CSS dependency). chat_screen._sync_console_compact_status_marker() mirrors the hidden header badge (text + status-* class) into it while the shell has -console-compact; called from _adapt_console_shell_to_height and _sync_console_workbench_state.
6. Tests: Tests/Chat/test_console_rail_state.py (forced left collapse <100 / none >=100 / single_pane <84 / band helper; stored pref preserved); new Tests/UI/test_console_narrow_layout.py with ConsoleHarness+_build_test_app at (80,24),(90,24),(60,18),(140,24),(140,42): transcript visible+nonzero width, rail/handle display per band, ready empty-line renders at 60x18, compact status marker mirrors header badge, live-resize hook via pilot.resize_terminal.
7. Run: pytest Tests/UI -k 'console_rail or compact or narrow' -x -q, Tests/Chat/test_console_rail_state.py, then Tests/UI -k console -q (time-boxed).
8. UAT: output/ux-review-console uat_console.py p4_narrow; verify p4-narrow-80x24.txt and p4-narrow-60x18.txt show the transcript + ready line.
Out of scope (TASK-2154.2): Inspector access <150 cols (LY-11), inert-handle feedback, control-bar/chip overflow clipping (LY-01..LY-03).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Three-band responsive fallback (left-rail force-collapse <100 cols as rendering override, single-pane <84, compact-height status marker in the control bar) implemented + verified: 2161+1911 console tests green apart from two failures that reproduce on HEAD; UAT p4_narrow captures show transcript + ready line at 80x24 and 60x18. No ADR (pattern-internal rendering override; persistence model untouched). See task file Implementation Notes.
<!-- SECTION:NOTES:END -->
