---
id: TASK-19642.1
title: Repair recurring Skills Library and import harness failures
status: Done
assignee:
  - '@codex'
created_date: '2026-08-21 23:23'
updated_date: '2026-08-22 19:55'
labels:
  - testing
  - skills
  - regression
dependencies: []
references:
  - backlog/docs/task-19520-verification-failure-inventory.md
  - >-
    Docs/superpowers/specs/2026-08-21-task-19642-1-skills-harness-repair-design.md
  - Docs/superpowers/plans/2026-08-22-task-19642-1-skills-harness-repair.md
parent_task_id: TASK-19642
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Restore the 29 completed Skills-gate failures in test_skills_import.py and
test_skills_library_flow.py. Repair the confirmed Library harness and editor
contract drift, plus the mounted-owner ordering race exposed by independent
closeout verification. The separate project-skills import-modal session-only
failure is tracked by `TASK-19642.27`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 All 29 completed-gate Skills nodes assigned to this task in the TASK-19520 verification inventory pass in a focused Skills run.
- [x] #2 Library readiness and backend fixtures fail deterministically without hidden background exceptions or missing Library rows.
- [x] #3 The resolution does not weaken trust-store permission assertions or bypass real runtime-policy coverage.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Reproduce the 29 assigned nodes in their original two-file order, by file,
   and through representative isolated/pair runs; capture complete tracebacks,
   background exceptions, app liveness, and mounted Library selectors before
   attributing a shared cause.
2. Trace each observed failure backward through the Skills test harness,
   Library composition/readiness path, optional Prompt/Study/Quiz seams, and
   trust-posture projection; compare against working Library harness patterns
   and recent relevant changes.
3. Establish the narrowest defensible root-cause grouping, complete the ADR
   check, and obtain approval for a focused design that changes fixtures or
   production behavior only where the evidence requires it.
4. Add regression tests that fail for the confirmed causes, align the stale
   harness interactions, and move the trust-posture refresh across the existing
   Skills-canvas mount boundary. Preserve strict targeted sync, route/generation
   guards, and the no-whole-screen-fallback contract.
5. Run only the two assigned test files, the deterministic mounted-owner test,
   directly related reconciliation owners, static checks, and inverse/mutation
   evidence.
6. Record exact verification evidence, obtain independent review, and close the
   task only after all acceptance criteria are met.

ADR required: no new ADR

ADR path: `backlog/decisions/076-library-lifecycle-progressive-disclosure.md`

Reason: ADR-076 and TASK-19025 already own the Library and Skill-editor
contracts, while the existing entry-reconciliation contract requires automatic
workers to project only into their mounted owner without a whole-screen
fallback. Moving one refresh trigger after that mount is a routine sequencing
fix. It changes no storage, security, service interface, or user-facing
architecture.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
**Reopened 2026-08-22.** Post-closeout independent review found an intermittent
full-owner failure: `test_orphaned_manifest_is_one_click_resetup` timed out
waiting for `#library-skills-trust-header` in a clean-HEAD two-file run
(`1 failed, 33 passed`), although isolated and predecessor-plus-node reruns
passed. Five subsequent instrumented ordered runs were green, but history and
lifecycle tracing identified the matching race window: rail entry starts the
trust-posture worker before the Skills canvas mounts, and a failed strict
projection is not retried. The prior `34 passed` completion evidence is not yet
sufficient; ACs 1 and 2 remain open until the approved mounted-owner ordering
repair and repeated focused verification are complete. AC 3 remains checked
because the repair preserves the existing trust/runtime-policy path and strict
route/generation guards.

Completed the harness and interaction portion without weakening any trust or
runtime-policy assertion. Confirmed drift classes:
fresh-profile versus full returning-user rail posture; optional Prompt/Study/Quiz
count seams; stale clean/dirty/delete/create interactions and first-save copy;
and latent compact-trust/advanced-tool assertions. Changed only
`Tests/Skills/test_skills_import.py` and `Tests/Skills/test_skills_library_flow.py`
(plus planning/evidence and the lesson) so far; real trust and policy owners
remain in the exercised path. The remaining approved production change is
limited to moving the existing trust-posture refresh after the Skills canvas
mount and adding its deterministic owner test.

Task 3 evidence: initial and restored two-file gates each reported `34 passed`;
the warning-level repeat also reported `34 passed`; the direct owner command
reported `12 passed`; Ruff check was green; `git diff --check origin/dev...HEAD`
exited 0. The known formatter baseline remained exit 1 for exactly the two
touched Skills tests; it was recorded, not reformatted. Representative causal
inverses each failed for the expected reason and were immediately restored:
direct fresh-profile factory import left the Skills rail row missing; skipping
More actions left Delete confirmation inaccessible to the test; restoring
`status_text == "Saved."` disagreed with the current trust-review guidance.

Focused live-log inspection found no Prompt/Study/Quiz backend exception traces
and no `Failed to load local Library source snapshot`. Excluded pre-existing
baseline diagnostics were the synchronous app-construction Prompts-not-initialized
warning, ChaChaNotesDB/CharactersRAGDB-not-assigned ERRORs without traceback,
and the optional OpenTelemetry warning; none was a background exception.

ADR-076 was reused; no new ADR was required. No equivalent returning-user versus
fresh-profile incident was present in the evidence lessons, so the repeated
TASK-19579/TASK-19642.1 finding was added there. Verification is scoped to the
assigned tests and direct owners only; this task makes no repository-wide-suite
claim.

Final mounted-owner repair: the existing Skills trust-posture refresh now begins only after the Skills canvas mounts. The missing-service lifecycle cancels any in-flight posture worker, clears cached posture, and strictly repaints only the mounted Browse Skills list; an open editor is not recomposed, preserving its live draft, widget identity, and focus. Added deterministic regressions for post-mount ordering, stale-header clearing, editor preservation, and stale-worker cancellation. Final recorded-base verification after a clean rebase onto origin/dev f2e274993: 10 ordering/reconciliation owners passed; the exact two-file Skills gate passed 34 tests in three consecutive runs; 14 editor/trust lifecycle cases passed; a warning-level 34-test repeat contained no scoped Prompt/Study/Quiz exception trace and no local Library snapshot-failure warning. Ruff passed on all five scoped Python files and revision diff-check passed. The formatter retains the documented five-file baseline and was not applied broadly. Independent final review found no Critical, Important, or Minor issues and range-diff confirmed all 19 patches were preserved through rebase. ADR-076 remains applicable; no new ADR was required. Verification remains intentionally scoped to modified functionality and makes no repository-wide-suite claim.

Reopened for Qodo PR review on 2026-08-22. Qodo found that repeated missing-service refreshes can strictly sync the active Skills list even after posture is already empty and no stale trust header remains. AC2 is reopened pending a deterministic no-op regression, the minimal stale-state repaint guard, scoped reverification, and review resolution. Qodo also requested docstrings for the three new entry tests and type annotations for the new editor-preservation test.

Qodo remediation preserves unconditional stale-worker cancellation and posture clearing, but repaints the mounted list only when prior posture or a mounted header is stale. The new no-op regression failed first with one unexpected sync and now passes without a sync; the 5 Qodo-affected cases, 11 ordering/reconciliation owners, 14 editor/trust lifecycle cases, and one exact 34-test two-file run pass. Ruff and whitespace checks are green; the formatter retains the documented five-file baseline. AC2 remains open pending controller-owned review resolution and final clean-HEAD closeout.

Qodo PR review resolved on 2026-08-22. Added concise intent docstrings to the three new entry regressions and Path, pytest.MonkeyPatch, and None annotations to the editor-preservation test. A new deterministic regression proved the repeated empty/no-header service-missing path previously performed one unnecessary strict sync. The fix keeps worker cancellation and cached-state clearing unconditional, but repaints the active Browse Skills list only when the prior posture is nonempty or a trust header is actually mounted; editor mode and strict no-fallback behavior remain unchanged. Clean-commit evidence: 5 Qodo-focused tests passed, the updated ordering/reconciliation gate passed 11 cases, the editor/trust lifecycle gate passed 14 cases, the exact two-file Skills gate passed 34 cases, Ruff and revision diff-check passed, and the documented five-file formatter baseline remained unchanged. Independent spec and quality reviews found no Critical, Important, or Minor issues.
<!-- SECTION:NOTES:END -->
