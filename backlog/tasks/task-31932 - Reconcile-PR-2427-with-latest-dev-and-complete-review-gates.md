---
id: TASK-31932
title: Reconcile PR 2427 with latest dev and complete review gates
status: In Progress
assignee:
  - '@codex'
created_date: '2026-09-06 17:14'
updated_date: '2026-09-06 17:38'
labels: []
dependencies: []
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Preserve the reviewed test repairs and newly landed dev behavior while making PR 2427 eligible for normal reviewed integration.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The PR contains latest dev with all reviewed behavior preserved and no unresolved rebase conflicts.
- [ ] #2 Review-created Backlog collisions are renumbered with upstream identities and historical evidence paths preserved.
- [ ] #3 Affected complete-file tests and derived artifact checks pass without weakening contracts or raising screen size limits.
- [ ] #4 Qodo findings and required checks on the final revision are handled before normal merge.
- [x] #5 Newly landed character-navigation test fixtures finalize only their own resources, with all seven cases passing and no retained SQLite descriptors under native attribution.
- [x] #6 New dev boot-worker warning probes retain their owned Loguru sinks for the mounted observation window, and newly attributed worker/smoke fixtures finalize exact owned resources.
- [x] #7 Incremental agent-step persistence tests finalize their owned database and write real run logs under their test-owned root without retaining a process-global workspace database.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Preserve the pushed head and isolated worktree, rebase onto fetched dev, and reconcile overlapping owners without losing upstream behavior.
2. Census all task buckets and refs/worktrees; renumber only review-owned collisions, preserve upstream identities and evidence paths, and verify the exact mapping.
3. Run complete targeted files for rebased runtime contracts and architecture inventories. Reconcile stale owner/constructor assertions and tighten slack budgets to measured counts. Keep genuine size failures open; obtain design approval before further owner extraction.
4. Review diagnostic statement deltas before regenerating the manifest. Run CSS, profile-path, schema, index, Backlog, affected tests, and scoped static checks.
5. Publish with an exact force-with-lease, request normal review, address verified Qodo findings, and merge without bypass only after final-head review and required checks succeed.
6. Apply the existing exact-owner real-app fixture adapter to the three new character-navigation test constructors, preserving all behavioral assertions. Verify the complete file with native descriptor attribution and shared cleanup fault controls. This is test-only reuse of established lifecycle APIs, with no new ADR or ownership design required.
7. Extend the already reviewed Loguru capture-lifetime repair (TASK-32136) to newly landed boot-worker parameter cases, keeping all three state transitions and the unknown-worker positive control. Attribute and close only exact worker/smoke fixture-owned resources through existing test lifecycle adapters, then run both complete files and shared cleanup controls with native attribution.

ADR required: no for the rebase and test/derived-artifact reconciliation.
ADR path: N/A; existing DESIGN.md section 7 governs retained controller ownership.
Reason: these steps preserve established storage, runtime, and UI contracts. Any new architectural change requires a separately approved design.

8. User-approved 2026-09-07: move private Canvas actions and citation discovery helpers into the existing ConsoleMessageController; preserve screen event, UI, and worker hooks, current dev behavior, and unchanged size limits. Verify all affected complete files and callback-ownership contracts.
9. User-approved 2026-09-07: restore useful settings failure diagnostics using fixed operation/phase, exception type, and validated opaque identifiers only. Exclude raw exception text, drafts, credentials, and provider/model labels. Add fault-injection privacy and usefulness assertions before implementing; regenerate the diagnostic inventory only after reviewing statement changes.
10. Finalize the incremental agent-step fixture with its existing database close API and supply RunLogWriter with the test-owned database directory. This preserves real logging while avoiding unrelated process-global workspace lookup. Verify the complete file with native descriptor attribution. No new ADR: test-only ownership using established constructors and lifecycle methods.
11. The second dev rebase removed the previously reviewed Save-as-Note owner repair and regression. Restore its test first, verify the wrong-owner failure, and retain the established notes_user_id contract. This is a routine regression repair under the existing notes ownership boundary, not a new storage design; unrelated upstream feature removals remain outside this repair.
12. Repair the reproduced Canvas gateway partial-body read: collect at most the existing byte limit plus one through EOF before JSON decoding; preserve oversized/malformed request refusal and cancellation. Add deterministic split-stream/boundary controls, run the complete gateway and browser files, and obtain independent review. Existing ADR-121 governs the unchanged bounded request/confirmation boundary; no new ADR required for this transport bug fix.
13. Reconcile only the four upstream appearance-width assertions with exact conversation identity, full state/tooltip text, and the owning tray's wrapped/truncated display contract; preserve service, selection, persistence, and resume assertions. Address Qodo comment 3954688544 by using the existing database transaction manager for both recovery-test reads, keeping queries and assertions unchanged. These are test-only contract reconciliations under steps 3 and 5, not new runtime or ownership designs; no ADR required. Verify both complete affected files before publication.
14. User approved the remaining Console/Library existing-owner moves, narrow selectors and Watchlists-only lazy stylesheet after checkpoint c27723b623. Execute the CSS work through `Docs/superpowers/plans/2026-09-08-pr2427-css-gate-paydown.md` under existing ADR-097, preserving all current caps and appearance. Record each controller cluster's exact owner/binding plan before its move, following DESIGN.md section 7 and existing Library migration governance. Keep source moves, test-callsite cleanup and any runtime bug repair attributable; retain all behavioral assertions and obtain independent spec/correctness review before publication.
15. The complete affected-file runs exposed stale fixtures from upstream changes, independent of the owner moves. Reconcile the two Console overflow-menu controls with TASK-31759's two added note actions, retaining exact ordered labels, captured-message/dismissal checks and explicit keyboard traversal of every enabled entry. Reconcile only the Scheduling overflow test's viewport with TASK-31712's intentional padding reduction, preserving all painted-scroll assertions and the separate 235x52 lifecycle check; repair the tooltip's internal reminder nouns to the existing scheduled-task vocabulary under TASK-23106. Verify complete affected files. ADR required: no; these are test-fixture and copy reconciliations of established behavior, not new UX or runtime contracts.
16. Reconcile dev 603812300f after the completed Wave7 rebase. Preserve scoped repeated-fault detection, real Media-state resume cache reset, and Copy selection. Retarget the nine stale test references to the existing MediaState owner without changing assertions; reproduce and investigate the remaining painted/layout failures separately. No new ADR: existing ownership and behavior remain unchanged.
17. Remove repeated status-widget updates and identical action-button construction within the existing Character Repair dialog. Keep fresh widget lookup, exact button IDs/classes/order/nesting and all repair/cancel/worker behavior. Characterize those contracts first, run the complete repair and existing CSS-parity files, and retain the 502-line ceiling. No new ADR: local deduplication without ownership, UI, or service-contract changes.
18. Reconcile painted tests with TASK-31983's focus-only left border, TASK-31979's empty-Reader width allocation, and TASK-31980's destructive-action gap. Keep the real compositor, focus movement/selection invariants, full metadata and action labels, and exact gap measurement. Treat any genuinely clipped action label as a runtime defect, not a reason to weaken text assertions. No new ADR: preserve the three established visual contracts.
19. Complete existing Prompts/Skills private-delegator cleanup instead of the obsolete Reader extraction. The tracked-file census found no external executable/test/patch/dispatch consumers for the 22 names below; retain constructor lambdas and change only their inner receiver. Characterize replacement-owner late binding before removal, update exact architecture inventories, and verify complete affected files. Existing controller bodies and all numeric ceilings remain unchanged; tighten only the measured Screen line pin after cleanup. No new ADR: existing owners and callback contracts, under the Library decomposition recipe's private-cleanup whitelist.
20. Isolate the roleplay-writer weakref test from the unrelated ten-second Environment poll cadence using its existing interval constant. Diagnostics traced the retained Screen to the cancelled Textual sleep TimerHandle's async context; shortening only that cadence makes the unchanged assertion pass. Keep the real timer, assert it exists before departure and is stopped on departure, retain the writer/durable-repair assertions and sub-0.5-second unmount bound, and never mutate asyncio's timer heap or framework internals. Retarget two Message test calls to their explicit existing owner so the seam guard sees the same receiver. No new ADR: test-only isolation and receiver spelling; production scheduling and lifecycle behavior stay unchanged.
21. Restore the real route-CSS lifecycle in the Watchlists latest-active-run routing test using existing FullAppDestinationContext for that one case. The boot-only harness bypasses `_ensure_screen_owned_css`, clipping the button so the compositor hits its parent and the real click never reaches the handler. Preserve exact target/route/label/status assertions and real scroll/click, and explicitly assert the click hit. No production or stylesheet change; no new ADR, under the already approved Watchlists-only lazy stylesheet contract.
22. Correct the two Console runtime-ownership fixtures that stack a second ChatScreen over production startup: await the existing production-screen helper and assert one Console visit, retaining every event, detach, identity and trusted raw-draft provenance assertion. TASK-31520 deliberately suspends rather than unmounts ordinary Console navigation; these two unmount-specific tests must explicitly use the existing `_unmount_installed_console` helper after real navigation to exercise actual Textual removal and recreation. Retain separate default-route reuse coverage and every cancellation/restoration assertion; do not change production suspend semantics. Reconcile the Canvas watcher fixture with the existing store-owned shared controller while keeping gateway absence, watcher identity, disable latching and disposal checks. Root-cause traces and the three failing cases precede edits; verify the complete ownership file. No new ADR: fixture-only alignment with existing app/runtime ownership.
23. Reconcile the newly completed Library inventory (968 passed / 31 failed) by distinct cause. Pin TASK-31645's actual one-row Chunking Lab chrome in compact Notes totals and select the Import command by its stable action rather than tuple position. Preserve exact remaining geometry, focus visibility and six-row surplus growth. Retain the per-profile grip contracts from TASK-31633/31951/31952 using a genuinely collapsed viewport and exact profile-specific grip widths. Seed actual Media data for route tests that require a list, and investigate the RAG-open and Clear-path failures without changing their behavior contracts. Keep metadata slot positions separate from human-readable text normalization. No new ADR for test-only reconciliation of these existing contracts (ADR-118 governs the retained Lab entry); any demonstrated production defect requires a separately recorded minimal repair and regression evidence.
24. Repair the inherited Media deep-link selection mismatch at the existing MediaController builder: local viewer projection uses the existing canonical Reader selection, preserving current-page membership/fallback and ordinary list/external-detail behavior. Add cold/warm, non-first-item and production numeric-source-ID regressions first, preserving exact Items/Reader identity and detail/title assertions; then run affected complete files. No new ADR: correction within existing Media selection ownership, not a new state owner or service contract. Separately reuse ConsolidatedCSSApp in the ingest fixture: the read-only compositor probe found MainNavigationBar consuming 23 rows and the fold hint intercepting Clear at y44 because the plain App omitted production widget defaults. Pin real compact navigation geometry before the real click; retain input identity, focus and clearing assertions. No production CSS or ingest behavior change.

25. Repair the reproduced Prompts resume/delete race: while the existing Prompt mutation gate is held, the admitted operation owns its snapshot and browse refresh, so the visit-refresh seam must not start either competing read. Keep all other resume work, source-failure severity gating, exactly-once focus/count assertions and post-delete stale-page refusal. Add cancellation/ordinary-resume controls and retarget the stale history version assertion to existing PromptsState. Preserve the Screen line ceiling, shortening redundant lifecycle documentation if needed rather than compressing code or raising the cap. ADR required: no; routine lifecycle admission repair under TASK-31521, with existing state and mutation ownership.
26. Reconcile Skills fixtures with the existing Library-owned stylesheet: use the existing runtime CSS union for the five parity guards and the isolated production-editor host, preserving exact CSS content and real compositor hit/containment assertions. Exercise a genuinely manual closed-to-open Items transition at TASK-31951's 82-column floor, retaining persistence after ordinary synchronization. Give the dirty-guard double the existing focus surface it bypasses. No production changes or new ADR: preserve current CSS ownership, compact layout, and dirty-edit contracts. Verify both complete canvas files and independent review before publication.

Step 25 review round 1: the initial guard passed all nine focused tests, but independent review found delete/undo failure settlement does not own the suppressed snapshot read. Preserve source-failure severity policy on those error paths and add an admitted-error return regression before accepting the guard. Ordinary/cancel controls alone do not establish this contract.

Fix-round design (superseded before implementation): an optional retry-only keyword checked inside the existing snapshot worker is unsafe because Textual's exclusive-worker wrapper cancels the previous worker before entering that body. A hard-failure automatic request could cancel an explicit manual retry even without fetching. Do not implement that design.

Safe fix-round design: give the existing Prompts controller a named, late-bound source-failure accessor through its established constructor-port pattern. Check the returned severity in the failed delete/undo settlement arms before invoking the unchanged snapshot worker. Keep source failure ownership in the Screen, preserve successful mutation's one unconditional refresh, and retain the worker signature/scheduling behavior. Add the narrow wiring/late-binding contract and real provider-read controls for healthy/warning/error results, including an already-running manual retry. No new state, owner or Screen method; shorten redundant documentation to retain caps. ADR required: no; routine error-path repair using the existing private dependency boundary, with no new public/service/ownership contract.

Step 24 follow-up: the fresh ratchet, after the eight-line deep-link repair, measures MediaController at 4,638 / 4,630. Shorten only redundant module documentation while preserving ownership/governance facts; verify executable AST identity and the unchanged cap. The earlier pre-repair ratchet result does not qualify this later change.

27. Repair the inherited isolated suspend fixture's constructor bypass: seed the existing unavailable-navigation module and navigation controller, a scoped non-modal app context, generation and return-control query. Keep real character-return cleanup and every seven-timer stop/clear assertion; verify navigation state clears, its generation advances and the return control hides. The production hook/helper and original test match dev exactly; no production behavior change or ADR is required. Run the complete reuse file and scoped independent review. Separately attribute the four unawaited RAG guidance callbacks in the completed entry-compose file before proposing any cleanup; do not filter warnings or suppress behavior under test.

28. Close the two raw reuse-app tests' exact indexing registrations. A scoped observer proves their new singleton retains the same app notifier after that app's loop closes, then the later export fixture dispatches it. Add an opt-in fixture only to those two producer tests: snapshot prior callback membership, hook flag, singleton identity and notifier identities; claim the actual constructed app. Remove only newly owned registrations and stop/reset only a newly created still-owned singleton. For a borrowed singleton, restore only notifier slots still owned by the claimed app; preserve pre-existing or subsequently transferred ownership. Verify teardown state and the real reuse-to-export sequence without filtering warnings, disabling indexing configuration, or changing production. Run complete reuse and entry-compose files; no new ADR for test-only exact-resource lifecycle repair.

Step 28 review: dispatching the process-global reset through `asyncio.to_thread`
opens a scheduling gap: it may stop a replacement singleton rather than the
captured test-owned one. Keep teardown local, revalidate exact ownership at the
stop operation, and add a deterministic transfer/replacement-during-dispatch
control before accepting the fixture. No production reset API expansion.

29. User approved 2026-09-08 after checkpoint 98a9aeeb85: repair the two Watchlists compact Select focus cues using theme-aware foreground/background on the actual painted SelectCurrent, keeping geometry, values, keyboard behavior, the 2.0:1 focus floor, and all CSS caps unchanged. Reproduce both original failures, cover dark/light/high-contrast themes, preserve the full overlay test file, regenerate through the existing builder, and obtain independent review. No new ADR: scoped accessibility repair under DESIGN.md and ADR-097.
30. The same approval authorizes a UI-local Media Browse state separation with a canonical ADR before implementation. Keep service reads, worker dispatch, active-generation admission and view callbacks in LibraryMediaBrowseController; move pure requested/applied-page/facet state and transitions into one UI/Library_Modules state owner. Preserve fault-context episodes, retry/clamp/cancellation/mutation behavior and all assignable state semantics; retarget exact consumers without a generic proxy/mixin. Keep controller <=371 lines and all existing caps unchanged. Root records the precise member map and ADR after the read-only inventory, then qualifies complete affected files and Library shell before final PR review and normal merge.
31. Address the three verified Qodo findings on 98a9aeeb85: use the existing transaction cursor for the two migration/retention tests' raw read snapshots, retaining historical/current schema and exact proof assertions; remove the duplicate UI-job editable install while keeping the documented installed-project requirement. Strengthen the CI test to require exactly one install before pytest, reproduce its failure, then run all three complete files. No new ADR: test-only transaction consistency and elimination of redundant existing CI work; no database behavior or dependency choice changes.

32. Restore the existing deferred Conversation Reader import contract: the complete packaging guard exposes an unused three-line Screen compatibility re-export introduced by rebased assembly commit 8cf9b10fd2 and still present at 9c6a003fc6. The tracked consumer census finds no remaining user of that alias; constructor assembly and architecture tests import the canonical controller directly. Remove only the eager re-export, retain constructor order and all assertions, and verify complete packaging, controller-assembly, conversations-wiring and Screen-size files before the mounted Library qualification. ADR required: no; routine removal of an unused import under existing ADR-097, not a new runtime boundary or compatibility layer.

33. Rebase the saved checkpoint onto dev 9a54013f07, retaining TASK-32046 keyboard routing and all four new shell tests. The complete Screen ratchet reproduces a 35,412/35,393-line breach after the 23-line upstream addition. Shorten only the existing verbose on_key docstring, preserving its keyboard/dirty-edit/focus-admission facts and every executable AST node; do not raise the cap or alter upstream behavior. Verify the complete ratchet, exact docstring-only AST normalization and the complete rebased shell/mounted files. ADR required: no; documentation-only paydown under the existing Screen ratchet, not a runtime change.

34. Address Qodo's four new findings on 91f4122884 after checking the exact tests. Use transaction cursors for the cold-restart timestamp and saved semantic-revision reads, preserving queries and assertions; ensure both cold-restart database instances close even on earlier failure without losing the intentional close/reopen boundary. Make the agent-swap fixture attempt every owned controller/chat/run-database cleanup and aggregate errors, including cancellation, using the established Console fixture pattern. Add a failing cleanup-continuation regression before changing that fixture. Preserve the existing narrow lifecycle fault-injection matrix; complement it with real SQLite cleanup evidence rather than removing its error-order/foreign-owner assertions. Qualify all four complete affected Chat test files and independent review. ADR required: no; test-only transaction and exact-owner cleanup repairs through established APIs, with no production lifecycle or storage boundary change.

35. Reconcile four obsolete fresh-only navigation tests with accepted TASK-24452/31520/31521. Production navigation and Library restore methods are AST-identical to dev. Keep the two-constructions/one-construction identity probe on actual nonreusable Settings/Personas routes and assert that metadata precondition. Parameterize the three real RAG/staged-launch/generic reentry journeys over default warm reuse and a test-scoped cold-route metadata arm: preserve every prior fresh/landing/snapshot assertion in the cold arm and prove identical installed screen, retained Search input/active row and complete staged launch payload in the warm arm. Retain dirty/flush/focus/deep-link tests and verify complete navigation plus reuse files. ADR required: no; test-only reconciliation with established warm/cold contracts, not a new navigation policy.

36. Fix the Reader filter test's reproduced DOM-settlement race: the applied filtered scope and pending Reader request can be observed while structural recompose has temporarily removed the Clear button (NoMatches in the complete cohort, isolated case passes). Await the existing stable mounted-selector helper before the unchanged typed Clear lookup and real press, retaining every service/search/page-three/anchor assertion. Verify the complete Reader flow file and combined neighboring cohort. ADR required: no; fixture readiness uses the existing test helper, with no production timing or selection-policy change.

37. Repair the real local Trash walkthrough's log-capture lifecycle: production startup intentionally resets Loguru sinks and stdlib root handlers, removing the pre-service capture before mounted assertions. Retain that initial capture and reattach exact test-owned sinks immediately after app mounting; require distinct pre-service, mounted-entry and completed-walk canaries from both logging APIs before accepting privacy assertions. Permit only the known pre-mount sink to have been removed; retain strict cleanup for the mounted sink. Cover pre-mount failure with the owned default-executor DB cleanup and close the main-thread connection in an outer finally. Preserve all real paging, focus, layout, privacy and zero-handle assertions, qualify the complete walkthrough, and obtain independent review. ADR required: no; test-only evidence and owner-lifecycle repair, with production logging unchanged.

38. Repair the three genuine Trash entry focus failures: immediate and fast-result publications currently precede destination canvas mounting and their fallback recomposes lose the focus settlement. Make the existing entry event handler async, prime the exact initial loading state through begin_media_trash_request, await the destination recompose, then invoke the unchanged guarded request once. Preserve generation/route admission, page reset, success/empty/Retry focus and loading-first-paint semantics. Update the sole direct-call fake to await the handler and prove loading precedes recompose and request follows it; use existing mounted failures and held-feed loading case as RED/GREEN controls. Keep all Screen ceilings unchanged, shortening only redundant existing on_key documentation if the existing import/state expression consumes its remaining line allowance. ADR required: no; routine mounted-publication ordering repair matching the established Prompts invariant, without changing ownership or service contracts.

39. Restore the Trash fold fixture's intended long-message precondition after upstream Items-width expansion: derive a payload exceeding two rendered rows from the actual mounted status width, retaining the fold visibility, twenty rows, binding list cap, action containment and painted Restore assertions. Do not alter production CSS or thresholds. Qualify the complete Trash file together with entry focus/loading and independent review. ADR required: no; test-data correction under the unchanged status-fold layout contract.

40. The temporary native-focus observer reproduces the Live Items toggle failure: after the helper focuses the unchanged grip, the completed page's queued publication moves focus to the newly mounted Trash Next button before Enter. Apply the existing current-widget/paint drain and refocus pattern already used by _submit_search to _toggle_pane; retain real keyboard Enter and the exact expected-open assertion. Reuse that corrected helper in _ensure_items_open, retaining its canvas-area requirement. Do not mutate layout directly or replace the keyboard action with Button.press. Qualify the complete local walkthrough and independent review. ADR required: no; fixture readiness repair for an attributed focus handoff, not a production focus/layout policy change.

41. The now-working Live pane toggle exposes duplicated mutation evidence: the existing narrow delete capture writes both the direct Loguru sink and production's Loguru-to-stdlib forwarding handler into one StringIO. Keep both APIs observable but capture them separately, check privacy across both texts, and require the original exact started/committed event sequence independently in each capture. Do not deduplicate events, filter messages, change production logging or weaken event counts. Include post-seed setup in the existing main-thread DB finally boundary. Qualify the complete walkthrough after final source freeze. ADR required: no; test-only correction to avoid counting the same logging event twice while strengthening forwarding evidence.

42. Native FD attribution identifies _LibraryIngestCanvasHarness, not generic Library mounts: two passing ingest pilots retain thirteen SQLite/WAL/SHM descriptors and an ingest progress thread. The harness omits the production inherited ingest shutdown and its owned MediaDatabase close. Add only its async unmount cleanup: invoke inherited shutdown, join a returned teardown thread off-loop with the existing bounded retirement expectation, fail if it remains alive, and close the main-thread database connection in finally. Add an exact real-SQLite lifecycle control including a shutdown-error path; preserve all ingest UI/queue/worker assertions and borrowed/foreign owners. The temporary exact cleanup injection already reduces those two pilots to zero regular DB descriptors and only MainThread alive. Qualify complete shell under the unchanged native observer after final rebase/source freeze, not merely below the warning threshold. ADR required: no; test-harness lifecycle parity through existing owner APIs, without changing production shutdown or database contracts.

43. Rebase the reviewed checkpoint and final fixes onto latest fetched dev 2c6a7de490 after all active tests stop. Preserve ONNX diarization, bulk-reader child-model/sidecar rules, sanitized ZAI deltas and the always-mounted visibility-hidden Media zero-selection explanation. Their runtime/test hunks do not overlap the dirty fixes; retain both sides of appended lessons and regenerate the diagnostic inventory from reconciled sources. The two older review-created IDs 32014/32015 still require the requested exception before renumbering; the bulk-reader increment adds no new collision. Qualify the complete new non-live Audio/Agents/Chat/ZAI/settings/Media files and crossed Console/selection/Skills contracts, plus unchanged import/UI-ready/size/CSS/artifact gates. Run the final complete Library shell with native FD attribution only after rebase/source freeze. Historical paid ZAI runs and device/model-download suites are not authorized verification steps. ADR required: no; integrate accepted upstream decisions and routine review fixes without new architectural choices.

44. The complete Reader/Trash/Live group now passes 180 cases and exposes the Live walkthrough's obsolete post-Restore stale-page assertion. TASK-31275 explicitly requires app-owned Trash mutations to reconcile and automatically refresh normal Media; the unchanged runtime follows that accepted contract. Compare the resulting fresh normal page 2 against an independent real-service authoritative page read, retaining exact page membership, exclusion of the off-page restored item and every stable selection/scroll/Back assertion. Preserve external-mutation stale coverage in the existing browse-controller tests; do not change production to recreate the obsolete manual-Retry policy. Qualify the complete Live file and independent review before rebase. ADR required: no; test-only reconciliation with TASK-31275's existing committed-mutation contract.

45. Update the Live 120x35 posture oracle under accepted TASK-31633: its implementation notes explicitly approve/pin the all-three-pane threshold moving from 120 to 112 after Media adopts one-cell grips. The current 120 posture therefore follows the existing wide collapse/reopen branch, while 100 and 80 remain compact. Change only that map entry and parameterize the existing reject-compact mutation control for both 160 and 120, preserving all real width conservation, Reader floors, row widening, keyboard and scroll checks. This is not a profile-carryover workaround or a new layout policy. Qualify the complete Live file on the latest rebased source. ADR required: no; fixture correction under ADR-086 and TASK-31633's accepted Media-specific grip amendment.

46. Reconcile ONNX readiness metadata with TASK-31827 and the accepted 2026-09-07 meeting-onnx-diarizer design, sections 1 and 4: sherpa-onnx/numpy are core dependencies but the named capability readout remains. Represent only this core-backed entry with no optional extra and baseline tier; reuse the existing baseline install commands, retaining every true-extra command and capability identity. Add RED controls for exact pyproject extra coverage plus the single core-backed capability, core dependency membership, and generic readiness/install-hint consumers before the production correction. Qualify complete optional-dependency, meeting-owner and affected capability files, with no model downloads or dependency installs. ADR required: no. ADR path: N/A. Reason: routine metadata correction implementing the already accepted core-install policy; no new provider, dependency, storage or runtime choice.

47. Repair the two reproduced selection test fixtures without changing production behavior: assert that the actual pressed row identity survives fallback instead of assuming a sort order that this source-order harness does not provide; equip the Media dotted-state-path fake with the query interface used by TASK-32045's accepted in-place reason update. Retain the exact fallback count, marker and checked-state assertions, and the no-fallback fault sentinel. Qualify the complete selection test file and independent review. ADR required: no. ADR path: N/A. Reason: test-only fixture alignment with existing source-order and mounted-query contracts. Investigate the separate 66/63 recompose census before proposing any ratchet change; do not increase its cap.

48. Close the newly attributed test-owned Notes/export SQLite resources through the existing same-file quiescence API after their workers/app contexts settle. Native late-run evidence identifies all eleven _real_notes_scope_service callers and the file-backed export-count test, not the repaired ingest harness. Convert the real Notes helper into an opt-in yielding fixture with a finally boundary immediately after its unique temporary database is constructed; preserve all eleven tests' real service/row assertions and cover service/setup failure. Quiesce only that test-owned database path, including its cached worker connections; assert its registry is empty. Apply the same exact-owner finally boundary to the file-backed export fixture, preserving all three database families and the real-thread/count assertions. Add real SQLite cached-worker/error-path lifecycle controls before the repair, and verify the complete affected shell plus native descriptors after final source freeze. Do not use global GC, descriptor limits, warning filters, or close borrowed paths. ADR required: no. ADR path: N/A. Reason: test-only owner lifecycle repair through established cross-thread quiescence APIs; no production storage or shutdown change.

49. Pay down the attributed inherited 66/63 whole-Library recompose breach without raising the cap or disturbing ADR-120 character-navigation structural barriers. The exact history is four new character-navigation sites offset by one Media fallback consolidation. Route three pre-existing viewer-substate sites in LibraryMediaController through the already sanctioned _sync_library_media_viewer_or_recompose seam: highlight reload and the two analysis-save early exits (missing selection, absent TextArea). Preserve stored highlights, editing reset, event stop, no worker dispatch on invalid save, footer/Items repaint and the existing missing-viewer full-screen fallback. Add RED seam controls and retain mounted add/delete highlight and analysis-save assertions; qualify complete affected Reader/render tests plus census, assembly and size guards after source freeze. ADR required: no. ADR path: backlog/decisions/097-boot-budget-ratchets.md. Reason: routine reuse of the existing targeted viewer-update contract; no new layout, ownership or navigation policy.

50. Save the independently reviewed steps 45–49 and rebase onto fetched dev a59536eb70 after all qualification processes stop. Preserve its schema-69 Console source-pin migration and request provenance/continuation transforms, complete-stream Kokoro encoding and validation, and half-flag Media correction. Resolve appended evidence additively and regenerate only the diagnostic inventory after reviewing its changed statements; retain PR transaction/cleanup repairs in overlapping tests. Run all twenty new/changed non-live upstream test files, the affected bridge and quiescence contracts, complete final Library shell with native attribution, and final unchanged architecture/import/UI-ready/CSS/preflight gates. No device/model downloads, paid provider calls or full-repository sweep. ADR required: no. ADR path: backlog/decisions/097-console-reference-backed-semantic-trace-ledger.md for retained upstream trace semantics. Reason: integrate accepted upstream behavior and verify review fixes without a new architectural decision.

51. User approved the three-review-task exception on 2026-09-08. The fresh all-local/remote-ref and registered-worktree Backlog filename census found maximum TASK-32107. Move only the review Loguru record 32014 to 32108, deferred Chunking Lab record 32015 to 32109, and Assistant stylesheet harness record 32040 to 32110. Update active references and append provenance, preserving upstream identities, original dates, completed implementation notes, and historical evidence paths. Run the complete ID test file, identity script, full derived-artifact preflight, and exact diff review before publication. Then rebase onto newly observed dev 7e81ed55db, preserve incoming Notes/TTS/trace/Media behavior, and qualify affected complete files and final review/CI gates. ADR required: no. ADR path: N/A. Reason: explicitly approved task-identity reconciliation and integration of accepted upstream behavior; no new architectural choice.

52. Post-wave-8 rebase qualification reproduced 3 failures among 85 guard cases: the Screen pin has 335 lines of slack, NotesController exceeds its unchanged cap by six restored Undo lines, and NotesState's census still expects the deleted inert timer. Preserve the Notes controller boundary by passing the existing Screen tree-reconciliation helper as a named late-bound dependency and extending its binding control; remove the unreachable historical placement block already superseded by upstream title re-sort/filter invalidation. Retarget only newly stale Screen Notes references, reconcile the 99-field census, tighten the Screen pin to the final measured value, and shorten redundant controller documentation to keep its 5276-line ceiling. Qualify complete affected owner, UI, trace/TTS and artifact files before publishing the rebase. ADR required: no. ADR path: N/A. Reason: existing-owner rebase adaptation and routine guard reconciliation; no new ownership, storage, or runtime boundary.

53. The mounted adaptive-Reader file exposes a merge-induced NameError in the new upstream zero-result Reader-clear branch: its retained-items read still names controller after the reviewed BrowseState move renamed that local to browse. Retarget that one read to the existing browse state, preserving every upstream empty-result/loading/external-detail guard and repaint. The mounted failure and F821 check reproduce before repair; verify the complete adaptive/Media files and static check. ADR required: no. ADR path: N/A. Reason: one stale local reference from overlapping accepted changes, not a behavior redesign.

54. Attribute the reproduced 973/974/975 UI-ready census to the existing scheduler first tick importing emergency_stop and scheduler_heartbeat before readiness. Keep scheduler timing and the 973 cap unchanged. Defer settings_diagnostics to its five existing failure branches and construct the real row-actions controller once on first explicit access through stdlib cached_property and its unchanged named wiring in the existing wiring module. Add cold-process absence/first-use and late-bound identity RED controls, then qualify complete settings-failure, row-action, wiring, ownership and repeated startup guards. ADR required: no. ADR path: backlog/decisions/097-boot-budget-ratchets.md. Reason: first-use import paydown implementing the existing ratchet policy, without a new controller boundary, proxy, or startup scheduling policy.

55. The complete wave-8 UI cohort stops at 167 passes on the compact File Notes exact-export paint check: enabled/focused state is present but the compositor still paints Saved at the sampled region; the unchanged isolated case passes. _update_controls queues layout refresh while hiding maintenance actions and changing the export label. Await the existing bounded predicate helper for the exact compositor label after focus, then retain the original display/enabled/focus and exact painted-label assertions. This changes only fixture readiness, not product rendering, timeouts, or expected copy. Qualify the complete File Notes file and neighboring Notes/navigation cohort. ADR required: no. ADR path: N/A. Reason: test-only asynchronous paint-settlement correction.

56. Incoming TASK-32043/32086 Reader tests retain thirteen reads of browse presentation fields on the controller, reproducing AttributeError after the approved ADR-128 state move. Retarget those reads to controller.state, as their neighboring Reader tests already do. Preserve exact empty-result, loaded/selected identity, mounted placeholder and service-release assertions, and scan all incoming affected Media files for sibling stale reads. Qualify the complete Reader/Media files and guards. ADR required: no. ADR path: backlog/decisions/128-library-media-browse-presentation-state.md. Reason: mechanical test adaptation to the existing presentation-state owner.

57. The complete row-action cohort (54 passes before failure) and isolated routing test reproduce a provider-selection KeyError, not a timing failure: its manually created inactive session has settings=None when the existing token preparation asks for that session's provider. The product new-chat path always supplies startup settings; the neighboring tab test obtains them through full UI sync. Seed the row-switch fixture with the existing default settings helper, retaining the real row Button press and exact store activation assertion. Do not bypass token preparation or weaken provider ownership checks. Qualify the complete routing/row-action and token-preparation files. ADR required: no. ADR path: N/A. Reason: test-only setup parity with the existing native session creation contract.

Step 56 sibling census: the complete Media retry reached 149 passes, then found four equivalent aliased reads in the incoming TASK-32085 empty-select refresh test. Retarget its local to browse state as well; preserve mounted/hidden reason and empty result assertions.

58. Complete verification exposes two fixture-observation mismatches. The native shell run stops at 503 passes on autosave's no-conflict DOM-absence check; the unchanged isolated case passes. NotesCanvas always mounts a hidden conflict region, and Back's state transition can precede removal of the old editor DOM. Assert no displayed conflict region, retaining the independent non-conflict state, exact single save and stored-version checks. Separately, the responsiveness mock census only inspects build_console_controllers and misses settings controllers now constructed by sibling builders. Inspect the same exact assignment pattern across the existing wiring module and add an explicit settings/submission slot control; keep real started/finished monitor brackets and failure assertions. Qualify both complete affected files again. ADR required: no. ADR path: N/A. Reason: test-only adaptation to existing retained-widget visibility and controller assembly, without production behavior changes.

59. The complete File Notes file passes after the paint-settlement correction. The next incoming Notes characterization test fails because it selects a tree placement ID while the shared static service deliberately exposes flat list rows. Reuse the existing _open_note_editor fixture route for both EditorReady cases; retain exact selected note n-1 and the paired armed/not-armed event assertions. The folder-new test stays unchanged. Qualify the full characterization and remaining Notes/navigation files. ADR required: no. ADR path: N/A. Reason: test-only selector parity with the shared service fixture, not a product routing change.

60. The diagnostic guard correctly detects that the reviewed metadata-only Undo failure moved from LibraryScreen into LibraryNotesController during wave 8. Move only that registry entry to its exact new owner, retaining the no-dynamic-fields/no-exception-capture requirement and the unchanged production label. The generated owner inventory already reflects this move. Qualify both complete diagnostic/privacy guard files. ADR required: no. ADR path: N/A. Reason: exact guard-owner retarget, without widening permitted diagnostic contents.

61. Native observation confirms the passing real-local Notes tree walkthrough retains 24 SQLite/WAL/SHM descriptors for its directly created notes-live.db after pytest returns; current-thread close cannot retire cached repository-worker connections. Give that exact temporary DB a yielding fixture whose finally quiesces its registered connections, covering all setup and assertion failures, while leaving borrowed/foreign databases untouched. Add a real worker-connection failure-unwind control and repeat the complete Live file under the unchanged native observer; require zero registered connections and final empty native paths. Preserve every four-size paging, error/retry and hierarchy assertion. ADR required: no. ADR path: N/A. Reason: test-owned resource lifecycle repair using the established exact-file quiescence API, without production ownership or shutdown changes.

62. The remaining inventory identifies two small oracle/setup mismatches: the ordinary Conversations empty-filter fake omits the existing unavailable-browse sentinel, and the Notes unmount source check mistakes the words 'await below' in a comment for executable suspension. Supply None for the ordinary fake's scope and compare parsed invalidation-call/await source positions, preserving the exact page-one request and before-first-executable-await contract. No runtime change. ADR required: no. ADR path: N/A. Reason: fixture state parity and a syntax-aware guard, not weakened behavior or ordering.

63. Two isolated Notes journey paint failures reproduce. Lasting-sync roots intentionally scroll their vertical action stack; test primary Review first-paint visibility, then scroll disabled Retarget into view for the unchanged contrast check and restore Review. Session Git is an action in Folder Files Manage mode, while the fixture starts in Edit; enter Manage through the real button before asserting/activating the visible Git entry. Preserve 40x20 geometry, exact copy, contrast, focus and navigation assertions. Requalify the complete journey using a fresh per-user temporary root: the four authority fixtures under /private/tmp inherited gid0 instead of the process gid20, correctly rejected by filesystem metadata validation. Do not alter the filesystem policy or strip xattrs. ADR required: no. ADR path: N/A. Reason: test navigation and filesystem precondition corrections under existing product contracts.

64. Independent review attributes six remaining honesty/row-census failures to accepted contracts: TASK-31553/25812 moved Library rules out of the boot-only bundle; TASK-30043 explicitly superseded the single-row toolbar and selected-label wording; ADR-113 retired the phase39 helper re-exports; TASK-31554 restored compatible note-row IDs. Read generated app_css_text plus the unchanged source rules; exercise the real browse-mode helper and exact chooser/action Horizontal parents; retarget only helper imports; retain exact marker/disabled flips with the approved short aligned labels; count all seven row construction sites by path/line/callee, including both flat and tree notes under the shared prefix. Qualify complete honesty, conversations multiselect and toolbar-adaptation files. ADR required: no. ADR path: backlog/decisions/113-collections-capture-authority-and-legacy-boundary.md and existing ADR-097. Reason: test-only reconciliation with explicit accepted product/CSS/identity changes, with no production edit or cap increase.

65. Independent diagnosis of the three remaining performance/settlement failures identifies real regressions. Restore the ordinary rail's existing validated layout-reference cache while preserving the Files-owned rail branch. For a non-entry Media deep-link, await the existing structural open-surface projection before dispatching detail/browse/facet work, so missing-owner callbacks cannot trigger repeated recomposes. Arm Media list-return settlement before projection and do not re-arm in its mounted follow-up, allowing the replacement row owner's first geometry to satisfy the request. Preserve the exact one-recompose, one-query, scroll-before-control-focus and exact-settled assertions; reproduce those cases, qualify their complete files and independent review, then requalify the changed Library source. ADR required: no. ADR path: N/A. Reason: routine restoration of existing cached-reference, structural mounting and settlement-order contracts; no new state owner, timing policy or raised limit.

Step 65 review/qualification refinement: unconditional pre-arming bound the retained viewer's old row owner before its targeted replacement, reproducing fourteen settlement regressions. Pre-arm only the structural missing-viewer return; retain post-mount arming for the existing viewer path. Independent review also requires a navigation-admission recheck after the new Media mount await and preservation of in-place same-kind Reader updates. Count actual awaited Screen recomposes in the cross-kind regression rather than only refresh requests, retaining exactly one structural transition. Add superseded-await and same-kind owner-identity controls before the refinement.

66. The remaining three honesty failures use incomplete fakes: two route fixtures supply a legacy Media service without search_media, and the in-place toggle host omits the analyzer-reason seam, triggering fallback and detached-widget assertions. Reuse the canonical page-capable Media fake with identical seeded rows and supply the provider-ready analyzer reason on the toggle host. Preserve all exact route, Escape, marker and enabled-state assertions; qualify the complete honesty cohort. ADR required: no. ADR path: N/A. Reason: test-only parity with existing service and host contracts.

67. Rebase the published wave-eight checkpoint onto fetched dev 38f7fe63d4, preserving all seven new Library keyboard/focus methods, the Notes surface-identity fence, TTS destination-resolution changes and both appended lesson sets. The incoming 336-case cohort reproduces a stale universal-binding expectation: only Tab/Shift+Tab are universal, while Shift+F6 retains its existing route gate. Correct that test metadata, not production check_action. Rerun offline fake-server TTS files with loopback permission after three sandbox port refusals. Preserve the original Screen and NotesController caps and investigate exact current dead-delegate paydown before any new extraction design. Qualify complete affected files, final native lifecycle and all preflight/review/CI gates before normal merge. ADR required: no for integration and test metadata. ADR path: N/A. Reason: preserve accepted upstream behavior; any genuinely new owner design requires its own recorded decision.

68. The follow-up reference census identifies a routine existing-owner reduction, not a new architecture: remove only the four unreferenced helpers _decorative_rail_counts_for_comparison, _safe_sync_scope_text, _hub_source_count_label and _library_workspace_scope_label. Remove three obsolete Screen forwarders (_cache_library_media_preview, _library_prompt_basic_unavailable_reason, _library_notes_work_session_reader_width), retarget their enumerated test/evidence callers to the existing Media/Prompts/Notes controllers, and extend the existing Screen-absence pins while preserving controller callable pins. Shorten redundant historical docstrings only to recover the remaining line excess, proving executable AST identity apart from those exact seven removals. Preserve all seven new dev keyboard methods. Restore the pre-increment NotesController 5276-line ceiling by concise documentation, retaining its new executable surface fence. Qualify complete affected controller/Media/Prompts/Notes/keyboard files and unchanged caps, then final native Library and preflight checks. ADR required: no. ADR path: N/A. Reason: dead-code removal and mechanical caller retargeting within existing owners; no new controller, public contract, dynamic dispatch, state ownership or behavior change. Independent review clarified that this stays within the approved reconciliation scope and needs no additional owner design.

69. After wave-nine native qualification and exact-head Qodo review pass, save its evidence and rebase onto fetched dev80f29a9a1d. Preserve incoming Notes-loader settlement, conversation handoff, ingest recovery and Media polish alongside existing review-owned controllers/state boundaries. Resolve overlapping tests against their canonical owners and retain all accepted behavior. Run complete incoming changed test files, bridge/controller/size/import/CSS/preflight guards, and final-source native Library verification. Do not raise any cap, infer a whole-repository sweep, or change unrelated environment state. ADR required: no. ADR path: N/A. Reason: integrate accepted upstream behavior and mechanically reconcile existing owners; any demonstrated behavioral defect gets its own recorded minimal repair and RED/GREEN evidence before implementation.

70. Wave-ten rebase reproduces Screen growth to 32193 lines / 1238 methods against unchanged 31868 / 1229 caps. Preserve all nine incoming methods; remove nine existing two-line Screen delegates whose remaining callers can directly use their existing Media/Notes controller owners. Retarget the exact Screen, canvas-sync and two test callers, preserving controller-callable guards and extending the established Screen-absence sets. Shorten historical documentation only to repay the remaining Screen and reader/ingest/media/prompts controller line growth. Verify normalized runtime AST identity except the enumerated delegates and caller retargets, complete architecture and affected UI files, generated diagnostic statements, preflight, and native lifecycle. ADR required: no. ADR path: N/A. Reason: bounded existing-owner forwarding cleanup and documentation compression; no new ownership boundary or relaxed limit.

Step 70 implementation refinement: the shared canvas-sync helpers intentionally accept Screen or controller receivers, so both remain on Screen. Replace those two proposed deletions with Media page-control focus and filter-request forwarders whose enumerated callers are real Screens. The final nine-name census and AST proof are recorded in the reconciliation report.

71. Wave-ten native verification and a separate two-case reproduction expose the same stale fixture assumption: retained Conversation handoff is now gated by workspace eligibility, but the stale-list test never selects a workspace or links its loaded conversation. Seed a real active workspace and membership for chat-001 before mounting, retaining every stale-action, independent Reader, tooltip, Retry and focus assertion. Do not bypass the new workspace gate or weaken the expected enabled state. Verify both sizes and the complete handoff file, then requalify the full Library file on the next integrated source. ADR required: no. ADR path: N/A. Reason: test-only setup parity with the accepted conversation-handoff contract.

72. After the frozen wave-ten native run finishes, save its exact outcome and integrate fetched dev c4a7b1911f14181faa471eb1ea209cfdeed98226 (structural-wait deadlines/cancellation, Collections row behavior and documentation). Preserve accepted behavior and existing owner boundaries, reconcile overlapping tests and current caps without raising them, and qualify complete affected files. The prior 1427-case run exited 143 without a pytest summary and is inconclusive; retry its complete files in smaller named cohorts with durable per-test results so another interruption cannot obscure completed-file evidence. Finish current-source native, diagnostic/preflight and final-head review gates before normal merge. ADR required: no. ADR path: N/A. Reason: upstream integration and test evidence recovery; any new behavior repair needs its own RED/GREEN plan.

73. Wave-eleven integration reproduces three unchanged cap failures: Screen32130/1239, Export1317 and Skill-import801. Preserve all ten new Screen methods and remove ten existing two-line forwarders (six Media settlement helpers and four Notes focus helpers) whose exhaustive fixed-ref census found only concrete Screen/test callers. Keep polymorphic, dynamic, decorated, identity-pinned and wiring-bound surfaces intact. Extend the established absence controls before deletion, retain controller-callable proof, retarget exact callers, and shorten historical documentation/formatting to meet all original caps. Require normalized executable-AST identity except those enumerated forwarders/callers, complete affected UI/owner files, and independent review. ADR required: no. ADR path: N/A. Reason: bounded existing-owner compatibility cleanup without behavior redesign, a new boundary, or relaxed limits.

Step 30 ADR required: yes. ADR path:
`backlog/decisions/128-library-media-browse-presentation-state.md`.
Reason: explicit UI-local state composition, preserving ADR-067 service/page
contracts. Exact member map and qualification steps are recorded in
`Docs/superpowers/plans/2026-09-08-pr2427-media-browse-state.md` before code changes.

Step 19 exact Prompts inventory: `_library_prompt_editor_active`,
`_sync_library_prompt_selection`, `_sync_library_prompt_memberships`,
`_stop_library_prompts_search_debounce`, `_capture_library_prompts_filter_cursor`,
`_invalidate_library_prompt_detail_generation`, `_invalidate_library_prompt_history`,
`_sync_library_prompt_history_region`, `_update_library_prompt_status_static`,
`_notify_prompt_dirty_veto`, `_clear_library_prompt_delete_pending`,
`_library_prompt_write_worker_is_active`, `_sync_library_prompt_mutation_presentation`,
`_library_prompt_nearest_survivor_focus`, `_notify_library_prompt_delete_failure`,
`_refocus_library_prompt_delete_action`.
Skills inventory: `_library_skills_canvas_kwargs`, `_consume_library_skill_scroll_pending`,
`_library_skill_on_disk_path`, `_update_library_skill_warnings_static`,
`_set_library_skill_discard_enabled`, `_refresh_library_skill_script_grant`.
Keep the deferred mirror/focus callbacks and `_library_prompt_can_update_original`
because the census found identity-sensitive or tracked evidence consumers.

2026-09-08 checkpoint: Wave7 rebase completed at cc0a5bd537. The affected Library
group is 359 passed / 18 failed; ratchets are 43 passed / 5 failed. Verification
and final-head review remain open. Fresh refs/worktree census required one more
review-only renumber: Assistant harness TASK-32013 to TASK-32040; upstream Media
debt TASK-32013 is unchanged. Historical mappings below remain provenance.

The isolated recovery worktree is `.worktrees/pr2427-review-recovery`. Rebase onto fetched dev `3090013cfea4dbf6133ac43d024656e2eb3a2a56` completed locally at `4a74c5d7e02552a5351d59df7647ea8811526bab`; publication remains open (AC #1). The three review-only collisions were renumbered to TASK-32013/32014/32015, with 3,589 task records passing the identity guard. Agent persistence has 34 complete-file passes and no retained SQLite descriptors under the native observer. Verification and final-head review remain open; see the dated reconciliation report for exact evidence and remaining failures.
74. Wave-eleven terminal qualification finds two deterministic stale fixtures: the analysed-secondary floor still measures the canvas as 36 instead of its accepted 32-cell content width inside the 36-cell pane (TASK-32060), and the unbound Skills status fake lacks the new structural-wait helper chain. Pin both real pane/content geometry while retaining painted-text assertions; bind the real status helpers with empty wait state and retain mounted/hidden-row/no-recompose assertions. Reproduce before changing tests. Separately investigate the intermittent Trash toolbar return and Media initial Retry failures; isolated passes are not fixes. Add bounded failure diagnostics if needed, preserve all final-state assertions, and qualify complete affected files. Then save progress and integrate fetched dev733f7a628c's accepted Buddy/scheduler changes, reviewing actual conflicts and qualifying affected offline files without raising caps. ADR required: no. ADR path: N/A. Reason: stale fixture reconciliation and integration of already accepted behavior; no new production boundary or architecture.
75. Repair the reproduced Console tab-strip teardown race at its existing surface owner: retain the exact strip across removal, stop replacement when it is detached, and preserve live mount errors. Add a deterministic mounted teardown control before the implementation and run the full surface/startup files. Align the no-splash setup-failure fixture with existing logged suppression while still asserting no readiness or scheduler admission. Reconcile only obsolete diagnostic pins and explicit redacted durable-marker expectations, preserving privacy and structured outcomes. Review headless navigation and resize fixtures against accepted retained-runtime/Buddy contracts before changing assertions. Investigate the painted-body readiness failure with a controlled render delay and retain final paint/geometry assertions. ADR required: no. ADR path: N/A. Reason: routine lifecycle bug repair within existing ownership and test alignment with accepted upstream contracts; no new architecture or privacy policy.
76. Wave-thirteen rebase at 59a5ad3826 preserves dev2e3389e694 but reproduces unchanged Screen ratchets: Chat506/505 methods and Library31999/31868 lines, 1236/1229 methods. Preserve every incoming method. Remove the single-use Chat character-loader delegate and seven existing Library forwarding methods: _sync_library_ingest_rail_from_shell, _update_library_ingest_fold_hint, _scroll_library_ingest_queue_into_view, _update_library_ingest_group_receipt, _record_library_notes_focus_interaction, _discard_new_library_note_claimed and _patch_local_media_record. Retarget the enumerated concrete Screen callers directly to their existing controllers, extend existing absence pins while retaining controller-callable proof, and shorten historical documentation only to cover the remaining line excess. First finish the active frozen incoming UI cohort; require independent census review, normalized executable-AST identity apart from these exact delegates/callers, complete affected architecture/UI files, original caps, preflight and native Library verification. Address only demonstrated additional failures with their own recorded root cause and RED/GREEN. ADR required: no new ADR. ADR path: backlog/decisions/140-official-kokoro-pytorch-runtime.md for the accepted incoming TTS boundary; existing Library ownership remains unchanged. Reason: routine existing-owner compatibility cleanup and upstream integration; no new abstraction, behavior, ownership or relaxed cap.
77. Wave-thirteen native Library verification reproduces both Starter geometry/focus cases: the fixture expects the former Chunking Lab rail stop after the final Starter action, although incoming dev moved Chunking Lab into Details, hidden in Starter. After the frozen native run finishes, pin wraparound to the already-established first visible control, preserving every geometry, compositor, ordered-Tab, navigation exclusion, painted-text and graduation assertion. Re-run both dimensions and qualify any other demonstrated native failures separately. Record the terminal descriptor snapshot; midpoint descriptors alone are not proof of a leak. ADR required: no. ADR path: N/A. Reason: stale fixture alignment with the accepted incoming layout; no production navigation change.
78. The existing 100x30 search-field regression test independently fails because the new three-cell clear button reduces input content width from 15 to 12 cells. After native source freeze ends, recover those cells within the existing search row by removing redundant input padding and the doubled input/clear boundary, without widening the rail, shortening the placeholder, hiding Clear, or changing navigation. Extend the real rendered test across compact/wide widths with painted placeholder, visible Clear, keyboard focus and clear-action checks; retain the semantic field frame and verify generated stylesheets. Separately review the remaining Notes geometry expectations against the removed three-row Chunking Lab strip before changing any expected allocation. ADR required: no. ADR path: N/A. Reason: bounded layout regression repair and fixture alignment, not a new UI structure or behavior.
79. Native completion has 830 passes and 21 failures, with no retained SQLite descriptors. Independent review confirms TASK-32064 returns exactly one compact row to the Notes flexible owner (not three); update the obsolete strip absence and exact allocation pins while retaining fixed rows, total height, focus visibility, zero-work seams and +6 surplus growth. The wide Editor Back failure passes alone, and source shows filter_records precedes canvas replacement and its deferred focus callback. Add a controlled delayed filter-projection variant before modifying its setup wait; then wait for the filtered DOM and focus settlement before selecting the target, retaining every exact Back focus, placement, scope and scroll assertion. Do not change production restoration without a failing application contract. ADR required: no. ADR path: N/A. Reason: stale geometry oracles and a fixture readiness race within accepted rendering contracts.
80. Qodo review on a515a74fcb requests two style/contract corrections: keep the Tests.Agents imports contiguous with other local-project imports, and annotate/document the split-JSON test parameter and return value. Apply only these nonbehavioral changes and run both complete test files plus scoped lint before replying and resolving the corresponding review threads. ADR required: no. ADR path: N/A. Reason: import grouping and test documentation only.
81. Independent review and nine compact RED cases confirm TASK-32063's unconditional empty Work authority prefix violates TASK-19000 when the list is hidden. Preserve wide deduplication, restore the prefix only in compact Work, and refresh the existing authority node on responsive transitions without remounting; add compact-wide-compact real-widget controls for create/loading/editor. Preserve the newer honest-Next rule: loading has no invented wait instruction. The search field review also finds its ID-level resting CSS masks generic Input focus styling; add a same-specificity focus rule using existing Notes filter tokens, preserve joined-border width, and require computed visible focus change (two RED dimensions). ADR required: no. ADR path: N/A. Reason: narrow regressions against existing authority/accessibility contracts; no storage, ownership or routing change.
82. After the stronger filtered-DOM wait, the ordinary wide Back case still fails. Exact focus-call stacks prove Back first restores row n-18, then an older canvas `_restore_then_explicit` restores the captured filter and zero scroll. Investigate the existing Notes targeted-restore owner and compare the established Media live-focus guard. Add mounted deterministic controls for late automatic restoration after a newer real row claim, plus missing/grip fallback controls, before changing the owner. Preserve explicit action follow-ups, cross-region vetoes and live scroll; qualify complete owning files and original ratchets. Temporary stack diagnostics are investigation evidence, not a substitute for the regression controls. ADR required: no. ADR path: N/A. Reason: lifecycle ordering bug within existing focus ownership, not a new navigation policy or boundary.
83. Current-head Qodo on acaa890250 reports five issues. After frozen native verification completes, reproduce the export-path and rejected-durability-admission contracts before production edits. For Markdown export, use the central canonical validator at the existing user-selected destination boundary and pass its returned path to both mkdir and open; retain tilde/relative-path behavior and cover symlink escape, accepted canonical paths and no write on rejection without claiming race-proof filesystem confinement. For roleplay refresh, use a real prepared plan and closed durability owner to prove rejected admission releases only that plan's fork transition without starting persistence; retain stale-plan and admitted-writer behavior. Add realistic loopback request-stack coverage for the requests factory without replacing Session.request, including deterministic cleanup and default/explicit timeout behavior. The code census currently finds no production caller of build_requests_session outside its definition, so do not invent a new caller merely to satisfy review wording. Correct the two row_actions import/docstring nits. Run complete affected files and unchanged owner/static/preflight gates, publish verified fixes, and reply to each exact review thread. ADR required: no. ADR path: N/A. Reason: routine validation/lifecycle bug fixes and missing boundary coverage within established owners; no new path policy, TLS policy, architecture or relaxed cap.
84. The final native Library run on acaa890250 completed 855 passed/1 failed with no retained SQLite descriptors. The sole wide deep-link Back case independently fails because it expects the filter after Escape, while the established action explicitly restores the selected rail row (ADR-086 return contract). The repaired late automatic-focus guard now prevents an old filter callback from concealing that return. Align the wide assertion with the existing rail-row contract, retain cleared explicit intent and verify the subsequent compact resize's actual contract before adjusting any further assertion. Qualify the complete deep-link/Back/focus group and unchanged layout owners; do not change production merely to restore a stale automatic focus steal. ADR required: no new ADR. ADR path: backlog/decisions/086-library-adaptive-reader-shell.md. Reason: test-only reconciliation with the accepted navigation owner; no new return policy.
85. After saving step83/84 repairs, integrate fetched dev9faf96d9f86b98916a54d7a895e3c0411fefc912. Incoming changes are Buddy qualification documents/artifacts, one test file and its task; no production changes. Preserve all incoming evidence, run the complete incoming offline test file, and retain existing production qualification. At publication re-sweep IDs and move only the review Loguru task32108 that now collides with the landed Buddy task; leave upstream IDs and history untouched. Require current-source native Library completion, exact-head Qodo, derived preflight and normal protected merge checks. ADR required: no. ADR path: N/A. Reason: integration of upstream qualification evidence and mechanical review-task hygiene, not a new runtime decision.
86. Qodo on 3fe94c842e requests complete fixture/parameter annotations and Google-style Args for the two public Evals import-closure tests. Apply only test documentation/types, leaving production and the active native Library test source frozen. Verify the complete packaging file and scoped Ruff, then publish and reply to comment3965835933 after the current qualification/integration checkpoint. ADR required: no. ADR path: N/A. Reason: nonbehavioral test contract documentation.
87. After frozen wave14 native completion, checkpoint its exact result and step86, then integrate dev a36fc6133c (or a freshly inspected successor). Main has read accepted ADR-124, ADR-125 and the character-TTS ADR-028 amendment completely. Preserve the incoming immutable Canvas profile/snapshot and source-only boundaries, Python>=3.12 floor, lock-safe bounded SQLite helper ownership, native live-TTS close policy and lazy repository close latch. At Canvas overlaps, transfer incoming language and served-profile fallback changes into existing ConsoleMessageController methods rather than restoring removed Screen delegates; keep controller-retargeted tests and incoming exact-card/profile readiness. Retain the bounded gateway read fix. SQLite owner registrations/census and backup controls already present upstream must appear once, with incoming helper/native-probe seams and C48 retirement preserved. Keep one quiescent connection factory keyword, incoming consistent shutdown labels/meeting-owner setup, and PR Buddy/no-screen/Watchlists changes alongside lazy TTS. Qualify complete changed offline Canvas/Chat/Web_Server/packaging, DB/private-helper and TTS owner files in named cohorts, including local SQLite cases whose live keyword requires explicit classification. Keep native/platform evidence separate from local host resource failures; no semaphore/host cleanup. Use verified cached Mermaid inputs for the new reproducibility guard where available and inspect any required asset-fetch scope before running it; do not bypass its hashes or admission policy. Preserve original owner/import/runtime caps and evaluate new debt at the accepted correction-specific baseline rules rather than raising limits. Review any demonstrated failures with RED/GREEN before bounded fixes; then current-source native/final-head review/checks/live-base gates and normal merge. ADR required: no new ADR. ADR paths: backlog/decisions/124-canvas-mermaid-subset-and-immutable-runtime-profiles.md; backlog/decisions/125-lock-safe-private-sqlite-validation.md; backlog/decisions/028-character-tts-generation-profile-ownership.md. Reason: integration of accepted architecture, not a new storage, runtime, profile or security decision.
88. Reconcile two demonstrated upstream test-oracle failures without changing runtime behavior. The real TTS child writes application startup logging onto its JSON phase channel; redirect that fixture's ordinary stdout to stderr before imports/construction while retaining explicit `sys.__stdout__` phase messages and exact JSON/timeout/foreign-cohort assertions. The runtime-floor test requires an unpinned `3.12` string for a generated-assets job intentionally pinned to `3.12.11`; assert the actual per-job versions, preserving the immutable builder pin and the 3.12 floor. Use the existing 14 failing child cases and failing complete packaging file as RED evidence, then run complete affected files/cohorts and scoped static checks. Only these two test files may change during the unrelated native Library/Canvas runs; production, shared fixtures and those runs' test bytes stay frozen. ADR required: no. ADR path: existing ADR-124 and ADR-125. Reason: fixture channel separation and current accepted CI-version assertions, not a runtime, authority or dependency change.
Step 88 diagnostic refinement: ordinary `sys.stdout` redirection alone still produces all 14 failures because installed Textual captures `sys.__stdout__` as `app._original_stdout` and forwards headless print events there. Point that fixture-owned app output stream to stderr as well, before mounting; keep the original phase pipe and strict parser unchanged. This is the same diagnostic-channel separation, not a production logging change.
89. Address six Qodo test-contract findings on published 69108f4d1d: complete the persona activation fixture annotation and Google-style docstrings in actor-pack activation/contracts, and add the missing documentation/annotations in the Console delegate, Library assembly/private-owner, and exclusive-worker inventory tests. Preserve all test bodies, assertions and production behavior. Verify the six complete affected files, scoped Ruff and whitespace checks, then publish before replying/resolving comments 3966299125/3966299137/3966299147/3966299157/3966299166/3966299174. These nonbehavioral test files are outside the active Library shell run; its runtime/shared fixtures and test bytes remain frozen. ADR required: no. ADR path: N/A. Reason: documentation and type annotations only.
90. Investigate the complete native Library result (855 passed, one autosaved-then-emptied blank-note GC failure, final SQLite descriptors empty). First reproduce the save/GC ordering deterministically with real SQLite and controlled service-reply boundaries; preserve the original failing test and its row-deletion assertion. Separate transient failure-traceback retention from actual terminal cleanup. If confirmed, use the existing note-session pending-work/admission mechanisms to serialize the affected operation, preserving session-created/finally-empty/title-provenance checks, version-checked external-conflict protection, explicit Save exemptions and best-effort exit behavior. Add controls for intervening authored edits and unrelated notes before any runtime fix. Keep the correction within existing owner/screen limits, then run complete affected Notes/Library files, independent review, derived gates and a fresh complete native Library qualification before publication/merge. ADR required: no new ADR for a routine ordering correction implementing accepted contracts. ADR paths: backlog/decisions/055-library-destructive-action-reversibility-rule.md and existing task-2858 AC5. Reason: preserve the established silent-GC exception and concurrency protections, not expand deletion authority or change persistence policy. Any necessary policy change requires separate approval.
91. The original autosaved-then-empty fixture still fails after the causal save/GC fix. Exact load_text/Changed/save-outcome tracing shows its unfocused body assignment is overwritten by accepted TASK-32062 snapshot projection before Changed consumes it; Back receives the old nonempty canonical draft and never enters GC. Focus the real body before editing and await the canonical empty-draft receipt before Back, retaining the original row-deletion assertion. Add an external-write-at-admitted-delete control and assert zero delete calls for intervening authored content/earlier save conflicts. Run complete affected files and native cleanup on stable source. ADR required: no. ADR path: existing ADR-027/055 and TASK-32062. Reason: fixture alignment with accepted focused-field authority and stronger concurrency evidence; no new UI policy.
92. Qodo on b84605f81d reports stale Console request/gateway source-line census entries plus four public-contract documentation/type issues. Reproduce the complete strict provenance test file before updating every stale exact source location against actual route markers and AST gateway calls; retain all route/ownership predicates and strict tests. Add Google-style parameter/result contracts to Settings lazy profile adapters and controller builders, keep type-only local imports grouped, and annotate both lazy evaluation export hooks without introducing eager runtime imports. Preserve all executable behavior and size/import caps; use complete affected files, executable-AST equivalence for documentation edits, architecture/import gates and preflight before publication/replies. ADR required: no. ADR path: N/A. Reason: mechanical census repair and callable contract documentation, not a new ownership, dependency or privacy policy.
93. Settings review finds all 13 lazy profile adapters defined twice: Qodo's earlier undocumented copies are immediately shadowed by the later documented implementations. Remove only the first duplicate set after proving no intervening top-level capture/call/default evaluation consumes it; retain the effective documented definitions and consolidate duplicate type-only imports. Add an exact one-definition characterization to the existing Settings lazy-import test file before deletion, preserve named monkeypatch seams, compare effective last-definition ASTs and runtime module AST after excluding only the shadowed copies/type-only block placement, and run complete Settings/profile/import files plus current caps. ADR required: no. ADR path: existing ADR-097 lazy-settings ownership. Reason: deleting dead duplicate definitions without changing effective behavior or import authority.
94. The complete Settings cohort passes 248 tests but reports descriptor growth of 262. Attribute it with the unchanged native SQLite observer and native descriptor types/paths, without forced GC, connection monkeypatches, relaxed thresholds or host cleanup. Separate existing fixture ownership from the behavior-equivalent Settings deduplication before assigning cause. Inspect any retained exact app/service owners and only implement a bounded fixture correction after controlled causal evidence; preserve foreign resources and qualify complete affected files. Publication of the verified Qodo fixes may proceed as a progress checkpoint, but merge waits for the resource finding to be explained and any required fix verified. ADR required: no for read-only diagnosis or established-owner fixture cleanup. ADR path: N/A. Reason: resource verification, not a new lifetime/cleanup policy; escalate any necessary ownership change.
<!-- SECTION:PLAN:END -->

## Implementation Notes

Steps 90/91 repair the proven committed-save/blank-GC ordering through existing
coordinator destructive admission, then revalidate the exact session and blank
provenance before a version-checked delete. Intervening authored drafts follow
normal saving; external changes and unrelated rows remain protected. A separate
original-fixture focus/Changed race is corrected without weakening its deletion
assertion. Four controlled real-SQLite cases pin actual destructive calls and
row/version preservation. ADR-027/055 apply; no new boundary, policy or cap.

Complete native verification: 902 passed, 10 warnings in 1662.29s, exit 0, final
SQLite descriptor inventory empty. Five complete architecture files pass 79
tests; derived preflight passes; changed controller/test lint is clean and
Screen lint matches the pre-existing baseline. Independent review cleared the
repair and strengthened tests. Exact evidence, frozen hashes and earlier
cohorts are in `backlog/docs/pr-2427-rebase-reconciliation.md`; the commit-vs-reply
and focused-draft lesson is recorded in `lessons-testing-evidence.md`.

Task remains In Progress until the published final revision's review/checks and
normal protected merge complete. No repository-wide sweep is claimed.
