---
id: TASK-18940
title: 'Server-offloaded scheduled agent tasks: execution seam and result pass-back'
status: In Progress
assignee:
  - '@robert'
created_date: '2026-08-19 11:05'
updated_date: '2026-08-19 11:05'
labels:
  - scheduling
  - agents
  - server
  - architecture
dependencies:
  - task-18937
  - task-18938
  - task-18939
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The strategic follow-up from the TASK-18936 parity audit, carrying the owner's stated long-term direction: **tldw_server is to be treated the same as hermes's hosted gateway — scheduled work is offloaded to the server to execute, and results are passed back to the client if so desired.** This closes the one capability where hermes runs scheduled agent work today and chatbook cannot: `agent_task` automations are fully modeled locally (ADR-018: families, lifecycle, health, preview, approval policies) but execution is `execution_unavailable` because ADR-018 deferred execution to server integration that never landed.

Scope: define and implement the first server-execution seam for `agent_task` automations — the client creates/previews a definition (already built), the server owns execution when the account is server-scoped (`owner_id="server:<user_id>"`), and completed results flow back through the existing sync/notification channels into the client's workbench (and optionally Console, reusing the existing handoff machinery). Local execution remains the offline fallback for local-owner definitions — this task adds the server path, it does not remove the local-first one. Hermes-precedent capabilities to fold into the contract: durable execution-audit history (the `AutomationAuditEvent` model exists), per-task model selection passed with the definition, and continuity/missed-fire reconciliation when the client reconnects (consumes TASK-18937's accounting).

This is architecture-first work: an ADR defining the client↔server execution contract (request shape, claim/lease semantics or server-side queue ownership, result-delivery channel, approval policy for server-side tool use, failure/timeout semantics, and what "passed back if so desired" means concretely — notification vs Console handoff vs workbench result row) must precede implementation. Sequenced after 18937–18939 so missed-fire, run-now, and timeout semantics exist as shared vocabulary in the contract.

**Server-side survey (2026-08-19, `tldw_server2` checkout, `origin/dev` @ `385afa95`)** — what the ADR builds on:

- **Reminders already execute server-side through a real jobs pipeline**: `app/services/reminders_scheduler.py` (APScheduler) enqueues due reminders into the core Jobs pipeline (`JobManager`, domain `notifications`, type `reminder_due`); `app/core/Reminders/reminder_jobs.py` consumes them with **durable run bookkeeping** (`create_reminder_task_run` rows, `run_slot_key` dedupe, terminal statuses `succeeded`/`skipped`/`failed`) and delivers via **user notifications** (`create_user_notification`, kind `reminder_due`, `dedupe_key=f"task:{task_id}:{run_slot_utc}"`). Gated by `REMINDERS_SCHEDULER_ENABLED` env.
- **The unified control plane exists and is client-ready**: `endpoints/scheduled_tasks_control_plane.py` exposes GET "" (unified task list across reminders/watchlist jobs/automation definitions), full CRUD on `/reminders`, and a complete **automation-definitions API** — capabilities, previews (24h TTL, payload-hash idempotency), definitions CRUD, audit history, pause/resume/archive/duplicate. Schemas in `scheduled_tasks_automation_schemas.py`; DB layer `Scheduled_Tasks_DB.py` (DefinitionRow/PreviewRow/AuditEventRow).
- **The gap is exactly where the client audit found it**: automation definitions on the server default `health="execution_unavailable"` (`DEFAULT_DEFINITION_HEALTH` in `scheduled_task_automation_service.py`) — definitions are validated, redaction-policy'd (agent_task input messages get `metadata_only` redaction), and audited, but **nothing dispatches them**. There is no APScheduler feed from automation definitions into the Jobs pipeline, and no `agent_task` job consumer. The modeling half is done on both sides; the execution half is missing on both sides.
- **Implications for the contract**: (1) the natural server execution shape mirrors the reminders pattern — a scheduler feed from automation definitions → Jobs pipeline → an `agent_task` consumer → notification delivery with a run-slot dedupe key, which composes with the client's missed-fire accounting (18937) for reconnect reconciliation; (2) the server's Jobs pipeline already has run lifecycle events/SLA plumbing the timeout semantics (18939) can align with; (3) the client's `missed_count`/`missed_at` being client-local (18937 decision) needs an explicit reconciliation rule in the ADR: server run rows are authoritative for what ran; client missed-state describes what the *client scheduler* missed, and the two must not double-count; (4) approval policy for server-side agent tool use must build on the definition's `approval_policy` field, which the server already validates but nothing enforces yet.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 An ADR (amending/superseding the relevant ADR-018 clause) defines the server-execution contract: definition upload, execution ownership, result-delivery channel(s), approval policy for server-side tool use, failure/timeout semantics, and reconnect reconciliation — drafted and accepted before implementation begins
- [ ] #2 A server-scoped `agent_task` definition can be created/previewed locally, submitted for server execution, and its `health`/lifecycle transitions honestly (no more permanent `execution_unavailable` for server owners)
- [ ] #3 Completed server executions deliver results back to the client through at least one concrete channel (workbench result row, notification, or Console handoff — per the ADR), with the delivery visible in the UI and durable
- [ ] #4 Execution-audit history is durable end-to-end (client-visible audit trail of server executions, reusing `AutomationAuditEvent`)
- [x] #5 Local-owner definitions keep today's local behavior unchanged — the local-first path is not regressed (pinned by tests)
- [ ] #6 Missed-fire/run-now/timeout semantics (18937–18939) reconcile correctly across a client reconnect (e.g. occurrences the server ran while the client was away are reported once, not re-derived or duplicated)
- [ ] #7 Per-task model selection rides the definition payload (hermes parity), bounded to the providers the server account can use
- [ ] #8 Live verification against a real tldw_server instance (per lessons-live-verification) — at minimum one real server-executed automation with result pass-back observed end-to-end; what was and was not verified live is recorded honestly
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: yes.
ADR path: backlog/decisions/077-server-offloaded-scheduled-agent-tasks.md (drafted as 072, renumbered twice at merge time as concurrent branches claimed 072–076; accepted 2026-08-21 with both judgment decisions approved by the owner; amends ADR-018's "execution remains execution_unavailable until server-side automation execution is integrated" clause).
Reason: cross-system service contract (client↔server execution ownership, result delivery, approval policy for server-side tool use) — squarely in ADR-required territory, and the owner has stated the long-term direction this task exists to realize.

1. Draft ADR-077: execution contract, result-delivery channels, approval policy, reconciliation semantics
2. Server client + service layer: definition submission, execution status polling/push, result retrieval
3. Client UI: health/lifecycle honesty for server owners; result row/notification/Console handoff per ADR
4. Audit trail wiring (AutomationAuditEvent end-to-end)
5. Reconciliation with 18937–18939 semantics; per-task model payload
6. Live verification against a real server; docs (schedules.md is a stub — this task should also give it its real content)
<!-- SECTION:PLAN:END -->

## Implementation Notes

**Progress log (task remains In Progress — slices land incrementally):**

- **Foundations (merged):** TASK-18937 (missed-fire accounting: `grace_seconds`/`missed_at`/`missed_count`, no-double-count rule), TASK-18938 (Run-now semantics), TASK-18939 (`timeout_seconds` + `TIMED_OUT` status) all landed on dev ahead of this task.
- **ADR-077 accepted** (`backlog/decisions/077-server-offloaded-scheduled-agent-tasks.md`): single-owner execution (server-scoped rows never dispatch locally), notification pass-back as the phase-1 result channel, phase-1 side-effect-free families only, `timed_out` as the shared vocabulary, control-plane authoring, run-now endpoint. Owner accepted both judgment decisions (no-double-count attribution; agent_task deferred with message-redaction preserved).
- **Server side (merged on tldw_server dev):** TASK-13020 (scheduler feed arming per-occurrence DateTrigger jobs), TASK-13021 (`agent_task_jobs.py` consumer with run-slot dedupe, timeout, notification pass-back; phase-1 unwired families skip with `family_not_wired_for_execution:<family>`), TASK-13022 (`scheduled_task_runs` table + statuses + result_summary), TASK-13110 (`POST /definitions/{id}/run` run-now endpoint with idempotency + lifecycle refusals). `agent_task` execution deliberately unwired (input.message redacted at rest — phase-2 design filed as server issue #2805).
- **Client slice 1 (PR #1986, merged 2026-08-29):** single-owner execution — `is_server_scoped_owner` predicate in `Scheduling/scheduler/queue.py` filters server-scoped rows from both `PriorityQueue.load` paths; `SchedulerLoop.tick`/`run_reminder_now` and `SchedulingService.run_reminder_now` refuse them; workbench Run-now shows a refusal toast using the shared predicate. Local-owner path pinned by tests (no regression). Files: `queue.py`, `loop.py`, `services/scheduling_service.py`, `UI/Screens/scheduling/schedules_workbench.py`, `Docs/User_Guide/schedules.md` callout, `Tests/Scheduling/test_owner_filter.py`. Diagnostic inventory pin regenerated for the two refusal logs.
- **Client slice 2 (PR #2213, merged 2026-08-29):** server definitions surfaced + run-now wired — new **Automations** tab on the Schedules workbench lists server automation definitions (name/family/lifecycle/health; follows `has_more` pages, cap 500; selection reconciled by id across refreshes) and `r` on that tab dispatches through the server control plane. Stack: `tldw_api/scheduled_tasks_automation_schemas.py` + client methods (`GET /scheduled-tasks/definitions`, `POST …/definitions/{id}/run` with `Idempotency-Key` passthrough); runtime-policy resource `scheduler.automations` (list/launch) under `scheduler_workflows`; `ServerNotificationsService.list_scheduled_automations`/`run_scheduled_automation_now`; `SchedulingServerClient` wrappers (run-now deliberately NOT retried — a retried trigger could enqueue a second run). Qodo review addressed: stale server-contract fixture (`Tests/Scheduling/fixtures/server_responses/automation_endpoints.md`) updated to current server dev including the runs/results family; three findings declined with layer-precedent rationale.
- **Client slice 3 (PR #2215):** result pass-back parses end-to-end — the client's closed `NotificationKind` Literal now includes the four `automation_run_{succeeded,failed,timed_out,skipped}` kinds the server consumer writes (previously ONE such notification in the server inbox would have failed the WHOLE feed list parse client-side). All downstream layers (scope services, stream schema `kind: str`, local DB TEXT columns, generic inbox rendering by title/message/severity) were verified permissive; the Literal was the single gate. Pinned by `Tests/Notifications/test_automation_run_notification_kinds.py` (including that truly unknown kinds still fail loudly).

- **Client slice 4 (this PR):** execution-audit trail surfaced (AC#4) — the Automations tab split into definitions | **Run history**, where the right pane loads the selected definition's durable server audit trail (`GET /definitions/{id}/audit`, `run_{status}` events with run ids for correlating results) on highlight and refreshes after a Run-now dispatch; stale-selection races guarded (a newer highlight wins). Stack: `ScheduledTaskAuditEvent`/`ScheduledTaskAuditList` schemas, client method with event-type filter, `ServerNotificationsService.list_scheduled_automation_audit` (same `scheduler.automations.list.server` policy action — audit is a control-plane read), `SchedulingServerClient.list_automation_definition_audit`. Note: `run_worker` in Textual 8.x takes no worker args — the definition id binds in a closure.

- **Client slice 5 (this PR):** AC#7 client visibility — a **Model** column on the Automations definitions table showing each definition's pinned execution target (`automation_execution_target_label`: `provider/model`, either part, or `auto` when the definition pins nothing and the server resolves from its own config/default chain). The payload protocol already carries per-task selection (server `resolve_execution_target` honors `input.provider`/`input.model`/`input.max_tokens` with definition→config→default precedence); the server-side authoring bound (validate the override against configured/BYOK/admin-override providers at preview/create/update) is filed as tldw_server TASK-12126.

- **schedules-handoff program (PR #2302 + this PR):** AC#2's client half is now satisfied for `recurring_question` — the remaining "create/preview/submit" slice landed via the schedules-handoff program's own spec (`backlog/docs/spec-2026-08-31-schedules-handoff-parity.md` §8): PR #2302 shipped the owner-dispatched authoring modal (create/edit, local `AutomationPreviewService` or server `POST /previews`→`POST /definitions` by `owner_id`), and this PR (schedules-handoff PR-5, per-task ownership transfer — ADR-112) shipped the transfer machine that moves an already-authored `recurring_question` definition between owners after the fact (`begin_transfer_to_server`/`begin_transfer_to_local`, spec §6). **AC#2's `agent_task` half remains open**: v1 authoring and transfer are `recurring_question`-only by spec decision (agent_task authoring/local-execution is the named follow-up program, spec §12) — the definition-family transfer refusal gate already always refuses `agent_task` to local, and nothing here changes AC#2's `agent_task` status, which stays pending the server's execution certification.

- **Live verification, 2026-09-02 (AC#8 evidence — schedules-handoff PR-6 task 6, the program's §10 gate):** real TUI in an isolated tmux server against tldw_server `origin/dev` `25fb0eca59`, local single-user profile, both server env gates enabled (`AGENT_TASK_JOBS_WORKER_ENABLED=1`, `SCHEDULED_TASKS_AUTOMATION_SCHEDULER_ENABLED=1` — without them run-now enqueues `agent_task_run` jobs nobody consumes), scratch client profile under `TLDW_CONFIG_PATH`; real user config byte-identical afterwards. Full record with capture-pane evidence: `.superpowers/sdd/plan-2026-09-02-schedules-handoff-pr6/task-6-report.md`.
  - **Verified live:** local `recurring_question` authored → `M` transfer → server `POST /definitions` 201 → local row rebinds to `owner_id server:<id>` with `server_id` set and the pending mutation drained; server-side execution really runs (audit `run_succeeded` with a provider pinned, `run_failed: ChatConfigurationError: LLM provider is required` without one); server results pull down as unread (`{'inserted': 2}`) and render with kind glyph / `(server: <id>)` suffix / relative created / review state, with the answer-evidence-review detail pane; `r` marks read locally, queues `automation_result_review`, and the **server** then reports `review_state: read` + `reviewed_at`; and the Results tab refreshed on a server-side run's notification **without pressing `s`** (Task 4's `_run_phase(..., _pull_results)` seam — no `sync_now` line in the log, SSE connection confirmed ESTABLISHED via `lsof`).
  - **Not verified:** server-side *result generation* (the executor reported `run_succeeded` but wrote no `automation_results` row under `generation_mode: optional`, and the server's preview validator rejects `always`) — the two result rows were therefore seeded server-side, so only the origin of the rows is synthetic; server→local transfer (leg b, blocked by the owner-id defect below); and reminder transfer round-trips including **the flagged past-`run_at` one-time case, which remains an open unknown** — the reminder create form's Save could not be actuated and the UI then froze (0% CPU, no repaint, background DB threads still logging; not isolated, `py-spy` needs root here).
  - **Defects found (fix round warranted before the PR goes up):** (1) HIGH — the live server sends `owner_id: "1"`, not a `server:`-prefixed value, so `is_server_scoped_owner()` misreads every server automation as local: rows render `[This device] …` while the same pane's count line says "2 automations on the server", `r` refuses with the local-health message instead of dispatching server-side, and `m` refuses with "This automation no longer exists." `_load_server_automations` only stamps `owner_id` when it is *absent*, and the captured fixture `automation_definition_list.json` uses `owner_id: "server:42"` — fixture drift is why no test caught it. (2) HIGH — the Results unread badge never renders: `pane.label = …` on a `TabPane` sets an inert attribute (Textual 8.2.8 stores `_title`; `TabPane` has no `label` reactive), and `Tests/UI/test_schedules_results_tab.py:408` passes because it reads back the attribute the code just set. Pre-existing in the Conflicts badge, copied by PR-6. (3) HIGH — mark-solved eligibility keys `definitions_by_id` by *local* definition id while a synced result's `definition_id` is the *server* id, so `o` always refuses with "definition could not be found" for exactly the server-owned rows it targets. (4) MEDIUM — the survey's parked non-UTC sort risk is real: the live server emits `2026-09-02T23:25:45.681750Z` while local rows write `+00:00`, and `list_automation_results` orders on the raw string. Docs updated with three "Known gap" callouts rather than silently restating behaviour that does not hold.

- **Live verification round 2, 2026-09-02 (same day, after the fix wave `4df041b61`):** re-driven against tldw_server `origin/dev` `25fb0eca59` on a **fresh port (8020)** with the credential written as `[tldw_api] auth_token` — both straight from round 1's own lessons (D5 keyring, D6 placeholder), and both paid off: auth succeeded on the first request instead of costing 25 minutes. Real user config byte-identical again.
  - **All three round-1 fixes confirmed live:** `r` on a server automation now dispatches on the server ("dispatched to the server (slot 2026-09-03T00:22:33+00:00)" plus the matching server POST) instead of refusing with the local-health message; the unread badge renders `Results (2)` and drops to `Results (1)` after `r`; a result carrying the SERVER's `definition_id` now reads `Solve: eligible` (round 1: "definition could not be found"); and `[PR-6]` survives in both the table cell and the detail pane.
  - **The three legs round 1 could not reach are now done.** (b) `m` on a server automation → confirmation → dormant local copy (`from_server_pending`) → sync → server archives its copy and the local copy **arms** (`transfer_state: None`) → local run-now dispatches ("ran now — deduped, a run was already in flight"); only the final result-in-inbox step is unverified, because the scratch profile's local retrieval returns `retrieval_status: empty` (environment, not a defect). (c) reminder round-trip: the server shows the task with `link_type: "chatbook_transfer"` and `link_id` = the original local task id, so a transferred reminder stays matchable from the server side. (d) **the spec's flagged past-`run_at` unknown is answered**: the client refuses to *author* one ("That time is in the past — pick a future time"), but a reminder that goes overdue transfers fine — the confirm dialog shows the documented "server behavior … is unverified" warning, and **the server accepts it and parks it `status: "disabled"`, `enabled: false`, `next_run_at: null`, unchanged 60s later** (its future-dated sibling stays `scheduled`/`enabled`). No catch-up fire, no rejection. The client mirrors it as a Disabled row with the missed-while-away marker; `Docs/User_Guide/schedules.md` now states this instead of "unverified".
  - **D7 struck as a product defect.** The "UI freeze" reproduced at boot, and the app's own `SIGUSR2` all-threads dump (`Logging_Config.py:522` — non-destructive, no root needed) showed an **idle, healthy** asyncio loop with the input thread parked in `select`. Two harness causes: injected input only reaches the app when a tmux **client is attached** (a stock Textual app in the same tmux server proved tmux itself was fine), and round 1's "Save does nothing" was the reminder form's own validation error — `Run at` *displays* `2026-08-28 09:00` as a **placeholder**, so `.value` was empty and the form answered "Run At is required for one-time tasks" on a line outside the capture window. Minor real UI trap (a placeholder indistinguishable from a value); no deadlock ever existed.
  - **Two new defects, both sitting behind the round-1 fixes:** (D8, MEDIUM) the Automations tab's `[<server id>]` owner prefix is swallowed by `DataTable`'s `rich.text.Text.from_markup` because `[http…]` matches its lowercase tag regex — server rows show no prefix while local rows keep `[This device]`; the old `server:42` fixture value would have been swallowed identically, so no fixture shape could have caught it, and routing is unaffected. (D9, MEDIUM) `SchedulingService.resolve_definition` funnels every non-policy `ServerClientError` into "this action requires a server connection", including the server's explicitly `retryable: false` 409 `scheduled_task_definition_archived` — a connected session is told to check its network. Both are documented as "Known gap" callouts in the user guide rather than left as false promises.
  - **Incident disclosed:** a broad `pkill -f 'tldw_chatbook.app.*'` during harness debugging also killed two app processes belonging to another session on this machine. Scope process kills to the scratch profile.

**Remaining slices:** `agent_task` authoring + execution certification (the rest of AC#2), the server-side provider bound (tldw_server TASK-12126, completes AC#7), schedules.md full write-up (Automations/Run history/Model sections added in slices 2/4/5; transfer machine section from PR-5 still needed). AC#8 is now evidenced across the whole client-side handoff surface — authoring, both transfer directions, the reminder round-trip with link fields, the past-`run_at` case, the results inbox, read pushback and the notification-triggered pull are all live-verified. Still not evidenced: server-side *result generation* (the executor reports `run_succeeded` but writes no `automation_results` row under `generation_mode: optional` — file against tldw_server at close-out) and a local run producing an inbox row (needs a profile with indexed local content). D8 and D9 want a fix round before the §10 gate closes.

