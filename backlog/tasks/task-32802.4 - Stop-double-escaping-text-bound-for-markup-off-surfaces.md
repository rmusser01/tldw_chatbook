---
id: TASK-32802.4
title: Stop double-escaping text bound for markup-off surfaces
status: To Do
assignee: []
created_date: '2026-09-18 16:49'
labels:
  - core-review
  - review-markup
dependencies: []
parent_task_id: TASK-32802
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Two sites escape text that is then rendered with markup disabled, so the escape itself becomes visible: Console evidence and inspector rows HTML-entity-escape, turning a Library title `R&D Report` into `R&amp;D Report` and escaping already-encoded text twice; and the prompt-queue shelf Rich-escapes into a `markup=False` Static, so `summarize [draft]` is shown to the user as `summarize \[draft]`. The queue escape also breaks the cell budget its own docstring says is unaffected. A sibling module documents this exact bug, found in live UAT, and fixed it there.

## Findings this closes

- **P1 [D4b]** (CHAT-rest-1) Console evidence/inspector rows HTML-entity-escape text destined for a terminal surface, so a Library title "R&D Report" reaches the user as "R&amp;D Report"; the Library surface fixed exactly this bug and Console did not
  - Where: `tldw_chatbook/Chat/console_display_state.py:8` (`from html import escape as html_escape`) and `:93-95 _safe_display_text`. 18 call sites: `:496, 507, 508, 519, 528, 829, 849, 852, 854, 855, 901, 902, 903, 1158, 1159, 1170, 1171`. Already-fixed sibling copy: `tldw_chatbook/Library/library_rag_state.
- **P1 [D1]** (CHAT-rest-2) the prompt-queue shelf renders a literal backslash in front of any `[` the user typed, and the escape eats the cell budget it was measured against
  - Where: `tldw_chatbook/Chat/console_prompt_queue.py:171` (`make_prompt_preview` → `rich.markup.escape`), consumed at `tldw_chatbook/UI/Console_Modules/prompt_queue.py:182` → `:219` → `:350` into `tldw_chatbook/UI/Console_Modules/prompt_queue.py:320` `Static("", id="console-prompt-queue-preview", markup=Fals

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A title containing `&` renders as `&` in the Console evidence and inspector rows
- [ ] #2 A queued prompt containing `[` renders without a backslash
- [ ] #3 The prompt-queue cell budget is measured against the rendered text
<!-- AC:END -->
