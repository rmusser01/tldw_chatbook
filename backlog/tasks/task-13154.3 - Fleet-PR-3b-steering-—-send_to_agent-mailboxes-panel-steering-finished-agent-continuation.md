---
id: TASK-13154.3
title: >-
  Fleet PR 3b: steering — send_to_agent, mailboxes, panel steering,
  finished-agent continuation
status: Done
assignee: []
created_date: '2026-08-17 22:24'
labels:
  - console
  - agents
dependencies: []
parent_task_id: TASK-13154
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Implements spec §6 per the plan at Docs/superpowers/plans/2026-08-17-fleet-pr3b-steering.md (every citation verified at dev a2b621c80). Two paths, one mechanism: the supervisor's send_to_agent tool and a fleet-panel drill-in input both append to a per-child coordinator-level mailbox, drained as source-labeled user-role messages at the loop's single protocol-coherent boundary. Steering a finished child starts a new run seeded from an in-memory retained transcript, linked via resumed_from_run_id (v8 migration, folding the task-15669 schema-drift fix per coordinator ruling). Stop semantics decouple under subagents_outlive_turn; the panel gains Cancel-all.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A live child receives supervisor and user steering as labeled user-role messages at a boundary that never splits native tool-call pairing, without being cancelled or restarted
- [x] #2 The fleet drill-in offers a steering input for live children with an honest queued state, painted-frame-asserted
- [x] #3 send_to_agent to a finished child starts a new contained run seeded from the retained transcript, linked via resumed_from_run_id; after restart it errors clearly instead
- [x] #4 Steering never satisfies a pending approval, pinned by test
- [x] #5 With subagents_outlive_turn on, Stop cancels only the supervisor's turn; Cancel-all exists in the panel; turn-scoped mode is byte-identical
- [x] #6 Cost-ticker audit executed and recorded; docs and spec stamps updated; live steering plus continuation exercised against a real provider
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Shipped as six landings on dev: #1776 (mailbox + protocol-coherent drain),
#1777 (`send_to_agent` for live children), #1793 (panel steering input +
queued state), #1797 (retention + continuation, `resumed_from_run_id`, the
v11 migration folding task-15669), #1808 (Stop decouples under
`subagents_outlive_turn`, "Cancel all agents", + the Qodo round that made
session close take its fleet), and this close-out (Task 6).

**Task 6 delivered:** spec §8's cost-ticker audit executed rather than
assumed — a resumed child's `total_tokens` reaches the fleet rollup at
finish, pinned by `Tests/Agents/test_fleet_continuation.py::
test_a_resumed_childs_spend_reaches_the_fleet_rollup_at_finish` and
mutation-checked; the honest gaps recorded in the same test and FILED as
TASK-18311 rather than patched. User Guide gains the steering story, the
continuation contract, and the new Stop contract with its four kill
switches; spec §7/§8/§10 carry shipped-notes and the config surface was
corrected.

**Live pass** (tmux, isolated scratch profile, real Anthropic model): all
five scenarios exercised — panel steering and `send_to_agent` steering
both delivered with their own labels; `queued (N)` observed; Stop left a
survivor that finished `done` and auto-woke; Cancel-all killed two live
children in one press; a finished child resumed as a NEW run with the
`resumed from <id>` header; a message steered while an approval card
pended stayed queued until the round resolved; closing the session
cancelled its live child. Full transcript in
`Docs/superpowers/plans/2026-08-17-fleet-pr3b-task6-report.md`.

Follow-ups filed, none patched here: TASK-18311 (cost rollup gaps),
TASK-18312 (pruned cancelled handle draws the unknown-id copy),
TASK-18313 (two controller gateway reds on dev), TASK-18414 (the
Anthropic request builder's two model-name gates).
<!-- SECTION:NOTES:END -->
