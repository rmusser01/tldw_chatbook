---
id: TASK-32810.1
title: 'P3 consistency bundle: Chat'
status: Done
assignee: []
created_date: '2026-09-18 16:59'
labels:
  - core-review
  - review-polish
dependencies: []
parent_task_id: TASK-32810
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The core review recorded 35 P3 findings in the Chat package: console controller, store, bridge and the three remainder slices. None is worth its own pull request and none changes behaviour a user sees; together they are the difference between a module that reads as maintained and one that does not. The individual findings are listed in the review report's P3 table, filtered to this area.

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Each of the 35 findings for this area is either fixed or explicitly declined with a reason recorded in the task
- [x] #2 No behaviour visible to a user changes
- [x] #3 The area's targeted tests stay green
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Increment 9 (`fix/core-review-polish-9`). Bundle .1 (Chat) DONE -- all findings
fixed-or-declined. FIXED (gate-free-verified: compile + import + targeted tests;
the many local Chat test failures are pre-existing ADR-126 RecoveryRequired):
- console_trace_service.py (CHAT-bridge #95): deleted 3 dead symbols --
  `_revision_only` (only self-recursion + a test-NAME substring), `_omission_source`
  (the other refs were `_surface_omission_source`), and the `reconstruct_logical_header`
  alias (0 refs).
- UI/Console_Modules/message.py (CHAT-rest-1 #163): deleted the dead verbatim
  `_console_message_role_from_persisted` static method (0 callers; the live call
  targets the Chat/ twin).
- conversation_local_marks_service.py (CHAT-rest-3 #186): get_mark is test-covered
  API, so KEPT the method and stripped its misleading docstring claim (an
  auto-wake `created_at` boundary that was never wired).
- console_chat_store.py (CHAT-store #78) + chat_screen.py (UI-chat #79): removed
  the 7 module-scope-duplicate function-body imports (AST-verified; re-run reports
  zero). chat_screen had a 6th beyond the 5 the report named (ScreenStackError).

DECLINED with reason (P3 hygiene bundle; none are P0/P1/P2):
- REAL DEFECTS deferred (rated P3; in ADR-126-gated files, need gated-path
  verification + careful analysis -- tracked as follow-up): CHAT-bridge #48
  (shutdown fence keys on the wrong id when conversation_id_for_session fails),
  CHAT-controller #131 (four silent `except Exception: pass` with no log on
  data-adjacent paths), UI-chat #72 (_active_lineage_rows swallows a failed
  get_conversation_by_id and exports every branch).
- STRUCTURAL: CHAT-store #42 god module (22k lines) -- decomposition series.
- PERF (D2, to_thread/loop restructure + gated): provider-readiness inline,
  no-sink SQLite on loop, string+= SSE accumulation, ensure_chat_store sync
  recovery, non-send sync sqlite default, raw SELECT outside transaction.
- CONSOLIDATIONS (D4a/D4b, larger cross-module, gated): the 49x hand-rolled
  strict-JSON parser + 7x reject-dup-key hook, 7x UTC-now re-rolls, _mapping_value
  / _metadata_object clones, 3x lazy-delegate wrappers, get_internal_prompt +
  _empty_profile_context_snapshot dups, chat-create confirm hand-roll, 20x
  boilerplate handler pairs, 4x attach/detach picker workers, _safe_text 12th
  re-roll, citation SQL-identifier validator re-roll, persisted_conversation_id
  direct assigns, 5x json.dumps, cross-module private-helper imports.
- #108 bridge/gateway/runtime redundant imports: deferred to a dedicated AST
  sweep across the 3 large gated files (must preserve the ADR-097 boot-budget
  deferrals + fix the false :474-478 comment) -- same mechanical pattern as the
  app.py #2773 pass, but needs the ADR-097 partition handled per-file.
- D3 logging mix (Chat_Functions ~100 stdlib calls): reroute risk (as app.py's).
Inventory unaffected (deletions had no diagnostics); preflight green.
<!-- SECTION:NOTES:END -->
