---
id: TASK-32810.4
title: 'P3 consistency bundle: DB'
status: Done
assignee: []
created_date: '2026-09-18 16:59'
labels:
  - core-review
  - review-polish
dependencies: []
parent_task_id: TASK-32810
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The core review recorded 23 P3 findings in the database modules: migration-step boilerplate, error-type collapsing, a LIKE-escape convention and paired fetchers applied inconsistently. None is worth its own pull request and none changes behaviour a user sees; together they are the difference between a module that reads as maintained and one that does not. The individual findings are listed in the review report's P3 table, filtered to this area.

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Each of the 23 findings for this area is either fixed or explicitly declined with a reason recorded in the task
- [x] #2 No behaviour visible to a user changes
- [x] #3 The area's targeted tests stay green
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Increment 5 (`fix/core-review-polish-5`), stream-12. Fixed the 3 clearest D1
correctness/security findings, each with a regression test:

- EvalsDB.search_tasks (DB-rest P3 [D1]): the LIKE branch bound `%{query}%`
  unescaped, and that branch is chosen *because* the query has punctuation, so
  typing a literal `_`/`%` returned every task. Now escapes `\`/`%`/`_` and adds
  `ESCAPE '\'` (Prompts_DB precedent). Test:
  test_search_tasks_escapes_like_metacharacters.
- RAGIndexingDB.needs_reindexing (DB-rest P3 [D1]): raised TypeError on a naive
  `current_modified` although the write path stamps UTC onto naive input. Now
  normalizes naive->UTC before comparing. Test:
  test_needs_reindexing_accepts_naive_datetime.
- ChaChaNotes _deserialize_row_fields (DB-chacha P3 [D1]): logged the first 100
  chars of a malformed JSON field (user content) on the corruption path. Now logs
  content_fingerprint() (the discipline the rest of the file follows).

REMAINING in this bundle (20 findings), for a later careful increment or declined:
- D4 consolidations (larger refactors touching the BaseDB hierarchy, gated):
  CharactersRAGDB/MediaDatabase not subclassing BaseDB; base_db self-referential
  subclass imports; 27 migration try/execute blocks; LIKE-escape / FTS-query
  4-way clones; ConflictError.__str__ x3; get_schema_version x2.
- Remaining D1s (real but riskier / behaviour- or error-type-touching, need
  per-site verification with gated tests): message-variant methods collapse
  ConflictError into a generic error; PromptsDatabase.soft_delete_keyword's
  ConflictError message/entity; the casefold(Python)-vs-LOWER(SQLite) unicode
  filter mismatch; BaseDB.vacuum/check_integrity connection leak + non-bool
  return; search_flashcards no LIMIT.
- loguru/stdlib logging mix (D3) in ChaChaNotes/Client_Media_DB (reroute risk,
  same reason as app.py's).
Verified: compile-all; Evals search + 13 RAG indexing tests green; 3 new tests
green; inventory regenerated (1 reworded warning); preflight green.
Increment 6 (`fix/core-review-polish-6`) — 3 more D1/D3 fixes, each tested:
- BaseDB.vacuum / check_integrity (DB-media-base P3 [D1]): DECLINED after Qodo
  review. The `with closing(self._get_connection())` fix is unsafe --
  LibraryIngestJobsDB overrides `_get_connection` to return a cached `_conn`, so
  closing it corrupts the cache for later ops. Reverted; the leak-on-exception
  needs subclass-aware connection lifecycle (the BaseDB-hierarchy consolidation).
- CharactersRAGDB.search_flashcards (DB-chacha P3 [D3]): had no LIMIT. Added
  `limit: int = 100` (matching list_flashcards) rather than deleting -- it has
  ~10 test callers (the review's grep missed Tests/, the #2775 lesson). Test:
  test_search_flashcards_respects_limit.
- create/get/select_message_variant (DB-chacha P3 [D1]): `except Exception` re-
  wrapped a ConflictError (a CharactersRAGDBError subclass) into a generic error,
  losing its type/entity. Added `except CharactersRAGDBError: raise` first (the
  file's own pattern). Test: test_create_message_variant_preserves_conflict_error.
Bundle .4 now 6 of 23 findings fixed. Remaining for later increments/declines:
BaseDB-hierarchy consolidations (decline candidates); the casefold(Py)-vs-LOWER
(SQLite) unicode filter D1; Prompts soft_delete_keyword ConflictError message;
DB-media dead helpers; loguru/stdlib mix.
CLOSE-OUT (increment 7): bundle .4 DONE -- all 23 P3 findings fixed-or-declined.
FIXED (6, increments 5-7): EvalsDB.search_tasks LIKE-escape; RAGIndexingDB
needs_reindexing naive-datetime; ChaChaNotes _deserialize_row_fields content-log
leak; search_flashcards LIMIT+validation+ORDER BY; message-variant ConflictError
type-preservation; PromptsDatabase.soft_delete_keyword ConflictError arg order
(was `("PromptKeywordsTable", kw_id)` -> now (message, entity, identifier); renders
a real sentence + correct .entity/.identifier; conflict path is a mid-transaction
version race, untriggerable in a single-threaded unit test).
DECLINED with reason (P3, none blocking):
- BaseDB.vacuum/check_integrity leak (#107): the closing() fix corrupts
  LibraryIngestJobsDB's cached _conn; needs subclass-aware lifecycle (hierarchy refactor).
- casefold(Py)-vs-LOWER(SQLite) unicode filter (#216): a REAL user-facing bug
  (non-ASCII term matches nothing) but the fix needs a Unicode search-semantics
  decision + custom SQLite collation/function or full Python-side compare -- a
  design task, not a hygiene one-liner. Recorded as a follow-up candidate.
- Consolidations (D4b/D4a): CharactersRAGDB/MediaDatabase not subclassing BaseDB;
  base_db self-referential subclass imports; 27 migration try/execute blocks
  (migration-critical, risky); LIKE/FTS 4-way clones (escape already shared in
  Utils/fts5_match_forms, only wrappers repeat); ConflictError.__str__ x3;
  get_schema_version x2 -- all larger refactors touching the gated DB hierarchy.
- DB-media dead helpers + 3 stubs (#94): dead-code, deferred (needs Tests/ sweep).
- loguru/stdlib logging mix (#73): reroute risk (same as app.py's).
- 3 divergent filename sanitisers (#142): overlaps a security concern; belongs to
  a security pass, not P3 hygiene.

NOTE (out of scope for this P3 bundle, spotted while reading DB-rest -- these are
P1/P2, i.e. the higher-severity streams' territory, and may be UNADDRESSED
shipping bugs worth a look): Prompts_DB.search_prompts materializes all matching
ids into one IN(...) list -> O(matches) + hard-fails past 32766 matches (P1 [D2]);
the markdown prompt export collapses distinct names to one .md filename -> silent
data loss with a "success" message (P2 [D1]); the three prompt-export paths write
user prompt bodies to predictable 0644 paths in the shared temp dir (P2 [D1],
CWE-377/378).
<!-- SECTION:NOTES:END -->
