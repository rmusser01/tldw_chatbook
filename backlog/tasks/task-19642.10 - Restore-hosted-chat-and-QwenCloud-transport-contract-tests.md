---
id: TASK-19642.10
title: Restore hosted-chat and QwenCloud transport contract tests
status: Done
assignee: []
created_date: '2026-08-21 23:26'
labels:
  - testing
  - llm
  - http
dependencies: []
references:
  - backlog/docs/task-19520-verification-failure-inventory.md
parent_task_id: TASK-19642
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Reproduce and repair the 25 failures in test_hosted_chat.py and test_qwencloud.py covering retry budgets, Retry-After, malformed bodies, redaction, SSE ownership, and sensitive-request policy.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 All 25 hosted-chat and QwenCloud nodes in the TASK-19520 verification inventory pass in focused runs.
- [x] #2 Retry, redaction, ownership, and no-retry-after-body-byte guarantees remain pinned.
- [x] #3 The tests use deterministic fake transports and make no external network calls.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Reproduce the failures against current dev and classify by root cause (not by symptom).
2. For each failure class, date the offending change (git log -S) and decide code-vs-test from the change's intent.
3. Apply the minimal fix per class; rerun focused; A/B the blast radius against the pristine tree.
4. Record any pre-existing failures found adjacent but out of scope, with evidence.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
PR: #2746 (wave 1, base dev). Implemented on branch `fix/cascade-prep` (worktree `tldw-cascades`, off origin/dev `cebe68148b`), commit `533c8ef84f`. Verified 248/248 green in the two files (was 22 failed / 226 passed on dev); the inventory's 25 nodes from 2026-08-21 were 22 still-red at this HEAD (3 changed/fixed upstream since).

Root cause, two classes (dated, not guessed):

1. **20 failures — admission fail-closed vs the per-test env redirect.** `owned_json_post`/qwencloud build sessions via `create_default_session()` whose default-timeout (`egress.py:930`) and TLS-trust reads go through `get_cli_setting` → `load_cli_config_and_ensure_existence` → the config-participant admission introduced by TASK-32628 (`b5251e9a6e`, 2026-09-16). The session bootstrap binds the config participant to the sandbox config; the autouse `isolate_test_environment` fixture then redirects `TLDW_CONFIG_PATH`/HOME to a per-test tmp dir; `_participant_state` (`raw_participants.py:124-127`) sees the selection changed and raises `RecoveryRequired("raw_source_selection_changed")`. Tests were last touched 2026-08-27, before the admission existed. Fix: added the two files to the existing `keep_bootstrap_profile` filename set in `Tests/conftest.py` — the same remedy TASK-32628 itself applied to three MCP widget files, for the same reason (they fake the transport, not the config getters).
2. **2 failures — stale type expectation.** The two SSE ownership tests asserted `isinstance(stream, OwnedSSEStream)`, but `@_provider_recovery.unqualified` (also TASK-32628) now retains every returned stream behind `recovery_review._OpenAIStream`. Fix: assert the wrapper, unwrap `.iterator`, and run the ownership/close-exactly-once and no-retry-after-body-byte assertions THROUGH the wrapper — the guarantees stay pinned at the contract consumers actually receive.

Blast radius, A/B verified (pristine conftest vs fixed, cp-method, no stash — shared repo): the two files went 226→248 passed; the rest of `Tests/LLM_Calls/` is byte-identical in failure count (119 pre-existing failures in both runs). Out of scope, recorded for the owners: those 119 summarization failures (`test_summarization_diagnostic_privacy` 91, `test_summarization_model_capabilities` 28) are the SAME admission root cause — `raw_source_selection_changed` swallowed into error strings by the summarize handlers' broad excepts (sampled: `Error: OpenAI unexpected error: raw_source_selection_changed`). They predate this change and belong to the summarization collapse tasks (TASK-32853/32854), where the ledger re-key must decide keep-profile vs per-case config fakes per file.

Ruff (repo config): the 27 findings on the touched files are pre-existing conventions (unused-noqa, alias imports, nested-with); the lines this change adds mirror the files' existing style and introduce no new finding class.

ADR check: none required — no production behavior changed; the conftest set is test isolation config and the two test updates track an already-shipped ADR-level change (TASK-32628's recovery boundary).

Lessons: one incident during close-out — a `git checkout -q -- .` appended to a diagnostic compound command reverted the uncommitted fixes in the worktree; recovered from the A/B backup at /tmp plus re-application, re-verified 255/255, and committed immediately. Candidate for backlog/docs/lessons: never append state-changing cleanup to a read-only diagnostic chain, even in a solo worktree.
<!-- SECTION:NOTES:END -->
