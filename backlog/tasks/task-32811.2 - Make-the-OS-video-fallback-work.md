---
id: TASK-32811.2
title: Make the OS video fallback work
status: To Do
assignee: []
created_date: '2026-09-18 16:58'
labels:
  - core-review
  - review-ux
dependencies: []
parent_task_id: TASK-32811
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The open-externally helper is a class-body function with neither `self` nor a static-method decorator, so every production call raises a type error inside a swallowing handler: the fallback is dead and every external save reports that it could not open the file automatically. Found independently from both the definition and the caller side.

## Findings this closes

- **P1 [D1]** (UI-chat) `_open_video_with_os` is a class-body function with neither `self` nor `@staticmethod`: every production call raises `TypeError`, so "open video with the OS player" silently never works
  - Where: `chat_screen.py:19648-19658`; wired at `UI/Console_Modules/wiring.py:808` (`open_video_with_os=lambda path: screen._open_video_with_os(path)`); called at `UI/Console_Modules/video.py:978` (external open after save) and `video.py:1308` (managed-open fallback when playback tools are missing) — both in
- **P1 [D1]** (UIM-console) the Console OS-video fallback is dead: every `_open_video_with_os` call raises `TypeError`, swallowed into a misleading notice
  - Where: callers `UI/Console_Modules/video.py:1308` (`_play_console_video`, the fallback when ffmpeg/ffplay are absent) and `video.py:978` (after an external save); wiring `UI/Console_Modules/wiring.py:808 open_video_with_os=lambda path: screen._open_video_with_os(path)`; definition `UI/Screens/chat_screen.p

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Opening a video externally works from both call sites
- [ ] #2 The swallowing handler logs what it caught
- [ ] #3 A test covers the call rather than stubbing the helper
<!-- AC:END -->
