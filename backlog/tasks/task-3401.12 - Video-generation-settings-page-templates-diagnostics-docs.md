---
id: TASK-3401.12
title: 'Video generation settings page, templates, diagnostics, docs'
status: Done
assignee: []
created_date: '2026-08-08 02:43'
updated_date: '2026-08-08 23:30'
labels:
  - video
  - settings
  - docs
dependencies:
  - TASK-3401.5
  - TASK-3401.10
parent_task_id: TASK-3401
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Parity polish once the feature works: a video settings surface mirroring the image-gen panel (canonical Settings destination only; no legacy parallel), @style video templates, diagnostics for backend and playback-binary availability, and user-guide coverage of the ephemerality semantics so tombstones are understood as designed. ADR: backlog/decisions/044-ephemeral-generated-video-storage-playback-and-streaming.md
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Settings surface mirrors the image-gen panel for [video_generation] backends with per-field guidance
- [x] #2 @style video templates plug into the existing command grammar
- [x] #3 Diagnostics reports backend configuration and ffmpeg/ffplay/yt-dlp availability
- [x] #4 User guide documents generate-video and stream-video and ephemerality and retention settings
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: yes (ADR-044). 1. AC2 templates: Video_Generation/video_templates.py (builtin cinematic/drone/timelapse/anime styles: prompt suffix + duration/fps/ratio defaults, pure apply/resolve/get functions mirroring generation_templates shape); extend parse_generate_video_args with the @style token in the image grammar's shape; chat_screen resolves style -> composed prompt + param defaults. 2. AC1 settings: UI/Screens/settings_video_gen_defaults.py (BACKEND_IDS minimax/comfyui/stable_diffusion_cpp, LABELS, curated FIELD_SCHEMA, configured checks, backend rows, effective_placeholder, load_user_video_generation_table, VideoGenDraftValues + diff_to_sections, playback-tool probe) + Widgets/settings_video_gen_panel.py (compose-only panel mirroring ImageGenSettingsPanel: default-backend select, per-backend enable/status/collapsible field editors with secret-never-echoed and key-source line, generation defaults, template count line, Save/Revert with overlay) + settings_screen.py wiring (VIDEO_GENERATION category contract, detail-pane branch, stage/unstage, select suppression, save/revert, handlers -- proportional to image's block, same idioms). 3. AC3 diagnostics: a Diagnostics section in the panel -- per-backend configured status + minimax key source + ffmpeg/ffplay/yt-dlp presence lines. 4. AC4 docs: Docs/User_Guide/console/video.md (generate-video, stream-video, ephemerality/retention, tombstones, playback, cost gate) + links from console.md and settings.md. Tests per layer; push to PR #1431; close per DoD.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Parity polish for the video programme (ADR-044). AC2 templates: Video_Generation/video_templates.py -- builtin cinematic/drone/timelapse/anime styles (prompt suffix + duration/fps/ratio defaults; user's prompt text always leads) plus [video_generation.styles.<id>] user overlay with malformed-entry skips (per-file directory convention deliberately deferred to v2, one source of truth first); parse_generate_video_args grew the @style token in the image grammar's exact shape (any order with :backend, bare @ stays prompt); the handler resolves style -> composed prompt + param defaults + style-aware cost estimate, unknown style refuses with the available catalog. AC1 settings: UI/Screens/settings_video_gen_defaults.py (3 backends, curated FIELD_SCHEMA incl. a bool kind for the allow_uploads privacy gate, rows/effective_placeholder/unmerged user-table/key-source-after-clear/canonical order/VideoGenDraftValues/diff_to_sections/validate_draft -- same semantics as image: merged baseline for dirty-tracking, secret never echoed, env/keyring secrets never written to TOML, emptied-but-set becomes deletion never sentinel, enabled_backends order-normalized so saves don't churn) + Widgets/settings_video_gen_panel.py (compose-only mirror incl. the bool-field checkbox and per-field advanced-key hints) + settings_screen.py wiring (VIDEO_GENERATION category + domain contract + summary, detail-pane branch with select suppression, stage/unstage, live checkbox/select reads at save, save/revert/clear through SettingsConfigAdapter.save_sections/delete_values + reset_video_generation_config_cache, draft-status sibling entries). AC3 diagnostics: panel section with per-backend configured status + minimax key source + ffmpeg/ffplay/yt-dlp presence lines (Settings' own binary health read, mirroring the image panel's badges idiom). AC4 docs: Docs/User_Guide/console/video.md (generate-video with backends table, ephemerality/retention/tombstones as DESIGNED behavior, preview/player/system-player tiers, stream-video with egress/progressive-v1/2h time-box) linked from console.md. Registration fallout caught by the hub: category summary entry added; expected_categories/mutable_categories/count-literal updates in the hub test (my first edit landed in mutable_categories by anchor luck and had to be completed in expected_categories -- both assertions now genuinely guard the new category). One new lint error introduced (bare Any annotation in a file with no typing import) fixed to plain dict; settings_screen.py holds its 7-error pre-existing baseline. Tests: 33 new (templates incl. user overlay/override/malformed, parser @style matrix, rows/placeholders/diff/validate/tool-probe, mounted-panel smoke via the app harness covering all sections incl. Diagnostics). Verification: new suites green; 79+24 video regressions green; 71 category-coupled hub tests green after the registration fixes; ruff clean on new modules, big screens at baseline. Pushed to PR #1431. ADR-044 linked; no new ADR required.
<!-- SECTION:NOTES:END -->
