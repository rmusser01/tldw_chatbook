---
id: TASK-32804.10
title: Fix the two database scans that run at startup and on search
status: To Do
assignee: []
created_date: '2026-09-18 16:52'
labels:
  - core-review
  - review-loop
dependencies: []
parent_task_id: TASK-32804
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Prompt search materialises every matching id in Python and binds them as one `IN (...)` list, which grows linearly with matches (6.2, 29.3 and 93.3 ms at 2k, 10k and 32k matches for a 20-row page) and hard-fails past 32,766 bound parameters; the same file already has the correct subquery shape in a sibling method. Separately, the agent-run orphan reconcile re-scans all terminal run history with one query per run inside `BEGIN IMMEDIATE` on first construction per process -- 295 ms at 250k step rows -- and one construction site is inside a settings compose path. The class already has a batch hydrate method for exactly this.

## Findings this closes

- **P1 [D2]** (DB-rest) `PromptsDatabase.search_prompts` materialises EVERY matching prompt id in Python and binds them as one `IN (?,?,…)` list: search cost grows linearly with the match count and the call hard-fails with `too many SQL variables` past 32766 matches
  - Where: `tldw_chatbook/DB/Prompts_DB.py:3884-3922` (the two unbounded `fetchall()`s into `matching_prompt_ids`, then `:3919-3921` `conditions.append(f"p.id IN ({id_placeholders})")`), consumed by the count at `:3935` and the page at `:3939`.
- **P1 [D2]** (DB-rest) `AgentRunsDB.reconcile_orphaned_runs` re-scans the ENTIRE terminal run history on the first construction per process, issuing one `agent_run_steps` query per run and JSON-parsing every step payload, inside a `BEGIN IMMEDIATE` write transaction — and one of the construction sites is a Textual `compose()`
  - Where: `tldw_chatbook/DB/AgentRuns_DB.py:2340-2519`. The cost is the second half: `:2494-2497` selects **every** non-running primary/subagent row (`SELECT id, status FROM agent_runs WHERE status != 'running' AND agent_kind IN ('primary','subagent')` — no time bound, no LIMIT), then `:2508` calls the closur

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Prompt search uses a bounded subquery and does not fail on large result sets
- [ ] #2 The orphan reconcile uses the existing batch hydrate rather than one query per run
- [ ] #3 Neither runs on the event loop from a compose path
- [ ] #4 Benchmarks pin both
<!-- AC:END -->
