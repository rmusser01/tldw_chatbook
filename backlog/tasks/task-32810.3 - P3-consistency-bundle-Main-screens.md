---
id: TASK-32810.3
title: 'P3 consistency bundle: Main screens'
status: Done
assignee: []
created_date: '2026-09-18 16:59'
labels:
  - core-review
  - review-polish
dependencies: []
parent_task_id: TASK-32810
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The core review recorded 29 P3 findings in the chat, library, settings and personas screens: duplicate method definitions, redundant function-body imports and boilerplate handler clones. None is worth its own pull request and none changes behaviour a user sees; together they are the difference between a module that reads as maintained and one that does not. The individual findings are listed in the review report's P3 table, filtered to this area.

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Each of the 29 findings for this area is either fixed or explicitly declined with a reason recorded in the task
- [x] #2 No behaviour visible to a user changes
- [x] #3 The area's targeted tests stay green
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Increment 10 (`fix/core-review-polish-10`). Bundle .3 (Main-screens) DONE.
FIXED (gate-free-verified: compile + import + direct registry check; the screen
tests are ADR-126-gated and run in CI):
- screen_registry.py (#266): deleted the dead `_SCREEN_ROUTES["customize"]` row --
  it targeted `customize_screen`, a module that no longer exists
  (ModuleNotFoundError), advertised by registered_screen_route_ids() though only
  the alias ever reached it. Kept `_SCREEN_ALIASES["customize"] = "settings"`;
  verified customize is gone from routes and still resolves via the alias. Aligns
  with test_screen_preimport's documented "customize dead-route regression".
- settings_screen.py (#87 partial): removed the redundant function-body
  `validate_url` import (bound at module top L218).
- personas_screen.py: trimmed the redundant `LocalPersonaProfileUpdate` from a
  function-body import (bound at top), keeping the genuinely-local `PersonaPolicyRule`.
- #70 (library_notes_controller NoteImportExecutor TYPE_CHECKING import): already
  present on dev -- moot.
DECLINED (P3 hygiene; the bundle's own description says "none is worth its own
PR"): the SettingsScreen 28k-line god module (structural); the many D4b
consolidations (draft-staging shape x15, leave/workspace modals, image-gen shims,
exports-dir writers, 9 generation counters, _utf8_prefix x2, _maybe_await x66,
maintenance poll x3, etc. -- larger cross-module, gated); perf-D2 (workspaces
compose N+1, buddy reconciler never stopped); cross-package private-access D3
(14/9/6-site reach-ins, persona controllers into PersonasScreen privates); the
remaining #87 imports (module-attribute style, per-site judgment); and the P3-rated
gated D1s deferred as follow-ups (on_unmount swallows BaseException, provider
api_key raw read [cross-ref ENTRY-config], 26 query_one outside try, swallowed
editor re-sync, destructive-confirm-on-Button-label). Inventory unaffected; preflight green.
<!-- SECTION:NOTES:END -->
