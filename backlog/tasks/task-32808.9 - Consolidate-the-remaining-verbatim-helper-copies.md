---
id: TASK-32808.9
title: Consolidate the remaining verbatim helper copies
status: To Do
assignee: []
created_date: '2026-09-18 16:56'
labels:
  - core-review
  - review-helpers
dependencies: []
parent_task_id: TASK-32808
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
A batch of small verbatim duplicates with an obvious home: a canonical-JSON digest in three byte-identical copies compared across module boundaries with a fourth strictness variant beside them; a thaw helper re-rolled five times, twice in modules that already import the original; an attachment setter byte-identical across the Chat and UI boundary, which the Chat layer reaches backwards to import; a role mapper duplicated across the same boundary; an identity helper byte-identical in three activation modules; a LIKE-escape one-liner in nine copies, one using a different escape character; a select-blank predicate in three copies where a fourth site got it wrong; and a thumbnail-mount block copy-pasted four times.

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Each named helper has one definition in a module both sides may import
- [ ] #2 The Chat layer no longer imports a private helper from a UI module
- [ ] #3 The copies are removed
<!-- AC:END -->

## Additional copy pair in the SAME three files (TASK-32901, tier-2 review S08)
<!-- Added 2026-09-22 by the tier-2 P2 burn-down, which closed this finding to
this task rather than doing a second, competing refactor of the same files. -->

`_effective_max_tokens` + `_invoke_chat` exist three times, in the same three
modules as this task's `_error_text`:

- `Subscriptions/briefing_service.py:570` / `:594`
- `Subscriptions/briefing_cast.py:477` / `:496`
- `Library/library_rag_answer_service.py:481` / `:506`

**Re-verified by AST diff (docstrings stripped, the `BRIEFING_`/`CAST_`/
`ANSWER_` prefixes normalised): all six bodies are IDENTICAL -- zero drift.**
Two of the copies carry a comment acknowledging the duplication. What they
carry is a PROVIDER fact, not a caller fact: the DeepSeek handler's
`max_tokens` is the whole reasoning-inclusive budget, so a reasoning-typed
default returns an empty completion. The TASK-21515 fix and its Qodo #7/#8
follow-up each had to land three times, and a fourth non-streaming single-shot
caller will hit it again. Suggested home: one
`invoke_single_shot_chat(...)` beside `chat_api_call`'s own seam in `Chat/`,
with the three call sites passing their own constants.

Note from S05: `_error_text` itself HAS drifted (Library caps at exactly
`ERROR_CHAR_CAP`; both Subscriptions copies emit `ERROR_CHAR_CAP + 6`), so the
consolidation must pick a behaviour deliberately.
