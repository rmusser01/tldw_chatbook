---
id: TASK-32804.8
title: Stop the splash screen and Speech settings re-reading cached config on open
status: To Do
assignee: []
created_date: '2026-09-18 16:52'
labels:
  - core-review
  - review-loop
dependencies: []
parent_task_id: TASK-32804
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The splash screen builds one nine-key dictionary with nine separate config reads, costing about 59 ms of every app launch before first paint; the same loader is copy-pasted in a settings viewer. Opening Settings then Speech and TTS spends 65 ms re-reading already-cached config on the UI thread, when the settings screen holds a snapshot one line away and both readers already accept a mapping.

## Findings this closes

- **P1 [D2]** (W-top) The splash screen re-reads the config file 9 times to build one 9-key dict, costing ~59 ms of every app launch before first paint
  - Where: `tldw_chatbook/Widgets/splash_screen.py:213-224` (`_load_splash_config`, a dict comprehension calling `get_cli_setting` once per key; called from `__init__:83`), plus a 10th read at `:280` (`_get_predefined_cards` → `splash_screen.custom_image_path`). Near-identical copy at `tldw_chatbook/Widgets/se
- **P1 [D2]** (W-persona-settings-chat) opening Settings ▸ Speech & TTS spends 65 ms re-reading cached config on the UI thread
  - Where: `Widgets/Settings_Widgets/speech_tts_settings_panel.py:878-882` (`SpeechTTSSettingsPanel.__init__`, the `restored is None` branch) → `_read_realtime_settings_draft()` (:291-320) and `_read_pipeline_voice_settings_draft()` (:322-341). The panel is constructed from a `yield` inside the settings screen

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The splash configuration is read as one section
- [ ] #2 The Speech and TTS panel is constructed from the snapshot the settings screen already holds
- [ ] #3 Probes pin the launch and panel-open costs
<!-- AC:END -->
