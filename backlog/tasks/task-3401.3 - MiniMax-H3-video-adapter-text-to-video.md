---
id: TASK-3401.3
title: MiniMax H3 video adapter (text-to-video)
status: To Do
assignee: []
created_date: '2026-08-08 02:38'
labels:
  - video
  - generation
  - minimax
dependencies:
  - TASK-3401.1
  - TASK-3401.2
parent_task_id: TASK-3401
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
First working video backend: MiniMax-H3 official async task API (submit then poll then immediate download from the expiring CDN URL). Establishes the long-running-adapter pattern (progress states; cancellation; generous timeout) every later video adapter follows. Depends on the Phase-0 spike for pinned API behavior. ADR: backlog/decisions/044-ephemeral-generated-video-storage-playback-and-streaming.md
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Adapter submits /v2/video_generation tasks and polls to terminal state with configurable interval and timeout
- [ ] #2 On success video bytes download immediately from the expiring CDN URL through the egress policy
- [ ] #3 Failures surface task status and base_resp detail without leaking the API key
- [ ] #4 Secret resolves via MINIMAX_API_KEY env then config then keyring
- [ ] #5 User cancellation stops polling and calls the MiniMax task cancel endpoint so stopped tasks do not bill to completion
- [ ] #6 Tests mock the HTTP layer covering submit and poll success and failure and timeout and cancellation paths
<!-- AC:END -->
