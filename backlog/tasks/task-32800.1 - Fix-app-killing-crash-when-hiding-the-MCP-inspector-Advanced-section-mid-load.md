---
id: TASK-32800.1
title: Fix app-killing crash when hiding the MCP inspector Advanced section mid-load
status: To Do
assignee: []
created_date: '2026-09-18 16:47'
labels:
  - core-review
  - review-crash
dependencies: []
parent_task_id: TASK-32800
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Clicking 'Advanced...' and then 'Hide advanced' while the section load is still in flight takes the whole app down. `_load_advanced_section` dereferences `#mcp-adv-content` after awaiting `load_section()`, `_hide_advanced` has already removed that subtree, and nothing cancels the worker -- `exclusive=True` only cancels another worker in the same group. `exit_on_error` defaults to True, so the `NoMatches` becomes a process exit. The toggle is re-enabled before the load is scheduled, so the window is the whole round trip.

## Findings this closes

- **P0 [D1]** (UIM-nav-mcp-persona) pressing "Hide advanced" while the MCP inspector's section load is in flight kills the whole app
  - Where: `tldw_chatbook/UI/MCP_Modules/mcp_inspector.py:3418` (`_load_advanced_section`, the two DOM reads after `await self._service.load_section(section)`: `query_one("#mcp-adv-content", Static)` and, on the next line, `self._refresh_advanced_actions()` which reads `#mcp-adv-action-select`/`#mcp-adv-payloa

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Hiding the Advanced section while its load is in flight leaves the app running
- [ ] #2 The in-flight section worker is cancelled, or the post-await lookup tolerates a removed subtree
- [ ] #3 A regression test drives the reveal/hide sequence against a slow load and asserts the app survives
<!-- AC:END -->
