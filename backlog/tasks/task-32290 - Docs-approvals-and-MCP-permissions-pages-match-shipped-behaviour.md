---
id: TASK-32290
title: 'Docs: approvals and MCP permissions pages match shipped behaviour'
status: Done
assignee: []
created_date: '2026-09-10 19:17'
updated_date: '2026-09-11 23:31'
labels:
  - docs
  - approvals
  - mcp
dependencies: []
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
agent-runs-and-tools.md lists four decisions while the card has five, says Always allow is MCP-only while local workspace tools also get it, quotes a path-warning string that differs from the code, and its approval-card SVG shows the pre-TASK-1846 single-line layout. mcp.md is a stub with no explanation of Inherit, Allow, Ask, Off, Space cycling, the kill switch, or that an explicit tool-level Allow bypasses the risk floor. Source: live approval-card / MCP-permissions UX review 2026-09-10 against dev 3315241674 (snapshot .impeccable/critique/2026-09-10T17-31-53Z__hatbook-widgets-chat-widgets-chat-approval-card-py.md).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Both pages describe the five decisions, their scopes and where to undo them.
- [x] #2 The path-warning text matches the code and the approval-card SVG is regenerated from the current layout.
- [x] #3 mcp.md documents the Permissions matrix states, Space cycling, the kill switch and the risk-floor rule for explicit tool-level Allow.
- [x] #4 Both pages carry an updated 'Verified against' stamp.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Read the shipped copy in the lane-B card/Console and lane-C MCP-hub worktrees and quote it verbatim.
2. Rewrite the Approvals section of `console/agent-runs-and-tools.md` around lane A's already-rewritten activity-line paragraph.
3. Start `mcp.md` from lane C's version and add the missing Permissions-mode material.
4. Regenerate the approval-card SVG from the real card; stamp both pages.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Documented the approval card and the MCP Permissions matrix as they actually ship, quoting every locked string from source rather than paraphrasing. `Docs/User_Guide/console/agent-runs-and-tools.md`: the Approvals section now lists five decisions (`_DECISION_OPTIONS`) with the scope line each one paints (`DECISION_SCOPE_COPY`, verbatim), says which rows narrow the set (MCP all five; local workspace tools drop `Always · these args`; built-ins get Once/This session/Deny because **Always** is the only decision that writes to disk; raw shell is Run once / All shell · session / Deny and defaults to Deny), quotes `_PATH_PRECHECK_SUFFIX` exactly ("path outside allowed folders; will fail even if approved"), adds the visible risk/definition reason lines and the `needs decision · ` prefix, the ticking `Auto-denies in M:SS` countdown (`format_approval_deadline`), a new "Reaching a card from the keyboard" subsection (Alt+A, the footer/F1 entries, the ◆-tab route) and an "After you decide" subsection (`denied by you` / `blocked (Off)` / `blocked (kill switch)` plus the **Sent to the model** disclosure). The old "Always allow (MCP tools only)" claim is corrected — it covers MCP *and* local workspace tools. `Docs/User_Guide/mcp.md` (started from lane C's copy so its 32280/32281/32284/32291 sections are preserved) gains "Permissions mode — Allow, Ask, Off": the four states, tool → server → global precedence, the `•` override marker, Space cycling with `_LEGEND_TEXT` quoted verbatim, the gate breadcrumb, the risk floor and the rule that an *explicit* tool-level Allow is never floored (`permission_store.resolve`), and the kill switch's real label/hint; the leftover "checkboxes" description of the Tool gates rows is fixed (they are buttons). `First_Run_Setup.md` now points the Tools row at MCP ▸ Servers ▸ Tool gates instead of "there is no Tools category" and describes the step's copy; `settings.md` stops quoting a raw-shell label that no longer exists. The SVG was regenerated from lane B's card with `scripts/regen_approval_card_svg.py` (new, ~60 lines: mounts `ChatApprovalCard` with `APP_STYLESHEETS`, one MCP row, `save_screenshot`) — the old image was a whole-Console shot showing the pre-TASK-1846 single-line layout and the retired "Approve once" select label. Trade-off: the new image is card-only, so it loses surrounding Console context but is reproducible whenever the card changes. Verification: `Tests/MCP/test_mcp_documentation_contract.py` + `Tests/Docs/test_console_library_controls_docs.py` give 41 failed / 55 passed both before and after, identical failure *name* sets (pre-existing `README.md` and `Docs/Design/MCP.md` contract drift, neither file touched here); `./scripts/preflight.sh` all green. Stamps on all four pages name the lane heads read: `fix/approval-wave-b-card` @ e7409210cc and `fix/approval-wave-c-hub` @ a999fcf6e6.

Final-review fix round (lane D, task-32290 final review, against the whole-branch docs review at 9a1d73a856): closed seven findings. `agent-runs-and-tools.md`: noted that a high-risk (`mutates`/`process`-tagged) MCP tool row drops **Always · these args** (`Agents/mcp_tool_provider.py` `_options_for_tool`/`_HIGH_RISK_OPTIONS`), pointing at `mcp.md#exact-input-allow-rules`, to match the parallel sentence mcp.md already carried; added a fourth `· blocked` refusal-vocabulary bullet covering a local-workspace-tool Deny (which can't name its own authority), approval timeouts, and a round that ended undecided (`Chat/console_agent_bridge.py` `_refusal_statuses()`: `LOCAL_DENY_REFUSAL`, `LOCAL_TIMEOUT_REFUSAL`/`MCP_TIMEOUT_REFUSAL`, `MCP_UNRESOLVED_REFUSAL` all map to the generic `blocked` state); deleted the duplicate second **Always · these args** paragraph (the surviving first paragraph already links `mcp.md#exact-input-allow-rules`); corrected the Agent-Lesson-save and Agent-Lesson-promotion card copy from "Approve once"/"Deny" to the shipped **Once**/**Deny** labels (both cards use `options=("approve_once", "deny")`, which `chat_approval_card.py`'s `_DECISION_OPTIONS` paints as "Once", not "Approve once" — that longer string is reserved for the single-pending-row fast button, which neither paragraph mentions); and anchored the "Audit mode" vocabulary link at `mcp.md#permission-continuity-for-built-in-tools` — mcp.md has no heading literally titled "Audit mode", but that section is where the same refusal vocabulary (Denied by you / Blocked (Off) / Blocked (kill switch) / Denied (timeout) / Denied (no decision)) is actually documented. Also dropped the stray 🚧 glyph from the two `../mcp.md` cross-links (the links themselves are kept). `mcp.md`: removed the stale "This page is a stub" banner — the page now carries dated, code-verified write-ups with their own "Docs pass" stamps, so a stub label was actively misleading — and added the blank line missing before `## Testing a tool (Tools mode)`. Verification: `Tests/MCP/test_mcp_documentation_contract.py` + `Tests/Docs/test_console_library_controls_docs.py` gave the identical 41 failed / 55 passed set, same FAILED test names, before and after (unrelated pre-existing README.md/Docs/Design/MCP.md drift); `./scripts/preflight.sh` all green except the pre-existing "Canvas Mermaid generated assets" check, which fails in this environment on a missing `emoji-data.txt` input unrelated to this change.

Lane D part 2 (Qodo copy sync, 2026-09-11): synced the same two pages plus `First_Run_Setup.md` to the fix rounds Qodo drove on lanes B and C after this task's first two passes landed. Corrected two claims this task itself had made stale: the read-class tool blurb changed from "Asks you each time before running." to "Asks before running unless you approve a longer scope." (`Agents/tool_catalog.py`'s `_GATEABLE_BUILTINS`), so both `First_Run_Setup.md`'s body prose and its own prior stamp's quotation needed the fix; and the Tools-mode master switch / Servers-mode Tool gates rows now flip their label optimistically on press and revert on a rejected save (commit `7812c505ac`), which inverted `mcp.md`'s prior claim that the toggle only updated after a successful save. Also folded in: the local/raw-shell refusal vocabulary split (`denied by you` for a local Deny, `blocked (Off)` for a local or raw-shell Off — `console_agent_bridge.py` `_refusal_statuses()`), which shrank `agent-runs-and-tools.md`'s bare `· blocked` bullet to just "an approval timeout, or a round that ended undecided"; the kind-aware `Waiting for your approval`/`answer`/`confirmation` copy, now documented as covering the "Run:" chip and the activity line but NOT the Inspector's `Live work` row or the pinned authority summary's `Run` fact, which stay approval-count-gated (`console_display_state.py`, `console_send_authority_summary.py`); the ◆-tab route now landing on whichever of approval/question/skill-install/skill-script is actually pending, falling back to an ordinary tab press for a stale marker or a worktree-merge confirm (neither has a card wired on this screen); exact-input allow rules now documenting an inherited rule's `· from <profile>` row, the `≡` marker covering it, and Remove targeting the owning profile; and the Audit-log paragraph distinguishing a Stop-cancelled round (`Denied (no decision)`) from a headless round with no app wired, which writes no audit row at all. The kill-switch refusal string was verified unchanged and is not quoted verbatim on any of the four pages, so no edit was needed there. Verification: `Tests/Docs/test_console_library_controls_docs.py` + `Tests/MCP/test_mcp_documentation_contract.py` + `Tests/Scripts/test_regen_approval_card_svg.py` gave 41 failed / 58 passed, the same 4 failing test functions and pre-existing causes as before (55 of the 58 passes match the prior two-file baseline; the extra 3 are the SVG test file, confirmed 0 failed/3 passed standalone); `./scripts/preflight.sh` exit 0, all green including Canvas Mermaid. Full report: `.superpowers/sdd/2026-09-10-approval-card-fix-wave/copy-sync-report.md`.
<!-- SECTION:NOTES:END -->
