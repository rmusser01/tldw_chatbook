---
id: TASK-32811.4
title: Fix the Library surfaces that show the wrong thing
status: To Do
assignee: []
created_date: '2026-09-18 16:58'
labels:
  - core-review
  - review-ux
dependencies: []
parent_task_id: TASK-32811
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Three Library surfaces display something other than the truth: renaming a speaker replaces the three-line metadata preview pane with the entire transcript (an existing test masks it by faking the preview); the note editor's location row is elided to a different pane's width; the ingest chunking-template picker shows 'None (manual settings)' while the form still submits a saved template; and a location row is overwritten with a factually false claim that the note exists only in the database before the real value arrives.

## Findings this closes

- **P1 [D1]** (W-library) Renaming a speaker in Library ▸ Media replaces the 3-line metadata preview pane with the entire transcript
  - Where: `tldw_chatbook/Widgets/Library/library_media_canvas.py:688-696` (`_refresh_after_speaker_rename`); the pane it writes into is composed at `:1900-1904` from `canvas.preview_lines`, built at `tldw_chatbook/Library/library_media_state.py:1197-1205` and `:1573-1583` as exactly three metadata lines (titl
- **P1 [D1]** (W-library) The note editor's "where does this note live" row is elided to the LIST pane's width, not its own
  - Where: `library_notes_canvas.py:1364 _effective_pane_width` (consumed at `:2760` compose and `:3650 _restate_note_location`); `:1387-1388` — `on_resize` returns before recording `_measured_width` for any mode but `"list"`. The work pane's `pane_width` is `reader_layout.items_width` (`library_notes_controll
- **P1 [D1]** (W-library) The ingest chunking-template picker shows "None (manual settings)" while the form still submits a saved template
  - Where: `library_ingest_canvas.py:1553-1560` (compose clamp) and `:2027-2036` (post-fetch restore).

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The metadata pane keeps its preview after a speaker rename
- [ ] #2 The location row is elided to its own width and never shows a false claim
- [ ] #3 The template picker shows the template the form will submit
- [ ] #4 The test that masks the preview defect is corrected
<!-- AC:END -->
