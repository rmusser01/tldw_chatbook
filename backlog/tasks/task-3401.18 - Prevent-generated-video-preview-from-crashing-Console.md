---
id: TASK-3401.18
title: Prevent generated-video preview from crashing Console
status: Done
assignee: []
created_date: '2026-08-09 16:41'
updated_date: '2026-08-09 21:14'
labels:
  - video
  - bug
  - playback
dependencies: []
parent_task_id: TASK-3401
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Fix the inline generated-video preview so explicitly activating a ready MP4 preview cannot terminate the Textual app. Live UAT observed an unhandled AttributeError immediately after preview activation; the persistent scratch log recorded the app-level exception and shutdown.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Activating a ready generated-video preview never raises an unhandled exception.
- [x] #2 The preview either plays or renders actionable degraded guidance when playback support is unavailable.
- [x] #3 Pause, resume, and end-of-file transitions keep Console responsive.
- [x] #4 A regression test reproduces the UAT activation path and fails if an app-level exception escapes.
- [x] #5 Failure logging includes actionable component context without paths, prompts, or media content.
- [x] #6 Opening the modal full player uses the same valid UI-thread bridge and cannot silently drop decoded frames.
- [x] #7 The existing repo-wide UI-thread bridge guard passes without exempting non-App controller calls.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Move inline source open/probe/decode and modal probe/pipeline startup off the UI thread, then publish state only through the owning Textual app thread bridge.
2. Propagate real frame-source decode failures, contain source/timer cleanup, gate inline and modal callbacks by source/pipeline generation, and keep modal decode/timing/stat state in per-generation run objects so stale workers cannot read or mutate resumed, sought, or unmounted playback.
3. Make PlayerPipeline startup and run teardown transactional: close parent pipe descriptors exactly once on every branch, stop already-started children on failure, and wait after forced kill.
4. Add focused RED/GREEN coverage for real click/mount responsiveness, pause/resume/EOF races, stale queued callbacks, sanitized decode/render/cleanup logs, modal frame/EOF dispatch, real first/second spawn cleanup, and mid-pump failure containment.
5. Mutation-check each load-bearing bridge/ownership/cleanup guard, then run only affected playback test and static-analysis scopes.
6. Resolve the required repo-wide bridge guard's pre-existing Console agent controller failure by using the canonical owning-App bridge directly, then verify only that guard and the affected run-log rail path.

ADR required: no

ADR path: backlog/decisions/044-ephemeral-generated-video-storage-playback-and-streaming.md

Reason: ADR-044 already owns inline preview, modal playback, optional dependencies, and ephemeral media lifecycle; this task corrects invalid Textual API usage and failure containment without changing those boundaries.

Design: Docs/superpowers/specs/2026-08-09-task-3401-18-video-preview-crash-design.md
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implemented worker-owned, generation-safe generated-video playback. AvFrameSource now propagates decoder and native-close failures after detaching state. PlayerPipeline uses per-generation PlayerRun state with transactional child-process, descriptor, and stdout ownership. Inline and modal playback keep blocking open, decode, seek, pump, and teardown work off the Textual UI thread; callbacks require matching generation and source or pipeline/run identity; stale callbacks and refused bridges cannot mutate or stop replacements; user guidance is generic and logs contain only component, phase, and error type. Modal startup, publication, seek replacement, and cleanup are race-covered.

Focused verification after rebasing onto latest dev: 107 tests passed with two environment dependency warnings. The command covered only test_frame_source.py, test_player_pipeline.py, test_player_integration.py, test_console_video_preview.py, test_player_screen.py, the repo bridge guard, and the one affected Console run-log rail regression. Ruff check passed on the five touched production files and five modified playback tests; Ruff format check reported all ten formatted. py_compile passed for the five production files using TemporaryDirectory outputs. git diff --check, duplicate Backlog ID scans, privacy/resource self-review, and focused post-rebase tests passed.

Mutation checks removed or weakened the load-bearing decode propagation, exact-once stdout cleanup, final process reap, generation/source/pipeline/run identity gates, App bridge, timer ownership, activation rollback, replacement-pump ordering, and refused-bridge race guards; each dedicated regression failed before restoration. Independent task-level spec and quality reviews reported no remaining findings.

ADR required: no. ADR-044 already owns ephemeral generated-video playback and optional dependency boundaries; this task corrected threading, lifecycle, and failure containment within that decision. Live H3 generation was not repeated and no media was retained.

Plan deviation: the required repo-wide call_from_thread guard exposed a safe but noncanonical ConsoleAgentController wrapper from earlier decomposition work. The wrapper was removed in favor of the canonical owning-App bridge and verified only by the guard and affected run-log rail regression. Ruff formatting was applied only to touched playback files after the formatter check identified drift.

Final review follow-up: the Console agent module header still described the removed controller bridge alias. Commit 52750c1a3 updated it to the canonical owning-App bridge; the exact guard and run-log rail tests passed (3 passed), Ruff and format checks passed, and the spec reviewer then approved the whole task.
<!-- SECTION:NOTES:END -->
