---
id: TASK-3401.16
title: Project video-generation settings through load_settings
status: Done
assignee: []
created_date: '2026-08-09 15:04'
updated_date: '2026-08-09 16:42'
labels:
  - video
  - settings
  - bug
dependencies: []
parent_task_id: TASK-3401
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Ensure persisted [video_generation] configuration reaches the runtime loader so enabled backends and defaults selected in Settings are honored by Console generation.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A persisted [video_generation] table is present in load_settings() output without dropping global or backend-specific values.
- [x] #2 The video-generation config loader honors persisted default_backend, enabled_backends, and ComfyUI connection/workflow settings after cache reset.
- [x] #3 The real Console generation path no longer reports a configured and enabled ComfyUI backend as disabled.
- [x] #4 Focused regression tests fail when the projection is absent and pass when both initial load and reload paths preserve the table.
- [x] #5 Malformed top-level or nested video-generation TOML values fall back safely instead of crashing runtime configuration.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Add a real scratch-TOML failing regression proving load_settings and the video runtime preserve global and nested video settings across reload.
2. Project the already parsed video_generation table through load_settings, matching the image_generation pass-through.
3. Normalize malformed top-level and backend tables at the video loader boundary so newly reachable invalid values retain defensive fallback behavior.
4. Run the touched config/video-loader/registry tests plus targeted static checks.
5. Self-review, document focused evidence, and commit.

ADR required: no new ADR

ADR path: backlog/decisions/044-ephemeral-generated-video-storage-playback-and-streaming.md

Reason: this restores the existing persisted configuration flow without changing its ownership or service contract.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
- Projected the parsed `video_generation` table through `load_settings()` and normalized malformed top-level/backend mappings at the video loader boundary (`00eaf61ab`, `1c41e53ba`).
- Strengthened scratch-TOML regression coverage for initial cached load, forced reload, exact cache restoration, registry refresh, and malformed backend fallbacks (`dbd967ed2`, `d879c8ae8`).
- TDD evidence included the initial missing-projection failure and malformed-input failures, followed by the controller's combined touched-file gate with 52 passed and 314 deselected.
- Live TASK-3401.14 evidence confirmed that a Settings-saved ComfyUI workflow was picked up by the same running process and both packaged H3 variants reached successful Console generation rather than the former disabled-backend rejection.
- Targeted Ruff, syntax compilation, diff hygiene, workflow-asset immutability, and independent spec/quality review passed. No new ADR was required; ADR-044 remains the governing boundary.
<!-- SECTION:NOTES:END -->
