---
id: TASK-32806.4
title: Bound the always-on calculator tool
status: In Progress
assignee: []
created_date: '2026-09-18 16:54'
updated_date: '2026-09-19 06:35'
labels:
  - core-review
  - review-security
dependencies: []
parent_task_id: TASK-32806
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The always-on built-in calculator evaluates model-supplied `**` and string multiplication with no bound: one call allocates a 20 MB string in 2 ms, and a prompt-injected exponentiation computes a multi-million-bit integer before anything can reject it, pinning a core for the process lifetime because the timeout abandons the thread rather than stopping it. This is reachable by prompt injection, and the tool inherits allow.

## Findings this closes

- **P1 [D1]** (TOOLS-MCP) `CalculatorTool` (always-on built-in) evaluates model-supplied `**` and `str * int` with no bound: one call allocates a 200 MB string in 20 ms and bignum powers scale superlinearly (7**4e6 = 1.26 s, 9x the cost of 7**1e6)
  - Where: `tldw_chatbook/Tools/tool_executor.py:181-220` (`allowed_operators` admits `ast.Pow`/`ast.Mult`; `ast.Constant` admits `str`, so `'ab' * 10**8` and `7**(4*10**6)` both evaluate)

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Exponent and string-repetition operands are bounded before evaluation
- [ ] #2 An oversized expression is refused with a message rather than evaluated and timed out
- [ ] #3 A test covers both the string and exponent cases
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implemented and verified; the code ships in PR #2723 (branch fix/core-review-security), split out per work stream like #2717. Closes when #2723 merges -- the task file lives in #2710 with the rest of the review deliverables and cannot be updated from the security branch until that lands.
<!-- SECTION:NOTES:END -->
