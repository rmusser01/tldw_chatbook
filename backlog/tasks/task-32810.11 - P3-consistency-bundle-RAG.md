---
id: TASK-32810.11
title: 'P3 consistency bundle: RAG'
status: Done
assignee: []
created_date: '2026-09-18 17:00'
labels:
  - core-review
  - review-polish
dependencies: []
parent_task_id: TASK-32810
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The core review recorded 8 P3 findings in `RAG_Search/`: circular-import fallout, an unbounded reranker cache and a single re-rolled model-path row. None is worth its own pull request and none changes behaviour a user sees; together they are the difference between a module that reads as maintained and one that does not. The individual findings are listed in the review report's P3 table, filtered to this area.

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Each of the 8 findings for this area is either fixed or explicitly declined with a reason recorded in the task
- [x] #2 No behaviour visible to a user changes
- [x] #3 The area's targeted tests stay green
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Increment 8 (`fix/core-review-polish-8`). Bundle .11 DONE -- all 8 findings
fixed-or-declined.
FIXED (4, all gate-free-verified: compile + import; the 68 RAG failures locally
are pre-existing ADR-126-gated RecoveryRequired, identical on clean dev):
- #268 parallel_processor.py: folded `log_gauge` into the unguarded metrics_logger
  import (it is always present at metrics_logger:185; the try/except installed a
  silent no-op stub that could only mask a real breakage).
- #277 config_profiles.quick_profile: dead (0 refs incl. Tests/); deleted.
- #301 simple_cache._make_key: removed the per-key `try: import xxhash` (xxhash is
  not a dependency and not installed, so it failed every call and fell back to
  md5 anyway -- ~15x the md5 cost). Now `hashlib.md5(..., usedforsecurity=False)`
  directly; keys are byte-identical to the prior always-fallback behaviour. Fixed
  the docstring line that claimed xxhash was used.
- #319 reranker.py + rag_service.py: the `# Exponential backoff` comments sat over
  LINEAR sleeps (`1 * retries`, `0.1 * retry_count`). Fixed the comments to say
  "Linear backoff" (behaviour unchanged per AC#2; circuit_breaker.py's real
  exponential-backoff comment left alone).
DECLINED with reason:
- #259 A/B-testing subsystem has no production entry point (dead): a whole-
  subsystem removal -- larger dead-code, deferred to a dead-code sweep with a
  full Tests/ trace.
- #286 EmbeddingsServiceWrapper.create_embeddings cache-hit can never fire: a real
  bug, but the fix is a cache-key-space redesign in an embeddings-gated path
  (needs the embeddings env + a keying decision) -- deferred.
- #310 process-RSS-in-MB re-rolled 5x / #328 "strip to non-empty or None" re-rolled
  5x: consolidation (D4b) across 4 packages -- shared-helper extraction, deferred.
Inventory regenerated (1 removed diagnostic = quick_profile's warning); preflight
green.
<!-- SECTION:NOTES:END -->
