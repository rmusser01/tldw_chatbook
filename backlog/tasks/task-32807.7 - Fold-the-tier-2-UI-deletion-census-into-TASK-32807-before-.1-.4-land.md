---
id: TASK-32807.7
title: Fold the tier-2 UI deletion census into TASK-32807 before .1/.4 land
status: To Do
assignee: []
created_date: '2026-09-22 19:51'
updated_date: '2026-09-22 19:52'
labels:
  - core-review
  - review-dead
dependencies: []
parent_task_id: TASK-32807
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The tier-2 code review (2026-09-21, slices S19/S20/S22-S23) found eleven more modules with zero production importers that no TASK-32807 sub-task names. Four of them are transitively dead BECAUSE of .1/.4: deleting their only importer orphans them rather than removing them. Recording them here so the census is complete before those deletions land, and so the one module with a standing keep-ruling is not deleted by mistake.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Every module below is recorded with its line count, its importer count, and whether an existing sub-task's deletion orphans it
- [ ] #2 Modules orphaned by TASK-32807.1 or .4 are deleted in the same commit as their importer, not left behind
- [ ] #3 media_screen.py / MediaWindow_v2.py are NOT deleted on this task; the standing ruling in screen_registry.py is either upheld or explicitly overturned first
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Census only -- no code deleted by this task. Verified at origin/dev d0face3ebe (tier-2 slices S19, S20, S22+S23).

**UI-root windows, 0 production importers, unreachable (S20's deletion table):**
| module | lines | pinning tests |
|---|---:|---|
| `UI/Stats_Window.py` | 42 | none |
| `UI/MediaWindowV88.py` | 16 | 1 test, 18 lines, asserts only `MediaWindowV88 is MediaWindow` |
| `UI/Chatbooks_Window.py` | 483 | none (one CSS census row) |
| `UI/CodeRepoCopyPasteWindow.py` | 1220 | a 399-line test-only lifeline |
| `UI/SiteConfigSettings.py` | 599 | a 225-line test-only lifeline; also `css/screen_css_scoped.tcss:11-70` + `css/screen_css_self.tcss:11-13` |

**Orphaned BY an existing sub-task -- must go in the same commit as their importer (S22+S23):**
| module | lines | orphaned by |
|---|---:|---|
| `UI/Widgets/config_search_widget.py` | 228 | .4 (only consumer is `Tools_Settings_Window.py`); not named in .4's ACs |
| `Widgets/Coding_Widgets/repo_tree_widgets.py` | 726 | its only importer `UI/CodeRepoCopyPasteWindow.py` is itself unreachable (row above) |
| `Widgets/Note_Widgets/note_creation_modal.py` | 265 | its only importer `Widgets/document_generation_modal.py` is on .1's own **Deferred** list |

**Unreferenced, unowned (S22+S23):**
- `UI/CCP_Modules/ccp_message_manager.py` (317) -- `grep -rn "CCPMessageManager("` -> one hit, `Tests/UI/test_ccp_handlers.py:415`. `personas_screen.py:344-346` imports the other CCP handlers, never this one. It carries a live-looking `@work(thread=True)` DB read with no `group=`, no `exclusive=True` and no generation token, so two rapid conversation selections would race and the older loader could win -- inert only because nothing constructs the class, which is exactly the shape a future wiring commit revives. Delete the class, or wire it AND fix the worker; do not leave it as-is.
- `UI/Workbench/route_inventory.py` (118) -- 0 production importers; reached only through a `_LAZY_EXPORTS` string table nothing pulls. `screen_registry.py:198,263` name it in COMMENTS only.

**DO NOT DELETE on this task -- standing ruling:**
- `UI/MediaWindow_v2.py` (2562) + `UI/Screens/media_screen.py` (163). S20 filed these as "delete (L)" on the grounds that `resolve_screen_route("media")` returns `library_screen` (confirmed: the alias is applied before `_SCREEN_ROUTES`, so `_SCREEN_ROUTES["media"]` can never be selected). But `screen_registry.py:278` carries an explicit repo ruling -- "``MediaScreen`` itself is not deleted -- its save_state/restore_state contracts stay directly exercised by their own unit tests, mirroring the 'skills' precedent" -- and **S19's own deletion table applies that same precedent and lands on "keep"** for both `media_screen.py` and `skills_screen.py`. The two slices contradict each other. Blast radius is 2,725 production lines and 17 test files including a 1,510-line parity test. Overturn the ruling deliberately, with the "skills" precedent, or leave it.

**Already actioned elsewhere:** `UI/Screens/schedules_screen.py` (516) WAS deleted under TASK-32901 (tier-2 S19 P2 [delete]) -- 0 importers, unrouted, and its docstring's stated reason for existing ("existing tests still exercise it directly") was false: all three test hits were string literals in inventories.
<!-- SECTION:NOTES:END -->
