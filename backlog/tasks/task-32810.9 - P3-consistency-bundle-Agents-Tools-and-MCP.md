---
id: TASK-32810.9
title: 'P3 consistency bundle: Agents, Tools and MCP'
status: Done
assignee: []
created_date: '2026-09-18 17:00'
labels:
  - core-review
  - review-polish
dependencies: []
parent_task_id: TASK-32810
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The core review recorded 13 P3 findings in `Agents/`, `Tools/` and `MCP/`: identity and spill helpers, git runners, truncation micro-helpers and swallowed teardown failures. None is worth its own pull request and none changes behaviour a user sees; together they are the difference between a module that reads as maintained and one that does not. The individual findings are listed in the review report's P3 table, filtered to this area.

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Each of the 13 findings for this area is either fixed or explicitly declined with a reason recorded in the task
- [x] #2 No behaviour visible to a user changes
- [x] #3 The area's targeted tests stay green
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Increment 12 (`fix/core-review-polish-12`). Bundle .9 (Agents, Tools, MCP) DONE.
FIXED:
- #125: run_webhooks.py guarded an INTERNAL `metrics_logger` import with
  try/except + a no-op fallback; metrics_logger is non-optional (psutil is a core
  dep; 66 other modules import it unguarded), so the guard could only mask a real
  regression as "metrics silently off". Now a plain import.
- #134 (D1): `_retire_agent_worktree` teardown failures were swallowed with a bare
  `except Exception: pass` on both the run_child-finally and thread-start-failure
  paths, leaking the run's tool-routing root for the process lifetime with no log.
  Both now `logger.warning(...)`, matching the adjacent _set_terminal_status wrap.
DECLINED: #71 (_write_spill vs atomic_write_text -- helpers-stream consolidation);
#80/#89/#98/#107/#116 (_now_iso/_identity/run_git/cap-marker/get_internal_prompt
duplications -- cross-module consolidations); #143 (except BaseException: pass x6
around webhook-delivery diagnostics -- deliberate "diagnostics must never fail the
send", documented); TOOLS-MCP #122/#128/#133 (note-preview truncation, _identity,
_format_size -- consolidations, some now have shared homes but the swaps are gated);
#140 (vague "assorted small gaps, none reproduced").
<!-- SECTION:NOTES:END -->
