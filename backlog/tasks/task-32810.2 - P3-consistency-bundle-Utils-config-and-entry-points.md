---
id: TASK-32810.2
title: 'P3 consistency bundle: Utils, config and entry points'
status: In Progress
assignee: []
created_date: '2026-09-18 16:59'
labels:
  - core-review
  - review-polish
dependencies: []
parent_task_id: TASK-32810
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The core review recorded 29 P3 findings in `Utils/`, `config.py`, `app.py` and `Constants.py`: dead symbols, a logger re-export, comment rot and redundant imports. None is worth its own pull request and none changes behaviour a user sees; together they are the difference between a module that reads as maintained and one that does not. The individual findings are listed in the review report's P3 table, filtered to this area.

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Each of the 29 findings for this area is either fixed or explicitly declined with a reason recorded in the task
- [ ] #2 No behaviour visible to a user changes
- [ ] #3 The area's targeted tests stay green
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
PR #2772 (`fix/core-review-polish`), incremental. Many of this bundle's 29
findings were already satisfied by the streams merged ahead of stream 12:
Utils/text.py 5 dead fns, paths.py project-* + always-failing import, six dead
shared-helper modules, secure_temp_files cleanup wrapper (all #2741 dead);
emergency_stop atomic write, token-estimator adoption, format_size_bytes public
(#2745 helpers). Done here so far:

- Constants.py dead `css_content`: removed the ~1,589-line string (production
  loads the css/ bundle; nothing read Constants.css_content). 1966 -> 377 lines.
- Utils/Utils.py 25-dead-symbols finding: removed the 13 top-level functions that
  are def-only with zero importers/callers/tests (clean_youtube_url,
  convert_to_seconds, ensure_directory_exists, format_api_name, format_file_path,
  generate_unique_id, generate_unique_identifier, get_api_name, safe_float,
  safe_int, sanitize_user_input, save_segments_to_json, save_temp_file). The other
  ~candidates (extract_media_id_from_result_string, verify_checksum,
  normalize_title) have internal callers and need cluster tracing — left for a
  follow-up in this bundle.
- Utils/Utils.py logging re-export: the 3 external modules that imported stdlib
  logging via Utils.Utils now import it directly.

REMAINING in this bundle (concrete): ENTRY-app redundant function-body imports
(20 redundant re-imports), the mutable-class-attrs cleanup on TldwCli, Constants
six small consistency defects, the loguru/stdlib logging split in Utils.py/app.py
(larger), and the god-module decompositions (config.py, not a P3 fix).
Follow-up increment (PR #NEXT, `fix/core-review-polish-2`): ENTRY-app redundant
function-body imports -- removed 24 that duplicated an unconditional module-level
import (AST-verified: only names with a top-level non-TYPE_CHECKING binding;
re-run now reports zero). One was a latent bug (`from textual.worker import work`
raises ImportError; the module-level `from textual import work` is correct).
config.py/Constants.py checked -- zero redundant there. Behaviour-preserving:
`import tldw_chatbook.app` succeeds; py_compile clean.
Increment 3 (`fix/core-review-polish-3`) — ENTRY-app dead-code cluster
(P3 [D3], ~153 lines). Removed, each re-verified 0 external refs against current
dev: `handle_ingest_ui_style_changed` (always-QueryError handler; kept the
`IngestUiStyleChanged` message class, which lives in tools_settings_messages.py
and is still used by the deprecated window), `hide_inactive_windows` (no-op)
+ its one call site, `TabDropdown` (its only Tests hit is a docstring),
`ALL_MAIN_WINDOW_IDS`, `_display_buffered_logs`, `_log_view_dimensions`,
write-only attrs `_default_rag_expansion_provider` / `note_import_success_handler`
/ `note_import_failure_handler` / `_initialized_tabs` / `media_types_for_ui` /
`_prompt_search_timer`, and the `API_FUNCTION_MAP` / `ALL_API_MODELS` /
`API_IMPORTS_SUCCESSFUL` cluster incl. the 19 dead `chat_with_*` imports that fed
only it (AVAILABLE_PROVIDERS preserved by inlining
`list({**API_MODELS_BY_PROVIDER, **LOCAL_PROVIDERS}.keys())`) and the paired
`Tests/fixtures/event_handler_mocks.py` line that set `API_IMPORTS_SUCCESSFUL`.
Verified: AST span removal + assertions, py_compile, `import tldw_chatbook.app`
succeeds with AVAILABLE_PROVIDERS n=29, test_call_from_thread_guard green.

DECLINED (recorded, per AC#1 fixed-or-declined; all P3-cosmetic with real risk):
- Mutable-class-attr annotation-only conversion of `_media_types_for_ui` /
  `parsed_notes_for_preview`: test-entangled — >=8 tests read/set
  `_media_types_for_ui` (incl. `assert app._media_types_for_ui[0]=="All Media"`
  and `object.__new__(TldwCli)` paths); annotation-only risks AttributeError.
  (The genuinely-unused non-underscore `media_types_for_ui` WAS removed above.)
- loguru/stdlib `logging` swap (38 sites): NOT mechanical — the app attaches
  RichLogHandler/RotatingFileHandler to the STDLIB root logger, and several sites
  are __main__/CLI-entry before loguru is wired; a blind swap risks rerouting
  messages away from the in-app log panel and reordering boot logging.
- `_save_shutdown_caches_with_timeout` no-op removal: test-entangled via
  `_run_blocking_quit_persistence` (called by test_config_encryption_effective_path,
  overridden by test_app_quit_guard).
- god-module decomposition (P2 [D3]): L + ADR + per-subsystem PR series
  (library-decomposition-recipe) — out of scope for a P3 hygiene increment.
- perf: folder-import UI-thread freeze (P1 [D2]) and sync-SQLite-on-loop (P3 [D2]):
  need asyncio.to_thread / batch-persist restructure + gated verification; a perf
  pass, not P3 hygiene.
- consolidations: 4-copy TTS message-widget query -> helper (P3 [D3]); 29
  from_config try/except copies -> helper (P3 [D4b]); Backup_Recovery.activation
  hoist (P3 [D3], boot-census re-pin) — touch gated TTS/wiring/census; deferred.
<!-- SECTION:NOTES:END -->
