---
id: TASK-3401.2
title: 'Video_Generation package: contracts, registry, config, validation'
status: Done
assignee: []
created_date: '2026-08-08 02:38'
updated_date: '2026-08-08 03:40'
labels:
  - video
  - generation
dependencies: []
parent_task_id: TASK-3401
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Foundation for all video backends: replicate the proven Image_Generation architecture (frozen request/result contracts; adapter Protocol; lazy registry; nested-TOML config with env/config/keyring secret precedence; single validation choke point) as a parallel Video_Generation package so image contracts stay honest and video fields (duration fps ratio frames reference assets) are first-class. ADR: backlog/decisions/044-ephemeral-generated-video-storage-playback-and-streaming.md
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 VideoGenRequest/VideoGenResult contracts and adapter Protocol exist mirroring Image_Generation with video fields (duration fps ratio frames reference assets)
- [x] #2 Lazy registry with DEFAULT_ADAPTERS map and enabled-backend resolution mirrors the image registry
- [x] #3 [video_generation] config section parses nested TOML with env then config then keyring secret precedence and unknown-key warnings
- [x] #4 Request validation enforces duration/fps/dimension bounds and per-backend extra_params allowlists at one choke point before adapter dispatch
- [x] #5 Unit tests cover config precedence and registry resolution and validation rejections
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: yes (existing ADR-044 covers this task; linked in description). Steps: 1. Create Video_Generation package skeleton mirroring Image_Generation: exceptions, contracts (adapters/base.py: VideoGenRequest/VideoGenResult/ResolvedReferenceAsset/adapter Protocol with duration/fps/ratio/frames/reference assets), lazy adapter registry with the three planned backend specs (minimax, comfyui, stable_diffusion_cpp). 2. Config loader: [video_generation] globals (bounds, retention, max_store_mb, confirm_cost_estimate, download cap) plus per-backend subsections with env then config then keyring secret precedence and unknown-key warnings (keyring namespace tldw_chatbook_videogen). 3. request_validation.py: duration/fps/dimension/pixel bounds, ratio shape, steps/cfg checks, per-backend extra_params allowlist, reference-asset kind/mime/size/count checks with module-level caps. 4. worker.py: build_request + run_generation single validation choke point before adapter dispatch. 5. Tests under Tests/Video_Generation mirroring Tests/Image_Generation: contracts, config loader precedence and warnings, registry resolution, validation rejections, worker choke point. 6. Run pytest for the new suite and lint; close out per DoD.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implemented the Video_Generation package scaffolding mirroring Image_Generation (ADR-044). Approach: parallel package so image contracts stay untouched; video fields (duration/fps/ratio/reference assets) are first-class. Added: contracts (adapters/base.py: VideoGenRequest/VideoGenResult/ResolvedReferenceAsset + adapter Protocol); lazy VideoAdapterRegistry with the three planned backend specs (minimax, comfyui, stable_diffusion_cpp) that resolve but intentionally do not import until backend tasks .3/.6/.7 land; [video_generation] config loader (globals: bounds, retention/max_store_mb for VideoStore .4, confirm_cost_estimate, download cap; per-backend subsections) with env then config then keyring secret precedence (namespace tldw_chatbook_videogen), key_sources metadata, and task-621-style unknown-key warnings; request_validation (duration/fps/dimension/pixel bounds, ratio shape, steps/cfg, per-backend extra_params allowlists, reference-asset kind/mime/size/count checks with MiniMax-H3 documented caps); worker build_request + run_generation single choke point. Tests: 35 new under Tests/Video_Generation (contracts, config loader, registry, validation, worker). Trade-offs/decisions: registry DEFAULT_ADAPTERS pre-populated with not-yet-existing class paths by design (lazy import fails cleanly via get_adapter None); the lazy-import test uses a deliberately nonexistent ghost module so it never time-bombs when .3 lands (pattern to keep for .6/.7). Verification: 35/35 new tests pass; Tests/Image_Generation 251 passed 9 skipped (no regressions); ruff check clean (ruff format not applied: repo is not black-formatted, image package fails the same check). Scope: additive-only; no existing files modified except AGENTS.md architecture line. ADR-044 linked; no new ADR required.
<!-- SECTION:NOTES:END -->
