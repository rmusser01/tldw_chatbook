---
id: TASK-13154.1
title: 'Fleet PR1: agent definitions (model, DB, spawn param, Settings editor)'
status: Done
assignee: []
created_date: '2026-08-09 13:57'
updated_date: '2026-08-09 16:49'
labels: []
dependencies: []
parent_task_id: TASK-13154
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Phase 1 of the fleet spec (§4): AgentDefinition + agent_definitions table + spawn 'agent' param + Settings ▸ Agents panel. Plan: Docs/superpowers/plans/2026-08-08-supervisor-fleet-pr1-agent-definitions.md
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Named definition resolves at spawn (prompt appended, tools intersected, model override)
- [x] #2 No-agent spawn is byte-identical to today
- [x] #3 Settings ▸ Agents CRUD works against the real DB
- [x] #4 Migration is idempotent on existing DB files
- [x] #5 User Guide updated (settings.md + console/agent-runs-and-tools.md)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Approach: seven tasks shipped the fleet spec's PR-1 phase end to end -- AgentDefinition model + validation + fingerprint (Task 1), agent_definitions table + CRUD (Task 2, schema v5), agent_runs audit columns (Task 3), build_spawn_schema with named-agent roster (Task 4), spawn_subagent resolution/composition/intersection/model-override (Task 5), Settings > Agents CRUD editor (Task 6), and this task (docs + full battery + live verification + backlog close-out).

Files: tldw_chatbook/Agents/agent_models.py (AgentDefinition, validation, fingerprint, RUNTIME_TOOL_NAMES), tldw_chatbook/DB/AgentRuns_DB.py (agent_definitions table, CRUD, schema v5, agent_runs.agent_definition/definition_fingerprint columns), tldw_chatbook/Agents/tool_catalog.py (build_spawn_schema), tldw_chatbook/Agents/agent_service.py (spawn() named-agent resolution: append-only instructions, tools intersection never union, same-provider model override, unknown-agent refusal before budget spend), tldw_chatbook/Widgets/settings_agents_panel.py (AgentsSettingsPanel), tldw_chatbook/UI/Screens/settings_screen.py (Agents category registration, Troubleshooting group), Docs/User_Guide/settings.md (new Troubleshooting -- Agents section + category-map row), Docs/User_Guide/console/agent-runs-and-tools.md (new Named agents subsection).

Battery (Task 7): pytest Tests/Agents/ Tests/DB/test_agent_runs_db.py Tests/UI/test_settings_agents_category.py Tests/UI/test_settings_configuration_hub.py Tests/UI/test_settings_category_sweep.py Tests/Chat/test_console_agent_bridge.py -> 1420 passed, 6 failed -- exactly the 6 pre-existing baseline failures predicted by the plan (3 AgentRunsDB trace-callback tests, task-3012 regression; 3 Settings Appearance tests, _appearance_bool_label undefined), no seventh failure. Full-tree --collect-only -> 34181 tests collected, 0 errors.

Live verification (real tmux TUI, scratch profile, real Anthropic key) found and fixed a real defect along the way: AgentsSettingsPanel's Input widgets (Name/Description/Model override/Tools) were missing the settings-compact-input CSS class every other Settings Input carries, so Textual's default 3-row bordered Input chrome collapsed into the panel's 1-row .settings-input-row and NEVER painted placeholder or value text on screen -- purely cosmetic (Save/DB/list were all correct throughout, proven before the fix), but a real user typing into the form would see nothing happen. Fixed by adding classes="settings-compact-input" to all four Input(...) calls; Tests/UI/test_settings_agents_category.py's 5 tests re-verified green after the fix (they could not have caught it -- all four set .value directly, never simulate a keypress).

All four Step-4 live checks passed after the fix: (1) created 'researcher' (instructions 'Always cite sources', no tool list) via Settings, form now paints correctly; (2) Console delegated to it, transcript showed '[researcher]' sub-agent marker and the reply visibly cited sources (NIST, CRC Handbook) per the instructions; (3) sqlite3 confirmed agent_runs.agent_definition='researcher', definition_fingerprint='4405adefb961c8e7' (16 hex chars); (4) disabling the definition in Settings removed it from the next turn's spawn schema -- the model, asked to list available named agents, correctly reported none available.

A second, unrelated defect surfaced mid-verification and was NOT fixed here (filed as task-13157): the app's own config-rewrite-on-boot pass can write a duplicate key into an already-rewritten TOML table across successive boots, producing invalid TOML; when that happens the config silently fails to parse and the app falls back to the default_user profile with no error surfaced. Caught via lsof before any real data was written; the scratch profile was rebuilt cleanly and the live checks above ran against a verified-clean, isolated profile. Filed as task-13157 (own fix + regression test, out of this task's scope). Two pre-existing baseline test failures were also filed as task-13155 (AgentRunsDB) and task-13156 (Settings Appearance).

Deviations from plan: none in scope; the CSS fix (one call-site change x4, no new AC) and the two new backlog filings were judged necessary/appropriate scope for an honest live-verification pass per the task brief's own instructions.
<!-- SECTION:NOTES:END -->
