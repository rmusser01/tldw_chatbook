---
id: TASK-2154.9
title: 'Wizard: clarify exit paths (FR-01, FR-02)'
status: Done
assignee:
  - '@kimi'
created_date: '2026-08-05 00:15'
updated_date: '2026-08-05 22:58'
labels:
  - console
  - ux
dependencies: []
parent_task_id: TASK-2154
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Welcome step offers Cancel vs 'Skip — explore on my own' vs 'Esc finish later' with no consequence differences; Skip is irreversible (never offered again) but looks equivalent. Relabel to make consequences explicit (e.g. 'Finish later' vs 'Don't ask again'); align step count with chosen track. See findings doc section 1.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Exit labels state their consequence, irreversible path marked,Step tracker matches the selected track length,Wizard tests updated
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: no
ADR path: N/A
Reason: copy/label-only change to existing wizard chrome; no storage, schema, sync, security, or boundary decisions. Persistence semantics of _skip_entirely (setup_completed=True) untouched.

Plan:
1. WelcomeStep.compose_step (FirstRunSetupWizard.py ~2249-2263): relabel skip link 'Skip — explore on my own' -> 'Skip setup — don\'t ask again' and add tooltip stating it is permanent at launch but rerunnable from Settings > Diagnostics; relabel quick radio 'Quick setup — provider & model (recommended)' -> 'Quick setup — provider, model & summary (recommended)' so the track promise matches the 4 tracker entries (FR-02).
2. SetupWizardContainer.on_mount (~2631): after super().on_mount(), relabel #wizard-cancel 'Cancel' -> 'Finish later' and variant error -> default (scoped to first-run wizard only; Chatbook creation/import wizards keep 'Cancel'). Esc hint footer and ConfirmationDialog copy stay unchanged (still accurate: Esc == Finish later button == same dialog).
3. _refresh_active_ids (~2701): call self.update_progress() after _rebuild_progress() so the 'Step X of Y' text total syncs immediately when conditional protect-keys joins on key entry (previously only the dots rebuilt; text total lagged until next step change). Tracker continues to count only committed steps — the AC-preferred behavior for the conditional step; no '4-5' range.
4. Tests/Wizards/test_first_run_setup_wizard.py: add test pinning new exit labels (skip label+tooltip, cancel 'Finish later'+default variant, Esc hint unchanged, quick radio enumerates provider/model/summary) and test that note_key_entered() syncs nav total to 5 immediately.
5. Run .venv/bin/python -m pytest Tests/ -k 'first_run or wizard' -q (time-boxed).
6. UAT: output/ux-review-console/uat_console.py f1_first_run; verify captures/f1-first-run-wizard.txt shows new labels.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FR-01: Welcome exit paths now state their consequences. Nav 'Cancel' -> 'Finish later' (variant error->default; scoped to SetupWizardContainer.on_mount so Chatbook creation/import wizards keep 'Cancel'); skip link 'Skip — explore on my own' -> 'Skip setup — don't ask again' + tooltip naming the Settings > Diagnostics rerun path; Esc hint and ConfirmationDialog copy unchanged (still accurate: Esc == Finish later == same dialog). Persistence untouched: _skip_entirely still commits first_run.setup_completed=True. FR-02: quick-track label now enumerates the steps the tracker shows ('Quick setup — provider, model & summary (recommended)') instead of promising a two-item 'provider & model' track; the tracker deliberately counts only committed steps (conditional protect-keys joins only when a secret exists — a static count would be misleading, and a '4-5' range was rejected by the AC), and _refresh_active_ids now also calls update_progress() so the 'Step X of Y' text total syncs with the rebuilt dots the moment protect-keys joins (previously lagged one navigation). Files: tldw_chatbook/UI/Wizards/FirstRunSetupWizard.py (WelcomeStep.compose_step, SetupWizardContainer.on_mount, _refresh_active_ids); Tests/Wizards/test_first_run_setup_wizard.py (+3 tests: exit labels, track label vs tracker, nav-total sync on key entry); Tests/UI/test_first_run_wizard_live_contract.py (80x24 rendered-frame assertion updated to 'Finish later'). Tests: .venv/bin/python -m pytest Tests/ -k 'first_run or wizard' -q --continue-on-collection-errors (28 pre-existing missing-optional-dep collection errors): 381 passed, 2 failed — both failures reproduce identically at HEAD with changes stashed (speech install-button timeout; rerun_go_to_chat timing flake that passes solo), i.e. pre-existing, not regressions. UAT: output/ux-review-console/uat_console.py f1_first_run regenerated captures/f1-first-run-wizard.{svg,txt} showing 'Skip setup — don't ask again', 'Finish later', 'Step 1 of 4', unchanged 'Esc finish later'. ADR: none — copy/label-only change to existing wizard chrome; no storage/schema/sync/security/boundary decision. Docs: no living doc referenced the old strings (plan/findings docs are historical artifacts).
<!-- SECTION:NOTES:END -->
