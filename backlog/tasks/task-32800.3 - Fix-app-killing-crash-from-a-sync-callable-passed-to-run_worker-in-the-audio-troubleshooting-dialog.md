---
id: TASK-32800.3
title: >-
  Fix app-killing crash from a sync callable passed to run_worker in the audio
  troubleshooting dialog
status: To Do
assignee: []
created_date: '2026-09-18 16:48'
labels:
  - core-review
  - review-crash
dependencies: []
parent_task_id: TASK-32800
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`run_worker(self._get_devices_safe)` passes a plain synchronous method with no `thread=True`. Textual's `Worker._run_async` raises `WorkerError('Request to run a non-async function as an async worker')`, `Worker._run` catches it generically, and `exit_on_error` defaults to True, so the app exits. Reachable from the shipped `stts` route through the STTS window and the dictation window. The fix is one keyword argument.

## Findings this closes

- **P0 [D1]** (W-top) The Audio Troubleshooting dialog passes a SYNC method to `run_worker` without `thread=True`; Textual raises inside the worker and, because `exit_on_error` defaults to True, kills the app
  - Where: `tldw_chatbook/Widgets/audio_troubleshooting_dialog.py:246-248` ```python self.audio_devices = await self.run_worker(self._get_devices_safe).wait() ``` `_get_devices_safe` is a plain `def` (`:275`). Reached from `_initialize_audio` (`:229`), started at `:226` by `self.run_worker(self._initialize_aud

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Opening the audio troubleshooting dialog from the STTS route leaves the app running
- [ ] #2 The device probe runs on a thread worker rather than as an async worker
- [ ] #3 A regression test covers the dialog's initialise path
<!-- AC:END -->
