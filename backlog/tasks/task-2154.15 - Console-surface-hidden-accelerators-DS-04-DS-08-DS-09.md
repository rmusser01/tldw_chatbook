---
id: TASK-2154.15
title: 'Console: surface hidden accelerators (DS-04, DS-08, DS-09)'
status: Done
assignee:
  - '@kimi'
created_date: '2026-08-05 00:15'
updated_date: '2026-08-07 16:02'
labels:
  - console
  - ux
dependencies: []
parent_task_id: TASK-2154
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Middle-click closes session tabs but is undocumented in UI; session switcher lacks in-modal key hints (F2 rename, Ctrl+Enter new tab); slash popup covers the status chips while open. Surface these in place. See findings doc section 5.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Tab close accelerator discoverable in tab tooltip or help,Switcher shows its key hints,Slash popup anchors without covering chips
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: no
ADR path: N/A
Reason: tooltip copy, an in-modal hints line, and an overlay anchor adjustment; no storage, schema, or contract change.

1. DS-04 (tab close accelerator): `console_session_surface.py`'s tooltip builder appends "Middle-click closes the tab." to active and inactive tab tooltips. Update the three exact-equality tooltip pins (test_console_session_tab_strip.py ×2, test_console_native_chat_flow.py ×1).
2. DS-08 (switcher key hints): add an in-modal hints line to `ConsoleSessionSwitcherModal` (`#console-switcher-hints`, muted): "Enter: open | F2: rename | Up/Down: navigate | Esc: close" — documents ONLY bindings that exist. Verification correction: the finding's "Ctrl+Enter opens in new tab" does NOT exist in the modal (Enter/F2/↑↓/Esc only; the 2026-07-02 spec §3 lists Ctrl+Enter but it was never implemented) — the hint omits it; the spec-vs-impl gap is recorded in Implementation Notes as a follow-up candidate, not silently documented into existence. Pin in test_console_keyboard_trust.py.
3. DS-09 (slash popup covers chips): `ConsoleCommandPopup.reposition()` anchors its bottom edge to the TOP of `#console-status-chips` when the strip is displayed (fallback: composer top as before), so the open popup covers transcript rows — the conventional autocomplete trade-off — instead of wiping the chips mid-composition. A border is deliberately NOT added: the OptionList chrome was removed to save rows (tcss comment) and the AC's harm is the wipe. Pin in test_console_command_popup.py.
4. Run the touched test files; Notes + AC + Done + commit.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Surfaced three hidden accelerators in place; no new keybindings were added.

- DS-04: tab tooltips now append "Middle-click closes the tab." (active and inactive tabs) in `console_session_surface.py`'s tooltip builder; the three exact-equality tooltip pins were updated to match (`test_console_session_tab_strip.py` ×2, `test_console_native_chat_flow.py` ×1).
- DS-08: `ConsoleSessionSwitcherModal` gained a muted footer hints line (`#console-switcher-hints`): "Enter: open | F2: rename | Up/Down: navigate | Esc: close" — pinned by a harness test asserting the line names only real bindings. Finding correction: the review's "Ctrl+Enter opens in new tab" does NOT exist in the modal (the 2026-07-02 spec §3 lists it, but it was never implemented); the hint deliberately omits it. Follow-up candidate (not implemented — out of AC scope): either implement Ctrl+Enter in the switcher or drop it from the spec.
- DS-09: `ConsoleCommandPopup.reposition()` anchors its bottom edge to the top of `#console-status-chips` whenever the strip is displayed (fallback: composer top), so the open popup covers transcript rows — the conventional autocomplete trade-off — instead of wiping the chips mid-composition. No border added: the OptionList chrome was deliberately removed to save rows, and the AC's harm was the wipe; the pre-existing no-overlap-with-composer pin still holds (`chips.y < composer.y`).
- Tests: new pins in `test_console_keyboard_trust.py` (switcher hints line) and `test_console_command_popup.py` (popup clears the chip strip, real-CSS harness).
<!-- SECTION:NOTES:END -->
