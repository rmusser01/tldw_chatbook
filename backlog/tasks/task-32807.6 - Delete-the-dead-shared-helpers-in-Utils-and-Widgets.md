---
id: TASK-32807.6
title: Delete the dead shared helpers in Utils and Widgets
status: In Progress
assignee: []
created_date: '2026-09-18 16:55'
labels:
  - core-review
  - review-dead
dependencies: []
parent_task_id: TASK-32807
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Ten helper modules and about 25 symbols have zero importers, re-verified independently against the collected-test inventory: two UI helper modules, a pagination module, a cost-estimation orphan, a debug-helpers module, an ingestion-preferences module, a splash-animations shim, a base-components module whose live sibling covers the same ground, five of seven functions in a text module, six unused validators, and a paths module whose project helpers import a symbol that has never existed, so the only branch that runs always raises.

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The dead modules and symbols are removed
- [x] #2 No production or test import breaks (each deletion grep-verified; preflight green)
- [x] #3 Helpers with re-rolls elsewhere are handled by the adoption tasks in the helper stream rather than deleted blindly
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
PR #2741. Executed the owner's 2026-09-20 dead-code decision ("delete safe tails,
keep entangled"). This pass removed the clean WHOLE-MODULE safe tails — zero
importers in production AND tests, each re-verified by grep (not trusting the
report), and not owned by the helper stream:

- `Utils/ui_helpers.py`, `Utils/pagination.py`, `Utils/cost_estimation.py`,
  `Utils/debug_helpers.py`, `Utils/ingestion_preferences.py` (670 lines).
  Regenerated the diagnostic inventory (drift was solely their own diagnostics
  disappearing).

KEPT per the decision / AC#3:
- `Widgets/base_components.py` — `Tests/UI/test_focus_token_parity.py` imports it
  (test-entangled).
- `Utils/splash_animations.py` — documented compatibility shim (CLAUDE.md) with a
  user example + developer guide referencing it (contract-entangled).
- Helper-stream adoption targets (`truncate_content`, `coerce_bool` family,
  filename sanitizers, `_format_size_bytes`, `token_counter`, `atomic_file_ops`)
  — TASK-32808 owns these; deleting here would pre-empt the consolidation.

AC#1 PARTIAL — remaining safe tails are SYMBOL-level deletions inside LIVE
modules (more surgical, separate pass): `Utils/input_validation` dead validators
(validate_email/username/ip_address/port/and_raise/filename — validate_port is
re-rolled at llm_management_events_vllm.py:104, independent), `Utils/Utils.py` 25
dead symbols, `Utils/text.py` 5 of 7 functions, `Utils/paths.py` project-* (4 fns
+ 4 constants; the import they depend on always fails), `Utils/Splash.py` 2 dead
functions, `db_status_manager._get_db_status_widget`,
`secure_temp_files.cleanup_all_temp_files`.

SECOND PASS 2026-09-20 — the SYMBOL-level safe tails above are now DONE (each
grep-verified zero prod+test importers, same-name twins ruled out):
- input_validation: the 6 dead validators removed.
- text.py: the 5 dead functions removed (+ orphaned logging import); the live
  extract_text_from_segments twin in Utils/Utils.py kept.
- paths.py: the 4 project-* helpers + __main__ demo + the always-failing
  PROJECT_DATABASES_DIR import block removed; module rewritten to its one live
  export get_user_data_dir. mkdir-census P04 re-disposed to migrated/
  remove_obsolete_creation and excluded from the census comparison (P05
  precedent), backlog inventory + Tests/DB/test_private_sqlite_inventory.py
  updated (suite green, 53).
- Splash.py: print_tldw_ascii + get_splash_card_config removed (kept get_ascii_art).
- db_status_manager: dead _get_db_status_widget method + orphaned AppFooterStatus import.
- secure_temp_files: dead cleanup_all_temp_files wrapper removed (kept cleanup_all).
Inventory regenerated.

STILL DEFERRED: Utils/Utils.py's ~25 dead symbols are a MIXED set — several are
helper-stream (32808) adoption targets (truncate_content/_format_size_bytes/
normalize_title) and one is a live twin — per-symbol handling in coordination
with 32808, not a blind sweep here.
<!-- SECTION:NOTES:END -->