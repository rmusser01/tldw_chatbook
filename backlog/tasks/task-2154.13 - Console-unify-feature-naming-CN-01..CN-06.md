---
id: TASK-2154.13
title: 'Console: unify feature naming (CN-01..CN-06)'
status: Done
assignee:
  - '@kimi'
created_date: '2026-08-05 00:15'
updated_date: '2026-08-06 22:53'
labels:
  - console
  - ux
dependencies: []
parent_task_id: TASK-2154
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
One feature, many names: Mic/Dictate/STT; Temporary worded 3 ways; ◌ glyph means temporary AND voice-working; ● means recording AND agent-running; Attach in two places with different meanings; Remove/Clear tooltip drift; Roleplay tab vs nav-personas. Pick one name per concept and apply everywhere. See findings doc section 4.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Dictation feature uses one name in label/tooltip/busy state,Temporary session uses one phrase everywhere,Glyph-per-meaning collisions resolved,Attach entry points disambiguated
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: no
ADR path: N/A
Reason: user-facing copy, glyph constants, and a 3-cell composer width bump only — no storage, schema, sync, boundary, or contract change.

Settled vocabulary (from findings doc section 4 + code reads):

1. **CN-01 dictation → one verb family "Dictate"** (`console_composer_bar.py`): mic button width 8→11 (BASE_ACTIONS_WIDTH 26→29, ATTACHMENT_ACTIONS_WIDTH 30→33, budget comment updated); labels idle `Dictate`, starting `Dictate…`, recording `Dictating` (glyph dropped — red variant + chip timer carry liveness), transcribing `Dictate…` (chip/tooltip decode the phase; rewrites the TX-07 "Text…" choice, comment cites both tasks); recording tooltip → `Stop dictating and transcribe.`
2. **CN-05 glyphs → voice gets its own pair** (`console_glyphs.py`): new `GLYPH_VOICE_RECORDING = "◉"` (capture live) and `GLYPH_VOICE_WORKING = "◐"` (pipeline working). Applied at: voice chip timer head (composer_bar, `●`→`◉`), `VOICE_CHIP_TRANSCRIBING_LABEL` (`◌`→`◐`), screen warmup prefix `f"◌ {msg}"` (chat_screen.py:5896 → `◐`). `●` stays agent-run-only, `◌` stays temporary-only. Comments note TASK-2154.19 will hang ASCII fallbacks off these.
3. **CN-02 temporary → tab tooltip decodes ◌ with the exact chip phrase**: `Temporary — not saved locally.` → `Temporary — not saved.` (session_surface.py:84, matching `TEMPORARY_LABEL`). Longer explanatory sentences (chip tooltip, menu "Save this chat", new-tab button tooltip) keep `not saved locally` per console_ephemeral's local-durability-only docstring.
4. **CN-03 attach → one name per entry point**: control-bar `FALLBACK_ACTIONS` label `Attach` → `Attach context` (byte-matches the live `console_workbench_state.py` action; tooltip already identical); composer ☰ menu entry `Attach` → `Attach file`, description → `Attach a file to the draft` (it opens the file picker via `_handle_console_attach_context`). Action ids unchanged (internal routing; same id serves both handlers deliberately).
5. **CN-04 → one clear-tip phrase**: live-sync tooltips `Clear the attachment.` / `Clear all {n} attachments.` → `Remove the pending attachment.` / `Remove all {n} pending attachments.` (composer_bar.py:3760-3762, matching compose-time copy at :4090).
6. **CN-06 → wizard exits name the Console destination**: first-run `Start chatting` → `Open Console`; rerun `Go to Chat` → `Go to Console` (FirstRunSetupWizard.py:2435-2437 + stale comment at :2965). Palette already says "Open Console"; nav id `nav-personas` is internal — untouched.
7. Sweep pinned copies in Tests (mechanical): `"Mic"`/`"Mic…"`/`"Rec ●"`/`"Text…"` label pins in test_console_dictation{,_firstrun,_streaming}.py, test_console_hands_free_wiring.py, test_console_internals_decomposition.py; `●` chip-head pins in test_console_voice_chip.py; `◌` warmup pins in Tests/Chat/test_console_voice_input.py + test_console_dictation_firstrun.py; `"Clear the attachment."` in test_console_native_chat_flow.py:10233; `"Start chatting"`/`"Go to Chat"` in test_first_run_wizard_live_contract.py + test_first_run_setup_wizard.py comment.
8. Guard test: voice glyphs differ from `GLYPH_TEMPORARY`/`GLYPH_IN_PROGRESS` (glyph-per-meaning, CN-05 regression lock); tab-tooltip phrase assertion where a session-surface test already builds one.
9. Run touched suites + neighbors (composer bar/menu, dictation×4, voice chip, voice input, session surface/ephemeral, control bar/workbench contract, wizard×2), then Implementation Notes + AC check + `-s Done` + commit.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
One name per concept, applied at the constant level so every render site picks it up at once:

- **CN-01 (dictation → "Dictate")**: composer mic button renamed end to end — idle `Dictate`, busy `Dictate…` (starting and transcribing; tooltip + ◐ voice chip decode the phase), recording `Dictating`. Supersedes TX-07's `Text…` (2154.12), which had itself replaced jargon `STT…`. Recording tooltip → "Stop dictating and transcribe." The 9-cell "Dictating" grew the button 8→11; Send and Stop tightened 8→6 (their exact 4-cell labels + padding — the width "Menu" already used), so the row nets **25 cells, one under the old 26**, and the draft keeps its 32-cell floor (pinned by `test_console_workbench_contract`; caught mid-task and fixed by the Send/Stop rebudget, CSS rule included, bundle regenerated).
- **CN-05 (glyph-per-meaning)**: voice lifecycle got its own pair in `console_glyphs.py` — `GLYPH_VOICE_RECORDING = "◉"` (chip timer head), `GLYPH_VOICE_WORKING = "◐"` (transcribing chip label + the screen's warmup/preparing prefix). `●` is agent-run-only, `◌` is temporary-only again. New `Tests/Chat/test_console_glyphs.py` locks the separation. MCP readiness's own ◌/● (different destination, different vocabulary) deliberately untouched; TASK-2154.19 will hang ASCII fallbacks off the new pair like the rest.
- **CN-02 (temporary → one phrase)**: tab tooltip's ◌ decode is now byte-for-byte the chip's `TEMPORARY_LABEL` ("Temporary — not saved.") instead of a third wording. Explanatory sentences (chip tooltip, "Save this chat" row, new-tab button tooltip) keep "not saved locally" per the ephemeral module's local-durability-only rule; extended the tab-tooltip test to pin the exact phrase.
- **CN-03 (attach disambiguated)**: control-bar fallback action `Attach` → `Attach context` (byte-matches the live workbench action); composer ☰ menu `Attach` → `Attach file` / "Attach a file to the draft" (opens the file picker; the control bar opens the Library/workspace rail). F1 Help's Composer row renamed to match. Action ids unchanged — `attach-context` still routes each entry point to its own handler.
- **CN-04**: live-sync clear tooltip `Clear the attachment.` / `Clear all {n} attachments.` → `Remove the pending attachment.` / `Remove all {n} pending attachments.` — one phrase with the compose-time copy.
- **CN-06**: first-run wizard exits name the destination — `Start chatting` → `Open Console`, rerun `Go to Chat` → `Go to Console`. Palette/guide/tab already said "Console"; `nav-personas` stays (internal id).

**Files**: `console_glyphs.py`, `console_composer_bar.py`, `console_session_surface.py`, `console_control_bar.py`, `console_composer_menu_modal.py`, `chat_screen.py` (warmup prefix + F1 row), `FirstRunSetupWizard.py`, `_agentic_terminal.tcss` + regenerated `tldw_cli_modular.tcss`, new `Tests/Chat/test_console_glyphs.py`, pin updates across 11 test files (~180 pinned label/glyph expectations swapped).

**Verification**: 165 + 270 + 81 (streaming) + 127 (internals decomposition) + 304 (native chat flow) + neighbor sweeps passed across dictation×4, voice chip, voice input, composer menu, ephemeral, session tab strip, workbench contract (incl. the draft-floor test after the rebudget), glyphs guard, and both wizard suites. Two failures triaged and resolved: `test_the_transcribing_indication_reverts_on_a_mid_capture_stop` (load flake — passes isolated in 3.4s and in a full-suite rerun), `test_staged_attachment_count_stays_visible…` (pin the sweep missed: substring `"2 attachments"` → `"2 pending attachments"`). One pre-existing failure — `test_speech_step_install_button_visible_at_120x40_without_scrolling` — reproduces at 1db4e1362 with all epic work stashed (environmental), filed **TASK-2156**. Full-file neighbor rerun of workbench-contract/destination-shells/visual-snapshots/rail-sections is folded into the epic's final `-k console` gate.

**ADR**: not required — copy, glyph constants, and a 2-cell composer rebudget only; no storage, sync, boundary, or contract change.
<!-- SECTION:NOTES:END -->
