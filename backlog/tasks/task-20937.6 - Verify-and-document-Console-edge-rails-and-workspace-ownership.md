---
id: TASK-20937.6
title: Verify and document Console edge rails and workspace ownership
status: In Progress
assignee: []
created_date: '2026-08-22 19:37'
updated_date: '2026-08-23 19:47'
labels:
  - console
  - ux
  - performance
dependencies:
  - TASK-20937.4
  - TASK-20937.5
references:
  - >-
    Docs/superpowers/specs/2026-08-22-console-edge-rails-workspace-tree-design.md
  - backlog/decisions/083-console-edge-rails-and-workspace-tree-ownership.md
parent_task_id: TASK-20937
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Prove production CSS geometry, accessibility, performance, mutation continuity, iTerm2 and Windows Terminal parity, update user documentation, and close the parent workstream without broad unrelated tests.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The exact production stylesheet stack passes a compositor matrix covering edge alignment, single dividers, 15/20/35 boundaries, local/outer hints, Tree natural/eight-row cases, Character containment, resize, recompose, hit testing, focus, and scroll clamping.
- [ ] #2 The deterministic small/representative/stress benchmark runs three warm-ups and 20 measured iterations for projection/mount, 5% marker update, search apply/clear, and selection, reporting median, p95, materialized nodes, and reconcile/recompose counts in committed raw evidence.
- [ ] #3 Any representative median regression over 20% is investigated and corrected or explicitly accepted in task notes; documentation makes no speed claim unsupported by the recorded measurements.
- [ ] #4 iTerm2 and Windows Terminal verification uses equivalent reported rows/columns and records section heights, edge/divider geometry, Tree navigation, Character contain behavior, and screenshots/captures; physical pixel size is not treated as the layout oracle.
- [ ] #5 Console and Context/RAG user guides explain workspace ownership, Default/unassigned Conversations, starred-first ordering, Tree disclosure/search/paging, local versus outer scrolling, exact section ceilings, and Character image behavior using current visible copy.
- [ ] #6 Only changed-functionality tests and proportional focused regressions run, followed by scoped Ruff, format, CSS integrity, and diff checks; every child task is Done, TASK-20937 has checked ACs and implementation notes, ADR-083 remains accurate, and the worktree is clean before closeout.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Inventory the exact changed-functionality gate from TASK-20937.1 through TASK-20937.5 and keep unrelated suites out.
2. Complete and verify the deterministic performance harness against the frozen pre-Task-2 baseline, append raw 3-warm-up/20-iteration evidence, and investigate any representative median regression over 20%.
3. Run the complete focused test gate plus scoped Ruff, format, CSS synchronization/integrity, compile, and diff checks.
4. Capture equivalent-cell iTerm2 evidence for edge geometry, 15/20/35 boundaries, Tree behavior, Character contain behavior, resize, focus, and scroll continuity.
5. Obtain the same-commit, same-cell Windows Terminal checklist and captures from the operator; keep the task open on missing or divergent evidence.
6. Update the concise Console guide and detailed Context/RAG guide with current ownership, ordering, Tree, scrolling, ceiling, and Character behavior.
7. Run final specification and quality reviews, audit all child task/ADR links, close TASK-20937.6 and parent TASK-20937 only when every gate is satisfied, then commit the final boundary.

ADR required: no new ADR.
ADR path: backlog/decisions/083-console-edge-rails-and-workspace-tree-ownership.md
Reason: ADR-083 already owns the long-lived rail, Tree, ownership, ceiling, and reconciliation contracts; Task 6 verifies and documents that approved architecture without changing a boundary.
<!-- SECTION:PLAN:END -->

## Diagnostic Inventory Review

- Reviewed the changed persistent diagnostics in `workspace.py` and
  `chat_screen.py` against inventory base `be401e7106d54d53bb5b2f26620b33a45daa09a8`.
- The new dynamic fields are limited to exception class names and internal
  workspace identifiers; no user content, secret, filesystem path, or URL is
  added to a persistent sink. The disclosure-persistence warning was hardened
  to record only the exception class name instead of the exception text.
- Regenerated `Docs/security/production-diagnostic-inventory.json` and reran
  the complete Derived Artifacts checker set under Python 3.11.

## Verification Evidence (2026-08-23, closeout still open)

The exact changed-functionality gate is the 19-file Task 6 gate from the
approved implementation plan, covering the pure workspace and flat projections,
bounded-section policy, Context reconciliation, edge geometry, shell and resize
behavior, native Tree lifecycle and keys, workspace controller/lifecycle,
Character containment, Inspector compatibility, and CSS integrity:

```text
Tests/Workspaces/test_workspace_tree_state.py
Tests/Workspaces/test_console_conversation_browser_state.py
Tests/UI/test_console_bounded_section.py
Tests/UI/test_console_rail_section_layout.py
Tests/UI/test_console_rail_reconciliation.py
Tests/UI/test_console_left_rail.py
Tests/UI/test_console_edge_rail_geometry.py
Tests/UI/test_console_shell_regions.py
Tests/UI/test_console_resize_reflow.py
Tests/UI/test_console_workspace_tree.py
Tests/UI/test_console_workspace_tree_performance.py
Tests/UI/test_console_workspace_context_rail.py
Tests/UI/test_console_workspace_keyboard.py
Tests/UI/test_console_workspace_controller.py
Tests/UI/test_console_workspace_lifecycle.py
Tests/UI/test_console_character_avatar.py
Tests/UI/test_console_character_controller.py
Tests/UI/test_console_right_rail.py
Tests/UI/test_css_build_integrity.py
```

- Frozen-baseline validation on clean source
  `25be0541956745896b9e86a3d07bdbc5a5948c04`: `1 passed, 1 warning in
  3.06s`. The immutable source remains
  `5729439e5ad4fe0959b59a1fe699ef9ee3ebb2f8`, with the committed fixture and
  JSON digests unchanged.
- Mounted new-path benchmark on that clean source: `1 passed, 1 warning in
  17.11s` (14.42s call). Small/representative/stress counts were respectively
  16/164/3,825 service records, 9/57/840 ordinary materialized nodes, 100
  reconciles, and zero recomposes. Three warmups preceded the 20 committed raw
  samples for every operation and dataset.
- Representative mounted/settled medians were 48.020 ms initial mount, 22.668
  ms marker update, 70.492 ms search apply/clear, and 22.166 ms selection. Each
  exceeds the 20% diagnostic threshold versus the frozen projection-only
  baseline. This difference is explicitly accepted because the new measurement
  includes native Tree mount, deferred layout/paint settling, compositor strip
  rendering, and full-scope search nodes while the old baseline measured only
  projection/row materialization. The evidence does not support, and the docs
  do not make, a relative speed claim.
- The first exact 488-test gate exposed one unrelated Inspector deduper failure:
  the environment emitted an additional credential-store warning into the
  test's monkeypatched logger. Its owner isolated that capture in test-only
  commit `d651b2c58` after rebase; the repaired node passed independently, then
  the complete exact gate passed all 488 tests with 6 known dependency/image
  warnings in 545.30s. After rebasing the two closeout commits onto current
  `origin/dev` (`33ff5b754`), the same 488-test gate passed again with the same
  6 warnings in 583.29s. The post-test pytest garbage-directory cleanup warnings
  are an existing environment condition and did not change the green result.
- `console.md` and `context-and-rag.md` were audited against the merged behavior.
  They already cover exclusive named-workspace versus Default/unassigned
  ownership, starred-first order within one owner, independent searches, Tree
  disclosure/navigation/paging and Retry, the Default Switch route, local and
  outer cues, exact 15/20/35 Context ceilings and Inspector's 20-row ceiling,
  and scale-down-only Character containment. No user-guide change was needed.
- Scoped Ruff lint passed on all six Task 6 Python test owners. Ruff format
  exposed and corrected two formatter-only expressions in
  `test_console_rail_reconciliation.py`; its three directly affected tests then
  passed, and all six files passed the format recheck. CSS integrity passed 13
  tests, and `git diff --check` passed.
- The current board resolves exactly one Console TASK-20937 parent and children
  `.1` through `.6`, with the spec, plan, and ADR-083 links present. The fetched
  stale ref `origin/fix/backlog-task-file-parse-guard` and seven stale worktrees
  still carry the old pre-renumber GGUF filename that once claimed TASK-20937;
  current dev renamed that task to TASK-20935. Those stale branches must retain
  the rename when rebased, but they do not create a duplicate in the current
  board.

### Operator evidence still required

Neither terminal checklist is present. TASK-20937.6 and TASK-20937 therefore
remain In Progress. For each terminal, attach evidence from the same commit (or
an explicitly proven source-equivalent build) and the same reported cell sizes:

- iTerm2: record macOS/iTerm2/Python/Textual versions, `git rev-parse HEAD`,
  clean-status or source-equivalence proof, terminal rows/columns, launch
  command, and screenshots/captures.
- Windows Terminal: record Windows/Windows Terminal/Python/Textual versions,
  the same commit/source-equivalence proof, the same rows/columns, launch
  command, and screenshots/captures.
- In both captures, verify expanded edge-aligned rails and one divider per
  boundary; all-open Context with exact 15/20/35 local ceilings plus local and
  outer cues; Tree selection, disclosure, search apply/clear, paging/Retry,
  star/unstar, and the Default route; portrait and landscape Character contain
  behavior; and collapse/reopen plus resize focus/offset continuity.
- Compare terminal cells and behavior, not physical window pixels. Any missing
  or divergent observation keeps both tasks open.
