---
id: TASK-2154.7
title: >-
  Console: sync setup card steps with action; fix empty-provider template
  (FR-05, FR-07)
status: Done
assignee:
  - '@kimi'
created_date: '2026-08-05 00:15'
updated_date: '2026-08-05 22:39'
labels:
  - console
  - ux
dependencies: []
parent_task_id: TASK-2154
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Setup card shows active step 'Finish provider setup' while its only button reads 'Choose model'. Console Settings modal renders empty provider name: Provider blocked: '' is not available in Console yet. Sync the primary action label to the first incomplete step; fix the empty interpolation. See findings doc section 1.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Card action label matches first incomplete step (provider vs model),No empty '' in provider blocked copy,Both verified with no-provider and provider-no-model states
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: no
ADR path: N/A
Reason: copy/state-mapping fix inside existing Console readiness/onboarding boundaries; no storage, sync, provider-boundary, or contract changes.

Root causes (verified via probe output/ux-review-console/probe_2154_7.py):
- FR-05: _active_console_settings_readiness short-circuits with label 'Missing model' before evaluating provider readiness, so the card's step resolution (provider_done=native_send_supported) and the action resolution (_console_provider_recovery_action checks model text before provider readiness) disagree. In the empty-config state the card showed fallback 'Finish provider setup' while the button resolved to 'Choose model'.
- FR-07: build_console_settings_readiness fires the unsupported-provider template with an empty provider_key, rendering Provider blocked: '' ...

Steps:
1. console_session_settings.py build_console_settings_readiness: guard empty provider_key before the 'not provider_supported' branch; return label 'Unknown' with the canonical no-provider copy from get_provider_readiness('') (Select a provider family) instead of interpolating ''.
2. chat_screen.py _active_console_settings_readiness: when model is missing, evaluate provider readiness first; if provider is not send-ready return that readiness (provider is the real first blocker), else keep the 'Missing model' sentinel (now strictly provider-ready + model-missing).
3. chat_screen.py _build_console_setup_card_state: feed raw provider readiness (build_console_settings_readiness on effective settings) so card steps 1/2 reflect true provider state.
4. chat_screen.py _console_provider_recovery_action / _console_provider_blocker_copy / _console_provider_recovery_field: blank session provider => Choose provider family; then provider readiness before model check; Missing model => Choose model; provider blockers => Set up provider / Configure endpoint (Endpoint not saved + Invalid URL) / Choose provider (Unknown) / Review settings.
5. Tests (Tests/UI/test_console_session_settings.py, off-pilot ChatScreen pattern): no provider at all; provider set + no key + no model; provider ready + no model (regression); endpoint invalid/not saved; empty-provider readiness copy has no empty quotes.
6. Targeted pytest: .venv/bin/python -m pytest Tests/ -k 'onboarding or setup_card or setup_modal or readiness' -q
7. UAT: output/ux-review-console uat_console.py f2_after_skip f3_send_blocked; verify f2b button is a provider action synced with step 1 and f3c copy has no empty quotes.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FR-05 + FR-07 fixed at the readiness/action resolution layer.

Root causes (confirmed via output/ux-review-console/probe_2154_7.py): FR-05 — _active_console_settings_readiness short-circuited to a 'Missing model' sentinel without evaluating provider readiness, and the recovery action/blocker copy checked model text before provider readiness, so a provider-blocked session showed card step 1 as the 'Finish provider setup' fallback while the button read 'Choose model'. FR-07 — build_console_settings_readiness interpolated an empty provider_key into the unsupported-provider template.

Changes:
- tldw_chatbook/Chat/console_session_settings.py (~576): empty provider_key returns label 'Unknown' with the canonical no-provider copy from get_provider_readiness('') ('Select a provider' family) before the unsupported-provider branch (FR-07).
- chat_screen.py _active_console_settings_readiness (~4732): evaluates provider readiness before the model check; provider-not-send-ready returns that readiness; 'Missing model' sentinel now strictly means provider-ready + model-missing (FR-05).
- chat_screen.py _build_console_setup_card_state (~13840): feeds raw build_console_settings_readiness so card steps reflect true provider state (step 1 done when only the model is missing).
- chat_screen.py _console_provider_blocker_copy / _console_provider_recovery_action / _console_provider_recovery_field (~13687-13820): blank session provider => Choose provider family; provider readiness before model check; provider blockers => Set up provider (missing key) / Configure endpoint (Endpoint not saved + Invalid URL) / Choose provider (Unknown); Missing model => Choose model; settings=None tolerated via display-provider fallback.

Verified states: no provider => step 1 'Choose a supported provider' + button 'Choose provider'; openai no key + no model => step 1 'Connect a provider' + button 'Set up provider' (Settings api_key field); llama_cpp ready + no model => step 1 done 'Provider ready', step 2 active 'Pick a model', button 'Choose model' (regression preserved); endpoint unsaved/invalid => 'Configure endpoint' + field 'endpoint'.

Tests: added 5 state-combo tests (Tests/UI/test_console_session_settings.py) + empty-provider copy test (Tests/Chat/test_console_session_settings.py). Updated tests that had encoded the FR-05 bug: 6 in Tests/UI/test_console_workbench_contract.py (two renamed: ..._setup_action_opens_settings, ..._provider_action_is_visible_and_primary_recovery; click assertions now use ConsoleNavigationHarness since the provider action targets the app Settings screen) and the clean-run case in Tests/UI/test_product_maturity_phase1_empty_setup_states.py.

Results: targeted -k 'onboarding or setup_card or setup_modal or readiness' 207 passed / 3 pre-existing skips (optional-dep dirs that cannot collect in this venv ignored: numpy/pydub gaps); affected files (session settings UI+Chat, maturity, send-disabled) 213 passed; workbench+rail+internals 232 passed; native-chat-flow recovery/setup 14 passed. UAT: uat_console.py f2_after_skip f3_send_blocked — f2b card button 'Choose provider' with step 1 provider action; f3c settings modal copy 'No provider is not ready: Select a provider. ...' with no empty quotes; PNGs regenerated via qlmanage.

ADR: none — copy/state-mapping fix inside existing Console readiness/onboarding boundaries; no storage, sync, provider-boundary, or contract changes.
<!-- SECTION:NOTES:END -->
