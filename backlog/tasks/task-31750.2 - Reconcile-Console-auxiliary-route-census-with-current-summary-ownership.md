---
id: TASK-31750.2
title: Reconcile Console auxiliary route census with current summary ownership
status: Done
assignee:
  - '@codex'
created_date: '2026-09-06 15:49'
updated_date: '2026-09-06 16:19'
labels: []
dependencies: []
parent_task_id: TASK-31750
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Restore exhaustive provider-route ownership evidence after manual summaries moved to the shared auxiliary service and Settings added bounded generation probes. Keep auxiliary operations capture-off and ordinary conversation admission fail-closed.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Manual and automatic summaries have distinct explicit auxiliary routes and remain capture-off; Settings probes remain excluded.
- [x] #2 The exhaustive route census rejects missing or duplicate ownership and ordinary conversation routes remain forbidden in auxiliary admission.
- [x] #3 The existing memory-restart fixture closes its reopened SQLite handle on success or failure, and the unchanged native descriptor probe reports no retained handles for it.
- [x] #4 Native Chat tests release their exact builder-owned app databases and temporary real databases through existing ownership fixtures; all 34 previously attributed cases and the complete file pass without retained descriptors, preserving generation-probe assertions.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Reproduce missing manual-summary routing and auxiliary admission failures with targeted negative controls; retain the two existing census failures as baseline evidence.
2. Pass distinct manual/automatic routes through explicit auxiliary keywords, admit only those capture-off routes, and make Settings exclusion explicit.
3. Refresh the closed logical-route and gateway-callsite census, preserving exhaustive discovery and unique ownership; prove missing routes, omitted/new calls, and duplicate ownership fail the guard.
4. Run complete affected test files with native descriptor attribution, scoped lint/format checks, and root review before completion.
5. Reproduce the unchanged restart fixture's retained reopened SQLite handle, then close that exact owned handle in finally and verify the resource case and complete compaction file with the unchanged descriptor probe.
6. Reuse the existing opt-in Console resource and app ownership fixtures in the native Chat test file. Normalize its seven redundant factory-alias calls to the identical existing builder so every app is tracked, preserving all arguments and assertions. Verify the 34 previously attributed cases, then the complete native file, without changing shared cleanup helpers or descriptor thresholds.

ADR required: no new ADR.
ADR paths: backlog/decisions/052-console-conversation-memory-and-compaction-policy.md and backlog/decisions/097-console-reference-backed-semantic-trace-ledger.md.
Reason: Preserve the existing auxiliary-memory and conversation-trace boundaries; explicit route identity does not change capture policy, storage, or provider authority.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Manual and automatic summaries now carry distinct explicit routes through the shared capture-off auxiliary gateway. Settings generation probes explicitly pass `route=None` and are censused as excluded. Updated logical/callsite metadata to current owners and locations; retained exhaustive AST discovery, explicit-route enforcement, exact capture disposition, and unique ownership. Six corruption controls reject missing/new calls, absent explicit routes even with forged matching metadata, duplicate calls/routes, and missing ownership. Gateway coverage rejects every ordinary conversation route before adapter entry and forbids trace/shadow creation for auxiliary requests.

Modified the compaction service, provider gateway, provenance census, Settings navigation, and their four existing test files. Aligned narrow gateway doubles with the real explicit keyword contract. The resource probe also reproduced the HEAD-identical memory-restart test retaining its reopened DB/WAL/SHM; only that handle gained a `finally` close, preserving all assertions.

Evidence under `/private/tmp/`: `tldw-31750-2-red` records 2 failures/14 passes before the runtime correction; `tldw-31750-2-green` records 16 passes. `tldw-31750-2-fd-red` reproduces the retained handle and `tldw-31750-2-fd-green` passes without retention. Complete compaction/census rerun: 153 passed (`tldw-31750-2-compaction-final`); final strict census: 10 passed (`tldw-31750-2-census-final`). Root's final three-file auxiliary run passed 593 with no retained handles (`tldw-31750-auxiliary-final`). Complete native Chat/settings file passed 349 in 470.25s (`tldw-31750-2-native-full`). All evidence has XML and log companions.

The initial native-file resource report attributed retained app/database handles to 34 existing cases outside the three changed generation-probe tests. Reused the two existing opt-in Console ownership fixtures; seven calls through a redundant alias now use the identical tracked factory. Those seven test bodies are AST-identical to HEAD apart from the factory name, and all generation-probe assertions remain intact. No shared helper, global cleanup, descriptor threshold, or production lifetime changed. The 34 attributed cases then passed in 56.38s (`tldw-31750-2-native-fd-green`), and the complete native file passed 349 in 431.25s (`tldw-31750-2-native-final`), both exit 0 with zero retained descriptors or ResourceWarnings. XML confirms zero failures, errors, and skips.

Eight-file Ruff lint and diff checks pass; seven files fully pass Ruff format, while native Chat retains HEAD-existing format drift outside the changed snippets, which individually pass. Four existing dependency warnings remain in the final native run. Root reviewed the route changes and exact-owned cleanup without findings; its cleanup-owner failure/isolation controls passed 13 (`tldw-31750-resource-owner-controls`). No full-repository sweep ran.

ADR required: no new ADR. Existing contracts: `backlog/decisions/052-console-conversation-memory-and-compaction-policy.md` and `backlog/decisions/097-console-reference-backed-semantic-trace-ledger.md`; test-only reuse of existing ownership fixtures adds no boundary. Root verified the final evidence and marked the task Done.
<!-- SECTION:NOTES:END -->
