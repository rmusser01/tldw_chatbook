---
id: TASK-2154.22
title: 'Docs: reconcile stale console specs with implementation (DR-01, DR-02, DR-03)'
status: Done
assignee:
  - '@kimi'
created_date: '2026-08-05 00:15'
updated_date: '2026-08-07 12:47'
labels:
  - console
  - ux
dependencies: []
parent_task_id: TASK-2154
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
2026-07-02 spec says Ctrl+M (code: Alt+M); dual-audience rail IA (Session/Context/Model/Details) differs from live (Session/Model/Agent/Details); persistent-rails spec says 'no auto-open ever' but code auto-opens in the 118-128 band. Update specs or code and record the decision. See findings doc section 9.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
<!-- AC:BEGIN -->
- [x] #1 #1 Specs updated to match implemented bindings and rail IA,Auto-open decision recorded (spec amended or code changed)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: no
ADR path: N/A
Reason: docs-only reconciliation of specs with long-implemented behavior; the DR-03 auto-open decision itself shipped under TASK-915/TASK-1140 — this task records it in the governing spec (no new architectural choice is made here).

1. DR-01: amend the 2026-07-02 dual-audience spec — Ctrl+M → Alt+M model popover (2 sites: §3 overlay bullet, §5 architecture table), noting Ctrl+M aliases Enter in terminals per §3's own conflict rule.
2. DR-02: amend the same spec's §1 rail IA with the live section set (`session / model / details / agent / character`; staged-context sources in the Inspector rail); add a pointer note atop the 2026-07-18 sidebar spec.
3. DR-03: amend the 2026-05-24 persistent-rails spec — record the three bounded, ephemeral auto-open exceptions (118–128-col standard-width Inspector open, pending-launch Inspector open, fleet Agent-section force-open) as the accepted behavior, superseding "no auto-open, ever"; all three are render-state-only and yield to explicit user toggles.
4. Implementation Notes + AC flip + Done + commit. No code changes, no test changes.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Docs-only reconciliation; no code or tests changed. All amendments verified against the live implementation before writing.

- **DR-01 (binding):** `Docs/superpowers/specs/2026-07-02-console-dual-audience-ux-design.md` — model popover binding corrected `Ctrl+M` → `Alt+M` in §3 (overlay bullet) and §5 (architecture table), with an inline note that `Ctrl+M` aliases Enter in terminals (the spec's own §3 conflict rule covers this case). Implementation (`chat_screen.py` binding) and user guide already shipped `Alt+M`; only the spec was stale.
- **DR-02 (rail IA):** same spec, §1 — added a dated reconciliation paragraph recording the live section set `session / model / details / agent / character` (verified: `CONSOLE_RAIL_SECTION_IDS` in `Chat/console_rail_state.py:16`), noting the Context section's staged-context sources moved to the Inspector rail (verified: `ConsoleStagedContextTray` composes with `console-inspector-context-section`, `chat_screen.py:15267`). A pointer blockquote was added atop `Docs/superpowers/specs/2026-07-18-console-left-sidebar-usability-design.md` directing readers to the amended IA; that document's styling guidance is unchanged.
- **DR-03 (auto-open):** `Docs/superpowers/specs/2026-05-24-console-persistent-rails-design.md` — added an "Auto-Open Reconciliation" subsection superseding "no auto-open, ever". Decision: **keep the implemented exceptions, amend the spec** (the code moved; the spec now follows). The three exceptions, all verified in `chat_screen.py`, are render-state-only, never persisted, and yield to explicit user toggles: (1) standard-width Inspector open in the 118–128-col band when `Run recipe` + ≥1 run-state row exist and no `right_open` preference is stored (`_should_open_standard_width_inspector`); (2) pending-launch Inspector open, cleared by the next explicit right-rail toggle (`_apply_pending_launch_inspector_auto_open`); (3) fleet Agent-section force-open while the fleet summary line is non-empty, suppressed for the busy window by manual collapse (`_apply_fleet_agent_section_auto_open`). Changing the code instead (removing discovery-oriented auto-opens) was rejected: it would regress first-run Inspector discoverability and re-hide run state that TASK-915/TASK-1140 deliberately surfaced.
<!-- SECTION:NOTES:END -->
