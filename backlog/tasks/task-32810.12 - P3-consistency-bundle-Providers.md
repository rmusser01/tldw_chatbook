---
id: TASK-32810.12
title: 'P3 consistency bundle: Providers'
status: Done
assignee: []
created_date: '2026-09-18 17:00'
labels:
  - core-review
  - review-polish
dependencies: []
parent_task_id: TASK-32810
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The core review recorded 4 P3 findings in `LLM_Calls/`: sixteen re-rolls of a session-and-retry block and duplicated provider helpers. None is worth its own pull request and none changes behaviour a user sees; together they are the difference between a module that reads as maintained and one that does not. The individual findings are listed in the review report's P3 table, filtered to this area.

Source: core-runtime code review 2026-09-17 — `qa/core-code-review-2026-09-17/report.md`; per-slice evidence in `qa/core-code-review-2026-09-17/slices/`, reproductions in `qa/core-code-review-2026-09-17/phase4-verification.md`.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Each of the 4 findings for this area is either fixed or explicitly declined with a reason recorded in the task
- [x] #2 No behaviour visible to a user changes
- [x] #3 The area's targeted tests stay green
<!-- AC:END -->


## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
PR (`fix/core-review-polish-4`), stream-12 increment 4. All 4 findings dispositioned:

- #3 (P3 [D1], FIXED) malformed-SSE warnings logged the raw provider line/event
  past the is_sensitive_llm_request() guard. Wrapped all 5 active sites with
  safe_llm_error_detail() (anthropic event_data_str, cohere decoded_line +
  cohere_event, google json_str, huggingface data_content). The 6th site the
  review named (HF "raw line") is already commented out.
- #4 (P3 [D3], PARTIALLY FIXED) assorted smells: deleted dead+broken
  summarize_chunk (0 callers; passed args positionally so api_name became the
  input text); removed the redundant function-body import requests/json in the
  openrouter summarizer; fixed the missing-f logging.error literal template. The
  module-level unused api_key global is already gone on dev. DECLINED sub-items:
  wrong-provider error copy (visible text + test-assert risk), per-call import
  hoists (cycle risk), bare requests.post -> session (behaviour/pooling), 5-copy
  best-effort-close helper + per-call Session churn (consolidation, gated paths).
- #1 (P3 [D4b], DECLINED) summarize_with_* 16x block: real behavioural drift
  (some close the response on abandon, some raise_for_status, one hardcodes URL);
  a blind unification would change behaviour -- needs per-provider verification.
- #2 (P3 [D4b], DECLINED) moonshot/zai 14 verbatim helpers: extraction is safe in
  principle but touches two live provider modules with only the error-label
  differing; deferred to a dedicated verified pass with the strict-JSON family.

Verified: both modules import gate-free; test_sensitive_llm_logging 70 passed
(3 pre-existing dev failures on the request-exception path, unrelated); inventory
regenerated; preflight green.
<!-- SECTION:NOTES:END -->