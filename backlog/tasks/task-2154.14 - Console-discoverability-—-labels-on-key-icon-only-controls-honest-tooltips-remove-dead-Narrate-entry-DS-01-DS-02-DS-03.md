---
id: TASK-2154.14
title: >-
  Console: discoverability — labels on key icon-only controls, honest tooltips,
  remove dead Narrate entry (DS-01, DS-02, DS-03)
status: Done
assignee:
  - '@kimi'
created_date: '2026-08-05 00:15'
updated_date: '2026-08-06 07:19'
labels:
  - console
  - ux
dependencies: []
parent_task_id: TASK-2154
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Icon-only controls (☰, ✕, ▾▸, 👍👎🗑♻) need hover to identify. ☰ tooltip lists 3 of 8 menu entries. 'Narrate Entire Conversation' ships as visible-but-unimplemented. Add text labels or labeled menus for critical controls, fix the ☰ tooltip, remove the dead Narrate entry. See findings doc section 5.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Critical icon-only controls gain text labels or labeled menus,☰ tooltip reflects actual menu contents,Narrate entry removed until implemented
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ADR required: no — UI copy/affordance only; no storage/schema, sync, boundary, security, or contract decisions (button label, tooltip, menu-row removal, legend text). ADR path: N/A.
Plan:
1. DS-03: remove ACTION_NARRATE_CONVERSATION constant + menu entry (console_composer_menu_modal.py:25,136-140) and the placeholder dispatch branch + import (chat_screen.py:432,6274-6279). Dated spec/QA-capture mentions stay — point-in-time docs, not live refs.
2. DS-02: ☰ tooltip -> honest summary of the real entries (console_composer_bar.py:3909).
3. DS-01a: composer ☰ button becomes labeled 'Menu' (width 4->6; it sits left of the draft, outside the BASE_ACTIONS_WIDTH row; _draft_render_width reads the live region so geometry self-adjusts). Update adjacent comments + the one label pin in test_console_internals_decomposition.py:486.
4. DS-01b: message-action legend — replace the static SELECTED_MESSAGE_ACTION_GUIDE with a per-row dynamic guide built from the row's actual actions: new pure action_row_guide() in console_message_actions.py naming each glyph in words (🔊 Speak, ♻ Regenerate, ---> Continue, 👍/👎 Rate, 🗑 Delete, ⏹ Stop, </> Variants) alongside the existing key hints (j/k, c, e, r, Esc); wire into console_transcript.py _build_rows action-help row + to_plain_text. Buttons keep compact glyph labels (row width budget); the legend — always visible under the selected row — carries the names. ✕ clear-attachment / per-tab ✕ / rail chevrons stay glyph-only: each already sits beside its text target with an accurate tooltip (verified); noted in Implementation Notes.
5. Tests: update order pin + import in test_console_composer_menu.py; new unit tests for action_row_guide (standard row, no-speak row, variant row); tooltip/label assertions where suites already pin composer chrome.
6. Run: pytest Tests/UI -k 'composer or menu or transcript' plus Tests/Chat/test_console_message_actions.py; py_compile changed files. UAT: uat_console.py p1_ready p2_send; inspect captures for the labeled Menu button + menu contents.
7. Update Docs/User_Guide/console.md ☰ mentions to the new label.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
DS-01/DS-02/DS-03 done: labeled composer Menu button with an honest tooltip, per-row glyph-naming action legend, dead Narrate entry removed.

Approach per finding:
- DS-03: removed ACTION_NARRATE_CONVERSATION and its menu entry (console_composer_menu_modal.py) plus the import and placeholder dispatch branch in chat_screen.py. A removal note sits where the constant was. Dated spec/QA-capture mentions left as point-in-time records. New regression test asserts no menu state (normal/temporary/image/undo) carries a narrate row or a 'not implemented' description.
- DS-02: tooltip now summarizes the real entries: 'Composer menu: prompts, attach, save as Chatbook, generate image or caption, impersonate.' (pinned in test_console_internals_decomposition.py).
- DS-01a: the bare ☰ button is now labeled 'Menu' (console_composer_bar.py compose). Budget: the left cluster keeps its old 18-cell footprint -- Composer ▾ tightened 14->12, menu button 4->6 -- so the draft loses no width and the 26-cell BASE_ACTIONS_WIDTH row is untouched. This required clearing Textual 8's default line-pad (1 cell each side of every rendered line) inline via styles.line_pad = 0 on both buttons: region/content-region math said the labels fit, but the painted capture showed 'Me' -- line-pad eats 2 cells inside the content region, and the TCSS parser rejects line-pad: 0, so it must be set in Python (new entry in backlog/docs/lessons-testing-evidence.md; the bundle's TASK-2154.4 comment had already hit the CSS-parser wall).
- DS-01b: message-action glyph buttons kept their compact labels (row width budget); instead the always-visible legend under a selected row now names the glyphs in words, built from the row's ACTUAL actions via new action_row_guide()/ACTION_GUIDE_SEGMENTS in console_message_actions.py ('Guide: j/k select · c Copy · 🔊 Speak · e Edit · r ♻ Regenerate · ---> Continue · 👍/👎 Rate · 🗑 Delete · Esc clear'). It follows the speak->⏹ swap, variant nav, and no-speak rows, and its text joined the action-help row signature so a stale legend can't survive a row change. plain export path uses the same builder (plain_action_guide). The other DS-01 controls were verified already-labeled by context + accurate tooltips and left glyph-only: rail chevrons (text section title + Collapse/Expand <title> tooltip), per-tab ✕ ('Close Console tab' on a titled tab), ✕ clear-attachment ('Remove the pending attachment.' beside the named 📎 indicator).

Files changed:
- App: console_composer_menu_modal.py, chat_screen.py (import+dispatch), console_composer_bar.py (label/widths/line_pad/tooltip/comments), console_message_actions.py (guide builder), console_transcript.py (dynamic guide wiring; static constant removed), css/components/_agentic_terminal.tcss (#console-composer-collapse 14->12) + regenerated css/tldw_cli_modular.tcss via build_css.sh.
- Tests: test_console_composer_menu.py (order pin, DS-03 regression), test_console_internals_decomposition.py (label+tooltip pin), test_console_native_transcript.py (guide assertions updated + DS-01 glyph-name assertions), test_console_message_actions.py (6 new action_row_guide tests), test_console_composer_collapse.py (12/10 pins, fixture token, docstring).
- Docs: User_Guide console.md (☰ -> Menu, 2 spots), console/chat-basics.md (new guide wording + composer description); backlog/docs/lessons-testing-evidence.md (line-pad lesson).

Verification: prescribed filter pytest Tests/UI -k 'composer or menu or transcript': 511 passed, 1 known sibling-stale failure (test_console_tab_reaches_major_console_screen_regions -- TASK-2154.11 Tab-is-pane-local, flagged at task start). Also green: test_console_internals_decomposition 127, test_console_workbench_contract 64, composer_collapse 39, message_actions+composer_menu+native_transcript 183 (same 1 known failure), Tests/Chat -k ephemeral 28. test_console_generation_actions: 34 passed + the flagged pre-existing HEAD failure ('ChatScreen' object has no attribute '_nodes', bare-construction issue, untouched by this diff). UAT uat_console.py p1_ready p2_send clean: captures/p1-ready-console.txt + p2c-reply-complete.txt paint 'Composer ▾' and 'Menu' in full with the draft row intact; live modal probe lists Prompts/Attach/Save as Chatbook/Generate Image/Generate Caption/Impersonate -- no Narrate. py_compile clean over all changed files (no ruff/flake8/black in the venv).

ADR check: none required -- UI copy/affordance only (button label, tooltip, menu-row removal, legend text, inline style); no storage/schema, sync/conflict, data-ownership, boundary, security, or contract decisions. The width rebudget is internal to one composer's left cluster and documented in code comments.
<!-- SECTION:NOTES:END -->
