---
id: TASK-2154.12
title: 'Console: terminology pass on chips, titles, commands (TX-01..TX-05, TX-07)'
status: Done
assignee:
  - '@kimi'
created_date: '2026-08-05 00:15'
updated_date: '2026-08-06 06:17'
labels:
  - console
  - ux
dependencies: []
parent_task_id: TASK-2154
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Untranslated jargon in primary surfaces: 'Transcript / Event Stream', 'Library RAG', 'RAG: off', 'Scope: empty' + ∩/→ tooltips + raw scope_empty token, 'Tools: not loaded', '/prefill' 'Arm', 'Save Chatbook', 'Sources: N staged', 'STT…'. Rename to plain language or add explanations; never show raw cause tokens. See findings doc section 3.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Pane title drops "Event Stream"
- [x] #2 Scope chip/tooltips in plain language without raw cause tokens
- [x] #3 Tools chip hides the lazy-loading detail
- [x] #4 Slash command descriptions jargon-free and never empty
- [x] #5 Send-disabled reason names the real blocker for the missing-key state
- [x] #6 User guide and affected tests updated; console suites green
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Copy-only terminology pass (findings doc section 3). ADR required: no — copy-only changes, no storage/schema/boundary decisions. Plan:
1. TX-01: CONSOLE_TRANSCRIPT_TITLE 'Transcript / Event Stream' -> 'Conversation' (console_session_surface.py:40; '| {session title}' form follows automatically).
2. TX-02: one vocabulary 'Library search': control-bar action 'Run Library RAG' -> 'Search Library' (console_workbench_state.py:80, console_control_bar.py fallback), RAG chip 'RAG: off/on' -> 'Library search: off/on' (console_display_state.py:443), RAG settings modal header/button/status copy (console_rag_settings_modal.py), live-work card run button (chat_screen.py:14262), blocked-send sentence (chat_screen.py:17564), chip binding descriptions.
3. TX-03: scope chip/row label 'Scope: empty' -> 'Scope: no sources' (console_status_chips.py, console_retrieval_scope_row.py); new scope_cause_phrase()/scope_empty_notice() in rag_scope.py mapping cause tokens (no-workspace-overlap/deleted-items/workspace-scope-unavailable/scope_empty) to plain phrases — used by chip tooltip, row tooltip, chat_rag_events notification, library_rag_service recovery (status_label -> 'Scope: no sources'); intersection tooltips in words ('Only searching: conversation scope (A items) and workspace scope (B items) — N in both.').
4. TX-04: _tools_ready_text(0) -> '—'; Tools chip hidden at zero count (compose + sync_state, mirrors scope/cost chip hiding); Inspector Tools row KEPT with the dash value (omitting it removed the whole Tools group heading the inspector layout tests pin).
5. TX-05: /prefill description -> 'Prepare the start of the assistant''s reply'; empty command description fallback 'Custom command'.
6. TX-07: 'Save Chatbook' -> 'Save as Chatbook' (inspector action label + composer menu row); 'Sources: N staged' -> 'Sources: N'; mic busy label 'STT…' -> 'Text…' (<=6-cell button budget).
7. Send-disabled reason: reorder substring checks in build_console_disabled_reason — 'api key' and 'endpoint' before 'model' (real _console_setup_blocked_reason strings contain 'Providers & Models', which the 'model' check matched first).
8. Guide: console.md + console/{context-and-rag,attachments-images-voice,agent-runs-and-tools,sessions-tabs-workspaces}.md updated to quoted strings.
9. Tests: update pinned copy in ~15 files; add coverage for empty-command-description fallback, scope tooltip wording, send-reason missing-key regression, tools-chip hide.
10. Run console suites + UAT p1_ready f4_slash_popup p6_send_failure; check workbench snapshot test.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Terminology pass complete: plain-language copy across Console title, chips, tooltips, slash commands, and send-blocked reasons; raw cause tokens never render. 1002+355+127 tests green across the affected suites; UAT captures confirm. No ADR (copy-only).

Approach: copy-only renames per findings doc section 3 (TX-01..TX-05, TX-07) plus the send-disabled-reason mis-mapping flagged by TASK-2154.7. No ids, classes, widget structure, or behavior changed — only user-visible copy, two new pure copy helpers in rag_scope.py, and one chip-visibility toggle.

String mapping (old -> new):
- TX-01: "Transcript / Event Stream" -> "Conversation" (pane title, incl. the "| {session}" form).
- TX-02: one vocabulary, "Library search": control-bar action "Run Library RAG" -> "Search Library" (registry + fallback); chip "RAG: off/on" -> "Library search: off/on"; settings-modal header/button/status copy, live-work card run button, blocked-send sentence ("Library search has no available evidence"), empty-query message, and "Resolve Library search setup" action label all match it.
- TX-03: "Scope: empty" -> "Scope: no sources" (chip + Inspector row); tooltips in words ("Only searching: conversation scope (A items) and workspace scope (B items) — N in both.", singular-aware); new scope_cause_phrase()/scope_empty_notice() map cause tokens (no-workspace-overlap/deleted-items/workspace-scope-unavailable/scope_empty/unknown) to plain phrases, applied at the chip tooltip, row tooltip, chat_rag_events notification, and library_rag_service recovery card (status_label "Scope empty" -> "Scope: no sources"). Raw cause tokens never render user-visibly anywhere.
- TX-04: "Tools: not loaded" -> "Tools: —" (_tools_ready_text); the chip HIDES at a zero count (compose + sync_state, same posture as the unscoped scope chip / None cost chip). Deviation from plan step 4: the Inspector Tools row is KEPT with the dash — omitting it removed the whole "Tools" group heading the inspector layout contract pins.
- TX-05: /prefill -> "Prepare the start of the assistant's reply"; registered commands missing from the description dict fall back to "Custom command"; skill rows with empty snapshot descriptions fall back to "Run this skill" — the popup never renders an empty description.
- TX-07: "Save Chatbook" -> "Save as Chatbook" (inspector action + composer ☰ menu; noun kept because the guide teaches Chatbooks); "Sources: N staged" -> "Sources: N"; mic transcribing label "STT…" -> "Text…" (6-cell button budget; tooltip and voice chip already say "Transcribing…"). Deliberately left: the collapsed-Inspector rail badge "3 staged" (compact badge grammar, separate surface), CN-01 Mic/Dictate naming unification (TASK-2154.13), and the /prefill confirmation rows' "armed" vocabulary (beyond TX-05's popup evidence).
- Send-disabled reason (2154.7 follow-up): build_console_disabled_reason checks "api key"/"endpoint" BEFORE "model" — the real blocker copy points at "Settings > Providers & Models", whose screen name contains "model" and matched first, so missing-key and missing-endpoint states were mis-named "choose a model".

Files changed:
- App: Chat/console_display_state.py, Chat/rag_scope.py, Chat/console_command_suggestions.py, Event_Handlers/Chat_Events/chat_rag_events.py, Library/library_rag_service.py, UI/Screens/chat_screen.py (6 copy hunks), Widgets/Console/{console_session_surface, console_status_chips, console_retrieval_scope_row, console_rag_settings_modal, console_composer_menu_modal, console_composer_bar, console_workbench_state, console_control_bar}.py, css/components/_agentic_terminal.tcss (comment only; the generated tldw_cli_modular.tcss edit was reverted — it is build_css.py output).
- Tests: pinned copy updated in 14 files; new coverage — command-description fallbacks + plain-language pins (Tests/Chat/test_console_command_suggestions.py), cause mapping + notice (Tests/Chat/test_rag_scope.py), tools-chip hide/show (Tests/UI/test_console_status_chips.py), send-reason missing-key/endpoint regressions using the verbatim production strings (Tests/UI/test_console_workbench_contract.py), raw-token-never-renders assertions (scope row/chip + Tests/Library/test_library_rag_scope.py).
- Docs: Docs/User_Guide/console.md + console/{context-and-rag, attachments-images-voice, agent-runs-and-tools, sessions-tabs-workspaces}.md; new entry in backlog/docs/lessons-testing-evidence.md (substring classifiers over user copy must be tested with verbatim production strings).

Verification: targeted suites green (display_state 30, rag_scope 15, command_suggestions 12, scope_row 33, status_chips, rag_settings_modal, run_inspector, library_rag_scope, shell_chip_actions); broad batches 550 + 355 passed; prescribed filter pytest Tests/UI -k "chip or command or transcript or scope or rag": 1002 passed, 1 pre-existing sibling failure; full test_console_internals_decomposition.py re-run: 127 passed. UAT p1_ready/f4_slash_popup/p6_send_failure captures show "Conversation | Chat 1", "Search Library", "Library search: off", "Sources: 0", no Tools chip, "/prefill Prepare the start of the assistant's reply". py_compile clean over all changed files (no ruff/flake8/black in the venv).

Known pre-existing failures in the shared dirty tree (not from this task, verified against the uncommitted sibling diffs): test_workbench_visual_snapshots::test_console_workbench_standard_width_inspector_snapshot and test_product_maturity_gate1...::test_console_core_loop_exposes_agentic_shell_regions fail on "Choose model" assertions — the TASK-2154.7 chat_screen changes render "Choose provider" for the Unknown-readiness state; test_console_native_transcript::test_console_tab_reaches_major_console_screen_regions fails because TASK-2154.11 made Tab pane-local (F6 moves between panes); test_console_dictionary_send_integration and test_console_generation_actions were flagged at task start.

ADR check: none required — copy-only changes; no storage/schema, sync/conflict, data-ownership, boundary, security, or contract decisions. The two new rag_scope helpers are display mapping, not a data contract (noted in the plan above).
<!-- SECTION:NOTES:END -->
