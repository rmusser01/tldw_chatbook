---
id: TASK-3070.6
title: Extract Console skill controller
status: In Progress
assignee:
  - '@codex'
created_date: '2026-08-13 17:09'
updated_date: '2026-08-21 07:47'
labels:
  - console
  - skills
dependencies:
  - TASK-3070.5
references:
  - Docs/superpowers/specs/2026-08-13-console-decomposition-wave6-design.md
parent_task_id: TASK-3070
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Move live skill discovery, trust/block policy, refusal, and pending-decision state out of ChatScreen while deleting the unreachable fallback/picker chain.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Live skill move inventory is controller-owned and command/Textual entry points are bounded delegates
- [x] #2 The unregistered fallback dispatch, dead run/search/picker methods, unused picker widget, CSS/cross-references, imports, obsolete tests, and surviving resolver/style-picker documentation references are removed or rewritten rather than extracted
- [x] #3 Live `/skills`, `$name`, trust, refusal, install, and script behavior is unchanged
- [x] #4 Isolated controller tests run without mounting Textual
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Follow [the implementation plan](../../Docs/superpowers/plans/2026-08-20-task-3070-6-console-skill-controller.md).

ADR required: no
ADR path: N/A
Reason: this is the behavior-preserving implementation of the skill ownership and dead-surface removal already approved in the Wave 6 design; it introduces no new storage, trust, security, dependency, or service contract.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implemented a DOM-free ConsoleSkillController for the exact nine-method live skill policy and routed the three framework-bound ChatScreen entry points through bounded delegates. Moved the candidate cache behind a read/write controller descriptor, added explicit late-bound wiring, and preserved fresh context, trust filtering/sorting, refusal copy, /skills/$name behavior, and pending install/script field isolation.

Deleted the unreachable KIND_FALLBACK screen dispatch, four dead run/search/picker methods, ConsoleSkillPickerModal and its dedicated tests/CSS, then regenerated the modular CSS bundle and rewrote surviving runtime/test documentation. No replacement picker, dependency, storage/config, trust-policy, or DOM surface was added. ADR required: no; this implements the approved Wave 6 boundary.

Evidence: the final related-only matrix is 316 passed with no failures; the CSS reproduction guard is 3/3; Ruff lint is green for every task-owned hunk, with one base-proven unrelated F401 retained in `console_chat_controller.py`; 10 changed files pass Ruff format while the other 7 retain only formatter drift reproduced at the pre-task base; isolated py_compile produced 8 pyc and removed its validated temporary root; and the metadata-only diagnostic architecture node is green. The repository-wide diagnostic checker remains red for inherited, untouched `retrieval.py` and `workspace.py` owner drift; TASK-3070.6 records only its exact `chat_screen.py` -> `skill.py` owner move, with the seven-sink topology unchanged. Mutations proved cached-context reuse, trust-filter removal, wrong pending field, each missing delegate, and a dead picker token are detected and were restored. ChatScreen changed 20,587/668 -> 20,195/655 (392 lines and 13 direct methods removed). Per user instruction, no full suite was run.
<!-- SECTION:NOTES:END -->
