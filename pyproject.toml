
[build-system]
requires = ["setuptools>=77.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tldw_chatbook"
version = "0.1.8.0"
authors = [
  { name="Robert Musser", email="contact@rmusser.net" },
]
description = "A Textual TUI for chatting with LLMs, and interacting with the tldw server."
readme = "README.md"
requires-python = ">=3.11"
license = "AGPL-3.0-or-later"
license-files = ["LICENSE"]
keywords = ["tui", "cli", "llm", "textual", "ai", "chat", "rag", "embeddings", "terminal"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Science/Research",
    "Topic :: Utilities",
    "Topic :: Terminals",
    "Topic :: Communications :: Chat",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Environment :: Console",
    "Operating System :: OS Independent",
    "Natural Language :: English",
    "Typing :: Typed",
]

# --- Dependencies ---
dependencies = [
    "chardet",
    "httpx",
    "loguru",
    "textual==8.2.8",  # exact pin: UI framework, bump deliberately (TASK-1353)
    "textual-diff-view==0.1.5",  # exact pin: diff widget, bump deliberately (TASK-1351)
    "requests",
    "rich",
    "wcwidth>=0.2.14,<1",
    "rich-pixels>=3.0.0",
    # TASK-18606: was pinned to ==11.2.1 for ADR-054 renderer identity. The
    # renderer now owns its own determinism (fixed-cell font pinned and metric-
    # verified, identity hashed from pixels not PNG bytes), so the identity no
    # longer rides on this version. Freezing an image PARSER that this app
    # points at untrusted input was too high a price for reproducibility that
    # belongs in the renderer. Floor is 10.4, which introduced
    # `ImageFont.load_default_imagefont()` (10.1 is what changed
    # `load_default()` to the proportional TrueType face -- the very break
    # this renderer pins its way out of -- but the explicit legacy-font
    # accessor the renderer needs did not exist until 10.4).
    "pillow>=10.4",
    "PyYAML",
    "pydantic>=2.4,<3",
    "portalocker==3.2.0",
    "psutil",
    "toml",
    "tomli; python_version < '3.11'",
    "pyperclip",
    "emoji",
    "jinja2",
    "pycryptodomex",
    "keyring",
    "markdownify",
    "aiofiles",
    "croniter>=1.4.0",
    "textual-image",
    "anytree",
    "tiktoken",      # engine tokens strategy (Q2: hard-required, shim-enforced)
    "defusedxml",    # engine xml security parsing (Q9: core)
]

# --- Optional Dependencies ---
[project.optional-dependencies]
coding_map = [
    "grep_ast",
    "pygments",
    "tqdm",

]
chunker = [
    "langdetect",
    "nltk",
    "scikit-learn",
    "fugashi",
    "transformers",
    "tqdm",
    "jieba",
]
embeddings_rag = [
    "torch",
    "numpy",
    "pydantic",
    "transformers",
    "chromadb>=0.4.0",
    "sentence-transformers",
    "opentelemetry-api",
    "opentelemetry-sdk",
]
websearch = [
    "lxml",
    "beautifulsoup4",
    "pandas",
    "playwright",
    #"playwright_stealth",
    "trafilatura",
    "langdetect",
    "nltk",
    "scikit-learn",
    "aiohttp",
    "defusedxml",
    "tqdm",
]
local_vllm = ["vllm"]
local_mlx = ["mlx-lm"]
# Individual transcription providers
transcription_faster_whisper = [
    "faster-whisper",  # CPU/CUDA optimized Whisper
]
transcription_parakeet_onnx = [
    "onnx-asr[cpu]==0.12.0",  # Cross-platform Parakeet ONNX runtime
]
transcription_transcribe_cpp = [
    "transcribe-cpp==0.1.3",  # Optional direct-local GGUF runtime
]
transcription_lightning_whisper = [
    "lightning-whisper-mlx; sys_platform == 'darwin'",  # Fast Whisper for Apple Silicon
]
transcription_parakeet = [
    "onnx-asr[cpu]==0.12.0",  # Cross-platform Parakeet ONNX runtime
]
# Legacy alias for backward compatibility
mlx_whisper = [
    "lightning-whisper-mlx; sys_platform == 'darwin'",  # Fast Whisper for Apple Silicon
    "parakeet-mlx; sys_platform == 'darwin'",  # Real-time ASR for Apple Silicon
]
local_transformers = ["transformers"]
mcp = [
    "mcp-unified==0.2.1",
]
image_generation = [
    # SwarmUI API client (Media_Creation/swarmui_client.py). aiohttp previously
    # shipped only in [websearch] -- for the article scraper -- so wanting image
    # generation meant installing the web-search stack (playwright, trafilatura,
    # pandas, nltk, scikit-learn, ...) to obtain one HTTP client (task-1262).
    "aiohttp",
]
video_playback = [
    # In-app video playback (task-3401.9/.10): PyAV decodes frames for the
    # in-transcript silent preview; textual-canvas backs the full player
    # screen's scrubber. Both are optional -- surfaces degrade to guidance
    # (or the OS player) when absent, and video generation never needs them.
    "av",
    "textual-canvas",
]
svg = [
    "cairosvg",  # SVG rasterization for image attachments; needs the cairo C library (e.g. `brew install cairo`)
]
frontmatter = [
    # YAML front matter consumed (not rendered as noise) in note/README
    # markdown previews (TASK-1993); absence falls back to default parsing.
    "mdit-py-plugins",
]
pdf = [
    "pymupdf",
    "pymupdf4llm",
    "docling",
]
ebook = [
    "ebooklib",
    "beautifulsoup4",
    "lxml",
    "html2text",
    "markdownify",
    "defusedxml",
]
audio = [
    "soundfile",
    "scipy",
    "yt-dlp",
    "numpy",
    "pydub",
    "audioop-lts; python_version >= '3.13'",  # stdlib audioop removed in 3.13; pydub needs it
    # macOS prefers parakeet-mlx at runtime (see config.py's darwin branch), so
    # ship it there rather than leaving the preferred engine uninstalled: the
    # preference could never engage on a normal macOS install, and audio ingest
    # failed with "faster-whisper is not installed" instead (task-839).
    "parakeet-mlx; sys_platform == 'darwin'",
    "onnx-asr[cpu]==0.12.0",  # Cross-platform Parakeet ONNX batch runtime
    # Fallback everywhere, and the default off darwin.
    "faster-whisper",
    # For the other Apple Silicon engine:
    # pip install -e ".[audio,transcription_lightning_whisper]"
]
video = [
    "soundfile",
    "scipy",
    "yt-dlp",
    "numpy",
    # macOS prefers parakeet-mlx at runtime (see config.py's darwin branch), so
    # ship it there rather than leaving the preferred engine uninstalled: the
    # preference could never engage on a normal macOS install, and audio ingest
    # failed with "faster-whisper is not installed" instead (task-839).
    "parakeet-mlx; sys_platform == 'darwin'",
    "onnx-asr[cpu]==0.12.0",  # Cross-platform Parakeet ONNX batch runtime
    # Fallback everywhere, and the default off darwin.
    "faster-whisper",
    # For the other Apple Silicon engine:
    # pip install -e ".[video,transcription_lightning_whisper]"
]
media_processing = [
    "soundfile",
    "scipy",
    "yt-dlp",
    "numpy",
    "faster-whisper",  # Default transcription provider
    "onnx-asr[cpu]==0.12.0",  # Cross-platform Parakeet ONNX batch runtime
    # Note: For Apple Silicon optimized transcription, also install:
    # pip install -e ".[media_processing,transcription_lightning_whisper]" or
    # pip install -e ".[media_processing,transcription_parakeet]"
]
chatterbox = [
    "chatterbox-tts",
    "torchaudio",
    "torch",
    "faster-whisper",  # For validation of generated audio
]
mindmap = [
    # anytree is already in base dependencies
    # No additional dependencies needed for basic functionality
]
local_tts = [
    "kokoro-onnx",
    "scipy",
    "numpy",
    "nltk",
    "pyaudio",
    "pydub",
    "audioop-lts; python_version >= '3.13'",  # stdlib audioop removed in 3.13; pydub needs it
    "transformers",  # For tokenization
    "torch",  # For PyTorch-based models
    "onnxruntime",  # For ONNX models
    "av",  # pyav for audio format conversion
]
higgs_tts = [
    # IMPORTANT: Higgs Audio requires manual installation BEFORE using this extra!
    # The boson-multimodal package cannot be installed via pip and must be installed from GitHub:
    #
    # Step 1: Clone and install Higgs Audio (REQUIRED):
    #   git clone https://github.com/boson-ai/higgs-audio.git
    #   cd higgs-audio
    #   pip install -r requirements.txt
    #   pip install -e .
    #   cd ..
    #
    # Step 2: Install tldw_chatbook with Higgs support:
    #   pip install -e ".[higgs_tts]"
    #
    # The following are supporting dependencies for Higgs Audio:
    "torch",  # PyTorch for model inference
    "torchaudio",  # For audio processing and voice cloning
    "numpy",  # For audio data manipulation
    "scipy",  # For audio signal processing
    "librosa",  # For advanced audio feature extraction
    "soundfile",  # For audio file I/O
    "transformers",  # For tokenization and text processing
]
nemo = [
    "nemo-toolkit[asr]>=1.20.0",  # For NVIDIA Parakeet and Canary models
    "torch",  # Required by NeMo
    "torchaudio",  # Required for audio processing
]
speech_recording = [
    "pyaudio>=0.2.14",  # Primary audio backend
    "sounddevice>=0.4.6",  # Fallback audio backend
    "webrtcvad-wheels>=2.0.14",  # Voice activity detection (drop-in `webrtcvad` fork; upstream webrtcvad 2.0.10 imports pkg_resources, which setuptools>=81 removed)
]
realtime = [
    "websockets>=14.0",  # Transport for the realtime voice engine (LLM_Calls/realtime); imported lazily by transport/session modules, never at package-import time. Floor is 14.0, NOT 12: `transport.py` calls `connect(additional_headers=...)`, which only the asyncio client accepts, and 14.0 is the release where the top-level `connect` resolves to it -- on 12/13 the legacy client takes `extra_headers` and raises TypeError at connect time
    "sounddevice>=0.4.6",  # Reply-audio output (Audio/streaming_sink.py) AND the realtime mic tap's capture backend -- nothing else a realtime-only install pulls in provides an audio device
    "numpy",  # Required by AudioRecordingService (the mic tap refuses to construct without it) and by the sink's frame handling
]
ocr_docext = [
    "docext",  # NanoNets document extraction toolkit
    "gradio_client",  # For API-based usage
    "transformers>=4.30.0",  # For direct model usage (optional)
    "torch",  # For direct model usage (optional)
    "openai",  # For OpenAI-compatible API usage
    "Pillow",  # For image handling
]
dev = [
    "pytest",
    "pytest-timeout",  # For handling hanging tests
    "pytest-xdist",  # Parallel test execution: pytest -n auto --dist loadscope
    "pytest-mock",  # Used throughout Tests/; was only in requirements-test.txt
    "pytest-cov",  # Coverage runs documented in Tests/README.md
    "textual-dev", # For Textual development tools
    "hypothesis",
    "pytest-asyncio",
    "build",  # For building distributions
    "uv>=0.8.0,<1",  # Isolated wheel/sdist test environments
    "twine",  # For uploading to PyPI
    "wheel",  # For building wheel distributions
    "prometheus_client",  # For metrics collection during development
]
debugging = [
    "prometheus_client",  # For metrics collection and monitoring
    "opentelemetry-api",  # For advanced telemetry
    "opentelemetry-sdk",  # OpenTelemetry SDK
    "opentelemetry-exporter-prometheus",  # Export metrics to Prometheus
    "opentelemetry-instrumentation-system-metrics",  # System metrics collection
]
diarization = [
    "torch",  # PyTorch for neural models
    "torchaudio",  # Audio processing utilities
    "speechbrain",  # Speaker embedding models
    "scikit-learn",  # For clustering algorithms (already in chunker deps)
    "numpy",  # Numerical operations (already in base deps)
]
subscriptions = [
    # markdown/schedule/feedparser were dropped in TASK-1221: nothing imported
    # them, and because the feature gate requires every listed package to
    # import, their absence disabled Watchlists for no reason. Feeds are parsed
    # by defusedxml/ElementTree plus bs4, never feedparser.
    "beautifulsoup4", # HTML parsing for subscription monitors (hard import)
    "cryptography", # Security.py -- without it credentials are stored plaintext
    "defusedxml",  # Safe XML parsing; falls back to stdlib xml.etree if absent
    "tqdm",
]
# Note: cryptography and defusedxml are shipped in this extra but are NOT part of
# the availability gate in Utils/optional_deps.py, because both have working
# fallbacks. Installing them is still the right default -- the fallbacks are
# plaintext credentials and a less safe XML parser respectively.
web = [
    "textual-serve",  # Web server for running Textual apps in browser
]

# Combined installation option for all optional features
all-tools = [
    # === Core Tools ===
    # Coding and mapping
    "grep_ast",
    "pygments",
    "tqdm",
    
    # Text processing and chunking
    "langdetect",
    "nltk",
    "scikit-learn",
    "fugashi",
    "jieba",
    
    # === AI/ML Frameworks ===
    # Embeddings and RAG
    "chromadb>=0.4.0",
    "torch",
    "numpy",
    "transformers",
    "sentence-transformers",
    "opentelemetry-api",
    "opentelemetry-sdk",
    
    # Local inference
    "vllm",  # VLLM inference
    "mlx-lm",  # MLX inference
    
    # === Web and Search ===
    "lxml",
    "beautifulsoup4",
    "pandas",
    "playwright",
    "trafilatura",
    # Serves both [websearch] (article scraper) and [image_generation]
    # (SwarmUI client); listed once here since all-tools enumerates packages.
    "aiohttp",
    "defusedxml",
    
    # === Transcription and Audio ===
    "faster-whisper",  # CPU/CUDA optimized Whisper
    "onnx-asr[cpu]==0.12.0",  # Cross-platform Parakeet ONNX runtime
    "lightning-whisper-mlx; sys_platform == 'darwin'",  # Fast Whisper for Apple Silicon
    "parakeet-mlx; sys_platform == 'darwin'",  # Real-time ASR for Apple Silicon
    
    # Audio processing
    "soundfile",
    "scipy",
    "yt-dlp",
    "pydub",
    "audioop-lts; python_version >= '3.13'",  # stdlib audioop removed in 3.13; pydub needs it
    "pyaudio>=0.2.14",
    "sounddevice>=0.4.6",
    "webrtcvad-wheels>=2.0.14",  # drop-in `webrtcvad` fork; see speech_recording extra note
    "librosa",
    
    # === TTS (Text-to-Speech) ===
    "chatterbox-tts",
    "torchaudio",
    "kokoro-onnx",
    "onnxruntime",
    "av",
    # Note: higgs_tts requires manual installation of boson-multimodal from GitHub
    
    # === Speech and Diarization ===
    "speechbrain",
    "nemo-toolkit[asr]>=1.20.0",
    
    # === Document Processing ===
    # PDF
    "pymupdf",
    "pymupdf4llm",
    "docling",
    
    # Ebooks
    "ebooklib",
    "html2text",
    "markdownify",
    
    # OCR and Document Extraction
    "docext",
    "gradio_client",
    "openai",
    "Pillow",
    
    # === MCP and Integrations ===
    "mcp-unified==0.2.1",
    
    # === Subscriptions and Scheduling ===
    "markdown",
    "schedule",
    "feedparser",
    "cryptography",
    
    # === Development and Debugging ===
    "pytest",
    "pytest-timeout",
    "textual-dev",
    "hypothesis",
    "pytest-asyncio",
    "build",
    "twine",
    "wheel",
    "prometheus_client",
    "opentelemetry-exporter-prometheus",
    "opentelemetry-instrumentation-system-metrics",
    
    # === Web Server ===
    "textual-serve",
]
# IMPORTANT: For Higgs Audio TTS support, you must manually install boson-multimodal:
#   git clone https://github.com/boson-ai/higgs-audio.git
#   cd higgs-audio && pip install -r requirements.txt && pip install -e .


[project.urls]
"Homepage" = "https://github.com/rmusser01/tldw_chatbook"
"Bug Tracker" = "https://github.com/rmusser01/tldw_chatbook/issues"
"Documentation" = "https://github.com/rmusser01/tldw_chatbook#readme"
"Source Code" = "https://github.com/rmusser01/tldw_chatbook"
"Changelog" = "https://github.com/rmusser01/tldw_chatbook/blob/main/CHANGELOG.md"

# This creates a command-line script called 'tldw-chatbook'
[project.scripts]
# Loads the full app only when the command is invoked.
tldw-cli = "tldw_chatbook.cli:main_cli_runner"
# Direct web server entry point
tldw-serve = "tldw_chatbook.Web_Server.serve:main"

[tool.setuptools]
include-package-data = false

[tool.setuptools.packages.find]
where = ["."]  # Look for packages in the current directory (where pyproject.toml is)
# Include the 'tldw_chatbook' package and its submodules
include = ["tldw_chatbook*"]
exclude = [
    "Tests*",
    ".venv*",
    "tldw_chatbook.Chunking.templates*",
]

[tool.setuptools.package-data]
# Bundle only the reviewed Samira character and Visual Identity pack inventory.
"tldw_chatbook" = [
    "assets/characters/samira/*.json",
    "assets/characters/samira/*.png",
    "assets/characters/samira/*.md",
    "assets/characters/samira/expressions/*.webp",
]
# Include all .tcss files in css directory and subdirectories
"tldw_chatbook.css" = ["*.tcss", "**/*.tcss"]
# Include theme files
"tldw_chatbook.css.Themes" = ["*.tcss"]
# Include JSON configuration, guides, and the RAG pipeline defaults
"tldw_chatbook.Config_Files" = ["*.json", "*.md", "rag_pipelines.toml"]
# Include runtime chunking templates and evaluator defaults
"tldw_chatbook.Chunking" = ["templates/*.json"]
"tldw_chatbook.Evals" = ["config/*.yaml"]
"tldw_chatbook.Image_Generation" = ["workflows/*.json"]
"tldw_chatbook.Video_Generation" = ["workflows/*.json"]
"tldw_chatbook.TTS" = ["audio_cpp_artifact_manifest.json"]
"tldw_chatbook.DB" = [
    "migrations/chachanotes_v26_to_v27_citation_provenance.sql",
    "migrations/chachanotes_v27_to_v28_character_authority.sql",
    "migrations/chachanotes_v32_to_v33_console_context_memory.sql",
    "migrations/chachanotes_v33_to_v34_visual_compaction_policy.sql",
    "migrations/chachanotes_v34_to_v35_conversation_dictionary_attachments.sql",
    "migrations/chachanotes_v35_to_v36_note_folders.sql",
    "migrations/chachanotes_v36_to_v37_provider_continuation.sql",
    "migrations/chachanotes_v37_to_v38_message_trajectory_metadata.sql",
    "migrations/chachanotes_v38_to_v39_visual_identity.sql",
    "migrations/chachanotes_v39_to_v40_transcript_annotations.sql",
]
# Preserve the licenses for vendored packages
"tldw_chatbook.Third_Party.aider" = ["LICENSE.txt"]
"tldw_chatbook.Third_Party.textual_fspicker" = ["LICENSE"]
"tldw_chatbook.Chunking.engine" = ["LICENSE"]
# Include CSS files in subdirectories
"tldw_chatbook.css.core" = ["*.tcss"]
"tldw_chatbook.css.features" = ["*.tcss"]
"tldw_chatbook.css.layout" = ["*.tcss"]
# If you have other assets inside your tldw_chatbook package (e.g., tldw_chatbook/assets/), add them here:
# "tldw_chatbook.assets" = ["*"]

[tool.pytest.ini_options]
# THE single pytest config (task-1457). Tests/UI/pytest.ini used to shadow this
# whenever pytest was invoked as `pytest Tests/UI` (different rootdir, its own
# asyncio mode, NO timeout, root conftest not loaded); every invocation now
# resolves here.
testpaths = ["Tests"]
# Exclude server tests that don't belong in the TUI.
# --durations: always report the slowest tests (>=1s) so wall-clock regressions are visible in every run.
# --strict-markers: an unregistered marker is a collection error, not a silent no-op.
addopts = "--ignore=STests --import-mode=importlib --durations=25 --durations-min=1.0 --strict-markers -ra"
# Auto mode suite-wide: async tests execute without per-function marks. Under
# the old split config, unmarked async tests silently DID NOT RUN in full runs
# from the repo root (pytest-asyncio strict mode skips unmarked coroutines).
asyncio_mode = "auto"
# Default timeout for all tests (in seconds)
timeout = 300
# Timeout method: thread (default) or signal
timeout_method = "thread"
markers = [
    "unit: marks tests as unit tests",
    "integration: marks tests as integration tests",
    "ui: marks tests as UI/Textual tests",
    "asyncio: marks tests as async",
    "timeout: marks tests with custom timeout values",
    "optional: marks tests that require optional dependencies (skipped unless --run-optional)",
    "performance: marks tests as performance tests",
    "property: marks tests as property-based tests",
    "slow: marks tests that take a long time to run (skipped unless --run-slow)",
    "smoke: fast sanity checks of core imports and wiring",
    "benchmark: performance-measurement tests (labeling only; pytest-benchmark is not installed)",
    "snapshot: visual regression tests using snapshots",
    "notes: Notes feature tests",
    "requires_display: tests that require a display (may fail in CI)",
    "requires_cleanup: tests that need a gc pass immediately after running",
    "real_audio_device: opts out of the autouse _no_real_audio_device guard (Tests/conftest.py) to exercise a real sounddevice OutputStream",
    "allow_network: opts out of the autouse _no_network_io guard (Tests/conftest.py) to open a real socket; justify it in the test's docstring",
    "loopback_network: permits only numeric IPv4 127.0.0.0/8 and IPv6 ::1 sockets under the autouse _no_network_io guard (Tests/conftest.py)",
    "local_server_probe: opts out of the autouse _no_local_server_probes guard (Tests/conftest.py) to exercise the real /v1/models probe against an injected httpx.MockTransport client",
    "owned_http_client: opts out of the autouse _console_gateway_http_client_is_offline guard (Tests/conftest.py) for tests that are ABOUT the gateway's real owned httpx client",
    "live: marks tests that call real paid external APIs (require key files + TLDW_LIVE_SEARCH_TESTS=1 + --run-live)",
]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
warn_return_any = false
warn_unused_configs = true
follow_imports = "skip"
disable_error_code = ["annotation-unchecked"]
exclude = [
    "tldw_chatbook/Local_Ingestion/API_Endpoint_Sample.py",
]
