# SDD ledger — plan: Qodo #1030 sweep + backlog tasks 1050-1052 (briefs = backlog task files + Qodo finding bodies)
Branch fix/approval-accounting-1050-1052 off origin/dev. Order: 1050 (round-keyed badge accounting + payload retention guards; Qodo findings 2+3 = this task; rider: rename _RunMarkerBearer→RunMarkerBearer + _FakeApp→FakeApp for PascalCase compliance, Qodo finding 1) → 1052 (bridges observe global shutdown for all session ids; flip the pinned evidence test) → 1051 (from_dict symmetry). Reviews each; final pass; PR; merge on user authorization.
Task 1050: implemented 345edb520 (option-b design: add/discard_pending_round + deprecated shim — caller audit verified sound, no double-count, arm-before-park guard). Review (sonnet) = Needs fixes. CRITICAL (reviewer REPRODUCED live): payload-pop guard's `or stored_is_this_round` clause evicts the slot when the NEWEST round resolves first (natural live ordering) while an older sibling is armed → badge ◆ + empty map + remount None. Fix = drop the OR-clause (pop only on not still_armed_same_session, matching the mounted-card guard); reverse-ordering tests + data-level no-double-count assert. Defect A + shim + rider all APPROVED. Fix round 1 dispatched.
Task 1050: fix round 1 complete (bbdaa1f20) — OR-clause dropped in all 3 bridges (order-independent pop), reverse-ordering tests (MCP+install; script via verified byte-identical-code argument), data-level singleton asserts. Re-review (same reviewer, repro rerun): CLOSED; residual (stale-slot content after newest-first, never eviction) verified safe (dead-round_id resolve = genuine no-op, older round stays decidable by own id) — strictly better, correctly scoped. Task 1050 COMPLETE — commits 345edb520 + bbdaa1f20.
Task 1052 dispatched next.
Task 1052: implemented 6e8a38ffb. Review (sonnet) = Approved. Descope of close_session AC VERIFIED TRUE by independent trace (registration atomically pre-await on every path incl. direct/retry; close_session same-event-loop only — no interleaving window). IMPORTANT doc correction dispatched: docstring's "process teardown" framing false — shutdown() also fires on ordinary screen navigation via on_unmount (behavior safe: fresh controller per Console mount, torn-down instances never revisited); fix the WHY. Minor: AC#2 strikethrough visibility. Doc round running.
