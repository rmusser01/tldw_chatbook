# Startup-perf follow-up ledger (task 163)
Plan: Docs/superpowers/plans/2026-07-11-followups-startup-perf.md
Branch: claude/followups-startup-perf off dev b8344550
Baseline: import tldw_chatbook.app ~4.8s/6518 modules; torch 1026 + transformers 256 + scipy 526 + sklearn 190 + pandas 293 + nltk 240 present.
Root causes (meta-path traced): (1) app.py:182 RAG_Admin -> Chroma_Lib -> Embeddings_Lib:53-55 module-scope get_safe_import(numpy/torch/transformers) [REAL __import__]; (2) Article_Extractor_Lib:99 eager Summarization_General_Lib -> Chunk_Lib -> nltk stack.

## Outcome (see .superpowers/sdd/startup-perf-report.md for full detail)
Fixed (in scope, Embeddings/Web_Scraping): Embeddings_Lib.py (torch/transformers/numpy
lazy via _ensure_*()), Article_Extractor_Lib.py (analyze deferral + pandas find_spec
deferral), Chroma_Lib.py (chunk_for_embedding + analyze deferral -- 2nd eager edge
found), WebSearch_APIs.py (analyze deferral -- 3rd eager edge found, separate boot
path via Tools/web_search_tool.py).
Result: torch/transformers fully eliminated from `import tldw_chatbook.app`
(6519->4659 modules, ~5.65s->~4.11s, fresh-dir apples-to-apples).
Commit 2 (scope extension AUTHORIZED 2026-07-11): Chunking/Chunk_Lib.py -- deferred
`import nltk` behind _ensure_nltk() + find_spec-based NLTK_AVAILABLE; removed the
module-scope ensure_nltk_data() call (which did a punkt NETWORK DOWNLOAD at import)
and made it idempotent + lazy; guarded _adaptive_chunk_size_nltk + _semantic_chunking
use sites. nltk-transitive scipy/sklearn/pandas removed automatically.
Result: ALL 8 HEAVY_MODULES absent at boot. 6519 -> 3291 modules (-49.5%), ~5.65s -> ~1.48s.
AC #1 SATISFIED. Full-set perf test promoted from xfail to hard assertion (RED-verified).
Feature regression: 404 passed / 20 skipped / 0 failed across Chunking/RAG/RAG_Admin/
RAG_Search/Web_Scraping/startup-polish/optional_deps. numpy stays (chromadb/pymupdf, allowed).
Pre-existing punkt vs punkt_tab nltk-5.x bug in semantic chunking documented as follow-up
(reproduces on original code, out of scope).

## Task 163 — COMPLETE (2026-07-11)
- Commit b5970b19: torch/transformers/numpy + summarization stack deferred (Embeddings_Lib, Chroma_Lib, Article_Extractor_Lib, WebSearch_APIs)
- Commit fd79452a: nltk deferred in Chunk_Lib → scipy+sklearn+pandas (all nltk-transitive) also gone
- Whole-branch review (opus): APPROVE, 2 Minor non-blocking thread-window notes
- Gate: perf test + Chunking + RAG + RAG_Admin + RAG_Search + embeddings-datatable = 319 passed / 20 skipped / 0 failed; app import OK
- Boot: 6,519 → 3,291 modules (−49.5%), ~5.65s → ~1.48s; torch/transformers/nltk/scipy/sklearn/pandas/docling/torchvision all absent
- Follow-up logged: pre-existing nltk 5.x punkt/punkt_tab semantic-chunk resource mismatch (reproduces on original code)

# ===== SDD ledger — Library Prompts + Console injection =====
Plan: Docs/superpowers/plans/2026-07-12-library-prompts-console-injection.md
Branch: claude/prompts-library-spec off origin/dev 6a9c624a (plan+spec at 56db9d9e)
Gates: Task 8 and Task 15 STOP for user approval with live captures.
Task 0: complete (commit a696a8a8, review Approved; decision = migration v17->v18 system_prompt TEXT NULL; NOTE: no update_conversation path exists — Task 13 adds it)
NOTE: backlog IDs 196-201 on this branch may collide with another session's 196-206 (PR #610) — reconcile/renumber at the Task 8 gate rebase.
Task 1: complete (commits ebae07f8+d4ec4660, review Approved; Minors: scope-adapter int-cast style, degrade-path test gap — for final review). ⚠ resolved: registry has no prompts.count action; count reuses prompts.list policy id by design (notes parity).
Task 2: complete (pending commit — pure state builders `tldw_chatbook/Library/library_prompts_state.py`: `PromptListRow`/`PromptsListState`/`build_prompts_list_state`, `PromptEditorState`/`build_prompt_editor_state`, `classify_prompt_save_error`; 14 tests in `Tests/Library/test_library_prompts_state.py`, RED→GREEN, no regressions in Tests/Library (462 passed)). Note: `created` field on `PromptEditorState` is always `""` for real rows — Prompts table has no `created_at` column; field kept per brief for shape parity.
Task 2: complete (commit 8cdc6be8, review Approved; Minors: unknown-sort untested, keywords-key-absent untested, speculative Mapping branch — final review list). ⚠ resolved: conversation_browser_state has no module-scope textual import. Data facts: prompts have NO created_at; list_prompts rows lack keywords (Task 3 must enrich via fetch).
Task 3: complete (pending commit — list canvas `tldw_chatbook/Widgets/Library/library_prompts_canvas.py` (`LibraryPromptsListCanvas`, structural copy of the notes canvas list view) + screen wiring in `library_screen.py`: `LIBRARY_SOURCE_PAGE_SIZES["prompts"]`, `_prompts_page_records_or_empty` (new `list_prompts` gather call, remaps the scope service's normalized composite-id envelope back to raw-shaped records for `build_prompts_list_state`), `_build_library_prompts_state`, canvas_kind "prompts" branch, `handle_library_prompts_sort`/`handle_library_prompts_filter`/`handle_library_prompt_row`, save_state/restore_state persistence for prompts sort/filter/selection. 13 new tests in `Tests/UI/test_library_prompts_canvas.py` (RED→GREEN); updated 1 existing test in `test_library_shell.py` whose old assertion (placeholder-empty canvas) no longer holds now that the row renders the real canvas. Full `test_library_shell.py` re-run: 251 passed (baseline held). ⚠ resolved: enrichment decision — did NOT bulk-enrich prompts rows with keywords; notes' own bulk `list_notes` fetch has never carried keywords either (only its single-note editor enriches via `_fetch_library_note_keywords`), so the "notes precedent" does not actually support bulk-list enrichment; documented as a concern for a future task (would need a new batched-join DB seam or N per-row `fetch_keywords_for_prompt` calls, out of this task's file scope).
Task 3: complete (commits 441098da+b8eba753; review Needs-fixes->Approved; fix wave: prompts CSS parity + dual-target pins. Minors: pinned-subset parity, report miscount — final review list)
Task 4: complete (pending commit — editor canvas `_compose_editor` in `library_prompts_canvas.py` (6 fields, 5 action ids, Back) + full save/conflict/delete flow in `library_screen.py`. Full report: `.superpowers/sdd/task-4-report.md`). ⚠ resolved: `Prompts.name` is globally UNIQUE regardless of soft-delete state, and `update_prompt_by_id` has no caller-supplied expected-version param (always re-derives version from a fresh in-transaction read) — so name-in-use/soft-deleted-name/stale-version are all pre-checked by the screen itself (extra `get_prompt` reads) BEFORE the real write, not left to the DB's own exception shape (which conflates all three into `ConflictError`). Also found: recomposing the editor after any state change while `_library_prompt_editor_armed` was already True re-mounted Input/TextArea and re-dirtied a just-saved prompt via Textual's spurious mount-time `Changed` event — fixed by switching all outcome-only updates (save success/name-conflict/error, delete-failed) to targeted Static patches (no recompose), mirroring `_save_library_note`'s "never recompose on save" discipline; `_refresh_local_source_snapshot()` deferred to Back/Delete (leaves the armed editor) rather than fired from inside a successful Save. 22 tests (6 brief scenarios + CSS pin + happy-path save), Tests/UI/test_library_shell.py 251 passed, Tests/Library 462 passed.
Task 4: complete (commits 1a990a0b+6827a430; review Needs-fixes->Approved; fix: write-time ConflictError routes to banner. Minors for final review: vacuous guarded status assertion, silent Overwrite-failure path, composite-id prompt_id trap in build_prompt_editor_state, empty-Keywords leave-untouched semantics)
Task 5: complete (pending commit — new pure `tldw_chatbook/Prompt_Management/prompt_markdown_export.py` (`render_prompt_markdown`), editor `Export…` wired to a `FileSave` dialog (mirrors `_export_library_note`), toolbar `Import…` inline path row (file OR folder) parses via `Prompts_Interop.parse_*_prompts_from_content` module-level functions and creates per-prompt through `PromptScopeService`/`LocalPromptService` (duplicate name = skip, never overwrite). Full report: `.superpowers/sdd/task-5-report.md`). ⚠ resolved: `import_prompts_from_files` has NO non-overwrite mode (hardcodes `overwrite=True`, bypasses the scope service) — NOT used; per-prompt parse+create through the service layer instead, mirroring `_save_library_prompt`'s duplicate-name pre-check. Found (out of this task's file scope, flagging for Task 8 gate): `parse_markdown_prompts_from_content` has two pre-existing bugs — a blank AUTHOR/SYSTEM/USER value bleeds into the next section's header text instead of parsing back as None (when anything follows it, the common case), and a multi-line SYSTEM/USER value truncates after its first line (bare `$` under MULTILINE) — pinned as characterization tests, not fixed (`Prompts_Interop.py` not in Task 5's file list). 5 round-trip tests + 14 new canvas/screen tests (widget Import-row + CSS pin + 6 screen Import scenarios + 5 screen Export scenarios); `Tests/UI/test_library_prompts_canvas.py` + `test_library_shell.py` combined: 288 passed (251 baseline held + 37).
Task 5: complete (commits 1491f520+e0a38e97; review Needs-fixes->Approved; CRITICAL fixed via authorized scope extension: parser multi-line truncation + blank-bleed regex fix, lossless round-trip proven byte-for-byte. Minors for final review: literal-###WORD###-body ambiguity docstring, leading-whitespace strip, MUST re-run test_library_shell 251 baseline)
Task 6: complete (commit 47ad93dd; review Approved first pass. Minors for final review: snippet only user_prompt/details — name/system/keyword matches show 'No snippet available', dead module-level search_prompts wrapper duplication)
Task 7: complete (pending commit — `prompts` route alias re-pointed Personas->Library (`screen_registry.py`, `shell_destinations.py`, `library_screen.py`'s `LIBRARY_NAV_MODE_TO_ROW_ID`, `route_inventory.py`); `app.py` gained `_LEGACY_ROUTE_LIBRARY_NAV_CONTEXT` since unlike "notes" the bare "prompts" alias has no dedicated re-entry action to carry nav-context; Personas `MODE_CHIP_ORDER` dropped "prompts" + placeholder copy; `CCPPromptHandler` deleted (confirmed dead beyond its own test files) with its dedicated test file. Full report: `.superpowers/sdd/task-7-report.md`). ⚠ resolved: Step-1 grep gate correctly caught the design doc's stale "confirmed-dead" claim for `Event_Handlers/prompt_ingest_events.py` — it is live production wiring (`ingest_events.py` -> `INGEST_BUTTON_HANDLERS` -> `app.py`/`event_dispatcher.py`), a legacy media-ingest flow unrelated to this feature; NOT deleted, STOP-and-report per the binding instruction. Pre-existing unrelated failure confirmed via `git stash` isolation: `test_unified_shell_phase5_recovery_taxonomy.py::test_service_backed_policy_destinations_use_async_workers_without_asyncio_run` (library_screen.py `thread=True` worker from commit 3efb31b3, not this task).
Task 7: complete (commit 59700b7c; review Approved. prompt_ingest_events NOT deleted — LIVE legacy wiring, spec claim wrong; CCPPromptHandler deleted w/ its tests. Minors: _apply_mode('prompts') direct-call tests, CCPPromptEditorWidget dead pocket noted for CCP sweep. Phase5 taxonomy failure = UPSTREAM 3efb31b3 thread=True export worker, fails at base)
Task 8 (Phase-1 gate): captures done + LIVE POLICY DEFECT fixed (commit 3a2bf5b5: prompts.detail.* capability was unregistered -> PolicyDeniedError -> editor never opened; all UI tests missed it via permissive-enforcer fixtures). Gate suites 1062 passed/1 skipped. QA evidence Docs/superpowers/qa/library-prompts-2026-07. AWAITING USER APPROVAL before Phase 2.
Task 8b (UX polish wave): dispatched — D1 New-prompt create path, D2 honest filter+secondary, D3 conflict Open-existing, D4 import Browse, U1 details-secondary, U2 author-demote, U3 duplicate, U4 Details->Description. User-approved fold-in before Phase 2.
Task 8b (UX polish): complete (commits 3f4b8bb3/d5f0f180/ae7f9092/21179d75; review Needs-fixes->Approved; fix: create-conflict nav-trap -> recoverable + Open-existing captured-name. D2 branch: no batched seam, filter+secondary use details. Minor for final review: no discard-without-save path (pre-existing Task-4 veto model)). Gate suites green. RE-CAPTURING polished screens.
Task 8c (2nd polish): complete (commits cced5fe0/b873ae5e/32e08546; review Needs-fixes->Approved; D5 dead Export removed, U6 unsaved marker (in-place meta update, no keystroke recompose), U7 field hints, U8 Copy text/Duplicate prompt. CRITICAL fixed: composite-id -> existing prompts showed 'New prompt'; _resolve_editor_prompt_id prefers local_id, real-path regression test. U9/U10 backlogged (202/203)). Gate green.
Phase 2 START: rebased Phase 1 (29 commits) onto origin/dev @ 993653d8 clean, no conflicts. 3b9b8b0a
Task 9: complete (commits 0ae2ba98+6cd71fc6; review Needs-fixes->fixed; paste-token gating removed from pure module -> caller's job. Minor for final review: /system argument_hint [name] inferred, verify at Task 14)
Task 10: complete (commit d0cc91fa; RED->GREEN 7 new tests Tests/UI/test_console_command_composer.py; interception placed in _send_console_message_from_visible_action AFTER empty-draft guard + _dismiss_console_guidance, BEFORE _console_send_blocked_reason/readiness gate + submit worker; paste gate = ConsoleComposerBar.has_paste_segments() (new helper, real segment collapse_state != "literal", NOT display sniffing); arm field _console_unknown_send_armed, disarmed via @on(Input.Changed,"#console-command-input"); stub handlers _console_command_insert_prompt/_console_command_apply_system notify "not wired yet". Enter-key paste-unfurl in on_key still runs FIRST (activate_focused_paste_token before Button.press), so command parse only ever sees a fully-unfurled draft. Required suites green: composer+native_chat_flow 163 passed; session_settings+persistent_rails+native_transcript+workbench_contract+grammar 252 passed. Full report .superpowers/sdd/task-10-report.md)
Task 10: complete (commit d0cc91fa; review Approved first pass. Minors for final review: dispatch_map rebuilt per-call w/ hardcoded literals vs imported constants; expanded-paste never command-eligible by design)
Task 11: complete (pending commit -- new `tldw_chatbook/Widgets/Console/console_prompt_picker_modal.py` (`ConsolePromptPickerModal`, modes `insert`/`apply-system`), CSS in `_agentic_terminal.tcss` + regenerated bundle + dual-file pin test, `Tests/UI/test_console_prompt_picker.py` (7 tests, RED->GREEN). Debounce mirrors personas/console-search 0.2s `set_timer` idiom + search-token staleness guard; Up/Down move a synthetic highlighted-row index via bubbled `on_key` (Input has no arrow bindings in installed Textual); Enter reacts to bubbled `Input.Submitted` (Input consumes raw Enter itself, matching `ConsoleSessionSwitcherModal`'s proven idiom). Row ids prefer `local_id` over composite `id` per Task 6's normalization trap. Full report `.superpowers/sdd/task-11-report.md`. ⚠ resolved: initial `initial_query` search on mount is NOT debounced (only post-open edits are) -- flagged as a deliberate reading for final review. Gate suites green: new file 7/7; `test_console_session_settings`+`test_console_command_composer`+`test_console_native_chat_flow`+`test_console_rail_sections` 324 passed/0 failed; `test_library_prompts_canvas.py -k css` 4 passed (bundle regen didn't disturb Library's own pins).
Task 11: complete (commits 24aa1e02+1535285c; review Needs-fixes->Approved; fix: rows+VerticalScroll can_focus=False + refocus filter (keyboard-first survives click), _row_id int-local_id-or-index (no BadIdentifier from composite id). Minors for final: NO_SYSTEM_PART_REASON copy not pinned, kwarg-only ctor verify at integration)
Task 12: complete (commit cd210ffe; RED->GREEN. `/prompt` handler `_console_command_insert_prompt` resolves exact-ci-name -> unique-ci-prefix -> picker(initial_query) over a bounded prefix-FTS `search_prompts` page shared with the picker's own `prompt_search` (fresh reads, no boot snapshot; prefix via `fts_match_query='"<esc>"*'` since raw MATCH needs full tokens). Composer paste API: new `ConsoleComposerBar.insert_text_as_paste` (alias over existing `insert_pasted_text`); `_insert_prompt_text_into_composer(text,*,replace)` -- replace=True clears then paste-inserts (/prompt draft IS the command), replace=False appends a "\n"+paste-insert only onto a non-empty draft (empty=clean, never clobber). Cross-screen handoff seam: new app field `pending_console_prompt_insert` + `stage_console_prompt_insert` (ChatHandoffPayload-free direct route, no tabs gate), consumed by ChatScreen on mount+resume via `set_timer(0.15,...)` (avoids racing `_sync_native_console_chat_ui`'s active-session draft reload which would wipe the insert; pending cleared only once insert lands). Blocked-composer (first-run setup) -> exact toast "Finish provider setup to insert prompts.", draft untouched. Library `#library-prompt-insert-console` handler reads LIVE user_prompt, refuses while dirty/empty (distinct notifies), never loses the prompt. +24 tests (21 composer/9->30 total incl on-mount auto-consume; 3 library canvas). Covering suites green: test_console_command_composer + test_library_prompts_canvas + test_console_native_chat_flow = 241 passed/0 failed. Gate 6-suite run earlier surfaced 1 unrelated pre-existing flake (test_console_unknown_command_first_enter... WidgetError on console-transcript-row-rule-end, a Task-10 test, passes in isolation). Full report .superpowers/sdd/task-12-report.md)
Task 12: complete (commits cd210ffe+4d26b3ca; review Needs-fixes->Approved; fix: _consume_pending_console_prompt_insert self-guards via synchronous _sync_console_session_draft before insert (atomic, closes resume clobber race), real-resume-timer + stale-session tests deterministic RED->GREEN. Composer paste API=insert_text_as_paste; handoff=app.pending_console_prompt_insert/stage_console_prompt_insert. FLAG for gate: pre-existing Task-10 test flake test_console_unknown_command_first_enter... WidgetError console-transcript-row-rule-end under parallel load, passes isolated)
Task 13: complete (pending commit; RED->GREEN. ConsoleSessionSettings.system_prompt (default None, never seeded from chat_defaults) threaded through ConsoleProviderSelection -> ConsoleChatController (both message builders _provider_messages_for_session/_provider_messages_through_message prepend via new _leading_system_message()) -- covers all 4 native send paths (submit/retry/regenerate/continue). Migration V17->V18 (_MIGRATE_V17_TO_V18_SQL: ALTER TABLE + redefined conversations_sync_* triggers, modeled on V13->V14) + add_conversation/update_conversation read-write + ChatPersistenceService.update_conversation_system_prompt (new -- none existed) + ConsoleChatStore.set_session_system_prompt. DEVIATION found+fixed: Task 0's claim that get_conversation_metadata "passes the dict straight through" was wrong -- normalize_conversation_row (chat_conversation_service.py) is a key allow-list that dropped system_prompt; caught via a real-DB (not fake-backed) resume test, one-line fix. Also fixed a pre-existing test broken by the version bump (test_local_marks_migrate_from_v16_to_v17_with_expected_schema assumed final version 17, now asserts _CURRENT_SCHEMA_VERSION). Gate: ChaChaNotesDB+Chat+Prompts_DB 754 passed/69 skipped; Tests/Chat -k console 328 passed; native_chat_flow+session_settings 285 passed (1 known pre-existing timing flake, non-reproducible, unrelated). Full report .superpowers/sdd/task-13-report.md)
Task 13: complete (commit 0b587fde; review Approved first pass. Migration v17->v18 system_prompt forward-safe (fresh 4->18 chain + ALTER + trigger fires), all 4 send paths (submit/retry/regenerate/continue) via _leading_system_message, generic dataclass serializer whitelist, normalize_conversation_row allow-list fix (Task 0 was WRONG about pass-through) w/ real-DB resume test. set_session_system_prompt plumbed, unwired pending Task 14. Minor->Task14: context-token estimate under-counts when system_prompt set (pass system_prompt to build_console_context_estimate))
Task 14 (Phase 2 FINAL): complete (pending commit -- new `tldw_chatbook/Widgets/Console/console_system_prompt_modal.py` (`ConsoleSystemPromptModal`); real `_console_command_apply_system` (exact-ci-name -> unique-ci-prefix -> apply immediately; blank system_prompt on a resolved match -> inline transcript error EXACTLY `Prompt "<name>" has no system part.`, settings unchanged; 0/2+ matches -> `ConsolePromptPickerModal(mode="apply-system", ...)`); new `_apply_console_session_system_prompt` wires Task 13's previously-unwired `store.set_session_system_prompt` from the UI layer (verified via an injected fake persistence adapter that an already-saved session's conversation update actually fires); Model rail `#console-rail-system-line` (`build_console_rail_system_line` in console_session_settings.py, task-186-style text truncation + nowrap/ellipsis, `System: none` dim state) folded into the EXISTING `_sync_console_settings_summary()` in-place-update fan-out (no new call sites, no recompose); "Save to Library" reuses Task 4's create-path pre-check idiom (`get_prompt` collision check before `save_prompt`) via a new `_save_console_system_prompt_to_library`, outcome copy duplicated verbatim from `library_screen.LIBRARY_PROMPT_SAVE_STATUS_COPY` (Screen-to-Screen import avoided; `classify_prompt_save_error` itself IS imported, it's pure); 2 new palette entries (`Insert prompt…`, `Edit system prompt`) via new guarded actions; context-estimate minor fixed (`system_prompt=` now passed). CSS: `_agentic_terminal.tcss` + regenerated bundle + dual-file pin test. +21 tests (`Tests/UI/test_console_system_prompt.py`, RED->GREEN) + 5 pure tests (`Tests/Chat/test_console_session_settings.py`). Gate: the 5 required Console UI suites + command_composer + prompt_picker + system_prompt + the 2 Chat-layer files = 551 passed/0 failed (one earlier partial run's 1 failure, an unrelated conversation-star-grouping timing assertion, confirmed a load-induced flake -- passes isolated and the full native_chat_flow file alone was 159/159 clean). Full report `.superpowers/sdd/task-14-report.md`.

# ===== Phase 2 COMPLETE (Tasks 9-14) -- ready for Task 15 gate =====
---
# SDD ledger — agent-runtime Plan A (engine + persistence) [2026-07-13]
Plan: Docs/superpowers/plans/2026-07-13-agent-runtime-plan-a-engine.md | Spec: Docs/superpowers/specs/2026-07-12-agent-runtime-vertical-slice-design.md | Branch: claude/agent-runtime-spec (worktree .claude/worktrees/agent-runtime)
Task 1: complete (commit 45c4107e, review Approved first pass — byte-exact transcription, purity verified, 7 tests)
Task 2: complete (commits ce642ddd+09eb8b3d; review Needs-fixes->Approved. Important fix: fence tag now requires clean line boundary — ```tool_calls/```tool_call_schema no longer mis-parse; split scans forward past bad tags; bare FENCE_OPEN now 'undecided'. RED-verified 4-fail pre-fix. Minor for final review: sniffer/parser whitespace-class mismatch on / + bare-CR tag line — fails toward text/None, safe direction)
Task 3: complete (commit 769df545; review Approved first pass — all 8 binding semantics traced in shipped code, ternary precedence verified via dis, truncation asymmetry + infinite-loop safety confirmed. Minors for final review: unused ToolCatalogEntry import (plan-inherited), load_tools no-room branch untested, multi-native-call batch untested)
Task 4: complete (commit 35271edd; review Approved first pass — byte-exact, asyncio.run-under-running-loop degrades safely (verified live), calculator content shape verified. Minors for final review: _owner_and_id per-lookup re-listing (no caching — revisit when MCP provider lands, else N network calls per invoke), raw.get('error') branch dead code today, brief '11 tests' prose miscount (file has 10))
Task 5: complete (commit 1c826b13; review Approved first pass — append_steps single-transaction RMW verified, COALESCE preserves result, no BaseDB connection-caching hazard (plain connect per call, verified by full base_db read), BaseDB.__init__ auto-inits schema at :70. Minors: duplicate PRAGMA/row_factory, both harmless pattern-mirrors)
Task 6: complete (commit 541e03ff; review Approved first pass — all 6 binding behaviors + adversarial checks verified LIVE (recursive-spawn rejection, disclosed-names closure mutation, child protocol excludes SPAWN); 59-test full suite independently re-run; byte-exact transcription. Brief prose miscounts (11/60 vs real 10/59) noted, non-issues. ALL Plan-A implementation tasks complete)
Final whole-branch review (opus): APPROVE WITH FIXES — 0 Critical/High, 1 Medium (F1 gate-vs-active-cap desync, repro-confirmed, dormant in slice) FIXED commit 15167461 + verified by reviewer (60 tests). Residual Minor F1-b (duplicate re-load desync: loop active list vs disclosed set dedupe) DEFERRED to task-201/200 gate w/ T3b (loop-side dedupe, no signature change) — documented in plan-a-final-review.md addendum. Minor triage: 3 drop / 5 defer / 0 block. Plan-B handoff MUSTs recorded: populate allowed_tools (fail-closed default), UI markup-escaping, worker thread (asyncio.run in provider), optional on_step hook.
LIVE SMOKE (informal, not a gate): AgentService vs REAL llama.cpp Qwen-27B — model emitted a perfectly-formed fence FIRST TRY, real calculator executed (379*6421=2433559 correct), result fed back, final answer, persisted done, 4 steps, 44s. Fence-first text protocol validated against the actual QA model.
PLAN A COMPLETE: 6 tasks + 2 fix waves, 60 tests green, commits 45c4107e..15167461 on claude/agent-runtime-spec.

---
# Plan B phase [2026-07-13]: #620 + #623 both MERGED into dev (also #621 attachments, #622 roleplay P1a landed in parallel).
# Branch claude/agent-runtime-plan-b off origin/dev 844b2720. Scout report: .superpowers/sdd/plan-b-scout.md
PLAN B Task 1: complete (commits 16cce3e5+b0fb1bb5; review Needs-fixes->Approved. Regenerate unified onto _stream_assistant_response w/ variant_mode; mid-stream stop now works for regenerate; Important FIXED: empty-stream regenerate restores (content, prior_status) — no silent skip_failed context drop; delete_message pops variant bases. Deviations adjudicated justified: brief fixture bug + mark_message_failed restore. Benign: failed->complete on successful regen variant = improvement)
PLAN B Task 2: fix wave caa00e43 landed (StreamGate defers to authoritative parser; invariant streamed+tail==visible across chunkings; 81/81). Re-review IN FLIGHT (reviewer a9a23e17dc6ad9b98). If verified -> ledger complete, proceed Task 3 (brief: task-brief PLAN 3, dispatch per SDD; Tasks 3-7 remain, Task 8 = live gate + STOP for user approval).
PLAN B Task 2: complete (commits 261aa632+caa00e43; review Critical->Approved. StreamGate now defers wholly to split_visible_text_and_tool_call: look-alike/malformed fences never drop text; streamed+tail==visible invariant fuzz-verified 3000 trials incl. prefix-at-every-feed property; genuine mid-stream fence truncates+converts; char-by-char trailing-space Minor fixed. 81/81 Tests/Agents)
PLAN B Task 3: complete (commit a1dd745a; review Approved first pass — on_step defaults no-op (backward compat, no test edits), fires once per step via the single add() helper, service wraps with agent_kind (per-frame closure, sub-agent binding verified), raising callback guarded callback-only. 96 tests. NOTE: run_id is NOT in on_step's interface — Task 5 bridge owns run correlation. NEXT: Task 4 (task-brief PLAN 4 — subagent count badge pure state + TOOL-row CSS source+bundle+pin), then 5 (bridge), 6 (controller swap + config gate), 7 (rail Agent section + badge + resume), 8 (LIVE GATE + STOP for user approval — no PR without it). Reviewers get: brief + report + review-package paths; fix waves per Critical/Important; implementers sonnet, reviewers sonnet, final review opus.
PLAN B Task 4: complete (commit e54786d4; review Approved first pass — pure subagent_counts passthrough (badge RENDERING is Task 7), TOOL+SYSTEM row classes, CSS source+bundle+dual pin verified by grep+regen timestamp. NEXT: Task 5 bridge.
PLAN B Task 5: fix wave 4d60cc59 landed (A: tool-classified streamed turns reset assistant content — no garbling, leaked prose lives in step log; B: agent_runtime rechecks should_cancel before no-tool RUN_DONE — cancel honesty, persisted+store agree; C: docstring). Re-review IN FLIGHT (reviewer a1d3c211714f8f2b3). On verified: ledger complete -> Task 6 (controller swap + [console] agent_runtime gate; brief via task-brief PLAN 6), then 7 (rail), 8 (LIVE GATE + STOP — no PR without user approval).
PLAN B Task 5: complete (commits 22791e70+4d60cc59; review 2xImportant->Approved. Bridge: garbling fixed via guarded reset_stream_content (well-behaved turns never reset; variant bases preserved); cancel honesty via agent_runtime recheck before no-tool RUN_DONE (RUN_CANCELLED carries final_text); docstring fixed. Residual Minor accepted: mid-stream materialization flush overwritten at complete. 8+86+53+51 green. NEXT: Task 6.
PLAN B Task 6: fix wave bb4130b3 landed (C1: catch-all in _run_agent_reply — no permanent wedge; C2: honest outcome mapping — RUN_ERROR/STUCK -> failed semantics, regenerate preserves original; I3: live gate refresh in _sync_console_chat_core_state; M4 documented; retry/continue/regenerate now agent-path-tested). Re-review IN FLIGHT (a75a999c35ca4653b). On verified: ledger complete -> Task 7 (rail Agent section + badge + resume; task-brief PLAN 7) -> Task 8 (LIVE GATE + STOP, no PR without user approval). Counts: 75 swap-adjacent + 372 -k console.
PLAN B Task 6: complete (commits 6ad02079+bb4130b3; review 2xCritical+Important->Approved. Swap live behind [console] agent_runtime gate w/ live refresh via update_agent_runtime; catch-all prevents controller wedge; honest outcome mapping (ERROR/STUCK->failed w/ distinct copy via _agent_failure_visible_copy, regenerate preserves original, defensive any-non-DONE->failed); retry/continue/regenerate agent-path tested. 12+75+372 green. NEXT: Task 7 (rail Agent section + badge render + resume re-derivation).
PLAN B Task 7: fix wave b2369734 landed (A: batched count_subagents_by_conversation + staleness gating; B: markup=False sites no longer escape — no backslash artifacts; C: drill-in cleared on switch + conversation_id verified; D: click-cycles all runs). Re-review IN FLIGHT (ad5166c94b3fef79c). On verified: ledger complete -> Task 8 (task-brief PLAN 8: LIVE GATE — served captures llama.cpp agent run w/ calculator + sub-agent spawn, transcript+rail+badge+Stop; broad Console sweep; STOP for USER APPROVAL — NO PR without it). Reuse capture recipe: scratchpad serve_qa.py/cap.py, 2050x1240, prompts-session QA profile precedent.
PLAN B Task 7: complete (commits 629654ba+b2369734; review 3xImportant+Minor->Approved. Rail Agent section (agent_open persisted, in-place sync), badge via batched count_subagents_by_conversation + TTL/dirty gating (N+1 gone), markup=False sites unescaped, drill-in conversation-scoped + click-cycling all runs, resume re-derivation proven. 120 green. RESIDUAL Minor for Task 8 pre-gate: _append_marker escape_markup renders backslashes through markup=False consumers (console_agent_bridge.py:261) — drop the escape + fix rationale. NEXT: Task 8 LIVE GATE + STOP.
PLAN B Task 8: evidence committed b956b6b5. Live gate DONE pending USER APPROVAL. Headline: event-loop httpx bug found+fixed live (22718fb6, 0%->100% agent sends). Deferred findings: (1) stop-before-first-token race -> status error + assistant row DROPPED (recommend fix before merge); (2) rail top-level summary not DB-re-derived on resume (badge+drill-in are); (3) badge clip at rail width (pre-existing). Sweep 1184 passed/0 fail both runs. STOP — no PR without user approval. Final whole-branch review (opus) ALSO pending before merge.
PLAN B gate fixes: complete (93cf3f33 stop-race store no-op -> clean cancelled + message survives reload; fb61c7c5 historical_snapshot rail fallback + cache). Backlog: task-226 badge clip, task-227 stop cancel-vs-finally race (new latent, narrow). User approved gate w/ fixes. NEXT: final whole-branch review (opus) over merge-base..HEAD, then PR.
PLAN B final review (opus): APPROVE WITH FIXES — 0 blocking, 2 Medium (M1 markers-not-rederived-on-resume, README claim corrected; M2 stopped regenerate discarded prior answer), 2 Low folded into task-227. Verified clean: image/vision+staged-context+system composition identical through agent path, allowlist safe, all render sites markup-safe, poll DB bounded. Residual triage 3 drop/5 defer. FINAL FIX WAVE in flight (agent aa7092db57d038cb6, both Mediums implemented + tests green, awaiting its sweep confirm + commits). THEN: verify commits, push claude/agent-runtime-plan-b, open PR to dev (user approved gate w/ fixes).

---
# SDD ledger — Skills (task-200) [2026-07-14]
Plan: Docs/superpowers/plans/2026-07-14-skills-library-console.md (14 tasks, 2 phases; Tasks 6+14 = STOP gates, no PR w/o user approval) | Spec: Docs/superpowers/specs/2026-07-14-skills-library-console-design.md | Branch: claude/skills-spec off origin/dev 9463a8a2 (worktree .claude/worktrees/agent-runtime)
Process: task-brief PLAN N -> implementer (sonnet; haiku only for fully-specified pure modules) -> review-package BASE HEAD -> reviewer (sonnet) -> fix waves for Critical/Important + re-verify via SendMessage to the same reviewer -> ledger. Final whole-branch review (opus) before each gate's PR question. Venv pytest FOREGROUND only; implementers must not park on background jobs.
USER DIRECTIVE (2026-07-14): test skills using https://github.com/obra/superpowers as the skillset — Task 5 (import) tests should exercise real superpowers SKILL.md files (clone to scratchpad, import via the UI flow; validate against skills_schemas name/description constraints, note any incompatibilities honestly); Tasks 6+14 gates seed from it (it IS the >8 catalog for disclosure). Local copy also at /Users/macbook-dev/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/ if cloning is unnecessary.
Task 1: complete (commits 8db97a35+db8a64ec — count_skills seam incl. blocked population + Skills (N) rail row; 5+103+25+257 green). NEXT: review Task 1, then Task 2.
Task 1: complete+APPROVED first pass (commits 8db97a35+db8a64ec; both populations counted via get_context available+blocked; rail row after Prompts w/ shared payload so rail/canvas can't disagree; policy ids skills.context.list.* pre-registered w/ real-registry test — no #620-style gap; scope-service-only reads)
Task 2: complete (commits 07306230+978c0533; review Important->fixed. Pure state builders w/ real-envelope fidelity verified (incl. live pydantic-classify check + markdown round-trip vs real parser); raw-text-out posture correct; drift-guard test now ties _SHADOWED_BUILTIN_NAMES to RUNTIME_TOOL_NAMES + BuiltinToolProvider + default_console_registry (subset asserts, no names missing). 11/11.
Task 3: complete+APPROVED (commit 0ba5c140; both populations render, blocked openable, escaping verified vs the #629 crash class, CSS dual-target byte-reproducible, no new action ids. Minors for final review: [/]-crash-class not test-locked (only [x]), no direct blocked-row press test, stale library_shell_state comment).
Task 4: complete (largest Phase-1 task — SKILL.md detail/trust editor + save/delete/shadow-warning wired onto Task 2/3's state+list canvas). handle_library_skill_row now opens the editor (was recording-only); create/update/delete route through skills_scope_service (mode="local", expected_version guard -> real version-conflict banner, Reload-only); save response IS a full detail so no second refetch is needed, which is also why save-marks-needs-review needs no special-casing (write never passes trust_approved=True). Trust panel (Unlock/Review changes/Approve) reuses skills_screen.SkillTrustPassphraseModal + mirrors its _call_skill_trust_service/_request_skill_trust_passphrase pattern verbatim (SkillTrustService has no policy_enforcer at all, same as that screen). Deviations from the brief's Checkbox/Switch/Select hints: user-invocable/disable-model are toggle Buttons, context is a cycling Button — matches library_notes_canvas.py's own documented render-safety discipline (Select/Switch don't render reliably in this canvas family). Policy ids skills.detail/create/update/delete.local were ALREADY registered (server_skills capability's CRUD_ACTIONS) — no #620-style gap, confirmed by a real-ServicePolicyEnforcer regression test. Caught+fixed one real bug pre-commit: an early draft recomposed on save success and re-triggered the mount-time spurious Changed event, leaving _library_skill_dirty True after a successful save. Tests: Tests/Skills/test_skills_library_flow.py (new, 7/7 — all six brief scenarios + the real-enforcer test) + Tests/UI/test_library_skills_canvas.py (32/32, replaced the Task-3 recording-only unit test) + Tests/UI/test_library_shell.py (257/257) + extra sweep Tests/Skills+Tests/Library+test_screen_navigation+test_destination_shells (786 passed/1 skipped/0 failed). NEXT: review Task 4, then Task 5.
Task 4: fix wave 897b2958 landed (rename Critical: Name read-only for existing + defensive name-force on update + validation_status asserted; stale-race guard covers is_create). Re-review IN FLIGHT (ab59286911584c247). Minors for final review: create-branch has NO entry point in the 14-task plan (near-dead code — final review triages), classify trust-blocked dead mapping, no wrong-passphrase automated test, delete-trust-manifest reconciliation follow-up, skills.trust registry entries dead project-wide (flag upstream). On verified: Task 5 (import w/ obra/superpowers real fixtures per USER DIRECTIVE) -> Task 6 GATE STOP.
Task 4: complete (commits 9105b460+897b2958; review Critical->verified. Editor+trust panel: passphrase modal genuinely reused, save-marks-needs-review real-service-tested, conflict recoverable, round-trip byte-stable, real-enforcer policy test; rename Critical FIXED belt+braces (disabled Name + name-force, validation stays valid, live-repro re-verified 41/41)). NEXT: Task 5 import w/ obra/superpowers real fixtures.
Task 5: complete (pending commit — Import row (`_run_library_skills_import` in `library_screen.py` + `_compose_import_row` in `library_skills_canvas.py`) always imports ONE skill/press, trust_approved=False; uses `import_skill(name=<directory name>, ...)` (not `import_skill_file`'s filename-derivation) whenever the path is a SKILL.md file or its directory — the real incompatibility this guards: every real skillset uses the literal filename "SKILL.md" for every skill, so filename-derivation would produce "skill" for every import. Only flat sibling files thread through as supporting_files (no nested-subfolder recursion — schemas' SUPPORTING_FILE_NAME_PATTERN has no path-separator support). Route retirement mirrors notes/prompts exactly: screen_registry "skills" ScreenRoute removed -> alias to library; shell_destinations' standalone ShellDestination("skills",...) removed -> folded into library's legacy_routes; app.py _LEGACY_ROUTE_LIBRARY_NAV_CONTEXT["skills"] added (no dedicated re-entry action, same as "prompts"). skills_screen.py/SkillsScreen NOT deleted -- Step 1 grep gate confirmed test_destination_shells.py's DestinationHarness constructs it directly, independent of screen_registry/shell_destinations (93 passed/1 pre-existing skip, untouched). Compat findings (Tests/fixtures/superpowers_skills/, 5 real + 1 synthetic-labeled fixture): every real superpowers SKILL.md uses ONLY name+description (no extra frontmatter fields at all -- schema supports far more than superpowers uses); unrecognized frontmatter fields silently dropped (no reject/warn path); nested reference subfolders (using-superpowers/references/) not importable as supporting files (pattern has no path-separator support); name-too-long/oversized-content are real Pydantic constraints, surfaced as a clean no-partial-write failure. Tests/Skills/test_skills_import.py (new, 9/9) + Tests/UI/test_screen_navigation.py (51/51, 3 hardcoded nav lists fixed + 2 new alias tests) + Tests/UI/test_library_shell.py (257/257, 1 confirmed-pre-existing unrelated flake) + Tests/UI/test_destination_shells.py (93/1 skip) + combined sweep 240/0. Full report: .superpowers/sdd/task-5-report.md. NEXT: review Task 5, then Task 6 GATE STOP.
Task 4 re-review: VERIFIED (257/257 collateral green). Task 5 landed 466914a1 (import trust-pending + tab retirement + obra/superpowers real fixtures; compat: only name+description frontmatter in real corpus, dir-name-not-filename import naming avoided a real bug, unrecognized fields silently dropped (follow-up?), nested refs not importable; 3 pre-existing unrelated failures stash-verified; palette stale copy follow-up). NEXT: review Task 5 -> Task 6 GATE STOP (serve+capture Phase-1 screens incl. superpowers-imported skills, present to user, NO PR).
Task 5: complete (commits 466914a1+ab421f9e; review Important->fixed. Import w/ real superpowers fixtures byte-identical provenance, trust-pending traced through real trust service, dir-name naming, per-file outcomes, path validation, policy enforced; retirement alias exact precedent; 3 pre-existing failures worktree-verified at base. Import-row CSS now source+bundle+pin 35/35. Context note for final review: skills.trust.* registry ids never enforced anywhere (pre-existing project-wide)). NEXT: Task 6 = PHASE-1 GATE — serve+capture Library Skills screens w/ superpowers-imported skills, STOP for user approval, NO PR.
Task 6 PHASE-1 GATE: evidence committed 40fb7b23 (Docs/superpowers/qa/skills-library-2026-07/). Sweep 1087/2-preexisting-verified. All 6 captures w/ service-read evidence, real superpowers corpus, live passphrase approve driven twice. HONEST FINDINGS for user: (a) trust panel below fold at default font — canvas can't scroll (fontsize=12 required); (b) fresh install has NO live-UI trust-bootstrap path. STOPPED — awaiting user approval before Phase 2 (Tasks 7-14). NO PR.
USER (2026-07-14): fix both gate findings BEFORE Phase 2 — (1) trust panel must be SCROLLABLE (detail canvas scrolls at default font); (2) fresh-install live-UI trust bootstrap path. Fix wave dispatched; re-capture both fixed states into the gate folder; then re-present gate. Phase 2 (Tasks 7-14) still awaits approval after.
GATE FIX WAVE: complete (9370f381 scrollable detail canvas VerticalScroll + afe122d7 SkillTrustBootstrapModal first-run setup + 553e5e8e QA re-captures at DEFAULT font). 420 tests green. Keyring wrinkle documented (served app needs PYTHON_KEYRING_BACKEND=fail for capture profiles). Phase-1 gate RE-PRESENTED to user — awaiting approval for Phase 2 (Tasks 7-14). NO PR.
PHASE-1 GATE APPROVED by user (2026-07-14, after scroll+bootstrap fixes). PHASE 2 BEGINS: Tasks 7-14 (resolver, picker, dispatch, substitution rule, SkillToolProvider, spawn-wired executor + allowlist, catalog fixes, live gate Task 14 = STOP no-PR). Same SDD process per ledger header.
Task 7: complete+APPROVED first pass (commit d39bb6f7; pure resolver — exact-ci>prefix>ambiguous, refuse copy byte-pinned, grammar untouched, /skills constant matches precedent; 20 tests. Minor: first-exact-match order-dependence on case-variant dupes (unlikely); matches-order stability = Task 9's candidates_getter concern). NEXT: Task 8 (skill picker modal; task-brief PLAN 8) then 9 (dispatch wiring — must scope candidates to user-invocable+trusted and give stable order), 10-13, 14=GATE STOP.
Task 8: complete (pending commit — new `tldw_chatbook/Widgets/Console/console_skill_picker_modal.py` (`ConsoleSkillPickerModal`), structural copy of `ConsolePromptPickerModal` per the brief, with the `apply-system`-style blocked-row mode dropped entirely (skills have one mode — `skill_search` is documented to already return only trusted, user-invocable rows; Task 9's closure filters `get_context`'s `available_skills` client-side). Row id is name-keyed (`console-skill-picker-row-<name>`, per the brief) with an index fallback for a malformed/duplicate name (mirrors Task 11's composite-id fallback discipline). Row label shows `name` + escaped `description` (both escape_markup'd) — this differs from the prompt picker's name-only row, per the brief's explicit escape-proof-description test. Empty-store copy imported verbatim from Task 7's `SKILLS_EMPTY_LIST_ROW` (no duplicated literal). CSS `_agentic_terminal.tcss` + regenerated bundle + dual-file pin test. 11 new tests (`Tests/UI/test_console_skill_picker.py`, RED->GREEN): the 5 brief scenarios + click-select, up/down-then-enter, duplicate-name fallback, initial-query prefill, CSS pin. `Tests/UI/test_console_prompt_picker.py` (19) + `Tests/Chat/test_console_skill_resolver.py` (20) unaffected/still green. Honest note: a non-empty `initial_query` causes `skill_search` to be called twice with the same settled query on open (Textual's `Input(value=...)` posts its own initial `Changed` message on construction, in addition to `on_mount`'s explicit non-debounced search) — harmless (both calls carry identical, already-settled text) but wasteful; this is a shared quirk with `ConsolePromptPickerModal` (same code shape), never exercised by that widget's own test suite, not fixed here as out of this task's brief. NEXT: review Task 8, then Task 9 (dispatch wiring — register `/skills` + fallback resolver + wire the real `skill_search` closure from `get_context`).
Task 8: complete+APPROVED (commit da20cff6; picker mirrors #620 disciplines traced line-by-line; deviation adjudicated brief-compliant — trusted-only search, no blocked mode; refocus-after-click N/A since every click dismisses). FIX PENDING (Important): test_typing_still_filters_after_a_row_click_does_not_dismiss at Tests/UI/test_console_skill_picker.py:253 is a byte-copy no-op — rename/remove or make real. CORRECTION: sibling suite counts are 10/10 prompt-picker + 9/9 resolver (my earlier 19/20 claims were wrong). FORWARD IMPORTANT FOR TASK 9: needs-review skills matching a typed prefix are silently invisible — picker shows 'No skills yet' which misleads; Task 9 dispatch owns SKILL_UNTRUSTED_REFUSE and must add a distinguishing empty-state/hint (e.g. 'N skills match but need review in Library'). NEXT: haiku fix for the no-op test -> Task 9 (dispatch wiring, brief via task-brief PLAN 9) -> 10-13 -> 14 GATE STOP.
Task 8 fix wave: ef61e0e9 (no-op test replaced w/ real filter-focus assertion, 11/11). Tasks 7-8 DONE. NEXT: Task 9 (task-brief PLAN 9 — dispatch wiring: register fallback resolver + /skills command in chat_screen/controller, real scope-service candidates STABLY SORTED + scoped user_invocable+trusted, SKILL_UNTRUSTED_REFUSE surfaced, AND the forward Important: distinguishing empty/hint when prefix matches only needs-review skills). Then 10 (substitution rule), 11 (SkillToolProvider), 12 (spawn executor+allowlist), 13 (catalog fixes), 14 (LIVE GATE >8 superpowers skills, STOP, NO PR).
Task 9: complete (grammar gets `SKILLS_COMMAND_NAME`/`ARGUMENT_HINT`/`HANDLER_ID` registered in `default_console_registry` — `available_names()` now `("prompt","system","skills")`, `_console_unknown_command_hint` derives its list from the registry instead of a hardcoded string. chat_screen.py registers the Task-7 fallback resolver on `_console_command_registry` in `__init__`, closing over a lazy `_console_skill_candidates` snapshot refreshed on `on_mount`/`on_screen_resume` via `_refresh_console_skill_candidates` (fresh `get_context(mode="local")`, filtered `user_invocable and not trust_blocked`, stably sorted by case-folded name). `_dispatch_console_command` routes `KIND_FALLBACK` straight to `_console_command_run_skill` and registered `"skills"` to `_console_command_skills` (bare lists via `format_skills_list`; `<name> [args]` runs). Run semantics (`_console_command_run_skill`) always re-resolve against a FRESH `get_context` (never the cached snapshot): resolved -> `_run_resolved_console_skill` submits the raw `/name [args]` text via a new shared `_dispatch_console_draft_send` gate helper (extracted from the existing send-tail, returns bool so the TOOL marker `skill {name} → driving this turn` is only appended once actually queued); ambiguous or 0-match-with-args -> `ConsoleSkillPickerModal` prefilled (args preserved for after the pick); 0-match-no-args or an exact blocked-name match -> `SKILL_UNTRUSTED_REFUSE` (absent vs. that skill's own `trust_reason_code`). Review-mandated addition implemented: a typed prefix matching ONLY needs-review skills gets `CONSOLE_SKILL_NEEDS_REVIEW_HINT_TEMPLATE` ("N matching skill(s) need review...") instead of the misleading empty picker/absent-refuse. `execute_skill` deliberately NOT called anywhere here -- Task 10 owns turning the submitted raw command into the actual rendered skill payload. 7 new tests (`Tests/UI/test_console_skill_commands.py`, real App + fake `skills_scope_service`) + 3 new grammar tests + updated stable-ids/hint-copy tests in the 2 existing suites the brief named. Full sweeps green: `Tests/Chat` 722 passed/69 skipped (+3 vs Task 7-8 baseline), `Tests/UI/test_console_native_chat_flow.py` 184/184, `test_console_command_composer.py`/`test_console_skill_picker.py`/`test_console_skill_resolver.py`/`test_console_prompt_picker.py`/`test_console_system_prompt.py` all green. NEXT: review Task 9, then Task 10 (substitution rule), 11 (SkillToolProvider), 12 (spawn executor+allowlist), 13 (catalog fixes), 14 (LIVE GATE, STOP, NO PR).
Task 9: fix wave 19933584 landed ([USER,TOOL,ASSISTANT] order via pending-field in on_submission_accepted + leak-clear; bare-fallback blocked-hint). Re-review IN FLIGHT (aad223ff06efdb699). On verified -> Task 10 (substitution rule — task-brief PLAN 10; the provider-payload final-user-message re-render w/ re-trust-check, fork=clean-context; sites: console_chat_controller._provider_messages_* + bridge). Then 11, 12, 13, 14=GATE.
Task 9: complete+VERIFIED (commits 0098385e+19933584; dispatch wiring approved — [USER,TOOL,ASSISTANT] order repro-verified, leak test RED-capable, bare-fallback blocked-hint, /banana still falls to unknown hint. Minor accepted: picker's own live search closure can't distinguish blocked matches — dispatch-level hint covers the primary UX).
Task 10: complete+APPROVED first pass (commit 4672e3f6; substitution rule at all 4 payload sites; final-message-only + history-raw adversarially verified; retry-of-edited-skill refuses recoverably; fork [system?, rendered]. Minors for final review: image+skill-command turn SKIPS substitution (raw text sent — safer than report claimed, narrow), fork last-item invariant undocumented, candidates-sort cosmetic divergence). NEXT: Task 11 (SkillToolProvider — task-brief PLAN 11), 12 (spawn executor+allowlist), 13 (catalog fixes), 14 (GATE STOP >8 superpowers skills).
Task 11: complete+APPROVED first pass (commit 957b2243; SkillToolProvider catalog/schema per spec, invoke-raises traced LIVE: pre-Task-12 skill-tool call aborts run status=error via agent_service outer catch — safe, loud, unreachable today (bridge registers builtins only). Minors: unguarded next/KeyError consistent w/ house style; duplicated provider shape acceptable). NEXT: Task 12 (per-run spawn-wired executor + run allowlist at the bridge — replaces the abort path; task-brief PLAN 12), 13, 14=GATE.
Task 12: complete (pending commit — `agent_service.py`: `AgentService(..., skill_runner=None)` (new `SkillRunner` Protocol: `is_skill_tool`/`run`); `_run_one`'s `spawn` gains `allowed_tools=None` override (default preserves shipped spawn_subagent behavior); a new skill-aware `invoke_tool`, built AFTER `spawn` (closes over it) and reusing the old builtin-path callable as fallback, replaces the direct `self._make_invoke_tool(...)` LoopDeps wire-up — a skill-tool call gates on `config.allowed_tools` (mirrors the SPAWN_TOOL_NAME precedent — no `disclosed_names` re-check, since the bridge's per-run allow-list IS the authoritative trusted+model-invocable boundary), then a `skill_spawns` local counter vs `budget.max_subagents` (own counter, independent of spawn_subagent's per Q13's documented v1 simplification), then `skill_runner.run(name, args, spawn)`. `console_agent_bridge.py`: `ConsoleAgentBridge(..., skills_service=None)`; `run_reply` composes a FRESH per-run registry+allow-list (new `_compose_run_registry_and_allowed`/pure `_compose_run_allowed_tools` helpers: builtins registered first, then a `SkillToolProvider` snapshot of trusted+model-invocable skills from `get_context(mode=\"local\")`, allow-list = builtins ∪ eligible skills ∪ spawn) only when `skills_service` is wired; without it, the shipped shared registry/allow-list is used unchanged (no-skills path byte-identical, verified). New `_BridgeSkillRunner` renders via `execute_skill(mode=\"local\")` (re-verifies trust at call time — a `SkillTrustBlockedError` becomes a `ToolResult(ok=False, error=SKILL_UNTRUSTED_REFUSE...)`, no spawn), then `spawn(rendered_prompt, allowed_tools=intersect_skill_tools(declared, builtin_names))` — builtins-only, so a skill's own sub-agent can never call another skill (skills-can't-call-skills) and inherits `clamp_child_budget`'s `max_subagents=0` (depth-1). Found+fixed during self-review (not in the brief): a skill front-matter'd with the same name as a builtin (e.g. `\"calculator\"`) would have shadowed the builtin AT INVOCATION, since `invoke_tool` checks `skill_runner.is_skill_tool(name)` before ever consulting the registry's own builtins-first shadowing order — added `_non_colliding_skill_entries` to exclude any skill name colliding with a builtin from BOTH the registry's `SkillToolProvider` population and the allow-list/skill_runner's skill-name set, with a dedicated end-to-end regression test. `chat_screen._ensure_console_agent_bridge` passes `skills_service=getattr(self.app_instance, \"skills_scope_service\", None)`. Tests: new `Tests/Agents/test_skill_tool_spawn.py` (2, brief's exact scripts — real `AgentRunsDB`+fake `SkillRunner`, budget-refusal case) + `Tests/Chat/test_console_agent_bridge.py` extended (+8: routes-through-spawn end-to-end w/ fake async `skills_service`, trust-blocked refuses without spawning, no-skills-service parity, 4 `_compose_run_allowed_tools` unit tests incl. collision, builtin-shadows-skill end-to-end). Full sweeps green (FOREGROUND venv python): `Tests/Agents` 96/96, `Tests/Chat` 738 passed/69 skipped (+8 vs Task 11 baseline, 0 regressions), `Tests/Chat/test_console_agent_bridge.py`+`test_console_agent_swap.py` 62/62, `Tests/UI/test_console_native_chat_flow.py`+`test_console_skill_commands.py`+`test_screen_navigation.py` 245/245. NEXT: review Task 12, then Task 13 (catalog dedupe/caching fixes — do not fix in 12, don't regress), 14 (LIVE GATE >8 superpowers skills, STOP, NO PR).
Task 12: fix wave f0db34e4 landed (disclosure gate now covers skill tools; combined sub-agent ceiling in the shared spawn closure; loop annotation-only). Re-review IN FLIGHT (a1581ac07fd6f06db). On verified -> Task 13 (catalog dedupe + per-run owner-map cache w/ regressions; task-brief PLAN 13) -> Task 14 LIVE GATE (>8 superpowers skills, /skill-name inline+fork live, agent-discovers-skill via disclosure, STOP NO PR).
Task 12: complete+VERIFIED (commits 741fa4b3+f0db34e4; spawn-wired executor, hijack fix, disclosed+allowed gate for skills, combined sub-agent ceiling in the shared closure — all live-re-repro'd). NEXT: Task 13 (catalog fixes: loop-side dup dedupe before slicing [F1-b lineage] + per-run owner-map cache [task-201-TODO] w/ regressions; task-brief PLAN 13). Then Task 14 = LIVE GATE STOP.
Task 13: complete+APPROVED first pass (commit 6e6712e0; F1-b dedupe-before-slice w/ addendum repro re-run; per-run owner-map cache reset at run_turn only — sub-agents share parent scope; TODO resolved; all Task-12 security props spot-verified green; RED-verified via throwaway pre-fix worktree). NEXT: Task 14 = PHASE-2 LIVE GATE (task-brief PLAN 14) — llama.cpp @9099, >8 superpowers skills seeded+approved, /skill-name inline + fork live, edited-skill refuse, agent discovers+calls a skill via disclosure, STOP NO PR.
Task 14: complete (STOP for user approval, no PR). Sweep 2000 passed/1 failed(exempt, verified isolated
pass, matches Task 6 gate's documented pre-existing flake)/69 skipped. Live gate: qa-home-skills-agent14
(17 skills: 14 real obra/superpowers + shout/fork-demo synthetic demos + quarantine-demo synthetic
never-approved; 15 trusted/2 blocked). CONFIRMED PRODUCT FINDING (out of scope, workaround shipped):
opening the Library Skills detail editor permanently breaks the served app's top-level tab-bar click
activation for the rest of that process (hover renders, no screen switch) -- reproduces with zero modal
interaction, isolated via 8+ probes. Workaround: serve_qa_autounlock.py monkeypatches TldwCli.on_mount to
call SkillTrustService.unlock_with_passphrase directly (same app_instance object Console reads), avoiding
the buggy click path while exercising every real trust-gated code path. All 6 scenarios captured +
DB-verified (ChaChaNotes messages table for render-vs-persist + AgentRunsDB parent/child rows for the
skill-tool spawn). Scenario 5 caveat: primary run ends `stuck` (step budget exhausted right after a
successful find_tools->load_tools->shout->"HELLO" sequence) -- pipeline correct, budget-tuning follow-up
noted. Evidence + README: Docs/superpowers/qa/skills-console-2026-07/. Full report:
.superpowers/sdd/task-14-report.md. STOP -- awaiting user sign-off before any PR.
Task 14 PHASE-2 GATE: evidence committed a5523e52 (Docs/superpowers/qa/skills-console-2026-07/, 8 captures + README). Sweep 2000/1-known-flake. ALL 6 scenarios LIVE+DB-verified incl. agent find_tools->load_tools->skill-call w/ parent-linked sub-agent row (>8 catalog, disclosure engaged). FINDINGS for user: (a) primary run ends stuck right after successful skill sequence — step-budget tuning; (b) PRODUCT BUG: opening Library Skills detail editor breaks tab-bar click activation for served-process lifetime (isolated 8+ probes, QA workaround shipped, needs ticket). STOPPED — awaiting user approval. Final whole-branch review (opus) still owed before PR.
USER (2026-07-14): fix BOTH gate findings (1 step-budget exhaustion on discovery runs + re-capture headline; 2 the tab-bar click-activation bug — FIX not just ticket) + create a backlog TASK for a tool-call-flow review (enhancements/efficiency in tool calling flows). Then final whole-branch review (opus) before PR question.
GATE FIX 1: complete (6736835d CONSOLE_RUN_BUDGET 16/480 config-site only + 96d21e50 re-capture: discovery run status=done, 10 steps, HELLO, child parent-linked; engine default 8 pinned). NEXT: gate fix 2 (tab-bar click bug — systematic debugging), then final whole-branch review (opus), then PR question. task-231 filed (tool-call flow review).
GATE FIX 2: complete (81700962+1c3baf9d; ROOT CAUSE: Input mouse-capture leak on recompose — App.mouse_captured points at removed widget, NoWidget swallows all mouse events app-wide; fixed in base_app_screen recompose path w/ capture-release guard mirroring push_screen; PRE-EXISTING via prompts editor on dev; 427 tests green). Both gate findings CLOSED. task-231 filed. NEXT: final whole-branch review (opus) over merge-base..HEAD then present + PR question.
SKILLS FINAL WHOLE-BRANCH REVIEW (opus): APPROVE — 0 block/0 medium/1 low observation (recompose capture-release strictly better than the bug). Security end-to-end solid (4-layer no-skills-calling-skills defense), composition correct, regression risk low, seams clean. Residual triage 0 BLOCK/9 DEFER/3 DROP. Both gate fixes in (budget 6736835d+96d21e50; mouse-capture root cause 81700962+1c3baf9d). task-231 filed. AWAITING USER: Phase-2 gate approval + PR word. NO PR YET.
SKILLS PR OPENED. Branch 75 behind dev — rebase-and-merge at the user's word, per pattern.
INDEPENDENT PRE-MERGE REVIEW (requesting-code-review skill): With-fixes — Important#1 Create▸New-skill entry point never built (spec-named, is_create branch dead); Important#2 resolve_name still re-lists per invocation (owner-cache half-delivery, MCP risk); Minor#3 ordinary child inherits skill names (wasted render before refusal); #4-7 notes. FIX WAVE dispatched for 1+2+3+doc nits. Then re-verify + merge-readiness.
PR #636 MERGED 2026-07-16 (after: merge-from-dev x2 incl. TASK-228 grouping + CONSOLE_RUN_ALREADY_RUNNING_COPY unions; bot fix wave c51a861a..6c34e52c — Qodo3 lying-marker reorder, Qodo4 RUNTIME_TOOL_NAMES collisions, Gemini already-loaded msg + empty-word guard, docs; 3 declines w/ evidence incl. Gemini's falsified is_running Critical; sweep 2090 passed / 2 known cross-file flakes both standalone-green). SKILLS SUB-PROJECT COMPLETE. Program remaining: MCP task-201 (parallel session spec; must plug ToolProvider), task-231 tool-call flow review, deferred minors in skills-final-review.md.

---
# Improvements sweep [2026-07-16] — branch claude/improvements-sweep off origin/dev
USER: general improvements/efficiency/bug squashing; MCP handled elsewhere. Slate: task-231 investigation (parallel, read-only) + bugs 227 (stop races, 3 ACs), 226 (badge clip), 195 (marker test root-cause), 2 library_shell cross-file flakes (export-registry + search-history — no task filed, fix isolation). task-232 SKIPPED (ACs already [x] — parallel session owns it; verify before touching).
Process: one implementer at a time on this worktree + reviewer per fix; 231 runs concurrently (read-only).
Task-226: complete (pending commit; RED->GREEN). Root cause: the `[N Sub-Agents]` badge shared the row's LAST line with the unbounded secondary-detail text (workspace label - status - age); a long secondary line pushed the trailing badge past the rail's rendered width, clipping it to a bare `[1` (per the agent-runtime live-gate capture). Fix: `format_console_conversation_row_label` now appends the badge on its own dedicated trailing line (`\n[dim]...[/dim]`) instead of sharing the last existing line, decoupling its visibility from title/secondary length; `_conversation_button`/`_compose_conversation_browser_row` grow the row+star buttons to 3 lines only when a badge will render (`_conversation_row_render_height`), and `_conversation_browser_list_height` sums per-row height so badge rows reserve the extra line without disturbing badge-less rows. 4 new tests in `Tests/UI/test_console_agent_rail.py` (RED-verified via `git stash` on the source file, then GREEN), all 21 tests in that file green, plus `test_console_persistent_rails`+`test_console_workspace_context_rail`+`test_console_conversation_browser_state`+`test_conversation_browser_subagents`+`test_console_rail_sections`+`test_console_native_chat_flow` = 347 passed/1 failed out of 348 (the 1 failure is `test_console_workspace_context_syncs_active_conversation_marker`, confirmed PRE-EXISTING via git-stash isolation — fails identically without this change). Full report `.superpowers/sdd/task-226-report.md`.
SWEEP: task-227 DONE+VERIFIED (eafabeea+6ddd44e5 — per-run cancel Event closure; AC3 regenerate window closed via cancel-authority finalize; reviewer re-ran both repros; Minor: non-stopped invalid-finalize raise verified-not-pinned). task-231 DONE (349032b6 report; follow-ups filed as 243-245: native tool-calls, tiered budgets+load-name-fallback, protocol memoize). task-226 landed 5698dba6 (badge own-line) — review next. THEN task-195 + the 2 library_shell flakes.
SWEEP COMPLETE: 227 (races, +review Critical fixed), 226 (badge own-line, +aggregate-height pin), 195 (test drift ×2: glyph rename + unreachable dual-placeholder), flakes (CPU-contention races → wall-clock waits, 3c6fed4d), 231 (report + 243-245 filed). Observed new pre-existing flake: test_skills_import trust-pending (out of scope, note in PR). Final sweep -> PR.
SWEEP PR #644 OPENED (claude/improvements-sweep): 227+226+195+flakes fixed, 231 report + 243-245 filed. Final sweep 1576/0 incl. former flakes cross-file. Awaiting bot findings + user's merge word per pattern.

---
# task-243 [2026-07-16] — native provider tool-calls w/ fence fallback. Branch claude/native-tool-calls off origin/dev (post-#644).
Basis: task file ACs 1-4 + Docs/superpowers/reviews/2026-07-16-tool-call-flow-review.md §opportunity 1. Loop already handles native multi-call ModelTurn.tool_calls (Plan A). Missing: capability check, tools= wiring in agent_service._make_call_model, native response parsing, bridge streaming-adapter native-delta handling (Plan-B spec: "structured deltas never hit the transcript"), fence suppression for capable providers.
Flow: scout (response/delta shapes, gateway, model_capabilities) -> plan -> SDD -> PR.
PLAN WRITTEN: Docs/superpowers/plans/2026-07-16-native-tool-calls.md (6 tasks; scout falsified task description's "anthropic/google/cohere native-ready" claim — their handlers DROP tool_use; native set = raw-OpenAI-dict providers only: openai/groq/openrouter/mistral/deepseek/moonshot/custom-openai x2; groq+deepseek need PROVIDER_PARAM_MAP additions). Key design: ModelTurn.assistant_message verbatim echo + role="tool" results keyed on call_id (fence path byte-identical); ProviderToolCalls sentinel from gateway ONLY when tools= requested; bridge passes execution_key-first as api_endpoint; kill-switch [console] native_tool_calls default ON. task-243 In Progress w/ plan. NEXT: SDD task 1.
Task 1: complete (78728b04..fb51a73b, review clean — native_tools module + groq/deepseek map additions verified independently)
Task 2: complete (fb51a73b..5a4bd374, review clean; getattr(turn,"assistant_message",None) deviation adjudicated CORRECT — dedupe-test duck-types out of file list; MINOR deferred: convert test_agent_loop_load_dedupe type("M",...) stand-ins to real ModelTurn)
Task 3: complete (5a4bd374..ba0a5cd1, review clean; AC#3 demonstrated end-to-end at service level). MINORS deferred: dead NATIVE_CFG in test_agent_service.py; no explicit empty-schema-native tools-absence test. Reviewer sequencing note: mid-branch, bridge on capable providers is native w/o adapter tools support (protocol suppressed, tools swallowed by **_ignored) — closed by Tasks 4+5 pre-merge; no live runs until gate.
Task 4: complete (ba0a5cd1..03a205b0 + fix f961ce0a, re-review clean both verdicts). CRITICAL caught+fixed: plan-mandated mapping-level tool_calls guards violated tools=None byte-identity (raw-dict delta silent-drop; message-shape copy swap) and were redundant — deleted, worker filter alone carries tools mode; regression test pins raw-dict tools=None path. MINOR deferred: worker copy-filter matches by content equality (legit answer == copy string theoretically suppressed).
Task 5: complete (f961ce0a..3a823b7b, review clean both verdicts). Deviation adjudicated ACCEPTABLE: adapter forwards tools= only when non-None (real gateway defaults None; service already omits tools on fence path one layer up) — preserves out-of-whitelist stale fakes. All 5 code tasks DONE. NEXT: Task 6 live gate (coordinator-run): cloud-provider native round-trip + llama.cpp fence regression + QA README + task-246 follow-up + final whole-branch review (opus) + PR.
Task 6 gate (in progress): keychain probe DENIED by classifier (dropped); NO cloud API keys in user config/env (placeholders + unset env vars) — AC#2 "cloud provider" literal wording needs user decision. DISCOVERY: local llama.cpp @9099 natively honors tools= (real tool_calls w/ ids) -> full real-stack gate via custom-openai-api handler. BLOCKER FIXED (858d9ce9): chat_with_custom_openai + chat_with_local_llm crashed on EVERY call (provider_name=cfg.capitalize() on a dict) — pre-existing, custom provider was dead on dev; +2 regression tests. task-246 filed (bbf29ec8, remote-max re-verified 245). Gate case A PASS (46.5s): tools= both turns, fence_in_system=False, calculator round-trip 18018, TOOL marker, run steps model/tool_call/tool_result/model. Cross-suite at HEAD: 337 passed. Cases B/C running.
Gate cases B/C: B timed out at 300s read-timeout (honest error outcome, no wedge) and llama.cpp server DIED (~end of B); C captured routing half (tools=None + fence protocol present = fallback contract proven live) but round-trip blocked by dead server — infrastructure, not code. Server still down, no llama process; CANNOT restart user's service. QA README committed w/ honest partials. NEXT: final whole-branch review (opus), then present to user: (1) AC#2 cloud-wording decision (key vs accept custom evidence), (2) rerun B/C when server back, (3) PR word.
FINAL WHOLE-BRANCH REVIEW (opus): APPROVE — 0 Critical/0 Important. Composition traced both modes through real code; permission gate verified (native names hit same allowed∩disclosed gate; refusals echoed role="tool" paired by id); mid-batch early-return dangling echo proven harmless (messages die with run; next run seeds from store transcript). m1-m4 ALL DEFER + 2 new Minors DEFER: (m5) id-less native tool_calls -> mixed role=user pairing vs id-carrying echo -> strict provider 400s next turn (graceful RUN_ERROR; fix: synthesize stable id); (m6) tools-mode genuinely-unsupported non-streaming response -> silent empty done (copy suppressed, no sentinel). Test honesty confirmed (real-signature fakes at bridge layer; no task-217-style masking). AWAITING USER: AC#2 cloud-wording decision + B/C rerun when server back + PR word. NO PR YET.
USER DECISIONS 2026-07-16: AC#2 reworded to "native-capable provider" + checked (custom-openai-api evidence accepted); PR opened now, merge on user word. task-243 Done.
PR #648 OPENED (claude/native-tool-calls -> dev, 13 commits): native tool-calls + fence fallback, custom-openai crasher fix, gate evidence, task-243 Done + task-246 filed. Awaiting bot findings + user merge word per pattern.
INDEPENDENT PRE-MERGE REVIEW (requesting-code-review skill, opus): READY TO MERGE — 0 Critical/0 Important. Minor 1 FIXED (RED->GREEN + scope-guard test): tools= turn w/ neither content nor calls now enqueues provider error (honest RUN_ERROR) instead of silent empty done; gateway 64 passed, cross-suite 180 passed. Minor 2 (custom vs custom_2 config-loading divergence) pre-existing/out of scope — noted only. Consideration confirmed as plan-documented decision: default-ON rollout w/ [console] native_tool_calls escape hatch. Pushed.
MERGE-FROM-DEV: clean auto-merge (chat_screen.py). ID COLLISION caught: dev minted tasks 246-262 (RAG renumber + perf audit — which also collide with EACH OTHER on dev, two 246s/247s/etc, not ours to fix) -> our Anthropic-family follow-up renumbered task-246 -> task-263 (file + TASK- frontmatter + all branch references: native_tools.py docstring, QA README, task-243 notes). Bot findings: Gemini shallow-copy FIXED (fresh literal); Qodo-1 order FIXED (sorted index + out-of-order test); Qodo-2 annotations FIXED (str | ProviderToolCalls); Qodo-3 magic-string DECLINED (file's _QueueItem kind literals are the established pattern — content/error/done all compared as literals in the same function); Qodo-4 id-less pairing FIXED (ensure_tool_call_ids synthesizes call_<n> into BOTH echo and role=tool reply; unit + service pairing tests). 217 cross-suite + 125 Agents green.
Post-merge sweep: 549/550 x2 — test_console_native_generic_provider_send_renders_completed_message fails ONLY under cross-file contention (fixture-server HTTP 502 on a PLAIN tools=None send). PROVEN PRE-EXISTING: identical failure reproduced on a clean origin/dev worktree (521 passed / same 1 failed, same 502). Passes standalone. Not a branch regression; noted in PR reply. Candidate follow-up for the wall-clock-wait flake family.
