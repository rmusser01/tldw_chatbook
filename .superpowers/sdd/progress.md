# RAG scope narrowing — SDD ledger (claude/rag-scope-spec)
Incident: T1 implementer committed on main-checkout foreign branch (cwd-reset trap); cherry-picked 7f2475300 here, reset --keep removed it there cleanly.
T1 minors for final-review triage: (a) isinstance(version,int) accepts bool True as v1; (b) dict/list source_id str()-coerced to garbage instead of rejected.
Task 1: complete (commits 7f24753..c0cac20, review clean after 1 fix wave; None-no-warning deviation endorsed). New minors for final triage: version-mismatch vs malformed message routing for str "1"; test sink-loop style.
Task 2: complete (commit 3c99d85, review clean first pass). Minors for triage: zero-item single-level scope gets "no-workspace-overlap" cause (misleading; Task 5 must normalize zero-item stored scopes to unscoped on read); sorted-by-len micro-opt beyond brief. NOTE for T5 dispatch: read_conversation_scope normalizes items==() to None.
Task 3: complete (store-level `metadata_allowlist` on both vector stores + RAGService.search/_semantic_search + EnhancedRAGServiceV2.search threading; 17 new tests in Tests/RAG/test_scope_store_filtering.py, full Tests/RAG/ green 379 passed/8 skipped/0 failed). CONCERN for whichever task wires `EffectiveScope.allowlist` into these calls (T4/T5?): `SimpleRAGCache` (`simple_cache.get_rag_cache`) is a process-wide singleton whose cache key (`query, search_type, top_k, filter_metadata`) does NOT include `metadata_allowlist` — two searches with the same query/search_type/top_k/filter_metadata but different scopes can read each other's cached hits. Confirmed empirically (own new tests collided against each other's cache entries until the test fixture added an explicit `cache.clear()`). Left out of Task 3's scope (brief only names vector_store.py/rag_service.py); needs its own task before real scoped callers start passing metadata_allowlist through cached paths.
