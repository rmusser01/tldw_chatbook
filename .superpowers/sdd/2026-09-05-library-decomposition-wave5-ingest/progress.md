# SDD ledger — plan: Docs/superpowers/plans/2026-09-05-library-decomposition-wave5-ingest.md

Spec (dev) + recipe §1-§19 binding. Branch: refactor/library-decomp-wave5-ingest (cut from origin/dev after the wave-4 merge). Worktree: .worktrees/library-decomp-foundation (own venv). Baseline: screen 41574/1302; ingest 78 methods/20 fields.

## Pre-flight conflict scan
| Check | Finding |
|---|---|
| T1→T2→T3→T4 same-shape series | Consistent with five prior series |
| Ingest-options trio | Named no-touch (module-globals, foundation-era discovery); verify location, never move |
| @work-heavy flows + worker groups | Early enumeration mandated |
| Form-persistence contract (task-2043) | Pin via characterization if uncovered, before any move |
| Rubric conflicts | Same plan-mandated duplication; carried in reviewer constraints |

Standing rulings inherited: all of waves 1-4.

## Task log
Task 1: dispatched (implementer sonnet), BASE=9e62dd8f7 — ingest state PR (series 1/3)
Task 1: INCIDENT — implementer killed by session usage limit mid-baseline; its in-place path-scoped checkout of the pre-task commit was left in the worktree (would have corrupted the next commit). Controller restored to HEAD; agent resumed with the isolated-baseline method mandated. Recipe candidate lesson for wave close: in-place baseline checkouts are interruption-unsafe — always use an isolated worktree/archive for baseline runs.
Task 1: DONE — a11220648 (RED: characterization + wiring pins) → 12ba4fb13 (GREEN: LibraryIngestState + shims, 20/20 fields, ratchet 41574/1302 → 41520/1302). Found and fixed a new bypass shape (`object.__new__(LibraryScreen)`/`.LibraryScreen.__new__` fixtures across 4 test files hand-setting flat `_library_ingest_*` names — 24 call sites patched with one `screen._ingest_state = LibraryIngestState()` line each, zero assertions touched) inside the same GREEN commit per no-red-ships. Full battery green: wiring RED→GREEN, characterization pre-change PASS (form-persistence contract already covered, no new pin needed), both ratchet rows, `-k "ingest and library"` (1298 passed/7 pre-existing), full sequential xdist paired-baseline sweep via isolated `git worktree` (356/3989 branch vs 370/3971 baseline, 8 branch-unique all resolved, zero real regressions), preflight all-green. Recipe §7 pre-existing-failure list and a new isolated-worktree-baseline lesson recorded. Report: .superpowers/sdd/2026-09-05-library-decomposition-wave5-ingest/task-1-report.md.
Task 1: implementer DONE post-recovery (commits a11220648/12ba4fb13/3b83eab93/3e2a43c14; 20 fields, simplest state PR; NEW object.__new__ bypass shape with 24 fixture seeds in GREEN; 5 handlers deferred as coverage debt; ratchet 41520/1302); reviewer dispatching (sonnet)
Task 1: review Needs Fixes — 1 CRITICAL (2 RED tests at HEAD in test_parakeet_v2_install_ui.py: the object.__new__ bypass fix missed the one file whose NAME contains neither "ingest" nor "library" — every -k sweep structurally blind to it) + 2 Important (shape not in recipe §3; 24/4-vs-25/5 count error) — fix round 1/5 dispatched
Task 1: systemic lesson for recipe: -k name filters are content-blind; bypass censuses must grep by content across all of Tests/
Task 1: Task-2 dispatch mandate recorded: the 5 deferred handlers get real .press() pins BEFORE their bodies move (reviewer's condition on the deferral)
Task 1: fix round 1 DONE — fixed test_parakeet_v2_install_ui.py (2 sites, LibraryIngestState seeded, file now 25/25); re-ran the repo-wide content grep (object.__new__(LibraryScreen)/LibraryScreen.__new__ co-occurring with _library_ingest_) and confirmed it is the ONLY remaining miss (test_library_screen.py reconfirmed a false positive by reading its fixture body); true totals corrected to 27 seeded sites/6 files (was misreported as 24/4); recipe §3 gained a formal numbered "seventh bypass shape" entry (fixture bypass + filter-blindness lesson + isolated-worktree cross-reference); report's §5/§10 annotated in place, §11 appended. Wiring suite (38 passed), both ratchets (32 passed/2 pre-existing chat_screen failures), preflight all green. Commit: fix(library): seed the missed object.__new__ fixtures; recipe gains the fixture-bypass shape (fix round 1).
Task 1: fix round 1/5 (CRITICAL + 2 Important addressed; independent count 27 sites/6 files confirmed; recipe seventh bypass shape landed; commit 74a6f5774)
Task 1: complete (commits 9e62dd8f7..74a6f5774, review clean after 1 fix round)
Task 2: dispatched (implementer sonnet), BASE=74a6f5774 — ingest controller move (series 2/3); HARD PRECONDITION: 5 deferred handlers pinned via real .press() in the RED commit before their bodies move
Task 2: DONE — 44ab7383b (RED: wiring rewrite to the 58-name controller shape + 5 new real .press()/message pins for handle_library_ingest_cancel/_force_stop/_retry_faster_whisper/_option_reset/_directory_browse, all 9 characterization tests passing pre-move, 4 wiring tests genuinely RED) → 68a896993 (GREEN: LibraryIngestController, UI/Library_Modules/library_ingest_controller.py, 57 of 78 candidates moved byte-for-byte; 21 excluded: 4 `@work` framework-decorator hazard, 2 module-globals-coupling, 9 unbound-fake-self/`object.__new__`-bypass, 6 instance-attribute-monkeypatch — the last found ONLY by running the full battery across 4 successive draft rounds, never by the static census) → 18e9c60f7 (blame-ignore follow-up).
Task 2: TWO new findings beyond the plan's own hard precondition: (1) the "seventh bypass shape" (task 1's own state-PR finding) recurs at the CONTROLLER level — an `object.__new__`-bypassed screen calling ANY mover (directly, or indirectly through an excluded method's own internal call) needs `_ingest_controller` seeded too, not just `_ingest_state`; fixed via a shared `wire_bypass_ingest_controller(screen)` helper in test_library_shell.py + 25 call sites across 5 files, same GREEN commit. (2) instance-attribute-monkeypatch bypass (recipe §3 shape 2) hit SIX names, not the zero expected going in — `_build_library_ingest_state`, `_notify_library_ingest_warning`, `_update_library_ingest_gate`, `_refresh_library_ingest_canvas_preserving_context`, `_update_library_ingest_dynamic_regions` (all seeded by test_library_ingest_inline_consent.py's shared `_minimal_library_screen()` fixture) and `_library_ingest_job_by_id` (found independently in test_library_screen.py) — each required excluding the method and re-deriving the mover count (78→63→62→59→58→57 across 5 correction rounds). Also found: `_apply_library_ingest_backend_save` forwards bare `self` into the shared `_sync_library_canvas` dispatcher, needing `is_running` + `_library_canvas_projection_depth`/`_library_canvas_resync_pending` added as bindings (skills/RAG's own identical precedent) — a first draft without them silently fell back to a full-screen recompose a real Pilot test asserts against.
Task 2: byte-for-byte canon verified two ways: AST-derived body extraction from the pre-move tree matched the controller's own bodies exactly (0 mismatches across 57 movers), plus the wiring test's own same-name-delegator-forwarding check.
Task 2: full battery green — wiring RED→GREEN, all 9 characterization tests, both ratchet rows (screen 41520/1302→40096/1302; controller born-governed at 2510 lines), `-k "ingest and library"` (1303 passed/7 pre-existing, all matching task 1's own documented list exactly), full `Tests/Architecture/` (551 passed/16 failed, all 16 confirmed pre-existing via isolated pristine-baseline reruns — one NEW-looking diagnostic-inventory drift traced to the moved `_resolve_ingest_source` body's own `logger.opt(...).warning(...)` call relocating verbatim, fixed by regenerating Docs/security/production-diagnostic-inventory.json in the GREEN commit), preflight all green. Full sequential xdist paired-baseline sweep in progress.
Task 2: full sequential xdist paired-baseline sweep (`Tests/UI -k "library"`) via isolated `git worktree` at 74a6f5774: branch 358 failed/3992 passed vs baseline 356 failed/3989 passed; 8 branch-unique, all resolved (6 clean on combined single-process re-run, 2 confirmed pre-existing -- one already in recipe §7's list, one new, confirmed via true-isolation reproduction on the pristine baseline); 6 baseline-unique, not investigated per established precedent. Zero real regressions. Recipe §7 gained the new sweep-evidence entry.
Task 2: implementer DONE (commits 74a6f5774..2b790783b: 44ab7383b RED, 68a896993 GREEN, 18e9c60f7 blame-ignore, 2b790783b sweep-evidence docs); reviewer dispatched.
Task 2: review round 6 (mandated exhaustive module-globals census) — 1 CRITICAL + 2 Important + 2 minors. CRITICAL: `_resolve_ingest_source` reads bare `validate_path_simple`/`validate_url`; `Tests/UI/test_library_shell.py::test_library_shell_ingest_canvas_invalid_path_notifies_and_submits_nothing` patches the screen-scoped name and went green-but-vacuous once the body moved (a nonexistent tmp path fails the REAL validator identically to the stub, so assertions pass either way). Important: `_apply_library_ingest_backend_save`/bare `_sync_library_canvas` — same census, confirmed LATENT (zero of ~20 patch sites across 7 files are ingest-related), verdict KEEP recorded. Important: recipe gains the mechanical module-globals census as a new numbered ("eighth") bypass shape. Minors: ratchet arity re-verify via `inspect.signature` (37->38), name `test_parakeet_v2_install_ui.py` explicitly in sweep evidence.
Task 2: fix round 1 DONE — reverted `_resolve_ingest_source` to LibraryScreen byte-for-byte (re-verified vs `74a6f5774` original), removed from controller + dead imports (logger/validate_path_simple/validate_url), added named-dependency binding for its one mover caller; mover count 57->56, exclusions 21->22. Confirmed via an existing-file probe (not committed) that the screen-scoped patch is observed again through both the direct call and the controller-forwarded call. `_apply_library_ingest_backend_save` kept as mover with documented latent-risk verdict. Recipe's new "eighth bypass shape" entry added (4-step method + both worked examples). Both ratchets fresh-pinned (screen 40096->40131; controller 2510->2536, arity 37->38 verified via inspect.signature). Diagnostic inventory regenerated a second time (same digest, owner moved back). Full battery green: wiring, characterization, inline_consent (130 passed total besides the 2 documented chat_screen ratchet failures), `-k "ingest and library"` (1303/7, same 7 pre-existing), full `Tests/Architecture/` (551/16, same category bucket), `test_parakeet_v2_install_ui.py` (25/25), preflight all green. Commit: fix(library): exclude _resolve_ingest_source (module-globals patch bypass); mechanical globals census (fix round 1).
Task 2: complete (commits 74a6f5774..<fix-round-1-sha>). Report: .superpowers/sdd/2026-09-05-library-decomposition-wave5-ingest/task-2-report.md (§12 fix round 1 appended).
Task 2: implementer DONE (commits 44ab7383b/68a896993/18e9c60f7/2b790783b; 57/78 moved, 21 excluded across 5 battery-driven correction rounds; 5 precondition pins in RED; zero real sweep regressions); reviewer dispatching (opus)
Task 2: review Needs Fixes — mechanics 57/57 verified; ROUND 6 FOUND by the reviewer's mechanical module-globals census: 1 CRITICAL (_resolve_ingest_source bypasses a validate_path_simple test stub — green-but-vacuous test) + 2 Important (latent _sync_library_canvas same-shape; census must become a numbered mechanical recipe shape) — fix round 1/5 dispatched (exclude+late-bind remedy)
Task 2: cross-wave follow-up flagged: the module-globals patch-bypass latency may exist in prior waves' controllers (canvas_sync direct imports) — census across all six controllers to be filed at wave close, not fixed here
Task 2: fix round 1/5 (all addressed; byte-compare + live probe verified; pins exact 40131/1302 + 2536; commit e3d85ad21)
Task 2: complete (commits 74a6f5774..e3d85ad21, review clean after 1 fix round — 56 moved/22 excluded final)
Task 2: minor carried to Task 3: the module-globals census evidence file-list undercounts (multi-line patch sites in test_library_canvas_scoped_sync.py + 2 other files missed from the listing; no active collision) — correct the recorded census in report/recipe during cleanup
Task 3: dispatched (implementer sonnet), BASE=e3d85ad21 — ingest cleanup (series 3/3)
Task 3: DONE — censused before deleting, per the mandated order. Dynamic-dispatch census: zero hazard shapes (Ingest never intersects the reader-preference dispatcher or any choice-strip dict); found 6 `getattr(self, "_library_ingest_<field>", default)` sites needing the skills series' own receiver-fix. Screen-side retarget: 37 pre-existing flat occurrences (31 attr + 6 getattr) across 12 still-screen-resident excluded/shell methods, all retargeted to `self._ingest_state.<field>`. Test retargets: content census (never `-k`) found flat-name mentions in 19 files; 2 were false-positive-only; the remaining 17 needed 297 attribute-path retargets + 3 `SimpleNamespace` fixture restructurings (4 flat kwargs) — zero assertion values changed. Shim block deleted once zero remaining consumers confirmed. Delegator census: 6 of 56 pruned (0 external references each, double-verified) — `_adopt_library_ingest_path`, `_ingest_job_id_from_button` (the cluster's 1 staticmethod), `_library_ingest_restage_discards_work`, `_restage_library_ingest_last_submission`, `_set_library_ingest_panels_collapsed`, `_update_library_ingest_retry_label`. 9 dead imports removed; 1 more (`_ingestible_file_filters`) found `_SURFACE`-pinned and kept — NOT the first such collision (self-review caught an overstated "first" draft claim; the conversations exemplar's own Task 7 hit the identical shape first, recipe §11). Fixed the carried census-listing minor (7 files/~20 sites → true 10 files/38 sites, 3 files missed for using a non-canonical import alias). 4 docstring corrections in the controller + 2 more in the wiring test + 2 in other state-module docstrings. Wiring test finalized (shim-surface test deleted, pruned-delegator/staticmethod skip+absence pairs added). Fresh pins: screen 40131/1302 → 40094/1296 (1296 = 1302−6, pure deletion); controller 2536 → 2558 (comment-only). Full battery green: 6 wiring suites + 4 characterization files + both ratchets + support-layer surface (131 passed/2 pre-existing chat_screen), `-k "ingest and library"` (1143 passed/7 pre-existing, all matching prior documented list), 16-file targeted battery (488 passed/9 failed, 8 confirmed pre-existing via `git stash -u` + 1 already-documented Notes name), `test_library_screen.py` standalone (32/32), full sequential xdist paired-baseline sweep via isolated `git worktree` at e3d85ad21 (branch 356/3994 vs baseline 358/3992; 351 shared, 5 branch-unique all resolved on combined rerun, 7 baseline-unique not investigated), preflight all green. New forward finding: `fd_leak_sentinel` flagged a 274-fd growth on a standalone `test_library_shell.py` run, plausibly explaining the massive shared `test_library_note_*` DOM-timeout burst both sweep trees developed identically near their tail — recorded as a lead, not chased. Self-review caught two mistakes before finalizing (missing test_library_screen.py/test_library_shell.py from the targeted battery; the overstated "first `_SURFACE` collision" claim) and fixed both in place. Report: .superpowers/sdd/2026-09-05-library-decomposition-wave5-ingest/task-3-report.md. Recipe gains §20 (ingest series as landed) + two new §7 sweep-evidence entries + the corrected §3 eighth-bypass-shape census listing.
Task 3: complete (single commit, ingest series 3/3 — a cleanup PR needs no RED/GREEN split). Wave-5 (ingest) series is now fully landed: state PR (task 1) → controller PR (task 2) → cleanup PR (task 3), matching the export/collections/search+RAG/skills precedent shape exactly.
