# SDD ledger — plan: Docs/superpowers/plans/2026-09-08-library-decomposition-wave8-notes.md
Wave-8 (notes) SDD start — THE FINAL EXTRACTION WAVE. Branch refactor/library-decomp-wave8-notes off origin/dev (post-wave-7 merge aecb14720). Worktree: .worktrees/library-decomp-foundation (same venv).
Pre-flight conflict scan (4 tasks, established shape):
| pair | producer/consumer | finding |
| T1->T2 | LibraryNotesState + shims -> controller bindings | consistent; media template |
| T2->T3 | delegators/bindings -> retargets/prune | consistent; three-member whitelist + five spellings |
| T2 canvas_sync branch -> T3 census | branch added in move commit, guard beside precedents; census counts it | consistent |
| T1..T4 self-consistency | batteries name the guard set as of wave 7 | consistent |
| plan vs rubric | no vacuous-test or duplication mandates | clean |
Wave-specific risks pinned: the three wave-7 inheritances (on_<message> enumeration with whitelist evidence; canvas_sync third branch + guard; shared-seam fixture census); four-member shell family stays whole; four prior-extracted wiring modules; sync/workspace seams; id-collision risk HIGH at filing time. No conflicts requiring pre-execution rulings.
Task 1 (notes state PR): COMPLETE per implementer. Commits f2b6de768 (RED: state + wiring + 3 characterization pins) -> ec7318181 (move: 100 fields, shims, seed, re-pin) -> 2641ff0a0 (blame-ignore). Ownership 105 = 100 MOVE / 3 WIRING (import controller, sync controller, note_session; the work-session module is a pure reducer so its 2 attrs are data and MOVE) / 2 BLOCKED (_library_notes_programmatic_focus_target + _library_notes_restoring_focus — media controller binds each with a SETTER; the line drawn: cross-subsystem READ = route guard -> moves [compact/source/focus_intent_generation on the mutation_in_flight precedent]; cross-subsystem WRITE = co-ownership -> blocked). Four-member shell family: NO notes twin exists (re-derived live) — ruled inapplicable with a pinning test. FOUR prefix families + two reader destinations; _library_file_notes_ marker kept (three pairs would collapse otherwise) — dedicated test. Battery 116/4 documented; paired isolated sweep 220 shared / 0 baseline-unique / 1 flaky-both-trees (6/10 vs 3/10 interleaved) — zero real. DISCLOSED-NOT-BANKED: the move flips a pre-existing red GREEN (test_notes_sync_cutover's literal-spelling AST census can't see shim-composed names; the field lives one indirection away) — recorded in module docstring + commit message + report; retarget belongs to the cutover work. Deliberate deferral (checked precedent first): on_screen_suspend flat string loop stays until the shim-DELETING commit (task 3), matching ingest/prompts/media's own sequencing — task 3 owns the explicit block + two assertion retargets. Hand-offs enumerated: canvas_sync :227 notes branch (task 2), library_inspection_admission._SCREEN_FIELDS, two reader-preference dispatch dicts, canvas_sync:456 partial(getattr,...), 15 refs in library_unavailable_navigation.py. 3 new §7 rows for task 4. Dispatching task review.
Task 1 review: SPEC PASS + APPROVED (no commit changes). Reviewer reproduced everything incl. a both-trees runtime dump of all 105 attributes (byte-identical) and re-derived the 2-BLOCKED set exactly via its own writer census. Plan premise OVERTURNED with evidence: the four-member shell family has NO notes twin (plan was wrong; implementer's pinning test correct). Three Importants, all forward-binding: I-1 the read/write ownership line silently covered 7 MORE cross-read fields than disclosed (all verified writer-free; verdicts stand) — TASK-4 MUST amend recipe §2's literal never-moved text with the refinement three waves rely on implicitly; I-2 a notes-named shared-seam writer DOES reach a bypassed fixture (inline-consent :242 wires _apply_library_notes_stage_visibility via the ingest controller; writes a moved field; no live failure yet) — TASK-2 MUST seed _notes_controller when that method moves (shape-7-at-controller-level); I-3 the flipped-green cutover guard becomes PERMANENTLY vacuous after shim deletion (attr invisible to startswith census; honest retarget keeps it RED — legacy timer still exists) — TASK-4 MUST file the backlog task; disclosure-without-retarget UPHELD as the right call. Minors 4-12: report count errata (66 not 105; 12-not-16 glob; the folder_new/folder_restore disposition asymmetry disclosed-and-pin-kept; 10 default_factory; 56/25 hand-off census with method; 4-write-1-read; 13-constants exclusion) + RowSelection dead-import hand-off + plan-vs-report canvas_sync timing reconciliation (plan wording was wrong; shim-deletion timing per the media precedent). Fix round dispatched (report-only edits expected).
Fix round 1: COMPLETE, report-only (HEAD unchanged at 2641ff0a0; zero tracked files touched). All items re-derived against live files; implementer additionally CORRECTED THE REVIEWER twice with evidence: (a) folder_new is ALSO unbound-reached (test_library_notes_reader.py:280) so neither folder handler was a genuine gap — pin kept, relabelled dispatch-path, asymmetry disclosed; (b) hand-off census is 57/16 not 56/25 (scoping: 100 moved names vs 105 candidates; the wiring file's 5 extra are f-string-built, invisible to ast.Constant) — all three values recorded WITH methods and task 3 ordered to re-derive rather than inherit. Ruling: ACCEPTED without scoped re-review — no tracked diff exists to review (report is git-ignored evidence); both reviewer-divergences resolved by measurement with methods stated; the meta-rule (downstream tasks re-derive, never inherit disputed counts) is the durable fix. Guards re-run 10 passed.
Task 1: complete (commits f2b6de768, ec7318181, 2641ff0a0; screen 35777/1290 -> 35621/1290; 100 fields moved; zero real regressions)
Task 2 (notes controller move — THE FINAL MOVE): COMPLETE per implementer. Commits afaf2320c (RED 6f/9p proven) -> 9e13f0207 (move: 186) -> a1bdd25a0 (blame-ignore) -> a529dbd3e (fix round 1: ONE MOVER REVERTED — partial(LibraryScreen._x, self, ...) defeats the direct-call-argument census; SIXTH §3 spelling established: census every LibraryScreen.<name> ast.Attribute in-file [27 targets, 22 notes, old census found 20]; two path censuses retargeted) -> 56999dbf6 (blame-ignore) -> 6f50060f7 (docstring 26/23 -> 27/22 + re-pin). Final: 185/285 moved, 100 excluded across 11 disjoint classes; SINGLE controller (components 154+9+2+31 singletons, no seam); screen 35621/1290 -> 32325/1284; controller born 5216. Plan's on_<message> prediction DISPROVEN both directions (36 Message-typed handlers, zero name-dispatched; 113 handler_names in Widgets/Library — only 4 builtins + 7 prompts match; §4's third member inert here, PINNED by test). Dev shipped a byte-identical 6-method duplicate block — both copies collapsed to one delegator; "method count unchanged on pure moves" no longer universal (1290->1284: -6 duplicates... reconciled in report). Hand-off (a) DISCHARGED as unnecessary (_apply_library_notes_stage_visibility doesn't move — real-screen monkeypatch; __new__ battery matches task-1 exactly). NEW task-3 obligations: canvas_sync dotted branch must resolve on TWO receivers (31 movers hand the dispatcher a controller self — screen-only guard passes vacuously); cutover guard naively retargeted false-positives on the sync-controller binding accessor. Battery 74+15 wiring green; paired sweep 220/911 IDENTICAL both trees zero unique; 1 new pre-existing red (tier1_toggle, parent-proven). Dispatching task review.
Task 2 review: COMPLIANT + FIX REQUIRED (5 Important + 8 Minor, all evidence-accuracy; the MOVE verified byte-perfect over the WHOLE set — 185/185 movers, 100/100 exclusions, zero non-mover changes, zero module-level changes, __init__ purely additive; on_<message> disproof RE-DERIVED end-to-end and upheld as a legitimate plan overturn with a both-directions pinning test; duplicate-block collapse ruled CANON-COMPATIBLE — the second copy already won at class creation, collapse forced not discretionary, disclosed three places; thread-path conclusion upheld via the reviewer's own widened 1,419-site sweep; hand-off (a) discharge verified; both-directions binding re-run: 0 unresolved, 0 setter-less stores). Importants: I-1 ratchet narrative lines :769-770 contradict the pin (stale 32286/-3335 beside correct -3296/32325 — fix round updated terms, missed the narrative); I-2 stale 103->102 binding count; I-3 split evidence computed over 196 (movers + class-2), not 185 — verdict unaffected, figures must describe their set (true 185 profile [142,9,3,2]+29); I-4 callback-identity exclusion justified with a WRONG mechanism (captured callback is a closure; cited test red at parent, already §7 :1121) — restate on the conservative rule, "a wrong reason is worse than a thin one"; I-5 the "NEW §7 red" is tier1_toggle reported new for the THIRD time (recipe :1062/:1594/:5937; :1594 exists BECAUSE wave 7 made this mistake) — withdraw + drop hand-off + meta-note: reason from §7's list, not from what earlier tasks ran. Minors 6-13 incl. the thread-census spelling gap (call_soon_threadsafe invisible to call_from_thread grep — recipe flag for wave close). Fix round 2 dispatched.
Task 2 review addendum: the reviewer's outstanding sweep-batch-3 corroboration finished — 26f/479p, EXACTLY the report's figure (whole sweep table now reproduced, not just arithmetic-consistent). It also identified the MECHANISM behind I-4: the class-11 cited test was inside the swept set and absorbed into the shared both-trees red backdrop, so its already-red status never surfaced as branch-unique — the same absorption mechanism §7:1062 warns about for tier1_toggle (I-5). Verdicts unchanged; fix round 2 already in flight with the right instructions.
Fix round 2: COMPLETE (991758c1f, doc-only + §17 re-pin 5216->5254). Scoped re-review: ALL ADDRESSED (8/8 checks; all four component profiles set-labeled plus a fifth isolating the literal-getattr spelling as the sole instrument delta; Form-E claim fully withdrawn; both meta-note elements present; doc-only confirmed with the single pinned integer). One trivial residual OUTSIDE the defect list — the :17651-17655 citation off by one (real span 17652-17656) — fixed directly by coordinator in both the controller docstring and report (same-length edit, line count 5254 unchanged, import verified; the line-ranges-are-numbers lesson's third strike, even in a fix round ABOUT citation accuracy).
Task 2: complete (commits afaf2320c, 9e13f0207, a1bdd25a0, a529dbd3e, 56999dbf6, 6f50060f7, 991758c1f + coordinator citation fix; screen 35621/1290 -> 32325/1284; controller born 5216 now 5254; 185 movers byte-perfect; zero unique failures)
Task 3 (notes cleanup — the FINAL cleanup): COMPLETE per implementer. Commits 3d670ee38 (cleanup: 571 screen + 43 getattr-receiver + 4 dotted; 1,031 test retargets/26 files; shim deleted 0/100 screen 100/100 controller; 26/185 pruned 14.05%; 11 dead imports 1 _SURFACE-saved; 2 modal rows construction-proven; canvas_sync notes branch + DUAL-RECEIVER guard) -> d3528342c (blame-ignore) -> 9d8244c72 (fix round 1: REAL 3-test regression caught by the paired baseline — three media-trash fakes needed _notes_state seeds; the bidirectional-unique-split disposition lesson) -> 4aca7171a (blame-ignore) -> 366428cd0 (report). Screen 32325/1284 -> 32230/1258; controller 5276. Battery 144/6 all §7-by-name; paired baselines byte-identical failure sets, zero unique post-fix; preflight green. MAJOR FIND (MUST FILE at task 4): live silent MEDIA defect — canvas_sync.py:437 assigns screen._media_state.selected_media_id but library_media_controller.py:2692/:2701 hand the dispatcher a CONTROLLER self with no _media_state (runtime-verified); AttributeError swallowed into full-screen recompose — every media Select-all/Clear silently takes the fallback wave 7 exists to avoid; four-line remedy, correctly NOT fixed here (cross-subsystem behavior fix). Cutover guard permanently vacuous re-verified — filing not fixing. Recipe additions queued: sixth spelling, fifth-spelling RECEIVER dimension, two-pass unused-import lesson, per-binding-site + getattr-receiver census holes (hid 4 of 6 seeds), and the disposition lesson (a bidirectional unique split is not a disposition — run §7's third level; it hid the real regression here). Dispatching task review.
Task 3 review: COMPLIANT + FIX REQUIRED (1 Important + 4 Minor, all doc/count; ZERO code defects). Reviewer reproduced the media defect END-TO-END at runtime (controller self -> AttributeError -> swallow -> full recompose, confirmed by execution; pre-existing from wave 7's own dotted retarget, media controller byte-untouched by this task); mutation-verified the dual-receiver guard BOTH directions in a table; ran its own inverse transform over all 40 files (20 exact, 20 = declared hand edits at word level); re-derived the prune set in full (26/26, zero external refs); traced the fix-round regression mechanically. Findings: I-1 the two errata were fixed in the report but NOT the live recipe (§8 row describes a third non-existent set — the very conflation §22 warns about; nestings 11 vs true 12); M-2 "31 movers" is 31 SITES across 26 movers (ships in 6 places); M-3 a false supporting claim (on_unmount DOES contain a retargeted name; conclusion survives on byte offsets); M-4 three class-3 prose survivors dropped by the ±3 filter; M-5 "goes EMPTY" is actually a 2-element set + a wrong declares-the-third attribution.
RULING (recorded): the media defect gets FIXED in task 4, not just filed — reviewer's recommendation adopted (3 lines + guard leg; program closes after task 4; the defect silently fires the exact recompose phase C exists to eliminate) — as its own clearly-labeled behavior-fix commit, mutation-verified both ways, surfaced in the PR body as the wave's ONE deliberate behavior change (wave-5 ported-edit precedent for behavior-carrying commits inside a decomposition PR). Fix round 2 dispatched.
Fix round 2: COMPLETE (652503886, doc-only, line-neutral on both tracked modules). Scoped re-review: ALL ADDRESSED (8/8 — byte-offset argument independently re-derived at both trees; the THREE-element mutation set reproduced verbatim; all seven sites-vs-movers copies confirmed; visible-correction pattern verified at both recipe spots; the task-4 FIX ruling recorded with ingredients + both mutation directions). Re-reviewer also flagged an instrument note: the repo's grep shell function silently skips .superpowers/ — naive greps pass trivially there; `command grep` needed for real searches (wave-close lesson candidate).
Task 3: complete (commits 3d670ee38, d3528342c, 9d8244c72, 4aca7171a, 366428cd0, 652503886; screen 32325/1284 -> 32230/1258; controller 5276; 26/185 pruned; the live wave-7 media defect found, reproduced, and ruled FIX-IN-TASK-4; zero unique failures)
Task 4 (wave close + PROGRAM CLOSE): COMPLETE. Commits 66e572868 (THE MEDIA FIX — shipped RED first, mutation-verified both ways, re-pin 4630->4669, no blame-ignore; two media suites 5/190 identical with/without) -> 96409c16e (prose sweep: 11 class-3 sites, only 3 known — incl. a state module cross-referencing its own siblings by pre-move spellings, invisible to the ±3 filter; moved-body comments deliberately left per canon) -> bad16ab8f (TASK-32040/32041 + TASK-31249 block; ids provisional — CLI offered 32014 already held on an unmerged branch) -> 9381fdad3 + 12ac59b45 (program close: trajectory, lessons, phase-C handoff, every wave-boundary commit named) -> 841261666/36088f222 (evidence). PROBE REVELATION: five trees, 118 runs — identical HEAD source measures +23ms in the working worktree vs -6ms in a scratch worktree; §3's baseline-at-scratch-path + branch-at-working-worktree means EVERY probe pair for eight waves compared two FILESYSTEMS; §9 now requires same-filesystem pairs; corrected pair: branch equal-or-faster on all eight rows, recompose 0 across 118 runs. PROGRAM TOTALS re-measured: 45,134/1,300 -> 32,230/1,258; waves removed 17,544 lines, dev gave back 4,640 between them (26.4%); NINE state objects, TEN controllers (brief said 8+8 — corrected), 368 fields, 795 methods, 154 pruned + 6 collapsed. Three brief figures corrected in artifacts (born 5232 not 5216; 9/10; :957-977 — §24 citation ledger's fourth entry). Battery all §7-by-name; paired sweep 397-vs-397 zero real through the full disposition ladder; preflight green; pins exact. Concerns: TASK-31880 should close before phase C (it gates the exact path); full-update pattern drifted on dev between waves (recorded so the next close doesn't re-derive it).
ALL WAVE-8 TASKS COMPLETE — THE EXTRACTION PROGRAM IS CLOSED. Dispatching final whole-branch review (fable).
FINAL REVIEW (fable): MERGE-READY WITH CONDITIONS — "strongest shape of any wave": all 16 program-close boundary measurements re-derived to the digit (spot-check exceeded); behavior-fix mutations re-verified both directions; fix-not-file ruling UPHELD end-to-end vs the pure-move doctrine (fix not blame-ignored — correct, blame must see it); new canon (§2 read/write rule, §25 duplicate-collapse with distinct-name invariant) judged durable; probe §9 rule runnable and §25 baseline reproduced row-for-row. Conditions: CRITICAL — TASK-32041 collision FIRED (dev's 553960448 minted its own 32041, 973b4c039 already closed it; filenames differ so git merges silently, duplicate-id CI fails post-merge) — renumber + 3 refs + visible SDD errata (32040 still free, re-sweep at merge); IMPORTANT — dev's task-32045 query() call in the media leg reds BOTH guard legs (PROVEN by splicing) — fix is a query stub per leg, NEVER weaken the refresh-raises assertion (it is the fix's entire mutation surface); also flag to dev: their own single-leg guard is red on origin/dev TODAY by the same mechanism; IMPORTANT — "two filesystems" label FAILS its spot-check (same device /dev/disk3s5) — phenomenon real, rule sound, label must become "checkout location" per the program's own wrong-reason doctrine; Minor — "six of the seven" gap count (true: five positive, one zero, one negative). Reconciliation: 87 commits, hotspot test_library_shell.py (dev +80 vs our 339), zero flat notes names in dev's additions — textual not semantic. Merge-reviewer key check recorded: re-run both mutations post-reconciliation; if either stops redding exactly one leg, the reconciliation broke the one deliberate change.
Ruling: ONE fix+reconcile dispatch (opus): renumber (fresh id sweep), relabel, errata, then merge origin/dev with the guard-preserving query stubs + standard catchup + battery; controller spot-checks then push/PR/converge per standing instruction.
Fix+reconcile: COMPLETE, MERGE-READY (19e5aa858 renumber+errata -> 07cf9d3f1 merge of 99 dev commits ZERO conflicts -> 81845a1ba guard query stub + re-pin 32230->32263 -> 7ce19d248 report). CRITICAL catch: the reviewer's suggested id 32040 was held by THIS BRANCH's other filing — a second collision avoided; all-refs sweep (655 refs) -> TASK-32047, older-keeps-id per TASK-19601; latent 32040 TRIPLE collision on two unmerged codex branches reported. Both mutations re-red exactly one leg each post-merge; refresh assertion untouched. This merge FIXES two dev reds (their single-leg guard; their screen ratchet 35810-vs-35777) — branch carries 4 ratchet reds where dev carries 5. Newly surfaced dev debt recorded not argued: test_library_shell.py 227/842 red ON DEV ITSELF (5/6 chunks name-identical; the sixth's two names dispositioned by five interleaved pairs with the parent worse on one) -> TASK-31249 census; media_render_fixes 11-red; a new unowned preimport red. Process lesson recorded: an 842-test single run died at 89% with no summary — partial F-stream would misreport as regression; ~141-test chunking produced comparable numbers. Probe mechanism now honestly UNIDENTIFIED. Controller spot-check green. Pushing + PR + converge — THE PROGRAM'S LAST LANDING.
Round-2 catch-up (resolved INLINE): 30 dev commits; sole conflict the diagnostic inventory (drift read: our own 8-diagnostic screen->controller relocation, total conserved; regenerated per protocol). Preflight then caught the collision striking TWICE MORE: dev's 30 commits minted BOTH 32040 and 32047 — both our filings collided in one endgame. Renumbered ours (32040->TASK-32088, 32047->TASK-32089) under two new rule refinements now in lessons-backlog-hygiene: LANDED-KEEPS-ID trumps older-keeps-id (a task on origin/dev never renumbers); at dev's minting rate an id chosen before a multi-hour reconciliation is stale by construction — renumber as the LAST commit before push. Dev's identically-numbered tasks + their references (lessons-testing-evidence:12295, decisions/029) untouched. Id check + preflight green.
