# SDD ledger — plan: Docs/superpowers/plans/2026-09-06-library-decomposition-wave7-media.md
Wave-7 (media) SDD start. Branch refactor/library-decomp-wave7-media off origin/dev (post-wave-6 merge 761416317). Worktree: .worktrees/library-decomp-foundation (same venv).
Pre-flight conflict scan (4 tasks, waves 2-6 shape):
| pair | producer/consumer | finding |
| T1->T2 | LibraryMediaState + shims -> controller bindings | consistent; prompts template |
| T2->T3 | delegators/bindings -> retargets/prune | consistent; prune whitelist now 3-member (incl. on_<message>) |
| T1..T4 self-consistency | batteries name the guard set as of wave 6 (8 wiring suites at close; preimport suffix added in move commit) | consistent |
| plan vs rubric | no vacuous-test or duplication mandates | clean |
Wave-specific risks (plan Global Constraints): split decision (~244 methods — one controller would be ~8-9k lines; two-series or gate contingencies pinned); suspend/resume seam (TASK-31521 fields screen-owned, accessor-at-most); standing media_browse dev-red 410-vs-371 (absorb-only-if-touched rule); phase C explicitly fenced out. No conflicts requiring pre-execution rulings.
Task 1 (media state PR): COMPLETE per implementer. Commits 600192d6e (RED: state+wiring+characterization) -> 76b248c05 (move: 82 fields, shims, seeds, re-pin 37537 -> 37333/1282, delta -204 reconciled) -> 23e442ad9 (blame-ignore) -> 999bd269d + 27d3efdea (self-caught errata, doc-only). Ownership: 85 attributes (plan's ~122 counted 39 method refs + 1 class-body attr) = 82 MOVE / 2 WIRING (the two prior-extracted browse controllers) / 1 BLOCKED (_library_pending_list_entry_media_return — shell four-member family, shared arm/disarm writers naming 4 subsystems). Suspend/resume seam ruled field-by-field: on_screen_suspend touches NO media field (methods via shim; string loop lists no media name); Timer/Worker HANDLES move per ingest/prompts precedent checked in live modules. Characterization: 79 handlers, 3 genuine gaps pinned, webbrowser.open patched on module so pins survive task-2 move; 0 on_<message> handlers (plan prediction wrong — recorded). Three paired sweeps zero real branch-unique. Flags: task-3 retarget is program's largest (1,214 BOUNDARY-MATCHED occurrences / 36 files — substring count 1,426 is WRONG and would corrupt method names); media_browse red left flagged (no controller file touched); 3 new §7 reds (2 return-settlement geometry inside the moved cluster + persona_visual, all parent-proven); near-miss lesson — a bare-# comment sweep deleted 8 unrelated lines, caught by DIFF-READING not tests, rewritten walk-upward-with-raise-guard. Dispatching task review.
Task 1 review: PASS + APPROVED — first straight approval of the program (no fix round). Reviewer reproduced RED in an isolated own-venv worktree, verified byte-for-byte for ALL 82 fields (AST + dual-tree runtime dump, 0 diffs), mutation-proved the literal-mapping test and one characterization gap, traced the suspend/resume shim path, upheld the BLOCKED ruling with the writers/docstring evidence, and confirmed ZERO unrelated deletions from the near-miss sweep. Findings: I-1 (FORWARD-BINDING on task 2) getattr census under-count 11 -> 13 attribute-naming sites (grep missed line-wrapped calls; two named misses incl. one MOVED field) — task 2 must AST-re-derive and bind 13; M-1..M-6 doc-accuracy (two in tracked files — blame-ignore 78/75 -> 77/74 and the screen-reuse comment four -> three-of-five — fixed directly by coordinator, one word each, ledgered; rest as report erratum). Ruling: no fix-round dispatch — reviewer required no code change; direct coordinator edits proportionate (two words in docs/comments) per the wave-6 precedent, erratum appended to the report.
Task 1: complete (commits 600192d6e, 76b248c05, 23e442ad9, 999bd269d, 27d3efdea + coordinator doc fix; screen 37537/1282 -> 37333/1282; 82 fields moved; zero real regressions)
Task 2 (media controller move): COMPLETE per implementer. Commits 3dd7a745a (RED 5f/6p isolated-proven) -> 9187bd281 (move) -> e97ac3a32 (blame-ignore) -> 40fe1f5bc (fix round 1: one mover reverted) -> 7fee56dd4 (blame-ignore) -> 1cf379d6c/9cc6a7ad4/e1f893b7c (docs). SPLIT DECISION: ONE controller — self.<name> graph gives a single 213-of-251 component (+28 singletons; 238 with state-field edges); browse-vs-trash candidate seam only 16 cross-edges/6 shared fields, 19 trash-named movers. 140 MOVE / 111 EXCLUDE (73 unbound-fake-self, 16 instance-attr-monkeypatch, 8 inspect.getsource, 4 ACTIVE module-globals, 3 screen-identity, 2 class-monkeypatch, 2 bypassed-construction lifecycle, 1 callback-identity, 1 shared shell helper, 1 generic dispatcher). Controller 4,461 born-governed; screen 37333 -> 34754/1282 (-2579 reconciled). I-1 honoured: AST getattr census 15 calls/13 attrs, both line-wrapped sites reproduced. THREE battery-found shapes beyond static census: (a) __new__-bypassed on_screen_suspend reaching movers via delegators; (b) NEW SHAPE — `self in widget.ancestors` membership-form screen-identity (is/is-not census scores it ZERO; 19-of-163 failures until fixed); (c) callback-identity where excluding the callee wouldn't help. Generalized replacement census: every bare-self ast.Name in moved bodies that isn't an attribute receiver — exhaustive, returns exactly 10 on the final set. Battery 117/3 (documented trio; media_browse untouched-left-flagged); full Architecture paired ZERO branch-unique; sweeps A/B/C zero real; probe order-swapped flat; byte-for-byte 140/140 text+AST. Task-3 flags: 111 exclusions stay full-bodied (imports not dead; _SURFACE exact-name UPPERCASE); library_media_state.py docstring one-phrase fix (_ANALYZE_ORIGIN_MEDIA now in screen_constants); retarget still program-largest (1,214/36/5 roots). Dispatching task review.
Task 2 review: PASS + APPROVED, no code change. Reviewer independently: reproduced RED (5f/6p isolated own-venv), byte-verified 140/140 (text+AST) + 111/111 exclusions unchanged + name-set identity, re-derived BOTH component graphs exactly (33/213 call-only; 13/238 with state edges; seam 16/6 exact), audited 13 exclusions ALL REAL (incl. the 4 zero-test-evidence ones justified source-side), verified the ADR-055 one-flag interlock guard intact, confirmed I-1's 13 getattr sites bound (11 screen + 2 controller), and ran its own paired sweep (34f/152p BOTH trees, zero unique). Findings (5, all doc): I-1 class-2 docstring claims a per-name mover-caller verification that doesn't reproduce — 7/16 have zero mover callers (exclusions still correct under §3's conservative whole-call-graph rule; the narrower stated condition is false precedent; the 7 are task-3 move candidates post-retarget); M-1 report §8 stale -3202 (true -3166, tracked files already right); M-2 trash-named 19 -> 20 (docstring self-contradicts); M-3 "exactly 10" bare-self census is pre-exclusion (final-140 = 7); M-4 a 4th ancestors-membership method exists (filed under class 2 — shape count for the recipe is 4); M-5 screen-ratchet comment "92 named dependencies" mislabels the binding-surface count (construction passes 83 kwargs; modules-ratchet comment already correct; 40fe1f5bc message carries the same mislabel — erratum-not-amend). Fix round 1 dispatched (resumed implementer, doc-only commit).
Fix round 1: COMPLETE (2325577f2, doc-only + the documented §17 same-commit controller re-pin 4461 -> 4496 for its own docstring growth). Scoped re-review: ALL ADDRESSED (9/9 — I-1 restated with the 9/7 split and an explicit false-precedent disclaimer, spot-verified zero mover callers for _request_library_media_type; both census figures paired; 4-shape count named; 83-vs-92 disambiguated IN the comment; §8 sums exactly; doc-only confirmed with the one pinned integer as the sole value change).
Task 2: complete (commits 3dd7a745a, 9187bd281, e97ac3a32, 40fe1f5bc, 7fee56dd4, 1cf379d6c, 9cc6a7ad4, e1f893b7c, 2325577f2; ONE controller ruling on 213-component evidence; 140 movers byte-for-byte; screen 37333 -> 34754/1282; controller 4496; zero real branch-unique)
Task 3 interruption: implementer killed by session limit MID-RETARGET — the risky shape: ZERO commits on base 78186d159 but a large dirty working tree (retarget partially applied across Tests/UI+Live+Media+Architecture, wiring test and screen ratchet modified, 38KB report draft). Resumed with the dirty-tree-is-ground-truth protocol: mechanically characterize the partial state (boundary census over the DIRTY tree per file), check for half-applied files (old+new spellings coexisting for the same name = silently-vacuous risk), finish, then commit; every verification without recorded output re-runs from scratch (the in-flight batch counts as never-run).
Task 3 (media cleanup): COMPLETE post-interruption (dirty-tree protocol held; implementer re-characterized the partial state and re-ran all unrecorded verification). Commits 5dd2e71cf (cleanup: 456 screen retargets + 9 getattr receiver fixes + 6 dotted strings; 1,182 test retargets/32 files/5 roots, residue prose-only; 37 SimpleNamespace fixtures restructured; shim block deleted; 22/140 delegators pruned 15.71%; 10 dead imports, 3 _SURFACE-saved; recipe §3 FIFTH SPELLING + §8 + §22; both guards re-pinned 34669/1260 — method drop 1282->1260 = 22 exactly) -> 946ea1b0a (blame-ignore) -> 1e8f4918 (report). Battery 284/3 (documented trio); three paired baselines vs pristine isolated worktree: 56 shared, 1 branch-unique passing in isolation, zero real. MAJOR FIND: FIFTH census spelling — canvas_sync.py builds field names at RUNTIME (f"_library_{kind}_row_selection"), all four documented spellings score zero, failure mode is SILENT (try/except swallows into full recompose); Notes hits the identical site in wave 8 — recipe §3 updated. Concern: the fix has no green test (the one real-row Pilot test is pre-RED both trees, byte-identical assert) — proven by construction + negative control; filing for the pre-existing red goes to task 4. The 7 move candidates correctly stayed excluded (cleanup retargets FIELD fixtures; their blockers are METHOD monkeypatches, still live — per-name verdicts recorded). Process erratum self-reported: add -A swept progress.md into the first commit attempt; reset --soft redo, diff-verified byte-identical, uncommitted. media_browse standing red untouched, third consecutive wave. Dispatching task review.
Task 3 review: SPEC PASS + FIX REQUIRED (2 Important + 4 Minor; landed code PROVEN correct — reviewer's inverse-transform AST comparison found parent ≡ current after undoing the declared transform, the strongest verification of the program; half-applied risk ZERO by full-Tests census; fifth-spelling find verified real with the silent-swallow mode reproduced + negative control; 7-candidates ruling upheld with spot-checked live blockers; ADR-055 interlock intact). Findings: I-1 the fifth-spelling fix lacks the green guard its own conversations precedent has (test_library_selection_updates.py:294-337 double-with-raising-refresh shape) — ~15-line media analogue is in scope, mutation-verify; I-2 §9.1's isolation disposition FALSE as cited (test fails 5/5 isolated on branch AND 3/3 at parent — conclusion survives on parent evidence, citation must be rewritten); M-1 "five roots" in FOUR durable artifacts is wrong (4 roots; Tests/Library has zero changed files) — blame-ignore prose + recipe fixed, commit-message erratum; M-2 40->41; M-3 stale line cites incl. recipe pointing wave 8 at canvas_sync.py:209 (live :219); M-4 present-tense deleted-field prose -> task-4 sweep hand-off. Fix round dispatched (resumed implementer; test addition + docs).
Fix round 1: COMPLETE (9275ee792 — guard test + docs). Scoped re-review: ALL ADDRESSED (7/7 — reviewer independently mutated the canvas_sync media branch: new guard FAILS with exactly the fallback assertion while the conversations precedent stays green under the same mutation, proving genuinely new coverage; restored clean; four-roots fix verified in all three durable artifacts; :219 citations verified live; doc-only confirmed). Second-order find from the fix round itself: honest §9.2 arithmetic exposed TWO never-run test files, now paired 1f/73p on both trees. Newly-surfaced §7 candidate: test_tier1_toggle_falls_back_to_recompose_on_query_one_failure (pre-existing at parent, verified by re-reviewer).
Task 3: complete (commits 5dd2e71cf, 946ea1b0a, 1e8f4918, 9275ee792; screen 34754/1282 -> 34669/1260; 22/140 pruned; 1,182 test retargets; fifth census spelling discovered, fixed, guarded; zero real regressions)
Task 4 interruption: host process exited mid-close (orphaned agent, transcript intact). State at resume: zero commits on 014f695fc; dirty tree (blame-ignore, recipe, 5 stale-doc/test files, both media modules); TASK-31880 (row-toggle blocked coverage) + TASK-31881 (media-route round-trip race) already filed untracked; 26KB report draft. Resumed under the dirty-tree protocol — interrupted runs count as never-run.
Task 4 (wave close): COMPLETE post-interruption. Commits f3ca373cc (TASK-31880 blocked-coverage + TASK-31881 route race) -> f9f81019c (stale-doc sweep + one sweep-found fixture seed) -> b7a170602 (recipe §3/§7/§8/§22) -> 38bef6d35 (durable evidence). Battery 301/5 (documented); guards 75; preflight green; Sweep A 365 shared/5 branch-unique all dispositioned zero real; Sweep B 23/0/0; probe order-swapped flat; pins exact 34669/1260 + 4496. Beyond-brief finds: (1) per-commit trajectory corrected two published figures (controller BORN 4474 not 4461; screen went 34717 -> 34754 inside task 2 — the program's only down-then-up); (2) a SHARED SHELL SEAM writes a moved media field from a Skills test's unbound fake — names no media field, assigns none; invisible to kwarg + Store-context censuses; seeded per seventh shape, proven byte-identical three ways; (3) TWO self-caught methodology failures: baseline venv at Python 3.11 vs branch 3.14.2 (both sweeps LOOKED plausible; §3 now demands python -V + uv pip list parity before reading any count) and a sequential 10-run batch verdict REVERSED by interleaved round-robin across three trees (now §7's third disposition level); (4) task-1's 1,214/36 vs close's 1,204/34 census figures IRRECONCILABLE (instrument difference, neither reconstructible) — both recorded, neither overwritten. Notes-wave inheritances flagged: on_<message> whitelist untested since prompts; third canvas_sync dotted branch; shared-seam hazard in other subsystems' files.
ALL WAVE-7 TASKS COMPLETE — dispatching final whole-branch review (fable).
FINAL REVIEW (fable): MERGE-READY WITH CONDITIONS. Branch sound: battery reproduced bit-for-bit (301/5 documented), canvas-sync guard mutation-verified by reviewer, byte-for-byte spot-checks AST-identical, suspend/resume traced live, ALL rulings upheld (one-controller at 44% honest-as-test-debt; the refusal correct; both-census recording the honest call with the nuance that task 3 reproduced task 1 exactly — the delta is instrument-vs-instrument; §3/§7 lessons verified RUNNABLE; all five erratum-not-amend instances consistent). Conditions all merge-side — LARGEST reconciliation of the program (292 commits): (1) 11 mover bodies edited on dev (109 lines; heaviest _sync_library_media_viewer_state 26) -> port each into controller; (2) ~49 new dev screen methods (16+ media-named) AUTO-MERGE cleanly with 15 screen + 47 test flat refs to moved fields — reads loud, WRITES SILENTLY SPLIT STATE, getattr-with-default silently defaults; census-driven port (all five spellings) over dev's drift mandatory; (3) ratchet rows conflict by construction -> fresh measure on merged tree (dev's media_browse row now 602-vs-371 — dev creep TRIPLED, refusal vindicated, PR-body pre-empt); (4) exclusion-debt backlog owner: extend dev's Library UI test-debt census task (origin/dev 880867297 extends it) with the 89 fixture exclusions + 7 candidates; (5) PR-body 7-item list incl. corrected ~201KB evidence size and 4 commit-message errata. Mitigations verified by reviewer: zero dev callers of pruned delegators; zero dev computed-name f-strings; zero dev uses of deleted imports; dev's canvas_sync hunk doesn't touch the fifth-spelling dispatch.
Ruling: ONE fix+reconcile dispatch (opus) mirroring wave-6's shape at larger scale; controller spot-verifies the ports post-hoc; then push, PR, converge per standing instruction.
Fix+reconcile: COMPLETE (3c11f902a merge of 306 dev commits + 2c4c4d709 debt-ownership). 11/11 ports BYTE-IDENTICAL (verified controller bodies still matched merge-base first, then wholesale replace); 8 new group-(e) bindings by AST (arity 83->91); unbound-attr walk []. Census caught 3 SILENT WRITES to deleted flat names — two in dev's new library_unavailable_navigation.py (every media deep link would have painted the wrong surface, no exception) — controller-spot-verified retargeted (:780-781, zero flat writes remain). Set check exact (merged-branch = dev's 29 new names; reverse empty); +957 reconciles vs dev's +988. Filed TASK-31976 (exclusion debt: 89 fixture shapes + 7 candidates + the bound-call census gap the battery caught + the fake-self-fixture-for-moved-name judgment call, both sides argued); TASK-31249 extended with the five wave-7 dev reds + cross-pointer. Judgment call ENDORSED by controller: retargeting dev's new fake-self fixture beats reverting a mover mid-merge (reshaping the pinned 140-set inside a reconciliation is the greater evil; reversal costs one test file if overruled). Standing items: media_browse refused FOURTH wave (410->649 vs 371 — trend is the problem, PR-body pre-empt); test_screen_navigation.py 32 identical names red BOTH trees — NOT flaky as wave 6 concluded, standing unowned dev regression (TASK-31881 covers the media route; the full set surfaced in PR body). Battery 101/4 all parent-proven with PROVEN venv parity (which immediately caught the editable-finder trap again). Pushing + PR + converge.
Round-2 catch-up: PR #2501 went CONFLICTING before the first convergence iteration — dev raced 75 more commits (fix/media-riders series, ACTIVE edits to the just-reconciled media cluster). Controller ran the merge (in progress, 3 files / 6 hunks: library_screen.py x4 [:15457/:15924/:31499/:31765], render_fixes x1, return_settlement x1) and resumed the round-1 reconciliation agent with the same procedure at smaller scale (port dev mover edits; merge test semantics; five-spelling census over the 75-commit drift; pruned-name check bound+unbound; fresh pins; battery with venv parity). Also noted: /tmp scratch was wiped by the earlier host crash (the convergence scripts died with it) — convergence loop will be rewritten to the session scratchpad after this round lands.
Round-2 catch-up: COMPLETE (16c45b545, 75 commits). All 13 ports across both rounds BYTE-IDENTICAL; 3 two-sided regions verified by inverse transform. Auto-merge-invisible catches: (1) module-globals coupling — dev promoted _library_media_int_backing_id to module level, ported body reads it as a bare global vs the controller's __globals__ (NameError on every Review-selected press if unbound; imported with comment) — the one finding a mechanical port would have shipped broken; (2) dev flat READ+WRITE auto-merged INTO an already-retargeted body with zero conflict markers (six task-31979 hits); (3) LIVE id collision — dev minted TASK-31976 within the 75 commits (the hygiene lessons' two-sessions-same-maximum failure mode reproducing exactly); tie-break applied, ours renumbered TASK-32013 with provenance. Battery: 62/0 wiring+characterization; dev's 9 changed media files 21f/652p IDENTICAL both trees (single unique cleared by interleaved round-robin 1/5-vs-2/5); preflight green. WARNINGS ledgered: reconciliation two-sided rate RISING (3/7 vs 1/18) with new classes per round — budget for the CLASS not the count; the binding pin is ONE-DIRECTIONAL (most likely future-defect site; fix shape on TASK-32013); media_browse 686-vs-371, refused FIFTH time, +37 lines in twelve hours BETWEEN reconciliations; both trash-return timing tests nondeterministic both trees — single runs mislead. Pushing + converge (loop rewritten to session scratchpad; /tmp proved volatile).
