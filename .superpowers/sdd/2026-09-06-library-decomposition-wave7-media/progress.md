# SDD ledger — plan: Docs/superpowers/plans/2026-09-06-library-decomposition-wave7-media.md
Wave-7 (media) SDD start. Branch refactor/library-decomp-wave7-media off origin/dev (post-wave-6 merge 761416317). Worktree: .worktrees/library-decomp-foundation (same venv).
Pre-flight conflict scan (4 tasks, waves 2-6 shape):
| pair | producer/consumer | finding |
| T1->T2 | LibraryMediaState + shims -> controller bindings | consistent; prompts template |
| T2->T3 | delegators/bindings -> retargets/prune | consistent; prune whitelist now 3-member (incl. on_<message>) |
| T1..T4 self-consistency | batteries name the guard set as of wave 6 (8 wiring suites at close; preimport suffix added in move commit) | consistent |
| plan vs rubric | no vacuous-test or duplication mandates | clean |
Wave-specific risks (plan Global Constraints): split decision (~244 methods — one controller would be ~8-9k lines; two-series or gate contingencies pinned); suspend/resume seam (TASK-31521 fields screen-owned, accessor-at-most); standing media_browse dev-red 410-vs-371 (absorb-only-if-touched rule); phase C explicitly fenced out. No conflicts requiring pre-execution rulings.
Task 1 (media state PR): COMPLETE per implementer. Commits 600192d6e (RED: state+wiring+characterization) -> 76b248c05 (move: 82 fields, shims, seeds, re-pin 37537 -> 37333/1282, delta -204 reconciled) -> 23e442ad9 (blame-ignore) -> 999bd269d + 27d3efdea (self-caught errata, doc-only). Ownership: 85 attributes (plan's ~122 counted 39 method refs + 1 class-body attr) = 82 MOVE / 2 WIRING (the two prior-extracted browse controllers) / 1 BLOCKED (_library_pending_list_entry_media_return — shell four-member family, shared arm/disarm writers naming 4 subsystems). Suspend/resume seam ruled field-by-field: on_screen_suspend touches NO media field (methods via shim; string loop lists no media name); Timer/Worker HANDLES move per ingest/prompts precedent checked in live modules. Characterization: 79 handlers, 3 genuine gaps pinned, webbrowser.open patched on module so pins survive task-2 move; 0 on_<message> handlers (plan prediction wrong — recorded). Three paired sweeps zero real branch-unique. Flags: task-3 retarget is program's largest (1,214 BOUNDARY-MATCHED occurrences / 36 files — substring count 1,426 is WRONG and would corrupt method names); media_browse red left flagged (no controller file touched); 3 new §7 reds (2 return-settlement geometry inside the moved cluster + persona_visual, all parent-proven); near-miss lesson — a bare-# comment sweep deleted 8 unrelated lines, caught by DIFF-READING not tests, rewritten walk-upward-with-raise-guard. Dispatching task review.
Task 1 review: PASS + APPROVED — first straight approval of the program (no fix round). Reviewer reproduced RED in an isolated own-venv worktree, verified byte-for-byte for ALL 82 fields (AST + dual-tree runtime dump, 0 diffs), mutation-proved the literal-mapping test and one characterization gap, traced the suspend/resume shim path, upheld the BLOCKED ruling with the writers/docstring evidence, and confirmed ZERO unrelated deletions from the near-miss sweep. Findings: I-1 (FORWARD-BINDING on task 2) getattr census under-count 11 -> 13 attribute-naming sites (grep missed line-wrapped calls; two named misses incl. one MOVED field) — task 2 must AST-re-derive and bind 13; M-1..M-6 doc-accuracy (two in tracked files — blame-ignore 78/75 -> 77/74 and the screen-reuse comment four -> three-of-five — fixed directly by coordinator, one word each, ledgered; rest as report erratum). Ruling: no fix-round dispatch — reviewer required no code change; direct coordinator edits proportionate (two words in docs/comments) per the wave-6 precedent, erratum appended to the report.
Task 1: complete (commits 600192d6e, 76b248c05, 23e442ad9, 999bd269d, 27d3efdea + coordinator doc fix; screen 37537/1282 -> 37333/1282; 82 fields moved; zero real regressions)
Task 2 (media controller move): COMPLETE per implementer. Commits 3dd7a745a (RED 5f/6p isolated-proven) -> 9187bd281 (move) -> e97ac3a32 (blame-ignore) -> 40fe1f5bc (fix round 1: one mover reverted) -> 7fee56dd4 (blame-ignore) -> 1cf379d6c/9cc6a7ad4/e1f893b7c (docs). SPLIT DECISION: ONE controller — self.<name> graph gives a single 213-of-251 component (+28 singletons; 238 with state-field edges); browse-vs-trash candidate seam only 16 cross-edges/6 shared fields, 19 trash-named movers. 140 MOVE / 111 EXCLUDE (73 unbound-fake-self, 16 instance-attr-monkeypatch, 8 inspect.getsource, 4 ACTIVE module-globals, 3 screen-identity, 2 class-monkeypatch, 2 bypassed-construction lifecycle, 1 callback-identity, 1 shared shell helper, 1 generic dispatcher). Controller 4,461 born-governed; screen 37333 -> 34754/1282 (-2579 reconciled). I-1 honoured: AST getattr census 15 calls/13 attrs, both line-wrapped sites reproduced. THREE battery-found shapes beyond static census: (a) __new__-bypassed on_screen_suspend reaching movers via delegators; (b) NEW SHAPE — `self in widget.ancestors` membership-form screen-identity (is/is-not census scores it ZERO; 19-of-163 failures until fixed); (c) callback-identity where excluding the callee wouldn't help. Generalized replacement census: every bare-self ast.Name in moved bodies that isn't an attribute receiver — exhaustive, returns exactly 10 on the final set. Battery 117/3 (documented trio; media_browse untouched-left-flagged); full Architecture paired ZERO branch-unique; sweeps A/B/C zero real; probe order-swapped flat; byte-for-byte 140/140 text+AST. Task-3 flags: 111 exclusions stay full-bodied (imports not dead; _SURFACE exact-name UPPERCASE); library_media_state.py docstring one-phrase fix (_ANALYZE_ORIGIN_MEDIA now in screen_constants); retarget still program-largest (1,214/36/5 roots). Dispatching task review.
Task 2 review: PASS + APPROVED, no code change. Reviewer independently: reproduced RED (5f/6p isolated own-venv), byte-verified 140/140 (text+AST) + 111/111 exclusions unchanged + name-set identity, re-derived BOTH component graphs exactly (33/213 call-only; 13/238 with state edges; seam 16/6 exact), audited 13 exclusions ALL REAL (incl. the 4 zero-test-evidence ones justified source-side), verified the ADR-055 one-flag interlock guard intact, confirmed I-1's 13 getattr sites bound (11 screen + 2 controller), and ran its own paired sweep (34f/152p BOTH trees, zero unique). Findings (5, all doc): I-1 class-2 docstring claims a per-name mover-caller verification that doesn't reproduce — 7/16 have zero mover callers (exclusions still correct under §3's conservative whole-call-graph rule; the narrower stated condition is false precedent; the 7 are task-3 move candidates post-retarget); M-1 report §8 stale -3202 (true -3166, tracked files already right); M-2 trash-named 19 -> 20 (docstring self-contradicts); M-3 "exactly 10" bare-self census is pre-exclusion (final-140 = 7); M-4 a 4th ancestors-membership method exists (filed under class 2 — shape count for the recipe is 4); M-5 screen-ratchet comment "92 named dependencies" mislabels the binding-surface count (construction passes 83 kwargs; modules-ratchet comment already correct; 40fe1f5bc message carries the same mislabel — erratum-not-amend). Fix round 1 dispatched (resumed implementer, doc-only commit).
Fix round 1: COMPLETE (2325577f2, doc-only + the documented §17 same-commit controller re-pin 4461 -> 4496 for its own docstring growth). Scoped re-review: ALL ADDRESSED (9/9 — I-1 restated with the 9/7 split and an explicit false-precedent disclaimer, spot-verified zero mover callers for _request_library_media_type; both census figures paired; 4-shape count named; 83-vs-92 disambiguated IN the comment; §8 sums exactly; doc-only confirmed with the one pinned integer as the sole value change).
Task 2: complete (commits 3dd7a745a, 9187bd281, e97ac3a32, 40fe1f5bc, 7fee56dd4, 1cf379d6c, 9cc6a7ad4, e1f893b7c, 2325577f2; ONE controller ruling on 213-component evidence; 140 movers byte-for-byte; screen 37333 -> 34754/1282; controller 4496; zero real branch-unique)
