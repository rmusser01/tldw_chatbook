# SDD ledger — plan: Docs/superpowers/plans/2026-09-08-library-phase-c-media-graduation.md
Phase C (media graduation, resident canvas) SDD start — the program's founding motivation, as a designed behavior-change series (NOT pure moves; TDD applies). Branch feat/library-phase-c-resident-canvas off origin/dev (post-program-close 7e81ed55d). TASK-31880 coverage-gate fix in flight on this branch (dispatched before the plan; its closure is the media coverage-density graduation criterion).
Pre-flight conflict scan (4 tasks):
| pair | producer/consumer | finding |
| T1->T2 | mechanism decision + failing acceptance test -> implementation | consistent; T2 is TDD from T1's red test |
| T2->T3 | residency-forced ownership clarity -> handler migration scope | consistent; T3 scope-honest per spec |
| T1..T4 | probe mechanics per recipe §9 (same-location, order-swap, interleave) | consistent |
| plan vs rubric | spike code labeled throwaway; no vacuous tests mandated | clean |
Named risks: TASK-31521 composition seams (per-task rulings required); the canvas_sync/TASK-32089 dispatcher debt adjacency (map, absorb only if required, labeled commits); the probe's location artifact (§9 corrected pairing mandatory). No conflicts requiring pre-execution rulings.
TASK-31880 gate: DONE (662be3667 test fix + ca9045b3e docs close; task marked Done). VERDICT: the ASSERTION was stale — task-30043 (e34b11b23) deliberately shortened media bulk labels for the ~40-col pane; the test never followed; no width-responsive truncation exists; NO production change. Find-behind-the-find: task-28007's Analyze flip calls _library_media_analyze_reason() which the duck-typed stand-in lacked — the blanket except swallowed the AttributeError and rerouted EVERY toggle onto the fallback (2/2 proven by instrumented probe), so the test was red for one reason and non-covering for another. Fixed with the established stub + a NEW widget-identity guard (a future silent fallback fails loudly). Mutation table clean; §7 entry removed; lessons entry added. PHASE-C DESIGN INPUT (ledgered): canvas_sync's blanket except is a COVERAGE SOLVENT — four days of plausible non-coverage; Task 1 must map the swallow behavior and the mechanism decision must address it (the two working harness patterns: full receiver-surface stubs; assert-no-fallback identity guards). Media coverage-density graduation criterion now satisfied. Dispatching Task 1 (mechanism spike).
Task 1 (mechanism spike): COMPLETE per implementer. 10 commits b81cb98b0..e2be56a7c (probe 32ef88cb4; design record 721a546d9; FAILING acceptance test 018b193a9 — structurally pinned 0 recompose / ≤25 mounts / ≤25 unmounts, red today at 1/114-179/120-175, deterministic under load; corrections; battery evidence). DECISION: mechanism C (mount-once-then-toggle lazy residency), GATED on first replacing the whole-screen recompose. PREMISE OVERTURNED BY MEASUREMENT: the switch is a WHOLE-SCREEN recompose (177 mounts = full 119-node route tree; only 87 canvas, 52 RAIL — which lives INSIDE the per-route reader shell; the media/notes-reader-shell id split is Task 2's real first step). Today: block 124ms/cpu 187ms/114-179 mounts/1 recompose; mechanism floor: block 30ms/cpu 15ms/0 mounts/0 unmounts; cost +17 resident nodes (119->136). n=8x6 interleaved order-swapped from scratch worktree; swap moved nothing. FINDINGS: §25's recompose-0 column measured the WRONG CALL and removes dead for four waves (App._unregister not 8.2.8's prune path) — doctrine acceptance hook that couldn't fire, Task-4 recipe correction; resident hidden canvas is query-findable and Pressed-dispatching — off-route sync would return True and rebuild 28 invisible children with NO exception (Task 2: route-ownership guard + narrowed swallow, TASK-32089-labeled; must read test_tier1_toggle first — it pins exactly the fallback being narrowed); TASK-31521 composes cleanly (DOM-neutral, verified); redundant sync storm (2-7 rebuilds/click) real but OUT of Task-2 scope. Battery: 9 pre-existing reds name-identical both trees; media suites 24f/686p vs 18 matched — 1 flake isolated, 5 names lost to a tail -20 (honest disclosure; machine shared with 2 other sessions' pytest on shared DBs), claim rests on the structural fact (diff touches nothing under tldw_chatbook/). Scratch worktree kept for Task 2's pair. Dispatching task review.
Task 1 supersession: the implementer's control run finished post-dispatch and landed 2aa2ed0a4 — its own disclosed capture gap CLOSED (media pair now clean: same failure set both trees, 1 flake passing in isolation, shared-DB load attribution; the wrong reading left visible per the errata convention). In-flight reviewer redirected: verify 2aa2ed0a4 rather than re-run the pair; verdict scope extended to b81cb98b0..2aa2ed0a4.
Task 1 final closeout received (same substance as the supersession; acceptance test re-verified red; cleanup done). The "two unattributed pytest processes" it declined to kill are almost certainly the in-flight task reviewer's own runs — correctly left alone. Awaiting the design review's verdict on b81cb98b0..2aa2ed0a4.
Task 1 review: SPEC MET + FIX REQUIRED (1 Critical + 6 Minor). Reviewer reproduced EVERY structural measurement exactly (all mount/unmount/region/class tables, node counts, the 177=119+2x29 arithmetic, the sync storm, all Textual semantics claims incl. B-works-against-expectation honesty); confirmed the §25 instrument bug THREE WAYS (App._unregister zero callers anywhere; Widget.recompose bypasses refresh so the probe's hook can't see it; the published baseline's recompose-0 on switch rows is WRONG for 3 of 8 rows — 8 waves of evidence couldn't have caught a new rail-switch recompose); verified the shell-split precondition in compose (rail is the shell's first positional child; the replace guard trips on every media<->notes switch); closed the 3-file control gap itself (zero branch-unique, one direction-reversed control-only failure); audited C-over-A/B as SOUND (decided on architecture not 1ms noise; B's working result reported rather than suppressed). CRITICAL: the acceptance pin passes VACUOUSLY on a no-op switch (proven: rail-selection no-op -> green pin, dead UI — the exact replaced-flag bug shape Task 2 risks) — fix: positive assertions (destination canvas present+displayed, selected_row_id moved) + strict xfail; mutation-verify with the reviewer's exact patch. Minors: tier1_toggle pins a DIFFERENT function's except (phantom-conflict risk for Task 2); 2 equality-comparison canvas-id sites missing from the census scope; chrome wording; control-coverage erratum; the recompose-column blindness is PATH-specific (narrow per the wrong-reason rule); a sum line. Task-2 note recorded: the ≤25 ceiling derives from a floor measured without the shell split — if tight, re-derive, never quietly raise. Fix round dispatched.
Fix round 1: FIXED (98ad1e0c3 liveness+strict-xfail, 2c8922113 minors, db2b179f5 erratum). Scoped re-review: GAPS REMAIN -> closed by coordinator. 5/6 checks clean with full reproduction (the no-op mutation re-run out-of-process: RED on six liveness counts with mounts 0/0 — exactly what previously passed; strict-XPASS behavior verified with a synthetic test; both equality-site citations live; the census-scope lesson landed). The one gap: the ceiling-provenance note reached the test docstring but NOT the spec's Acceptance-evidence section (2c8922113 touched the spec for other minors and missed it) — closed by coordinator direct edit (short paragraph after the ceiling rationale, cross-referencing the docstring, with the re-derive-never-quietly-raise instruction). Wave-6 precedent for proportionate direct edits; ledgered.
Task 1: complete (commits b81cb98b0..db2b179f5 + coordinator spec paragraph; DECISION mechanism C gated on replacing the whole-screen recompose; failing acceptance test strict-xfail with liveness assertions, mutation-proven both ways; the §25 instrument bug triple-confirmed for Task 4; zero production files touched)
Task 2 (resident canvas): COMPLETE WITH NAMED SHORTFALL per implementer. Commits 7b4a7e289 (mechanism: ONE LibraryBrowseReaderShell with route-neutral #library-browse-reader-shell + marker classes .library-media-route/.library-notes-route with apply_route as single writer — preserving ~35 route-proxy sites as a #->. rewrite; acceptance flip; re-pin 32263->32242) -> 51533602c (TASK-32089: route-ownership guard, hidden-canvas event gate, suspend/resume pin) -> cb1a47ce2 (3 census defects the suites caught) -> d4574d907 (design addendum + invariant test + user-guide re-stamp) -> 59029290b (inventory, rows read) -> a42aa094c (the ONE legitimate expectation change, flagged) -> 7b8df2dc7 (report). Acceptance: notes 1/114/120 -> 0/26/2; media-back 1/179/175 -> 0/81/86, liveness green. STRUCTURAL CAUSE DEAD; FREEZE NOT YET: block 98->98ms media-back (cpu 216->150), because the SYNC STORM owns 100% of the remaining block — 88ms/58% is CSS restyle from the 81 mounts of TWO destination repaints (5 sync calls coalesce to exactly 2 rebuilds from genuinely different states: retained list then worker result; equality short-circuit measured useless; removing any of the first 3 calls saves nothing). Ceiling re-derived 25->90/95 UNDER THE PROVENANCE CLAUSE (the record's "pin doesn't depend on the storm" was wrong — honest invocation of the rule built for exactly this). Blanket-except narrowing BLOCKED by test_strict_failure_retry (requires the swallow for retry, attempts==2) — REVERTED not forced, reasoning at the code site. Suites: 3 differing names all individually dispositioned; shell 40F/3P identical both arms; ratchets improved. PLAN AMENDMENT PENDING (my ruling, after review): the motivating change needs a storm task — Task 2.5 — since the plan's GOAL is the felt freeze, not the recompose. Dispatching task review with the shortfall + re-derivation + blocked ruling + expectation change as named audit targets.
Task 2 review: SPEC MET + FIX REQUIRED (1 Important + 8 Minor; NO production defect — reviewer could not break the landed code: liveness mutation still reds six ways, event-gate mutation reds the hidden-canvas pin, the BLOCKED-narrowing ruling re-proven by re-applying the narrow except and watching the retry test red). All headline counts exact (0/26/2, 0/81/86, 35-site census with 0 live retired-id references, single-writer marker claim verified, old behavior fails the re-derived pin on every column both directions). IMPORTANT: the remaining-cost attribution is WRONG in three landed documents — measured THREE rebuilds per switch (incl. the OUTGOING canvas ~16ms and a STILL-HIDDEN first rebuild, 24/62 mounts — the cheapest lead), and apply-count is NOT mount-proportional (459 applies on a 26-mount/zero-rebuild switch vs 405 on the 81-mount arm — full-tree restyle is substantially rebuild-independent); "storm owns 100% of the block" unsupported (58% CPU ≠ 100% block). SHORTFALL AUDIT: sound in core (5->2 coalescing confirmed directly; both negative results corroborated; 405-429 applies / 83-101ms restyle reproduced), unsound in the causal claim — the AMENDMENT IS JUSTIFIED but must NOT copy this record's scope: the storm task OPENS with attribution measurement (apply-calls by trigger: mount vs full-tree) and scopes explicitly over the outgoing-canvas repaint + the pre-display hidden repaint. Minors: probe buckets on a retired id (Task 4 reuses it); a mechanically-falsified comment; gate-docstring overclaim; a SECOND deleted test unaccounted (the 710->709); a now-vacuous CSS test; a same-branch ratchet raise disclosed only in a commit body; no false-refusal-direction test on the fail-closed guard; census nits. Fix round dispatched.
Fix round 1: FIX COMPLETE (5fc35f1fa — attribution corrected in all three documents with the storm-lead list + opens-with-measurement instruction; probe dual-generation bucketing; the new refusal-direction test mutation-verified; the vacuous CSS test retargeted to live ids; all disclosures in the report body; implementer self-caught another add -A sweep of progress.md and amended it out). Scoped re-review: GAPS REMAIN -> closed by coordinator. 6.5/7 checks confirmed with live reproduction (guard-flip mutation re-run; 1259-methods red measured at THREE reference points — genuinely dev's, identical at base/merge-base/task-base; the tier1_toggle incidental red re-proven pre-existing in an isolated worktree). The one residual: the Notes canvas gate docstring copy-pasted the Media file's LibraryMediaRowGeometryChanged claim (no Notes analogue exists — zero Notes-side posters by grep) — closed by coordinator direct edit (docstring-only; states the absence explicitly and widens the future-change caveat; import verified).
Task 2: complete (commits 7b4a7e289..5fc35f1fa + coordinator docstring fix; STRUCTURAL CAUSE OF THE FOUNDING FREEZE ELIMINATED — 0 whole-screen recomposes, mounts 177->81/26; felt freeze NOT yet — the corrected record attributes the remaining ~98ms to three rebuilds incl. an outgoing-canvas and a still-hidden repaint plus substantially rebuild-independent restyle; ceiling honestly re-derived 25->90/95 under the provenance clause; both hazard guards + suspend/resume pinned; screen 32263->32242)
PLAN AMENDMENT (coordinator ruling, gated on the review's "amendment justified" verdict): Task 2.5 — THE SYNC STORM — added to the plan: opens with the attribution measurement (apply-calls by trigger: mount-proportional vs full-tree restyle), then scopes explicitly over (1) the outgoing-canvas rebuild, (2) the pre-display hidden rebuild, (3) whatever the attribution shows of the restyle split; acceptance = the felt-freeze block-time evidence the plan's Goal names, plus structural pins where measurable. Task 3 (region ownership) follows it; Task 4 closes.
Task 2.5 (sync storm): COMPLETE per implementer — THE PLAN'S GOAL IS MET: felt freeze roughly HALVED, non-overlapping ranges (media 97->55ms [95-107 vs 51-65]; notes 148->73ms [140-152 vs 68-78]; cpu -42/-50%; applies 423->135, 463->64). Commits 497bc5f8e..d76edf717. The MANDATED attribution table (new library_restyle_attribution_probe.py, applies by trigger + first out-of-textual frame) OVERTURNED the plan's lead ordering — followed per instruction: apply_route's own two set_class flips were the top cost on ALL THREE arms (41-62% of applies — restyling the shell's whole 96-119-node subtree for marker classes NO stylesheet rule references; Task 2's own mechanism was the largest waste), then the sync_layout disabled flip (44% on first-visit), with the plan's mount-side leads at just 6-22%. Four fixes in table order (marker flips update=False -> outgoing rebuild -> hidden pre-display rebuild -> placeholder layout), each red-first with the measured number in its docstring, each with a structural pin; the two known-useless moves not repeated. render-once-when-worker-lands MEASURED as buying nothing and stranding a worker result — decided, recorded. Concerns: update=False contract guarded by a stylesheet-scan test (CSS for a marker class reds it); residency ids spelled in two modules (pinned equal; a third canvas touches both); a refused sync drops its then= follow-up (established guard shape, notes-reader failure set byte-identical); full shell suite not run (~2h/arm); remaining cost = the destination canvas's own single repaint. Dispatching task review.
Task 2.5 review: SPEC MET + APPROVED (6 Minor, no Critical/Important). THE FOUNDING CLAIM REPRODUCED by the reviewer's own §9 interleaved pair: media 96.5->57.5ms (93-98 vs 53-80), notes 147.5->75 (139-152 vs 69-77) — non-overlapping both arms; attribution table digit-for-digit on the notes rows; the no-CSS-references premise verified against tcss + DEFAULT_CSS + the parsed sheet (zero rules; all hits are Python selectors); update=False semantics verified against Textual 8.2.8 source (classes still mutate, queries unaffected — sound iff no rule matches) with the stylesheet-scan guard redding under THREE mutation shapes; all four fix pins mutation-verified (the hidden-rebuild revert reds with exactly the docstring's "5 canvas repaint(s)"); Task-1's liveness no-op still reds six ways; ceilings 69/74 follow the same x1.11 formula as 90/95; ZERO branch-unique failures anywhere incl. the reviewer's own 64-test both-arms selection over the disclosed shell gap. Minors: a missing TASK-32089 marker line; an only-writer overclaim; the option-A spike below the §9 bar (reviewer re-ran it properly — conclusion holds, now evidenced); two table-label disclosures (nested component-class applies; the four-flips-vs-two label); one omitted suite row. Fix round dispatched (doc-only).
Fix round 1: COMPLETE (4645e4c8c, doc-only, each item re-verified against code or fresh measurement — incl. re-measuring the nested-applies split at the base and correcting the column arithmetic 238->240/192->194/206->208 with the same correction carried into the pin docstrings). Coordinator spot-check in lieu of a re-review agent (proportionality: prescriptive doc edits with the reviewer's own numbers): both comment fixes verified live (TASK-32089 marker at :405; "hidden and alive" at :420), 6 phase-C pins green, preflight green per the implementer's run. Ruling ledgered.
Task 2.5: complete (commits 497bc5f8e..4645e4c8c; THE PLAN'S GOAL MET — felt freeze halved with non-overlapping ranges, reviewer-reproduced; the attribution table overturned and replaced the plan's lead ordering; four fixes red-first with mutation-verified structural pins; screen 32242->32241)
