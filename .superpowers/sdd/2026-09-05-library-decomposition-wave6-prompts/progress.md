# SDD ledger — plan: Docs/superpowers/plans/2026-09-05-library-decomposition-wave6-prompts.md
Wave-6 (prompts) SDD start. Branch refactor/library-decomp-wave6-prompts off origin/dev (post-wave-5 merge 7aa048790). Worktree: .worktrees/library-decomp-foundation (same venv).
Pre-flight conflict scan (plan is 4 tasks, same shape as waves 2-5; table):
| pair | producer/consumer | finding |
| T1->T2 | LibraryPromptsState fields + shims -> controller bindings | consistent; T2 consumes T1's state accessor naming per ingest template |
| T2->T3 | delegators + bindings -> retargets/prune | consistent; prune only at zero consumers per census |
| T1..T4 self-consistency | each task names its own battery; close names all seven wiring suites | consistent with the guard set now on dev (preimport-closure + ui_ready census added per round-3) |
| plan vs rubric | no mandated vacuous tests, no verbatim-logic duplication mandates | clean |
Known wave-specific risks (from plan Global Constraints): born-lazy controller import (preimport-closure guard), library_prompts_state basename collision (package-qualified greps), two prefix families, 4 extra prompts test roots. No conflicts requiring pre-execution rulings.
Task 1 (prompts state PR): DONE per implementer. Commits a41cbe8c6 (RED/pins: state module + wiring test + characterization, screen untouched) -> f59db7c94 (pure move: 43 fields, shims, bypass seed in test_library_screen_reuse.py, re-pin 41393/1321 -> 41359/1321) -> 1e3e0b6ef (blame-ignore, rev-parse'd). Ownership 46 fields: 43 MOVE / 3 WIRING (the three live prompt controller instances) / 0 BLOCKED; THREE prefix families (31 _library_prompt_ / 11 _library_prompts_ / 1 bare _selected_prompt_id) via one prompt_state_shim_attr(). Two cross-subsystem calls resolved by body-reading (mutation_in_flight nav-guard precedent; reader_layout generic-dispatcher precedent). RED at parent proven 2f/3p -> 5p. Characterization: 40/44 covered, 4 new pins. Byte-for-byte: 23 comment lines mechanical + 43-value runtime dump identical under PYTHONHASHSEED=0. Battery green incl. preimport-closure + ui_ready census; paired baseline on 5 prompt files: run-2 exact failure-set match, zero real. Flags for later tasks: 17 undocumented pre-existing prompt-file dev reds (wave close adds to §7); 2 dead imports + 14 quoted-string sites incl. 2 receiver-fixes (task 3); unbound SimpleNamespace call on handle_library_prompts_sort_choice in test_library_choice_strips.py:605 (task-2 exclusion candidate). Dispatching task review.
Task 1 review: FIX REQUIRED (doc-only; spec 6/7 constraints pass, count-accuracy fails). Reviewer independently re-derived 27/30 counts exact and verified all mechanisms (RED-at-parent reproduced; bare-name shim round-trips with setter winning over instance dict; byte-for-byte via AST + runtime dump; both cross-subsystem rulings READ and upheld — mutation_in_flight 4-writers-all-prompt/93-readers, reader_layout dispatcher precedent). Findings: IMPORTANT — "4 genuine gaps" is false, Discard was covered (test_library_prompt_compatibility_editor_discard_returns_to_current_list, proven by mutation); real split 41/3; pin kept (adds detail/block-state/DB assertions). MINOR — state-module docstring arithmetic (39+2 vs correct 37+2); commit-message accounting doesn't sum (ruling: NOT amended — f59db7c94 is load-bearing in blame-ignore; erratum to report instead); tautological mapping assertion inherited from skills precedent (adopting reviewer's literal-assertion hardening, one per family); §11 hand-off under-scoped (19 fake-self sites across 3 files, not 1 — expanded for task 2). Fix round 1 dispatched (resumed implementer).
Fix round 1: DONE (4ada1c55d, doc/test-hardening only). Scoped re-review: ALL ADDRESSED (5/5, mutation spot-check reproduced: literal-assertion test is now the sole failure under the mapping mutation; scope clean — no production logic, no ratchet numbers). Implementer's own re-derivation corrected one relayed count (canvas_scoped_sync fixture: 6 moved fields + 2 prompt-adjacent, not 8 moved) and found 15 additional pre-existing Architecture reds (timer-path/(file,line)-keyed inventories) proven name-for-name identical at base — fold into recipe §7 at wave close alongside the 17 prompts-file reds.
Task 1: complete (commits a41cbe8c6, f59db7c94, 1e3e0b6ef, 4ada1c55d; wiring 6 + characterization 4 green; screen 41393/1321 -> 41359/1321)
Task 2 interruption: implementer killed mid-verification by API session limit ("While the sweep runs, drafting the report" was its last line). State verified by controller before resume: all 3 commits present (8ae30f490 RED -> d0ec95b16 move -> 926b0431c blame-ignore), worktree CLEAN, controller file 216KB, report draft 40KB. Resumed with state-verification instructions: re-run any verification lacking recorded evidence (the in-flight sweep is gone), finish battery, finalize report, standard contract. Established resume protocol (wave-5 precedent): trust the ledger and git over agent memory.
Task 2 (controller move): COMPLETE per implementer (post-resume; verification re-run foreground). Commits 8ae30f490 (RED, proven) -> d0ec95b16 (139 movers/22 exclusions, single controller by connected-components: one 145-blob + 16 singletons, no seam) -> 926b0431c (blame-ignore) -> 52268ea42 (comment fix) -> 9a9fa02a2 (report force-add). Screen 41359/1321 -> 37722/1321; controller born-governed 4956. Byte-for-byte 139/139 by raw text AND normalized AST; 22 exclusions byte-identical. NEW guard surface: constructor-binding pins (42 names) — added against the skills-era unbound-getattr regression class. Census depth finding: 4 exclusions invisible to call-grep (class-attr harness, parametrize tuple of unbound functions, getattr(LibraryScreen, "<name>") string dispatch) — found only by non-call-reference + quoted-string census. Interaction trap: static-method class-forwarding delegator needs its own function-local import under born-lazy (NameError x8 caught by battery pre-commit). Battery 108/3 (the authorized trio); Tests/Architecture 18 reds diff-identical at parent; preflight green.
Concerns held for review: (1) full Tests/UI -k library paired sweep DNF (50-min alarm under load 20-52 + foreign pytest processes); four paired baselines with transcripts substituted, residual gap = test_library_shell.py -k "note" (254 tests) — REVIEWER runs it (Notes cluster is relevant surface: mutation_in_flight's 3 Notes readers); (2) ui_ready census 974-vs-972 BOTH trees under load — dev debt, flappy; (3) test_library_modal_dismissal.py pre-red (skills-era, dies pre-comparison), 4 more stale edges added, coherent repair filed to task-3/close. Dispatching task review.
Task 2 review: FIX REQUIRED (doc-only). Mechanism verification the strongest of the program: reviewer's OWN comparator confirmed 139/139 byte-for-byte (raw text + normalized AST, full population not sample), all 22 exclusions intact, binding surface re-derived independently and set-identical (42), components reproduced exactly (17: [145 + 16x1], no seam), RED-at-parent reproduced (5f/7p same names), binding pin mutation-tested load-bearing, born-lazy verified incl. the static-delegator local import. SWEEP GAP CLOSED by reviewer: paired -k "note" slice (xdist -n8 worksteal; isolated worktree + own uv venv at parent — correctly rejected the venv-reuse shortcut for the editable-finder reason) = 172 shared / 1 branch-unique passing 3/3 isolated on BOTH trees / 0 baseline-unique; caveats recorded (ambient load 35-45 saturates 68% of slice on both trees; note tests exercise construction path not moved bodies). Findings: I1 stale 37718 in ratchet COMMENT :503 (row correct; report's "never shipped" claim false); I2 modal-dismissal census deferral accepted but must be a titled TASK-3 MUST (4 stale rows + _OwnerScope gap); M1-M8 count slips (five not four grep-invisible names — my own earlier ledger entry repeated "4", corrected here; 7-not-11 movers among canvas forwarders; 41-not-33 sites; 2+11+5 string census; LATENT evidence rewrite — verdict upheld via per-test monkeypatch scoping; 35-not-38 files; stash-overlay process deviation to record). Fix round 1 dispatched (resumed implementer).
Fix round 1: FIXES COMPLETE (31b7d4754, doc-only; controller row re-pinned 4956 -> 4991 same-commit for its own docstring growth per §17). Rulings on the two residual count disputes (definitional, verdict-immaterial — file sets identical under every reading): (E5) _sync_library_canvas sites = 33 STANDS under the ingest-series standing definition (dedup by file+line), which is the program precedent; erratum records the per-shape sum 42 and the widest reading 70/21 with methods. (E10) module-object importers = 38 ADOPTED (36 alias-binding + 2 bare-dotted, derivation shown); reviewer's 35 had no stated definition. Cost if wrong: a count in an erratum table, self-describing. New lessons flagged for wave close: (a) a ratchet re-pin has THREE places a number lives (row, arithmetic, prose) and a passing guard proves only the row; (b) FOURTH patch spelling found — patch-target TABLE rows (screen, "<name>", key) consumed by a distant setattr, invisible to setattr-anchored regexes (test_library_shell.py:5146) — recipe census shape candidate; (c) the unwritten short-foreground carve-out from the isolated-worktree rule has now been leaned on twice and bitten once — §3 must write it down with a bound or drop it; (d) modal-dismissal inventory repair is CROSS-WAVE (ingest + skills rows stale for the same reason) — task-3 MUST owns the prompts rows, wave close owns filing the cross-wave repair task.
Scoped re-review: GAPS REMAIN -> closed by coordinator. 5/6 checks PASS (ratchet comment 37722; controller row 4991 == wc -l exact, re-pin the sole non-comment line in the commit as documented; docstrings say FIVE; errata complete with both disputed figures + methods; controller imports). The one gap was FRESH: the fix round's "re-verified" _SUPPORTED_OWNER_SCOPES range (:518-524) was itself wrong — the ORIGINAL reviewer's :520-526 was correct (verified against the live file by re-reviewer AND coordinator independently). One-line report correction applied directly by coordinator (report is git-ignored SDD evidence; no code touched; burning a fix round on one line in a lead-list note is waste). Lesson appended to the report line itself: the number-verification discipline applies to LINE RANGES too — a "re-verified" claim that overrides a correct value is worse than no claim.
Task 2: complete (commits 8ae30f490, d0ec95b16, 926b0431c, 52268ea42, 9a9fa02a2, 31b7d4754; screen 41359/1321 -> 37722/1321; controller born-governed 4991; 139 movers byte-for-byte; zero real branch-unique across all paired baselines incl. the reviewer-closed note slice)
Task 3 (cleanup): DONE per implementer. Commits 6fd2b753a (cleanup: shim block deleted, 128 screen + 465 test retargets, 4 strings dotted + 2 receiver fixes, 13 SimpleNamespace + 1 class-attr harness restructured, 39/139 delegators pruned ~28% [in range], 25 dead imports, modal-inventory 4 rows repointed behind new _OwnerScope, recipe §8 row + §21) -> d838b7d63 (blame-ignore) -> 770a47736 (report + 2 lessons). Pins 37722/1321 -> 37574/1282 (delta -148 reconciled term-by-term; method drop 39 = prunes exactly); controller 4991 -> 4998. Battery green except the documented trio; preflight green; paired baselines ISOLATED-worktree+venv: zero real branch-unique (5-file set 24F/404P vs 29F/399P baseline — noisy machine, every diff isolated-rerun-proven at parent; shell -k prompt 1F/10P identical). Flags: (1) NEW HAZARD CLASS — on_prompt_block_editor_* x6 are Textual NAME-dispatched: zero references, prune would silently unhook and stay green; transform whitelist needs a third member and recipe §4 says only @on/action_* — task-4 MUST fold into §4; (2) _SURFACE check must be EXACT-NAME (lowercase grep "prompt" returned 0 while five UPPERCASE-PROMPT names were pinned); (3) modal-dismissal retarget proven by construction (4=4 exact incl. modal types), file stays 1-red on the skills-era blocker identically both trees; (4) machine noise floor high — dismissals rest on isolated reruns not count comparisons, task 4 budget likewise; (5) two out-of-scope prose mentions forward-noted per skills precedent. Dispatching task review.
Task 3 review: SPEC-COMPLIANT + FIX REQUIRED (1 Important + 4 Minor, all doc-accuracy; zero functional defects). Reviewer independently: reclassified ALL 139 names (44 @on + 1 action_* + 6 on_<msg> + 49 external + 39 pruned = 139 exact), verified the 6 name-dispatched handlers unpruned against Textual's own dispatch source (message.py:86, message_pump.py:743-758), re-ran the modal construction proof (4/4/0/0 EXACT), reproduced -148 term-by-term (39x3 - 29 - 10 + 6 + 2) and the lowercase-_SURFACE trap, and confirmed canon (controller hunks docstring-only; the 3 screen text edits are non-movers by AST). Findings: I1 commit-message pin wrong (37576/-146 vs landed 37574/-148) — RULING: no amend (hash blame-ignore-load-bearing, wave-5 precedent), titled erratum + PR-body surfacing; M1 report row presents ui_ready red as green ("quiet process" claim not reproducible); M2 25-consumers mischaracterized (14/25 console_*); M3 recipe §21 prune chain mis-sorted (28.06 before 28.57); M4 ONE stale FIELD-name prose repo-wide (export controller :375) — root cause: sweep censused pruned method names but never deleted field names; field-name prose sweep is a census-shape candidate for wave close. Fix round 1 dispatched (resumed implementer).
Fix round 1 (post-interruption re-execution): COMPLETE (d010b65a1, doc-only). Scoped re-review: ALL ADDRESSED (8/8 — blame-ignore pin line verified; no "green when quiet" claim survives; 14+11=25 module split arithmetic checked; recipe §21 all seven fractions recomputed and ordering exact; export-controller edit line-neutral at 1307 inside docstring 301-417 with live wiring cross-checked; both further prose hits verified byte-for-byte; doc-only confirmed; guards 50/1 with only the documented red). Implementer discipline notes: M4 written line-neutral after the first draft would have tripped the export controller's own pin; ran the field-name prose census itself rather than accept "only hit" — 2 further present-tense hits disclosed to wave close.
Task 3: complete (commits 6fd2b753a, d838b7d63, 770a47736, d010b65a1; screen 37722/1321 -> 37574/1282; controller 4998; 39/139 pruned; zero real branch-unique)
Task 4 (wave close): DONE. Commits 33cffc4a4 (stale-prose sweep, 6 line-neutral) -> 3cf89dbe9 (TASK-31815 modal-inventory/BLOCKED-guard repair + TASK-31816 ui_ready breach ownership) -> a595347f3 (recipe close §3/§4/§6/§7/§8/§9/§20/§21) -> 12749306d + af3a96c5a (durable evidence) -> d33ea61c7 (heredoc lesson). Close battery 301/4 (all four documented); sweep A (Tests/UI -k library, isolated baseline) 10 branch-unique ALL resolved none-Prompts; sweep B (Tests/Library + Tests/Architecture) 22F both trees name-identical, ZERO branch-unique; preflight green; pins exact 37574/1282 + 4998; probe order-swapped pair in band (round-1 false alarm caught by the swap — §9 now requires it).
Rulings audited and ACCEPTED: (1) §3 carve-out DROPPED not bounded — implementer MEASURED the cost being avoided (5s provisioning, 528MB) and the carve-out bought nothing; better-grounded than my brief's bound-or-drop framing. (2) Sweep-scope deviation from brief text — my brief called three full roots "the established scope" but they collect 23,898 tests vs §7's actual 4,477; implementer covered the intent in ~72min with per-name dispositions; final review to audit. New methodology now recipe-recorded: 10-run matched batches for timing-sensitive dispositions (the 6-for-6-then-5/10 near-miss); probe order swap. TASK-31816 self-corrected mid-flight (ADR-097 ratchet never rises; breach is +2 vs ±1 wobble — real, dev-owned). Modal-dismissal reframed: BLOCKED guard proving nothing, not a 1-red — five waves misread it; TASK-31815 owns.
ALL WAVE-6 TASKS COMPLETE — dispatching final whole-branch review (fable).
